import * as path from "node:path";
import * as fs from "node:fs";
import { parseSync } from "oxc-parser";
import { analyze } from "eslint-scope";
import * as eslintVisitorKeys from "eslint-visitor-keys";
//#region src/plugin/utils/is-testlike-filename.ts
const NON_PRODUCTION_PATH_SEGMENTS = [
	"/test/",
	"/tests/",
	"/__tests__/",
	"/__test__/",
	"/__fixtures__/",
	"/fixtures/",
	"/__mocks__/",
	"/mocks/",
	"/cypress/",
	"/.storybook/",
	"/stories/",
	"/__stories__/",
	"/playground/",
	"/playgrounds/",
	"/examples/",
	"/example/",
	"/demo/",
	"/demos/",
	"/sandbox/",
	"/sandboxes/",
	"/e2e/",
	"/e2e-tests/",
	"/specs/",
	"/spec/",
	"/integration-tests/",
	"/integration/",
	"/it/",
	"/benchmarks/",
	"/benchmark/",
	"/__benchmarks__/",
	"/perf/",
	"/perf-tests/",
	"/scripts/",
	"/cli/",
	"/bin/",
	"/tooling/",
	"/tools/",
	"/codemods/",
	"/codemod/",
	"/migrations/",
	"/migration/",
	"/generators/",
	"/generator/",
	"/runbooks/",
	"/devtools/",
	"/internal-tools/",
	"/seeds/",
	"/seed/",
	"/dev-seeder/"
];
const NON_PRODUCTION_FILENAME_SUFFIXES = [
	".test.",
	".spec.",
	".cy.",
	".stories.",
	".story.",
	".bench.",
	".benchmark.",
	".e2e.",
	".integration-spec.",
	".int-spec.",
	".mock.",
	".fixture."
];
const NON_PRODUCTION_BASENAMES = new Set([
	"setuptests.js",
	"setuptests.ts",
	"setuptests.jsx",
	"setuptests.tsx",
	"setupvitest.js",
	"setupvitest.ts",
	"setupvitest.jsx",
	"setupvitest.tsx",
	"setupjest.js",
	"setupjest.ts",
	"vitest.setup.js",
	"vitest.setup.ts",
	"vitest.setup.mjs",
	"vitest.config.ts",
	"vitest.config.js",
	"vitest.config.mts",
	"vitest.config.mjs",
	"jest.setup.js",
	"jest.setup.ts",
	"jest.setup.jsx",
	"jest.setup.tsx",
	"jest.config.js",
	"jest.config.ts",
	"jest.config.mjs",
	"playwright.config.ts",
	"playwright.config.js",
	"cypress.config.ts",
	"cypress.config.js",
	"karma.conf.js",
	"karma.conf.ts",
	"vite.config.ts",
	"vite.config.js",
	"vite.config.mts",
	"vite.config.mjs",
	"webpack.config.ts",
	"webpack.config.js",
	"webpack.config.mjs",
	"rollup.config.ts",
	"rollup.config.js",
	"rollup.config.mjs",
	"esbuild.config.ts",
	"esbuild.config.js",
	"esbuild.config.mjs",
	"tsup.config.ts",
	"tsup.config.js",
	"tsup.config.mjs",
	"rsbuild.config.ts",
	"rsbuild.config.js",
	"rspack.config.ts",
	"rspack.config.js",
	"next.config.ts",
	"next.config.js",
	"next.config.mjs",
	"remix.config.js",
	"remix.config.ts",
	"astro.config.ts",
	"astro.config.js",
	"astro.config.mjs",
	"tailwind.config.ts",
	"tailwind.config.js",
	"tailwind.config.mjs",
	"postcss.config.ts",
	"postcss.config.js",
	"postcss.config.mjs",
	"biome.config.ts",
	"biome.config.js",
	"drizzle.config.ts",
	"drizzle.config.js",
	"prisma.config.ts",
	"prisma.config.js",
	"knip.config.ts",
	"knip.config.js",
	"knip.config.mjs",
	"lint-staged.config.js",
	"lint-staged.config.mjs"
]);
const SOURCE_ROOT_SEGMENTS = [
	"/src/",
	"/app/",
	"/lib/",
	"/components/",
	"/pages/",
	"/features/",
	"/modules/",
	"/packages/",
	"/apps/",
	"/frontend/",
	"/client/"
];
const sliceBelowSourceRoot = (filename) => {
	let cutAt = -1;
	for (const segment of SOURCE_ROOT_SEGMENTS) {
		const lastIdx = filename.lastIndexOf(segment);
		if (lastIdx > cutAt) cutAt = lastIdx;
	}
	if (cutAt < 0) return filename;
	return filename.slice(cutAt);
};
const isTestlikeFilename = (rawFilename) => {
	if (!rawFilename) return false;
	const filename = rawFilename.replaceAll("\\", "/");
	const lastSlash = filename.lastIndexOf("/");
	const basename = lastSlash === -1 ? filename : filename.slice(lastSlash + 1);
	if (NON_PRODUCTION_BASENAMES.has(basename.toLowerCase())) return true;
	for (const suffix of NON_PRODUCTION_FILENAME_SUFFIXES) if (basename.includes(suffix)) return true;
	const scopedFilename = sliceBelowSourceRoot(filename);
	for (const segment of NON_PRODUCTION_PATH_SEGMENTS) if (scopedFilename.includes(segment)) return true;
	return false;
};
//#endregion
//#region src/plugin/utils/has-type-property.ts
const hasTypeProperty = (value) => Boolean(value && typeof value === "object" && "type" in value);
//#endregion
//#region src/plugin/utils/is-node-of-type.ts
const isNodeOfType = (node, type) => Boolean(hasTypeProperty(node) && node.type === type);
//#endregion
//#region src/plugin/utils/non-react-jsx-dialect.ts
const NON_REACT_JSX_DIALECT_PACKAGES = new Set([
	"solid-js",
	"solid-js/web",
	"solid-js/store",
	"solid-js/h",
	"solid-js/html",
	"@builder.io/qwik",
	"@builder.io/qwik-city",
	"@builder.io/qwik-react",
	"voby",
	"vidode"
]);
const NON_REACT_JSX_DIALECT_PACKAGE_PREFIXES = ["solid-js", "@builder.io/qwik"];
const startsWithAny = (source, prefixes) => prefixes.some((prefix) => source === prefix || source.startsWith(`${prefix}/`));
const fileImportsNonReactJsxDialect = (program) => {
	for (const statement of program.body) {
		if (!isNodeOfType(statement, "ImportDeclaration")) continue;
		const source = statement.source;
		const value = source && typeof source.value === "string" ? source.value : null;
		if (!value) continue;
		if (NON_REACT_JSX_DIALECT_PACKAGES.has(value)) return true;
		if (startsWithAny(value, NON_REACT_JSX_DIALECT_PACKAGE_PREFIXES)) return true;
	}
	return false;
};
const jsxAttributeIsNonReactDialectMarker = (openingNode) => {
	for (const attribute of openingNode.attributes) {
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
		if (attribute.name.name === "classList") return true;
		if (attribute.name.name.startsWith("class:") || attribute.name.name.startsWith("bind:")) return true;
	}
	return false;
};
//#endregion
//#region src/plugin/utils/define-rule.ts
const wrapCreateForTestNoise = (create) => ((context) => {
	if (isTestlikeFilename(context.filename)) return {};
	return create(context);
});
const VISITOR_NODE_NAME_PATTERN = /^[A-Z]/;
const wrapCreateForReactJsxOnly = (create) => ((context) => {
	const innerVisitors = create(context);
	let fileIsNonReactJsx = false;
	const wrappedVisitors = {};
	for (const [key, visitor] of Object.entries(innerVisitors)) {
		if (typeof visitor !== "function") {
			wrappedVisitors[key] = visitor;
			continue;
		}
		if (!VISITOR_NODE_NAME_PATTERN.test(key)) {
			wrappedVisitors[key] = visitor;
			continue;
		}
		if (key === "Program") {
			wrappedVisitors.Program = (node) => {
				fileIsNonReactJsx = fileImportsNonReactJsxDialect(node);
				visitor(node);
			};
			continue;
		}
		if (key === "JSXOpeningElement") {
			wrappedVisitors.JSXOpeningElement = (node) => {
				if (!fileIsNonReactJsx && jsxAttributeIsNonReactDialectMarker(node)) fileIsNonReactJsx = true;
				if (fileIsNonReactJsx) return;
				visitor(node);
			};
			continue;
		}
		wrappedVisitors[key] = (...args) => {
			if (fileIsNonReactJsx) return;
			visitor(...args);
		};
	}
	if (!("Program" in wrappedVisitors)) wrappedVisitors.Program = (node) => {
		fileIsNonReactJsx = fileImportsNonReactJsxDialect(node);
	};
	return wrappedVisitors;
});
const defineRule = (rule) => {
	const tags = rule.tags;
	const create = rule.create;
	if (typeof create !== "function") return rule;
	let wrappedCreate = create;
	if (tags?.includes("test-noise") && !tags?.includes("migration-hint")) wrappedCreate = wrapCreateForTestNoise(wrappedCreate);
	if (tags?.includes("react-jsx-only")) wrappedCreate = wrapCreateForReactJsxOnly(wrappedCreate);
	if (wrappedCreate === create) return rule;
	return {
		...rule,
		create: wrappedCreate
	};
};
//#endregion
//#region src/plugin/constants/library.ts
const HEAVY_LIBRARIES = new Set([
	"@monaco-editor/react",
	"monaco-editor",
	"recharts",
	"@react-pdf/renderer",
	"react-quill",
	"@codemirror/view",
	"@codemirror/state",
	"chart.js",
	"react-chartjs-2",
	"@toast-ui/editor",
	"draft-js"
]);
const FETCH_CALLEE_NAMES = new Set([
	"fetch",
	"ky",
	"got",
	"wretch",
	"ofetch"
]);
const FETCH_MEMBER_OBJECTS = new Set([
	"axios",
	"ky",
	"got",
	"ofetch",
	"wretch",
	"request"
]);
const MUTATION_METHOD_NAMES = new Set([
	"create",
	"insert",
	"insertInto",
	"update",
	"upsert",
	"delete",
	"remove",
	"destroy",
	"set",
	"append"
]);
const MUTATING_HTTP_METHODS = new Set([
	"POST",
	"PUT",
	"DELETE",
	"PATCH"
]);
const SAFE_MUTABLE_CONSTRUCTOR_NAMES = new Set([
	"Map",
	"Set",
	"WeakMap",
	"WeakSet",
	"Headers",
	"URLSearchParams",
	"FormData",
	"Response",
	"NextResponse"
]);
const RESPONSE_FACTORY_OBJECTS = new Set(["Response", "NextResponse"]);
const RESPONSE_FACTORY_METHODS = new Set([
	"json",
	"redirect",
	"next",
	"rewrite",
	"error"
]);
//#endregion
//#region src/plugin/constants/dom.ts
const PASSIVE_EVENT_NAMES = new Set([
	"scroll",
	"wheel",
	"touchstart",
	"touchmove",
	"touchend"
]);
const SCRIPT_LOADING_ATTRIBUTES = new Set(["defer", "async"]);
const EXECUTABLE_SCRIPT_TYPES = new Set([
	"text/javascript",
	"application/javascript",
	"module"
]);
const TIMER_AND_SCHEDULER_DIRECT_CALLEE_NAMES = new Set([
	"setTimeout",
	"setInterval",
	"requestAnimationFrame",
	"requestIdleCallback",
	"queueMicrotask"
]);
const TIMER_CALLEE_NAMES_REQUIRING_CLEANUP = new Set(["setInterval", "setTimeout"]);
const TIMER_CLEANUP_CALLEE_NAMES = new Set(["clearInterval", "clearTimeout"]);
const MUTABLE_GLOBAL_ROOTS = new Set([
	"location",
	"window",
	"document",
	"navigator",
	"history",
	"screen",
	"performance"
]);
const EXTERNAL_SYNC_OBSERVER_CONSTRUCTORS = new Set([
	"IntersectionObserver",
	"MutationObserver",
	"ResizeObserver",
	"PerformanceObserver"
]);
const STORAGE_OBJECTS$1 = new Set(["localStorage", "sessionStorage"]);
//#endregion
//#region src/plugin/constants/react.ts
const INDEX_PARAMETER_NAMES = new Set([
	"index",
	"idx",
	"i"
]);
const LOADING_STATE_PATTERN = /^(?:isLoading|isPending)$/;
const STABLE_HOOK_WRAPPERS = new Set([
	"useState",
	"useMemo",
	"useRef"
]);
const GENERIC_EVENT_SUFFIXES = new Set([
	"Click",
	"Change",
	"Input",
	"Blur",
	"Focus"
]);
const TRIVIAL_INITIALIZER_NAMES = new Set([
	"Boolean",
	"String",
	"Number",
	"Array",
	"Object",
	"parseInt",
	"parseFloat"
]);
const TRIVIAL_DERIVATION_CALLEE_NAMES = new Set([
	"Boolean",
	"String",
	"Number",
	"Array",
	"Object",
	"parseInt",
	"parseFloat",
	"isNaN",
	"isFinite",
	"BigInt",
	"Symbol"
]);
const SETTER_PATTERN = /^set[A-Z]/;
const RENDER_FUNCTION_PATTERN = /^render[A-Z]/;
const UPPERCASE_PATTERN = /^[A-Z]/;
const REACT_HANDLER_PROP_PATTERN = /^on[A-Z]/;
const EFFECT_HOOK_NAMES$1 = new Set(["useEffect", "useLayoutEffect"]);
const HOOKS_WITH_DEPS = new Set([
	"useEffect",
	"useLayoutEffect",
	"useMemo",
	"useCallback"
]);
const REACT_HOC_NAMES = new Set([
	"memo",
	"forwardRef",
	"React.memo",
	"React.forwardRef"
]);
const MEMOIZING_HOOK_NAMES = new Set(["useMemo", "useCallback"]);
const COMPONENT_HOC_WRAPPER_NAMES = new Set([
	"memo",
	"forwardRef",
	"observer",
	"lazy"
]);
const SUBSCRIPTION_METHOD_NAMES = new Set([
	"subscribe",
	"addEventListener",
	"addListener",
	"on",
	"watch",
	"listen",
	"sub"
]);
const CLEANUP_RETURNING_SUBSCRIPTION_METHOD_NAMES = new Set(["subscribe", "sub"]);
const GLOBAL_RELEASE_METHOD_NAMES = new Set([
	"unsubscribe",
	"removeEventListener",
	"removeListener",
	"off",
	"unwatch",
	"unlisten",
	"unsub",
	"abort"
]);
const BOUND_RESOURCE_RELEASE_METHOD_NAMES = new Set([
	"remove",
	"cleanup",
	"dispose",
	"destroy",
	"stop",
	"teardown"
]);
const CLEANUP_LIKE_RELEASE_CALLEE_NAMES = new Set([
	...GLOBAL_RELEASE_METHOD_NAMES,
	"cleanup",
	"dispose",
	"destroy",
	"teardown"
]);
const EXTERNAL_SYNC_MEMBER_METHOD_NAMES = new Set([
	...SUBSCRIPTION_METHOD_NAMES,
	"connect",
	"disconnect",
	"open",
	"close",
	"fetch",
	"post",
	"put",
	"patch"
]);
const EXTERNAL_SYNC_HTTP_CLIENT_RECEIVERS = new Set([
	...FETCH_MEMBER_OBJECTS,
	"api",
	"client",
	"http",
	"fetcher"
]);
const EXTERNAL_SYNC_AMBIGUOUS_HTTP_METHOD_NAMES = new Set([
	"get",
	"head",
	"options",
	"delete"
]);
const EXTERNAL_SYNC_DIRECT_CALLEE_NAMES = new Set([...FETCH_CALLEE_NAMES, ...TIMER_AND_SCHEDULER_DIRECT_CALLEE_NAMES]);
const EVENT_TRIGGERED_SIDE_EFFECT_CALLEES = new Set([
	...FETCH_CALLEE_NAMES,
	"post",
	"put",
	"patch",
	"navigate",
	"navigateTo",
	"showNotification",
	"toast",
	"alert",
	"confirm",
	"logVisit",
	"captureEvent"
]);
const EVENT_TRIGGERED_SIDE_EFFECT_MEMBER_METHODS = new Set([
	"post",
	"put",
	"patch",
	"delete",
	"navigate",
	"capture",
	"track",
	"logEvent"
]);
const EVENT_TRIGGERED_NAVIGATION_METHOD_NAMES = new Set(["push", "replace"]);
const NAVIGATION_RECEIVER_NAMES = new Set([
	"router",
	"navigation",
	"navigator",
	"history",
	"location"
]);
//#endregion
//#region src/plugin/utils/find-program-root.ts
/**
* Walks up the AST `parent` chain from `node` to its enclosing
* `Program` root and returns it; `null` when the chain doesn't lead
* to a `Program` (e.g. detached fragments used by test utilities).
*
* Was duplicated byte-identical across seven sites (five rule files
* + two utility modules). Promoted to a shared helper so adding a
* new ESTree top-level shape only touches one place.
*/
const findProgramRoot = (node) => {
	let cursor = node;
	while (cursor) {
		if (isNodeOfType(cursor, "Program")) return cursor;
		cursor = cursor.parent ?? null;
	}
	return null;
};
//#endregion
//#region src/plugin/utils/get-imported-name.ts
const getImportedName$1 = (importSpecifier) => {
	if (!isNodeOfType(importSpecifier, "ImportSpecifier")) return void 0;
	const imported = importSpecifier.imported;
	if (isNodeOfType(imported, "Identifier")) return imported.name;
	if (isNodeOfType(imported, "Literal") && typeof imported.value === "string") return imported.value;
};
//#endregion
//#region src/plugin/utils/get-callee-name.ts
const getCalleeName$1 = (node) => {
	if (!isNodeOfType(node, "CallExpression") && !isNodeOfType(node, "NewExpression")) return null;
	if (isNodeOfType(node.callee, "Identifier")) return node.callee.name;
	if (isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.property, "Identifier")) return node.callee.property.name;
	return null;
};
//#endregion
//#region src/plugin/utils/is-hook-call.ts
const isHookCall$1 = (node, hookName) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	const calleeName = getCalleeName$1(node);
	if (!calleeName) return false;
	return typeof hookName === "string" ? calleeName === hookName : hookName.has(calleeName);
};
//#endregion
//#region src/plugin/utils/is-ast-node.ts
const isAstNode = (value) => {
	if (!hasTypeProperty(value)) return false;
	return typeof value.type === "string";
};
//#endregion
//#region src/plugin/utils/walk-ast.ts
const walkAst = (node, visitor) => {
	if (!node || typeof node !== "object") return;
	if (visitor(node) === false) return;
	const nodeRecord = node;
	for (const key of Object.keys(nodeRecord)) {
		if (key === "parent") continue;
		const child = nodeRecord[key];
		if (Array.isArray(child)) {
			for (const item of child) if (isAstNode(item)) walkAst(item, visitor);
		} else if (isAstNode(child)) walkAst(child, visitor);
	}
};
//#endregion
//#region src/plugin/rules/state-and-effects/activity-wraps-effect-heavy-subtree.ts
const ACTIVITY_IMPORTED_NAMES = new Set(["Activity", "unstable_Activity"]);
const isStaticallyKnownMode = (modeAttribute) => {
	const value = modeAttribute.value;
	if (!value) return false;
	if (isNodeOfType(value, "Literal")) return true;
	if (isNodeOfType(value, "JSXExpressionContainer")) return isNodeOfType(value.expression, "Literal");
	return false;
};
const collectChildComponentNames = (element, into) => {
	walkAst(element, (child) => {
		if (!isNodeOfType(child, "JSXOpeningElement")) return;
		if (!isNodeOfType(child.name, "JSXIdentifier")) return;
		const name = child.name.name;
		if (!UPPERCASE_PATTERN.test(name)) return;
		into.add(name);
	});
};
const findSameFileComponentBody = (programRoot, componentName) => {
	let foundBody = null;
	walkAst(programRoot, (node) => {
		if (foundBody) return false;
		if (isNodeOfType(node, "FunctionDeclaration") && node.id && node.id.name === componentName) {
			foundBody = node.body;
			return false;
		}
		if (isNodeOfType(node, "VariableDeclarator") && isNodeOfType(node.id, "Identifier") && node.id.name === componentName) {
			const initializer = node.init;
			if (isNodeOfType(initializer, "ArrowFunctionExpression") || isNodeOfType(initializer, "FunctionExpression")) {
				foundBody = initializer.body;
				return false;
			}
		}
	});
	return foundBody;
};
const countEffectHookCalls = (body) => {
	if (!body) return 0;
	let count = 0;
	walkAst(body, (child) => {
		if (!isNodeOfType(child, "CallExpression")) return;
		if (isHookCall$1(child, EFFECT_HOOK_NAMES$1)) count++;
	});
	return count;
};
const activityWrapsEffectHeavySubtree = defineRule({
	id: "activity-wraps-effect-heavy-subtree",
	title: "Activity wraps an effect-heavy subtree",
	severity: "warn",
	requires: ["react:19.2"],
	recommendation: "Check what's under `<Activity>`. Every hide and show rebuilds every Effect inside from scratch. Move subscriptions and effect-driven setState out of the Activity, or load the data above it.",
	create: (context) => {
		const localActivityNames = /* @__PURE__ */ new Set();
		const reactNamespaceLocalNames = /* @__PURE__ */ new Set();
		return {
			ImportDeclaration(node) {
				if (node.source?.value !== "react") return;
				for (const specifier of node.specifiers ?? []) {
					if (isNodeOfType(specifier, "ImportNamespaceSpecifier")) {
						if (isNodeOfType(specifier.local, "Identifier")) reactNamespaceLocalNames.add(specifier.local.name);
						continue;
					}
					if (isNodeOfType(specifier, "ImportDefaultSpecifier")) {
						if (isNodeOfType(specifier.local, "Identifier")) reactNamespaceLocalNames.add(specifier.local.name);
						continue;
					}
					if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
					const importedName = getImportedName$1(specifier);
					if (!importedName || !ACTIVITY_IMPORTED_NAMES.has(importedName)) continue;
					if (isNodeOfType(specifier.local, "Identifier")) localActivityNames.add(specifier.local.name);
				}
			},
			JSXElement(node) {
				const openingElement = node.openingElement;
				if (!openingElement) return;
				const elementName = openingElement.name;
				let isActivity = false;
				if (isNodeOfType(elementName, "JSXIdentifier")) isActivity = localActivityNames.has(elementName.name);
				else if (isNodeOfType(elementName, "JSXMemberExpression")) {
					if (isNodeOfType(elementName.object, "JSXIdentifier") && reactNamespaceLocalNames.has(elementName.object.name) && isNodeOfType(elementName.property, "JSXIdentifier")) isActivity = ACTIVITY_IMPORTED_NAMES.has(elementName.property.name);
				}
				if (!isActivity) return;
				let modeAttribute = null;
				for (const attribute of openingElement.attributes ?? []) {
					if (!isNodeOfType(attribute, "JSXAttribute")) continue;
					if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
					if (attribute.name.name !== "mode") continue;
					modeAttribute = attribute;
					break;
				}
				if (!modeAttribute) return;
				if (isStaticallyKnownMode(modeAttribute)) return;
				const childComponentNames = /* @__PURE__ */ new Set();
				collectChildComponentNames(node, childComponentNames);
				for (const activityName of localActivityNames) childComponentNames.delete(activityName);
				if (childComponentNames.size === 0) return;
				const programRoot = findProgramRoot(node);
				if (!programRoot) return;
				let totalEffects = 0;
				const effectfulChildren = [];
				for (const componentName of childComponentNames) {
					const body = findSameFileComponentBody(programRoot, componentName);
					if (!body) continue;
					const effectCount = countEffectHookCalls(body);
					if (effectCount === 0) continue;
					totalEffects += effectCount;
					effectfulChildren.push(`<${componentName}>`);
				}
				if (totalEffects === 0) return;
				context.report({
					node: openingElement,
					message: `Every hide & show rebuilds ${effectfulChildren.join(", ")} from scratch because <Activity> wraps them & they use ${totalEffects} effect hook${totalEffects === 1 ? "" : "s"}.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/get-effect-callback.ts
const getEffectCallback = (node) => {
	if (!isNodeOfType(node, "CallExpression") && !isNodeOfType(node, "NewExpression")) return null;
	if (!node.arguments?.length) return null;
	const callback = node.arguments[0];
	if (isNodeOfType(callback, "ArrowFunctionExpression") || isNodeOfType(callback, "FunctionExpression")) return callback;
	return null;
};
//#endregion
//#region src/plugin/rules/state-and-effects/advanced-event-handler-refs.ts
const advancedEventHandlerRefs = defineRule({
	id: "advanced-event-handler-refs",
	title: "Listener re-subscribes on every handler change",
	tags: ["test-noise"],
	severity: "warn",
	category: "Performance",
	recommendation: "Store the handler in a ref and have the listener read `handlerRef.current()`. The subscription stays put while the latest handler still runs.",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1)) return;
		if ((node.arguments?.length ?? 0) < 2) return;
		const callback = getEffectCallback(node);
		if (!callback || !isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) return;
		const depsNode = node.arguments[1];
		if (!isNodeOfType(depsNode, "ArrayExpression") || !depsNode.elements?.length) return;
		const depIdentifierNames = /* @__PURE__ */ new Set();
		for (const element of depsNode.elements) if (isNodeOfType(element, "Identifier")) depIdentifierNames.add(element.name);
		if (depIdentifierNames.size === 0) return;
		let registeredHandlerName = null;
		walkAst(callback.body, (child) => {
			if (registeredHandlerName) return;
			if (!isNodeOfType(child, "CallExpression")) return;
			if (!isNodeOfType(child.callee, "MemberExpression")) return;
			if (!isNodeOfType(child.callee.property, "Identifier")) return;
			if (!SUBSCRIPTION_METHOD_NAMES.has(child.callee.property.name)) return;
			const handlerArg = child.arguments?.[1];
			if (!isNodeOfType(handlerArg, "Identifier")) return;
			if (depIdentifierNames.has(handlerArg.name)) registeredHandlerName = handlerArg.name;
		});
		if (registeredHandlerName) context.report({
			node,
			message: `useEffect re-adds the "${registeredHandlerName}" listener every time the handler changes.`
		});
	} })
});
//#endregion
//#region src/plugin/utils/get-jsx-prop-string-value.ts
const getJsxPropStringValue = (attribute) => {
	const value = attribute.value;
	if (!value) return null;
	if (isNodeOfType(value, "Literal") && typeof value.value === "string") return value.value;
	return null;
};
//#endregion
//#region src/plugin/utils/get-jsx-attribute-name.ts
const getJsxAttributeName = (node) => {
	if (isNodeOfType(node, "JSXIdentifier")) return node.name;
	if (isNodeOfType(node, "JSXNamespacedName")) return `${node.namespace.name}:${node.name.name}`;
	return null;
};
//#endregion
//#region src/plugin/utils/has-jsx-prop-ignore-case.ts
const hasJsxPropIgnoreCase = (attributes, targetProp) => {
	const targetLower = targetProp.toLowerCase();
	for (const attribute of attributes) {
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		const name = getJsxAttributeName(attribute.name);
		if (name && name.toLowerCase() === targetLower) return attribute;
	}
};
//#endregion
//#region src/plugin/utils/get-element-type.ts
const readJsxA11ySettings = (settings) => {
	if (!settings) return {};
	const block = settings["jsx-a11y"];
	if (!block || typeof block !== "object") return {};
	return block;
};
const flattenJsxName$2 = (name) => {
	if (isNodeOfType(name, "JSXIdentifier")) return name.name;
	if (isNodeOfType(name, "JSXMemberExpression")) return `${flattenJsxName$2(name.object)}.${name.property.name}`;
	if (isNodeOfType(name, "JSXNamespacedName")) return `${name.namespace.name}:${name.name.name}`;
	return "";
};
const getElementType = (openingElement, settings) => {
	const a11ySettings = readJsxA11ySettings(settings);
	const baseName = flattenJsxName$2(openingElement.name);
	if (a11ySettings.polymorphicPropName) {
		const polymorphicAttribute = hasJsxPropIgnoreCase(openingElement.attributes, a11ySettings.polymorphicPropName);
		if (polymorphicAttribute) {
			const polymorphicValue = getJsxPropStringValue(polymorphicAttribute);
			if (polymorphicValue !== null) return polymorphicValue;
		}
	}
	if (a11ySettings.components && baseName in a11ySettings.components) return a11ySettings.components[baseName];
	return baseName;
};
//#endregion
//#region src/plugin/utils/is-nextjs-metadata-image-route-filename.ts
const isNextjsMetadataImageRouteFilename = (rawFilename) => {
	if (!rawFilename) return false;
	return /^(opengraph-image|twitter-image|icon|apple-icon)\d*\.(jsx?|tsx?)$/.test(path.basename(rawFilename));
};
//#endregion
//#region src/plugin/utils/normalize-filename.ts
const normalizeFilename$1 = (filename) => filename.replaceAll("\\", "/");
//#endregion
//#region src/plugin/utils/is-hidden-from-screen-reader.ts
const isHiddenFromScreenReader = (openingElement, settings) => {
	if (getElementType(openingElement, settings).toLowerCase() === "input") {
		const typeAttribute = hasJsxPropIgnoreCase(openingElement.attributes, "type");
		if (typeAttribute) {
			const typeValue = getJsxPropStringValue(typeAttribute);
			if (typeValue && typeValue.toLowerCase() === "hidden") return true;
		}
	}
	const ariaHidden = hasJsxPropIgnoreCase(openingElement.attributes, "aria-hidden");
	if (!ariaHidden) return false;
	const value = ariaHidden.value;
	if (!value) return true;
	if (isNodeOfType(value, "Literal") && typeof value.value === "string") return value.value === "true";
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal")) return Boolean(expression.value);
		if (isNodeOfType(expression, "Identifier") && expression.name === "true") return true;
	}
	return false;
};
//#endregion
//#region src/plugin/utils/object-has-accessible-child.ts
const objectHasAccessibleChild = (jsxElement, settings) => {
	for (const child of jsxElement.children) {
		if (isNodeOfType(child, "JSXText")) {
			if (child.value.length > 0) return true;
			continue;
		}
		if (isNodeOfType(child, "JSXElement")) {
			const innerOpening = child.openingElement;
			if (!isHiddenFromScreenReader(innerOpening, settings)) return true;
			continue;
		}
		if (isNodeOfType(child, "JSXExpressionContainer")) {
			const expression = child.expression;
			if (isNodeOfType(expression, "Literal") && expression.value === null) continue;
			if (isNodeOfType(expression, "Identifier") && expression.name === "undefined") continue;
			return true;
		}
	}
	if (hasJsxPropIgnoreCase(jsxElement.openingElement.attributes, "dangerouslySetInnerHTML")) return true;
	if (hasJsxPropIgnoreCase(jsxElement.openingElement.attributes, "children")) return true;
	return false;
};
//#endregion
//#region src/plugin/rules/a11y/alt-text.ts
const MISSING_ALT_PROP = "Blind users can't use this image because screen readers skip it without `alt`, so add `alt=\"...\"` (or `alt=\"\"` if decorative).";
const MISSING_ALT_VALUE = "Blind users can't use this image because its `alt` is empty or invalid, so add a short description (or `alt=\"\"` if decorative).";
const ARIA_LABEL_VALUE = "Blind users hear nothing here because `aria-label` has no value, so give it a short description.";
const ARIA_LABELLEDBY_VALUE = "Blind users hear nothing here because `aria-labelledby` has no value, so point it at the id of the text that labels this.";
const PREFER_ALT = "Screen readers skip a decorative image more reliably with `alt=\"\"` than `role=\"presentation\"`, so use `alt=\"\"` instead.";
const MESSAGE_OBJECT = "Blind users can't use this `<object>` because screen readers can't describe it, so add `alt`, `aria-label`, `aria-labelledby`, `title`, or inner fallback text.";
const MESSAGE_AREA = "Blind users can't use this `<area>` of the image map because screen readers can't describe it, so add `alt`, `aria-label`, or `aria-labelledby`.";
const MESSAGE_INPUT_IMAGE = "Blind users can't use this image button because screen readers can't describe it, so add `alt`, `aria-label`, or `aria-labelledby`.";
const resolveSettings$53 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.altText ?? {} : {};
};
const isUndefinedOrNullExpression = (expression) => {
	if (isNodeOfType(expression, "Identifier") && expression.name === "undefined") return true;
	if (isNodeOfType(expression, "Literal") && expression.value === null) return true;
	return false;
};
const isValidAltProp = (attribute) => {
	if (!attribute.value) return false;
	if (isNodeOfType(attribute.value, "JSXExpressionContainer")) {
		const expression = attribute.value.expression;
		if (expression && expression.type !== "JSXEmptyExpression") return !isUndefinedOrNullExpression(expression);
		return true;
	}
	return true;
};
const isPresentationRole$1 = (attribute) => {
	if (!attribute) return false;
	const value = getJsxPropStringValue(attribute);
	return value === "presentation" || value === "none";
};
const ariaLabelHasValue = (attribute) => {
	if (!attribute.value) return false;
	if (isNodeOfType(attribute.value, "Literal")) {
		if (typeof attribute.value.value === "string") return attribute.value.value.length > 0;
		return true;
	}
	if (isNodeOfType(attribute.value, "JSXExpressionContainer")) {
		const expression = attribute.value.expression;
		if (!expression || expression.type === "JSXEmptyExpression") return true;
		if (isNodeOfType(expression, "Identifier") && expression.name === "undefined") return false;
		return true;
	}
	return true;
};
const imgRule = (opening, reportNode, context) => {
	const altAttribute = hasJsxPropIgnoreCase(opening.attributes, "alt");
	if (altAttribute) {
		if (!isValidAltProp(altAttribute)) context.report({
			node: reportNode,
			message: MISSING_ALT_VALUE
		});
		return;
	}
	if (isPresentationRole$1(hasJsxPropIgnoreCase(opening.attributes, "role"))) {
		context.report({
			node: reportNode,
			message: PREFER_ALT
		});
		return;
	}
	const ariaLabelAttribute = hasJsxPropIgnoreCase(opening.attributes, "aria-label");
	if (ariaLabelAttribute) {
		if (!ariaLabelHasValue(ariaLabelAttribute)) context.report({
			node: reportNode,
			message: ARIA_LABEL_VALUE
		});
		return;
	}
	const ariaLabelledbyAttribute = hasJsxPropIgnoreCase(opening.attributes, "aria-labelledby");
	if (ariaLabelledbyAttribute) {
		if (!ariaLabelHasValue(ariaLabelledbyAttribute)) context.report({
			node: reportNode,
			message: ARIA_LABELLEDBY_VALUE
		});
		return;
	}
	context.report({
		node: reportNode,
		message: MISSING_ALT_PROP
	});
};
const objectRule = (opening, parent, reportNode, context) => {
	const ariaLabelAttribute = hasJsxPropIgnoreCase(opening.attributes, "aria-label");
	const ariaLabelledbyAttribute = hasJsxPropIgnoreCase(opening.attributes, "aria-labelledby");
	const hasLabel = ariaLabelAttribute && ariaLabelHasValue(ariaLabelAttribute) || ariaLabelledbyAttribute && ariaLabelHasValue(ariaLabelledbyAttribute);
	const titleAttribute = hasJsxPropIgnoreCase(opening.attributes, "title");
	const titleValue = titleAttribute ? getJsxPropStringValue(titleAttribute) : null;
	const hasTitle = Boolean(titleValue && titleValue.length > 0);
	if (hasLabel || hasTitle) return;
	if (objectHasAccessibleChild(parent, context.settings)) return;
	context.report({
		node: reportNode,
		message: MESSAGE_OBJECT
	});
};
const areaRule = (opening, reportNode, context) => {
	const ariaLabelAttribute = hasJsxPropIgnoreCase(opening.attributes, "aria-label");
	const ariaLabelledbyAttribute = hasJsxPropIgnoreCase(opening.attributes, "aria-labelledby");
	if (ariaLabelAttribute && ariaLabelHasValue(ariaLabelAttribute) || ariaLabelledbyAttribute && ariaLabelHasValue(ariaLabelledbyAttribute)) return;
	const altAttribute = hasJsxPropIgnoreCase(opening.attributes, "alt");
	if (!altAttribute || !isValidAltProp(altAttribute)) context.report({
		node: reportNode,
		message: MESSAGE_AREA
	});
};
const inputTypeImageRule = (opening, reportNode, context) => {
	const ariaLabelAttribute = hasJsxPropIgnoreCase(opening.attributes, "aria-label");
	const ariaLabelledbyAttribute = hasJsxPropIgnoreCase(opening.attributes, "aria-labelledby");
	if (ariaLabelAttribute && ariaLabelHasValue(ariaLabelAttribute) || ariaLabelledbyAttribute && ariaLabelHasValue(ariaLabelledbyAttribute)) return;
	const altAttribute = hasJsxPropIgnoreCase(opening.attributes, "alt");
	if (!altAttribute || !isValidAltProp(altAttribute)) context.report({
		node: reportNode,
		message: MESSAGE_INPUT_IMAGE
	});
};
const altText = defineRule({
	id: "alt-text",
	title: "Image missing alt text",
	tags: ["react-jsx-only"],
	severity: "error",
	recommendation: "Give every meaningful image an `alt`, `aria-label`, or `aria-labelledby`.",
	category: "Accessibility",
	create: (context) => {
		if (isNextjsMetadataImageRouteFilename(normalizeFilename$1(context.filename ?? ""))) return {};
		const settings = resolveSettings$53(context.settings);
		const checkImg = !settings.elements || settings.elements.includes("img");
		const checkObject = !settings.elements || settings.elements.includes("object");
		const checkArea = !settings.elements || settings.elements.includes("area");
		const checkInputImage = !settings.elements || settings.elements.includes("input[type=\"image\"]");
		const imgAliases = new Set(settings.img ?? []);
		const objectAliases = new Set(settings.object ?? []);
		const areaAliases = new Set(settings.area ?? []);
		const inputImageAliases = new Set(settings["input[type=\"image\"]"] ?? []);
		return { JSXOpeningElement(node) {
			const tag = getElementType(node, context.settings);
			if (checkImg && (tag === "img" || imgAliases.has(tag))) {
				imgRule(node, node, context);
				return;
			}
			if (checkObject && (tag === "object" || objectAliases.has(tag))) {
				const parent = node.parent;
				if (parent && isNodeOfType(parent, "JSXElement")) objectRule(node, parent, node, context);
				return;
			}
			if (checkArea && (tag === "area" || areaAliases.has(tag))) {
				areaRule(node, node, context);
				return;
			}
			if (checkInputImage) {
				if (tag.toLowerCase() === "input" && (() => {
					const typeAttribute = hasJsxPropIgnoreCase(node.attributes, "type");
					if (!typeAttribute) return false;
					return getJsxPropStringValue(typeAttribute) === "image";
				})() || inputImageAliases.has(tag)) inputTypeImageRule(node, node, context);
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/a11y/anchor-ambiguous-text.ts
const buildMessage$29 = (text) => `Screen reader users can't tell where \`${text}\` goes, so name the destination, like "View pricing details".`;
const DEFAULT_AMBIGUOUS = [
	"click here",
	"here",
	"link",
	"a link",
	"learn more"
];
const resolveSettings$52 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { words: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.anchorAmbiguousText ?? {} : {}).words ?? DEFAULT_AMBIGUOUS };
};
const normalizeText = (text) => {
	return text.toLowerCase().replace(/[,.?¿!‽¡;:]/g, "").split(/\s+/).filter((part) => part.length > 0).join(" ");
};
const getAccessibleText = (element, settings) => {
	const opening = element.openingElement;
	const ariaLabel = hasJsxPropIgnoreCase(opening.attributes, "aria-label");
	if (ariaLabel) {
		const labelValue = getJsxPropStringValue(ariaLabel);
		if (labelValue !== null) return labelValue;
	}
	if (getElementType(opening, settings) === "img") {
		const altAttribute = hasJsxPropIgnoreCase(opening.attributes, "alt");
		if (altAttribute) {
			const altValue = getJsxPropStringValue(altAttribute);
			if (altValue !== null) return altValue;
		}
	}
	if (isHiddenFromScreenReader(opening, settings)) return null;
	const parts = [];
	for (const child of element.children) {
		const childNode = child;
		if (isNodeOfType(childNode, "JSXText")) parts.push(childNode.value);
		else if (isNodeOfType(childNode, "JSXElement")) {
			const inner = getAccessibleText(childNode, settings);
			if (inner !== null) parts.push(inner);
		}
	}
	return parts.join(" ");
};
const anchorAmbiguousText = defineRule({
	id: "anchor-ambiguous-text",
	title: "Ambiguous link text",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Name where a link goes. Avoid 'click here', 'learn more', and 'link'.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$52(context.settings);
		const ambiguousSet = new Set(settings.words.map((word) => word.toLowerCase()));
		return { JSXElement(node) {
			if (getElementType(node.openingElement, context.settings) !== "a") return;
			const accessibleText = getAccessibleText(node, context.settings);
			if (accessibleText === null || accessibleText.trim().length === 0) return;
			const normalized = normalizeText(accessibleText);
			if (ambiguousSet.has(normalized)) context.report({
				node: node.openingElement.name,
				message: buildMessage$29(normalized)
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/a11y/anchor-has-content.ts
const MESSAGE$51 = "Blind users can't follow this link because screen readers announce nothing, so add visible text, `aria-label`, or `aria-labelledby`.";
const anchorHasContent = defineRule({
	id: "anchor-has-content",
	title: "Anchor has no content",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Put readable text inside every `<a>`.",
	category: "Accessibility",
	create: (context) => ({ JSXElement(node) {
		const opening = node.openingElement;
		if (getElementType(opening, context.settings) !== "a") return;
		if (isHiddenFromScreenReader(opening, context.settings)) return;
		if (objectHasAccessibleChild(node, context.settings)) return;
		for (const attribute of ["title", "aria-label"]) if (hasJsxPropIgnoreCase(opening.attributes, attribute)) return;
		context.report({
			node: opening.name,
			message: MESSAGE$51
		});
	} })
});
//#endregion
//#region src/plugin/utils/get-static-template-literal-value.ts
const getStaticTemplateLiteralValue = (templateLiteral) => {
	const expressions = templateLiteral.expressions ?? [];
	const quasis = templateLiteral.quasis ?? [];
	if (expressions.length !== 0 || quasis.length !== 1) return null;
	const value = quasis[0]?.value;
	return value?.cooked ?? value?.raw ?? null;
};
//#endregion
//#region src/plugin/rules/a11y/anchor-is-valid.ts
const MESSAGE_MISSING_HREF = "Keyboard users can't reach this link because it has no `href`, so add a real `href` (or use `<button>` for actions).";
const MESSAGE_INCORRECT_HREF = "Keyboard users can't reach this link because its `href` goes nowhere (`#`, `javascript:`, or empty), so point it at a real destination.";
const MESSAGE_CANT_BE_ANCHOR = "Keyboard users can't trigger this link because it's a click handler with no real `href`, so use `<button>` instead.";
const resolveSettings$51 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.anchorIsValid ?? {} : {};
	const jsxA11y = settings?.["jsx-a11y"];
	const a11ySettings = typeof jsxA11y === "object" && jsxA11y !== null ? jsxA11y : {};
	return {
		validHrefs: new Set(ruleSettings.validHrefs ?? []),
		hrefAttributeNames: a11ySettings.attributes?.href ?? ["href"]
	};
};
const isInvalidHref = (value, validHrefs) => {
	if (validHrefs.has(value)) return false;
	return value === "" || value === "#" || value === "javascript:void(0)";
};
const checkValueIsEmptyOrInvalid = (value, validHrefs) => {
	if (isNodeOfType(value, "Literal")) return typeof value.value === "string" ? isInvalidHref(value.value, validHrefs) : false;
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Identifier") && expression.name === "undefined") return true;
		if (isNodeOfType(expression, "Literal")) {
			if (expression.value === null) return true;
			if (typeof expression.value === "string") return isInvalidHref(expression.value, validHrefs);
		}
		if (isNodeOfType(expression, "TemplateLiteral")) {
			const staticValue = getStaticTemplateLiteralValue(expression);
			return staticValue === null ? false : isInvalidHref(staticValue, validHrefs);
		}
	}
	if (isNodeOfType(value, "JSXFragment")) return true;
	return false;
};
const anchorIsValid = defineRule({
	id: "anchor-is-valid",
	title: "Anchor used as a button",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Give links a real destination. Use `<button>` for in-page actions.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$51(context.settings);
		return { JSXOpeningElement(node) {
			if (getElementType(node, context.settings) !== "a") return;
			let hrefAttribute;
			for (const attributeName of settings.hrefAttributeNames) {
				hrefAttribute = hasJsxPropIgnoreCase(node.attributes, attributeName);
				if (hrefAttribute) break;
			}
			if (hrefAttribute) {
				if (!hrefAttribute.value) {
					context.report({
						node: node.name,
						message: MESSAGE_INCORRECT_HREF
					});
					return;
				}
				if (checkValueIsEmptyOrInvalid(hrefAttribute.value, settings.validHrefs)) {
					const hasOnClick = Boolean(hasJsxPropIgnoreCase(node.attributes, "onClick"));
					context.report({
						node: node.name,
						message: hasOnClick ? MESSAGE_CANT_BE_ANCHOR : MESSAGE_INCORRECT_HREF
					});
				}
				return;
			}
			if (node.attributes.some((attribute) => isNodeOfType(attribute, "JSXSpreadAttribute"))) return;
			context.report({
				node: node.name,
				message: MESSAGE_MISSING_HREF
			});
		} };
	}
});
//#endregion
//#region src/plugin/constants/html-tags.ts
const RESERVED_HTML_TAGS = new Set([
	"base",
	"col",
	"colgroup",
	"head",
	"html",
	"link",
	"meta",
	"noembed",
	"noscript",
	"param",
	"picture",
	"script",
	"source",
	"style",
	"title",
	"track"
]);
const HTML_TAGS = new Set([
	"a",
	"abbr",
	"acronym",
	"address",
	"applet",
	"area",
	"article",
	"aside",
	"audio",
	"b",
	"base",
	"basefont",
	"bdi",
	"bdo",
	"bgsound",
	"big",
	"blink",
	"blockquote",
	"body",
	"br",
	"button",
	"canvas",
	"caption",
	"center",
	"cite",
	"code",
	"col",
	"colgroup",
	"command",
	"content",
	"data",
	"datalist",
	"dd",
	"del",
	"details",
	"dfn",
	"dialog",
	"dir",
	"div",
	"dl",
	"dt",
	"element",
	"em",
	"embed",
	"fieldset",
	"figcaption",
	"figure",
	"font",
	"footer",
	"form",
	"frame",
	"frameset",
	"h1",
	"h2",
	"h3",
	"h4",
	"h5",
	"h6",
	"head",
	"header",
	"hgroup",
	"hr",
	"html",
	"i",
	"iframe",
	"image",
	"img",
	"input",
	"ins",
	"isindex",
	"kbd",
	"keygen",
	"label",
	"legend",
	"li",
	"link",
	"listing",
	"main",
	"map",
	"mark",
	"marquee",
	"math",
	"menu",
	"menuitem",
	"meta",
	"meter",
	"multicol",
	"nav",
	"nextid",
	"nobr",
	"noembed",
	"noframes",
	"noscript",
	"object",
	"ol",
	"optgroup",
	"option",
	"output",
	"p",
	"param",
	"picture",
	"plaintext",
	"pre",
	"progress",
	"q",
	"rb",
	"rbc",
	"rp",
	"rt",
	"rtc",
	"ruby",
	"s",
	"samp",
	"script",
	"search",
	"section",
	"select",
	"shadow",
	"slot",
	"small",
	"source",
	"spacer",
	"span",
	"strike",
	"strong",
	"style",
	"sub",
	"summary",
	"sup",
	"svg",
	"table",
	"tbody",
	"td",
	"template",
	"textarea",
	"tfoot",
	"th",
	"thead",
	"time",
	"title",
	"tr",
	"track",
	"tt",
	"u",
	"ul",
	"var",
	"video",
	"wbr",
	"xmp"
]);
const NON_INTERACTIVE_ELEMENTS = new Set([
	"abbr",
	"address",
	"article",
	"aside",
	"blockquote",
	"br",
	"caption",
	"code",
	"dd",
	"del",
	"details",
	"dfn",
	"dialog",
	"dir",
	"dl",
	"dt",
	"em",
	"fieldset",
	"figcaption",
	"figure",
	"footer",
	"form",
	"h1",
	"h2",
	"h3",
	"h4",
	"h5",
	"h6",
	"hr",
	"html",
	"iframe",
	"img",
	"ins",
	"label",
	"legend",
	"li",
	"main",
	"mark",
	"marquee",
	"menu",
	"meter",
	"nav",
	"ol",
	"optgroup",
	"output",
	"p",
	"pre",
	"progress",
	"ruby",
	"section",
	"strong",
	"sub",
	"sup",
	"table",
	"tbody",
	"tfoot",
	"thead",
	"time",
	"ul"
]);
//#endregion
//#region src/plugin/utils/is-interactive-element.ts
const isInteractiveElement = (tagName, openingElement) => {
	switch (tagName) {
		case "audio":
		case "button":
		case "canvas":
		case "datalist":
		case "embed":
		case "menuitem":
		case "option":
		case "select":
		case "summary":
		case "td":
		case "th":
		case "tr":
		case "textarea":
		case "video": return true;
		case "input": {
			const typeAttribute = hasJsxPropIgnoreCase(openingElement.attributes, "type");
			if (typeAttribute) {
				const typeValue = getJsxPropStringValue(typeAttribute);
				if (typeValue && typeValue.toLowerCase() === "hidden") return false;
			}
			return true;
		}
		case "a":
		case "area": return hasJsxPropIgnoreCase(openingElement.attributes, "href") !== void 0;
		case "img": return hasJsxPropIgnoreCase(openingElement.attributes, "usemap") !== void 0;
		default: return false;
	}
};
//#endregion
//#region src/plugin/utils/parse-jsx-value.ts
const parseJsxValue = (value) => {
	if (!value) return null;
	if (isNodeOfType(value, "Literal")) {
		if (typeof value.value === "number") return value.value;
		if (typeof value.value === "string") {
			const parsed = Number(value.value);
			return Number.isFinite(parsed) ? parsed : null;
		}
	}
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal")) {
			if (typeof expression.value === "number") return expression.value;
			if (typeof expression.value === "string") {
				const parsed = Number(expression.value);
				return Number.isFinite(parsed) ? parsed : null;
			}
		}
		if (isNodeOfType(expression, "UnaryExpression") && expression.operator === "-" && isNodeOfType(expression.argument, "Literal") && typeof expression.argument.value === "number") return -expression.argument.value;
		if (isNodeOfType(expression, "TemplateLiteral")) {
			const staticValue = getStaticTemplateLiteralValue(expression);
			if (staticValue === null) return null;
			const parsed = Number(staticValue);
			return Number.isFinite(parsed) ? parsed : null;
		}
		if (isNodeOfType(expression, "ConditionalExpression")) {
			const wrap = (innerExpression) => ({
				type: "JSXExpressionContainer",
				expression: innerExpression
			});
			if (isNodeOfType(expression.test, "Literal")) return parseJsxValue(wrap(expression.test.value ? expression.consequent : expression.alternate));
			return parseJsxValue(wrap(expression.consequent));
		}
	}
	return null;
};
//#endregion
//#region src/plugin/rules/a11y/aria-activedescendant-has-tabindex.ts
const MESSAGE$50 = "Keyboard users can't focus this element with `aria-activedescendant` because it isn't tabbable, so add `tabIndex={0}`.";
const ariaActivedescendantHasTabindex = defineRule({
	id: "aria-activedescendant-has-tabindex",
	title: "aria-activedescendant missing tabindex",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Add `tabIndex` so keyboard users can reach elements that use `aria-activedescendant`.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!hasJsxPropIgnoreCase(node.attributes, "aria-activedescendant")) return;
		const tag = getElementType(node, context.settings);
		if (!HTML_TAGS.has(tag)) return;
		const tabIndex = hasJsxPropIgnoreCase(node.attributes, "tabIndex");
		if (tabIndex) {
			const tabIndexValue = parseJsxValue(tabIndex.value ?? null);
			if (tabIndexValue === null || tabIndexValue >= -1) return;
			context.report({
				node: node.name,
				message: MESSAGE$50
			});
			return;
		}
		if (isInteractiveElement(tag, node)) return;
		context.report({
			node: node.name,
			message: MESSAGE$50
		});
	} })
});
//#endregion
//#region src/plugin/constants/aria-properties.ts
const ARIA_PROPERTIES = new Map([
	["aria-activedescendant", { type: "id" }],
	["aria-atomic", { type: "boolean" }],
	["aria-autocomplete", {
		type: "token",
		allowedTokens: [
			"inline",
			"list",
			"both",
			"none"
		]
	}],
	["aria-braillelabel", { type: "string" }],
	["aria-brailleroledescription", { type: "string" }],
	["aria-busy", { type: "boolean" }],
	["aria-checked", { type: "tristate" }],
	["aria-colcount", { type: "integer" }],
	["aria-colindex", { type: "integer" }],
	["aria-colspan", { type: "integer" }],
	["aria-controls", { type: "idlist" }],
	["aria-current", {
		type: "token",
		allowedTokens: [
			"page",
			"step",
			"location",
			"date",
			"time",
			"true",
			"false"
		]
	}],
	["aria-describedby", { type: "idlist" }],
	["aria-description", { type: "string" }],
	["aria-details", { type: "id" }],
	["aria-disabled", { type: "boolean" }],
	["aria-dropeffect", {
		type: "tokenlist",
		allowedTokens: [
			"copy",
			"execute",
			"link",
			"move",
			"none",
			"popup"
		]
	}],
	["aria-errormessage", { type: "id" }],
	["aria-expanded", { type: "true-false-undefined" }],
	["aria-flowto", { type: "idlist" }],
	["aria-grabbed", { type: "true-false-undefined" }],
	["aria-haspopup", {
		type: "token",
		allowedTokens: [
			"false",
			"true",
			"menu",
			"listbox",
			"tree",
			"grid",
			"dialog"
		]
	}],
	["aria-hidden", { type: "true-false-undefined" }],
	["aria-invalid", {
		type: "token",
		allowedTokens: [
			"grammar",
			"false",
			"spelling",
			"true"
		]
	}],
	["aria-keyshortcuts", { type: "string" }],
	["aria-label", { type: "string" }],
	["aria-labelledby", { type: "idlist" }],
	["aria-level", { type: "integer" }],
	["aria-live", {
		type: "token",
		allowedTokens: [
			"assertive",
			"off",
			"polite"
		]
	}],
	["aria-modal", { type: "boolean" }],
	["aria-multiline", { type: "boolean" }],
	["aria-multiselectable", { type: "boolean" }],
	["aria-orientation", {
		type: "token",
		allowedTokens: [
			"horizontal",
			"undefined",
			"vertical"
		]
	}],
	["aria-owns", { type: "idlist" }],
	["aria-placeholder", { type: "string" }],
	["aria-posinset", { type: "integer" }],
	["aria-pressed", { type: "tristate" }],
	["aria-readonly", { type: "boolean" }],
	["aria-relevant", {
		type: "tokenlist",
		allowedTokens: [
			"additions",
			"all",
			"removals",
			"text"
		]
	}],
	["aria-required", { type: "boolean" }],
	["aria-roledescription", { type: "string" }],
	["aria-rowcount", { type: "integer" }],
	["aria-rowindex", { type: "integer" }],
	["aria-rowspan", { type: "integer" }],
	["aria-selected", { type: "true-false-undefined" }],
	["aria-setsize", { type: "integer" }],
	["aria-sort", {
		type: "token",
		allowedTokens: [
			"ascending",
			"descending",
			"none",
			"other"
		]
	}],
	["aria-valuemax", { type: "number" }],
	["aria-valuemin", { type: "number" }],
	["aria-valuenow", { type: "number" }],
	["aria-valuetext", { type: "string" }]
]);
const isValidAriaProperty = (name) => ARIA_PROPERTIES.has(name);
//#endregion
//#region src/plugin/rules/a11y/aria-props.ts
const buildMessage$28 = (name) => `Screen reader users get no help from \`${name}\` because it isn't a real ARIA attribute, so fix the spelling against the WAI-ARIA list.`;
const ariaProps = defineRule({
	id: "aria-props",
	title: "Invalid ARIA attribute",
	tags: ["react-jsx-only"],
	severity: "error",
	recommendation: "Only use `aria-*` attributes that actually exist.",
	category: "Accessibility",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		const name = getJsxAttributeName(node.name);
		if (!name || !name.startsWith("aria-")) return;
		if (!isValidAriaProperty(name)) context.report({
			node: node.name,
			message: buildMessage$28(name)
		});
	} })
});
//#endregion
//#region src/plugin/rules/a11y/aria-proptypes.ts
const ARIA_PROP_TYPES = {
	"aria-activedescendant": { kind: "id" },
	"aria-details": { kind: "id" },
	"aria-errormessage": { kind: "id" },
	"aria-atomic": { kind: "boolean" },
	"aria-busy": { kind: "boolean" },
	"aria-disabled": { kind: "boolean" },
	"aria-modal": { kind: "boolean" },
	"aria-multiline": { kind: "boolean" },
	"aria-multiselectable": { kind: "boolean" },
	"aria-readonly": { kind: "boolean" },
	"aria-required": { kind: "boolean" },
	"aria-braillelabel": { kind: "string" },
	"aria-brailleroledescription": { kind: "string" },
	"aria-description": { kind: "string" },
	"aria-keyshortcuts": { kind: "string" },
	"aria-label": { kind: "string" },
	"aria-placeholder": { kind: "string" },
	"aria-roledescription": { kind: "string" },
	"aria-valuetext": { kind: "string" },
	"aria-checked": { kind: "tristate" },
	"aria-pressed": { kind: "tristate" },
	"aria-colcount": { kind: "integer" },
	"aria-colindex": { kind: "integer" },
	"aria-colspan": { kind: "integer" },
	"aria-level": { kind: "integer" },
	"aria-posinset": { kind: "integer" },
	"aria-rowcount": { kind: "integer" },
	"aria-rowindex": { kind: "integer" },
	"aria-rowspan": { kind: "integer" },
	"aria-setsize": { kind: "integer" },
	"aria-controls": { kind: "id-list" },
	"aria-describedby": { kind: "id-list" },
	"aria-flowto": { kind: "id-list" },
	"aria-labelledby": { kind: "id-list" },
	"aria-owns": { kind: "id-list" },
	"aria-expanded": { kind: "optional-boolean" },
	"aria-grabbed": { kind: "optional-boolean" },
	"aria-hidden": { kind: "optional-boolean" },
	"aria-selected": { kind: "optional-boolean" },
	"aria-valuemax": { kind: "number" },
	"aria-valuemin": { kind: "number" },
	"aria-valuenow": { kind: "number" },
	"aria-autocomplete": {
		kind: "token",
		tokens: [
			"none",
			"inline",
			"list",
			"both"
		]
	},
	"aria-current": {
		kind: "token",
		tokens: [
			"page",
			"step",
			"location",
			"date",
			"time",
			"true",
			"false"
		]
	},
	"aria-haspopup": {
		kind: "token",
		tokens: [
			"false",
			"true",
			"menu",
			"listbox",
			"tree",
			"grid",
			"dialog"
		]
	},
	"aria-invalid": {
		kind: "token",
		tokens: [
			"grammar",
			"false",
			"spelling",
			"true"
		]
	},
	"aria-live": {
		kind: "token",
		tokens: [
			"assertive",
			"off",
			"polite"
		]
	},
	"aria-orientation": {
		kind: "token",
		tokens: [
			"horizontal",
			"undefined",
			"vertical"
		]
	},
	"aria-sort": {
		kind: "token",
		tokens: [
			"ascending",
			"descending",
			"none",
			"other"
		]
	},
	"aria-dropeffect": {
		kind: "token-list",
		tokens: [
			"copy",
			"execute",
			"link",
			"move",
			"none",
			"popup"
		]
	},
	"aria-relevant": {
		kind: "token-list",
		tokens: [
			"additions",
			"all",
			"removals",
			"text"
		]
	}
};
const buildExpectedDescription = (propType) => {
	switch (propType.kind) {
		case "boolean":
		case "optional-boolean": return "'true' or 'false'";
		case "tristate": return "'true', 'false', or 'mixed'";
		case "string": return "a string value";
		case "integer": return "an integer value";
		case "number": return "a number value";
		case "id": return "a single element ID";
		case "id-list": return "a space-separated list of element IDs";
		case "token": return `one of: ${propType.tokens.join(", ")}`;
		case "token-list": return `a space-separated list of: ${propType.tokens.join(", ")}`;
	}
};
const buildMessage$27 = (propName, propType) => `Screen reader users get no help from \`${propName}\` because its value isn't readable, so set it to ${buildExpectedDescription(propType)}.`;
const allowNoneValue = (propType) => {
	switch (propType.kind) {
		case "boolean":
		case "optional-boolean":
		case "tristate":
		case "string": return true;
		case "token":
		case "token-list": return propType.tokens.includes("true");
		default: return false;
	}
};
const expressionToBoolean = (expression) => {
	if (isNodeOfType(expression, "Literal")) {
		if (typeof expression.value === "boolean") return expression.value;
		if (typeof expression.value === "string") return expression.value.length > 0;
		if (typeof expression.value === "number") return expression.value !== 0 && !Number.isNaN(expression.value);
		if (expression.value === null) return false;
		return null;
	}
	if (isNodeOfType(expression, "TemplateLiteral")) {
		const staticValue = getStaticTemplateLiteralValue(expression);
		return staticValue === null ? null : staticValue.length > 0;
	}
	if (isNodeOfType(expression, "UnaryExpression") && expression.operator === "!") {
		const inner = expressionToBoolean(expression.argument);
		return inner === null ? null : !inner;
	}
	return null;
};
const isTargetLiteralValue = (value) => {
	if (isNodeOfType(value, "Literal")) return true;
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal")) return expression.value !== null;
		if (isNodeOfType(expression, "TemplateLiteral")) return true;
		if (isNodeOfType(expression, "UnaryExpression") && expression.operator === "!") return expressionToBoolean(expression.argument) !== null;
		return false;
	}
	return false;
};
const parseAriaValueAsString = (value, booleanAsString) => {
	if (isNodeOfType(value, "Literal")) {
		if (typeof value.value === "string") return value.value.toLowerCase();
		return null;
	}
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal")) {
			if (typeof expression.value === "string") return expression.value.toLowerCase();
			if (typeof expression.value === "boolean") return booleanAsString ? String(expression.value) : null;
			return null;
		}
		if (isNodeOfType(expression, "TemplateLiteral")) return getStaticTemplateLiteralValue(expression)?.toLowerCase() ?? null;
		if (booleanAsString && isNodeOfType(expression, "UnaryExpression") && expression.operator === "!") {
			const inner = expressionToBoolean(expression.argument);
			if (inner === null) return null;
			return String(!inner);
		}
	}
	return null;
};
const isMultiQuasiTemplate = (value) => {
	if (!isNodeOfType(value, "JSXExpressionContainer")) return false;
	const expression = value.expression;
	if (!isNodeOfType(expression, "TemplateLiteral")) return false;
	return (expression.expressions ?? []).length > 0;
};
const isValidValueForType = (propType, value) => {
	if (!isTargetLiteralValue(value)) return true;
	switch (propType.kind) {
		case "boolean":
		case "optional-boolean": {
			const stringValue = parseAriaValueAsString(value, true);
			return stringValue === "true" || stringValue === "false";
		}
		case "tristate": {
			const stringValue = parseAriaValueAsString(value, true);
			return stringValue === "true" || stringValue === "false" || stringValue === "mixed";
		}
		case "string":
		case "id":
			if (isMultiQuasiTemplate(value)) return true;
			return parseAriaValueAsString(value, false) !== null;
		case "integer":
		case "number": {
			const stringValue = parseAriaValueAsString(value, false);
			if (stringValue !== null) {
				const parsed = Number(stringValue);
				return Number.isFinite(parsed);
			}
			if (isNodeOfType(value, "JSXExpressionContainer")) {
				const expression = value.expression;
				if (isNodeOfType(expression, "Literal") && typeof expression.value === "number") return true;
				if (isNodeOfType(expression, "UnaryExpression") && (expression.operator === "-" || expression.operator === "+" || expression.operator === "~") && isNodeOfType(expression.argument, "Literal") && typeof expression.argument.value === "number") return true;
			}
			return false;
		}
		case "id-list": {
			if (isMultiQuasiTemplate(value)) return true;
			const stringValue = parseAriaValueAsString(value, false);
			if (stringValue === null) return false;
			return stringValue.trim().length > 0;
		}
		case "token": {
			const stringValue = parseAriaValueAsString(value, true);
			return stringValue !== null && propType.tokens.includes(stringValue);
		}
		case "token-list": {
			const stringValue = parseAriaValueAsString(value, true);
			if (stringValue === null) return false;
			const tokens = stringValue.split(/\s+/).filter((token) => token.length > 0);
			if (tokens.length === 0) return false;
			return tokens.every((token) => propType.tokens.includes(token));
		}
	}
};
const ariaProptypes = defineRule({
	id: "aria-proptypes",
	title: "Invalid ARIA attribute value",
	tags: ["react-jsx-only"],
	severity: "error",
	recommendation: "Give each `aria-*` attribute the kind of value it expects.",
	category: "Accessibility",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		const rawName = getJsxAttributeName(node.name);
		if (!rawName) return;
		const propName = rawName.toLowerCase();
		const propType = ARIA_PROP_TYPES[propName];
		if (!propType) return;
		if (!node.value) {
			if (!allowNoneValue(propType)) context.report({
				node,
				message: buildMessage$27(propName, propType)
			});
			return;
		}
		if (!isValidValueForType(propType, node.value)) context.report({
			node,
			message: buildMessage$27(propName, propType)
		});
	} })
});
//#endregion
//#region src/plugin/constants/aria-roles.ts
const VALID_ARIA_ROLES = new Set([
	"alert",
	"alertdialog",
	"application",
	"article",
	"banner",
	"blockquote",
	"button",
	"caption",
	"cell",
	"checkbox",
	"code",
	"columnheader",
	"combobox",
	"complementary",
	"contentinfo",
	"definition",
	"deletion",
	"dialog",
	"directory",
	"doc-abstract",
	"doc-acknowledgments",
	"doc-afterword",
	"doc-appendix",
	"doc-backlink",
	"doc-biblioentry",
	"doc-bibliography",
	"doc-biblioref",
	"doc-chapter",
	"doc-colophon",
	"doc-conclusion",
	"doc-cover",
	"doc-credit",
	"doc-credits",
	"doc-dedication",
	"doc-endnote",
	"doc-endnotes",
	"doc-epigraph",
	"doc-epilogue",
	"doc-errata",
	"doc-example",
	"doc-footnote",
	"doc-foreword",
	"doc-glossary",
	"doc-glossref",
	"doc-index",
	"doc-introduction",
	"doc-noteref",
	"doc-notice",
	"doc-pagebreak",
	"doc-pagelist",
	"doc-part",
	"doc-preface",
	"doc-prologue",
	"doc-pullquote",
	"doc-qna",
	"doc-subtitle",
	"doc-tip",
	"doc-toc",
	"document",
	"emphasis",
	"feed",
	"figure",
	"form",
	"generic",
	"graphics-document",
	"graphics-object",
	"graphics-symbol",
	"grid",
	"gridcell",
	"group",
	"heading",
	"img",
	"insertion",
	"link",
	"list",
	"listbox",
	"listitem",
	"log",
	"main",
	"mark",
	"marquee",
	"math",
	"menu",
	"menubar",
	"menuitem",
	"menuitemcheckbox",
	"menuitemradio",
	"meter",
	"navigation",
	"none",
	"note",
	"option",
	"paragraph",
	"presentation",
	"progressbar",
	"radio",
	"radiogroup",
	"region",
	"row",
	"rowgroup",
	"rowheader",
	"scrollbar",
	"search",
	"searchbox",
	"separator",
	"slider",
	"spinbutton",
	"status",
	"strong",
	"subscript",
	"superscript",
	"switch",
	"tab",
	"table",
	"tablist",
	"tabpanel",
	"term",
	"textbox",
	"time",
	"timer",
	"toolbar",
	"tooltip",
	"tree",
	"treegrid",
	"treeitem"
]);
const INTERACTIVE_ROLES = new Set([
	"button",
	"checkbox",
	"columnheader",
	"combobox",
	"grid",
	"gridcell",
	"link",
	"listbox",
	"menu",
	"menubar",
	"menuitem",
	"menuitemcheckbox",
	"menuitemradio",
	"option",
	"radio",
	"radiogroup",
	"row",
	"rowheader",
	"scrollbar",
	"searchbox",
	"separator",
	"slider",
	"spinbutton",
	"switch",
	"tab",
	"tablist",
	"textbox",
	"toolbar",
	"tree",
	"treegrid",
	"treeitem"
]);
const NON_INTERACTIVE_ROLES = new Set([
	"alert",
	"alertdialog",
	"application",
	"article",
	"banner",
	"blockquote",
	"caption",
	"cell",
	"complementary",
	"contentinfo",
	"definition",
	"deletion",
	"dialog",
	"directory",
	"document",
	"feed",
	"figure",
	"form",
	"group",
	"heading",
	"img",
	"insertion",
	"list",
	"listitem",
	"log",
	"main",
	"marquee",
	"math",
	"navigation",
	"note",
	"paragraph",
	"progressbar",
	"region",
	"rowgroup",
	"search",
	"status",
	"table",
	"tabpanel",
	"term",
	"time",
	"timer",
	"tooltip"
]);
const ABSTRACT_ROLES = new Set([
	"command",
	"composite",
	"input",
	"landmark",
	"range",
	"roletype",
	"section",
	"sectionhead",
	"select",
	"structure",
	"widget",
	"window"
]);
const PRESENTATION_ROLES$2 = new Set(["presentation", "none"]);
//#endregion
//#region src/plugin/rules/a11y/aria-role.ts
const buildBaseMessage = (suffix) => `Screen reader users get no help from this \`role\` because it isn't a valid ARIA role, so use a real, non-abstract role.${suffix}`;
const resolveSettings$50 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.ariaRole ?? {} : {};
	return {
		allowedInvalidRoles: ruleSettings.allowedInvalidRoles ?? [],
		ignoreNonDOM: ruleSettings.ignoreNonDOM ?? false
	};
};
const ariaRole = defineRule({
	id: "aria-role",
	title: "Invalid ARIA role",
	tags: ["react-jsx-only"],
	severity: "error",
	recommendation: "Use a real, non-abstract ARIA role.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$50(context.settings);
		return { JSXOpeningElement(node) {
			const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
			if (!roleAttribute) return;
			const elementType = getElementType(node, context.settings);
			if (settings.ignoreNonDOM && !HTML_TAGS.has(elementType)) return;
			const value = roleAttribute.value;
			if (!value) {
				context.report({
					node: roleAttribute,
					message: buildBaseMessage("")
				});
				return;
			}
			if (isNodeOfType(value, "Literal")) {
				if (typeof value.value !== "string") {
					context.report({
						node: roleAttribute,
						message: buildBaseMessage("")
					});
					return;
				}
				const stringValue = value.value;
				if (stringValue.trim().length === 0) {
					context.report({
						node: roleAttribute,
						message: buildBaseMessage("")
					});
					return;
				}
				const tokens = stringValue.split(/\s+/).filter((token) => token.length > 0);
				for (const token of tokens) if (!VALID_ARIA_ROLES.has(token) && !settings.allowedInvalidRoles.includes(token)) {
					context.report({
						node: roleAttribute,
						message: buildBaseMessage(` \`${token}\` is not one.`)
					});
					return;
				}
				return;
			}
			if (isNodeOfType(value, "JSXExpressionContainer")) {
				const expression = value.expression;
				if (isNodeOfType(expression, "Literal")) {
					if (expression.value === null) {
						context.report({
							node: roleAttribute,
							message: buildBaseMessage("")
						});
						return;
					}
				}
				if (isNodeOfType(expression, "Identifier") && expression.name === "undefined") {
					context.report({
						node: roleAttribute,
						message: buildBaseMessage("")
					});
					return;
				}
				return;
			}
			context.report({
				node: roleAttribute,
				message: buildBaseMessage("")
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/a11y/aria-unsupported-elements.ts
const buildMessage$26 = (tag, attribute) => `Screen reader users get no help from \`${attribute}\` because \`<${tag}>\` doesn't accept it, so remove it from this element.`;
const ariaUnsupportedElements = defineRule({
	id: "aria-unsupported-elements",
	title: "ARIA on unsupported element",
	tags: ["react-jsx-only"],
	severity: "error",
	recommendation: "Do not put `role` or `aria-*` on reserved HTML elements.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const tag = getElementType(node, context.settings);
		if (!RESERVED_HTML_TAGS.has(tag)) return;
		for (const attribute of node.attributes) {
			if (!isNodeOfType(attribute, "JSXAttribute")) continue;
			if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
			const attrName = getJsxAttributeName(attribute.name);
			if (!attrName) continue;
			if (attrName.startsWith("aria-") || attrName === "role") context.report({
				node: attribute,
				message: buildMessage$26(tag, attrName)
			});
		}
	} })
});
//#endregion
//#region src/plugin/constants/js.ts
const LOOP_TYPES = [
	"ForStatement",
	"ForInStatement",
	"ForOfStatement",
	"WhileStatement",
	"DoWhileStatement"
];
const FUNCTION_LIKE_TYPES$1 = new Set([
	"FunctionDeclaration",
	"FunctionExpression",
	"ArrowFunctionExpression"
]);
const BUILTIN_GLOBAL_NAMESPACE_NAMES = new Set([
	"Math",
	"Date",
	"JSON",
	"Object",
	"Array",
	"Number",
	"String",
	"Boolean",
	"RegExp",
	"Symbol",
	"BigInt",
	"Reflect"
]);
const MUTATING_ARRAY_METHODS = new Set([
	"push",
	"pop",
	"shift",
	"unshift",
	"splice",
	"sort",
	"reverse",
	"fill",
	"copyWithin"
]);
const MUTATING_COLLECTION_METHODS = new Set([
	"add",
	"clear",
	"delete",
	"set"
]);
const CHAINABLE_ITERATION_METHODS = new Set([
	"map",
	"filter",
	"forEach",
	"flatMap"
]);
const ITERATOR_PRODUCING_METHOD_NAMES = new Set([
	"values",
	"keys",
	"entries"
]);
const BROWSER_TEST_FILE_PATTERN = /\.browser\.[cm]?[jt]sx?$/;
const TEST_LIBRARY_IMPORT_SOURCES = new Set([
	"vitest",
	"jest",
	"mocha",
	"chai",
	"sinon",
	"expect",
	"ava",
	"uvu",
	"node:test",
	"bun:test",
	"@testing-library/react",
	"@testing-library/react-native",
	"@testing-library/react-hooks",
	"@testing-library/dom",
	"@testing-library/user-event",
	"@testing-library/jest-dom",
	"@testing-library/vue",
	"@testing-library/svelte",
	"@testing-library/preact",
	"@testing-library/cypress",
	"playwright",
	"playwright-core",
	"@playwright/test",
	"@playwright/experimental-ct-react",
	"@playwright/experimental-ct-react17",
	"cypress",
	"@cypress/react",
	"@cypress/react18",
	"@storybook/test",
	"@storybook/test-runner",
	"@storybook/testing-library",
	"@storybook/jest",
	"puppeteer",
	"puppeteer-core",
	"webdriverio",
	"@wdio/globals",
	"@nuxt/test-utils"
]);
const TEST_LIBRARY_IMPORT_SOURCE_PREFIXES = [
	"vitest/",
	"@vitest/",
	"@jest/",
	"@testing-library/",
	"@playwright/",
	"@storybook/test/",
	"@storybook/test-runner/",
	"@storybook/testing-library/",
	"@cypress/",
	"@nuxt/test-utils/"
];
const ORDERED_UI_FLOW_CALLEE_NAMES = new Set([
	"render",
	"rerender",
	"renderHook",
	"renderToString",
	"renderToStaticMarkup",
	"act",
	"click",
	"dblClick",
	"dblclick",
	"tripleClick",
	"tap",
	"press",
	"longPress",
	"type",
	"clear",
	"fill",
	"focus",
	"blur",
	"hover",
	"unhover",
	"check",
	"uncheck",
	"selectOption",
	"selectOptions",
	"setChecked",
	"setInputFiles",
	"scrollIntoViewIfNeeded",
	"dragTo",
	"dragAndDrop",
	"drop",
	"evaluate",
	"evaluateHandle",
	"waitFor",
	"waitForLoadState",
	"waitForSelector",
	"waitForURL",
	"waitForResponse",
	"waitForRequest",
	"waitForEvent",
	"waitForFunction",
	"waitForElementToBeRemoved",
	"goto",
	"goBack",
	"goForward",
	"reload",
	"screenshot",
	"snapshot",
	"toMatchSnapshot",
	"toMatchInlineSnapshot",
	"expect",
	"expectTypeOf",
	"step",
	"describe",
	"test",
	"it",
	"beforeAll",
	"beforeEach",
	"afterAll",
	"afterEach",
	"play",
	"userEvent",
	"screen",
	"within"
]);
const ORDERED_UI_FLOW_CALLEE_PREFIXES = ["findBy", "findAllBy"];
const INTENTIONAL_SEQUENCING_CALLEE_NAMES = new Set([
	"sleep",
	"delay",
	"wait",
	"pause",
	"throttle",
	"debounce",
	"tick",
	"nextTick",
	"advanceTimersByTime",
	"advanceTimersByTimeAsync",
	"runAllTimers",
	"runAllTimersAsync",
	"runOnlyPendingTimers",
	"runOnlyPendingTimersAsync",
	"setTimeout",
	"setInterval",
	"requestAnimationFrame",
	"requestIdleCallback",
	"animate",
	"transition",
	"spring",
	"tween",
	"stagger",
	"sequence",
	"timeline",
	"scrub",
	"query",
	"execute",
	"exec",
	"raw",
	"transaction",
	"$transaction",
	"$executeRaw",
	"$queryRaw",
	"$executeRawUnsafe",
	"$queryRawUnsafe",
	"begin",
	"commit",
	"rollback",
	"savepoint",
	"lock",
	"unlock",
	"spawn",
	"spawnSync",
	"execSync",
	"execFile",
	"execFileSync",
	"fork",
	"$",
	"sh",
	"mkdir",
	"rmdir",
	"rename",
	"rm",
	"unlink",
	"writeFile",
	"appendFile",
	"copyFile",
	"navigate",
	"goto",
	"waitForNavigation",
	"waitForURL",
	"waitForLoadState",
	"waitForResponse",
	"waitForRequest",
	"waitForSelector",
	"waitForFunction",
	"waitForEvent"
]);
//#endregion
//#region src/plugin/utils/is-function-like.ts
/**
* Type-guard for the three "function-like" ESTree node shapes:
* `ArrowFunctionExpression`, `FunctionExpression`,
* `FunctionDeclaration`. Accepts `null | undefined` so callers
* walking parent chains don't need their own pre-check.
*
* Was duplicated across five sites as both a plain boolean check
* (`FUNCTION_LIKE_TYPES.has(node.type)`) and as a type-guard. The
* type-guard form covers both shapes without callers paying a cast.
*/
const isFunctionLike$2 = (node) => Boolean(node && (isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "FunctionDeclaration")));
//#endregion
//#region src/plugin/utils/is-inline-function-expression.ts
/**
* Type-guard for the two "inline function expression" ESTree forms:
* `FunctionExpression` and `ArrowFunctionExpression`. Differs from
* the canonical `isFunctionLike` (utils/is-function-like.ts) which
* ALSO matches `FunctionDeclaration` — declarations only appear as
* statements, so when callers check positions that can only hold
* expressions (a `VariableDeclarator.init`, a `CallExpression`
* argument, a JSX prop value, an event-handler value), the narrower
* predicate is correct.
*
* Was previously inlined as a 2-type triplet in many rule files;
* this canonical helper lets them all share one type guard.
*/
const isInlineFunctionExpression = (node) => Boolean(node && (isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression")));
//#endregion
//#region src/plugin/rules/js-performance/async-await-in-loop.ts
const findFirstAwaitOutsideNestedFunctions = (block) => {
	let firstAwait = null;
	walkAst(block, (child) => {
		if (firstAwait) return false;
		if (child !== block && isFunctionLike$2(child)) return false;
		if (isNodeOfType(child, "AwaitExpression")) firstAwait = child;
	});
	return firstAwait;
};
const isAwaitingSleepLikeCall = (awaitNode) => {
	if (!isNodeOfType(awaitNode, "AwaitExpression")) return false;
	const argument = awaitNode.argument;
	if (!argument) return false;
	if (!isNodeOfType(argument, "CallExpression")) return false;
	if (isNodeOfType(argument.callee, "Identifier") && INTENTIONAL_SEQUENCING_CALLEE_NAMES.has(argument.callee.name)) return true;
	if (isNodeOfType(argument.callee, "MemberExpression") && isNodeOfType(argument.callee.property, "Identifier") && INTENTIONAL_SEQUENCING_CALLEE_NAMES.has(argument.callee.property.name)) return true;
	return false;
};
const collectPatternIdentifiers = (pattern, target) => {
	if (isNodeOfType(pattern, "Identifier")) target.add(pattern.name);
	else if (isNodeOfType(pattern, "ObjectPattern")) {
		for (const property of pattern.properties ?? []) if (isNodeOfType(property, "Property") && property.value) collectPatternIdentifiers(property.value, target);
		else if (isNodeOfType(property, "RestElement") && property.argument) collectPatternIdentifiers(property.argument, target);
	} else if (isNodeOfType(pattern, "ArrayPattern")) {
		for (const element of pattern.elements ?? []) if (element) collectPatternIdentifiers(element, target);
	} else if (isNodeOfType(pattern, "AssignmentPattern") && pattern.left) collectPatternIdentifiers(pattern.left, target);
};
const collectAssignedIdentifiers = (block) => {
	const assigned = /* @__PURE__ */ new Set();
	walkAst(block, (child) => {
		if (isInlineFunctionExpression(child) || isNodeOfType(child, "FunctionDeclaration")) return false;
		if (isNodeOfType(child, "AssignmentExpression") && child.left) collectPatternIdentifiers(child.left, assigned);
	});
	return assigned;
};
const collectAwaitedArgIdentifiers = (block) => {
	const referenced = /* @__PURE__ */ new Set();
	walkAst(block, (child) => {
		if (isInlineFunctionExpression(child) || isNodeOfType(child, "FunctionDeclaration")) return false;
		if (!isNodeOfType(child, "AwaitExpression") || !child.argument) return;
		walkAst(child.argument, (innerChild) => {
			if (isNodeOfType(innerChild, "Identifier")) referenced.add(innerChild.name);
			if (isNodeOfType(innerChild, "MemberExpression") && isNodeOfType(innerChild.object, "Identifier")) referenced.add(innerChild.object.name);
		});
	});
	return referenced;
};
const hasLoopCarriedDependency = (block) => {
	const assigned = collectAssignedIdentifiers(block);
	if (assigned.size === 0) return false;
	const awaitedReferences = collectAwaitedArgIdentifiers(block);
	for (const name of assigned) if (awaitedReferences.has(name)) return true;
	return false;
};
const loopBodyHasOnlySleepLikeAwaits = (block) => {
	let allAreSleepLike = true;
	let foundAny = false;
	walkAst(block, (child) => {
		if (isInlineFunctionExpression(child) || isNodeOfType(child, "FunctionDeclaration")) return false;
		if (isNodeOfType(child, "AwaitExpression")) {
			foundAny = true;
			if (!isAwaitingSleepLikeCall(child)) allAreSleepLike = false;
		}
	});
	return foundAny && allAreSleepLike;
};
const ITERATION_METHOD_NAMES_WITH_CALLBACK = new Set([
	"forEach",
	"map",
	"filter",
	"reduce",
	"reduceRight",
	"find",
	"findIndex",
	"some",
	"every",
	"flatMap"
]);
const PROMISE_CONCURRENCY_METHODS = new Set([
	"all",
	"allSettled",
	"race",
	"any"
]);
const isWrappedInPromiseConcurrency = (mapCall) => {
	const parent = mapCall.parent;
	if (!isNodeOfType(parent, "CallExpression")) return false;
	if (parent.arguments?.[0] !== mapCall) return false;
	const callee = parent.callee;
	if (!isNodeOfType(callee, "MemberExpression") || callee.computed) return false;
	if (!isNodeOfType(callee.object, "Identifier") || callee.object.name !== "Promise") return false;
	if (!isNodeOfType(callee.property, "Identifier")) return false;
	return PROMISE_CONCURRENCY_METHODS.has(callee.property.name);
};
const asyncAwaitInLoop = defineRule({
	id: "async-await-in-loop",
	title: "await inside a loop",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Collect the items, then use `await Promise.all(items.map(...))` so independent work runs at the same time",
	create: (context) => {
		const inspectLoopBody = (loopBody, label) => {
			if (!loopBody) return;
			if (loopBodyHasOnlySleepLikeAwaits(loopBody)) return;
			if (hasLoopCarriedDependency(loopBody)) return;
			const firstAwait = findFirstAwaitOutsideNestedFunctions(loopBody);
			if (firstAwait) context.report({
				node: firstAwait,
				message: `This makes the ${label} slow because each await runs one after another, so collect the independent calls & run them together with \`await Promise.all(items.map(...))\``
			});
		};
		return {
			ForStatement(node) {
				inspectLoopBody(node.body, "for-loop");
			},
			ForInStatement(node) {
				inspectLoopBody(node.body, "for…in loop");
			},
			ForOfStatement(node) {
				if (node.await) return;
				inspectLoopBody(node.body, "for…of loop");
			},
			WhileStatement(node) {
				inspectLoopBody(node.body, "while-loop");
			},
			DoWhileStatement(node) {
				inspectLoopBody(node.body, "do-while loop");
			},
			CallExpression(node) {
				if (!isNodeOfType(node.callee, "MemberExpression")) return;
				if (!isNodeOfType(node.callee.property, "Identifier")) return;
				const methodName = node.callee.property.name;
				if (!ITERATION_METHOD_NAMES_WITH_CALLBACK.has(methodName)) return;
				const callback = node.arguments?.[0];
				if (!callback || !isInlineFunctionExpression(callback)) return;
				if (!callback.async) return;
				const body = callback.body;
				if (!body) return;
				if ((methodName === "map" || methodName === "flatMap") && isWrappedInPromiseConcurrency(node)) return;
				const firstAwait = findFirstAwaitOutsideNestedFunctions(body);
				if (firstAwait) {
					const message = methodName === "forEach" ? "Async callback in .forEach silently drops every await, so the work never finishes before the loop moves on. Use a `for…of` loop, or `await Promise.all(items.map(async (item) => {...}))`" : `Async callback in .${methodName} runs the awaits one after another, so it is slow. Use \`await Promise.all(items.map(async (item) => {...}))\` to run them at the same time`;
					context.report({
						node: firstAwait,
						message
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/constants/ts-type-position-keys.ts
const TYPE_POSITION_CHILD_KEYS = new Set([
	"implements",
	"returnType",
	"superTypeArguments",
	"superTypeParameters",
	"typeAnnotation",
	"typeArguments",
	"typeParameters"
]);
//#endregion
//#region src/plugin/utils/collect-pattern-names.ts
const collectPatternNames = (pattern, into) => {
	if (!pattern) return;
	if (isNodeOfType(pattern, "Identifier")) {
		into.add(pattern.name);
		return;
	}
	if (isNodeOfType(pattern, "AssignmentPattern")) {
		collectPatternNames(pattern.left, into);
		return;
	}
	if (isNodeOfType(pattern, "RestElement")) {
		collectPatternNames(pattern.argument, into);
		return;
	}
	if (isNodeOfType(pattern, "ArrayPattern")) {
		for (const element of pattern.elements ?? []) collectPatternNames(element, into);
		return;
	}
	if (isNodeOfType(pattern, "ObjectPattern")) for (const property of pattern.properties ?? []) {
		if (isNodeOfType(property, "RestElement")) {
			collectPatternNames(property.argument, into);
			continue;
		}
		if (isNodeOfType(property, "Property")) collectPatternNames(property.value, into);
	}
};
//#endregion
//#region src/plugin/utils/collect-reference-identifier-names.ts
const collectScopedReferencesInPattern = (pattern, into, shadowed) => {
	if (!pattern) return;
	if (isNodeOfType(pattern, "Identifier")) return;
	if (isNodeOfType(pattern, "AssignmentPattern")) {
		collectScopedReferenceIdentifierNames(pattern.right, into, shadowed);
		collectScopedReferencesInPattern(pattern.left, into, shadowed);
		return;
	}
	if (isNodeOfType(pattern, "RestElement")) {
		collectScopedReferencesInPattern(pattern.argument, into, shadowed);
		return;
	}
	if (isNodeOfType(pattern, "ArrayPattern")) {
		for (const element of pattern.elements ?? []) collectScopedReferencesInPattern(element, into, shadowed);
		return;
	}
	if (isNodeOfType(pattern, "ObjectPattern")) for (const property of pattern.properties ?? []) {
		if (isNodeOfType(property, "RestElement")) {
			collectScopedReferencesInPattern(property.argument, into, shadowed);
			continue;
		}
		if (!isNodeOfType(property, "Property")) continue;
		if (property.computed) collectScopedReferenceIdentifierNames(property.key, into, shadowed);
		collectScopedReferencesInPattern(property.value, into, shadowed);
	}
};
const collectFromFunction = (functionNode, into, shadowed) => {
	if (!isNodeOfType(functionNode, "FunctionDeclaration") && !isNodeOfType(functionNode, "FunctionExpression") && !isNodeOfType(functionNode, "ArrowFunctionExpression")) return;
	const innerShadowed = new Set(shadowed);
	for (const param of functionNode.params ?? []) collectPatternNames(param, innerShadowed);
	for (const param of functionNode.params ?? []) collectScopedReferencesInPattern(param, into, innerShadowed);
	collectScopedReferenceIdentifierNames(functionNode.body, into, innerShadowed);
};
const collectScopedReferenceIdentifierNames = (node, into, shadowed) => {
	if (!node) return;
	if (isNodeOfType(node, "Identifier")) {
		if (!shadowed.has(node.name)) into.add(node.name);
		return;
	}
	if (isNodeOfType(node, "MemberExpression")) {
		collectScopedReferenceIdentifierNames(node.object, into, shadowed);
		if (node.computed) collectScopedReferenceIdentifierNames(node.property, into, shadowed);
		return;
	}
	if (isNodeOfType(node, "Property")) {
		if (node.computed) collectScopedReferenceIdentifierNames(node.key, into, shadowed);
		collectScopedReferenceIdentifierNames(node.value, into, shadowed);
		return;
	}
	if (isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression")) {
		collectFromFunction(node, into, shadowed);
		return;
	}
	if (isNodeOfType(node, "TSAsExpression") || isNodeOfType(node, "TSSatisfiesExpression") || isNodeOfType(node, "TSTypeAssertion") || isNodeOfType(node, "TSNonNullExpression") || isNodeOfType(node, "TSInstantiationExpression")) {
		collectScopedReferenceIdentifierNames(node.expression, into, shadowed);
		return;
	}
	if (typeof node.type === "string" && node.type.startsWith("TS")) return;
	for (const [key, child] of Object.entries(node)) {
		if (key === "parent") continue;
		if (TYPE_POSITION_CHILD_KEYS.has(key)) continue;
		if (Array.isArray(child)) {
			for (const item of child) if (isAstNode(item)) collectScopedReferenceIdentifierNames(item, into, shadowed);
		} else if (isAstNode(child)) collectScopedReferenceIdentifierNames(child, into, shadowed);
	}
};
const collectReferenceIdentifierNames = (node, into) => collectScopedReferenceIdentifierNames(node, into, /* @__PURE__ */ new Set());
//#endregion
//#region src/plugin/utils/collect-pattern-default-reference-names.ts
const collectPatternDefaultReferenceNames = (pattern, into) => {
	if (!pattern) return;
	if (isNodeOfType(pattern, "AssignmentPattern")) {
		collectReferenceIdentifierNames(pattern.right, into);
		collectPatternDefaultReferenceNames(pattern.left, into);
		return;
	}
	if (isNodeOfType(pattern, "RestElement")) {
		collectPatternDefaultReferenceNames(pattern.argument, into);
		return;
	}
	if (isNodeOfType(pattern, "ArrayPattern")) {
		for (const element of pattern.elements ?? []) collectPatternDefaultReferenceNames(element, into);
		return;
	}
	if (isNodeOfType(pattern, "ObjectPattern")) {
		for (const property of pattern.properties ?? []) if (isNodeOfType(property, "RestElement")) collectPatternDefaultReferenceNames(property.argument, into);
		else if (isNodeOfType(property, "Property")) {
			if (property.computed) collectReferenceIdentifierNames(property.key, into);
			collectPatternDefaultReferenceNames(property.value, into);
		}
	}
};
//#endregion
//#region src/plugin/utils/contains-direct-await.ts
const containsDirectAwait = (node) => {
	if (!node) return false;
	let foundAwait = false;
	walkAst(node, (child) => {
		if (foundAwait) return false;
		if (isNodeOfType(child, "FunctionDeclaration") || isNodeOfType(child, "FunctionExpression") || isNodeOfType(child, "ArrowFunctionExpression")) return false;
		if (isNodeOfType(child, "AwaitExpression")) {
			foundAwait = true;
			return false;
		}
	});
	return foundAwait;
};
//#endregion
//#region src/plugin/utils/is-bare-await-expression-statement.ts
const isBareAwaitExpressionStatement = (statement) => {
	if (!isNodeOfType(statement, "ExpressionStatement")) return false;
	return isNodeOfType(statement.expression, "AwaitExpression");
};
//#endregion
//#region src/plugin/utils/is-early-exit-if-statement.ts
const isEarlyExitStatement = (statement) => isNodeOfType(statement, "ReturnStatement") || isNodeOfType(statement, "ThrowStatement") || isNodeOfType(statement, "ContinueStatement") || isNodeOfType(statement, "BreakStatement");
const isEarlyExitIfStatement = (statement) => {
	if (!isNodeOfType(statement, "IfStatement")) return false;
	const consequent = statement.consequent;
	if (!consequent) return false;
	if (isEarlyExitStatement(consequent)) return true;
	if (!isNodeOfType(consequent, "BlockStatement")) return false;
	for (const inner of consequent.body ?? []) if (isEarlyExitStatement(inner)) return true;
	return false;
};
//#endregion
//#region src/plugin/rules/performance/async-defer-await.ts
const hasAnyIdentifierName = (identifierNames, candidateNames) => {
	for (const candidateName of candidateNames) if (identifierNames.has(candidateName)) return true;
	return false;
};
const collectDeclaratorDependencyIdentifierNames = (declarator, into) => {
	if (!isNodeOfType(declarator, "VariableDeclarator")) return;
	collectReferenceIdentifierNames(declarator.init, into);
	collectPatternDefaultReferenceNames(declarator.id, into);
};
const processVariableDeclaration = (declaration, awaitedBindingNames) => {
	if (!isNodeOfType(declaration, "VariableDeclaration")) return {
		didIntroduceAwait: false,
		didGrowBindings: false
	};
	let didIntroduceAwait = false;
	const sizeBeforeAll = awaitedBindingNames.size;
	let hasChanged = true;
	while (hasChanged) {
		hasChanged = false;
		for (const declarator of declaration.declarations ?? []) {
			if (!isNodeOfType(declarator, "VariableDeclarator")) continue;
			if (containsDirectAwait(declarator.init) || containsDirectAwait(declarator.id)) {
				didIntroduceAwait = true;
				const sizeBefore = awaitedBindingNames.size;
				collectPatternNames(declarator.id, awaitedBindingNames);
				if (awaitedBindingNames.size > sizeBefore) hasChanged = true;
				continue;
			}
			const dependencyIdentifiers = /* @__PURE__ */ new Set();
			collectDeclaratorDependencyIdentifierNames(declarator, dependencyIdentifiers);
			if (!hasAnyIdentifierName(dependencyIdentifiers, awaitedBindingNames)) continue;
			const sizeBefore = awaitedBindingNames.size;
			collectPatternNames(declarator.id, awaitedBindingNames);
			if (awaitedBindingNames.size > sizeBefore) hasChanged = true;
		}
	}
	const didGrowBindings = awaitedBindingNames.size > sizeBeforeAll;
	return {
		didIntroduceAwait,
		didGrowBindings
	};
};
const CANCELLATION_GUARD_NAMES = new Set([
	"cancelled",
	"canceled",
	"isCancelled",
	"isCanceled",
	"aborted",
	"isAborted",
	"disposed",
	"isDisposed",
	"destroyed",
	"isDestroyed",
	"stopped",
	"isStopped",
	"mounted",
	"isMounted",
	"unmounted",
	"isUnmounted",
	"active",
	"isActive",
	"stale",
	"isStale",
	"signal",
	"abortSignal",
	"abortController"
]);
const isCancellationGuardTest = (test) => {
	if (!test) return false;
	const referenced = /* @__PURE__ */ new Set();
	collectReferenceIdentifierNames(test, referenced);
	if (referenced.size === 0) return false;
	for (const name of referenced) if (CANCELLATION_GUARD_NAMES.has(name)) return true;
	return false;
};
const collectAwaitWindow = (statements, startIndex) => {
	const firstStatement = statements[startIndex];
	const awaitedBindingNames = /* @__PURE__ */ new Set();
	let isAwaitingStatement = false;
	if (isNodeOfType(firstStatement, "VariableDeclaration")) {
		if (processVariableDeclaration(firstStatement, awaitedBindingNames).didIntroduceAwait) isAwaitingStatement = true;
	} else if (isBareAwaitExpressionStatement(firstStatement)) isAwaitingStatement = true;
	if (!isAwaitingStatement) return null;
	let cursor = startIndex + 1;
	while (cursor < statements.length) {
		const candidate = statements[cursor];
		if (isBareAwaitExpressionStatement(candidate)) {
			cursor++;
			continue;
		}
		if (!isNodeOfType(candidate, "VariableDeclaration")) break;
		const result = processVariableDeclaration(candidate, awaitedBindingNames);
		if (!result.didIntroduceAwait && !result.didGrowBindings) break;
		cursor++;
	}
	return {
		firstAwaitStatement: firstStatement,
		awaitedBindingNames,
		guardCandidateIndex: cursor
	};
};
const asyncDeferAwait = defineRule({
	id: "async-defer-await",
	title: "await before an early-return guard",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Move the `await` below the early-return guard so the skip path stays fast",
	create: (context) => {
		const inspectStatements = (statements) => {
			for (let statementIndex = 0; statementIndex < statements.length - 1; statementIndex++) {
				const window = collectAwaitWindow(statements, statementIndex);
				if (!window) continue;
				if (window.guardCandidateIndex >= statements.length) continue;
				const guardStatement = statements[window.guardCandidateIndex];
				if (!isEarlyExitIfStatement(guardStatement)) continue;
				if (!isNodeOfType(guardStatement, "IfStatement")) continue;
				const testIdentifierNames = /* @__PURE__ */ new Set();
				collectReferenceIdentifierNames(guardStatement.test, testIdentifierNames);
				if (hasAnyIdentifierName(testIdentifierNames, window.awaitedBindingNames)) {
					statementIndex = window.guardCandidateIndex - 1;
					continue;
				}
				if (isCancellationGuardTest(guardStatement.test)) {
					statementIndex = window.guardCandidateIndex - 1;
					continue;
				}
				const consequentIdentifierNames = /* @__PURE__ */ new Set();
				collectReferenceIdentifierNames(guardStatement.consequent, consequentIdentifierNames);
				if (hasAnyIdentifierName(consequentIdentifierNames, window.awaitedBindingNames)) {
					statementIndex = window.guardCandidateIndex - 1;
					continue;
				}
				context.report({
					node: window.firstAwaitStatement,
					message: "This await blocks the function before an early-return that doesn't use the awaited value, so the skip path waits for nothing. Move the await below the guard so it only runs when you need the data"
				});
				statementIndex = window.guardCandidateIndex - 1;
			}
		};
		const inspectAllStatementBlocks = (functionBody) => {
			if (!functionBody) return;
			walkAst(functionBody, (descendant) => {
				if (isFunctionLike$2(descendant)) return false;
				if (isNodeOfType(descendant, "BlockStatement")) inspectStatements(descendant.body ?? []);
				else if (isNodeOfType(descendant, "SwitchCase")) inspectStatements(descendant.consequent ?? []);
			});
		};
		const enterFunction = (node) => {
			if (!isFunctionLike$2(node)) return;
			if (!node.async) return;
			if (!isNodeOfType(node.body, "BlockStatement")) return;
			inspectAllStatementBlocks(node.body);
		};
		return {
			FunctionDeclaration: enterFunction,
			FunctionExpression: enterFunction,
			ArrowFunctionExpression: enterFunction
		};
	}
});
//#endregion
//#region src/plugin/utils/get-callee-identifier-trail.ts
const getCalleeIdentifierTrail = (call) => {
	let entry = call;
	while (isNodeOfType(entry, "ChainExpression")) entry = entry.expression;
	if (!isNodeOfType(entry, "CallExpression") && !isNodeOfType(entry, "NewExpression")) return [];
	const trail = [];
	let cursor = entry.callee;
	while (cursor) {
		if (isNodeOfType(cursor, "ChainExpression")) {
			cursor = cursor.expression;
			continue;
		}
		if (isNodeOfType(cursor, "MemberExpression")) {
			if (isNodeOfType(cursor.property, "Identifier")) trail.push(cursor.property.name);
			cursor = cursor.object;
			continue;
		}
		if (isNodeOfType(cursor, "CallExpression")) {
			cursor = cursor.callee;
			continue;
		}
		if (isNodeOfType(cursor, "Identifier")) trail.push(cursor.name);
		break;
	}
	return trail;
};
//#endregion
//#region src/plugin/utils/is-test-library-import-source.ts
const isTestLibraryImportSource = (source) => {
	if (typeof source !== "string" || source.length === 0) return false;
	if (TEST_LIBRARY_IMPORT_SOURCES.has(source)) return true;
	return TEST_LIBRARY_IMPORT_SOURCE_PREFIXES.some((prefix) => source.startsWith(prefix));
};
//#endregion
//#region src/plugin/rules/js-performance/async-parallel.ts
const getAwaitedCall = (statement) => {
	if (isNodeOfType(statement, "VariableDeclaration")) {
		const declarator = statement.declarations?.[0];
		if (declarator && isNodeOfType(declarator.init, "AwaitExpression")) return declarator.init.argument ?? null;
	}
	if (isNodeOfType(statement, "ExpressionStatement") && isNodeOfType(statement.expression, "AwaitExpression")) return statement.expression.argument ?? null;
	return null;
};
const isOrderedUiFlowName = (name) => {
	if (ORDERED_UI_FLOW_CALLEE_NAMES.has(name)) return true;
	return ORDERED_UI_FLOW_CALLEE_PREFIXES.some((prefix) => name.startsWith(prefix));
};
const isOrderedUiFlowAwait = (awaitedCall) => {
	if (!awaitedCall) return false;
	return getCalleeIdentifierTrail(awaitedCall).some(isOrderedUiFlowName);
};
const isIntentionalSequencingAwait = (awaitedCall) => {
	if (!awaitedCall) return false;
	return getCalleeIdentifierTrail(awaitedCall).some((name) => INTENTIONAL_SEQUENCING_CALLEE_NAMES.has(name));
};
const sequenceContainsSerializationSignal = (statements) => {
	for (const statement of statements) {
		const awaitedCall = getAwaitedCall(statement);
		if (isOrderedUiFlowAwait(awaitedCall)) return true;
		if (isIntentionalSequencingAwait(awaitedCall)) return true;
	}
	return false;
};
const reportIfIndependent = (statements, context) => {
	const declaredNames = /* @__PURE__ */ new Set();
	for (const statement of statements) {
		if (!isNodeOfType(statement, "VariableDeclaration")) continue;
		const declarator = statement.declarations[0];
		if (!isNodeOfType(declarator.init, "AwaitExpression")) continue;
		const awaitArgument = declarator.init.argument;
		let referencesEarlierResult = false;
		walkAst(awaitArgument, (child) => {
			if (isNodeOfType(child, "Identifier") && declaredNames.has(child.name)) referencesEarlierResult = true;
		});
		if (referencesEarlierResult) return;
		if (isNodeOfType(declarator.id, "Identifier")) declaredNames.add(declarator.id.name);
	}
	context.report({
		node: statements[0],
		message: `These ${statements.length} sequential await statements run one after another even though they look independent, so the page waits longer than it needs to. Run them together with Promise.all() instead`
	});
};
const asyncParallel = defineRule({
	id: "async-parallel",
	title: "Independent awaits run sequentially",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use `const [a, b] = await Promise.all([fetchA(), fetchB()])` so independent calls run at the same time",
	create: (context) => {
		const filename = normalizeFilename$1(context.filename ?? "");
		const isBrowserTestFile = BROWSER_TEST_FILE_PATTERN.test(filename);
		let hasTestLibraryImport = false;
		const shouldSkipFile = () => isBrowserTestFile || hasTestLibraryImport;
		return {
			ImportDeclaration(node) {
				if (hasTestLibraryImport) return;
				if (isTestLibraryImportSource(node.source?.value)) hasTestLibraryImport = true;
			},
			BlockStatement(node) {
				if (shouldSkipFile()) return;
				const consecutiveAwaitStatements = [];
				const flushConsecutiveAwaits = () => {
					if (consecutiveAwaitStatements.length >= 3) {
						if (!sequenceContainsSerializationSignal(consecutiveAwaitStatements)) reportIfIndependent(consecutiveAwaitStatements, context);
					}
					consecutiveAwaitStatements.length = 0;
				};
				for (const statement of node.body ?? []) if (isNodeOfType(statement, "VariableDeclaration") && statement.declarations?.length === 1 && isNodeOfType(statement.declarations[0].init, "AwaitExpression") || isNodeOfType(statement, "ExpressionStatement") && isNodeOfType(statement.expression, "AwaitExpression")) consecutiveAwaitStatements.push(statement);
				else flushConsecutiveAwaits();
				flushConsecutiveAwaits();
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/a11y/autocomplete-valid.ts
const buildMessage$25 = (value) => `Users who rely on autofill can't fill this field because \`${value}\` isn't a known token, so use a valid \`autoComplete\` token.`;
const AUTOFILL_TOKENS = new Set([
	"off",
	"on",
	"name",
	"honorific-prefix",
	"given-name",
	"additional-name",
	"family-name",
	"honorific-suffix",
	"nickname",
	"email",
	"username",
	"new-password",
	"current-password",
	"one-time-code",
	"organization-title",
	"organization",
	"street-address",
	"address-line1",
	"address-line2",
	"address-line3",
	"address-level4",
	"address-level3",
	"address-level2",
	"address-level1",
	"country",
	"country-name",
	"postal-code",
	"cc-name",
	"cc-given-name",
	"cc-additional-name",
	"cc-family-name",
	"cc-number",
	"cc-exp",
	"cc-exp-month",
	"cc-exp-year",
	"cc-csc",
	"cc-type",
	"transaction-currency",
	"transaction-amount",
	"language",
	"bday",
	"bday-day",
	"bday-month",
	"bday-year",
	"sex",
	"tel",
	"tel-country-code",
	"tel-national",
	"tel-area-code",
	"tel-local",
	"tel-extension",
	"impp",
	"url",
	"photo"
]);
const FORM_CONTROL_TAGS = new Set([
	"input",
	"textarea",
	"select",
	"form"
]);
const resolveSettings$49 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { inputComponents: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.autocompleteValid ?? {} : {}).inputComponents ?? [] };
};
const autocompleteValid = defineRule({
	id: "autocomplete-valid",
	title: "Invalid autocomplete value",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Use a valid autofill token in `autoComplete`.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$49(context.settings);
		return { JSXOpeningElement: (node) => {
			const tag = getElementType(node, context.settings);
			if (!FORM_CONTROL_TAGS.has(tag) && !settings.inputComponents.includes(tag)) return;
			const attribute = hasJsxPropIgnoreCase(node.attributes, "autoComplete");
			if (!attribute) return;
			const value = getJsxPropStringValue(attribute);
			if (!value) return;
			const tokens = value.toLowerCase().split(/\s+/).filter((token) => token.length > 0);
			for (const token of tokens) {
				if ((token.startsWith("section-") ? "section-" : token) === "section-") continue;
				if (!AUTOFILL_TOKENS.has(token)) {
					context.report({
						node: attribute,
						message: buildMessage$25(value)
					});
					return;
				}
			}
		} };
	}
});
//#endregion
//#region src/plugin/utils/is-create-element-call.ts
const memberChainContainsDocument = (memberExpression) => {
	let current = memberExpression;
	while (current) {
		if (isNodeOfType(current, "MemberExpression")) {
			if (isNodeOfType(current.property, "Identifier") && current.property.name === "document") return true;
			if (isNodeOfType(current.object, "Identifier") && current.object.name === "document") return true;
			current = current.object;
			continue;
		}
		if (isNodeOfType(current, "Identifier") && current.name === "document") return true;
		return false;
	}
	return false;
};
const isCreateElementCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	const callee = node.callee;
	if (isNodeOfType(callee, "Identifier")) return callee.name === "createElement";
	if (isNodeOfType(callee, "MemberExpression")) {
		if (memberChainContainsDocument(callee.object)) return false;
		if (callee.computed) return isNodeOfType(callee.property, "Literal") && callee.property.value === "createElement";
		return isNodeOfType(callee.property, "Identifier") && callee.property.name === "createElement";
	}
	return false;
};
//#endregion
//#region src/plugin/rules/react-builtins/button-has-type.ts
const MISSING_MESSAGE$2 = "Your users can submit the form by accident because a `<button>` with no `type` defaults to submit.";
const INVALID_MESSAGE = "This button's `type` is invalid.";
const resolveSettings$48 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.buttonHasType ?? {} : {};
	return {
		button: ruleSettings.button ?? true,
		submit: ruleSettings.submit ?? true,
		reset: ruleSettings.reset ?? true
	};
};
const isValidTypeValue = (rawValue, settings) => {
	if (rawValue === "button") return settings.button;
	if (rawValue === "submit") return settings.submit;
	if (rawValue === "reset") return settings.reset;
	return false;
};
const isProvenValidExpression = (expression, settings) => {
	if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") return isValidTypeValue(expression.value, settings);
	if (isNodeOfType(expression, "TemplateLiteral")) {
		const staticValue = getStaticTemplateLiteralValue(expression);
		if (staticValue !== null) return isValidTypeValue(staticValue, settings);
	}
	if (isNodeOfType(expression, "ConditionalExpression")) return isProvenValidExpression(expression.consequent, settings) && isProvenValidExpression(expression.alternate, settings);
	return false;
};
const isConsumerPropForward = (expression) => {
	if (isNodeOfType(expression, "Identifier") && expression.name === "type") return true;
	if (isNodeOfType(expression, "MemberExpression") && !expression.computed && isNodeOfType(expression.property, "Identifier") && expression.property.name === "type") return true;
	if (isNodeOfType(expression, "LogicalExpression") && (expression.operator === "??" || expression.operator === "||")) return isConsumerPropForward(expression.left);
	return false;
};
const reportInvalid = (context, reportNode) => {
	context.report({
		node: reportNode,
		message: INVALID_MESSAGE
	});
};
const buttonHasType = defineRule({
	id: "button-has-type",
	title: "Button missing explicit type",
	severity: "warn",
	recommendation: "Always set a `type` on a `<button>`: `type=\"button\"`, `\"submit\"`, or `\"reset\"`.",
	create: (context) => {
		const settings = resolveSettings$48(context.settings);
		const isTestlikeFile = isTestlikeFilename(context.filename);
		return {
			JSXOpeningElement(node) {
				if (isTestlikeFile) return;
				if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "button") return;
				const typeAttr = hasJsxPropIgnoreCase(node.attributes, "type");
				if (!typeAttr) {
					context.report({
						node: node.name,
						message: MISSING_MESSAGE$2
					});
					return;
				}
				const value = typeAttr.value;
				if (!value) {
					reportInvalid(context, typeAttr);
					return;
				}
				if (isNodeOfType(value, "Literal")) {
					if (!isProvenValidExpression(value, settings)) reportInvalid(context, typeAttr);
					return;
				}
				if (isNodeOfType(value, "JSXExpressionContainer")) {
					const expression = value.expression;
					if (!expression || expression.type === "JSXEmptyExpression") return;
					if (isConsumerPropForward(expression)) return;
					if (!isProvenValidExpression(expression, settings)) reportInvalid(context, typeAttr);
				}
			},
			CallExpression(node) {
				if (isTestlikeFile) return;
				if (!isCreateElementCall(node)) return;
				const firstArgument = node.arguments[0];
				if (!firstArgument || !isNodeOfType(firstArgument, "Literal") || firstArgument.value !== "button") return;
				const propsArgument = node.arguments[1];
				if (!propsArgument || !isNodeOfType(propsArgument, "ObjectExpression")) {
					context.report({
						node,
						message: MISSING_MESSAGE$2
					});
					return;
				}
				let typeProp = null;
				for (const property of propsArgument.properties) {
					if (!isNodeOfType(property, "Property")) continue;
					const propertyKey = property.key;
					if (isNodeOfType(propertyKey, "Identifier") && propertyKey.name === "type" || isNodeOfType(propertyKey, "Literal") && propertyKey.value === "type") {
						typeProp = property.value;
						break;
					}
				}
				if (!typeProp) {
					context.report({
						node: propsArgument,
						message: MISSING_MESSAGE$2
					});
					return;
				}
				if (isConsumerPropForward(typeProp)) return;
				if (!isProvenValidExpression(typeProp, settings)) reportInvalid(context, typeProp);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/checked-requires-onchange-or-readonly.ts
const MISSING_MESSAGE$1 = "Your users can't toggle this input because `checked` has no `onChange`.";
const EXCLUSIVE_MESSAGE = "This input behaves unpredictably with both `checked` & `defaultChecked` set.";
const resolveSettings$47 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.checkedRequiresOnchangeOrReadonly ?? {} : {};
	return {
		ignoreMissingProperties: ruleSettings.ignoreMissingProperties ?? false,
		ignoreExclusiveCheckedAttribute: ruleSettings.ignoreExclusiveCheckedAttribute ?? false
	};
};
const collectFromJsxAttributes = (attributes) => {
	let checkedNode = null;
	let defaultCheckedNode = null;
	let hasOnChangeOrReadOnly = false;
	for (const attribute of attributes) {
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		const name = getJsxAttributeName(attribute.name);
		if (name === "checked") checkedNode = attribute;
		else if (name === "defaultChecked" && !defaultCheckedNode) defaultCheckedNode = attribute;
		else if (name === "onChange" || name === "readOnly") hasOnChangeOrReadOnly = true;
	}
	return {
		checkedNode,
		defaultCheckedNode,
		hasOnChangeOrReadOnly
	};
};
const collectFromObjectProperties = (objectExpression) => {
	let checkedNode = null;
	let defaultCheckedNode = null;
	let hasOnChangeOrReadOnly = false;
	for (const property of objectExpression.properties) {
		if (!isNodeOfType(property, "Property")) continue;
		const key = property.key;
		let propertyName = null;
		if (isNodeOfType(key, "Identifier")) propertyName = key.name;
		else if (isNodeOfType(key, "Literal") && typeof key.value === "string") propertyName = key.value;
		if (!propertyName) continue;
		if (propertyName === "checked") checkedNode = property;
		else if (propertyName === "defaultChecked" && !defaultCheckedNode) defaultCheckedNode = property;
		else if (propertyName === "onChange" || propertyName === "readOnly") hasOnChangeOrReadOnly = true;
	}
	return {
		checkedNode,
		defaultCheckedNode,
		hasOnChangeOrReadOnly
	};
};
const checkedRequiresOnchangeOrReadonly = defineRule({
	id: "checked-requires-onchange-or-readonly",
	title: "Checked input without onChange",
	severity: "warn",
	recommendation: "Add `onChange` (controlled) or `readOnly` (display-only), or use `defaultChecked` for an uncontrolled checkbox.",
	category: "Correctness",
	create: (context) => {
		const settings = resolveSettings$47(context.settings);
		const reportFromPresence = (presence) => {
			if (presence.checkedNode && presence.defaultCheckedNode && !settings.ignoreExclusiveCheckedAttribute) context.report({
				node: presence.checkedNode,
				message: EXCLUSIVE_MESSAGE
			});
			if (presence.checkedNode && !presence.hasOnChangeOrReadOnly && !settings.ignoreMissingProperties) context.report({
				node: presence.checkedNode,
				message: MISSING_MESSAGE$1
			});
		};
		return {
			JSXOpeningElement(node) {
				if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "input") return;
				reportFromPresence(collectFromJsxAttributes(node.attributes));
			},
			CallExpression(node) {
				if (!isCreateElementCall(node)) return;
				const firstArgument = node.arguments[0];
				if (!firstArgument) return;
				if (!isNodeOfType(firstArgument, "Literal") || firstArgument.value !== "input") return;
				const propsArgument = node.arguments[1];
				if (!propsArgument || !isNodeOfType(propsArgument, "ObjectExpression")) return;
				reportFromPresence(collectFromObjectProperties(propsArgument));
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/is-pure-event-blocker-handler.ts
const BLOCKER_METHOD_NAMES = new Set([
	"stopPropagation",
	"preventDefault",
	"stopImmediatePropagation"
]);
const isEventBlockerCall = (node) => {
	if (!node) return false;
	const inner = isNodeOfType(node, "ChainExpression") ? node.expression : node;
	if (!isNodeOfType(inner, "CallExpression")) return false;
	const callee = inner.callee;
	if (!isNodeOfType(callee, "MemberExpression")) return false;
	if (!isNodeOfType(callee.property, "Identifier")) return false;
	return BLOCKER_METHOD_NAMES.has(callee.property.name);
};
const isPureEventBlockerBody = (body) => {
	if (!body) return false;
	if (isEventBlockerCall(body)) return true;
	if (isNodeOfType(body, "BlockStatement")) {
		const statements = body.body ?? [];
		if (statements.length === 0) return false;
		for (const statement of statements) {
			if (!isNodeOfType(statement, "ExpressionStatement")) return false;
			if (!isEventBlockerCall(statement.expression)) return false;
		}
		return true;
	}
	return false;
};
const isPureEventBlockerHandler = (attribute) => {
	if (!attribute.value || !isNodeOfType(attribute.value, "JSXExpressionContainer")) return false;
	const expression = attribute.value.expression;
	if (isNodeOfType(expression, "ArrowFunctionExpression") || isNodeOfType(expression, "FunctionExpression")) return isPureEventBlockerBody(expression.body);
	return false;
};
//#endregion
//#region src/plugin/rules/a11y/click-events-have-key-events.ts
const PRESENTATION_ROLES$1 = new Set(["presentation", "none"]);
const MESSAGE$49 = "Keyboard users can't trigger this click handler because there's no keyboard one, so add `onKeyUp`, `onKeyDown`, or `onKeyPress`.";
const KEY_HANDLERS = [
	"onKeyUp",
	"onKeyDown",
	"onKeyPress"
];
const clickEventsHaveKeyEvents = defineRule({
	id: "click-events-have-key-events",
	title: "Click handler missing keyboard handler",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Pair `onClick` with a key handler so keyboard users can trigger it.",
	category: "Accessibility",
	create: (context) => {
		const isTestlikeFile = isTestlikeFilename(context.filename);
		return { JSXOpeningElement(node) {
			if (isTestlikeFile) return;
			const tag = getElementType(node, context.settings);
			if (!HTML_TAGS.has(tag)) return;
			if (isInteractiveElement(tag, node)) return;
			const onClick = hasJsxPropIgnoreCase(node.attributes, "onClick");
			if (!onClick) return;
			if (isPureEventBlockerHandler(onClick)) return;
			if (isHiddenFromScreenReader(node, context.settings)) return;
			const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
			if (roleAttribute) {
				const roleValue = getJsxPropStringValue(roleAttribute);
				if (roleValue && PRESENTATION_ROLES$1.has(roleValue)) return;
			}
			if (KEY_HANDLERS.some((handler) => hasJsxPropIgnoreCase(node.attributes, handler))) return;
			context.report({
				node: node.name,
				message: MESSAGE$49
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/client/client-localstorage-no-version.ts
const VERSIONED_KEY_PATTERN = /(?:[._:-]v\d+|@\d+|\bv\d+\b)/i;
const STORAGE_OBJECTS = new Set(["localStorage", "sessionStorage"]);
const isJsonStringifyCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (!isNodeOfType(node.callee, "MemberExpression")) return false;
	if (!isNodeOfType(node.callee.object, "Identifier")) return false;
	if (node.callee.object.name !== "JSON") return false;
	if (!isNodeOfType(node.callee.property, "Identifier")) return false;
	return node.callee.property.name === "stringify";
};
const clientLocalstorageNoVersion = defineRule({
	id: "client-localstorage-no-version",
	title: "Unversioned localStorage key",
	tags: ["test-noise"],
	severity: "warn",
	category: "Correctness",
	recommendation: "Put a version in the storage key (e.g. \"myKey:v1\"). If you change the data shape later, old saved data can be ignored instead of crashing the app.",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		if (!isNodeOfType(node.callee.object, "Identifier")) return;
		if (!STORAGE_OBJECTS.has(node.callee.object.name)) return;
		if (!isNodeOfType(node.callee.property, "Identifier")) return;
		if (node.callee.property.name !== "setItem") return;
		const keyArg = node.arguments?.[0];
		if (!keyArg) return;
		if (!isNodeOfType(keyArg, "Literal")) return;
		if (typeof keyArg.value !== "string") return;
		if (VERSIONED_KEY_PATTERN.test(keyArg.value)) return;
		const valueArg = node.arguments?.[1];
		if (!valueArg) return;
		if (!isJsonStringifyCall(valueArg)) return;
		context.report({
			node: keyArg,
			message: `${node.callee.object.name}.setItem("${keyArg.value}", JSON.stringify(...)) has no version, so changing the data shape later crashes your users' saved sessions. Add one to the key (e.g. "${keyArg.value}:v1").`
		});
	} })
});
//#endregion
//#region src/plugin/utils/is-member-property.ts
const isMemberProperty = (node, propertyName) => Boolean(node && isNodeOfType(node, "MemberExpression") && isNodeOfType(node.property, "Identifier") && node.property.name === propertyName);
//#endregion
//#region src/plugin/rules/client/client-passive-event-listeners.ts
const clientPassiveEventListeners = defineRule({
	id: "client-passive-event-listeners",
	title: "Non-passive scroll listener",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Add `{ passive: true }` as the third argument: `addEventListener('scroll', handler, { passive: true })`. Only do this if the handler doesn't call `event.preventDefault()`, since passive listeners ignore it (which breaks pull-to-refresh, custom gestures, and nested scrolling).",
	create: (context) => ({ CallExpression(node) {
		if (!isMemberProperty(node.callee, "addEventListener")) return;
		if ((node.arguments?.length ?? 0) < 2) return;
		const eventNameNode = node.arguments[0];
		if (!isNodeOfType(eventNameNode, "Literal") || typeof eventNameNode.value !== "string" || !PASSIVE_EVENT_NAMES.has(eventNameNode.value)) return;
		const eventName = eventNameNode.value;
		const optionsArgument = node.arguments[2];
		if (!optionsArgument) {
			context.report({
				node,
				message: `"${eventName}" listener without { passive: true } makes scrolling janky for your users. Only add it if the handler doesn't call event.preventDefault(), since passive listeners silently ignore preventDefault().`
			});
			return;
		}
		if (!isNodeOfType(optionsArgument, "ObjectExpression")) return;
		if (!optionsArgument.properties?.some((property) => isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "passive" && isNodeOfType(property.value, "Literal") && property.value.value === true)) context.report({
			node,
			message: `"${eventName}" listener without { passive: true } makes scrolling janky for your users. Only add it if the handler doesn't call event.preventDefault(), since passive listeners silently ignore preventDefault().`
		});
	} })
});
//#endregion
//#region src/plugin/utils/is-interactive-role.ts
const isInteractiveRole = (role) => INTERACTIVE_ROLES.has(role);
//#endregion
//#region src/plugin/utils/is-react-component-name.ts
const isReactComponentName = (name) => {
	if (name.length === 0) return false;
	const firstCharacter = name.charCodeAt(0);
	return firstCharacter >= 65 && firstCharacter <= 90;
};
//#endregion
//#region src/plugin/utils/strip-paren-expression.ts
const TS_WRAPPER_TYPES = new Set([
	"ParenthesizedExpression",
	"TSAsExpression",
	"TSSatisfiesExpression",
	"TSTypeAssertion",
	"TSNonNullExpression",
	"TSInstantiationExpression"
]);
const stripParenExpression = (node) => {
	let current = node;
	while (true) {
		if (TS_WRAPPER_TYPES.has(current.type) && "expression" in current && current.expression) {
			current = current.expression;
			continue;
		}
		if (isNodeOfType(current, "ChainExpression") && current.expression) {
			current = current.expression;
			continue;
		}
		break;
	}
	return current;
};
//#endregion
//#region src/plugin/rules/a11y/control-has-associated-label.ts
const MESSAGE$48 = "Blind users can't tell what this control does because screen readers find no label, so add visible text, `aria-label`, or `aria-labelledby`.";
const DEFAULT_IGNORE_ELEMENTS = ["link", "canvas"];
const DEFAULT_LABELLING_PROPS = [
	"alt",
	"aria-label",
	"aria-labelledby"
];
const ID_ATTRIBUTE = "id";
const HTML_FOR_ATTRIBUTE = "htmlFor";
const LABEL_ELEMENT = "label";
const DEFAULT_DEPTH = 5;
const MAX_DEPTH = 25;
const resolveSettings$46 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.controlHasAssociatedLabel ?? {} : {};
	return {
		depth: Math.min(ruleSettings.depth ?? DEFAULT_DEPTH, MAX_DEPTH),
		labelAttributes: ruleSettings.labelAttributes ?? [],
		controlComponents: ruleSettings.controlComponents ?? [],
		ignoreElements: ruleSettings.ignoreElements ?? [],
		ignoreRoles: ruleSettings.ignoreRoles ?? []
	};
};
const hasLabellingProp = (attributes, customAttributes) => {
	for (const attribute of attributes) {
		if (isNodeOfType(attribute, "JSXSpreadAttribute")) return true;
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
		const propName = getJsxAttributeName(attribute.name);
		if (!propName) continue;
		if (!(DEFAULT_LABELLING_PROPS.includes(propName) || customAttributes.includes(propName))) continue;
		if (!attribute.value) return false;
		if (isNodeOfType(attribute.value, "Literal") && typeof attribute.value.value === "string") return attribute.value.value.trim().length > 0;
		return true;
	}
	return false;
};
const toAttributeMatchKey = (kind, value) => {
	const trimmedValue = value.trim();
	return trimmedValue.length > 0 ? `${kind}:${trimmedValue}` : null;
};
const getLiteralAttributeMatchKey = (value) => {
	if (typeof value === "string") return toAttributeMatchKey("literal", value);
	if (typeof value === "number") return toAttributeMatchKey("literal", String(value));
	return null;
};
const getAttributeMatchKey = (attribute) => {
	if (!attribute?.value) return null;
	const value = attribute.value;
	if (isNodeOfType(value, "Literal")) return getLiteralAttributeMatchKey(value.value);
	if (!isNodeOfType(value, "JSXExpressionContainer")) return null;
	const expression = value.expression;
	if (isNodeOfType(expression, "Identifier")) return toAttributeMatchKey("identifier", expression.name);
	if (isNodeOfType(expression, "Literal")) return getLiteralAttributeMatchKey(expression.value);
	if (isNodeOfType(expression, "TemplateLiteral")) {
		const staticValue = getStaticTemplateLiteralValue(expression);
		return staticValue === null ? null : toAttributeMatchKey("literal", staticValue);
	}
	return null;
};
const checkChildForLabel = (child, currentDepth, context) => {
	if (currentDepth > context.depth) return false;
	if (isNodeOfType(child, "JSXExpressionContainer")) return true;
	if (isNodeOfType(child, "JSXText")) return child.value.trim().length > 0;
	if (isNodeOfType(child, "JSXFragment")) return child.children.some((nestedChild) => checkChildForLabel(nestedChild, currentDepth + 1, context));
	if (isNodeOfType(child, "JSXElement")) {
		if (hasLabellingProp(child.openingElement.attributes, context.customAttributes)) return true;
		if (child.children.length === 0) {
			const tagName = getElementType(child.openingElement, context.settings);
			if (isReactComponentName(tagName) && !context.controlComponents.includes(tagName)) return true;
		}
		for (const nestedChild of child.children) if (checkChildForLabel(nestedChild, currentDepth + 1, context)) return true;
	}
	return false;
};
const hasAccessibleLabelText = (element, context) => {
	if (hasLabellingProp(element.openingElement.attributes, context.customAttributes)) return true;
	return element.children.some((child) => checkChildForLabel(child, 1, context));
};
const isFunctionBoundary = (node) => isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "FunctionDeclaration");
const hasAncestorLabel = (element, context) => {
	let current = element.parent;
	while (current) {
		if (isFunctionBoundary(current)) break;
		if (isNodeOfType(current, "JSXElement")) {
			if (getElementType(current.openingElement, context.settings) === LABEL_ELEMENT && hasAccessibleLabelText(current, context)) return true;
		}
		current = current.parent ?? null;
	}
	return false;
};
const findEnclosingJsxTreeRoot = (element) => {
	let root = element;
	let current = element.parent;
	while (current) {
		if (isFunctionBoundary(current)) break;
		if (isNodeOfType(current, "JSXElement") || isNodeOfType(current, "JSXFragment")) root = current;
		current = current.parent ?? null;
	}
	return root;
};
const collectJsxFromExpression = (rawExpression) => {
	const expression = stripParenExpression(rawExpression);
	if (isNodeOfType(expression, "JSXElement") || isNodeOfType(expression, "JSXFragment")) return [expression];
	if (isNodeOfType(expression, "LogicalExpression")) return [...collectJsxFromExpression(expression.left), ...collectJsxFromExpression(expression.right)];
	if (isNodeOfType(expression, "ConditionalExpression")) return [...collectJsxFromExpression(expression.consequent), ...collectJsxFromExpression(expression.alternate)];
	return [];
};
const searchForHtmlForLabel = (node, controlIdKey, context) => {
	if (isNodeOfType(node, "JSXExpressionContainer")) return collectJsxFromExpression(node.expression).some((jsxNode) => searchForHtmlForLabel(jsxNode, controlIdKey, context));
	const children = isNodeOfType(node, "JSXElement") || isNodeOfType(node, "JSXFragment") ? node.children : [];
	if (isNodeOfType(node, "JSXElement")) {
		if (getElementType(node.openingElement, context.settings) === LABEL_ELEMENT) {
			if (getAttributeMatchKey(hasJsxPropIgnoreCase(node.openingElement.attributes, HTML_FOR_ATTRIBUTE)) === controlIdKey && hasAccessibleLabelText(node, context)) return true;
		}
	}
	for (const child of children) if (searchForHtmlForLabel(child, controlIdKey, context)) return true;
	return false;
};
const hasHtmlForLabel = (element, context) => {
	const controlIdKey = getAttributeMatchKey(hasJsxPropIgnoreCase(element.openingElement.attributes, ID_ATTRIBUTE));
	if (controlIdKey === null) return false;
	return searchForHtmlForLabel(findEnclosingJsxTreeRoot(element), controlIdKey, context);
};
const controlHasAssociatedLabel = defineRule({
	id: "control-has-associated-label",
	title: "Control missing accessible label",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Give every interactive control a label screen readers can read.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$46(context.settings);
		const isTestlikeFile = isTestlikeFilename(context.filename);
		return { JSXElement(node) {
			if (isTestlikeFile) return;
			const opening = node.openingElement;
			const tagName = getElementType(opening, context.settings);
			if (DEFAULT_IGNORE_ELEMENTS.includes(tagName)) return;
			if (settings.ignoreElements.includes(tagName)) return;
			const roleAttribute = hasJsxPropIgnoreCase(opening.attributes, "role");
			const role = roleAttribute ? getJsxPropStringValue(roleAttribute) : null;
			if (role && settings.ignoreRoles.includes(role)) return;
			if (isHiddenFromScreenReader(opening, context.settings)) return;
			const isDomElement = HTML_TAGS.has(tagName);
			const isInteractiveEl = isInteractiveElement(tagName, opening);
			const isInteractiveRoleEl = role !== null && isInteractiveRole(role);
			const isControlComponent = settings.controlComponents.includes(tagName);
			if (!(isInteractiveEl || isDomElement && isInteractiveRoleEl || isControlComponent)) return;
			if (hasLabellingProp(opening.attributes, settings.labelAttributes)) return;
			const checkContext = {
				depth: settings.depth,
				customAttributes: settings.labelAttributes,
				controlComponents: settings.controlComponents,
				settings: context.settings
			};
			if (hasAncestorLabel(node, checkContext)) return;
			if (hasHtmlForLabel(node, checkContext)) return;
			for (const child of node.children) if (checkChildForLabel(child, 1, checkContext)) return;
			context.report({
				node: opening,
				message: MESSAGE$48
			});
		} };
	}
});
const LONG_TRANSITION_DURATION_THRESHOLD_MS = 1e3;
const VAGUE_BUTTON_LABELS = new Set([
	"continue",
	"submit",
	"ok",
	"okay",
	"click here",
	"here",
	"yes",
	"no",
	"go",
	"done"
]);
const TYPOGRAPHY_PUNCTUATION_EXCLUDED_TAG_NAMES = new Set([
	"code",
	"pre",
	"kbd",
	"samp",
	"var",
	"tt",
	"markdown",
	"markdownblock",
	"markdowncontent",
	"markdownrenderer",
	"markdowntext",
	"markdownview",
	"mdx",
	"mdxcontent",
	"mdxremote",
	"md",
	"prose",
	"richtext",
	"article",
	"blockquote",
	"quote",
	"trans",
	"translation",
	"translated",
	"fbt",
	"fbs"
]);
const PADDING_HORIZONTAL_AXIS_PATTERN = /(?:^|\s)(-?)px-(\d+(?:\.\d+)?|\[[^\]]+\])(?=$|[\s:])/g;
const PADDING_VERTICAL_AXIS_PATTERN = /(?:^|\s)(-?)py-(\d+(?:\.\d+)?|\[[^\]]+\])(?=$|[\s:])/g;
const SIZE_WIDTH_AXIS_PATTERN = /(?:^|\s)(-?)w-(\d+(?:\.\d+)?|\[[^\]]+\])(?=$|[\s:])/g;
const SIZE_HEIGHT_AXIS_PATTERN = /(?:^|\s)(-?)h-(\d+(?:\.\d+)?|\[[^\]]+\])(?=$|[\s:])/g;
const FLEX_OR_GRID_DISPLAY_TOKENS = new Set([
	"flex",
	"inline-flex",
	"grid",
	"inline-grid"
]);
const SPACE_AXIS_PATTERN = /(?:^|\s)(?:-)?space-(x|y)-(\d+(?:\.\d+)?|\[[^\]]+\])(?=$|[\s:])/;
const TRAILING_THREE_PERIOD_ELLIPSIS_PATTERN = /[A-Za-z]\.\.\./;
//#endregion
//#region src/plugin/utils/find-jsx-attribute.ts
const findJsxAttribute = (attributes, attributeName) => attributes?.find((attribute) => isNodeOfType(attribute, "JSXAttribute") && isNodeOfType(attribute.name, "JSXIdentifier") && attribute.name.name === attributeName);
//#endregion
//#region src/plugin/rules/react-ui/utils/get-opening-element-tag-name.ts
const getOpeningElementTagName = (openingElement) => {
	if (!openingElement) return null;
	if (!isNodeOfType(openingElement, "JSXOpeningElement")) return null;
	if (isNodeOfType(openingElement.name, "JSXIdentifier")) return openingElement.name.name;
	if (isNodeOfType(openingElement.name, "JSXMemberExpression")) {
		let cursor = openingElement.name;
		while (isNodeOfType(cursor, "JSXMemberExpression")) cursor = cursor.property;
		if (isNodeOfType(cursor, "JSXIdentifier")) return cursor.name;
	}
	return null;
};
//#endregion
//#region src/plugin/rules/react-ui/utils/is-inside-excluded-typography-ancestor.ts
const isInsideExcludedTypographyAncestor = (jsxTextNode) => {
	let cursor = jsxTextNode.parent;
	while (cursor) {
		if (isNodeOfType(cursor, "JSXElement")) {
			const tagName = getOpeningElementTagName(cursor.openingElement);
			if (tagName && TYPOGRAPHY_PUNCTUATION_EXCLUDED_TAG_NAMES.has(tagName.toLowerCase())) return true;
			const translateAttribute = findJsxAttribute(cursor.openingElement?.attributes ?? [], "translate");
			if (isNodeOfType(translateAttribute?.value, "Literal") && translateAttribute.value.value === "no") return true;
		}
		cursor = cursor.parent;
	}
	return false;
};
//#endregion
//#region src/plugin/rules/react-ui/no-em-dash-in-jsx-text.ts
const EM_DASH = "—";
const noEmDashInJsxText = defineRule({
	id: "design-no-em-dash-in-jsx-text",
	title: "Em dash in JSX text",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	category: "Architecture",
	recommendation: "Replace em dashes in UI text with commas, colons, semicolons, or parentheses so the copy reads less like AI output.",
	create: (context) => ({ JSXText(jsxTextNode) {
		if (!(typeof jsxTextNode.value === "string" ? jsxTextNode.value : "").includes(EM_DASH)) return;
		if (isInsideExcludedTypographyAncestor(jsxTextNode)) return;
		context.report({
			node: jsxTextNode,
			message: "Em dash (—) in UI text reads like AI output to your users."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-ui/utils/get-class-name-literal.ts
const getClassNameLiteral = (classAttribute) => {
	if (!isNodeOfType(classAttribute, "JSXAttribute")) return null;
	if (!classAttribute.value) return null;
	if (isNodeOfType(classAttribute.value, "Literal") && typeof classAttribute.value.value === "string") return classAttribute.value.value;
	if (isNodeOfType(classAttribute.value, "JSXExpressionContainer")) {
		const expression = classAttribute.value.expression;
		if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") return expression.value;
		if (isNodeOfType(expression, "TemplateLiteral") && expression.quasis?.length === 1) return expression.quasis[0].value?.raw ?? null;
	}
	return null;
};
//#endregion
//#region src/plugin/rules/react-ui/utils/collect-axis-shorthand-pairs.ts
const collectAxisShorthandPairs = (classNameValue, horizontalPattern, verticalPattern) => {
	const horizontalValues = /* @__PURE__ */ new Set();
	for (const horizontalMatch of classNameValue.matchAll(horizontalPattern)) horizontalValues.add(`${horizontalMatch[1]}${horizontalMatch[2]}`);
	const matchedPairs = [];
	for (const verticalMatch of classNameValue.matchAll(verticalPattern)) {
		const verticalValue = `${verticalMatch[1]}${verticalMatch[2]}`;
		if (horizontalValues.has(verticalValue)) matchedPairs.push({ value: verticalValue });
	}
	return matchedPairs;
};
//#endregion
//#region src/plugin/rules/react-ui/utils/has-responsive-prefix.ts
const hasResponsivePrefix = (classNameValue, axisPrefix) => new RegExp(`(?:^|\\s)\\w+:${axisPrefix}-`).test(classNameValue);
//#endregion
//#region src/plugin/rules/react-ui/no-redundant-padding-axes.ts
const noRedundantPaddingAxes = defineRule({
	id: "design-no-redundant-padding-axes",
	title: "Redundant padding axes",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	category: "Architecture",
	recommendation: "Collapse `px-N py-N` to `p-N` when both sides match. Keep them split only when one side changes at a breakpoint (`py-2 md:py-3`).",
	create: (context) => ({ JSXAttribute(jsxAttribute) {
		if (!isNodeOfType(jsxAttribute.name, "JSXIdentifier") || jsxAttribute.name.name !== "className") return;
		const classNameLiteral = getClassNameLiteral(jsxAttribute);
		if (!classNameLiteral) return;
		if (hasResponsivePrefix(classNameLiteral, "px") || hasResponsivePrefix(classNameLiteral, "py")) return;
		const matchedPairs = collectAxisShorthandPairs(classNameLiteral, PADDING_HORIZONTAL_AXIS_PATTERN, PADDING_VERTICAL_AXIS_PATTERN);
		if (matchedPairs.length === 0) return;
		for (const matchedPair of matchedPairs) context.report({
			node: jsxAttribute,
			message: `px-${matchedPair.value} & py-${matchedPair.value} are the same.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-ui/no-redundant-size-axes.ts
const noRedundantSizeAxes = defineRule({
	id: "design-no-redundant-size-axes",
	title: "Redundant width and height axes",
	requires: ["tailwind:3.4"],
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	category: "Architecture",
	recommendation: "Collapse `w-N h-N` to `size-N` (Tailwind v3.4+) when both sides match.",
	create: (context) => ({ JSXAttribute(jsxAttribute) {
		if (!isNodeOfType(jsxAttribute.name, "JSXIdentifier") || jsxAttribute.name.name !== "className") return;
		const classNameLiteral = getClassNameLiteral(jsxAttribute);
		if (!classNameLiteral) return;
		if (hasResponsivePrefix(classNameLiteral, "w") || hasResponsivePrefix(classNameLiteral, "h")) return;
		const matchedPairs = collectAxisShorthandPairs(classNameLiteral, SIZE_WIDTH_AXIS_PATTERN, SIZE_HEIGHT_AXIS_PATTERN);
		if (matchedPairs.length === 0) return;
		for (const matchedPair of matchedPairs) context.report({
			node: jsxAttribute,
			message: `w-${matchedPair.value} & h-${matchedPair.value} are the same.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-ui/no-space-on-flex-children.ts
const tokenizeClassName = (classNameValue) => classNameValue.split(/\s+/).filter(Boolean);
const noSpaceOnFlexChildren = defineRule({
	id: "design-no-space-on-flex-children",
	title: "space-* utility on flex children",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	category: "Architecture",
	recommendation: "Use `gap-*` on the flex or grid parent. `space-x-*` and `space-y-*` leave gaps when a child is hidden, miss spacing on wrapped lines, and don't flip in right-to-left layouts.",
	create: (context) => ({ JSXAttribute(jsxAttribute) {
		if (!isNodeOfType(jsxAttribute.name, "JSXIdentifier") || jsxAttribute.name.name !== "className") return;
		const classNameLiteral = getClassNameLiteral(jsxAttribute);
		if (!classNameLiteral) return;
		const tokens = tokenizeClassName(classNameLiteral);
		let hasFlexOrGridLayout = false;
		for (const token of tokens) {
			const lastSegment = token.includes(":") ? token.slice(token.lastIndexOf(":") + 1) : token;
			if (FLEX_OR_GRID_DISPLAY_TOKENS.has(lastSegment)) {
				hasFlexOrGridLayout = true;
				break;
			}
		}
		if (!hasFlexOrGridLayout) return;
		const spaceMatch = classNameLiteral.match(SPACE_AXIS_PATTERN);
		if (!spaceMatch) return;
		const spaceAxis = spaceMatch[1];
		const spaceValue = spaceMatch[2];
		context.report({
			node: jsxAttribute,
			message: `space-${spaceAxis}-${spaceValue} on a flex or grid parent breaks spacing for your users.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-ui/no-three-period-ellipsis.ts
const noThreePeriodEllipsis = defineRule({
	id: "design-no-three-period-ellipsis",
	title: "Three dots instead of ellipsis",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	category: "Architecture",
	recommendation: "Use the real ellipsis \"…\" (or `&hellip;`) instead of three dots. Good for labels like \"Rename…\" and \"Loading…\".",
	create: (context) => ({ JSXText(jsxTextNode) {
		const textValue = typeof jsxTextNode.value === "string" ? jsxTextNode.value : "";
		if (!TRAILING_THREE_PERIOD_ELLIPSIS_PATTERN.test(textValue)) return;
		if (isInsideExcludedTypographyAncestor(jsxTextNode)) return;
		context.report({
			node: jsxTextNode,
			message: "Three dots (\"...\") look unpolished to your users."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-ui/no-vague-button-label.ts
const isButtonLikeTagName = (tagName) => {
	if (tagName === "button") return true;
	if (tagName === "Button") return true;
	return false;
};
const collectJsxLabelText = (jsxElementNode) => {
	if (!isNodeOfType(jsxElementNode, "JSXElement") && !isNodeOfType(jsxElementNode, "JSXFragment")) return null;
	const childList = jsxElementNode.children ?? [];
	if (childList.length === 0) return null;
	const collectedFragments = [];
	for (const childNode of childList) {
		if (isNodeOfType(childNode, "JSXText")) {
			collectedFragments.push(typeof childNode.value === "string" ? childNode.value : "");
			continue;
		}
		if (isNodeOfType(childNode, "JSXExpressionContainer")) {
			const expression = childNode.expression;
			if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") {
				collectedFragments.push(expression.value);
				continue;
			}
			if (isNodeOfType(expression, "TemplateLiteral")) {
				const staticValue = getStaticTemplateLiteralValue(expression);
				if (staticValue !== null) {
					collectedFragments.push(staticValue);
					continue;
				}
			}
			return null;
		}
		if (isNodeOfType(childNode, "JSXFragment")) {
			const fragmentLabel = collectJsxLabelText(childNode);
			if (fragmentLabel === null) return null;
			collectedFragments.push(fragmentLabel);
			continue;
		}
		if (isNodeOfType(childNode, "JSXElement")) return null;
	}
	return collectedFragments.join("").trim();
};
const noVagueButtonLabel = defineRule({
	id: "design-no-vague-button-label",
	title: "Vague button label",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Name the action: \"Save changes\" instead of \"Continue\", \"Send invite\" instead of \"Submit\". The label is the button's accessible name.",
	create: (context) => ({ JSXElement(jsxElementNode) {
		const tagName = getOpeningElementTagName(jsxElementNode.openingElement);
		if (!tagName || !isButtonLikeTagName(tagName)) return;
		const labelText = collectJsxLabelText(jsxElementNode);
		if (!labelText) return;
		const normalizedLabel = labelText.toLowerCase().replace(/[.!?…]+$/, "").trim();
		if (!VAGUE_BUTTON_LABELS.has(normalizedLabel)) return;
		context.report({
			node: jsxElementNode.openingElement ?? jsxElementNode,
			message: `Screen reader & unsure users can't tell what "${labelText}" does.`
		});
	} })
});
//#endregion
//#region src/plugin/utils/is-es5-component.ts
const PRAGMA$2 = "React";
const CREATE_CLASS = "createReactClass";
const isEs5Component = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	const callee = node.callee;
	if (isNodeOfType(callee, "MemberExpression")) {
		if (isNodeOfType(callee.object, "Identifier") && callee.object.name === PRAGMA$2 && isNodeOfType(callee.property, "Identifier") && callee.property.name === CREATE_CLASS) return true;
		return false;
	}
	if (isNodeOfType(callee, "Identifier")) return callee.name === CREATE_CLASS;
	return false;
};
//#endregion
//#region src/plugin/utils/is-es6-component.ts
const PRAGMA$1 = "React";
const COMPONENT = "Component";
const PURE_COMPONENT = "PureComponent";
const isReactComponentMemberRef = (node) => {
	if (!isNodeOfType(node, "MemberExpression")) return false;
	if (!isNodeOfType(node.object, "Identifier") || node.object.name !== PRAGMA$1) return false;
	if (!isNodeOfType(node.property, "Identifier")) return false;
	return node.property.name === COMPONENT || node.property.name === PURE_COMPONENT;
};
const isEs6Component = (node) => {
	if (!isNodeOfType(node, "ClassDeclaration") && !isNodeOfType(node, "ClassExpression")) return false;
	const superClass = node.superClass;
	if (!superClass) return false;
	if (isReactComponentMemberRef(superClass)) return true;
	if (isNodeOfType(superClass, "Identifier")) return superClass.name === COMPONENT || superClass.name === PURE_COMPONENT;
	return false;
};
//#endregion
//#region src/plugin/rules/react-builtins/display-name.ts
const MESSAGE$47 = "This component shows up as Anonymous in React DevTools because it has no `displayName`.";
const DEFAULT_ADDITIONAL_HOCS = [
	"observer",
	"lazy",
	"withTracking"
];
const resolveSettings$45 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.displayName ?? {} : {};
	const additionalHoCs = new Set(ruleSettings.additionalHoCs ?? DEFAULT_ADDITIONAL_HOCS);
	return {
		ignoreTranspilerName: ruleSettings.ignoreTranspilerName ?? false,
		checkContextObjects: ruleSettings.checkContextObjects ?? false,
		reactVersion: ruleSettings.reactVersion ?? "",
		additionalHoCs
	};
};
const isReactVersionAtLeast$1 = (version, major, minor) => {
	if (!version) return true;
	const match = version.match(/^(\d+)\.(\d+)/);
	if (!match) return true;
	const actualMajor = Number(match[1]);
	const actualMinor = Number(match[2]);
	return actualMajor > major || actualMajor === major && actualMinor >= minor;
};
const containsJsx$1 = (root) => {
	let found = false;
	const visit = (node) => {
		if (found) return;
		if (node.type === "JSXElement" || node.type === "JSXFragment") {
			found = true;
			return;
		}
		if (isNodeOfType(node, "CallExpression") && isCreateElementCall(node)) {
			found = true;
			return;
		}
		const nodeRecord = node;
		for (const key of Object.keys(nodeRecord)) {
			if (key === "parent") continue;
			const child = nodeRecord[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
			if (found) return;
		}
	};
	visit(root);
	return found;
};
const getStaticMemberName = (node) => {
	if (!isNodeOfType(node, "MemberExpression")) return null;
	if (!node.computed && isNodeOfType(node.property, "Identifier")) return node.property.name;
	if (node.computed && isNodeOfType(node.property, "Literal") && typeof node.property.value === "string") return node.property.value;
	return null;
};
const getAssignedName = (node) => {
	let parent = node.parent;
	while (parent && (parent.type === "TSAsExpression" || parent.type === "TSSatisfiesExpression" || parent.type === "TSNonNullExpression" || parent.type === "TSTypeAssertion")) parent = parent.parent ?? null;
	if (!parent) return null;
	if (isNodeOfType(parent, "VariableDeclarator") && isNodeOfType(parent.id, "Identifier")) return parent.id.name;
	if (isNodeOfType(parent, "AssignmentExpression")) {
		const left = parent.left;
		if (isNodeOfType(left, "Identifier")) return left.name;
		if (isNodeOfType(left, "MemberExpression")) return getStaticMemberName(left);
	}
	return null;
};
const isModuleExportsAssignment = (node) => {
	const parent = node.parent;
	if (!parent || !isNodeOfType(parent, "AssignmentExpression")) return false;
	const left = parent.left;
	return isNodeOfType(left, "MemberExpression") && isNodeOfType(left.object, "Identifier") && left.object.name === "module" && getStaticMemberName(left) === "exports";
};
const isCreateClassLikeCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (isEs5Component(node)) return true;
	const callee = node.callee;
	if (isNodeOfType(callee, "MemberExpression")) return getStaticMemberName(callee) === "createClass";
	return false;
};
const isCreateContextCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	const callee = node.callee;
	if (isNodeOfType(callee, "Identifier")) return callee.name === "createContext";
	return isNodeOfType(callee, "MemberExpression") && getStaticMemberName(callee) === "createContext";
};
const isNamedFunctionLike = (node) => (isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "FunctionDeclaration")) && Boolean(node.id?.name);
const firstCallArgument = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return null;
	const first = node.arguments[0];
	return first ? first : null;
};
const resolveHoCCalleeName = (node, additionalHoCs) => {
	if (!isNodeOfType(node, "CallExpression")) return null;
	const callee = node.callee;
	if (isNodeOfType(callee, "Identifier")) {
		if (callee.name === "memo" || callee.name === "forwardRef") return callee.name;
		if (additionalHoCs.has(callee.name)) return callee.name;
		return null;
	}
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed && isNodeOfType(callee.property, "Identifier")) {
		const propertyName = callee.property.name;
		if (propertyName === "memo" || propertyName === "forwardRef") return propertyName;
		if (additionalHoCs.has(propertyName)) return propertyName;
	}
	return null;
};
const isDisplayNameHoC = (node, additionalHoCs) => resolveHoCCalleeName(node, additionalHoCs) !== null;
const supportsComposedForwardRefDisplayName = (version) => {
	if (!version) return false;
	if (isReactVersionAtLeast$1(version, 15, 7)) return true;
	const match = version.match(/^0\.14\.(\d+)/);
	return Boolean(match && Number(match[1]) >= 11);
};
const shouldReportHoCDisplayName = (node, settings) => {
	if (!isDisplayNameHoC(node, settings.additionalHoCs)) return false;
	if (!containsJsx$1(node)) return false;
	const assignedName = getAssignedName(node);
	const programRoot = findProgramRoot(node);
	if (assignedName && programRoot && hasDisplayNameAssignment(assignedName, programRoot)) return false;
	const callName = resolveHoCCalleeName(node, settings.additionalHoCs);
	const firstArgument = firstCallArgument(node);
	if (!firstArgument) return false;
	if (callName === "forwardRef" && isNodeOfType(node.parent, "CallExpression") && resolveHoCCalleeName(node.parent, settings.additionalHoCs) === "memo" && firstCallArgument(node.parent) === node && supportsComposedForwardRefDisplayName(settings.reactVersion)) return false;
	if (callName === "memo" && isNodeOfType(firstArgument, "CallExpression")) {
		if (resolveHoCCalleeName(firstArgument, settings.additionalHoCs) !== "forwardRef") return false;
		return !supportsComposedForwardRefDisplayName(settings.reactVersion);
	}
	if (isNamedFunctionLike(firstArgument)) return false;
	return isNodeOfType(firstArgument, "FunctionExpression") || isNodeOfType(firstArgument, "ArrowFunctionExpression");
};
const hasDisplayNameMember = (classNode) => {
	const classBody = classNode.body;
	if (!classBody) return false;
	const members = classBody.body ?? [];
	for (const member of members) if ((isNodeOfType(member, "PropertyDefinition") || isNodeOfType(member, "MethodDefinition")) && "static" in member && member.static && isNodeOfType(member.key, "Identifier") && member.key.name === "displayName") return true;
	return false;
};
const hasDisplayNameAssignment = (className, programRoot) => {
	let found = false;
	const visit = (node) => {
		if (found) return;
		if (isNodeOfType(node, "AssignmentExpression") && isNodeOfType(node.left, "MemberExpression") && isNodeOfType(node.left.object, "Identifier") && node.left.object.name === className && getStaticMemberName(node.left) === "displayName") {
			found = true;
			return;
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
			if (found) return;
		}
	};
	visit(programRoot);
	return found;
};
const memberExpressionPath = (node) => {
	if (isNodeOfType(node, "Identifier")) return [node.name];
	if (!isNodeOfType(node, "MemberExpression")) return [];
	const objectPath = memberExpressionPath(node.object);
	const propertyName = getStaticMemberName(node);
	return propertyName ? [...objectPath, propertyName] : objectPath;
};
const hasDisplayNameAssignmentForProperty = (propertyName, programRoot) => {
	let found = false;
	const visit = (node) => {
		if (found) return;
		if (isNodeOfType(node, "AssignmentExpression") && isNodeOfType(node.left, "MemberExpression") && getStaticMemberName(node.left) === "displayName") {
			if (memberExpressionPath(node.left.object).includes(propertyName)) {
				found = true;
				return;
			}
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
			if (found) return;
		}
	};
	visit(programRoot);
	return found;
};
const displayName = defineRule({
	id: "display-name",
	title: "Component missing display name",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Give each component a `displayName` so DevTools shows a clear name.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$45(context.settings);
		const ignoreNamed = settings.ignoreTranspilerName ? false : true;
		const reportAt = (node) => {
			context.report({
				node,
				message: MESSAGE$47
			});
		};
		return {
			ClassDeclaration(node) {
				if (!isEs6Component(node)) return;
				if (node.id && isReactComponentName(node.id.name) && ignoreNamed) return;
				if (hasDisplayNameMember(node)) return;
				if (node.id) {
					const programRoot = findProgramRoot(node);
					if (programRoot && hasDisplayNameAssignment(node.id.name, programRoot)) return;
				}
				reportAt(node.id ?? node);
			},
			ClassExpression(node) {
				if (!isEs6Component(node)) return;
				if (node.id && isReactComponentName(node.id.name) && ignoreNamed) return;
				if (hasDisplayNameMember(node)) return;
				if (node.id) {
					const programRoot = findProgramRoot(node);
					if (programRoot && hasDisplayNameAssignment(node.id.name, programRoot)) return;
				}
				reportAt(node.id ?? node);
			},
			FunctionExpression(node) {
				if (!containsJsx$1(node)) return;
				if (node.id && isReactComponentName(node.id.name) && ignoreNamed) return;
				if (isNodeOfType(node.parent, "Property") && node.parent.method) {
					const key = node.parent.key;
					const propertyName = isNodeOfType(key, "Identifier") ? key.name : isNodeOfType(key, "Literal") && typeof key.value === "string" ? key.value : null;
					const programRoot = findProgramRoot(node);
					if (propertyName && isReactComponentName(propertyName) && settings.ignoreTranspilerName && (!programRoot || !hasDisplayNameAssignmentForProperty(propertyName, programRoot))) {
						reportAt(node);
						return;
					}
				}
				const assignedName = getAssignedName(node);
				if (assignedName && isReactComponentName(assignedName) && ignoreNamed) return;
				if (isModuleExportsAssignment(node) && !node.id) {
					reportAt(node);
					return;
				}
				if (isNodeOfType(node.parent, "ReturnStatement") && !node.id) reportAt(node);
			},
			ArrowFunctionExpression(node) {
				if (!containsJsx$1(node)) return;
				if (isNodeOfType(node.parent, "ArrowFunctionExpression")) {
					reportAt(node);
					return;
				}
				let parent = node.parent;
				while (parent) {
					if (isNodeOfType(parent, "VariableDeclarator") && isNodeOfType(parent.id, "Identifier")) {
						if (isReactComponentName(parent.id.name) && ignoreNamed) return;
						break;
					}
					if (isNodeOfType(parent, "ExportDefaultDeclaration")) {
						reportAt(node);
						return;
					}
					if (isModuleExportsAssignment(node)) {
						reportAt(node);
						return;
					}
					if (isNodeOfType(parent, "FunctionDeclaration") || isNodeOfType(parent, "FunctionExpression") || isNodeOfType(parent, "ArrowFunctionExpression") || isNodeOfType(parent, "ClassDeclaration") || isNodeOfType(parent, "ClassExpression") || isNodeOfType(parent, "Program")) break;
					parent = parent.parent ?? null;
				}
			},
			CallExpression(node) {
				if (settings.checkContextObjects && isReactVersionAtLeast$1(settings.reactVersion, 16, 3) && isCreateContextCall(node)) {
					const assignedName = getAssignedName(node);
					if (assignedName) {
						const programRoot = findProgramRoot(node);
						if (programRoot && hasDisplayNameAssignment(assignedName, programRoot)) return;
					}
					reportAt(node);
					return;
				}
				if (shouldReportHoCDisplayName(node, settings)) {
					reportAt(node);
					return;
				}
				if (!isCreateClassLikeCall(node)) return;
				const propsArgument = node.arguments[0];
				if (!propsArgument || !isNodeOfType(propsArgument, "ObjectExpression")) {
					reportAt(node);
					return;
				}
				let hasDisplayName = false;
				for (const property of propsArgument.properties) {
					if (!isNodeOfType(property, "Property")) continue;
					if (property.computed) continue;
					const key = property.key;
					if (isNodeOfType(key, "Identifier") && key.name === "displayName") {
						hasDisplayName = true;
						break;
					}
					if (isNodeOfType(key, "Literal") && key.value === "displayName") {
						hasDisplayName = true;
						break;
					}
				}
				if (hasDisplayName) return;
				const parent = node.parent;
				if (parent && isNodeOfType(parent, "VariableDeclarator") && isNodeOfType(parent.id, "Identifier") && isReactComponentName(parent.id.name) && ignoreNamed) return;
				if (parent && isNodeOfType(parent, "AssignmentExpression")) {
					const left = parent.left;
					if (isNodeOfType(left, "Identifier") && isReactComponentName(left.name) && ignoreNamed) return;
					if (isNodeOfType(left, "MemberExpression") && isNodeOfType(left.property, "Identifier") && isReactComponentName(left.property.name) && ignoreNamed) return;
				}
				reportAt(node);
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/walk-inside-statement-blocks.ts
const walkInsideStatementBlocks = (node, visitor) => {
	if (!node || typeof node !== "object") return;
	if (isFunctionLike$2(node)) return;
	visitor(node);
	const nodeRecord = node;
	for (const key of Object.keys(nodeRecord)) {
		if (key === "parent") continue;
		const child = nodeRecord[key];
		if (Array.isArray(child)) {
			for (const item of child) if (isAstNode(item)) walkInsideStatementBlocks(item, visitor);
		} else if (isAstNode(child)) walkInsideStatementBlocks(child, visitor);
	}
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/is-subscribe-like-call-expression.ts
const getSubscribeLikeMethodName = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return null;
	if (!isNodeOfType(node.callee, "MemberExpression")) return null;
	if (!isNodeOfType(node.callee.property, "Identifier")) return null;
	return node.callee.property.name;
};
const isSubscribeLikeCallExpression = (node) => {
	const methodName = getSubscribeLikeMethodName(node);
	return methodName !== null && SUBSCRIPTION_METHOD_NAMES.has(methodName);
};
const isCleanupReturningSubscribeLikeCallExpression = (node) => {
	const methodName = getSubscribeLikeMethodName(node);
	return methodName !== null && CLEANUP_RETURNING_SUBSCRIPTION_METHOD_NAMES.has(methodName);
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/is-cleanup-return.ts
const ITERATOR_CALLBACK_METHOD_NAMES = new Set([
	"each",
	"every",
	"filter",
	"find",
	"findIndex",
	"findLast",
	"findLastIndex",
	"flatMap",
	"forEach",
	"map",
	"reduce",
	"reduceRight",
	"some",
	"sort",
	"toSorted"
]);
const STATIC_ITERATOR_CALLBACK_METHOD_NAMES = new Set([
	"from",
	"fromAsync",
	"groupBy"
]);
const unwrapChainExpression$2 = (node) => isNodeOfType(node, "ChainExpression") ? node.expression : node;
const isNullLiteral = (node) => isNodeOfType(node, "Literal") && node.value === null;
const isListenerRemovalViaNullHandler = (callNode) => {
	if (!isNodeOfType(callNode, "CallExpression")) return false;
	const callee = unwrapChainExpression$2(callNode.callee);
	return isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && callee.property.name === "on" && isNullLiteral(callNode.arguments?.[1]);
};
const isReleaseLikeCall = (node, knownCleanupFunctionNames, knownBoundSubscriptionNames) => {
	const callNode = unwrapChainExpression$2(node);
	if (!isNodeOfType(callNode, "CallExpression")) return false;
	if (isListenerRemovalViaNullHandler(callNode)) return true;
	const callee = unwrapChainExpression$2(callNode.callee);
	if (isNodeOfType(callee, "Identifier")) {
		if (TIMER_CLEANUP_CALLEE_NAMES.has(callee.name)) return true;
		if (CLEANUP_LIKE_RELEASE_CALLEE_NAMES.has(callee.name)) return true;
		if (knownCleanupFunctionNames.has(callee.name)) return true;
		return false;
	}
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) {
		if (BOUND_RESOURCE_RELEASE_METHOD_NAMES.has(callee.property.name) && isNodeOfType(callee.object, "Identifier") && knownBoundSubscriptionNames.has(callee.object.name)) return true;
		return GLOBAL_RELEASE_METHOD_NAMES.has(callee.property.name);
	}
	return false;
};
const isStaticIteratorCallbackCallee = (callee) => isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && isNodeOfType(callee.property, "Identifier") && (callee.object.name === "Array" || callee.object.name === "Map" || callee.object.name === "Object") && STATIC_ITERATOR_CALLBACK_METHOD_NAMES.has(callee.property.name);
const isIteratorCallbackArgument = (node) => {
	const parentNode = node.parent;
	if (!isNodeOfType(parentNode, "CallExpression")) return false;
	if (!parentNode.arguments?.some((argument) => argument === node)) return false;
	const callee = unwrapChainExpression$2(parentNode.callee);
	if (parentNode.arguments[1] === node && isStaticIteratorCallbackCallee(callee)) return true;
	return isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && ITERATOR_CALLBACK_METHOD_NAMES.has(callee.property.name);
};
const containsReleaseLikeCall = (node, knownCleanupFunctionNames, knownBoundSubscriptionNames) => {
	let didFindRelease = false;
	walkAst(node, (child) => {
		if (didFindRelease) return false;
		if (child !== node && isFunctionLike$2(child) && !isIteratorCallbackArgument(child)) return false;
		if (isReleaseLikeCall(child, knownCleanupFunctionNames, knownBoundSubscriptionNames)) {
			didFindRelease = true;
			return false;
		}
	});
	return didFindRelease;
};
const isCleanupFunctionLike = (node, knownCleanupFunctionNames, knownBoundSubscriptionNames) => {
	if (!isFunctionLike$2(node)) return false;
	return containsReleaseLikeCall(node.body, knownCleanupFunctionNames, knownBoundSubscriptionNames);
};
const isCleanupReturn = (returnedValue, knownCleanupFunctionNames, knownBoundSubscriptionNames, options = {}) => {
	if (!returnedValue) return false;
	const unwrappedValue = unwrapChainExpression$2(returnedValue);
	if (isNodeOfType(unwrappedValue, "Literal") && unwrappedValue.value === null) return false;
	if (isNodeOfType(unwrappedValue, "Identifier")) {
		if (unwrappedValue.name === "undefined") return false;
		if (knownCleanupFunctionNames.has(unwrappedValue.name)) return true;
		return options.allowOpaqueReturn === true && !knownBoundSubscriptionNames.has(unwrappedValue.name);
	}
	if (isCleanupReturningSubscribeLikeCallExpression(unwrappedValue)) return true;
	if (options.allowOpaqueReturn === true && !isSubscribeLikeCallExpression(unwrappedValue)) return true;
	if (isCleanupFunctionLike(unwrappedValue, knownCleanupFunctionNames, knownBoundSubscriptionNames)) return true;
	return false;
};
//#endregion
//#region src/plugin/rules/state-and-effects/effect-needs-cleanup.ts
const findSubscribeLikeUsages = (callback) => {
	const usages = [];
	if (!isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) return usages;
	let cleanupArgument = null;
	if (isNodeOfType(callback.body, "BlockStatement")) {
		const callbackStatements = callback.body.body ?? [];
		const lastCallbackStatement = callbackStatements[callbackStatements.length - 1];
		if (isNodeOfType(lastCallbackStatement, "ReturnStatement") && lastCallbackStatement.argument) cleanupArgument = lastCallbackStatement.argument;
	}
	walkAst(callback, (child) => {
		if (child === cleanupArgument && !isSubscribeLikeCallExpression(child)) return false;
		if (!isNodeOfType(child, "CallExpression")) return;
		if (isNodeOfType(child.callee, "Identifier") && TIMER_CALLEE_NAMES_REQUIRING_CLEANUP.has(child.callee.name)) {
			usages.push({
				kind: "timer",
				node: child,
				resourceName: child.callee.name
			});
			return;
		}
		if (isNodeOfType(child.callee, "MemberExpression") && isNodeOfType(child.callee.property, "Identifier") && SUBSCRIPTION_METHOD_NAMES.has(child.callee.property.name)) usages.push({
			kind: "subscribe",
			node: child,
			resourceName: child.callee.property.name
		});
	});
	return usages;
};
const collectCleanupBindings = (effectCallback) => {
	const bindings = {
		cleanupFunctionNames: /* @__PURE__ */ new Set(),
		subscriptionNames: /* @__PURE__ */ new Set(),
		effectScopeVariableNames: /* @__PURE__ */ new Set()
	};
	if (!isNodeOfType(effectCallback, "ArrowFunctionExpression") && !isNodeOfType(effectCallback, "FunctionExpression")) return bindings;
	if (!isNodeOfType(effectCallback.body, "BlockStatement")) return bindings;
	walkInsideStatementBlocks(effectCallback.body, (child) => {
		if (!isNodeOfType(child, "VariableDeclaration")) return;
		for (const declarator of child.declarations ?? []) {
			if (!isNodeOfType(declarator.id, "Identifier")) continue;
			const bindingName = declarator.id.name;
			bindings.effectScopeVariableNames.add(bindingName);
			const init = declarator.init;
			if (!init || !isNodeOfType(init, "CallExpression")) continue;
			if (isSubscribeLikeCallExpression(init)) {
				bindings.subscriptionNames.add(bindingName);
				if (isCleanupReturningSubscribeLikeCallExpression(init)) bindings.cleanupFunctionNames.add(bindingName);
			}
		}
	});
	walkAst(effectCallback.body, (child) => {
		if (child !== effectCallback.body && (isNodeOfType(child, "ArrowFunctionExpression") || isNodeOfType(child, "FunctionExpression"))) return false;
		if (isNodeOfType(child, "FunctionDeclaration") && child.id && isCleanupFunctionLike(child, bindings.cleanupFunctionNames, bindings.subscriptionNames)) {
			bindings.cleanupFunctionNames.add(child.id.name);
			return false;
		}
	});
	walkInsideStatementBlocks(effectCallback.body, (child) => {
		if (!isNodeOfType(child, "VariableDeclaration")) return;
		for (const declarator of child.declarations ?? []) {
			if (!isNodeOfType(declarator.id, "Identifier") || !declarator.init) continue;
			if (isCleanupFunctionLike(declarator.init, bindings.cleanupFunctionNames, bindings.subscriptionNames)) bindings.cleanupFunctionNames.add(declarator.id.name);
		}
	});
	walkAst(effectCallback.body, (child) => {
		if (isNodeOfType(child, "AssignmentExpression") && isNodeOfType(child.left, "Identifier") && bindings.effectScopeVariableNames.has(child.left.name) && isCleanupFunctionLike(child.right, bindings.cleanupFunctionNames, bindings.subscriptionNames)) bindings.cleanupFunctionNames.add(child.left.name);
	});
	return bindings;
};
const getRangeStart = (node) => {
	const rangeStart = node.range?.[0];
	return typeof rangeStart === "number" ? rangeStart : null;
};
const cleanupReturnRunsAfterUsage = (returnStatement, usages) => {
	if (returnStatement.argument && isCleanupReturningSubscribeLikeCallExpression(returnStatement.argument)) return true;
	const returnStart = getRangeStart(returnStatement);
	if (returnStart === null) return true;
	return usages.some((usage) => {
		const usageStart = getRangeStart(usage.node);
		return usageStart === null || usageStart < returnStart;
	});
};
const effectHasCleanupReturn = (callback, usages) => {
	if (!isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) return false;
	if (!isNodeOfType(callback.body, "BlockStatement")) return isCleanupReturningSubscribeLikeCallExpression(callback.body);
	const cleanupBindings = collectCleanupBindings(callback);
	let didFindCleanupReturn = false;
	walkInsideStatementBlocks(callback.body, (child) => {
		if (didFindCleanupReturn) return;
		if (!isNodeOfType(child, "ReturnStatement")) return;
		if (!cleanupReturnRunsAfterUsage(child, usages)) return;
		if (isCleanupReturn(child.argument, cleanupBindings.cleanupFunctionNames, cleanupBindings.subscriptionNames, { allowOpaqueReturn: true })) didFindCleanupReturn = true;
	});
	return didFindCleanupReturn;
};
const effectNeedsCleanup = defineRule({
	id: "effect-needs-cleanup",
	title: "Effect subscription or timer never cleaned up",
	severity: "error",
	tags: ["test-noise"],
	recommendation: "Return a cleanup function that stops the subscription or timer: `return () => target.removeEventListener(name, handler)` for listeners, `return () => clearInterval(id)` or `clearTimeout(id)` for timers, or `return unsubscribe` if the subscribe call already gave you one.",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1)) return;
		const callback = getEffectCallback(node);
		if (!callback) return;
		const usages = findSubscribeLikeUsages(callback);
		if (usages.length === 0) return;
		if (effectHasCleanupReturn(callback, usages)) return;
		const firstUsage = usages[0];
		const verb = firstUsage.kind === "timer" ? "schedules" : "subscribes via";
		context.report({
			node,
			message: `\`${firstUsage.resourceName}(...)\` leaks memory because useEffect ${verb} it but never cleans it up.`
		});
	} })
});
//#endregion
//#region src/plugin/semantic/scope-analysis.ts
const isHoistedBindingKind = (kind) => kind === "var" || kind === "function";
const findHoistTargetScope = (scope) => {
	let current = scope;
	while (current) {
		if (current.kind === "module" || current.kind === "function" || current.kind === "arrow-function" || current.kind === "method") return current;
		current = current.parent;
	}
	return scope;
};
const createScope = (kind, node, parent, state) => {
	const scope = {
		id: state.nextScopeId++,
		kind,
		node,
		parent,
		children: [],
		symbols: [],
		references: [],
		symbolsByName: /* @__PURE__ */ new Map()
	};
	if (parent) parent.children.push(scope);
	return scope;
};
const pushScope = (kind, node, state) => {
	const scope = createScope(kind, node, state.currentScope, state);
	state.scopeStack.push(scope);
	state.currentScope = scope;
	state.ownScopeForNode.set(node, scope);
	return scope;
};
const popScope = (state) => {
	state.scopeStack.pop();
	const previous = state.scopeStack[state.scopeStack.length - 1];
	if (!previous) throw new Error("scope stack underflow");
	state.currentScope = previous;
};
const recordSymbol = (scope, state, options) => {
	const symbol = {
		id: state.nextSymbolId++,
		name: options.name,
		kind: options.kind,
		bindingIdentifier: options.bindingIdentifier,
		declarationNode: options.declarationNode,
		scope,
		initializer: options.initializer,
		references: []
	};
	scope.symbols.push(symbol);
	scope.symbolsByName.set(options.name, symbol);
	state.symbolByBindingIdentifier.set(options.bindingIdentifier, symbol);
	return symbol;
};
const collectBindingNamesFromPattern = (pattern) => {
	const out = [];
	const visit = (node) => {
		if (isNodeOfType(node, "Identifier")) {
			out.push(node);
			return;
		}
		if (isNodeOfType(node, "ObjectPattern")) {
			for (const property of node.properties) if (isNodeOfType(property, "Property")) {
				const propValue = property.value;
				visit(propValue);
			} else if (isNodeOfType(property, "RestElement")) visit(property.argument);
			return;
		}
		if (isNodeOfType(node, "ArrayPattern")) {
			for (const element of node.elements) if (element) visit(element);
			return;
		}
		if (isNodeOfType(node, "RestElement")) {
			visit(node.argument);
			return;
		}
		if (isNodeOfType(node, "AssignmentPattern")) visit(node.left);
	};
	visit(pattern);
	return out;
};
const visitDestructuringDeclarations = (pattern, baseInitializer, scope, state, symbolKind, declarationNode) => {
	if (isNodeOfType(pattern, "Identifier")) {
		recordSymbol(scope, state, {
			name: pattern.name,
			kind: symbolKind,
			bindingIdentifier: pattern,
			declarationNode,
			initializer: baseInitializer
		});
		return;
	}
	if (isNodeOfType(pattern, "ObjectPattern")) {
		for (const property of pattern.properties) if (isNodeOfType(property, "Property")) {
			const propertyValue = property.value;
			visitDestructuringDeclarations(propertyValue, isNodeOfType(propertyValue, "AssignmentPattern") ? propertyValue.right : baseInitializer, scope, state, symbolKind, declarationNode);
		} else if (isNodeOfType(property, "RestElement")) visitDestructuringDeclarations(property.argument, baseInitializer, scope, state, symbolKind, declarationNode);
		return;
	}
	if (isNodeOfType(pattern, "ArrayPattern")) {
		for (const element of pattern.elements) {
			if (!element) continue;
			visitDestructuringDeclarations(element, isNodeOfType(element, "AssignmentPattern") ? element.right ?? null : baseInitializer, scope, state, symbolKind, declarationNode);
		}
		return;
	}
	if (isNodeOfType(pattern, "AssignmentPattern")) {
		visitDestructuringDeclarations(pattern.left, pattern.right ?? null, scope, state, symbolKind, declarationNode);
		return;
	}
	if (isNodeOfType(pattern, "RestElement")) visitDestructuringDeclarations(pattern.argument, null, scope, state, symbolKind, declarationNode);
};
const tagAsBinding = (state, identifier) => {
	bindingPositionMarker.add(identifier);
};
const bindingPositionMarker = /* @__PURE__ */ new WeakSet();
const recordReference = (state, identifier, flag) => {
	const reference = {
		id: state.nextReferenceId++,
		identifier,
		resolvedSymbol: null,
		flag,
		scope: state.currentScope
	};
	state.currentScope.references.push(reference);
	state.referenceByIdentifier.set(identifier, reference);
};
const isFunctionBodyBlock = (block) => {
	if (!block.parent) return false;
	return isFunctionLike$2(block.parent);
};
const isCatchClauseBlock = (block) => block.parent !== null && block.parent !== void 0 && block.parent.type === "CatchClause";
const handleVariableDeclaration = (declaration, state) => {
	if (!isNodeOfType(declaration, "VariableDeclaration")) return;
	const symbolKind = declaration.kind === "var" ? "var" : declaration.kind === "let" ? "let" : declaration.kind === "const" ? "const" : "using";
	const targetScope = isHoistedBindingKind(symbolKind) ? findHoistTargetScope(state.currentScope) : state.currentScope;
	for (const declarator of declaration.declarations) {
		const declaratorNode = declarator;
		const init = declarator.init ?? null;
		visitDestructuringDeclarations(declarator.id, init, targetScope, state, symbolKind, declaratorNode);
		for (const identifier of collectBindingNamesFromPattern(declarator.id)) tagAsBinding(state, identifier);
	}
};
const handleFunctionDeclaration = (fn, state) => {
	if (!isNodeOfType(fn, "FunctionDeclaration")) return;
	if (fn.id) {
		recordSymbol(findHoistTargetScope(state.currentScope), state, {
			name: fn.id.name,
			kind: "function",
			bindingIdentifier: fn.id,
			declarationNode: fn,
			initializer: fn
		});
		tagAsBinding(state, fn.id);
	}
};
const handleClassDeclaration = (cls, state) => {
	if (!isNodeOfType(cls, "ClassDeclaration")) return;
	if (cls.id) {
		recordSymbol(state.currentScope, state, {
			name: cls.id.name,
			kind: "class",
			bindingIdentifier: cls.id,
			declarationNode: cls,
			initializer: cls
		});
		tagAsBinding(state, cls.id);
	}
};
const handleImportDeclaration = (importDeclaration, state) => {
	if (!isNodeOfType(importDeclaration, "ImportDeclaration")) return;
	const target = findHoistTargetScope(state.currentScope);
	for (const specifier of importDeclaration.specifiers) {
		const local = specifier.local;
		if (!isNodeOfType(local, "Identifier")) continue;
		recordSymbol(target, state, {
			name: local.name,
			kind: "import",
			bindingIdentifier: local,
			declarationNode: specifier,
			initializer: specifier
		});
		tagAsBinding(state, local);
	}
};
const handleTsDeclarations = (node, state) => {
	if (node.type !== "TSImportEqualsDeclaration" && node.type !== "TSEnumDeclaration" && node.type !== "TSTypeAliasDeclaration" && node.type !== "TSInterfaceDeclaration" && node.type !== "TSModuleDeclaration") return;
	const idNode = node.id;
	if (!idNode || !isNodeOfType(idNode, "Identifier")) return;
	const kind = node.type === "TSImportEqualsDeclaration" ? "ts-import-equals" : node.type === "TSEnumDeclaration" ? "ts-enum" : node.type === "TSTypeAliasDeclaration" ? "ts-type-alias" : node.type === "TSInterfaceDeclaration" ? "ts-interface" : "ts-module";
	recordSymbol(findHoistTargetScope(state.currentScope), state, {
		name: idNode.name,
		kind,
		bindingIdentifier: idNode,
		declarationNode: node,
		initializer: null
	});
	tagAsBinding(state, idNode);
};
const handleFunctionParameters = (params, scope, state) => {
	for (const param of params) {
		visitDestructuringDeclarations(param, null, scope, state, "parameter", param);
		for (const identifier of collectBindingNamesFromPattern(param)) tagAsBinding(state, identifier);
	}
};
const shouldPushBlockScope = (block) => {
	if (!isNodeOfType(block, "BlockStatement")) return false;
	if (isFunctionBodyBlock(block)) return false;
	if (isCatchClauseBlock(block)) return false;
	if (block.parent && block.parent.type === "TSModuleDeclaration") return false;
	return true;
};
const isJsxIdentifierBindingReference = (identifier) => {
	const parent = identifier.parent;
	if (!parent) return false;
	if (parent.type === "JSXMemberExpression") return parent.object === identifier;
	if (parent.type === "JSXNamespacedName") return false;
	const ASCII_LOWERCASE_A = 97;
	const ASCII_LOWERCASE_Z = 122;
	const firstCharCode = identifier.name.charCodeAt(0);
	if (firstCharCode >= ASCII_LOWERCASE_A && firstCharCode <= ASCII_LOWERCASE_Z) return false;
	return true;
};
const isNonReferencePosition = (identifier) => {
	const parent = identifier.parent;
	if (!parent) return false;
	switch (parent.type) {
		case "MemberExpression": return parent.property === identifier && !parent.computed;
		case "Property": return parent.key === identifier && !parent.computed && !parent.shorthand;
		case "MethodDefinition":
		case "PropertyDefinition": return parent.key === identifier && !parent.computed;
		case "JSXAttribute": return parent.name === identifier;
		case "ImportSpecifier": return parent.imported === identifier;
		case "ExportSpecifier": return parent.exported === identifier && parent.local !== parent.exported;
		case "LabeledStatement":
		case "BreakStatement":
		case "ContinueStatement": return parent.label === identifier;
		default: return false;
	}
};
const inferReferenceFlag = (identifier) => {
	const parent = identifier.parent;
	if (!parent) return "read";
	switch (parent.type) {
		case "AssignmentExpression":
			if (parent.left === identifier) return parent.operator === "=" ? "write" : "read-write";
			return "read";
		case "UpdateExpression": return "read-write";
		case "ForInStatement":
		case "ForOfStatement": return parent.left === identifier ? "write" : "read";
		default: return "read";
	}
};
const setNodeScope = (node, state) => {
	state.nodeScope.set(node, state.currentScope);
};
const walkParameterReferences = (pattern, state) => {
	if (isNodeOfType(pattern, "AssignmentPattern")) {
		walkParameterReferences(pattern.left, state);
		const defaultValue = pattern.right ?? null;
		if (defaultValue) walk(defaultValue, state);
		return;
	}
	if (isNodeOfType(pattern, "ObjectPattern")) {
		for (const property of pattern.properties) {
			const propertyNode = property;
			if (isNodeOfType(propertyNode, "RestElement")) {
				walkParameterReferences(propertyNode.argument, state);
				continue;
			}
			if (!isNodeOfType(propertyNode, "Property")) continue;
			const propertyDetail = propertyNode;
			if (propertyDetail.computed) walk(propertyDetail.key, state);
			walkParameterReferences(propertyDetail.value, state);
		}
		return;
	}
	if (isNodeOfType(pattern, "ArrayPattern")) {
		for (const element of pattern.elements) if (element) walkParameterReferences(element, state);
		return;
	}
	if (isNodeOfType(pattern, "RestElement")) walkParameterReferences(pattern.argument, state);
};
const walk = (node, state) => {
	if (isFunctionLike$2(node)) {
		if (isNodeOfType(node, "FunctionDeclaration") && node.id) handleFunctionDeclaration(node, state);
		setNodeScope(node, state);
		const fnScope = pushScope(node.type === "ArrowFunctionExpression" ? "arrow-function" : "function", node, state);
		if ((isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "FunctionDeclaration")) && node.id) {
			recordSymbol(fnScope, state, {
				name: node.id.name,
				kind: "function",
				bindingIdentifier: node.id,
				declarationNode: node,
				initializer: node
			});
			tagAsBinding(state, node.id);
		}
		const functionParams = node.params ?? [];
		handleFunctionParameters(functionParams, fnScope, state);
		for (const param of functionParams) walkParameterReferences(param, state);
		const body = node.body;
		if (body) walk(body, state);
		popScope(state);
		return;
	}
	if (isNodeOfType(node, "ClassDeclaration") || isNodeOfType(node, "ClassExpression")) {
		if (isNodeOfType(node, "ClassDeclaration") && node.id) handleClassDeclaration(node, state);
		const classScope = pushScope("class", node, state);
		setNodeScope(node, state);
		if (isNodeOfType(node, "ClassExpression") && node.id) {
			recordSymbol(classScope, state, {
				name: node.id.name,
				kind: "class",
				bindingIdentifier: node.id,
				declarationNode: node,
				initializer: node
			});
			tagAsBinding(state, node.id);
		}
		if (node.superClass) walk(node.superClass, state);
		if (node.body) walk(node.body, state);
		popScope(state);
		return;
	}
	if (isNodeOfType(node, "CatchClause")) {
		const catchScope = pushScope("catch", node, state);
		setNodeScope(node, state);
		if (node.param) {
			visitDestructuringDeclarations(node.param, null, catchScope, state, "catch-clause-parameter", node);
			for (const identifier of collectBindingNamesFromPattern(node.param)) tagAsBinding(state, identifier);
		}
		if (node.body) walk(node.body, state);
		popScope(state);
		return;
	}
	if (isNodeOfType(node, "ForStatement") || isNodeOfType(node, "ForInStatement") || isNodeOfType(node, "ForOfStatement")) {
		pushScope("for", node, state);
		setNodeScope(node, state);
		const nodeRecord = node;
		for (const key of Object.keys(nodeRecord)) {
			if (key === "parent") continue;
			if (TYPE_POSITION_CHILD_KEYS.has(key)) continue;
			const child = nodeRecord[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) walk(item, state);
			} else if (isAstNode(child)) walk(child, state);
		}
		popScope(state);
		return;
	}
	if (isNodeOfType(node, "SwitchStatement")) {
		pushScope("switch", node, state);
		setNodeScope(node, state);
		if (node.discriminant) walk(node.discriminant, state);
		for (const switchCase of node.cases) walk(switchCase, state);
		popScope(state);
		return;
	}
	if (isNodeOfType(node, "TSModuleDeclaration")) {
		const moduleScope = pushScope("ts-module", node, state);
		setNodeScope(node, state);
		if (node.id && isNodeOfType(node.id, "Identifier")) {
			const identifier = node.id;
			recordSymbol(findHoistTargetScope(moduleScope.parent ?? state.currentScope), state, {
				name: identifier.name,
				kind: "ts-module",
				bindingIdentifier: identifier,
				declarationNode: node,
				initializer: null
			});
			tagAsBinding(state, identifier);
		}
		if (node.body) walk(node.body, state);
		popScope(state);
		return;
	}
	if (isNodeOfType(node, "TSEnumDeclaration")) {
		handleTsDeclarations(node, state);
		pushScope("ts-enum", node, state);
		setNodeScope(node, state);
		const members = node.members ?? [];
		for (const member of members) walk(member, state);
		popScope(state);
		return;
	}
	if (isNodeOfType(node, "BlockStatement") && shouldPushBlockScope(node)) {
		pushScope("block", node, state);
		setNodeScope(node, state);
		for (const statement of node.body) walk(statement, state);
		popScope(state);
		return;
	}
	setNodeScope(node, state);
	if (isNodeOfType(node, "VariableDeclaration")) handleVariableDeclaration(node, state);
	else if (isNodeOfType(node, "FunctionDeclaration")) handleFunctionDeclaration(node, state);
	else if (isNodeOfType(node, "ClassDeclaration")) handleClassDeclaration(node, state);
	else if (isNodeOfType(node, "ImportDeclaration")) handleImportDeclaration(node, state);
	else if (node.type === "TSImportEqualsDeclaration" || node.type === "TSTypeAliasDeclaration" || node.type === "TSInterfaceDeclaration") handleTsDeclarations(node, state);
	if ((isNodeOfType(node, "Identifier") || isNodeOfType(node, "JSXIdentifier")) && !bindingPositionMarker.has(node) && !isNonReferencePosition(node)) if (isNodeOfType(node, "JSXIdentifier")) {
		if (isJsxIdentifierBindingReference(node)) recordReference(state, node, inferReferenceFlag(node));
	} else recordReference(state, node, inferReferenceFlag(node));
	const nodeRecord = node;
	for (const key of Object.keys(nodeRecord)) {
		if (key === "parent") continue;
		if (TYPE_POSITION_CHILD_KEYS.has(key)) continue;
		const child = nodeRecord[key];
		if (Array.isArray(child)) {
			for (const item of child) if (isAstNode(item)) walk(item, state);
		} else if (isAstNode(child)) walk(child, state);
	}
};
const resolveReferences = (rootScope) => {
	const visitScope = (scope) => {
		for (const reference of scope.references) {
			const name = reference.identifier.name;
			if (typeof name !== "string") continue;
			let lookup = scope;
			while (lookup) {
				const found = lookup.symbolsByName.get(name);
				if (found) {
					reference.resolvedSymbol = found;
					found.references.push(reference);
					break;
				}
				lookup = lookup.parent;
			}
		}
		for (const child of scope.children) visitScope(child);
	};
	visitScope(rootScope);
};
const analyzeScopes = (program) => {
	const rootScope = {
		id: 0,
		kind: "module",
		node: program,
		parent: null,
		children: [],
		symbols: [],
		references: [],
		symbolsByName: /* @__PURE__ */ new Map()
	};
	const state = {
		nextScopeId: 1,
		nextSymbolId: 0,
		nextReferenceId: 0,
		currentScope: rootScope,
		scopeStack: [rootScope],
		rootScope,
		nodeScope: /* @__PURE__ */ new WeakMap(),
		ownScopeForNode: /* @__PURE__ */ new WeakMap(),
		symbolByBindingIdentifier: /* @__PURE__ */ new WeakMap(),
		referenceByIdentifier: /* @__PURE__ */ new WeakMap()
	};
	state.nodeScope.set(program, rootScope);
	state.ownScopeForNode.set(program, rootScope);
	if (isNodeOfType(program, "Program")) for (const statement of program.body) walk(statement, state);
	else walk(program, state);
	resolveReferences(rootScope);
	const scopeFor = (node) => {
		let current = node;
		while (current) {
			const scope = state.nodeScope.get(current);
			if (scope) return scope;
			current = current.parent ?? null;
		}
		return rootScope;
	};
	const symbolFor = (identifier) => {
		const reference = state.referenceByIdentifier.get(identifier);
		if (reference) return reference.resolvedSymbol;
		const symbolForBinding = state.symbolByBindingIdentifier.get(identifier);
		if (symbolForBinding) return symbolForBinding;
		return null;
	};
	const referenceFor = (identifier) => {
		return state.referenceByIdentifier.get(identifier) ?? null;
	};
	const isGlobalReference = (identifier) => {
		const reference = state.referenceByIdentifier.get(identifier);
		if (!reference) return false;
		return reference.resolvedSymbol === null;
	};
	const ownScopeFor = (node) => {
		return state.ownScopeForNode.get(node) ?? null;
	};
	return {
		rootScope,
		scopeFor,
		ownScopeFor,
		symbolFor,
		referenceFor,
		isGlobalReference
	};
};
const isDescendantScope = (inner, outer) => {
	let current = inner;
	while (current) {
		if (current === outer) return true;
		current = current.parent;
	}
	return false;
};
//#endregion
//#region src/plugin/utils/is-ast-descendant.ts
/**
* True when `inner` is `outer` itself or any descendant in the AST
* `parent` chain. Walks `inner.parent` upward and stops at either a
* match (`true`) or the chain's root (`false`).
*
* Was duplicated byte-identical in:
* - exhaustive-deps-symbol-stability.ts
* - semantic/closure-captures.ts
*/
const isAstDescendant = (inner, outer) => {
	let current = inner;
	while (current) {
		if (current === outer) return true;
		current = current.parent ?? null;
	}
	return false;
};
//#endregion
//#region src/plugin/semantic/closure-captures.ts
const closureCaptures = (functionNode, scopes) => {
	const functionScope = scopes.ownScopeFor(functionNode) ?? scopes.scopeFor(functionNode);
	const out = [];
	const seen = /* @__PURE__ */ new Set();
	const visit = (node) => {
		if (node !== functionNode && isFunctionLike$2(node)) {
			const innerCaptures = closureCaptures(node, scopes);
			for (const reference of innerCaptures) if (reference.resolvedSymbol && !isDescendantScope(reference.resolvedSymbol.scope, functionScope)) {
				if (!seen.has(reference.id)) {
					out.push(reference);
					seen.add(reference.id);
				}
			}
			return;
		}
		const reference = scopes.referenceFor(node);
		if (reference && reference.resolvedSymbol) {
			if (!isDescendantScope(reference.resolvedSymbol.scope, functionScope)) {
				if (!seen.has(reference.id)) {
					out.push(reference);
					seen.add(reference.id);
				}
			}
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			if (TYPE_POSITION_CHILD_KEYS.has(key)) continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(functionNode);
	return out.filter((reference) => isAstDescendant(reference.identifier, functionNode));
};
//#endregion
//#region src/plugin/utils/is-react-hook-name.ts
const isReactHookName = (name) => {
	if (!name.startsWith("use")) return false;
	if (name.length === 3) return true;
	const fourthCharacter = name.charCodeAt(3);
	return fourthCharacter >= 65 && fourthCharacter <= 90 || fourthCharacter >= 48 && fourthCharacter <= 57;
};
//#endregion
//#region src/plugin/utils/is-react-component-or-hook-name.ts
const isReactComponentOrHookName = (name) => isReactComponentName(name) || isReactHookName(name);
//#endregion
//#region src/plugin/rules/react-builtins/exhaustive-deps-low-level.ts
/**
* Lowest-level helpers consumed by both the main `exhaustive-deps`
* rule body AND the symbol-stability cluster
* (`exhaustive-deps-symbol-stability.ts`). Sit in their own module so
* the two top-level files can each import without a circular
* dependency.
*
* Behaviour mirrors the previous inlined versions in `exhaustive-deps.ts`
* exactly. The doc comment that used to argue against reusing the
* shared `stripParenExpression` util still applies: this module's
* `TRANSPARENT_WRAPPER_TYPES.has(...)` membership check is also read
* directly by the member-chain walker in the main rule, so keeping
* both `TRANSPARENT_WRAPPER_TYPES` and `unwrapExpression` co-located
* here keeps that intent in one place.
*/
/**
* Strip TypeScript expression wrappers transparently — `(x as T)`,
* `x satisfies T`, `x!`, `(x)` — so they don't change the dep key.
*/
const TRANSPARENT_WRAPPER_TYPES = new Set([
	"TSAsExpression",
	"TSSatisfiesExpression",
	"TSNonNullExpression",
	"TSTypeAssertion",
	"ParenthesizedExpression",
	"ChainExpression"
]);
const unwrapExpression$1 = (node) => {
	let current = node;
	while (TRANSPARENT_WRAPPER_TYPES.has(current.type)) {
		const inner = current.expression;
		if (!inner) return current;
		current = inner;
	}
	return current;
};
/**
* Get the hook name from a call expression's callee, regardless of
* whether the hook is called as `useFoo()` (Identifier) or
* `React.useFoo()` (MemberExpression).
*/
const getHookName = (callee) => {
	if (isNodeOfType(callee, "Identifier")) return callee.name;
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed && isNodeOfType(callee.property, "Identifier")) return callee.property.name;
	return null;
};
const FUNCTION_SCOPE_KINDS = new Set([
	"function",
	"arrow-function",
	"method"
]);
/**
* True for symbols declared at module scope (outside any function
* scope). Module-scope bindings don't change between renders so they
* don't need to live in dependency arrays.
*/
const isOutsideAllFunctions = (symbol) => {
	let scope = symbol.scope;
	while (scope) {
		if (FUNCTION_SCOPE_KINDS.has(scope.kind)) return false;
		if (scope.kind === "module") return true;
		scope = scope.parent ?? null;
	}
	return true;
};
//#endregion
//#region src/plugin/rules/react-builtins/exhaustive-deps-messages.ts
const buildMissingDepMessage = (hookName, depName) => `\`${hookName}\` can run with a stale \`${depName}\` & show your users old data.`;
const buildUnnecessaryDepMessage = (hookName, depName) => `\`${hookName}\` re-runs whenever \`${depName}\` changes even though it never uses it.`;
const buildDuplicateDepMessage = (hookName, depName) => `\`${hookName}\` lists \`${depName}\` twice in its dependency array.`;
const buildLiteralDepMessage = (hookName) => `A literal in \`${hookName}\`'s dependency array never changes & does nothing.`;
const buildRefCurrentDepMessage = (hookName, depName) => `\`${hookName}\` won't re-run when \`${depName}\` changes, since a ref never triggers a redraw.`;
const buildNonArrayDepsMessage = (hookName) => `\`${hookName}\`'s dependencies can't be checked because its second argument isn't an inline array.`;
const buildMissingDepArrayMessage = (hookName) => `\`${hookName}\` re-runs on every render with no dependency array.`;
const buildMissingCallbackMessage = (hookName) => `\`${hookName}\` crashes without a function as its first argument.`;
const buildEffectEventDepMessage = () => `A function from \`useEffectEvent\` is stable & shouldn't sit in the dependency array.`;
const buildSpreadDepMessage = (hookName) => `\`${hookName}\`'s dependencies can't be checked because of a spread in the array.`;
const buildComplexDepMessage = (hookName) => `\`${hookName}\`'s dependencies can't be checked because of a complex expression in the array.`;
const buildAsyncEffectMessage = (hookName) => `\`${hookName}\` was given an async function, so its cleanup breaks.`;
const buildUnknownCallbackMessage = (hookName) => `\`${hookName}\`'s dependencies can't be checked because its function is defined elsewhere.`;
const buildUnstableDepMessage = (hookName, depName) => `\`${depName}\` is rebuilt every render, so \`${hookName}\` runs every time.`;
const buildSetStateWithoutDepsMessage = (hookName, setterName) => `\`${hookName}\` calls \`${setterName}\` with no dependency array, so it can loop forever & freeze the component.`;
const buildRefCleanupMessage = (depName) => `Your cleanup may read the wrong node since the ref \`${depName}\` can change before it runs.`;
const buildAssignmentMessage = (name) => `Assigning to \`${name}\` inside a hook is thrown away after each render.`;
//#endregion
//#region src/plugin/rules/react-builtins/exhaustive-deps-settings.ts
const resolveExhaustiveDepsSettings = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const reactHooks = settings?.["react-hooks"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.exhaustiveDeps ?? {} : {};
	const upstreamSettings = typeof reactHooks === "object" && reactHooks !== null ? reactHooks : {};
	return {
		additionalHooks: ruleSettings.additionalHooks ?? ruleSettings.additionalEffectHooks ?? upstreamSettings.additionalHooks ?? upstreamSettings.additionalEffectHooks ?? "",
		additionalEffectHooks: ruleSettings.additionalEffectHooks ?? upstreamSettings.additionalEffectHooks ?? "",
		enableDangerousAutofixThisMayCauseInfiniteLoops: ruleSettings.enableDangerousAutofixThisMayCauseInfiniteLoops ?? false,
		experimental_autoDependenciesHooks: ruleSettings.experimental_autoDependenciesHooks ?? upstreamSettings.experimental_autoDependenciesHooks ?? [],
		requireExplicitEffectDeps: ruleSettings.requireExplicitEffectDeps ?? upstreamSettings.requireExplicitEffectDeps ?? false
	};
};
//#endregion
//#region src/plugin/rules/react-builtins/exhaustive-deps-symbol-stability.ts
/**
* Symbol-stability helpers consumed by the `exhaustive-deps` rule.
*
* One cohesive concept: "given a captured symbol, is its value
* structurally stable across re-renders (and therefore unnecessary
* in a deps array)?". The rule reads `symbolHasStableValue` /
* `symbolHasStableHookOrigin` / `symbolHasUseEffectEventOrigin` /
* `isRecursiveInitializerCapture` at multiple sites — extracting
* them lets the rule body stay focused on the diff-the-captured-vs-
* declared logic.
*
* Low-level helpers (`unwrapExpression`, `getHookName`,
* `isOutsideAllFunctions`) live in
* `./exhaustive-deps-low-level.ts` so both this module and the main
* rule can import them without a circular dependency.
*/
/**
* True for symbols whose returned value (or destructured pieces) are
* stable across re-renders and don't need to live in deps arrays:
*   - useState's setter (`setX`)
*   - useReducer's dispatch
*   - useRef's ref object
*   - useEffectEvent's return value
*   - primitive-literal local consts (the value never changes
*     between renders unless the literal does)
*/
const symbolHasStableHookOrigin = (symbol) => {
	if (symbol.references.some((reference) => reference.flag !== "read")) return false;
	let declarator = symbol.declarationNode;
	while (declarator && declarator.type !== "VariableDeclarator") declarator = declarator.parent ?? null;
	if (!declarator || !isNodeOfType(declarator, "VariableDeclarator")) return false;
	const initializerRaw = declarator.init;
	if (!initializerRaw) return false;
	const initializer = unwrapExpression$1(initializerRaw);
	if (symbol.kind === "const") {
		if (isNodeOfType(initializer, "Literal") && (initializer.value === null || typeof initializer.value === "number" || typeof initializer.value === "string" || typeof initializer.value === "boolean")) return true;
		if (isNodeOfType(initializer, "TemplateLiteral") && getStaticTemplateLiteralValue(initializer) !== null) return true;
	}
	if (!isNodeOfType(initializer, "CallExpression")) return false;
	const initializerHookName = getHookName(initializer.callee);
	if (!initializerHookName) return false;
	if (initializerHookName === "useRef") return true;
	if (initializerHookName === "useEffectEvent") return true;
	if (initializerHookName === "useState" || initializerHookName === "useReducer" || initializerHookName === "useActionState" || initializerHookName === "useTransition") {
		if (!isNodeOfType(declarator.id, "ArrayPattern")) return false;
		const stableElement = declarator.id.elements[1];
		if (!stableElement) return false;
		const innerBinding = isNodeOfType(stableElement, "AssignmentPattern") ? stableElement.left : stableElement;
		return isNodeOfType(innerBinding, "Identifier") && symbol.bindingIdentifier === innerBinding;
	}
	return false;
};
const symbolHasUseEffectEventOrigin = (symbol) => {
	const initializer = symbol.initializer ? unwrapExpression$1(symbol.initializer) : null;
	if (!initializer || !isNodeOfType(initializer, "CallExpression")) return false;
	return getHookName(initializer.callee) === "useEffectEvent";
};
const getFunctionValueNode = (symbol) => {
	if (symbol.kind === "function" && isNodeOfType(symbol.declarationNode, "FunctionDeclaration")) return symbol.declarationNode;
	const initializer = symbol.initializer ? unwrapExpression$1(symbol.initializer) : null;
	if (initializer && (isNodeOfType(initializer, "FunctionExpression") || isNodeOfType(initializer, "ArrowFunctionExpression"))) return initializer;
	return null;
};
const isRecursiveInitializerCapture = (symbol, callback) => {
	const initializer = symbol.initializer;
	return Boolean(initializer && isAstDescendant(callback, initializer));
};
const symbolHasStableFunctionOrigin = (symbol, scopes, visitedSymbolIds) => {
	if (visitedSymbolIds.has(symbol.id)) return true;
	const functionNode = getFunctionValueNode(symbol);
	if (!functionNode) return false;
	visitedSymbolIds.add(symbol.id);
	for (const reference of closureCaptures(functionNode, scopes)) {
		const capturedSymbol = reference.resolvedSymbol;
		if (!capturedSymbol) continue;
		if (capturedSymbol.id === symbol.id) continue;
		if (isOutsideAllFunctions(capturedSymbol)) continue;
		if (symbolHasStableValue(capturedSymbol, scopes, visitedSymbolIds)) continue;
		return false;
	}
	return true;
};
const symbolHasStableValue = (symbol, scopes, visitedSymbolIds = /* @__PURE__ */ new Set()) => symbolHasStableHookOrigin(symbol) || symbolHasStableFunctionOrigin(symbol, scopes, visitedSymbolIds);
//#endregion
//#region src/plugin/rules/react-builtins/exhaustive-deps.ts
const HOOKS_REQUIRING_DEPS_MATCH = new Set([
	"useEffect",
	"useLayoutEffect",
	"useCallback",
	"useMemo",
	"useImperativeHandle",
	"useInsertionEffect"
]);
const HOOKS_REQUIRING_DEPS_ARRAY = new Set(["useMemo", "useCallback"]);
const EFFECT_HOOKS_ALLOWING_EXTRA_REACTIVE_DEPS = new Set([
	"useEffect",
	"useLayoutEffect",
	"useInsertionEffect"
]);
const buildAdditionalHooksRegex = (additional) => {
	if (!additional) return null;
	try {
		return new RegExp(additional);
	} catch {
		return null;
	}
};
const getCallExpressionCalleeName$1 = (callExpression) => {
	const callee = callExpression.callee;
	if (isNodeOfType(callee, "Identifier")) return callee.name;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && isNodeOfType(callee.property, "Identifier") && !callee.computed) return `${callee.object.name}.${callee.property.name}`;
	return null;
};
const inferFunctionName$1 = (functionNode) => {
	if ((isNodeOfType(functionNode, "FunctionDeclaration") || isNodeOfType(functionNode, "FunctionExpression")) && functionNode.id) return functionNode.id.name;
	let parent = functionNode.parent;
	while (parent && isNodeOfType(parent, "CallExpression")) {
		const calleeName = getCallExpressionCalleeName$1(parent);
		if (calleeName && REACT_HOC_NAMES.has(calleeName)) parent = parent.parent ?? null;
		else break;
	}
	if (parent && isNodeOfType(parent, "VariableDeclarator") && isNodeOfType(parent.id, "Identifier")) return parent.id.name;
	return null;
};
const findEnclosingComponentOrHookFunction$1 = (node) => {
	let current = node.parent;
	while (current) {
		if (isNodeOfType(current, "FunctionDeclaration") || isNodeOfType(current, "FunctionExpression") || isNodeOfType(current, "ArrowFunctionExpression")) {
			const functionName = inferFunctionName$1(current);
			if (functionName && isReactComponentOrHookName(functionName)) return current;
		}
		current = current.parent ?? null;
	}
	return null;
};
const getCallbackArgumentIndex = (hookName) => hookName === "useImperativeHandle" ? 1 : 0;
const getDepsArgumentIndex = (hookName) => hookName === "useImperativeHandle" ? 2 : 1;
const getDestructuredPropertyPath = (pattern) => {
	if (!isNodeOfType(pattern, "ObjectPattern")) return null;
	const firstProperty = pattern.properties[0];
	if (!firstProperty || !isNodeOfType(firstProperty, "Property")) return null;
	const key = firstProperty.key;
	const value = firstProperty.value;
	const keyName = isNodeOfType(key, "Identifier") ? key.name : isNodeOfType(key, "Literal") && typeof key.value === "string" ? key.value : null;
	if (!keyName) return null;
	const nestedPath = getDestructuredPropertyPath(value);
	return nestedPath ? `${keyName}.${nestedPath}` : keyName;
};
const flattenReferenceRootName = (reference) => {
	const referencedIdentifier = reference.identifier;
	if (isNodeOfType(referencedIdentifier, "Identifier")) return referencedIdentifier.name;
	if (isNodeOfType(referencedIdentifier, "JSXIdentifier")) return referencedIdentifier.name;
	return "";
};
const computeDepKey = (reference) => {
	const referencedIdentifier = reference.identifier;
	let parent = referencedIdentifier.parent ?? null;
	if (parent && parent.type === "ChainExpression") parent = parent.parent ?? null;
	if (!parent || !isNodeOfType(parent, "MemberExpression") || parent.object !== referencedIdentifier) {
		if (parent && isNodeOfType(parent, "VariableDeclarator") && parent.init === referencedIdentifier) {
			const destructuredPath = getDestructuredPropertyPath(parent.id);
			const rootName = flattenReferenceRootName(reference);
			if (destructuredPath && rootName) return `${rootName}.${destructuredPath}`;
		}
		return flattenReferenceRootName(reference);
	}
	let outermost = parent;
	while (true) {
		const grandparent = outermost.parent;
		if (!grandparent) break;
		const isTransparentWrapper = TRANSPARENT_WRAPPER_TYPES.has(grandparent.type);
		const candidate = isTransparentWrapper ? grandparent.parent : grandparent;
		const expectedObject = isTransparentWrapper ? grandparent : outermost;
		if (candidate && isNodeOfType(candidate, "MemberExpression") && candidate.object === expectedObject) {
			outermost = candidate;
			continue;
		}
		break;
	}
	const fullName = stringifyMemberChain(outermost);
	if (fullName === null) return flattenReferenceRootName(reference);
	const declarator = outermost.parent;
	if (declarator && isNodeOfType(declarator, "VariableDeclarator") && declarator.init === outermost) {
		const destructuredPath = getDestructuredPropertyPath(declarator.id);
		if (destructuredPath) return `${fullName}.${destructuredPath}`;
	}
	if (reference.flag !== "read") {
		const lastDotIndex = fullName.lastIndexOf(".");
		if (lastDotIndex !== -1) return fullName.slice(0, lastDotIndex);
	}
	if (fullName.endsWith(".current")) return fullName.slice(0, -8);
	return fullName;
};
const computeDeclaredDepKey = (entry) => {
	const stripped = unwrapExpression$1(entry);
	if (isNodeOfType(stripped, "Identifier")) return stripped.name;
	if (isNodeOfType(stripped, "MemberExpression")) return stringifyMemberChain(stripped);
	return null;
};
const depsArrayContainsIdentifier = (depsArgument, identifierName) => {
	if (!depsArgument) return false;
	const strippedDepsArgument = unwrapExpression$1(depsArgument);
	if (!isNodeOfType(strippedDepsArgument, "ArrayExpression")) return false;
	return strippedDepsArgument.elements.some((element) => {
		if (!element) return false;
		const strippedElement = unwrapExpression$1(element);
		return isNodeOfType(strippedElement, "Identifier") && strippedElement.name === identifierName;
	});
};
const stringifyMemberChain = (node) => {
	const stripped = unwrapExpression$1(node);
	if (isNodeOfType(stripped, "Identifier")) return stripped.name;
	if (isNodeOfType(stripped, "ThisExpression")) return "this";
	if (isNodeOfType(stripped, "MemberExpression")) {
		const objectName = stringifyMemberChain(stripped.object);
		if (objectName && stripped.computed) return objectName;
		if (objectName && !stripped.computed && isNodeOfType(stripped.property, "Identifier")) return `${objectName}.${stripped.property.name}`;
	}
	return null;
};
const collectCaptureDepKeys = (callback, scopes) => {
	const keys = /* @__PURE__ */ new Set();
	const stableCapturedNames = /* @__PURE__ */ new Set();
	const componentOrHookFunction = findEnclosingComponentOrHookFunction$1(callback);
	const componentOrHookScope = componentOrHookFunction ? scopes.ownScopeFor(componentOrHookFunction) : null;
	for (const reference of closureCaptures(callback, scopes)) {
		const symbol = reference.resolvedSymbol;
		if (!symbol) continue;
		if (isRecursiveInitializerCapture(symbol, callback)) continue;
		if (symbolHasStableValue(symbol, scopes)) {
			stableCapturedNames.add(symbol.name);
			continue;
		}
		if (isOutsideAllFunctions(symbol)) continue;
		if (componentOrHookScope && !isDescendantScope(symbol.scope, componentOrHookScope)) continue;
		const depKey = computeDepKey(reference);
		if (!depKey) continue;
		keys.add(depKey);
	}
	return {
		keys,
		stableCapturedNames
	};
};
const isLiteralOrEmptyTemplate = (node) => isNodeOfType(node, "Literal") || isNodeOfType(node, "TemplateLiteral") && getStaticTemplateLiteralValue(node) !== null;
const isNonStringLiteral = (node) => isNodeOfType(node, "Literal") && typeof node.value !== "string";
const isMatchingDepOrPrefix = (declaredKey, captureKey) => captureKey === declaredKey || captureKey.startsWith(`${declaredKey}.`);
const hasBroaderDeclaredDependency = (declaredKey, declaredKeys) => {
	for (const otherDeclaredKey of declaredKeys) if (otherDeclaredKey !== declaredKey && declaredKey.startsWith(`${otherDeclaredKey}.`)) return true;
	return false;
};
const getMemberRootIdentifier = (node) => {
	const stripped = unwrapExpression$1(node);
	if (isNodeOfType(stripped, "Identifier")) return stripped;
	if (isNodeOfType(stripped, "MemberExpression")) return getMemberRootIdentifier(stripped.object);
	return null;
};
const hasComputedMemberExpression = (node) => {
	const stripped = unwrapExpression$1(node);
	if (!isNodeOfType(stripped, "MemberExpression")) return false;
	if (stripped.computed) return true;
	return hasComputedMemberExpression(stripped.object);
};
const isExtraEffectDepAllowed = (node, scopes) => {
	const rootIdentifier = getMemberRootIdentifier(node);
	if (!rootIdentifier) return false;
	const symbol = scopes.symbolFor(rootIdentifier);
	return Boolean(symbol && !isOutsideAllFunctions(symbol));
};
const getRootSymbol = (node, scopes) => {
	const rootIdentifier = getMemberRootIdentifier(node);
	return rootIdentifier ? scopes.symbolFor(rootIdentifier) : null;
};
const isRegExpLiteral = (node) => {
	if (!isNodeOfType(node, "Literal")) return false;
	return Boolean(node.regex);
};
const isUnstableInitializer = (node) => {
	if (!node) return false;
	const stripped = unwrapExpression$1(node);
	if (isRegExpLiteral(stripped)) return true;
	if (isNodeOfType(stripped, "ConditionalExpression")) return isUnstableInitializer(stripped.consequent) || isUnstableInitializer(stripped.alternate);
	if (isNodeOfType(stripped, "LogicalExpression")) return isUnstableInitializer(stripped.left) || isUnstableInitializer(stripped.right);
	return isNodeOfType(stripped, "ObjectExpression") || isNodeOfType(stripped, "ArrayExpression") || isNodeOfType(stripped, "ClassExpression") || isNodeOfType(stripped, "ClassDeclaration") || isNodeOfType(stripped, "JSXElement") || isNodeOfType(stripped, "JSXFragment") || isNodeOfType(stripped, "AssignmentExpression") || isNodeOfType(stripped, "NewExpression");
};
const hasDirectIdentifierDeclarator = (symbol) => isNodeOfType(symbol.declarationNode, "VariableDeclarator") && isNodeOfType(symbol.declarationNode.id, "Identifier") || isNodeOfType(symbol.declarationNode, "ClassDeclaration");
const isFunctionValueSymbol = (symbol) => getFunctionValueNode(symbol) !== null;
const isStableSetterLikeSymbol = (symbol) => {
	if (!symbolHasStableHookOrigin(symbol)) return false;
	return symbol.name.startsWith("set") || symbol.name.startsWith("dispatch") || symbol.name.startsWith("startTransition");
};
const findStableSetterReference = (node, scopes) => {
	let setterName = null;
	const visit = (current) => {
		if (setterName) return;
		if (current !== node && (isNodeOfType(current, "FunctionDeclaration") || isNodeOfType(current, "FunctionExpression") || isNodeOfType(current, "ArrowFunctionExpression"))) return;
		if (isNodeOfType(current, "Identifier")) {
			const symbol = scopes.referenceFor(current)?.resolvedSymbol;
			if (symbol && isStableSetterLikeSymbol(symbol)) {
				setterName = symbol.name;
				return;
			}
		}
		const record = current;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(node);
	return setterName;
};
const collectOuterAssignments = (callback, scopes) => {
	const callbackScope = scopes.ownScopeFor(callback) ?? scopes.scopeFor(callback);
	const assignments = [];
	const seenNames = /* @__PURE__ */ new Set();
	const visit = (node) => {
		if (isNodeOfType(node, "AssignmentExpression") && isNodeOfType(node.left, "Identifier")) {
			const symbol = scopes.symbolFor(node.left);
			if (symbol && !seenNames.has(symbol.name) && !isOutsideAllFunctions(symbol) && !isDescendantScope(symbol.scope, callbackScope)) {
				seenNames.add(symbol.name);
				assignments.push({
					name: symbol.name,
					node: node.left
				});
			}
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(callback);
	return assignments;
};
const getRefCurrentNameFromMemberExpression = (node) => {
	const chain = stringifyMemberChain(node);
	if (!chain) return null;
	const currentIndex = chain.indexOf(".current");
	return currentIndex === -1 ? null : chain.slice(0, currentIndex + 8);
};
const findRefCurrentInCleanup = (callback, scopes) => {
	let cleanupFunction = null;
	const findReturn = (node) => {
		if (cleanupFunction) return;
		if (node !== callback && (isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression"))) return;
		if (isNodeOfType(node, "ReturnStatement")) {
			const argument = node.argument;
			if (argument && (isNodeOfType(argument, "FunctionExpression") || isNodeOfType(argument, "ArrowFunctionExpression"))) {
				cleanupFunction = argument;
				return;
			}
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) findReturn(item);
			} else if (isAstNode(child)) findReturn(child);
		}
	};
	findReturn(callback);
	if (!cleanupFunction) return null;
	let refCurrentName = null;
	const visitCleanup = (node) => {
		if (refCurrentName) return;
		if (isNodeOfType(node, "MemberExpression")) {
			const candidateName = getRefCurrentNameFromMemberExpression(node);
			if (candidateName) {
				const rootIdentifier = getMemberRootIdentifier(node);
				const symbol = rootIdentifier ? scopes.symbolFor(rootIdentifier) : null;
				const callbackScope = scopes.ownScopeFor(callback) ?? scopes.scopeFor(callback);
				if (!symbol || !isDescendantScope(symbol.scope, callbackScope)) {
					refCurrentName = candidateName;
					return;
				}
			}
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visitCleanup(item);
			} else if (isAstNode(child)) visitCleanup(child);
		}
	};
	visitCleanup(cleanupFunction);
	return refCurrentName;
};
const hasRefCurrentAssignment = (callback, refCurrentName) => {
	let didAssignRefCurrent = false;
	const visit = (node) => {
		if (didAssignRefCurrent) return;
		if (node !== callback && (isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression"))) return;
		if (isNodeOfType(node, "AssignmentExpression")) {
			if (getRefCurrentNameFromMemberExpression(node.left) === refCurrentName) {
				didAssignRefCurrent = true;
				return;
			}
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(callback);
	return didAssignRefCurrent;
};
const isOuterFunctionScopeDep = (node, callback, scopes) => {
	const rootIdentifier = getMemberRootIdentifier(node);
	if (!rootIdentifier) return false;
	const symbol = scopes.symbolFor(rootIdentifier);
	if (!symbol || isOutsideAllFunctions(symbol)) return false;
	const componentOrHookFunction = findEnclosingComponentOrHookFunction$1(callback);
	const componentOrHookScope = componentOrHookFunction ? scopes.ownScopeFor(componentOrHookFunction) : null;
	return Boolean(componentOrHookScope && !isDescendantScope(symbol.scope, componentOrHookScope));
};
const hasMemberCallForRoot = (node, rootName) => {
	let didFindMemberCall = false;
	const visit = (current) => {
		if (didFindMemberCall) return;
		if (isNodeOfType(current, "CallExpression")) {
			if (getMemberRootIdentifier(unwrapExpression$1(current.callee))?.name === rootName) {
				didFindMemberCall = true;
				return;
			}
		}
		const record = current;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(node);
	return didFindMemberCall;
};
const addAggregatePropsDependency = (captureKeys, declaredKeys, callback) => {
	if ([...captureKeys].filter((captureKey) => captureKey.startsWith("props.")).length < 2 || declaredKeys.has("props")) return;
	if (hasMemberCallForRoot(callback, "props")) captureKeys.add("props");
};
const exhaustiveDeps = defineRule({
	id: "exhaustive-deps",
	title: "Missing effect dependencies",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: `Don't blindly add missing dependencies. Read the hook callback first.

Bad:
useEffect(() => {
  setCount(count + 1);
}, [count]);

Better:
useEffect(() => {
  setCount((currentCount) => currentCount + 1);
}, []);

If the missing value is recreated every render, move it inside the hook or stabilize it before adding it to deps.`,
	category: "Correctness",
	create: (context) => {
		const settings = resolveExhaustiveDepsSettings(context.settings);
		const additionalHooksRegex = buildAdditionalHooksRegex(settings.additionalHooks);
		const isHookOfInterest = (hookName, callee) => {
			if (HOOKS_REQUIRING_DEPS_MATCH.has(hookName)) return true;
			if (additionalHooksRegex && isNodeOfType(callee, "Identifier") && additionalHooksRegex.test(hookName)) return true;
			return false;
		};
		const isAutoDependenciesHook = (hookName) => settings.experimental_autoDependenciesHooks.includes(hookName);
		return { CallExpression(node) {
			const hookName = getHookName(node.callee);
			if (!hookName || !isHookOfInterest(hookName, node.callee)) return;
			const callbackArgumentIndex = getCallbackArgumentIndex(hookName);
			const depsArgumentIndex = getDepsArgumentIndex(hookName);
			const callbackArgument = node.arguments[callbackArgumentIndex];
			if (!callbackArgument) {
				context.report({
					node,
					message: buildMissingCallbackMessage(hookName)
				});
				return;
			}
			const depsArgumentRaw = node.arguments[depsArgumentIndex];
			const callbackExpression = unwrapExpression$1(callbackArgument);
			let callbackToAnalyze = null;
			const forcedCaptureKeys = /* @__PURE__ */ new Set();
			if (isNodeOfType(callbackExpression, "ArrowFunctionExpression") || isNodeOfType(callbackExpression, "FunctionExpression")) callbackToAnalyze = callbackExpression;
			else if (isNodeOfType(callbackExpression, "Identifier")) {
				const callbackSymbol = context.scopes.symbolFor(callbackExpression);
				const functionValueNode = callbackSymbol ? getFunctionValueNode(callbackSymbol) : null;
				if (functionValueNode) callbackToAnalyze = functionValueNode;
				else if (callbackSymbol?.initializer && isNodeOfType(unwrapExpression$1(callbackSymbol.initializer), "CallExpression")) forcedCaptureKeys.add(callbackExpression.name);
				else if (depsArgumentRaw) {
					if (depsArrayContainsIdentifier(depsArgumentRaw, callbackExpression.name)) return;
					context.report({
						node: callbackExpression,
						message: buildUnknownCallbackMessage(hookName)
					});
					return;
				}
			} else if (depsArgumentRaw) {
				context.report({
					node: callbackArgument,
					message: buildUnknownCallbackMessage(hookName)
				});
				return;
			} else {
				if (HOOKS_REQUIRING_DEPS_ARRAY.has(hookName) && !node.arguments[depsArgumentIndex]) context.report({
					node,
					message: buildMissingDepArrayMessage(hookName)
				});
				return;
			}
			if (callbackToAnalyze && (isNodeOfType(callbackToAnalyze, "ArrowFunctionExpression") || isNodeOfType(callbackToAnalyze, "FunctionExpression")) && callbackToAnalyze.async && (EFFECT_HOOKS_ALLOWING_EXTRA_REACTIVE_DEPS.has(hookName) || hookName === "useEffect")) context.report({
				node: callbackArgument,
				message: buildAsyncEffectMessage(hookName)
			});
			if (callbackToAnalyze) {
				const outerAssignments = collectOuterAssignments(callbackToAnalyze, context.scopes);
				for (const assignment of outerAssignments) context.report({
					node: assignment.node,
					message: buildAssignmentMessage(assignment.name)
				});
				if (outerAssignments.length > 0) return;
				const refCurrentName = findRefCurrentInCleanup(callbackToAnalyze, context.scopes);
				const shouldCheckRefCleanup = EFFECT_HOOKS_ALLOWING_EXTRA_REACTIVE_DEPS.has(hookName) || Boolean(additionalHooksRegex && additionalHooksRegex.test(hookName));
				if (refCurrentName && shouldCheckRefCleanup && !hasRefCurrentAssignment(callbackToAnalyze, refCurrentName)) context.report({
					node: callbackToAnalyze,
					message: buildRefCleanupMessage(refCurrentName)
				});
			}
			if (!depsArgumentRaw) {
				if (callbackToAnalyze && EFFECT_HOOKS_ALLOWING_EXTRA_REACTIVE_DEPS.has(hookName)) {
					const setterName = findStableSetterReference(callbackToAnalyze, context.scopes);
					if (setterName) {
						context.report({
							node: callbackToAnalyze,
							message: buildSetStateWithoutDepsMessage(hookName, setterName)
						});
						return;
					}
				}
				if (HOOKS_REQUIRING_DEPS_ARRAY.has(hookName) || settings.requireExplicitEffectDeps && HOOKS_REQUIRING_DEPS_MATCH.has(hookName)) context.report({
					node,
					message: buildMissingDepArrayMessage(hookName)
				});
				return;
			}
			const depsArgument = unwrapExpression$1(depsArgumentRaw);
			if (isNodeOfType(depsArgument, "Literal") && depsArgument.value === null || isNodeOfType(depsArgument, "Identifier") && depsArgument.name === "undefined") {
				if (isAutoDependenciesHook(hookName)) return;
				if (HOOKS_REQUIRING_DEPS_ARRAY.has(hookName)) {
					context.report({
						node: depsArgument,
						message: buildMissingDepArrayMessage(hookName)
					});
					return;
				}
				const nonArrayCaptureKeys = callbackToAnalyze !== null ? new Set(collectCaptureDepKeys(callbackToAnalyze, context.scopes).keys) : /* @__PURE__ */ new Set();
				for (const forcedCaptureKey of forcedCaptureKeys) nonArrayCaptureKeys.add(forcedCaptureKey);
				if (nonArrayCaptureKeys.size > 0) {
					context.report({
						node: depsArgument,
						message: buildNonArrayDepsMessage(hookName)
					});
					context.report({
						node: depsArgument,
						message: buildMissingDepMessage(hookName, [...nonArrayCaptureKeys].join(", "))
					});
				}
				return;
			}
			if (!isNodeOfType(depsArgument, "ArrayExpression")) {
				context.report({
					node: depsArgument,
					message: buildNonArrayDepsMessage(hookName)
				});
				const nonArrayCaptureKeys = callbackToAnalyze !== null ? new Set(collectCaptureDepKeys(callbackToAnalyze, context.scopes).keys) : /* @__PURE__ */ new Set();
				for (const forcedCaptureKey of forcedCaptureKeys) nonArrayCaptureKeys.add(forcedCaptureKey);
				if (nonArrayCaptureKeys.size > 0) context.report({
					node: depsArgument,
					message: buildMissingDepMessage(hookName, [...nonArrayCaptureKeys].join(", "))
				});
				return;
			}
			const { keys: captureKeys, stableCapturedNames } = collectCaptureDepKeys(callbackToAnalyze ?? callbackArgument, context.scopes);
			for (const forcedCaptureKey of forcedCaptureKeys) captureKeys.add(forcedCaptureKey);
			const hasLiteralDepElement = depsArgument.elements.some((element) => {
				if (!element) return false;
				return isLiteralOrEmptyTemplate(unwrapExpression$1(element));
			});
			const hasNonStringLiteralDep = depsArgument.elements.some((element) => {
				if (!element) return false;
				return isNonStringLiteral(unwrapExpression$1(element));
			});
			if (hasNonStringLiteralDep) context.report({
				node: depsArgument,
				message: buildLiteralDepMessage(hookName)
			});
			const declaredKeys = /* @__PURE__ */ new Set();
			const declaredKeyToReportNode = /* @__PURE__ */ new Map();
			const seenDeclaredKeys = /* @__PURE__ */ new Set();
			let didReportRefCurrentDep = false;
			let didReportDuplicateDep = false;
			for (const element of depsArgument.elements) {
				if (!element) continue;
				const elementNode = element;
				if (isNodeOfType(elementNode, "SpreadElement")) {
					context.report({
						node: elementNode,
						message: buildSpreadDepMessage(hookName)
					});
					continue;
				}
				const stripped = unwrapExpression$1(elementNode);
				if (isLiteralOrEmptyTemplate(stripped)) continue;
				if (isNodeOfType(stripped, "Identifier")) {
					const depSymbol = context.scopes.symbolFor(stripped);
					if (depSymbol && symbolHasUseEffectEventOrigin(depSymbol)) {
						context.report({
							node: elementNode,
							message: buildEffectEventDepMessage()
						});
						continue;
					}
				}
				const fullChain = stringifyMemberChain(stripped);
				if (fullChain && fullChain.endsWith(".current") && isNodeOfType(stripped, "MemberExpression") && isNodeOfType(stripped.object, "Identifier")) {
					const refSymbol = context.scopes.symbolFor(stripped.object);
					if (refSymbol && symbolHasStableHookOrigin(refSymbol)) {
						if (!didReportRefCurrentDep) {
							context.report({
								node: elementNode,
								message: buildRefCurrentDepMessage(hookName, fullChain)
							});
							didReportRefCurrentDep = true;
						}
						continue;
					}
				}
				const key = computeDeclaredDepKey(elementNode);
				if (key === null) {
					context.report({
						node: elementNode,
						message: buildComplexDepMessage(hookName)
					});
					continue;
				}
				if (hasComputedMemberExpression(stripped)) {
					context.report({
						node: elementNode,
						message: buildComplexDepMessage(hookName)
					});
					continue;
				}
				if (seenDeclaredKeys.has(key)) {
					context.report({
						node: elementNode,
						message: buildDuplicateDepMessage(hookName, key)
					});
					didReportDuplicateDep = true;
					continue;
				}
				seenDeclaredKeys.add(key);
				declaredKeys.add(key);
				declaredKeyToReportNode.set(key, elementNode);
			}
			addAggregatePropsDependency(captureKeys, declaredKeys, callbackToAnalyze ?? callbackArgument);
			const missingCaptureKeys = [];
			for (const captureKey of captureKeys) {
				let isCoveredByDeclared = false;
				for (const declaredKey of declaredKeys) if (isMatchingDepOrPrefix(declaredKey, captureKey)) {
					isCoveredByDeclared = true;
					break;
				}
				if (isCoveredByDeclared) continue;
				missingCaptureKeys.push(captureKey);
			}
			const shouldGroupMissingDeps = !hasLiteralDepElement;
			if (missingCaptureKeys.length > 0 && shouldGroupMissingDeps) context.report({
				node: depsArgument,
				message: buildMissingDepMessage(hookName, missingCaptureKeys.join(", "))
			});
			else for (const missingCaptureKey of missingCaptureKeys) context.report({
				node: depsArgument,
				message: buildMissingDepMessage(hookName, missingCaptureKey)
			});
			if (missingCaptureKeys.length === 0 && hasLiteralDepElement && !hasNonStringLiteralDep) context.report({
				node: depsArgument,
				message: buildLiteralDepMessage(hookName)
			});
			for (const declaredKey of declaredKeys) {
				if (declaredKey.includes(".")) continue;
				const reportNode = declaredKeyToReportNode.get(declaredKey) ?? depsArgument;
				const rootSymbol = getRootSymbol(reportNode, context.scopes);
				if (!rootSymbol || !isFunctionValueSymbol(rootSymbol) || isRecursiveInitializerCapture(rootSymbol, callbackToAnalyze ?? callbackArgument)) continue;
				context.report({
					node: reportNode,
					message: buildUnstableDepMessage(hookName, declaredKey)
				});
			}
			let hasUnusedDeclaredDep = false;
			const unnecessaryDeclaredKeys = [];
			let unnecessaryReportNode = depsArgument;
			for (const declaredKey of declaredKeys) {
				let isUsed = false;
				for (const captureKey of captureKeys) if (isMatchingDepOrPrefix(declaredKey, captureKey)) {
					isUsed = true;
					break;
				}
				if (!isUsed && !stableCapturedNames.has(declaredKey)) {
					hasUnusedDeclaredDep = true;
					break;
				}
			}
			let didReportUnstableDep = false;
			for (const declaredKey of declaredKeys) {
				if (missingCaptureKeys.length > 0 || hasUnusedDeclaredDep || didReportDuplicateDep || didReportUnstableDep || declaredKey.includes(".")) continue;
				let isUsed = false;
				for (const captureKey of captureKeys) if (isMatchingDepOrPrefix(declaredKey, captureKey)) {
					isUsed = true;
					break;
				}
				if (!isUsed && stableCapturedNames.has(declaredKey)) isUsed = true;
				if (!isUsed) continue;
				const reportNode = declaredKeyToReportNode.get(declaredKey) ?? depsArgument;
				const rootSymbol = getRootSymbol(reportNode, context.scopes);
				if (!rootSymbol || !hasDirectIdentifierDeclarator(rootSymbol) || !isUnstableInitializer(rootSymbol.initializer)) continue;
				context.report({
					node: reportNode,
					message: buildUnstableDepMessage(hookName, declaredKey)
				});
				didReportUnstableDep = true;
			}
			for (const declaredKey of declaredKeys) {
				let isUsed = false;
				for (const captureKey of captureKeys) if (isMatchingDepOrPrefix(declaredKey, captureKey)) {
					isUsed = true;
					break;
				}
				if (missingCaptureKeys.length > 0) continue;
				if (isUsed && !EFFECT_HOOKS_ALLOWING_EXTRA_REACTIVE_DEPS.has(hookName) && hasBroaderDeclaredDependency(declaredKey, declaredKeys)) {
					const reportNode = declaredKeyToReportNode.get(declaredKey) ?? depsArgument;
					unnecessaryDeclaredKeys.push(declaredKey);
					unnecessaryReportNode = reportNode;
					continue;
				}
				if (isUsed) continue;
				if (didReportRefCurrentDep) continue;
				const rootName = declaredKey.split(".")[0];
				if (stableCapturedNames.has(rootName)) continue;
				const reportNode = declaredKeyToReportNode.get(declaredKey) ?? depsArgument;
				if (EFFECT_HOOKS_ALLOWING_EXTRA_REACTIVE_DEPS.has(hookName) && isExtraEffectDepAllowed(reportNode, context.scopes)) continue;
				if (missingCaptureKeys.length > 0 && isOuterFunctionScopeDep(reportNode, callbackToAnalyze ?? callbackArgument, context.scopes)) continue;
				const rootSymbol = getRootSymbol(reportNode, context.scopes);
				if (rootSymbol && missingCaptureKeys.length > 0 && isRecursiveInitializerCapture(rootSymbol, callbackToAnalyze ?? callbackArgument)) continue;
				unnecessaryDeclaredKeys.push(declaredKey);
				unnecessaryReportNode = reportNode;
			}
			if (unnecessaryDeclaredKeys.length > 0) context.report({
				node: unnecessaryReportNode,
				message: buildUnnecessaryDepMessage(hookName, unnecessaryDeclaredKeys.join(", "))
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-native/expo-no-non-inlined-env.ts
const EMPTY_VISITORS$3 = {};
const NODE_OR_BUILD_FILE = /(\.config\.[cm]?[jt]sx?$)|((^|\/)(scripts|tools|tooling|cli|bin)\/)|(\+(api|html)\.[cm]?[jt]sx?$)|(\.server\.[cm]?[jt]sx?$)|(\.(test|spec)\.[cm]?[jt]sx?$)|((^|\/)__tests__\/)|(\.e2e\.[cm]?[jt]sx?$)/;
const isNonExpoPublicLiteralKey = (key) => isNodeOfType(key, "Literal") && typeof key.value === "string" && !key.value.startsWith("EXPO_PUBLIC_");
const isProcessEnv = (node) => isNodeOfType(node, "MemberExpression") && !node.computed && isNodeOfType(node.object, "Identifier") && node.object.name === "process" && isNodeOfType(node.property, "Identifier") && node.property.name === "env";
const expoNoNonInlinedEnv = defineRule({
	id: "expo-no-non-inlined-env",
	title: "Non-inlinable process.env access (Expo)",
	requires: ["expo"],
	severity: "warn",
	recommendation: "Read env vars with static dotted access (`process.env.EXPO_PUBLIC_NAME`). Computed access and destructuring aren't inlined by babel-preset-expo and resolve to `undefined` at runtime.",
	create: (context) => {
		const filename = normalizeFilename$1(context.filename ?? "");
		if (filename && NODE_OR_BUILD_FILE.test(filename)) return EMPTY_VISITORS$3;
		return {
			MemberExpression(node) {
				if (!node.computed) return;
				if (!isProcessEnv(node.object)) return;
				if (isNonExpoPublicLiteralKey(node.property)) return;
				context.report({
					node,
					message: "Computed `process.env[...]` access isn't inlined by babel-preset-expo and is `undefined` at runtime. Use static `process.env.EXPO_PUBLIC_NAME`."
				});
			},
			VariableDeclarator(node) {
				if (!isNodeOfType(node.id, "ObjectPattern")) return;
				if (!isProcessEnv(node.init)) return;
				context.report({
					node,
					message: "Destructuring `process.env` isn't inlined by babel-preset-expo, so the values are `undefined` at runtime. Read each var via `process.env.EXPO_PUBLIC_NAME`."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/compile-glob.ts
/**
* Compiles a simple glob pattern (only `*` as a wildcard) into an
* anchored RegExp. Used by allow-list / deny-list rules
* (`forbid-component-props`, `forbid-elements`, `jsx-handler-names`,
* `label-has-associated-control`, `no-unstable-nested-components`)
* for matching component names + prop names against user-supplied
* patterns.
*
* NOT a full picomatch / minimatch — character classes, `?`,
* `**`, brace expansion, etc. are not handled. The four rule-side
* call sites only ever pass plain `*` globs (`onClick`, `on*`,
* `*Handler`), so the cheap escape-then-replace shape is enough
* and avoids pulling picomatch into the per-file rule path.
*/
const compileGlob = (pattern) => {
	const escaped = pattern.replace(/[.+?^${}()|[\]\\]/g, "\\$&").replaceAll("*", ".*");
	return new RegExp(`^${escaped}$`);
};
//#endregion
//#region src/plugin/rules/react-builtins/forbid-component-props.ts
const DEFAULT_FORBID_PROPS = ["className", "style"];
const resolveSettings$44 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const forbid = (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.forbidComponentProps ?? {} : {}).forbid;
	if (!forbid || forbid.length === 0) return { forbid: DEFAULT_FORBID_PROPS };
	return { forbid };
};
const normalizeEntry = (raw) => {
	if (typeof raw === "string") return {
		propRegex: new RegExp(`^${raw.replace(/[.+?^${}()|[\]\\]/g, "\\$&")}$`),
		isExactProp: true,
		exactPropName: raw,
		allowedFor: [],
		allowedForPatterns: [],
		disallowedFor: [],
		disallowedForPatterns: [],
		message: null
	};
	return {
		propRegex: compileGlob(raw.propNamePattern ?? raw.propName ?? ""),
		isExactProp: raw.propName !== void 0 && raw.propNamePattern === void 0,
		exactPropName: raw.propName ?? null,
		allowedFor: raw.allowedFor ?? [],
		allowedForPatterns: (raw.allowedForPatterns ?? []).map(compileGlob),
		disallowedFor: raw.disallowedFor ?? [],
		disallowedForPatterns: (raw.disallowedForPatterns ?? []).map(compileGlob),
		message: raw.message ?? null
	};
};
const isForbiddenForTag = (entry, tag) => {
	if (entry.disallowedFor.length > 0 || entry.disallowedForPatterns.length > 0) {
		if (entry.disallowedFor.includes(tag)) return true;
		return entry.disallowedForPatterns.some((regex) => regex.test(tag));
	}
	if (entry.allowedFor.length === 0 && entry.allowedForPatterns.length === 0) return true;
	if (entry.allowedFor.includes(tag)) return false;
	if (entry.allowedForPatterns.some((regex) => regex.test(tag))) return false;
	return true;
};
const flattenJsxName$1 = (name) => {
	if (isNodeOfType(name, "JSXIdentifier")) return name.name;
	if (isNodeOfType(name, "JSXMemberExpression")) return `${flattenJsxName$1(name.object)}.${name.property.name}`;
	if (name.type === "ThisExpression" || name.type === "JSXThisExpression") return "this";
	return "";
};
const isSupportedJsxName = (name) => isNodeOfType(name, "JSXIdentifier") || isNodeOfType(name, "JSXMemberExpression");
const buildMessage$24 = (propName, message) => message ?? `Your project blocks the \`${propName}\` prop on this component.`;
const forbidComponentProps = defineRule({
	id: "forbid-component-props",
	title: "Forbidden prop on component",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "List the props you want to block per component in the `forbidComponentProps.forbid` setting.",
	category: "Architecture",
	create: (context) => {
		const entries = resolveSettings$44(context.settings).forbid.map(normalizeEntry);
		return { JSXOpeningElement(node) {
			if (entries.length === 0) return;
			if (!isSupportedJsxName(node.name)) return;
			const tag = flattenJsxName$1(node.name);
			if (!tag) return;
			if (!(isReactComponentName(tag.split(".")[0]) || tag.includes("."))) return;
			for (const attribute of node.attributes) {
				if (!isNodeOfType(attribute, "JSXAttribute")) continue;
				if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
				const propName = getJsxAttributeName(attribute.name);
				if (!propName) continue;
				for (const entry of entries) {
					if (entry.isExactProp && entry.exactPropName !== propName) continue;
					if (!entry.propRegex.test(propName)) continue;
					if (!isForbiddenForTag(entry, tag)) continue;
					context.report({
						node: attribute,
						message: buildMessage$24(propName, entry.message)
					});
					break;
				}
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/forbid-dom-props.ts
const buildMessage$23 = (propName, customMessage) => customMessage ?? `Your project blocks the \`${propName}\` prop on plain HTML tags.`;
const resolveSettings$43 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.forbidDomProps ?? {} : {};
	const map = /* @__PURE__ */ new Map();
	for (const item of ruleSettings.forbid ?? []) if (typeof item === "string") map.set(item, { propName: item });
	else {
		const disallowedForSet = item.disallowedFor ? new Set(item.disallowedFor) : void 0;
		map.set(item.propName, {
			propName: item.propName,
			disallowedFor: disallowedForSet,
			message: item.message
		});
	}
	return map;
};
const forbidDomProps = defineRule({
	id: "forbid-dom-props",
	title: "Forbidden DOM prop used",
	severity: "warn",
	recommendation: "List the HTML props you want to block in the `forbidDomProps.forbid` setting.",
	category: "Architecture",
	create: (context) => {
		const forbidMap = resolveSettings$43(context.settings);
		return { JSXOpeningElement(node) {
			if (forbidMap.size === 0) return;
			if (!isNodeOfType(node.name, "JSXIdentifier")) return;
			const elementName = node.name.name;
			if (isReactComponentName(elementName)) return;
			for (const attribute of node.attributes) {
				if (!isNodeOfType(attribute, "JSXAttribute")) continue;
				if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
				const propName = attribute.name.name;
				const descriptor = forbidMap.get(propName);
				if (!descriptor) continue;
				const disallowedFor = descriptor.disallowedFor;
				if (disallowedFor && disallowedFor.size > 0 && !disallowedFor.has(elementName)) continue;
				context.report({
					node: attribute.name,
					message: buildMessage$23(propName, descriptor.message)
				});
			}
		} };
	}
});
//#endregion
//#region src/plugin/utils/flatten-callee-name.ts
/**
* Flattens a call-expression `callee` AST node into its dotted source
* string when it's an `Identifier` or a `MemberExpression` chain.
*
*   `memo`                  → `"memo"`
*   `React.memo`            → `"React.memo"`
*   `a.b.c.memo`            → `"a.b.c.memo"`
*   `obj[computed]`         → `null` (computed members can't flatten)
*   `someCall().foo`        → `null` (only Identifier roots flatten)
*
* Used by HOC-detection sites (`no-multi-comp`, `exhaustive-deps`,
* `rules-of-hooks`, `build-same-file-memo-registry`) which were each
* carrying their own near-identical inlined implementation.
*/
const flattenCalleeName = (callee) => {
	if (isNodeOfType(callee, "Identifier")) return callee.name;
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed) {
		const objectName = flattenCalleeName(callee.object);
		if (!objectName) return null;
		if (isNodeOfType(callee.property, "Identifier")) return `${objectName}.${callee.property.name}`;
	}
	return null;
};
//#endregion
//#region src/plugin/utils/flatten-jsx-name.ts
/**
* Flattens a JSX opener / member-expression chain into a dotted name.
*
*   `<Foo />`           → `"Foo"`
*   `<Namespace.Foo />` → `"Namespace.Foo"`
*   `<a.b.c />`         → `"a.b.c"`
*
* Returns `null` when the chain root isn't a `JSXIdentifier` (e.g.
* the rare-but-valid `<this.x />` case, which JSX surfaces as a
* `JSXThisExpression` root).
*
* Used by `forbid-elements` and `jsx-props-no-spreading`. Two other
* rules (`forbid-component-props`, `utils/get-element-type`) keep
* their own extended variants because they handle additional node
* types (ThisExpression / JSXNamespacedName) and return a non-
* nullable string with a sentinel value — semantically distinct.
*/
const flattenJsxName = (node) => {
	if (isNodeOfType(node, "JSXIdentifier")) return node.name;
	if (isNodeOfType(node, "JSXMemberExpression")) {
		const objectName = flattenJsxName(node.object);
		if (!objectName) return null;
		return `${objectName}.${node.property.name}`;
	}
	return null;
};
//#endregion
//#region src/plugin/utils/is-react-function-call.ts
const PRAGMA = "React";
const isReactFunctionCall = (node, expectedCall) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (getCalleeName$1(node) !== expectedCall) return false;
	if (isNodeOfType(node.callee, "MemberExpression")) return isNodeOfType(node.callee.object, "Identifier") && node.callee.object.name === PRAGMA;
	return true;
};
//#endregion
//#region src/plugin/rules/react-builtins/forbid-elements.ts
const buildMessage$22 = (element, customHelp) => customHelp ? `Your project blocks \`<${element}>\` here. ${customHelp}` : `Your project blocks \`<${element}>\` here.`;
const resolveSettings$42 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.forbidElements ?? {} : {};
	const map = /* @__PURE__ */ new Map();
	for (const item of ruleSettings.forbid ?? []) if (typeof item === "string") map.set(item, void 0);
	else map.set(item.element, item.message);
	return map;
};
const flattenMemberName$1 = flattenCalleeName;
const forbidElements = defineRule({
	id: "forbid-elements",
	title: "Forbidden element used",
	severity: "warn",
	recommendation: "List the element names you want to block in the `forbidElements.forbid` setting.",
	category: "Architecture",
	create: (context) => {
		const forbidMap = resolveSettings$42(context.settings);
		return {
			JSXOpeningElement(node) {
				if (forbidMap.size === 0) return;
				const fullName = flattenJsxName(node.name);
				if (!fullName || !forbidMap.has(fullName)) return;
				context.report({
					node: node.name,
					message: buildMessage$22(fullName, forbidMap.get(fullName))
				});
			},
			CallExpression(node) {
				if (forbidMap.size === 0) return;
				if (!isReactFunctionCall(node, "createElement")) return;
				const firstArgument = node.arguments[0];
				if (!firstArgument) return;
				let elementName = null;
				if (isNodeOfType(firstArgument, "Literal") && typeof firstArgument.value === "string") {
					const value = firstArgument.value;
					const firstChar = value.charCodeAt(0);
					if (firstChar >= 97 && firstChar <= 122 && !value.includes(".")) elementName = value;
				} else if (isNodeOfType(firstArgument, "Identifier")) {
					const firstChar = firstArgument.name.charCodeAt(0);
					if (firstChar >= 65 && firstChar <= 90 || firstChar === 95) elementName = firstArgument.name;
				} else if (isNodeOfType(firstArgument, "MemberExpression")) elementName = flattenMemberName$1(firstArgument);
				if (!elementName || !forbidMap.has(elementName)) return;
				context.report({
					node: firstArgument,
					message: buildMessage$22(elementName, forbidMap.get(elementName))
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/forward-ref-uses-ref.ts
const MESSAGE$46 = "The parent can't reach this component's node because the `forwardRef` wrapper ignores `ref`.";
const forwardRefUsesRef = defineRule({
	id: "forward-ref-uses-ref",
	title: "forwardRef without ref parameter",
	severity: "warn",
	recommendation: "Either accept a `ref` parameter, or drop the `forwardRef` wrapper.",
	category: "Architecture",
	create: (context) => ({ CallExpression(node) {
		if (getCalleeName$1(node) !== "forwardRef") return;
		const firstArgument = node.arguments[0];
		if (!firstArgument) return;
		let inner = null;
		if (isNodeOfType(firstArgument, "ArrowFunctionExpression") || isNodeOfType(firstArgument, "FunctionExpression")) inner = firstArgument;
		else return;
		if (!("params" in inner) || !Array.isArray(inner.params)) return;
		if (inner.params.length !== 1) return;
		const onlyParam = inner.params[0];
		if (isNodeOfType(onlyParam, "RestElement")) return;
		context.report({
			node: inner,
			message: MESSAGE$46
		});
	} })
});
//#endregion
//#region src/plugin/rules/a11y/heading-has-content.ts
const MESSAGE$45 = "Blind users can't use this heading to navigate because screen readers skip it empty, so add text, `aria-label`, or `aria-labelledby`.";
const DEFAULT_HEADING_TAGS = [
	"h1",
	"h2",
	"h3",
	"h4",
	"h5",
	"h6"
];
const resolveSettings$41 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.headingHasContent ?? {} : {};
	return { headingTags: [...DEFAULT_HEADING_TAGS, ...ruleSettings.components ?? []] };
};
const headingHasContent = defineRule({
	id: "heading-has-content",
	title: "Heading has no content",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Put readable text in every heading.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$41(context.settings);
		return { JSXOpeningElement(node) {
			const elementType = getElementType(node, context.settings);
			if (!settings.headingTags.includes(elementType)) return;
			const parent = node.parent;
			if (parent && isNodeOfType(parent, "JSXElement")) {
				if (objectHasAccessibleChild(parent, context.settings)) return;
			}
			if (isHiddenFromScreenReader(node, context.settings)) return;
			context.report({
				node,
				message: MESSAGE$45
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/hook-use-state.ts
const REQUIRE_DESTRUCTURE_MESSAGE = "This `useState` result is hard to follow.";
const NAMING_CONVENTION_MESSAGE = "The setter here is hard to spot.";
const resolveSettings$40 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { allowDestructuredState: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.hookUseState ?? {} : {}).allowDestructuredState ?? false };
};
const isDestructurePattern = (node) => isNodeOfType(node, "ObjectPattern") || isNodeOfType(node, "ArrayPattern");
const splitLeadingLowercase = (name) => {
	let splitIndex = 0;
	while (splitIndex < name.length) {
		const code = name.charCodeAt(splitIndex);
		if (!(code >= 97 && code <= 122)) break;
		splitIndex += 1;
	}
	if (splitIndex === 0) return null;
	return {
		prefix: name.slice(0, splitIndex),
		suffix: name.slice(splitIndex)
	};
};
const expectedSetterNames = (prefix, suffix) => [`set${prefix.charAt(0).toUpperCase()}${prefix.slice(1)}${suffix}`, `set${prefix.toUpperCase()}${suffix}`];
const hookUseState = defineRule({
	id: "hook-use-state",
	title: "useState not destructured",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Destructure useState as `const [thing, setThing] = useState(…)`.",
	category: "Architecture",
	create: (context) => {
		const { allowDestructuredState } = resolveSettings$40(context.settings);
		return { CallExpression(node) {
			if (!isReactFunctionCall(node, "useState")) return;
			const parent = node.parent;
			if (!parent) return;
			if (isNodeOfType(parent, "ReturnStatement")) return;
			if (!isNodeOfType(parent, "VariableDeclarator")) {
				context.report({
					node,
					message: REQUIRE_DESTRUCTURE_MESSAGE
				});
				return;
			}
			const idPattern = parent.id;
			if (!isNodeOfType(idPattern, "ArrayPattern")) {
				context.report({
					node: parent,
					message: REQUIRE_DESTRUCTURE_MESSAGE
				});
				return;
			}
			if (idPattern.elements.length !== 2) {
				context.report({
					node: idPattern,
					message: REQUIRE_DESTRUCTURE_MESSAGE
				});
				return;
			}
			const valueElement = idPattern.elements[0];
			const setterElement = idPattern.elements[1];
			if (!valueElement || !setterElement) return;
			if (isDestructurePattern(setterElement)) {
				context.report({
					node: idPattern,
					message: REQUIRE_DESTRUCTURE_MESSAGE
				});
				return;
			}
			if (!isNodeOfType(setterElement, "Identifier")) return;
			if (isDestructurePattern(valueElement)) {
				if (allowDestructuredState) return;
				context.report({
					node: idPattern,
					message: REQUIRE_DESTRUCTURE_MESSAGE
				});
				return;
			}
			if (!isNodeOfType(valueElement, "Identifier")) return;
			const setterName = setterElement.name;
			const split = splitLeadingLowercase(valueElement.name);
			if (!split) {
				context.report({
					node: idPattern,
					message: NAMING_CONVENTION_MESSAGE
				});
				return;
			}
			if (expectedSetterNames(split.prefix, split.suffix).includes(setterName)) return;
			context.report({
				node: idPattern,
				message: NAMING_CONVENTION_MESSAGE
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/hooks-no-nan-in-deps.ts
const HOOKS_WITH_DEP_ARRAY = new Set([
	"useEffect",
	"useLayoutEffect",
	"useInsertionEffect",
	"useCallback",
	"useMemo",
	"useImperativeHandle"
]);
const NAN_MESSAGE = "`NaN` in a dependency array silently breaks your hook, since React never sees it change.";
const isNanLiteral = (node) => {
	if (isNodeOfType(node, "Identifier") && node.name === "NaN") return true;
	if (isNodeOfType(node, "MemberExpression") && !node.computed && isNodeOfType(node.object, "Identifier") && node.object.name === "Number" && isNodeOfType(node.property, "Identifier") && node.property.name === "NaN") return true;
	return false;
};
const hooksNoNanInDeps = defineRule({
	id: "hooks-no-nan-in-deps",
	title: "NaN in a hook dependency array",
	severity: "warn",
	recommendation: "Remove `NaN` (or `Number.NaN`) from the dependency array. If a value can be NaN at runtime, normalise it (`Number.isNaN(x) ? 0 : x`) before passing it.",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, HOOKS_WITH_DEP_ARRAY)) return;
		const depsIndex = getCalleeName$1(node) === "useImperativeHandle" ? 2 : 1;
		const depsArgument = node.arguments[depsIndex];
		if (!depsArgument || !isNodeOfType(depsArgument, "ArrayExpression")) return;
		for (const element of depsArgument.elements) {
			if (!element) continue;
			if (isNanLiteral(element)) context.report({
				node: element,
				message: NAN_MESSAGE
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/a11y/html-has-lang.ts
const MESSAGE$44 = "Screen readers may mispronounce this page because it doesn't declare a language, so add a `lang` attribute like `en`.";
const resolveSettings$39 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { htmlTags: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.htmlHasLang ?? {} : {}).htmlTags ?? ["html"] };
};
const evaluateLang = (attributeValue) => {
	if (!attributeValue) return "ok";
	if (isNodeOfType(attributeValue, "Literal")) {
		if (typeof attributeValue.value === "string") return attributeValue.value.trim().length > 0 ? "ok" : "empty";
		if (attributeValue.value === null) return "empty";
		return attributeValue.value === false || attributeValue.value === 0 ? "empty" : "ok";
	}
	if (isNodeOfType(attributeValue, "JSXExpressionContainer")) {
		const expression = attributeValue.expression;
		if (isNodeOfType(expression, "Literal")) {
			if (typeof expression.value === "string") return expression.value.trim().length > 0 ? "ok" : "empty";
			return "empty";
		}
		if (isNodeOfType(expression, "Identifier")) {
			if (expression.name === "undefined") return "empty";
			return "ok";
		}
		if (isNodeOfType(expression, "TemplateLiteral")) {
			const staticValue = getStaticTemplateLiteralValue(expression);
			return staticValue === null ? "ok" : staticValue.length > 0 ? "ok" : "empty";
		}
		return "ok";
	}
	return "ok";
};
const htmlHasLang = defineRule({
	id: "html-has-lang",
	title: "html element missing lang",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Set `<html lang=\"…\">` so screen readers know the page language.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$39(context.settings);
		const tagSet = new Set(settings.htmlTags);
		return { JSXOpeningElement(node) {
			const tag = getElementType(node, context.settings);
			if (!tagSet.has(tag)) return;
			const hasSpread = node.attributes.some((attribute) => isNodeOfType(attribute, "JSXSpreadAttribute"));
			const lang = hasJsxPropIgnoreCase(node.attributes, "lang");
			if (!lang) {
				context.report({
					node: node.name,
					message: MESSAGE$44
				});
				return;
			}
			const verdict = evaluateLang(lang.value);
			if (verdict === "missing" || verdict === "empty") {
				context.report({
					node: lang,
					message: MESSAGE$44
				});
				return;
			}
			if (hasSpread && !lang) context.report({
				node: node.name,
				message: MESSAGE$44
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/correctness/html-no-invalid-paragraph-child.ts
const BLOCK_LEVEL_ELEMENTS = new Set([
	"address",
	"article",
	"aside",
	"blockquote",
	"details",
	"div",
	"dl",
	"fieldset",
	"figcaption",
	"figure",
	"footer",
	"form",
	"h1",
	"h2",
	"h3",
	"h4",
	"h5",
	"h6",
	"header",
	"hgroup",
	"hr",
	"main",
	"menu",
	"nav",
	"ol",
	"p",
	"pre",
	"search",
	"section",
	"table",
	"ul"
]);
const buildMessage$21 = (childTagName) => `Your users get reshuffled HTML because \`<${childTagName}>\` can't go inside a \`<p>\`, so the browser closes the paragraph early. Move it out of the \`<p>\`, or use a \`<div>\` instead.`;
const isParagraphElement = (candidate) => {
	if (!isNodeOfType(candidate, "JSXElement")) return false;
	const opening = candidate.openingElement;
	if (!isNodeOfType(opening.name, "JSXIdentifier")) return false;
	return opening.name.name === "p";
};
const findEnclosingParagraph = (openingElement) => {
	const owningElement = openingElement.parent;
	if (!owningElement) return null;
	let ancestor = owningElement.parent;
	while (ancestor) {
		if (isParagraphElement(ancestor)) return ancestor;
		ancestor = ancestor.parent ?? null;
	}
	return null;
};
const htmlNoInvalidParagraphChild = defineRule({
	id: "html-no-invalid-paragraph-child",
	title: "Block element inside a paragraph",
	severity: "warn",
	recommendation: "Swap the `<p>` for a `<div>`, or move the child outside the paragraph.",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		const childTagName = node.name.name;
		if (!BLOCK_LEVEL_ELEMENTS.has(childTagName)) return;
		if (!findEnclosingParagraph(node)) return;
		context.report({
			node: node.name,
			message: buildMessage$21(childTagName)
		});
	} })
});
//#endregion
//#region src/plugin/rules/correctness/html-no-invalid-table-nesting.ts
const TABLE_ELEMENTS = new Set([
	"table",
	"thead",
	"tbody",
	"tfoot",
	"tr",
	"td",
	"th"
]);
const ROW_GROUPS = new Set([
	"thead",
	"tbody",
	"tfoot"
]);
const buildMessage$20 = (childTag, expectedParent, actualParent) => `Your users see a rearranged table because \`<${childTag}>\` must sit directly inside ${expectedParent}, not \`<${actualParent}>\`, so the browser fixes the markup for you. Put it in the right parent.`;
const buildNestedTableMessage = () => "Your users see a broken table because a `<table>` can't sit directly inside another table element. To nest a table, put it inside a `<td>` or `<th>` cell.";
const getHostTagName = (jsxElement) => {
	if (!isNodeOfType(jsxElement, "JSXElement")) return null;
	const opening = jsxElement.openingElement;
	if (!isNodeOfType(opening.name, "JSXIdentifier")) return null;
	const tagName = opening.name.name;
	if (tagName.length === 0 || tagName[0] !== tagName[0].toLowerCase()) return null;
	return tagName;
};
const findClosestHostAncestor = (jsxElement) => {
	let ancestor = jsxElement.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "JSXElement")) {
			const opening = ancestor.openingElement;
			if (isNodeOfType(opening.name, "JSXIdentifier")) {
				const ancestorTag = opening.name.name;
				if (ancestorTag.length === 0) {
					ancestor = ancestor.parent ?? null;
					continue;
				}
				if (ancestorTag[0] === ancestorTag[0].toLowerCase()) return {
					kind: "host",
					tagName: ancestorTag,
					element: ancestor
				};
				return { kind: "opaque" };
			}
			return { kind: "opaque" };
		}
		ancestor = ancestor.parent ?? null;
	}
	return { kind: "none" };
};
const NESTED_TABLE_BOUNDARY_CELLS = new Set(["td", "th"]);
const findEnclosingTable = (jsxElement) => {
	let ancestor = jsxElement.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "JSXElement")) {
			const tag = getHostTagName(ancestor);
			if (tag === "table") return ancestor;
			if (tag !== null && NESTED_TABLE_BOUNDARY_CELLS.has(tag)) return null;
			if (tag === null) return null;
		}
		ancestor = ancestor.parent ?? null;
	}
	return null;
};
const htmlNoInvalidTableNesting = defineRule({
	id: "html-no-invalid-table-nesting",
	title: "Invalid table element nesting",
	severity: "warn",
	recommendation: "Put each table element in its required parent: `<thead>`/`<tbody>`/`<tfoot>` directly inside `<table>`, `<tr>` inside a row group, `<td>`/`<th>` inside `<tr>`. Browsers quietly fix broken table markup, so write it to spec.",
	create: (context) => ({ JSXElement(node) {
		const tagName = getHostTagName(node);
		if (!tagName || !TABLE_ELEMENTS.has(tagName)) return;
		if (tagName === "table") {
			if (findEnclosingTable(node)) context.report({
				node: node.openingElement.name,
				message: buildNestedTableMessage()
			});
			return;
		}
		const closestHost = findClosestHostAncestor(node);
		if (closestHost.kind !== "host") return;
		const actualParent = closestHost.tagName;
		if (ROW_GROUPS.has(tagName)) {
			if (actualParent !== "table") context.report({
				node: node.openingElement.name,
				message: buildMessage$20(tagName, "`<table>`", actualParent)
			});
			return;
		}
		if (tagName === "tr") {
			if (!ROW_GROUPS.has(actualParent) && actualParent !== "table") context.report({
				node: node.openingElement.name,
				message: buildMessage$20(tagName, "`<thead>`, `<tbody>`, or `<tfoot>`", actualParent)
			});
			return;
		}
		if (tagName === "td" || tagName === "th") {
			if (actualParent !== "tr") context.report({
				node: node.openingElement.name,
				message: buildMessage$20(tagName, "`<tr>`", actualParent)
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/correctness/html-no-nested-interactive.ts
const buildMessage$19 = (tagName) => `Your users get broken clicks, focus & screen readers because you can't put a \`<${tagName}>\` inside another \`<${tagName}>\`, so the browser closes the outer one early. Move the inner one out.`;
const isJsxElementWithTagName = (candidate, tagName) => {
	if (!isNodeOfType(candidate, "JSXElement")) return false;
	const opening = candidate.openingElement;
	if (!isNodeOfType(opening.name, "JSXIdentifier")) return false;
	return opening.name.name === tagName;
};
const findEnclosingSameTag = (openingElement, tagName) => {
	const owningElement = openingElement.parent;
	if (!owningElement) return null;
	let ancestor = owningElement.parent;
	while (ancestor) {
		if (isJsxElementWithTagName(ancestor, tagName)) return ancestor;
		ancestor = ancestor.parent ?? null;
	}
	return null;
};
const htmlNoNestedInteractive = defineRule({
	id: "html-no-nested-interactive",
	title: "Nested interactive elements",
	severity: "warn",
	recommendation: "Move the inner `<a>` or `<button>` so it's a sibling, or change the outer one to a plain `<div>` or `<span>`.",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		const tagName = node.name.name;
		if (tagName !== "a" && tagName !== "button") return;
		if (!findEnclosingSameTag(node, tagName)) return;
		context.report({
			node: node.name,
			message: buildMessage$19(tagName)
		});
	} })
});
//#endregion
//#region src/plugin/rules/a11y/iframe-has-title.ts
const MESSAGE$43 = "Blind users can't tell what this `<iframe>` holds because screen readers have no title to read, so add a `title` describing its content.";
const evaluateTitleValue = (value) => {
	if (!value) return "missing";
	if (isNodeOfType(value, "Literal")) {
		if (typeof value.value === "string") return value.value.trim().length > 0 ? "ok" : "empty";
		return "empty";
	}
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal")) {
			if (typeof expression.value === "string") return expression.value.trim().length > 0 ? "ok" : "empty";
			return "empty";
		}
		if (isNodeOfType(expression, "Identifier")) {
			if (expression.name === "undefined") return "empty";
			return "dynamic-ok";
		}
		if (isNodeOfType(expression, "TemplateLiteral")) {
			const staticValue = getStaticTemplateLiteralValue(expression);
			return staticValue === null ? "dynamic-ok" : staticValue.length > 0 ? "ok" : "empty";
		}
		return "dynamic-ok";
	}
	return "ok";
};
const iframeHasTitle = defineRule({
	id: "iframe-has-title",
	title: "iframe missing title",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Add a descriptive `title` to every `<iframe>`.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const tag = getElementType(node, context.settings);
		if (tag !== "iframe") return;
		const hasSpread = node.attributes.some((attribute) => isNodeOfType(attribute, "JSXSpreadAttribute"));
		const titleAttr = hasJsxPropIgnoreCase(node.attributes, "title");
		if (!titleAttr) {
			if (hasSpread || tag === "iframe") context.report({
				node: node.name,
				message: MESSAGE$43
			});
			return;
		}
		const verdict = evaluateTitleValue(titleAttr.value);
		if (verdict === "missing" || verdict === "empty") context.report({
			node: titleAttr,
			message: MESSAGE$43
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/iframe-missing-sandbox.ts
const ALLOWED_SANDBOX_VALUES = new Set([
	"downloads-without-user-activation",
	"downloads",
	"forms",
	"modals",
	"orientation-lock",
	"pointer-lock",
	"popups",
	"popups-to-escape-sandbox",
	"presentation",
	"same-origin",
	"scripts",
	"storage-access-by-user-activation",
	"top-navigation",
	"top-navigation-by-user-activation"
]);
const MISSING_MESSAGE = "An `<iframe>` with no `sandbox` is a security hole: the embedded page gets full access to your site.";
const INVALID_VALUE_MESSAGE = (value) => `\`${value}\` isn't a valid \`sandbox\` token, so the browser ignores it & leaves your iframe exposed.`;
const INVALID_COMBINATION_MESSAGE = "Combining `allow-scripts` & `allow-same-origin` lets the iframe remove its own sandbox, defeating the protection.";
const isAllowedSandboxToken = (token) => {
	if (token === "") return true;
	if (!token.startsWith("allow-")) return false;
	return ALLOWED_SANDBOX_VALUES.has(token.slice(6));
};
const validateSandboxValue = (context, value, reportNode) => {
	let hasAllowScripts = false;
	let hasAllowSameOrigin = false;
	for (const rawToken of value.split(" ")) {
		const token = rawToken.trim();
		if (!isAllowedSandboxToken(token)) context.report({
			node: reportNode,
			message: INVALID_VALUE_MESSAGE(token)
		});
		if (token === "allow-scripts") hasAllowScripts = true;
		if (token === "allow-same-origin") hasAllowSameOrigin = true;
	}
	if (hasAllowScripts && hasAllowSameOrigin) context.report({
		node: reportNode,
		message: INVALID_COMBINATION_MESSAGE
	});
};
const iframeMissingSandbox = defineRule({
	id: "iframe-missing-sandbox",
	title: "iframe missing sandbox attribute",
	severity: "warn",
	recommendation: "Add `sandbox=\"\"` (or a curated value) to your iframe.",
	category: "Security",
	create: (context) => ({
		JSXOpeningElement(node) {
			if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "iframe") return;
			const sandboxAttr = hasJsxPropIgnoreCase(node.attributes, "sandbox");
			if (!sandboxAttr) {
				context.report({
					node: node.name,
					message: MISSING_MESSAGE
				});
				return;
			}
			const stringValue = getJsxPropStringValue(sandboxAttr);
			if (stringValue === null) return;
			validateSandboxValue(context, stringValue, sandboxAttr);
		},
		CallExpression(node) {
			if (!isCreateElementCall(node)) return;
			const firstArgument = node.arguments[0];
			if (!firstArgument) return;
			if (!isNodeOfType(firstArgument, "Literal") || firstArgument.value !== "iframe") return;
			const propsArgument = node.arguments[1];
			if (!propsArgument || !isNodeOfType(propsArgument, "ObjectExpression")) {
				context.report({
					node,
					message: MISSING_MESSAGE
				});
				return;
			}
			let sandboxValueNode = null;
			for (const property of propsArgument.properties) {
				if (!isNodeOfType(property, "Property")) continue;
				const propertyKey = property.key;
				if (isNodeOfType(propertyKey, "Identifier") && propertyKey.name === "sandbox" || isNodeOfType(propertyKey, "Literal") && propertyKey.value === "sandbox") {
					sandboxValueNode = property.value;
					break;
				}
			}
			if (!sandboxValueNode) {
				context.report({
					node: propsArgument,
					message: MISSING_MESSAGE
				});
				return;
			}
			if (!isNodeOfType(sandboxValueNode, "Literal") || typeof sandboxValueNode.value !== "string") return;
			validateSandboxValue(context, sandboxValueNode.value, sandboxValueNode);
		}
	})
});
//#endregion
//#region src/plugin/rules/a11y/img-redundant-alt.ts
const MESSAGE$42 = "Screen reader users hear \"image\" or \"photo\" twice because they already announce it, so describe what the image shows instead.";
const DEFAULT_COMPONENTS = ["img"];
const DEFAULT_REDUNDANT_WORDS = [
	"image",
	"photo",
	"picture"
];
const resolveSettings$38 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.imgRedundantAlt ?? {} : {};
	return {
		components: [...DEFAULT_COMPONENTS, ...ruleSettings.components ?? []],
		words: [...DEFAULT_REDUNDANT_WORDS, ...ruleSettings.words ?? []]
	};
};
const isWordBoundary = (text, start, end) => {
	const isAlphanumeric = (charCode) => charCode >= 48 && charCode <= 57 || charCode >= 65 && charCode <= 90 || charCode >= 97 && charCode <= 122;
	const startsBoundary = start === 0 || !isAlphanumeric(text.charCodeAt(start - 1));
	const endsBoundary = end === text.length || !isAlphanumeric(text.charCodeAt(end));
	return startsBoundary && endsBoundary;
};
const containsRedundantWord = (altText, words) => {
	const lower = altText.toLowerCase();
	for (const word of words) {
		const lowerWord = word.toLowerCase();
		let cursor = 0;
		while (cursor < lower.length) {
			const index = lower.indexOf(lowerWord, cursor);
			if (index === -1) break;
			if (isWordBoundary(lower, index, index + lowerWord.length)) return true;
			cursor = index + 1;
		}
	}
	return false;
};
const altValueRedundant = (attribute, words) => {
	const value = attribute.value;
	if (!value) return false;
	if (isNodeOfType(value, "Literal") && typeof value.value === "string") return containsRedundantWord(value.value, words);
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") return containsRedundantWord(expression.value, words);
		if (isNodeOfType(expression, "TemplateLiteral")) return expression.quasis.some((quasi) => containsRedundantWord(quasi.value.raw, words));
	}
	return false;
};
const imgRedundantAlt = defineRule({
	id: "img-redundant-alt",
	title: "Redundant words in image alt",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Do not put 'image' or 'photo' in alt text. Describe what is shown.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$38(context.settings);
		return { JSXOpeningElement(node) {
			const tag = getElementType(node, context.settings);
			if (!settings.components.includes(tag)) return;
			if (isHiddenFromScreenReader(node, context.settings)) return;
			const altAttribute = hasJsxPropIgnoreCase(node.attributes, "alt");
			if (!altAttribute) return;
			if (altValueRedundant(altAttribute, settings.words)) context.report({
				node: altAttribute,
				message: MESSAGE$42
			});
		} };
	}
});
//#endregion
//#region src/plugin/constants/event-handlers.ts
const MOUSE_EVENT_HANDLERS = [
	"onClick",
	"onContextMenu",
	"onDblClick",
	"onDoubleClick",
	"onDrag",
	"onDragEnd",
	"onDragEnter",
	"onDragExit",
	"onDragLeave",
	"onDragOver",
	"onDragStart",
	"onDrop",
	"onMouseDown",
	"onMouseEnter",
	"onMouseLeave",
	"onMouseMove",
	"onMouseOut",
	"onMouseOver",
	"onMouseUp"
];
const KEYBOARD_EVENT_HANDLERS = [
	"onKeyDown",
	"onKeyPress",
	"onKeyUp"
];
const ALL_EVENT_HANDLERS = [...MOUSE_EVENT_HANDLERS, ...KEYBOARD_EVENT_HANDLERS];
//#endregion
//#region src/plugin/utils/is-disabled-element.ts
const isDisabledElement = (openingElement) => {
	if (hasJsxPropIgnoreCase(openingElement.attributes, "disabled")) return true;
	const ariaDisabled = hasJsxPropIgnoreCase(openingElement.attributes, "aria-disabled");
	if (!ariaDisabled) return false;
	const value = ariaDisabled.value;
	if (!value) return false;
	if (isNodeOfType(value, "Literal") && value.value === "true") return true;
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal") && expression.value === true) return true;
	}
	return false;
};
//#endregion
//#region src/plugin/utils/is-non-interactive-element.ts
const isNonInteractiveElement = (elementType, openingElement) => {
	if (!HTML_TAGS.has(elementType)) return false;
	if (elementType === "header") return false;
	if (elementType === "section") return Boolean(hasJsxPropIgnoreCase(openingElement.attributes, "aria-label") || hasJsxPropIgnoreCase(openingElement.attributes, "aria-labelledby"));
	return NON_INTERACTIVE_ELEMENTS.has(elementType);
};
//#endregion
//#region src/plugin/utils/is-non-interactive-role.ts
const isNonInteractiveRole = (role) => NON_INTERACTIVE_ROLES.has(role);
//#endregion
//#region src/plugin/utils/is-presentation-role.ts
const isPresentationRole = (openingElement) => {
	const roleAttribute = hasJsxPropIgnoreCase(openingElement.attributes, "role");
	if (!roleAttribute) return false;
	const value = getJsxPropStringValue(roleAttribute);
	return value !== null && PRESENTATION_ROLES$2.has(value);
};
//#endregion
//#region src/plugin/rules/a11y/interactive-supports-focus.ts
const buildTabbableMessage = (role) => `Keyboard users can't tab to this '${role}' because it isn't focusable, so add \`tabIndex={0}\`.`;
const buildFocusableMessage = (role) => `Keyboard users can't focus this '${role}' because it can't receive focus, so add \`tabIndex={0}\` or \`tabIndex={-1}\`.`;
const DEFAULT_TABBABLE_ROLES = [
	"button",
	"checkbox",
	"link",
	"searchbox",
	"spinbutton",
	"switch",
	"textbox"
];
const resolveSettings$37 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { tabbable: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.interactiveSupportsFocus ?? {} : {}).tabbable ?? DEFAULT_TABBABLE_ROLES };
};
const interactiveSupportsFocus = defineRule({
	id: "interactive-supports-focus",
	title: "Interactive element not focusable",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Add `tabIndex` to elements with interactive roles and handlers.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$37(context.settings);
		const tabbableSet = new Set(settings.tabbable);
		return { JSXOpeningElement(node) {
			const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
			const role = roleAttribute ? getJsxPropStringValue(roleAttribute) : null;
			const elementType = getElementType(node, context.settings);
			if (!HTML_TAGS.has(elementType)) return;
			const hasInteractiveHandler = ALL_EVENT_HANDLERS.some((handler) => Boolean(hasJsxPropIgnoreCase(node.attributes, handler)));
			const hasTabIndex = Boolean(hasJsxPropIgnoreCase(node.attributes, "tabIndex"));
			if (!hasInteractiveHandler || isDisabledElement(node) || isHiddenFromScreenReader(node, context.settings) || isPresentationRole(node)) return;
			if (!role) return;
			if (!isInteractiveRole(role) || isInteractiveElement(elementType, node) || isNonInteractiveRole(role) || isNonInteractiveElement(elementType, node) || hasTabIndex) return;
			const message = tabbableSet.has(role) ? buildTabbableMessage(role) : buildFocusableMessage(role);
			context.report({
				node,
				message
			});
		} };
	}
});
//#endregion
//#region src/plugin/utils/find-import-source-for-name.ts
const collectFromProgram = (programRoot) => {
	const lookup = /* @__PURE__ */ new Map();
	const visit = (node) => {
		if (node.type === "ImportDeclaration" && "source" in node && node.source) {
			const source = node.source.value;
			if (typeof source !== "string") return;
			if ("specifiers" in node && Array.isArray(node.specifiers)) for (const specifier of node.specifiers) {
				if (!("local" in specifier) || !specifier.local) continue;
				const local = specifier.local;
				if (typeof local.name !== "string") continue;
				if (specifier.type === "ImportDefaultSpecifier") lookup.set(local.name, {
					source,
					imported: null,
					isDefault: true,
					isNamespace: false
				});
				else if (specifier.type === "ImportNamespaceSpecifier") lookup.set(local.name, {
					source,
					imported: null,
					isDefault: false,
					isNamespace: true
				});
				else if (specifier.type === "ImportSpecifier") {
					const importedNode = specifier.imported;
					const importedName = importedNode?.name ?? (typeof importedNode?.value === "string" ? importedNode.value : null);
					lookup.set(local.name, {
						source,
						imported: importedName,
						isDefault: false,
						isNamespace: false
					});
				}
			}
			return;
		}
		const nodeRecord = node;
		for (const key of Object.keys(nodeRecord)) {
			if (key === "parent") continue;
			const child = nodeRecord[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(programRoot);
	return lookup;
};
const importLookupCache = /* @__PURE__ */ new WeakMap();
const getImportLookup = (node) => {
	const programRoot = findProgramRoot(node);
	if (!programRoot) return null;
	let cached = importLookupCache.get(programRoot);
	if (!cached) {
		cached = collectFromProgram(programRoot);
		importLookupCache.set(programRoot, cached);
	}
	return cached;
};
const isImportedFromModule = (contextNode, localIdentifierName, moduleSource) => {
	const lookup = getImportLookup(contextNode);
	if (!lookup) return false;
	const info = lookup.get(localIdentifierName);
	if (!info) return false;
	return info.source === moduleSource;
};
const isNamespaceImportFromModule = (contextNode, localIdentifierName, moduleSource) => {
	const lookup = getImportLookup(contextNode);
	if (!lookup) return false;
	const info = lookup.get(localIdentifierName);
	if (!info) return false;
	return info.isNamespace && info.source === moduleSource;
};
const getImportedNameFromModule = (contextNode, localIdentifierName, moduleSource) => {
	const lookup = getImportLookup(contextNode);
	if (!lookup) return null;
	const info = lookup.get(localIdentifierName);
	if (!info) return null;
	if (info.source !== moduleSource) return null;
	return info.imported;
};
//#endregion
//#region src/plugin/rules/jotai/jotai-derived-atom-returns-fresh-object.ts
const isAtomFromJotai = (callExpression) => {
	if (!isNodeOfType(callExpression.callee, "Identifier")) return false;
	const localName = callExpression.callee.name;
	if (!isImportedFromModule(callExpression, localName, "jotai")) return false;
	return getImportedNameFromModule(callExpression, localName, "jotai") === "atom";
};
const isFunctionLike$1 = (node) => Boolean(node && (isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression")));
const getFirstParameterName = (fn) => {
	const parameters = fn.params ?? [];
	if (parameters.length !== 1) return null;
	const first = parameters[0];
	return isNodeOfType(first, "Identifier") ? first.name : null;
};
const FRESH_ARRAY_INSTANCE_METHODS = new Set([
	"filter",
	"map",
	"flatMap",
	"slice",
	"concat",
	"flat",
	"toSorted",
	"toReversed",
	"toSpliced",
	"with",
	"sort",
	"reverse"
]);
const FRESH_STATIC_OBJECT_CALLS = {
	Object: new Set([
		"keys",
		"values",
		"entries",
		"fromEntries",
		"assign",
		"create"
	]),
	Array: new Set(["from", "of"])
};
const freshFromObjectLiteral = (expression) => {
	if (isNodeOfType(expression, "ObjectExpression")) return {
		kind: "object",
		reportNode: expression
	};
	if (isNodeOfType(expression, "ArrayExpression")) return {
		kind: "array",
		reportNode: expression
	};
	return null;
};
const freshFromMethodChain = (expression) => {
	if (!isNodeOfType(expression, "CallExpression")) return null;
	const callee = expression.callee;
	if (!isNodeOfType(callee, "MemberExpression")) return null;
	if (callee.computed) return null;
	if (!isNodeOfType(callee.property, "Identifier")) return null;
	const methodName = callee.property.name;
	if (FRESH_ARRAY_INSTANCE_METHODS.has(methodName)) return {
		kind: "array",
		reportNode: expression
	};
	if (isNodeOfType(callee.object, "Identifier")) {
		if (FRESH_STATIC_OBJECT_CALLS[callee.object.name]?.has(methodName)) return {
			kind: callee.object.name === "Array" || methodName === "keys" || methodName === "values" || methodName === "entries" ? "array" : "object",
			reportNode: expression
		};
	}
	return null;
};
const classifyReturnedExpression = (expression) => {
	if (!expression) return null;
	const inner = stripParenExpression(expression);
	const literalReturn = freshFromObjectLiteral(inner);
	if (literalReturn) return literalReturn;
	return freshFromMethodChain(inner);
};
const collectTopLevelReturnExpressions$1 = (block) => {
	const returns = [];
	walkAst(block, (child) => {
		if (isNodeOfType(child, "FunctionDeclaration") || isNodeOfType(child, "FunctionExpression") || isNodeOfType(child, "ArrowFunctionExpression")) return false;
		if (isNodeOfType(child, "ReturnStatement")) returns.push(child.argument);
	});
	return returns;
};
const getFreshReturnForFunction = (fn) => {
	const body = fn.body;
	if (!body) return null;
	if (!isNodeOfType(body, "BlockStatement")) return classifyReturnedExpression(body);
	const returnExpressions = collectTopLevelReturnExpressions$1(body);
	if (returnExpressions.length === 0) return null;
	let firstFresh = null;
	for (const returnArgument of returnExpressions) {
		const classification = classifyReturnedExpression(returnArgument);
		if (!classification) return null;
		if (!firstFresh) firstFresh = classification;
	}
	return firstFresh;
};
const functionBodyReferencesGetParameter = (fn, getParameterName) => {
	const body = fn.body;
	if (!body) return false;
	let found = false;
	walkAst(body, (child) => {
		if (found) return false;
		if (isNodeOfType(child, "FunctionDeclaration") || isNodeOfType(child, "FunctionExpression") || isNodeOfType(child, "ArrowFunctionExpression")) {
			if (child !== fn) return false;
		}
		if (!isNodeOfType(child, "CallExpression")) return;
		if (!isNodeOfType(child.callee, "Identifier")) return;
		if (child.callee.name === getParameterName) {
			found = true;
			return false;
		}
	});
	return found;
};
const jotaiDerivedAtomReturnsFreshObject = defineRule({
	id: "jotai-derived-atom-returns-fresh-object",
	title: "Derived atom returns fresh object",
	severity: "warn",
	recommendation: "Split it into one derived atom per field (each compares cleanly with Object.is), or wrap with `selectAtom(source, fn, shallow)` from jotai/utils if you really need one object.",
	create: (context) => ({ CallExpression(node) {
		if (!isAtomFromJotai(node)) return;
		const args = node.arguments ?? [];
		if (args.length === 0) return;
		const reader = args[0];
		if (!isFunctionLike$1(reader)) return;
		const getParameterName = getFirstParameterName(reader);
		if (!getParameterName) return;
		const freshReturn = getFreshReturnForFunction(reader);
		if (!freshReturn) return;
		if (!functionBodyReferencesGetParameter(reader, getParameterName)) return;
		const shape = freshReturn.kind === "object" ? "object" : "array";
		context.report({
			node: freshReturn.reportNode,
			message: `This derived atom returns a new ${shape} each time, so jotai's Object.is check fails & re-renders every consumer on every update. Split into one atom per field, or use \`selectAtom(source, fn, shallow)\`.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/jotai/jotai-select-atom-in-render-body.ts
const JOTAI_SELECT_ATOM_SOURCES = ["jotai/utils", "jotai"];
const COMPONENT_NAME_PATTERN = /^[A-Z]/;
const HOOK_NAME_PATTERN = /^use[A-Z]/;
const isFunctionLikeNode = (node) => isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression");
const isImportedSelectAtom = (callExpression) => {
	if (!isNodeOfType(callExpression.callee, "Identifier")) return false;
	const localName = callExpression.callee.name;
	for (const source of JOTAI_SELECT_ATOM_SOURCES) {
		if (!isImportedFromModule(callExpression, localName, source)) continue;
		if (getImportedNameFromModule(callExpression, localName, source) === "selectAtom") return true;
	}
	return false;
};
const isCallbackOfMemoizingHook = (functionNode) => {
	const callParent = functionNode.parent;
	if (!isNodeOfType(callParent, "CallExpression")) return false;
	if (!isNodeOfType(callParent.callee, "Identifier")) return false;
	if (!MEMOIZING_HOOK_NAMES.has(callParent.callee.name)) return false;
	return callParent.arguments?.[0] === functionNode;
};
const containingFunctionIsComponentOrHook = (functionNode) => {
	if (isNodeOfType(functionNode, "FunctionDeclaration") && functionNode.id) {
		const declaredName = functionNode.id.name;
		return COMPONENT_NAME_PATTERN.test(declaredName) || HOOK_NAME_PATTERN.test(declaredName);
	}
	let cursor = functionNode.parent ?? null;
	while (cursor && isNodeOfType(cursor, "CallExpression")) cursor = cursor.parent ?? null;
	if (!cursor) return false;
	if (!isNodeOfType(cursor, "VariableDeclarator")) return false;
	if (!isNodeOfType(cursor.id, "Identifier")) return false;
	return COMPONENT_NAME_PATTERN.test(cursor.id.name) || HOOK_NAME_PATTERN.test(cursor.id.name);
};
const jotaiSelectAtomInRenderBody = defineRule({
	id: "jotai-select-atom-in-render-body",
	title: "selectAtom called during render",
	severity: "error",
	recommendation: "Lift `selectAtom(base, fn)` to module scope, or wrap it: `const atom = useMemo(() => selectAtom(base, fn), [deps])`. Calling it during render makes a new atom every time and re-subscribes forever.",
	create: (context) => ({ CallExpression(node) {
		if (!isImportedSelectAtom(node)) return;
		let cursor = node.parent ?? null;
		let nearestFunctionLike = null;
		while (cursor) {
			if (isFunctionLikeNode(cursor)) {
				nearestFunctionLike = cursor;
				break;
			}
			cursor = cursor.parent ?? null;
		}
		if (!nearestFunctionLike) return;
		if (isCallbackOfMemoizingHook(nearestFunctionLike)) return;
		let outerCursor = nearestFunctionLike;
		while (outerCursor) {
			if (isFunctionLikeNode(outerCursor) && containingFunctionIsComponentOrHook(outerCursor)) {
				context.report({
					node,
					message: "`selectAtom(...)` runs in a component or hook without `useMemo`, so every render makes a new atom & re-subscribes forever, freezing the page for your users. Lift it to module scope, or wrap it in `useMemo(() => selectAtom(...), [deps])`."
				});
				return;
			}
			outerCursor = outerCursor.parent ?? null;
		}
	} })
});
//#endregion
//#region src/plugin/rules/jotai/jotai-tq-use-raw-query-atom.ts
const QUERY_ATOM_FACTORY_IMPORTED_NAMES = new Set([
	"atomWithQuery",
	"atomWithSuspenseQuery",
	"atomWithInfiniteQuery",
	"atomWithSuspenseInfiniteQuery"
]);
const SUBSCRIBING_HOOK_NAMES = new Set(["useAtomValue", "useAtom"]);
const QUERY_ATOM_NAMING_CONVENTION = /(SuspenseInfiniteQuery|SuspenseQuery|InfiniteQuery|Query)Atom$/;
const jotaiTqUseRawQueryAtom = defineRule({
	id: "jotai-tq-use-raw-query-atom",
	title: "Subscribing to raw query atom",
	severity: "warn",
	recommendation: "Derive the field you read: `const dataAtom = atom((get) => get(queryAtom).data)`. Subscribing to the whole query atom re-renders on every refetch, focus, or no-op cache hit.",
	create: (context) => {
		const queryAtomFactoryLocalNames = /* @__PURE__ */ new Set();
		const queryAtomBindingNames = /* @__PURE__ */ new Set();
		return {
			ImportDeclaration(node) {
				const source = node.source?.value;
				for (const specifier of node.specifiers ?? []) {
					if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
					if (!isNodeOfType(specifier.local, "Identifier")) continue;
					const localName = specifier.local.name;
					if (source === "jotai-tanstack-query") {
						const importedName = getImportedName$1(specifier);
						if (importedName && QUERY_ATOM_FACTORY_IMPORTED_NAMES.has(importedName)) queryAtomFactoryLocalNames.add(localName);
						continue;
					}
					if (typeof source !== "string") continue;
					if (source.startsWith("jotai") || source === "react" || source.startsWith("react/")) continue;
					if (QUERY_ATOM_NAMING_CONVENTION.test(localName)) queryAtomBindingNames.add(localName);
				}
			},
			VariableDeclarator(node) {
				if (queryAtomFactoryLocalNames.size === 0) return;
				if (!isNodeOfType(node.id, "Identifier")) return;
				const initializer = node.init;
				if (!isNodeOfType(initializer, "CallExpression")) return;
				if (!isNodeOfType(initializer.callee, "Identifier")) return;
				if (!queryAtomFactoryLocalNames.has(initializer.callee.name)) return;
				queryAtomBindingNames.add(node.id.name);
			},
			CallExpression(node) {
				if (queryAtomBindingNames.size === 0) return;
				if (!isNodeOfType(node.callee, "Identifier")) return;
				if (!SUBSCRIBING_HOOK_NAMES.has(node.callee.name)) return;
				const args = node.arguments ?? [];
				if (args.length === 0) return;
				const firstArgument = args[0];
				if (!isNodeOfType(firstArgument, "Identifier")) return;
				if (!queryAtomBindingNames.has(firstArgument.name)) return;
				context.report({
					node,
					message: `\`${node.callee.name}(${firstArgument.name})\` subscribes to the whole query atom, so it re-renders your component on every refetch, focus, or no-op cache hit. Derive the field first: \`const dataAtom = atom((get) => get(${firstArgument.name}).data)\`.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/js-performance/js-async-reduce-without-awaited-acc.ts
const isAsyncFunctionLike$1 = (node) => {
	if (!node) return false;
	if (!isNodeOfType(node, "ArrowFunctionExpression") && !isNodeOfType(node, "FunctionExpression")) return false;
	return node.async === true;
};
const classifyFirstParameter = (fn) => {
	const parameters = fn.params ?? [];
	if (parameters.length === 0) return null;
	const first = parameters[0];
	if (isNodeOfType(first, "Identifier")) return {
		kind: "identifier",
		name: first.name
	};
	if (isNodeOfType(first, "ArrayPattern") || isNodeOfType(first, "ObjectPattern")) return { kind: "destructured" };
	if (isNodeOfType(first, "AssignmentPattern")) {
		if (isNodeOfType(first.left, "Identifier")) return {
			kind: "identifier",
			name: first.left.name
		};
		if (isNodeOfType(first.left, "ArrayPattern") || isNodeOfType(first.left, "ObjectPattern")) return { kind: "destructured" };
	}
	return null;
};
const isReduceCallee = (callee) => {
	if (!isNodeOfType(callee, "MemberExpression")) return null;
	if (!callee.computed) {
		if (!isNodeOfType(callee.property, "Identifier")) return null;
		if (callee.property.name !== "reduce" && callee.property.name !== "reduceRight") return null;
		return { methodName: callee.property.name };
	}
	if (isNodeOfType(callee.property, "Literal") && typeof callee.property.value === "string") {
		const propertyName = callee.property.value;
		if (propertyName !== "reduce" && propertyName !== "reduceRight") return null;
		return { methodName: propertyName };
	}
	return null;
};
const bodyAwaitsAccumulator = (fn, accumulatorName) => {
	const body = fn.body;
	if (!body) return false;
	let awaitsAccumulator = false;
	walkAst(body, (child) => {
		if (awaitsAccumulator) return false;
		if (isNodeOfType(child, "FunctionDeclaration") || isNodeOfType(child, "FunctionExpression") || isNodeOfType(child, "ArrowFunctionExpression")) {
			if (child !== fn) return false;
		}
		if (!isNodeOfType(child, "AwaitExpression")) return;
		if (!child.argument) return;
		const awaitArgument = stripParenExpression(child.argument);
		if (isNodeOfType(awaitArgument, "Identifier") && awaitArgument.name === accumulatorName) {
			awaitsAccumulator = true;
			return false;
		}
	});
	return awaitsAccumulator;
};
const jsAsyncReduceWithoutAwaitedAcc = defineRule({
	id: "js-async-reduce-without-awaited-acc",
	title: "Async reduce drops its accumulator",
	severity: "warn",
	recommendation: "Await the accumulator first: `const acc = await previous; ...; return acc;`. Seed it with `Promise.resolve(initial)` so the first run also gets a Promise",
	create: (context) => ({ CallExpression(node) {
		const reduceMatch = isReduceCallee(node.callee);
		if (!reduceMatch) return;
		const args = node.arguments ?? [];
		if (args.length === 0) return;
		const reducerCandidate = stripParenExpression(args[0]);
		if (!isAsyncFunctionLike$1(reducerCandidate)) return;
		const reducer = reducerCandidate;
		if (!containsDirectAwait(reducer.body)) return;
		const firstParameter = classifyFirstParameter(reducer);
		if (!firstParameter) return;
		if (firstParameter.kind === "destructured") {
			context.report({
				node: reducer,
				message: `This async \`.${reduceMatch.methodName}\` reducer destructures its accumulator, but every run after the first gets a Promise, so the pieces come out empty & the work is lost. Await it first: \`async (previous, item) => { const [...] = await previous; ...; return [...]; }\`, & seed with \`Promise.resolve([...])\``
			});
			return;
		}
		if (bodyAwaitsAccumulator(reducer, firstParameter.name)) return;
		const previousParamName = [
			"previous",
			"prev",
			"priorResult"
		].find((candidate) => candidate !== firstParameter.name) ?? `${firstParameter.name}Prev`;
		context.report({
			node: reducer,
			message: `This async \`.${reduceMatch.methodName}\` reducer never awaits its accumulator "${firstParameter.name}", so each run gets a Promise instead of the real value & the work is lost. Reassign it at the top (\`${firstParameter.name} = await ${firstParameter.name};\`), or rewrite as \`async (${previousParamName}, item) => { const ${firstParameter.name} = await ${previousParamName}; ...; return ${firstParameter.name}; }\`, & seed with \`Promise.resolve(...)\``
		});
	} })
});
//#endregion
//#region src/plugin/rules/js-performance/js-batch-dom-css.ts
const ITERATOR_METHOD_NAMES$1 = new Set([
	"forEach",
	"map",
	"flatMap",
	"filter",
	"reduce",
	"reduceRight"
]);
const isIteratorCallback = (fn) => {
	const functionParent = fn.parent;
	if (!functionParent || !isNodeOfType(functionParent, "CallExpression")) return false;
	const callee = functionParent.callee;
	if (!isNodeOfType(callee, "MemberExpression")) return false;
	if (!isNodeOfType(callee.property, "Identifier")) return false;
	if (functionParent.arguments[0] === fn && ITERATOR_METHOD_NAMES$1.has(callee.property.name)) return true;
	if (functionParent.arguments[1] === fn && callee.property.name === "from" && isNodeOfType(callee.object, "Identifier") && callee.object.name === "Array") return true;
	return false;
};
const isInsideLoopContext = (node) => {
	let current = node.parent;
	while (current) {
		if (isNodeOfType(current, "ForStatement") || isNodeOfType(current, "ForInStatement") || isNodeOfType(current, "ForOfStatement") || isNodeOfType(current, "WhileStatement") || isNodeOfType(current, "DoWhileStatement")) return true;
		if (isFunctionLike$2(current)) {
			if (isIteratorCallback(current)) return true;
			return false;
		}
		current = current.parent ?? null;
	}
	return false;
};
const jsBatchDomCss = defineRule({
	id: "js-batch-dom-css",
	title: "Repeated inline style writes",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Do all your reads first, then all your writes. Mixing them inside a loop makes the browser recalculate the layout again and again, which is slow",
	create: (context) => {
		const isStyleAssignment = (node) => isNodeOfType(node, "ExpressionStatement") && isNodeOfType(node.expression, "AssignmentExpression") && isNodeOfType(node.expression.left, "MemberExpression") && isNodeOfType(node.expression.left.object, "MemberExpression") && isNodeOfType(node.expression.left.object.property, "Identifier") && node.expression.left.object.property.name === "style";
		return { BlockStatement(node) {
			if (!isInsideLoopContext(node)) return;
			const statements = node.body ?? [];
			for (let statementIndex = 1; statementIndex < statements.length; statementIndex++) if (isStyleAssignment(statements[statementIndex]) && isStyleAssignment(statements[statementIndex - 1])) context.report({
				node: statements[statementIndex],
				message: "This makes the browser recalculate layout again & again because you set several element.style properties in a row, so set them all at once with cssText or a CSS class to redraw once"
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/js-performance/js-cache-property-access.ts
const buildMemberAccessKey = (node) => {
	if (isNodeOfType(node, "Identifier")) return node.name;
	if (isNodeOfType(node, "ThisExpression")) return "this";
	if (!isNodeOfType(node, "MemberExpression") || node.computed) return null;
	const objectKey = buildMemberAccessKey(node.object);
	if (!objectKey) return null;
	if (!isNodeOfType(node.property, "Identifier")) return null;
	return `${objectKey}.${node.property.name}`;
};
const jsCachePropertyAccess = defineRule({
	id: "js-cache-property-access",
	title: "Repeated property access in a loop",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Read the value once into a variable at the top of the loop: `const { x, y } = obj.deeply.nested`",
	create: (context) => {
		const inspectLoopBody = (loopBody) => {
			const counts = /* @__PURE__ */ new Map();
			walkAst(loopBody, (child) => {
				if (!isNodeOfType(child, "MemberExpression")) return;
				if (child.computed) return;
				if (isNodeOfType(child.parent, "MemberExpression") && child.parent.object === child) return;
				if (isNodeOfType(child.parent, "CallExpression") && child.parent.callee === child) return;
				const key = buildMemberAccessKey(child);
				if (!key) return;
				if (key.split(".").length < 3) return;
				const existing = counts.get(key);
				if (existing) existing.count++;
				else counts.set(key, {
					count: 1,
					firstNode: child
				});
			});
			for (const [key, { count, firstNode }] of counts) if (count >= 3) context.report({
				node: firstNode,
				message: `This slows the loop because ${key} is read ${count} times inside it, so read it once into a variable at the top`
			});
		};
		const handleLoop = (node) => {
			if (!isNodeOfType(node, "ForStatement") && !isNodeOfType(node, "ForInStatement") && !isNodeOfType(node, "ForOfStatement") && !isNodeOfType(node, "WhileStatement") && !isNodeOfType(node, "DoWhileStatement")) return;
			if (node.body) inspectLoopBody(node.body);
		};
		return {
			ForStatement: handleLoop,
			ForInStatement: handleLoop,
			ForOfStatement: handleLoop,
			WhileStatement: handleLoop,
			DoWhileStatement: handleLoop
		};
	}
});
//#endregion
//#region src/plugin/rules/js-performance/js-cache-storage.ts
const jsCacheStorage = defineRule({
	id: "js-cache-storage",
	title: "Repeated localStorage reads",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Read `localStorage`/`sessionStorage` once and reuse the value. Every read has to parse the data again, which is slow",
	create: (context) => {
		const storageReadCounts = /* @__PURE__ */ new Map();
		return { CallExpression(node) {
			if (!isMemberProperty(node.callee, "getItem")) return;
			if (!isNodeOfType(node.callee.object, "Identifier") || !STORAGE_OBJECTS$1.has(node.callee.object.name)) return;
			if (!isNodeOfType(node.arguments?.[0], "Literal")) return;
			const storageKey = String(node.arguments[0].value);
			const readCount = (storageReadCounts.get(storageKey) ?? 0) + 1;
			storageReadCounts.set(storageKey, readCount);
			if (readCount === 2) {
				const storageName = node.callee.object.name;
				context.report({
					node,
					message: `This is slow because ${storageName}.getItem("${storageKey}") runs several times & re-parses the data each call, so read it once & reuse the value`
				});
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/js-performance/js-combine-iterations.ts
const isIteratorProducingCall = (callExpression, generatorNamesInFile) => {
	const callee = callExpression.callee;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && callee.object.name === "Iterator" && isNodeOfType(callee.property, "Identifier") && callee.property.name === "from") return true;
	if (isNodeOfType(callee, "Identifier") && generatorNamesInFile.has(callee.name)) return true;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && ITERATOR_PRODUCING_METHOD_NAMES.has(callee.property.name)) {
		const receiver = callee.object;
		if (isNodeOfType(receiver, "Identifier") && receiver.name === "Object") return false;
		return true;
	}
	return false;
};
const isChainPassThroughCall = (callExpression) => {
	const callee = callExpression.callee;
	if (!isNodeOfType(callee, "MemberExpression")) return false;
	if (!isNodeOfType(callee.property, "Identifier")) return false;
	return CHAINABLE_ITERATION_METHODS.has(callee.property.name);
};
const isReceiverChainIteratorRooted = (receiverNode, generatorNamesInFile) => {
	let cursor = receiverNode;
	while (cursor) {
		if (isNodeOfType(cursor, "ChainExpression")) {
			cursor = cursor.expression;
			continue;
		}
		if (!isNodeOfType(cursor, "CallExpression")) return false;
		if (isIteratorProducingCall(cursor, generatorNamesInFile)) return true;
		if (!isChainPassThroughCall(cursor)) return false;
		const nextCallee = cursor.callee;
		if (!isNodeOfType(nextCallee, "MemberExpression")) return false;
		cursor = nextCallee.object;
	}
	return false;
};
const isNullishComparison = (expression) => {
	if (!expression) return false;
	if (isNodeOfType(expression, "BinaryExpression")) {
		const operator = expression.operator;
		if (operator !== "!=" && operator !== "!==" && operator !== "==" && operator !== "===") return false;
		const isNullLiteral = (n) => {
			if (!n) return false;
			if (isNodeOfType(n, "Literal") && n.value === null) return true;
			if (isNodeOfType(n, "Identifier") && n.name === "undefined") return true;
			return false;
		};
		return isNullLiteral(expression.left) || isNullLiteral(expression.right);
	}
	return false;
};
const isNullFilteringPredicateBody = (body) => {
	if (isNullishComparison(body)) return true;
	if (isNodeOfType(body, "LogicalExpression") && (body.operator === "&&" || body.operator === "||")) return isNullFilteringPredicateBody(body.left) && isNullFilteringPredicateBody(body.right);
	return false;
};
const isTypePredicateArrow = (filterArgument) => {
	if (!filterArgument) return false;
	if (!isNodeOfType(filterArgument, "ArrowFunctionExpression")) return false;
	const returnType = filterArgument.returnType;
	if (!returnType || typeof returnType !== "object") return false;
	const annotation = returnType.typeAnnotation;
	if (!annotation || typeof annotation !== "object") return false;
	const annotationType = annotation.type;
	return typeof annotationType === "string" && annotationType.includes("TypePredicate");
};
const isNullFilteringPredicate = (filterArgument) => {
	if (!filterArgument) return false;
	if (!isNodeOfType(filterArgument, "ArrowFunctionExpression")) return false;
	if ((filterArgument.params?.length ?? 0) === 0) return false;
	const body = filterArgument.body;
	if (!isNodeOfType(body, "BlockStatement")) return isNullFilteringPredicateBody(body);
	const statements = body.body ?? [];
	if (statements.length !== 1) return false;
	const only = statements[0];
	if (!isNodeOfType(only, "ReturnStatement") || !only.argument) return false;
	return isNullFilteringPredicateBody(only.argument);
};
const isStringSplitRootedChain = (receiverNode) => {
	let cursor = receiverNode;
	let hops = 0;
	while (cursor && hops < 12) {
		hops += 1;
		if (isNodeOfType(cursor, "ChainExpression")) {
			cursor = cursor.expression;
			continue;
		}
		if (!isNodeOfType(cursor, "CallExpression")) return false;
		const callee = cursor.callee;
		if (!isNodeOfType(callee, "MemberExpression")) return false;
		if (!isNodeOfType(callee.property, "Identifier")) return false;
		if (callee.property.name === "split") return true;
		if (!isChainPassThroughCall(cursor)) return false;
		cursor = callee.object;
	}
	return false;
};
const isSmallLiteralArrayRootedChain = (receiverNode) => {
	let cursor = receiverNode;
	while (cursor) {
		if (isNodeOfType(cursor, "ChainExpression")) {
			cursor = cursor.expression;
			continue;
		}
		if (isNodeOfType(cursor, "ArrayExpression")) {
			const elements = cursor.elements ?? [];
			if (elements.length === 0 || elements.length > 8) return false;
			for (const element of elements) {
				if (!element) continue;
				if (isNodeOfType(element, "SpreadElement")) return false;
			}
			return true;
		}
		if (!isNodeOfType(cursor, "CallExpression")) return false;
		if (!isChainPassThroughCall(cursor)) return false;
		const nextCallee = cursor.callee;
		if (!isNodeOfType(nextCallee, "MemberExpression")) return false;
		cursor = nextCallee.object;
	}
	return false;
};
const collectGeneratorNames = (programNode) => {
	const generatorNames = /* @__PURE__ */ new Set();
	walkAst(programNode, (child) => {
		if (isNodeOfType(child, "FunctionDeclaration") && child.generator === true && isNodeOfType(child.id, "Identifier")) {
			generatorNames.add(child.id.name);
			return;
		}
		if (isNodeOfType(child, "VariableDeclarator") && isNodeOfType(child.id, "Identifier") && isNodeOfType(child.init, "FunctionExpression") && child.init.generator === true) generatorNames.add(child.id.name);
	});
	return generatorNames;
};
const jsCombineIterations = defineRule({
	id: "js-combine-iterations",
	title: "Chained array iterations",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Combine `.map().filter()` style chains into one pass with `.reduce()` or a `for...of` loop, so you only loop over the list once",
	create: (context) => {
		let generatorNamesInFile = /* @__PURE__ */ new Set();
		return {
			Program(programNode) {
				generatorNamesInFile = collectGeneratorNames(programNode);
			},
			CallExpression(node) {
				if (!isNodeOfType(node.callee, "MemberExpression") || !isNodeOfType(node.callee.property, "Identifier")) return;
				const outerMethod = node.callee.property.name;
				if (!CHAINABLE_ITERATION_METHODS.has(outerMethod)) return;
				const innerCall = node.callee.object;
				if (!isNodeOfType(innerCall, "CallExpression") || !isNodeOfType(innerCall.callee, "MemberExpression") || !isNodeOfType(innerCall.callee.property, "Identifier")) return;
				const innerMethod = innerCall.callee.property.name;
				if (!CHAINABLE_ITERATION_METHODS.has(innerMethod)) return;
				if (innerMethod === "map" && outerMethod === "filter") {
					const filterArgument = node.arguments?.[0];
					if (isNodeOfType(filterArgument, "Identifier") && filterArgument.name === "Boolean" || isNodeOfType(filterArgument, "ArrowFunctionExpression") && filterArgument.params?.length === 1 && isNodeOfType(filterArgument.body, "Identifier") && isNodeOfType(filterArgument.params[0], "Identifier") && filterArgument.body.name === filterArgument.params[0].name) return;
					if (isNullFilteringPredicate(filterArgument)) return;
					if (isTypePredicateArrow(filterArgument)) return;
				}
				if (innerMethod === "filter" && outerMethod === "map") {
					const filterArgument = innerCall.arguments?.[0];
					if (isNullFilteringPredicate(filterArgument)) return;
					if (isTypePredicateArrow(filterArgument)) return;
				}
				if (isReceiverChainIteratorRooted(innerCall.callee.object, generatorNamesInFile)) return;
				if (isSmallLiteralArrayRootedChain(innerCall.callee.object)) return;
				if (isStringSplitRootedChain(innerCall.callee.object)) return;
				context.report({
					node,
					message: `This loops over your list twice because .${innerMethod}().${outerMethod}() makes two passes, so do it in one pass with .reduce() or a for...of loop`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/js-performance/js-early-exit.ts
const jsEarlyExit = defineRule({
	id: "js-early-exit",
	title: "Deeply nested if statements",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Add an early `return` or `continue` so the deep nesting flattens out and you bail as soon as the answer is known",
	create: (context) => ({ IfStatement(node) {
		if (!isNodeOfType(node.consequent, "BlockStatement") || !node.consequent.body) return;
		let nestingDepth = 0;
		let currentBlock = node.consequent;
		while (isNodeOfType(currentBlock, "BlockStatement") && currentBlock.body?.length === 1) {
			const innerStatement = currentBlock.body[0];
			if (!isNodeOfType(innerStatement, "IfStatement")) break;
			nestingDepth++;
			currentBlock = innerStatement.consequent;
		}
		if (nestingDepth >= 3) context.report({
			node,
			message: `This is hard to follow because there are ${nestingDepth + 1} levels of nested if statements, so return early to keep it flat`
		});
	} })
});
//#endregion
//#region src/plugin/rules/js-performance/js-flatmap-filter.ts
const jsFlatmapFilter = defineRule({
	id: "js-flatmap-filter",
	title: ".map().filter(Boolean) loops twice",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use `.flatMap(item => condition ? [value] : [])` to change and drop items in one pass, instead of building a throwaway array in between",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression") || !isNodeOfType(node.callee.property, "Identifier")) return;
		if (node.callee.property.name !== "filter") return;
		const filterArgument = node.arguments?.[0];
		if (!filterArgument) return;
		const isIdentityArrow = isNodeOfType(filterArgument, "ArrowFunctionExpression") && filterArgument.params?.length === 1 && isNodeOfType(filterArgument.body, "Identifier") && isNodeOfType(filterArgument.params[0], "Identifier") && filterArgument.body.name === filterArgument.params[0].name;
		if (!(isNodeOfType(filterArgument, "Identifier") && filterArgument.name === "Boolean" || isIdentityArrow)) return;
		const innerCall = node.callee.object;
		if (!isNodeOfType(innerCall, "CallExpression") || !isNodeOfType(innerCall.callee, "MemberExpression") || !isNodeOfType(innerCall.callee.property, "Identifier")) return;
		if (innerCall.callee.property.name !== "map") return;
		const receiver = innerCall.callee.object;
		if (receiver && isNodeOfType(receiver, "ArrayExpression")) {
			const elements = receiver.elements ?? [];
			if (elements.length > 0 && elements.length <= 8 && elements.every((element) => element == null || !isNodeOfType(element, "SpreadElement"))) return;
		}
		context.report({
			node,
			message: "This loops over your list twice because .map().filter(Boolean) makes two passes, so use .flatMap() to change & drop items in one pass"
		});
	} })
});
//#endregion
//#region src/plugin/rules/js-performance/js-hoist-intl.ts
const INTL_CLASSES = new Set([
	"NumberFormat",
	"DateTimeFormat",
	"Collator",
	"RelativeTimeFormat",
	"ListFormat",
	"PluralRules",
	"Segmenter",
	"DisplayNames"
]);
const isIntlNewExpression = (node) => {
	if (!isNodeOfType(node, "NewExpression")) return false;
	const callee = node.callee;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && callee.object.name === "Intl" && isNodeOfType(callee.property, "Identifier") && INTL_CLASSES.has(callee.property.name)) return true;
	return false;
};
const jsHoistIntl = defineRule({
	id: "js-hoist-intl",
	title: "Intl formatter rebuilt each call",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Move `new Intl.NumberFormat(...)` to the top of the file or wrap it in `useMemo`. Building one is slow, so don't redo it on every call",
	create: (context) => ({ NewExpression(node) {
		if (!isIntlNewExpression(node)) return;
		let cursor = node.parent ?? null;
		let inFunctionBody = false;
		while (cursor) {
			if (isFunctionLike$2(cursor)) {
				inFunctionBody = true;
				const fnParent = cursor.parent;
				if (fnParent && isNodeOfType(fnParent, "CallExpression") && fnParent.arguments?.[0] === cursor) {
					const callee = fnParent.callee;
					const calleeName = isNodeOfType(callee, "Identifier") ? callee.name : isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") ? callee.property.name : null;
					if (calleeName === "useMemo" || calleeName === "useCallback" || calleeName === "useRef") return;
				}
				break;
			}
			cursor = cursor.parent ?? null;
		}
		if (!inFunctionBody) return;
		const className = isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.property, "Identifier") ? node.callee.property.name : "Intl";
		context.report({
			node,
			message: `This is slow because new Intl.${className}() rebuilds on every call inside a function, so move it to the top of the file, or wrap it in useMemo`
		});
	} })
});
//#endregion
//#region src/plugin/utils/create-loop-aware-visitors.ts
const createLoopAwareVisitors = (innerVisitors) => {
	let loopDepth = 0;
	const incrementLoopDepth = () => {
		loopDepth++;
	};
	const decrementLoopDepth = () => {
		loopDepth--;
	};
	const visitors = {};
	for (const loopType of LOOP_TYPES) {
		visitors[loopType] = incrementLoopDepth;
		visitors[`${loopType}:exit`] = decrementLoopDepth;
	}
	for (const [nodeType, handler] of Object.entries(innerVisitors)) visitors[nodeType] = (node) => {
		if (loopDepth > 0) handler(node);
	};
	return visitors;
};
//#endregion
//#region src/plugin/rules/js-performance/js-hoist-regexp.ts
const jsHoistRegexp = defineRule({
	id: "js-hoist-regexp",
	title: "RegExp built inside a loop",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Move `new RegExp(...)` (or large regex literals) to a constant outside the loop so it isn't rebuilt on every pass",
	create: (context) => createLoopAwareVisitors({ NewExpression(node) {
		if (isNodeOfType(node.callee, "Identifier") && node.callee.name === "RegExp") context.report({
			node,
			message: "This slows the loop because new RegExp() rebuilds the pattern every pass, so move it to a constant outside the loop"
		});
	} })
});
//#endregion
//#region src/plugin/rules/js-performance/js-index-maps.ts
const looksLikeArrayCallbackCall = (node) => {
	const first = node.arguments?.[0];
	if (!first) return false;
	if (isNodeOfType(first, "ArrowFunctionExpression") || isNodeOfType(first, "FunctionExpression") || isNodeOfType(first, "Identifier") || isNodeOfType(first, "MemberExpression") || isNodeOfType(first, "CallExpression")) return true;
	return false;
};
const jsIndexMaps = defineRule({
	id: "js-index-maps",
	title: "array.find() inside a loop",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Build a `Map` once before the loop instead of calling `array.find(...)` inside it",
	create: (context) => createLoopAwareVisitors({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression") || !isNodeOfType(node.callee.property, "Identifier")) return;
		const methodName = node.callee.property.name;
		if (methodName !== "find" && methodName !== "findIndex") return;
		if (!looksLikeArrayCallbackCall(node)) return;
		context.report({
			node,
			message: `This gets slow as your list grows because array.${methodName}() runs inside a loop, so build a Map once before the loop for instant lookups`
		});
	} })
});
//#endregion
//#region src/plugin/utils/are-expressions-structurally-equal.ts
const areExpressionsStructurallyEqual = (a, b) => {
	if (!a || !b) return a === b;
	if (a.type !== b.type) return false;
	if (isNodeOfType(a, "Identifier") && isNodeOfType(b, "Identifier")) return a.name === b.name;
	if (isNodeOfType(a, "Literal") && isNodeOfType(b, "Literal")) return a.value === b.value;
	if (isNodeOfType(a, "MemberExpression") && isNodeOfType(b, "MemberExpression")) {
		if (a.computed !== b.computed) return false;
		return areExpressionsStructurallyEqual(a.object, b.object) && areExpressionsStructurallyEqual(a.property, b.property);
	}
	if (isNodeOfType(a, "CallExpression") && isNodeOfType(b, "CallExpression")) {
		if (!areExpressionsStructurallyEqual(a.callee, b.callee)) return false;
		const argumentsA = a.arguments ?? [];
		const argumentsB = b.arguments ?? [];
		if (argumentsA.length !== argumentsB.length) return false;
		return argumentsA.every((argument, index) => areExpressionsStructurallyEqual(argument, argumentsB[index]));
	}
	return false;
};
//#endregion
//#region src/plugin/utils/flatten-logical-and-chain.ts
const flattenLogicalAndChain = (node) => {
	if (isNodeOfType(node, "LogicalExpression") && node.operator === "&&") return [...flattenLogicalAndChain(node.left), ...flattenLogicalAndChain(node.right)];
	return [node];
};
//#endregion
//#region src/plugin/utils/collect-earlier-and-guard-operands.ts
const collectEarlierAndGuardOperands = (node) => {
	const earlierOperands = [];
	let currentNode = node;
	let parentNode = currentNode.parent ?? null;
	while (parentNode) {
		if (isNodeOfType(parentNode, "LogicalExpression")) {
			if (parentNode.operator === "&&" && parentNode.right === currentNode) earlierOperands.push(...flattenLogicalAndChain(parentNode.left));
			currentNode = parentNode;
			parentNode = currentNode.parent ?? null;
			continue;
		}
		if (isNodeOfType(parentNode, "ChainExpression")) {
			currentNode = parentNode;
			parentNode = currentNode.parent ?? null;
			continue;
		}
		break;
	}
	return earlierOperands;
};
//#endregion
//#region src/plugin/rules/js-performance/js-length-check-first.ts
const findIndexedArrayObject = (callbackBody, indexParameterName) => {
	let indexedArrayObject = null;
	walkAst(callbackBody, (child) => {
		if (indexedArrayObject) return;
		if (isNodeOfType(child, "MemberExpression") && child.computed && isNodeOfType(child.property, "Identifier") && child.property.name === indexParameterName) indexedArrayObject = child.object;
	});
	return indexedArrayObject;
};
const unwrapChainExpression$1 = (node) => isNodeOfType(node, "ChainExpression") ? node.expression : node;
const isMatchingLengthEqualityGuard = (guardOperand, receiverArray, indexedArray) => {
	const binaryGuard = unwrapChainExpression$1(guardOperand);
	if (!isNodeOfType(binaryGuard, "BinaryExpression")) return false;
	if (binaryGuard.operator !== "===" && binaryGuard.operator !== "==") return false;
	const leftSide = unwrapChainExpression$1(binaryGuard.left);
	const rightSide = unwrapChainExpression$1(binaryGuard.right);
	if (!isMemberProperty(leftSide, "length")) return false;
	if (!isMemberProperty(rightSide, "length")) return false;
	const leftLengthObject = unwrapChainExpression$1(leftSide.object);
	const rightLengthObject = unwrapChainExpression$1(rightSide.object);
	const normalizedReceiver = unwrapChainExpression$1(receiverArray);
	const normalizedIndexed = unwrapChainExpression$1(indexedArray);
	const matchesReceiverThenIndexed = areExpressionsStructurallyEqual(leftLengthObject, normalizedReceiver) && areExpressionsStructurallyEqual(rightLengthObject, normalizedIndexed);
	const matchesIndexedThenReceiver = areExpressionsStructurallyEqual(leftLengthObject, normalizedIndexed) && areExpressionsStructurallyEqual(rightLengthObject, normalizedReceiver);
	return matchesReceiverThenIndexed || matchesIndexedThenReceiver;
};
const jsLengthCheckFirst = defineRule({
	id: "js-length-check-first",
	title: "Array compare without length check",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Check `a.length === b.length && a.every((x, i) => x === b[i])` so arrays of different sizes stop right away",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		if (!isNodeOfType(node.callee.property, "Identifier")) return;
		if (node.callee.property.name !== "every") return;
		const callback = node.arguments?.[0];
		if (!isInlineFunctionExpression(callback)) return;
		const callbackParameters = callback.params ?? [];
		if (callbackParameters.length < 2) return;
		const indexParameter = callbackParameters[1];
		if (!isNodeOfType(indexParameter, "Identifier")) return;
		const indexedArrayObject = findIndexedArrayObject(callback.body, indexParameter.name);
		if (!indexedArrayObject) return;
		const receiverArrayObject = node.callee.object;
		if (collectEarlierAndGuardOperands(node).some((guardOperand) => isMatchingLengthEqualityGuard(guardOperand, receiverArrayObject, indexedArrayObject))) return;
		context.report({
			node,
			message: "This is slow because .every() compares two arrays item by item, so check `a.length === b.length` first to bail out immediately when sizes differ"
		});
	} })
});
//#endregion
//#region src/plugin/rules/js-performance/js-min-max-loop.ts
const jsMinMaxLoop = defineRule({
	id: "js-min-max-loop",
	title: "sort() to find min or max",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use `Math.min(...array)` or `Math.max(...array)` instead of sorting the whole list just to read the first or last item",
	create: (context) => ({ MemberExpression(node) {
		if (!node.computed) return;
		const object = node.object;
		if (!isNodeOfType(object, "CallExpression") || !isMemberProperty(object.callee, "sort")) return;
		const isFirstElement = isNodeOfType(node.property, "Literal") && node.property.value === 0;
		const isLastElement = isNodeOfType(node.property, "BinaryExpression") && node.property.operator === "-" && isNodeOfType(node.property.right, "Literal") && node.property.right.value === 1;
		if (isFirstElement || isLastElement) {
			const targetFunction = isFirstElement ? "min" : "max";
			context.report({
				node,
				message: `This is slow because array.sort()[${isFirstElement ? "0" : "length-1"}] sorts the whole list just to grab the smallest or largest, so use Math.${targetFunction}(...array) instead`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/js-performance/js-set-map-lookups.ts
const STRING_RETURNING_METHODS = new Set([
	"toString",
	"toLocaleString",
	"toLowerCase",
	"toUpperCase",
	"toLocaleLowerCase",
	"toLocaleUpperCase",
	"trim",
	"trimStart",
	"trimEnd",
	"padStart",
	"padEnd",
	"normalize",
	"repeat",
	"replace",
	"replaceAll",
	"substring",
	"substr",
	"charAt",
	"toFixed",
	"toExponential",
	"toPrecision",
	"toJSON"
]);
const STRING_TYPED_PROPERTY_NAMES = new Set([
	"textContent",
	"innerText",
	"innerHTML",
	"outerHTML",
	"nodeValue",
	"nodeName",
	"localName",
	"namespaceURI",
	"baseURI",
	"documentURI",
	"tagName",
	"className",
	"id",
	"lang",
	"dir",
	"title",
	"alt",
	"type",
	"name",
	"placeholder",
	"href",
	"src",
	"value",
	"accessKey",
	"contentEditable",
	"hash",
	"host",
	"hostname",
	"pathname",
	"port",
	"protocol",
	"search",
	"origin",
	"username",
	"password",
	"characterSet",
	"contentType",
	"charset",
	"mimeType",
	"mediaType",
	"cssText",
	"message",
	"stack",
	"fileName",
	"code",
	"label",
	"slug",
	"prefix"
]);
const STRING_TYPED_IDENTIFIER_SUFFIXES = [
	"Text",
	"Path",
	"Url",
	"Uri",
	"Href",
	"Pattern",
	"Suffix",
	"Prefix",
	"String",
	"Source",
	"Locale",
	"Codepoint",
	"Char",
	"Word",
	"Markdown",
	"HTML",
	"Html",
	"Css",
	"Xml",
	"Json",
	"Yaml",
	"Sql",
	"Query",
	"Line",
	"Filename",
	"Filepath"
];
const hasStringTypedSuffix = (name) => {
	for (const suffix of STRING_TYPED_IDENTIFIER_SUFFIXES) if (name.length > suffix.length && name.endsWith(suffix)) return true;
	return false;
};
const STRING_TYPED_IDENTIFIER_NAMES = new Set([
	"text",
	"string",
	"str",
	"content",
	"contents",
	"html",
	"xml",
	"json",
	"css",
	"yaml",
	"markdown",
	"md",
	"source",
	"sourceCode",
	"template",
	"raw",
	"comment",
	"description",
	"summary",
	"snippet",
	"url",
	"uri",
	"path",
	"filename",
	"filepath",
	"fileName",
	"filePath",
	"line",
	"char",
	"character",
	"letter",
	"word",
	"phrase",
	"sentence",
	"paragraph",
	"query",
	"search",
	"haystack",
	"needle",
	"suffix",
	"prefix",
	"extension",
	"ext",
	"tableSuffix",
	"tablePrefix",
	"filenameSuffix",
	"filenamePrefix",
	"moduleSuffix",
	"modulePrefix",
	"declaration",
	"expression",
	"statement",
	"literal"
]);
const isLikelyStringReceiver = (receiver) => {
	if (!receiver) return false;
	if (isNodeOfType(receiver, "Literal") && typeof receiver.value === "string") return true;
	if (isNodeOfType(receiver, "TemplateLiteral")) return true;
	if (isNodeOfType(receiver, "CallExpression") && isNodeOfType(receiver.callee, "Identifier") && receiver.callee.name === "String") return true;
	if (isNodeOfType(receiver, "CallExpression") && isNodeOfType(receiver.callee, "MemberExpression") && isNodeOfType(receiver.callee.property, "Identifier") && STRING_RETURNING_METHODS.has(receiver.callee.property.name)) return true;
	if (isNodeOfType(receiver, "MemberExpression") && isNodeOfType(receiver.property, "Identifier")) {
		if (STRING_TYPED_PROPERTY_NAMES.has(receiver.property.name)) return true;
	}
	if (isNodeOfType(receiver, "ChainExpression") && receiver.expression && isLikelyStringReceiver(receiver.expression)) return true;
	if (isNodeOfType(receiver, "Identifier")) {
		if (STRING_TYPED_IDENTIFIER_NAMES.has(receiver.name)) return true;
		if (hasStringTypedSuffix(receiver.name)) return true;
	}
	if (isNodeOfType(receiver, "MemberExpression") && isNodeOfType(receiver.property, "Identifier")) {
		if (hasStringTypedSuffix(receiver.property.name)) return true;
	}
	return false;
};
const INDEX_LIKE_IDENTIFIER_NAMES = new Set([
	"i",
	"j",
	"k",
	"idx",
	"index",
	"cursor",
	"position",
	"pos",
	"lineNumber",
	"lineIndex",
	"ln",
	"row",
	"col",
	"column"
]);
const isIndexedArrayElementWithStringArgument = (receiver, callArgument) => {
	if (!receiver || !isNodeOfType(receiver, "MemberExpression") || !receiver.computed) return false;
	const property = receiver.property;
	if (!(isNodeOfType(property, "Identifier") && INDEX_LIKE_IDENTIFIER_NAMES.has(property.name) || isNodeOfType(property, "Literal") && typeof property.value === "number")) return false;
	if (!callArgument) return false;
	if (isNodeOfType(callArgument, "Literal") && typeof callArgument.value === "string") return true;
	if (isNodeOfType(callArgument, "TemplateLiteral")) return true;
	return false;
};
const jsSetMapLookups = defineRule({
	id: "js-set-map-lookups",
	title: "Array lookup inside a loop",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use a `Set` or `Map` when you check for the same items over and over. `Array.includes`/`find` scans the whole list each time",
	create: (context) => createLoopAwareVisitors({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression") || !isNodeOfType(node.callee.property, "Identifier")) return;
		const methodName = node.callee.property.name;
		if (methodName !== "includes" && methodName !== "indexOf") return;
		if (isLikelyStringReceiver(node.callee.object)) return;
		if (isIndexedArrayElementWithStringArgument(node.callee.object, node.arguments?.[0])) return;
		context.report({
			node,
			message: `This gets slow because array.${methodName}() inside a loop scans the whole list every time, so use a Set for instant lookups`
		});
	} })
});
//#endregion
//#region src/plugin/rules/js-performance/js-tosorted-immutable.ts
const jsTosortedImmutable = defineRule({
	id: "js-tosorted-immutable",
	title: "Spread copy before sort()",
	tags: ["test-noise"],
	severity: "warn",
	disabledBy: ["react-native"],
	recommendation: "Use `array.toSorted()` (ES2023) instead of `[...array].sort()` so you sort without copying the array first",
	create: (context) => ({ CallExpression(node) {
		if (!isMemberProperty(node.callee, "sort")) return;
		const receiver = node.callee.object;
		if (isNodeOfType(receiver, "ArrayExpression") && receiver.elements?.length === 1 && isNodeOfType(receiver.elements[0], "SpreadElement")) context.report({
			node,
			message: "This wastes work because [...array].sort() copies the array just to sort it, so use array.toSorted() to sort without the extra copy (ES2023)"
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-boolean-value.ts
const NEVER_MESSAGE$2 = () => `This prop is written inconsistently.`;
const ALWAYS_MESSAGE$2 = () => `This prop is written inconsistently.`;
const FALSE_OMITTED_MESSAGE = (attributeName) => `\`${attributeName}={false}\` does nothing.`;
const resolveSettings$36 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxBooleanValue ?? {} : {};
	return {
		mode: ruleSettings.mode ?? "never",
		always: ruleSettings.always ?? [],
		never: ruleSettings.never ?? [],
		assumeUndefinedIsFalse: ruleSettings.assumeUndefinedIsFalse ?? false
	};
};
const jsxBooleanValue = defineRule({
	id: "jsx-boolean-value",
	title: "Inconsistent boolean prop notation",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Pick a single boolean-attribute style across the codebase (default: omit `={true}`).",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$36(context.settings);
		const alwaysSet = new Set(settings.always);
		const neverSet = new Set(settings.never);
		return { JSXAttribute(node) {
			if (!isNodeOfType(node.name, "JSXIdentifier")) return;
			const attributeName = node.name.name;
			const value = node.value;
			const isShorthand = value === null || value === void 0;
			const isExpressionBooleanLiteral = value !== null && isNodeOfType(value, "JSXExpressionContainer") && isNodeOfType(value.expression, "Literal") && typeof value.expression.value === "boolean";
			if (settings.mode === "never") {
				if (isShorthand && alwaysSet.has(attributeName)) {
					context.report({
						node,
						message: ALWAYS_MESSAGE$2()
					});
					return;
				}
				if (!isExpressionBooleanLiteral || !value || !isNodeOfType(value, "JSXExpressionContainer")) return;
				const literalValue = value.expression.value;
				if (literalValue === true && !alwaysSet.has(attributeName)) {
					context.report({
						node,
						message: NEVER_MESSAGE$2()
					});
					return;
				}
				if (literalValue === false && settings.assumeUndefinedIsFalse && !alwaysSet.has(attributeName)) context.report({
					node,
					message: FALSE_OMITTED_MESSAGE(attributeName)
				});
				return;
			}
			if (isShorthand && !neverSet.has(attributeName)) {
				context.report({
					node,
					message: ALWAYS_MESSAGE$2()
				});
				return;
			}
			if (isExpressionBooleanLiteral && neverSet.has(attributeName) && value && isNodeOfType(value, "JSXExpressionContainer") && isNodeOfType(value.expression, "Literal")) {
				const literalValue = value.expression.value;
				if (literalValue === true || literalValue === false) context.report({
					node,
					message: NEVER_MESSAGE$2()
				});
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-curly-brace-presence.ts
const UNNECESSARY_BRACES_MESSAGE = "These curly braces do nothing here.";
const REQUIRED_BRACES_MESSAGE = "This value needs curly braces `{ }` to read as an expression.";
const isAllowedMode = (value) => value === "always" || value === "never" || value === "ignore";
const resolveSettings$35 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettingsRaw = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxCurlyBracePresence : void 0;
	if (isAllowedMode(ruleSettingsRaw)) return {
		props: ruleSettingsRaw,
		children: ruleSettingsRaw,
		propElementValues: ruleSettingsRaw
	};
	const ruleSettings = typeof ruleSettingsRaw === "object" && ruleSettingsRaw !== null ? ruleSettingsRaw : {};
	return {
		props: ruleSettings.props ?? "never",
		children: ruleSettings.children ?? "never",
		propElementValues: ruleSettings.propElementValues ?? "ignore"
	};
};
const HTML_ENTITY_REGEX = /(&[A-Za-z\d#]+;)/;
const HTML_ENTITY_GLOBAL_REGEX = /(&[A-Za-z\d#]+;)/g;
const isWhitespaceOnly = (text) => text.length > 0 && /^\s+$/.test(text);
const containsLineBreak = (text) => /[\n\r]/.test(text);
const containsLineBreakLiteral = (text) => /\\[nr]/.test(text);
const containsDisallowedJsxTextChars = (text) => /[<>{}\\]/.test(text);
const containsMultilineComment = (text) => text.includes("/*") || text.includes("*/");
const containsHtmlEntity = (text) => HTML_ENTITY_REGEX.test(text);
const containsOnlyHtmlEntities = (text) => text.replace(HTML_ENTITY_GLOBAL_REGEX, "").trim().length === 0;
const containsUtf8Escape = (text) => /\\u/.test(text);
const containsBothQuotes = (text) => text.includes("\"") && text.includes("'");
const containsAnyQuote = (text) => /["']/.test(text);
const hasAdjacentExpressionContainerSibling = (container) => {
	const parent = container.parent;
	if (!parent) return false;
	if (!isNodeOfType(parent, "JSXElement") && !isNodeOfType(parent, "JSXFragment")) return false;
	const children = parent.children;
	for (let index = 0; index < children.length; index += 1) {
		if (children[index] !== container) continue;
		const previous = children[index - 1];
		const next = children[index + 1];
		if (previous && isNodeOfType(previous, "JSXExpressionContainer")) return true;
		if (next && isNodeOfType(next, "JSXExpressionContainer")) return true;
		return false;
	}
	return false;
};
const isAllowedStringLikeInContainer = (text, container, isProp) => isWhitespaceOnly(text) || containsLineBreak(text) || containsHtmlEntity(text) || isProp && containsBothQuotes(text) || !isProp && containsDisallowedJsxTextChars(text) || !isProp && text.trim() !== text || containsMultilineComment(text) || containsLineBreakLiteral(text) || containsUtf8Escape(text) || hasAdjacentExpressionContainerSibling(container);
const checkExpressionContainer = (container, parentIsAttribute, context, settings) => {
	const expression = container.expression;
	if (expression.type === "JSXEmptyExpression") return;
	const allowed = parentIsAttribute ? settings.props : settings.children;
	if (isNodeOfType(expression, "JSXFragment")) {
		if (!parentIsAttribute && settings.children === "never" && !hasAdjacentExpressionContainerSibling(container)) context.report({
			node: container,
			message: UNNECESSARY_BRACES_MESSAGE
		});
		return;
	}
	if (isNodeOfType(expression, "JSXElement")) {
		if (parentIsAttribute) {
			if (settings.propElementValues === "never" && !expression.closingElement) context.report({
				node: container,
				message: UNNECESSARY_BRACES_MESSAGE
			});
		} else if (settings.children === "never" && !hasAdjacentExpressionContainerSibling(container)) context.report({
			node: container,
			message: UNNECESSARY_BRACES_MESSAGE
		});
		return;
	}
	if (isNodeOfType(expression, "Literal") && typeof expression.value === "string" && allowed === "never") {
		const rawWithQuotes = expression.raw;
		if (isAllowedStringLikeInContainer(typeof rawWithQuotes === "string" && rawWithQuotes.length >= 2 ? rawWithQuotes.slice(1, -1) : expression.value, container, parentIsAttribute)) return;
		context.report({
			node: container,
			message: UNNECESSARY_BRACES_MESSAGE
		});
		return;
	}
	if (isNodeOfType(expression, "TemplateLiteral") && allowed === "never") {
		const cooked = getStaticTemplateLiteralValue(expression);
		if (cooked === null) return;
		const rawSource = expression.quasis?.[0]?.value.raw ?? "";
		if (!parentIsAttribute && containsAnyQuote(cooked)) return;
		if (isAllowedStringLikeInContainer(rawSource, container, parentIsAttribute)) return;
		context.report({
			node: container,
			message: UNNECESSARY_BRACES_MESSAGE
		});
	}
};
const isScriptElement = (openingElement) => isNodeOfType(openingElement.name, "JSXIdentifier") && openingElement.name.name === "script";
const jsxCurlyBracePresence = defineRule({
	id: "jsx-curly-brace-presence",
	title: "Unnecessary curly braces in JSX",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Pick a consistent quoting style for JSX literal values.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$35(context.settings);
		return {
			JSXAttribute(node) {
				const value = node.value;
				if (!value) return;
				if (isNodeOfType(value, "JSXExpressionContainer")) {
					checkExpressionContainer(value, true, context, settings);
					return;
				}
				if (isNodeOfType(value, "JSXElement") || isNodeOfType(value, "JSXFragment")) {
					if (settings.propElementValues === "always") context.report({
						node: value,
						message: REQUIRED_BRACES_MESSAGE
					});
					return;
				}
				if (isNodeOfType(value, "Literal") && typeof value.value === "string") {
					if (settings.props === "always") context.report({
						node: value,
						message: REQUIRED_BRACES_MESSAGE
					});
				}
			},
			JSXExpressionContainer(node) {
				const parent = node.parent;
				if (!parent) return;
				if (!isNodeOfType(parent, "JSXElement") && !isNodeOfType(parent, "JSXFragment")) return;
				if (isNodeOfType(parent, "JSXElement") && settings.children === "never" && isScriptElement(parent.openingElement)) return;
				checkExpressionContainer(node, false, context, settings);
			},
			JSXText(node) {
				if (settings.children !== "always") return;
				const text = node.value;
				if (containsOnlyHtmlEntities(text)) return;
				if (isWhitespaceOnly(text)) return;
				if (text.trim().length === 0) return;
				context.report({
					node,
					message: REQUIRED_BRACES_MESSAGE
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/contains-jsx-element.ts
/**
* True when `root` or any descendant in its subtree is a `JSXElement`
* or `JSXFragment`. Walks the AST without `parent` traversal.
*
* Used by `prefer-function-component` and `jsx-filename-extension`
* to recognise component bodies that render JSX. The richer variants
* in `display-name.ts` (which also recognises `createElement(...)`
* calls) and `no-multi-comp.ts` (which additionally stops at nested
* function / class boundaries) layer extra predicates on top of this
* base scan — they keep their own implementations because the
* augmentation isn't easily expressible as a composition.
*/
const containsJsxElement = (root) => {
	let found = false;
	const visit = (node) => {
		if (found) return;
		if (node.type === "JSXElement" || node.type === "JSXFragment") {
			found = true;
			return;
		}
		const nodeRecord = node;
		for (const key of Object.keys(nodeRecord)) {
			if (key === "parent") continue;
			const child = nodeRecord[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
			if (found) return;
		}
	};
	visit(root);
	return found;
};
//#endregion
//#region src/plugin/rules/react-builtins/jsx-filename-extension.ts
const JSX_NOT_ALLOWED = (extension) => `This file has JSX but a \`${extension}\` name.`;
const EXTENSION_ONLY_FOR_JSX = (extension) => `\`${extension}\` files are meant for JSX, but this one has none.`;
const resolveSettings$34 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxFilenameExtension ?? {} : {};
	return {
		extensions: ruleSettings.extensions ?? ["jsx", "tsx"],
		allow: ruleSettings.allow ?? "always",
		ignoreFilesWithoutCode: ruleSettings.ignoreFilesWithoutCode ?? false
	};
};
const normalizeExtensions = (raw) => {
	const set = /* @__PURE__ */ new Set();
	for (const item of raw) set.add(item.startsWith(".") ? item.slice(1) : item);
	return set;
};
const jsxFilenameExtension = defineRule({
	id: "jsx-filename-extension",
	title: "JSX in disallowed file extension",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Name files with JSX `.jsx` or `.tsx`, or whatever extension your project uses.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$34(context.settings);
		const allowedExtensions = normalizeExtensions(settings.extensions);
		const filename = normalizeFilename$1(context.filename ?? "fixture.tsx");
		const extensionOnly = path.extname(filename).slice(1);
		const fileHasAllowedExtension = allowedExtensions.has(extensionOnly);
		let didReportMismatch = false;
		return {
			Program(node) {
				if (settings.allow !== "as-needed") return;
				if (!fileHasAllowedExtension) return;
				const hasAnyStatements = Array.isArray(node.body) && node.body.length > 0;
				if (settings.ignoreFilesWithoutCode && !hasAnyStatements) return;
				if (containsJsxElement(node)) return;
				context.report({
					node,
					message: EXTENSION_ONLY_FOR_JSX(`.${extensionOnly}`)
				});
			},
			JSXElement(node) {
				if (didReportMismatch || fileHasAllowedExtension) return;
				didReportMismatch = true;
				context.report({
					node,
					message: JSX_NOT_ALLOWED(`.${extensionOnly}`)
				});
			},
			JSXFragment(node) {
				if (didReportMismatch || fileHasAllowedExtension) return;
				didReportMismatch = true;
				context.report({
					node,
					message: JSX_NOT_ALLOWED(`.${extensionOnly}`)
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/is-jsx-fragment-element.ts
const isJsxFragmentElement = (node) => {
	if (!isNodeOfType(node, "JSXOpeningElement")) return false;
	const elementName = node.name;
	if (isNodeOfType(elementName, "JSXIdentifier")) return elementName.name === "Fragment";
	if (isNodeOfType(elementName, "JSXMemberExpression")) {
		if (!isNodeOfType(elementName.object, "JSXIdentifier")) return false;
		if (elementName.object.name !== "React") return false;
		return elementName.property.name === "Fragment";
	}
	return false;
};
//#endregion
//#region src/plugin/rules/react-builtins/jsx-fragments.ts
const SYNTAX_MESSAGE = "This fragment is written inconsistently.";
const ELEMENT_MESSAGE = "This fragment is written inconsistently.";
const resolveSettings$33 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { mode: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxFragments ?? {} : {}).mode ?? "syntax" };
};
const jsxFragments = defineRule({
	id: "jsx-fragments",
	title: "Inconsistent fragment syntax",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Pick one fragment style across the codebase.",
	category: "Architecture",
	create: (context) => {
		const { mode } = resolveSettings$33(context.settings);
		return {
			JSXElement(node) {
				if (mode !== "syntax") return;
				if (!node.closingElement) return;
				const openingElement = node.openingElement;
				if (!isJsxFragmentElement(openingElement)) return;
				if (openingElement.attributes.length > 0) return;
				context.report({
					node: openingElement,
					message: SYNTAX_MESSAGE
				});
			},
			JSXFragment(node) {
				if (mode !== "element") return;
				context.report({
					node: node.openingFragment,
					message: ELEMENT_MESSAGE
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-handler-names.ts
const buildHandlerNameMessage = (handlerName, propKey) => `The handler "${handlerName}" for "${propKey}" is hard to recognize.`;
const buildHandlerPropMessage = (propKey, propValue) => `The prop "${propKey}" passes a handler "${propValue}".`;
const DEFAULT_HANDLER_PREFIX = "handle";
const DEFAULT_HANDLER_PROP_PREFIX = "on";
const splitPrefixes = (prefixes) => prefixes.split("|").map((token) => token.trim()).filter((token) => token.length > 0);
const buildHandlerRegex = (handlerPrefix, handlerPropPrefix) => {
	if (handlerPrefix.length === 0 || handlerPropPrefix.length === 0) return null;
	const tokens = splitPrefixes(handlerPrefix);
	if (tokens.length === 0) return null;
	const escaped = tokens.map((token) => token.replace(/[.+?^${}()|[\]\\]/g, "\\$&")).join("|");
	return new RegExp(`^((.*\\.)?(${escaped}))[0-9]*[A-Z].*$`);
};
const buildPropRegex = (handlerPropPrefix) => {
	if (handlerPropPrefix.length === 0) return null;
	const tokens = splitPrefixes(handlerPropPrefix);
	if (tokens.length === 0) return null;
	const escaped = tokens.map((token) => token.replace(/[.+?^${}()|[\]\\]/g, "\\$&")).join("|");
	return new RegExp(`^(${escaped})[A-Z].*$`);
};
const resolveSettings$32 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxHandlerNames ?? {} : {};
	let handlerPrefix = DEFAULT_HANDLER_PREFIX;
	let propPrefix = DEFAULT_HANDLER_PROP_PREFIX;
	if (ruleSettings.eventHandlerPrefix === false) handlerPrefix = "";
	else if (typeof ruleSettings.eventHandlerPrefix === "string") handlerPrefix = ruleSettings.eventHandlerPrefix;
	if (ruleSettings.eventHandlerPropPrefix === false) propPrefix = "";
	else if (typeof ruleSettings.eventHandlerPropPrefix === "string") propPrefix = ruleSettings.eventHandlerPropPrefix;
	return {
		checkInlineFunction: ruleSettings.checkInlineFunction ?? false,
		checkLocalVariables: ruleSettings.checkLocalVariables ?? false,
		eventHandlerPrefix: handlerPrefix,
		eventHandlerPropPrefix: propPrefix,
		ignoreComponentNames: ruleSettings.ignoreComponentNames ?? [],
		handlerRegex: buildHandlerRegex(handlerPrefix, propPrefix),
		propRegex: buildPropRegex(propPrefix)
	};
};
const getHandlerNameFromMemberExpression = (memberExpression) => {
	if (!isNodeOfType(memberExpression.property, "Identifier")) return null;
	const lastName = memberExpression.property.name;
	const object = memberExpression.object;
	if (isNodeOfType(object, "Identifier")) return {
		name: lastName,
		isPropsHandler: object.name === "props"
	};
	if (isNodeOfType(object, "MemberExpression")) {
		if (isNodeOfType(object.object, "ThisExpression") && isNodeOfType(object.property, "Identifier")) return {
			name: lastName,
			isPropsHandler: object.property.name === "props"
		};
		return {
			name: lastName,
			isPropsHandler: false
		};
	}
	if (isNodeOfType(object, "ThisExpression")) return {
		name: lastName,
		isPropsHandler: false
	};
	return {
		name: lastName,
		isPropsHandler: false
	};
};
const getHandlerNameFromArrowFunction = (arrowFunction) => {
	const body = arrowFunction.body;
	if (isNodeOfType(body, "BlockStatement")) return null;
	if (!isNodeOfType(body, "CallExpression")) return null;
	const callee = body.callee;
	if (isNodeOfType(callee, "Identifier")) return {
		name: callee.name,
		isPropsHandler: false
	};
	if (isNodeOfType(callee, "MemberExpression")) return getHandlerNameFromMemberExpression(callee);
	return null;
};
const isMemberExpressionCallee = (arrowFunction) => {
	const body = arrowFunction.body;
	let callExpression = null;
	if (isNodeOfType(body, "CallExpression")) callExpression = body;
	else if (isNodeOfType(body, "BlockStatement")) {
		const first = body.body[0];
		if (first && isNodeOfType(first, "ExpressionStatement") && isNodeOfType(first.expression, "CallExpression")) callExpression = first.expression;
	}
	return callExpression !== null && isNodeOfType(callExpression.callee, "MemberExpression");
};
const jsxHandlerNames = defineRule({
	id: "jsx-handler-names",
	title: "Inconsistent event handler names",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Use the `on…` prefix for event-handler props and `handle…` for handlers.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$32(context.settings);
		return { JSXAttribute(node) {
			if (settings.ignoreComponentNames.length > 0) {
				const opening = node.parent;
				if (!opening || !isNodeOfType(opening, "JSXOpeningElement")) return;
				const openingName = opening.name;
				if (!isNodeOfType(openingName, "JSXIdentifier")) return;
				const componentName = openingName.name;
				for (const pattern of settings.ignoreComponentNames) if (compileGlob(pattern).test(componentName)) return;
			}
			const propName = getJsxAttributeName(node.name);
			if (!propName) return;
			if (propName === "ref") return;
			const value = node.value;
			if (!value || !isNodeOfType(value, "JSXExpressionContainer")) return;
			const expression = value.expression;
			if (expression.type === "JSXEmptyExpression") return;
			let handlerName = null;
			let isPropsHandler = false;
			if (isNodeOfType(expression, "MemberExpression")) {
				const result = getHandlerNameFromMemberExpression(expression);
				handlerName = result?.name ?? null;
				isPropsHandler = result?.isPropsHandler ?? false;
			} else if (isNodeOfType(expression, "Identifier")) {
				if (!settings.checkLocalVariables) return;
				handlerName = expression.name;
			} else if (isNodeOfType(expression, "ArrowFunctionExpression")) {
				if (!settings.checkInlineFunction) return;
				if (!settings.checkLocalVariables && !isMemberExpressionCallee(expression)) return;
				const result = getHandlerNameFromArrowFunction(expression);
				handlerName = result?.name ?? null;
				isPropsHandler = result?.isPropsHandler ?? false;
			} else {
				if (!settings.checkLocalVariables) return;
				handlerName = "";
			}
			const propIsEventHandler = settings.propRegex ? settings.propRegex.test(propName) : null;
			let handlerNameIsCorrect;
			if (handlerName === null) handlerNameIsCorrect = false;
			else if (isPropsHandler && settings.propRegex !== null && settings.propRegex.test(handlerName)) handlerNameIsCorrect = true;
			else if (settings.handlerRegex) handlerNameIsCorrect = settings.handlerRegex.test(handlerName);
			else handlerNameIsCorrect = null;
			if (propIsEventHandler === true && handlerNameIsCorrect === false) {
				context.report({
					node,
					message: buildHandlerNameMessage(handlerName ?? "", propName)
				});
				return;
			}
			if (propIsEventHandler === false && handlerNameIsCorrect === true) context.report({
				node,
				message: buildHandlerPropMessage(propName, handlerName ?? "")
			});
		} };
	}
});
//#endregion
//#region src/plugin/utils/has-jsx-key-attribute.ts
/**
* True when a JSX opening element carries a `key={...}` attribute.
* Used by `jsx-key` and `jsx-no-useless-fragment` — both rules need to
* see through fragment wrappers that exist only to hold a key.
*
* Was duplicated in both rule files; consolidated here so adding
* variants (e.g. spread-attribute handling) propagates to both.
*/
const hasJsxKeyAttribute = (openingElement) => {
	for (const attribute of openingElement.attributes) {
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
		if (attribute.name.name === "key") return true;
	}
	return false;
};
//#endregion
//#region src/plugin/rules/react-builtins/jsx-key.ts
const ITERATOR_METHOD_NAMES = new Set([
	"map",
	"flatMap",
	"from"
]);
const MISSING_KEY_ARRAY = "Your users can see the wrong data when this array reorders.";
const MISSING_KEY_ITERATOR = "Your users can see the wrong data when this list reorders.";
const KEY_BEFORE_SPREAD = "The `{...spread}` can overwrite this `key` & break React's tracking.";
const DUPLICATE_KEY = (keyValue) => `Your users can see the wrong data because two elements share the key "${keyValue}".`;
const resolveSettings$31 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxKey ?? {} : {};
	return {
		checkKeyMustBeforeSpread: ruleSettings.checkKeyMustBeforeSpread ?? true,
		warnOnDuplicates: ruleSettings.warnOnDuplicates ?? false
	};
};
const findEnclosingIteratorContext = (jsxNode) => {
	let current = jsxNode;
	let isOutsideContainingFunction = false;
	let didSeeReturnStatement = false;
	while (current && current.parent) {
		const parent = current.parent;
		if (isNodeOfType(parent, "ArrowFunctionExpression") || isNodeOfType(parent, "FunctionExpression") || isNodeOfType(parent, "FunctionDeclaration")) {
			if (isNodeOfType(parent, "ArrowFunctionExpression")) {
				const isExpressionBody = parent.body && parent.body.type !== "BlockStatement";
				if (!didSeeReturnStatement && !isExpressionBody) return null;
			} else if (!didSeeReturnStatement) return null;
			const grandparent = parent.parent;
			if (grandparent && isNodeOfType(grandparent, "Property")) return null;
			if (isOutsideContainingFunction) return null;
			isOutsideContainingFunction = true;
		} else if (isNodeOfType(parent, "ArrayExpression")) {
			if (isOutsideContainingFunction) return null;
			const arrayParent = parent.parent;
			if (arrayParent && isNodeOfType(arrayParent, "Property")) return null;
			if (arrayParent && isNodeOfType(arrayParent, "ArrayExpression")) return null;
			return { kind: "array" };
		} else if (isNodeOfType(parent, "CallExpression")) {
			const callee = parent.callee;
			if (!isNodeOfType(callee, "MemberExpression")) return null;
			if (!isNodeOfType(callee.property, "Identifier")) return null;
			const methodName = callee.property.name;
			if (!ITERATOR_METHOD_NAMES.has(methodName)) return null;
			const targetArgIndex = methodName === "from" ? 1 : 0;
			const targetArg = parent.arguments[targetArgIndex];
			if (!targetArg) return null;
			let walker = current;
			while (walker && walker !== parent) {
				if (walker === targetArg) return {
					kind: "iterator",
					callExpression: parent
				};
				walker = walker.parent ?? null;
			}
			return null;
		} else if (isNodeOfType(parent, "JSXElement") || isNodeOfType(parent, "JSXOpeningElement") || isNodeOfType(parent, "JSXFragment") || isNodeOfType(parent, "Property")) return null;
		else if (isNodeOfType(parent, "ReturnStatement")) didSeeReturnStatement = true;
		current = parent;
	}
	return null;
};
const resolveIterationItemName = (callExpression) => {
	if (!isNodeOfType(callExpression, "CallExpression")) return null;
	const callee = callExpression.callee;
	if (!isNodeOfType(callee, "MemberExpression")) return null;
	if (!isNodeOfType(callee.property, "Identifier")) return null;
	const targetArgIndex = callee.property.name === "from" ? 1 : 0;
	const callback = callExpression.arguments[targetArgIndex];
	if (!callback || !isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) return null;
	const firstParam = callback.params[0];
	return firstParam && isNodeOfType(firstParam, "Identifier") ? firstParam.name : null;
};
const spreadsIterationItem = (openingElement, iterationItemName) => {
	for (const attribute of openingElement.attributes) {
		if (!isNodeOfType(attribute, "JSXSpreadAttribute")) continue;
		const argument = attribute.argument;
		if (isNodeOfType(argument, "Identifier") && argument.name === iterationItemName) return true;
	}
	return false;
};
const isWithinChildrenToArray = (jsxNode) => {
	let current = jsxNode.parent;
	while (current) {
		if (isNodeOfType(current, "CallExpression")) {
			const callee = current.callee;
			if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && callee.property.name === "toArray") {
				const objectExpression = callee.object;
				if (isNodeOfType(objectExpression, "Identifier") && objectExpression.name === "Children") return true;
				if (isNodeOfType(objectExpression, "MemberExpression") && isNodeOfType(objectExpression.property, "Identifier") && objectExpression.property.name === "Children") return true;
			}
		}
		current = current.parent ?? null;
	}
	return false;
};
const checkKeyBeforeSpread = (context, openingElement) => {
	let keyIndex = null;
	let keyAttribute = null;
	let firstSpreadIndex = null;
	for (let attributeIndex = 0; attributeIndex < openingElement.attributes.length; attributeIndex++) {
		const attribute = openingElement.attributes[attributeIndex];
		if (isNodeOfType(attribute, "JSXAttribute")) {
			if (isNodeOfType(attribute.name, "JSXIdentifier") && attribute.name.name === "key") {
				keyIndex = attributeIndex;
				keyAttribute = attribute;
			}
		} else if (isNodeOfType(attribute, "JSXSpreadAttribute")) {
			if (firstSpreadIndex === null) firstSpreadIndex = attributeIndex;
		}
	}
	if (keyIndex !== null && firstSpreadIndex !== null && keyIndex > firstSpreadIndex && keyAttribute) context.report({
		node: keyAttribute,
		message: KEY_BEFORE_SPREAD
	});
};
const getKeyAttributeValueString = (openingElement) => {
	for (const attribute of openingElement.attributes) {
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		if (!isNodeOfType(attribute.name, "JSXIdentifier") || attribute.name.name !== "key") continue;
		const value = attribute.value;
		if (!value) return null;
		if (isNodeOfType(value, "Literal")) {
			const literalValue = value.value;
			if (typeof literalValue === "string" || typeof literalValue === "number") return {
				keyValue: String(literalValue),
				node: attribute
			};
			return null;
		}
		if (isNodeOfType(value, "JSXExpressionContainer")) {
			const expression = value.expression;
			if (isNodeOfType(expression, "Literal")) {
				const literalValue = expression.value;
				if (typeof literalValue === "string" || typeof literalValue === "number") return {
					keyValue: String(literalValue),
					node: attribute
				};
				return null;
			}
			if (isNodeOfType(expression, "TemplateLiteral")) {
				const staticValue = getStaticTemplateLiteralValue(expression);
				if (staticValue !== null) return {
					keyValue: staticValue,
					node: attribute
				};
			}
		}
	}
	return null;
};
const jsxKey = defineRule({
	id: "jsx-key",
	title: "Missing key in list",
	severity: "error",
	recommendation: "Add a `key={...}` prop to each element produced inside `.map` / array literal.",
	create: (context) => {
		const settings = resolveSettings$31(context.settings);
		return {
			JSXElement(node) {
				const openingElement = node.openingElement;
				if (settings.checkKeyMustBeforeSpread) checkKeyBeforeSpread(context, openingElement);
				if (settings.warnOnDuplicates) {
					const seenKeys = /* @__PURE__ */ new Set();
					for (const child of node.children) {
						if (!isNodeOfType(child, "JSXElement")) continue;
						const keyValue = getKeyAttributeValueString(child.openingElement);
						if (!keyValue) continue;
						if (seenKeys.has(keyValue.keyValue)) context.report({
							node: keyValue.node,
							message: DUPLICATE_KEY(keyValue.keyValue)
						});
						else seenKeys.add(keyValue.keyValue);
					}
				}
				const enclosingContext = findEnclosingIteratorContext(node);
				if (!enclosingContext) return;
				if (isWithinChildrenToArray(node)) return;
				if (hasJsxKeyAttribute(openingElement)) return;
				if (enclosingContext.kind === "iterator") {
					const iterationItemName = resolveIterationItemName(enclosingContext.callExpression);
					if (iterationItemName && spreadsIterationItem(openingElement, iterationItemName)) return;
				}
				context.report({
					node: openingElement,
					message: enclosingContext.kind === "array" ? MISSING_KEY_ARRAY : MISSING_KEY_ITERATOR
				});
			},
			ArrayExpression(node) {
				if (!settings.warnOnDuplicates) return;
				const seenKeys = /* @__PURE__ */ new Set();
				for (const element of node.elements) {
					if (!element) continue;
					if (!isNodeOfType(element, "JSXElement")) continue;
					const keyValue = getKeyAttributeValueString(element.openingElement);
					if (!keyValue) continue;
					if (seenKeys.has(keyValue.keyValue)) context.report({
						node: keyValue.node,
						message: DUPLICATE_KEY(keyValue.keyValue)
					});
					else seenKeys.add(keyValue.keyValue);
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/find-variable-initializer.ts
const FUNCTION_LIKE_TYPES = new Set([
	"FunctionDeclaration",
	"FunctionExpression",
	"ArrowFunctionExpression",
	"MethodDefinition",
	"Program"
]);
const findScopeOwner = (node) => {
	let ancestor = node;
	while (ancestor) {
		if (FUNCTION_LIKE_TYPES.has(ancestor.type)) return ancestor;
		ancestor = ancestor.parent ?? null;
	}
	return null;
};
const findBlockScopeOwner = (declaratorNode, declarationKind) => {
	if (declarationKind !== "let" && declarationKind !== "const") return findScopeOwner(declaratorNode);
	let ancestor = declaratorNode.parent;
	while (ancestor) {
		if (ancestor.type === "BlockStatement") {
			const blockParent = ancestor.parent;
			if (blockParent && (blockParent.type === "FunctionDeclaration" || blockParent.type === "FunctionExpression" || blockParent.type === "ArrowFunctionExpression" || blockParent.type === "MethodDefinition")) return findScopeOwner(declaratorNode);
			return ancestor;
		}
		if (FUNCTION_LIKE_TYPES.has(ancestor.type)) return ancestor;
		ancestor = ancestor.parent ?? null;
	}
	return null;
};
const collectFromBindingPattern = (pattern, initializer, scopeOwner, out) => {
	if (isNodeOfType(pattern, "Identifier")) {
		const list = out.get(pattern.name) ?? [];
		list.push({
			bindingIdentifier: pattern,
			initializer,
			scopeOwner
		});
		out.set(pattern.name, list);
		return;
	}
	if (isNodeOfType(pattern, "ObjectPattern")) {
		for (const property of pattern.properties) if (isNodeOfType(property, "Property")) {
			const valueNode = property.value;
			collectFromBindingPattern(valueNode, isNodeOfType(valueNode, "AssignmentPattern") ? valueNode.right : null, scopeOwner, out);
		} else if (isNodeOfType(property, "RestElement")) collectFromBindingPattern(property.argument, null, scopeOwner, out);
		return;
	}
	if (isNodeOfType(pattern, "ArrayPattern")) {
		for (const element of pattern.elements) {
			if (!element) continue;
			collectFromBindingPattern(element, isNodeOfType(element, "AssignmentPattern") ? element.right ?? null : null, scopeOwner, out);
		}
		return;
	}
	if (isNodeOfType(pattern, "AssignmentPattern")) {
		collectFromBindingPattern(pattern.left, pattern.right ?? null, scopeOwner, out);
		return;
	}
	if (isNodeOfType(pattern, "RestElement")) collectFromBindingPattern(pattern.argument, null, scopeOwner, out);
};
const buildBindingIndex = (root) => {
	const out = /* @__PURE__ */ new Map();
	const visit = (node) => {
		if (isNodeOfType(node, "VariableDeclarator")) {
			const declaration = node.parent;
			const scopeOwner = findBlockScopeOwner(node, declaration && isNodeOfType(declaration, "VariableDeclaration") ? declaration.kind : void 0);
			if (scopeOwner) collectFromBindingPattern(node.id, node.init ?? null, scopeOwner, out);
		}
		if ((isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression")) && node.id) {
			const enclosing = node.parent ? findScopeOwner(node.parent) : null;
			if (enclosing) {
				const list = out.get(node.id.name) ?? [];
				list.push({
					bindingIdentifier: node.id,
					initializer: node,
					scopeOwner: enclosing
				});
				out.set(node.id.name, list);
			}
		}
		if ((isNodeOfType(node, "ClassDeclaration") || isNodeOfType(node, "ClassExpression")) && node.id) {
			const enclosing = node.parent ? findScopeOwner(node.parent) : null;
			if (enclosing) {
				const list = out.get(node.id.name) ?? [];
				list.push({
					bindingIdentifier: node.id,
					initializer: node,
					scopeOwner: enclosing
				});
				out.set(node.id.name, list);
			}
		}
		if (isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression")) {
			if (Array.isArray(node.params)) for (const param of node.params) {
				if (!param) continue;
				collectFromBindingPattern(param, null, node, out);
				if (isNodeOfType(param, "AssignmentPattern")) collectFromBindingPattern(param.left ?? null, param.right ?? null, node, out);
			}
		}
		if (isNodeOfType(node, "ImportDeclaration")) {
			const scopeOwner = findScopeOwner(node);
			if (scopeOwner && Array.isArray(node.specifiers)) for (const specifier of node.specifiers) {
				const local = specifier.local;
				if (local && isNodeOfType(local, "Identifier")) {
					const list = out.get(local.name) ?? [];
					list.push({
						bindingIdentifier: local,
						initializer: specifier,
						scopeOwner
					});
					out.set(local.name, list);
				}
			}
		}
		if (node.type === "TSImportEqualsDeclaration" || node.type === "TSEnumDeclaration" || node.type === "TSModuleDeclaration") {
			const idNode = node.id;
			if (idNode && idNode.type === "Identifier") {
				const idObject = idNode;
				const scopeOwner = findScopeOwner(node);
				if (scopeOwner && typeof idObject.name === "string") {
					const list = out.get(idObject.name) ?? [];
					list.push({
						bindingIdentifier: idNode,
						initializer: null,
						scopeOwner
					});
					out.set(idObject.name, list);
				}
			}
		}
		const nodeRecord = node;
		for (const key of Object.keys(nodeRecord)) {
			if (key === "parent") continue;
			const child = nodeRecord[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(root);
	return out;
};
const programRootCache = /* @__PURE__ */ new WeakMap();
const getBindingIndex = (referenceNode) => {
	const programRoot = findProgramRoot(referenceNode);
	if (!programRoot) return null;
	let index = programRootCache.get(programRoot);
	if (!index) {
		index = buildBindingIndex(programRoot);
		programRootCache.set(programRoot, index);
	}
	return index;
};
const findVariableInitializer = (referenceNode, bindingName) => {
	const index = getBindingIndex(referenceNode);
	if (!index) return null;
	const candidates = index.get(bindingName);
	if (!candidates || candidates.length === 0) return null;
	const referenceAncestors = /* @__PURE__ */ new Set();
	let walker = referenceNode;
	while (walker) {
		referenceAncestors.add(walker);
		walker = walker.parent ?? null;
	}
	let best = null;
	for (const candidate of candidates) {
		if (!referenceAncestors.has(candidate.scopeOwner)) continue;
		if (best === null) {
			best = candidate;
			continue;
		}
		let cursor = candidate.scopeOwner;
		while (cursor) {
			if (cursor === best.scopeOwner) {
				best = candidate;
				break;
			}
			cursor = cursor.parent ?? null;
		}
	}
	return best;
};
//#endregion
//#region src/plugin/rules/react-builtins/jsx-max-depth.ts
const buildMessage$18 = (depth, max) => `This JSX is hard to read at ${depth} levels deep, past the limit of ${max}.`;
const DEFAULT_MAX_DEPTH = 14;
const resolveSettings$30 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { max: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxMaxDepth ?? {} : {}).max ?? DEFAULT_MAX_DEPTH };
};
const isLeafJsxNode = (node) => {
	if (!isNodeOfType(node, "JSXElement") && !isNodeOfType(node, "JSXFragment")) return true;
	const children = node.children;
	for (const child of children) if (isNodeOfType(child, "JSXElement") || isNodeOfType(child, "JSXFragment")) return false;
	return true;
};
const computeJsxAncestorDepth = (node) => {
	let depth = 0;
	let ancestor = node.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "JSXElement") || isNodeOfType(ancestor, "JSXFragment")) depth += 1;
		ancestor = ancestor.parent ?? null;
	}
	return depth;
};
const computeExpressionDepth = (expression, visited) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "JSXElement") || isNodeOfType(stripped, "JSXFragment")) {
		const children = stripped.children;
		return computeChildrenDepth(children, visited);
	}
	if (isNodeOfType(stripped, "Identifier")) {
		if (visited.has(stripped.name)) return 0;
		visited.add(stripped.name);
		const binding = findVariableInitializer(stripped, stripped.name);
		if (!binding || !binding.initializer) return 0;
		return computeExpressionDepth(binding.initializer, visited);
	}
	return 0;
};
const computeChildrenDepth = (children, visited) => {
	let maxDepth = 0;
	for (const child of children) {
		let depth = 0;
		if (isNodeOfType(child, "JSXElement") || isNodeOfType(child, "JSXFragment")) {
			const innerChildren = child.children;
			depth = computeChildrenDepth(innerChildren, visited) + 1;
		} else if (isNodeOfType(child, "JSXExpressionContainer")) {
			const inner = child.expression;
			if (inner && inner.type !== "JSXEmptyExpression") {
				const resolved = computeExpressionDepth(inner, visited);
				depth = resolved > 0 ? resolved + 1 : 0;
			}
		}
		if (depth > maxDepth) maxDepth = depth;
	}
	return maxDepth;
};
const jsxMaxDepth = defineRule({
	id: "jsx-max-depth",
	title: "JSX nested too deeply",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Pull deeply nested JSX into smaller components so it's easier to read.",
	category: "Architecture",
	create: (context) => {
		const { max } = resolveSettings$30(context.settings);
		const checkNode = (node) => {
			if (!isLeafJsxNode(node)) return;
			const total = computeJsxAncestorDepth(node) + computeChildrenDepth(node.children ?? [], /* @__PURE__ */ new Set());
			if (total > max) context.report({
				node,
				message: buildMessage$18(total, max)
			});
		};
		return {
			JSXElement(node) {
				checkNode(node);
			},
			JSXFragment(node) {
				checkNode(node);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-comment-textnodes.ts
const MESSAGE$41 = "Your users see this comment as text on the page because `//` & `/*` aren't hidden in JSX.";
const LITERAL_TEXT_TAGS = new Set([
	"code",
	"pre",
	"kbd",
	"samp",
	"tt"
]);
const hasCommentLikePattern = (text) => {
	for (const rawLine of text.split("\n")) {
		const trimmed = rawLine.trim();
		if (trimmed.startsWith("//") || trimmed.startsWith("/*")) return true;
	}
	return false;
};
const isInsideLiteralTextTag = (node) => {
	let ancestor = node.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "JSXElement")) {
			const openingName = ancestor.openingElement.name;
			if (isNodeOfType(openingName, "JSXIdentifier") && LITERAL_TEXT_TAGS.has(openingName.name)) return true;
		}
		ancestor = ancestor.parent ?? null;
	}
	return false;
};
const jsxNoCommentTextnodes = defineRule({
	id: "jsx-no-comment-textnodes",
	title: "Comment rendered as JSX text",
	severity: "warn",
	recommendation: "Wrap JSX comments in `{/* … */}` so they're parsed as comments, not children.",
	create: (context) => ({ JSXText(node) {
		if (!hasCommentLikePattern(node.value)) return;
		if (isInsideLiteralTextTag(node)) return;
		context.report({
			node,
			message: MESSAGE$41
		});
	} })
});
//#endregion
//#region src/plugin/utils/is-canonical-react-namespace-name.ts
const isCanonicalReactNamespaceName = (namespaceName) => {
	if (namespaceName === "React") return true;
	if (namespaceName === "react") return true;
	if (namespaceName.startsWith("_react")) return true;
	if (namespaceName.startsWith("_React")) return true;
	return false;
};
//#endregion
//#region src/plugin/utils/is-inside-function-scope.ts
const FUNCTION_NODE_TYPES$1 = new Set([
	"FunctionDeclaration",
	"FunctionExpression",
	"ArrowFunctionExpression",
	"MethodDefinition"
]);
const isInsideFunctionScope = (node) => {
	let ancestor = node.parent;
	while (ancestor) {
		if (FUNCTION_NODE_TYPES$1.has(ancestor.type)) return true;
		ancestor = ancestor.parent ?? null;
	}
	return false;
};
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-constructed-context-values.ts
const MESSAGE$40 = "Every reader of this context redraws on each render because you build its `value` inline.";
const CONTEXT_MODULES$1 = [
	"react",
	"use-context-selector",
	"react-tracked"
];
const isConstructedValue = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "ObjectExpression") || isNodeOfType(stripped, "ArrayExpression") || isNodeOfType(stripped, "ArrowFunctionExpression") || isNodeOfType(stripped, "FunctionExpression") || isNodeOfType(stripped, "ClassExpression") || isNodeOfType(stripped, "NewExpression") || isNodeOfType(stripped, "JSXElement") || isNodeOfType(stripped, "JSXFragment")) return true;
	if (isNodeOfType(stripped, "ConditionalExpression")) return isConstructedValue(stripped.consequent) || isConstructedValue(stripped.alternate);
	if (isNodeOfType(stripped, "LogicalExpression")) return isConstructedValue(stripped.left) || isConstructedValue(stripped.right);
	return false;
};
const isProviderMemberName = (node) => {
	if (!isNodeOfType(node, "JSXMemberExpression")) return false;
	return node.property.name === "Provider";
};
const isCreateContextCallExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "CallExpression")) return false;
	const callee = stripped.callee;
	if (isNodeOfType(callee, "Identifier")) {
		for (const moduleName of CONTEXT_MODULES$1) if (getImportedNameFromModule(callee, callee.name, moduleName) === "createContext") return true;
		return false;
	}
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed) {
		const namespaceIdentifier = callee.object;
		const propertyIdentifier = callee.property;
		if (!isNodeOfType(namespaceIdentifier, "Identifier")) return false;
		if (!isNodeOfType(propertyIdentifier, "Identifier")) return false;
		if (propertyIdentifier.name !== "createContext") return false;
		if (isCanonicalReactNamespaceName(namespaceIdentifier.name)) return true;
		for (const moduleName of CONTEXT_MODULES$1) {
			if (getImportedNameFromModule(namespaceIdentifier, namespaceIdentifier.name, moduleName) === null) continue;
			return true;
		}
	}
	return false;
};
const collectContextBindings = (programRoot) => {
	const bindings = /* @__PURE__ */ new Set();
	if (!isNodeOfType(programRoot, "Program")) return bindings;
	for (const topLevel of programRoot.body ?? []) {
		let declaration = topLevel;
		if (isNodeOfType(topLevel, "ExportNamedDeclaration") && topLevel.declaration) declaration = topLevel.declaration;
		if (!declaration || !isNodeOfType(declaration, "VariableDeclaration")) continue;
		for (const declarator of declaration.declarations ?? []) {
			if (!isNodeOfType(declarator, "VariableDeclarator")) continue;
			if (!isNodeOfType(declarator.id, "Identifier")) continue;
			if (!declarator.init) continue;
			if (!isAstNode(declarator.init)) continue;
			if (!isCreateContextCallExpression(declarator.init)) continue;
			bindings.add(declarator.id.name);
		}
	}
	return bindings;
};
const isCreateContextBindingJsxName = (node, contextBindings) => {
	if (!isNodeOfType(node, "JSXIdentifier")) return false;
	if (!contextBindings.has(node.name)) return false;
	const binding = findVariableInitializer(node, node.name);
	if (!binding) return false;
	return binding.scopeOwner.type === "Program";
};
const jsxNoConstructedContextValues = defineRule({
	id: "jsx-no-constructed-context-values",
	title: "Unstable context provider value",
	tags: ["react-jsx-only"],
	severity: "warn",
	disabledBy: ["react-compiler"],
	recommendation: "Wrap the context value in `useMemo`, or move it outside the component.",
	category: "Performance",
	create: (context) => {
		const isTestlikeFile = isTestlikeFilename(context.filename);
		let contextBindings = /* @__PURE__ */ new Set();
		return {
			Program(node) {
				contextBindings = collectContextBindings(node);
			},
			JSXOpeningElement(node) {
				if (isTestlikeFile) return;
				const nameNode = node.name;
				const isLegacyProvider = isProviderMemberName(nameNode);
				const isReact19Shorthand = isCreateContextBindingJsxName(nameNode, contextBindings);
				if (!isLegacyProvider && !isReact19Shorthand) return;
				if (!isInsideFunctionScope(node)) return;
				for (const attribute of node.attributes) {
					if (!isNodeOfType(attribute, "JSXAttribute")) continue;
					if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
					if (attribute.name.name !== "value") continue;
					const attributeValue = attribute.value;
					if (!attributeValue) continue;
					if (!isNodeOfType(attributeValue, "JSXExpressionContainer")) continue;
					const innerExpression = attributeValue.expression;
					if (!innerExpression || innerExpression.type === "JSXEmptyExpression") continue;
					if (!isConstructedValue(innerExpression)) continue;
					context.report({
						node: attribute,
						message: MESSAGE$40
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-duplicate-props.ts
const jsxNoDuplicateProps = defineRule({
	id: "jsx-no-duplicate-props",
	title: "Duplicate props on element",
	severity: "error",
	recommendation: "Remove or rename one of the duplicate props.",
	create: (context) => ({ JSXOpeningElement(node) {
		const seenPropNames = /* @__PURE__ */ new Set();
		for (const attribute of node.attributes) {
			if (!isNodeOfType(attribute, "JSXAttribute")) continue;
			if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
			const propName = attribute.name.name;
			if (seenPropNames.has(propName)) context.report({
				node: attribute,
				message: `Your users can get the wrong value because React keeps only the last "${propName}" & drops the first.`
			});
			seenPropNames.add(propName);
		}
	} })
});
//#endregion
//#region src/plugin/utils/build-same-file-memo-registry.ts
const HOC_NAMES_FOR_MEMOISATION = new Set([
	"memo",
	"React.memo",
	"observer",
	"observable",
	"lazy",
	"React.lazy",
	"withTracking"
]);
const isMemoisingCall = (call) => {
	if (!isNodeOfType(call, "CallExpression")) return false;
	const name = flattenCalleeName(call.callee);
	return name !== null && HOC_NAMES_FOR_MEMOISATION.has(name);
};
const buildSameFileMemoRegistry = (program) => {
	const registry = /* @__PURE__ */ new Map();
	if (!isNodeOfType(program, "Program")) return registry;
	for (const statement of program.body) {
		const root = isNodeOfType(statement, "ExportNamedDeclaration") ? statement.declaration : isNodeOfType(statement, "ExportDefaultDeclaration") ? statement.declaration : statement;
		if (!root) continue;
		if (isNodeOfType(root, "VariableDeclaration")) {
			for (const declarator of root.declarations ?? []) {
				if (!isNodeOfType(declarator, "VariableDeclarator")) continue;
				if (!isNodeOfType(declarator.id, "Identifier")) continue;
				if (!declarator.init) continue;
				const init = declarator.init;
				if (isMemoisingCall(init)) registry.set(declarator.id.name, "memoised");
				else if (isNodeOfType(init, "FunctionExpression") || isNodeOfType(init, "ArrowFunctionExpression")) registry.set(declarator.id.name, "not-memoised");
			}
			continue;
		}
		if (isNodeOfType(root, "FunctionDeclaration") && root.id) {
			registry.set(root.id.name, "not-memoised");
			continue;
		}
	}
	return registry;
};
const memoStatusForJsxOpeningName = (registry, openingName) => {
	if (!registry || !openingName) return "unknown";
	if (!isNodeOfType(openingName, "JSXIdentifier")) return "unknown";
	return registry.get(openingName.name) ?? "unknown";
};
//#endregion
//#region src/plugin/utils/is-on-intrinsic-html-element.ts
const isJsxAttributeOnIntrinsicHtmlElement = (attribute) => {
	const openingElement = attribute.parent;
	if (!openingElement) return false;
	if (!isNodeOfType(openingElement, "JSXOpeningElement")) return false;
	const elementName = openingElement.name;
	if (!isNodeOfType(elementName, "JSXIdentifier")) return false;
	const firstCharacterCode = elementName.name.charCodeAt(0);
	return firstCharacterCode >= 97 && firstCharacterCode <= 122;
};
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-jsx-as-prop.ts
const MESSAGE$39 = "This child redraws every render because the prop gets brand new JSX each time.";
const KNOWN_SLOT_PROP_NAMES = new Set([
	"icon",
	"Icon",
	"iconLeft",
	"iconRight",
	"leftIcon",
	"rightIcon",
	"startIcon",
	"endIcon",
	"prefixIcon",
	"suffixIcon",
	"iconBefore",
	"iconAfter",
	"prefix",
	"suffix",
	"before",
	"after",
	"header",
	"footer",
	"title",
	"subtitle",
	"description",
	"caption",
	"label",
	"labelExtra",
	"tooltip",
	"trigger",
	"triggerContent",
	"content",
	"body",
	"action",
	"actions",
	"controls",
	"placeholder",
	"endAdornment",
	"startAdornment",
	"leftSection",
	"rightSection",
	"addonBefore",
	"addonAfter",
	"selectButton",
	"badge",
	"message",
	"info",
	"infoMessage",
	"help",
	"helpText",
	"helpTooltip",
	"avatar",
	"preview",
	"adornment",
	"callToAction",
	"extraControls",
	"contextualText",
	"topHeading",
	"topContent",
	"bottomContent",
	"leftContent",
	"rightContent",
	"value",
	"currentValue",
	"form",
	"text",
	"count",
	"modal",
	"rightOptions",
	"leftOptions",
	"titleHelper",
	"inputDisplay",
	"outputDisplay",
	"animatedSvg",
	"Status",
	"additionalEmptyState",
	"left",
	"right",
	"top",
	"bottom",
	"start",
	"end",
	"aside",
	"details",
	"extra",
	"overlay",
	"emptyState",
	"element",
	"fallback",
	"fallbackRender",
	"FallbackComponent",
	"ErrorFallback",
	"loadingFallback",
	"loader",
	"errorElement",
	"render",
	"renderItem",
	"renderRow",
	"renderCell",
	"renderEmpty",
	"renderError",
	"renderLoading",
	"renderHeader",
	"renderFooter",
	"renderItemActions",
	"renderName",
	"renderContent",
	"renderTrigger",
	"renderOption",
	"button",
	"primaryButton",
	"secondaryButton",
	"tertiaryButton",
	"leftButton",
	"rightButton",
	"submitButton",
	"cancelButton",
	"closeButton",
	"actionButton",
	"ctaButton",
	"menuButton",
	"iconButton",
	"dialog",
	"drawer",
	"popover",
	"sheet",
	"menu",
	"submenu",
	"dropdown",
	"dropdownContent",
	"dropdownComponents",
	"toolbar",
	"toolbarContent",
	"navigation",
	"breadcrumbs",
	"sidebar",
	"topBar",
	"bottomBar",
	"container",
	"wrapper",
	"main",
	"section",
	"panel",
	"card",
	"tile",
	"row",
	"column",
	"cell",
	"item",
	"items",
	"list",
	"table",
	"tableHeader",
	"tableFooter",
	"input",
	"inputElement",
	"select",
	"checkbox",
	"radio",
	"switch",
	"field",
	"fieldset",
	"legend",
	"control",
	"controlPanel",
	"image",
	"img",
	"thumbnail",
	"logo",
	"media",
	"cover",
	"banner",
	"hero"
]);
const SLOT_PROP_SUFFIXES = [
	"Button",
	"Buttons",
	"Icon",
	"Icons",
	"Component",
	"Components",
	"Element",
	"Elements",
	"Slot",
	"Slots",
	"Content",
	"Contents",
	"Renderer",
	"Trigger",
	"Header",
	"Footer",
	"Badge",
	"Label",
	"Tooltip",
	"Indicator",
	"Adornment",
	"Section",
	"Panel",
	"Overlay",
	"Shape",
	"Override",
	"Overrides",
	"Items",
	"Item",
	"Action",
	"Actions",
	"Controls",
	"Message",
	"Heading",
	"Details",
	"Preview",
	"Info"
];
const isSlotPropName = (propName) => {
	if (KNOWN_SLOT_PROP_NAMES.has(propName)) return true;
	for (const suffix of SLOT_PROP_SUFFIXES) if (propName.length > suffix.length && propName.endsWith(suffix)) return true;
	return false;
};
const isJsxProducingExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "JSXElement") || isNodeOfType(stripped, "JSXFragment")) return true;
	if (isNodeOfType(stripped, "LogicalExpression")) return isJsxProducingExpression(stripped.left) || isJsxProducingExpression(stripped.right);
	if (isNodeOfType(stripped, "ConditionalExpression")) return isJsxProducingExpression(stripped.consequent) || isJsxProducingExpression(stripped.alternate);
	return false;
};
const followsRenderLocalJsxBinding = (expression, jsxAttribute) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "Identifier")) return false;
	const binding = findVariableInitializer(stripped, stripped.name);
	if (!binding || !binding.initializer) return false;
	let walker = jsxAttribute;
	while (walker) {
		if (walker === binding.scopeOwner) {
			if (binding.scopeOwner.type === "Program") return false;
			break;
		}
		walker = walker.parent ?? null;
	}
	return isJsxProducingExpression(binding.initializer);
};
const jsxNoJsxAsProp = defineRule({
	id: "jsx-no-jsx-as-prop",
	title: "JSX element passed as a prop",
	tags: ["react-jsx-only"],
	severity: "warn",
	disabledBy: ["react-compiler"],
	recommendation: "Move the JSX outside the component, or wrap it in `useMemo`.",
	category: "Performance",
	create: (context) => {
		const isTestlikeFile = isTestlikeFilename(context.filename);
		let memoRegistry = null;
		return {
			Program(node) {
				memoRegistry = buildSameFileMemoRegistry(node);
			},
			JSXAttribute(node) {
				if (isTestlikeFile) return;
				if (isJsxAttributeOnIntrinsicHtmlElement(node)) return;
				const parentJsxOpening = node.parent;
				const openingName = parentJsxOpening && isNodeOfType(parentJsxOpening, "JSXOpeningElement") ? parentJsxOpening.name : null;
				if (memoStatusForJsxOpeningName(memoRegistry, openingName) === "not-memoised") return;
				if (isNodeOfType(node.name, "JSXIdentifier") && isSlotPropName(node.name.name)) return;
				if (!isInsideFunctionScope(node)) return;
				const value = node.value;
				if (!value || !isNodeOfType(value, "JSXExpressionContainer")) return;
				const expression = value.expression;
				if (!expression || expression.type === "JSXEmptyExpression") return;
				const expressionNode = expression;
				if (!isJsxProducingExpression(expressionNode) && !followsRenderLocalJsxBinding(expressionNode, node)) return;
				context.report({
					node,
					message: MESSAGE$39
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-new-array-as-prop-tables.ts
const DATA_ARRAY_PROP_NAMES = new Set([
	"data",
	"items",
	"options",
	"entries",
	"list",
	"dataset",
	"elements",
	"values",
	"tabs",
	"columns",
	"rows",
	"pages",
	"categories",
	"tags",
	"keywords",
	"files",
	"blocks",
	"entities",
	"shapes",
	"events",
	"messages",
	"users",
	"series",
	"datasets",
	"children",
	"subRows",
	"nodes",
	"edges",
	"size",
	"ticks",
	"yAxis",
	"xAxis",
	"actions",
	"commands",
	"customCommandPaletteItems",
	"renderingShapes",
	"calendarEvents",
	"bars",
	"trails",
	"lines",
	"areas",
	"marks",
	"points",
	"labels",
	"filters",
	"selectedValues",
	"resources",
	"propertyFilters",
	"dayTimes",
	"links",
	"panels",
	"shortcuts",
	"hotkeys",
	"keybind",
	"keybinds",
	"modifiers",
	"dataSource",
	"suggestions",
	"people",
	"tasks",
	"colors",
	"nouns",
	"verbs",
	"iconButtons",
	"breadcrumbs",
	"fallbackPlacements",
	"value",
	"currentValue",
	"listParts",
	"objectNameSingulars",
	"tagsAvailable",
	"properties",
	"middleware",
	"middlewares",
	"variants",
	"surveys",
	"choices",
	"layers",
	"models",
	"roles",
	"metrics",
	"teams",
	"additionalActions",
	"additionalRefs",
	"addonFeatures",
	"operatorAllowlist",
	"goalLines",
	"selectedProperties",
	"panelActions",
	"operandsForFilterType",
	"dataWarehousePopoverFields",
	"avatars",
	"participants",
	"members",
	"accounts",
	"workspaces",
	"projects",
	"folders",
	"notes",
	"comments",
	"notifications",
	"posts",
	"threads",
	"authors",
	"recipients",
	"subscribers",
	"selections",
	"versions",
	"branches",
	"commits",
	"releases",
	"builds",
	"deployments",
	"jobs",
	"stages",
	"phases",
	"milestones",
	"cards",
	"tiles",
	"slides",
	"routes",
	"permissions",
	"capabilities",
	"settings",
	"attributes",
	"aspects",
	"stats",
	"statistics",
	"insights",
	"findings",
	"issues",
	"tickets",
	"bugs",
	"defects",
	"annotations",
	"markers",
	"pins",
	"stickers",
	"tools",
	"plugins",
	"extensions",
	"modules",
	"services",
	"providers",
	"adapters",
	"helpers",
	"validators",
	"transformers",
	"formatters",
	"serializers",
	"parsers",
	"vehicles",
	"devices"
]);
const DATA_ARRAY_PROP_SUFFIXES = [
	"Items",
	"Options",
	"Tabs",
	"Columns",
	"Rows",
	"List",
	"Series",
	"Categories",
	"Events",
	"Entries",
	"Elements",
	"Shapes",
	"Children",
	"Nodes",
	"Edges",
	"Data",
	"Collection",
	"Collections",
	"Models",
	"Records",
	"Filters",
	"Values",
	"Times",
	"Resources",
	"Types",
	"Ids",
	"IdArray",
	"IDs",
	"Names",
	"Tags",
	"Keys",
	"Labels",
	"Groups",
	"Buttons",
	"Icons",
	"Links",
	"Steps",
	"Stages",
	"Sources",
	"Targets",
	"Suggestions",
	"Operations",
	"Contexts",
	"Placements",
	"Breadcrumbs",
	"Hotkeys",
	"Shortcuts",
	"Panels",
	"Actions",
	"Activities",
	"Avatars",
	"Participants",
	"Members",
	"Users",
	"Accounts",
	"Workspaces",
	"Projects",
	"Files",
	"Folders",
	"Notes",
	"Comments",
	"Messages",
	"Notifications",
	"Posts",
	"Threads",
	"Authors",
	"Recipients",
	"Subscribers",
	"Choices",
	"Selections",
	"Variants",
	"Versions",
	"Branches",
	"Commits",
	"Releases",
	"Builds",
	"Deployments",
	"Jobs",
	"Tasks",
	"Phases",
	"Milestones",
	"Cards",
	"Tiles",
	"Slides",
	"Pages",
	"Routes",
	"Roles",
	"Permissions",
	"Capabilities",
	"Settings",
	"Properties",
	"Attributes",
	"Aspects",
	"Metrics",
	"Stats",
	"Statistics",
	"Insights",
	"Findings",
	"Issues",
	"Tickets",
	"Bugs",
	"Defects",
	"Annotations",
	"Markers",
	"Pins",
	"Stickers",
	"Tools",
	"Plugins",
	"Extensions",
	"Modules",
	"Services",
	"Providers",
	"Adapters",
	"Helpers",
	"Validators",
	"Transformers",
	"Formatters",
	"Serializers",
	"Parsers",
	"Vehicles",
	"Devices"
];
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-new-array-as-prop.ts
const MESSAGE$38 = "This child redraws every render because the prop gets a brand new array each time.";
const isDataArrayPropName = (propName) => {
	if (DATA_ARRAY_PROP_NAMES.has(propName)) return true;
	for (const suffix of DATA_ARRAY_PROP_SUFFIXES) if (propName.length > suffix.length && propName.endsWith(suffix)) return true;
	return false;
};
const ARRAY_CONSTRUCTOR_NAMES = new Set(["Array"]);
const SINGLE_ARG_ARRAY_METHODS = new Set(["map", "filter"]);
const ANY_ARG_ARRAY_METHODS = new Set(["concat"]);
const isEmptyArrayLiteralExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	return isNodeOfType(stripped, "ArrayExpression") && (stripped.elements ?? []).length === 0;
};
const isArrayProducingExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "ArrayExpression")) return true;
	if (isNodeOfType(stripped, "NewExpression")) return isNodeOfType(stripped.callee, "Identifier") && ARRAY_CONSTRUCTOR_NAMES.has(stripped.callee.name);
	if (isNodeOfType(stripped, "CallExpression")) {
		if (isNodeOfType(stripped.callee, "Identifier") && ARRAY_CONSTRUCTOR_NAMES.has(stripped.callee.name)) return true;
		if (isNodeOfType(stripped.callee, "MemberExpression") && isNodeOfType(stripped.callee.property, "Identifier")) {
			const methodName = stripped.callee.property.name;
			if (SINGLE_ARG_ARRAY_METHODS.has(methodName) && stripped.arguments.length === 1) return true;
			if (ANY_ARG_ARRAY_METHODS.has(methodName)) return true;
		}
		return false;
	}
	if (isNodeOfType(stripped, "LogicalExpression")) {
		if (stripped.operator === "??" || stripped.operator === "||") {
			const leftIsEmptyFallback = isEmptyArrayLiteralExpression(stripped.left);
			const rightIsEmptyFallback = isEmptyArrayLiteralExpression(stripped.right);
			if (leftIsEmptyFallback) return isArrayProducingExpression(stripped.right);
			if (rightIsEmptyFallback) return isArrayProducingExpression(stripped.left);
		}
		return isArrayProducingExpression(stripped.left) || isArrayProducingExpression(stripped.right);
	}
	if (isNodeOfType(stripped, "ConditionalExpression")) return isArrayProducingExpression(stripped.consequent) || isArrayProducingExpression(stripped.alternate);
	return false;
};
const followsRenderLocalArrayBinding = (expression, jsxAttribute) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "Identifier")) return false;
	const binding = findVariableInitializer(stripped, stripped.name);
	if (!binding || !binding.initializer) return false;
	let walker = jsxAttribute;
	while (walker) {
		if (walker === binding.scopeOwner) {
			if (binding.scopeOwner.type === "Program") return false;
			break;
		}
		walker = walker.parent ?? null;
	}
	return isArrayProducingExpression(binding.initializer);
};
const jsxNoNewArrayAsProp = defineRule({
	id: "jsx-no-new-array-as-prop",
	title: "New array passed as a prop",
	tags: ["react-jsx-only"],
	severity: "warn",
	disabledBy: ["react-compiler"],
	recommendation: "Wrap the array in `useMemo`, or move it outside the component.",
	category: "Performance",
	create: (context) => {
		const isTestlikeFile = isTestlikeFilename(context.filename);
		let memoRegistry = null;
		return {
			Program(node) {
				memoRegistry = buildSameFileMemoRegistry(node);
			},
			JSXAttribute(node) {
				if (isTestlikeFile) return;
				if (isJsxAttributeOnIntrinsicHtmlElement(node)) return;
				const parentJsxOpening = node.parent;
				const openingName = parentJsxOpening && isNodeOfType(parentJsxOpening, "JSXOpeningElement") ? parentJsxOpening.name : null;
				if (memoStatusForJsxOpeningName(memoRegistry, openingName) !== "memoised") return;
				if (isNodeOfType(node.name, "JSXIdentifier") && isDataArrayPropName(node.name.name)) return;
				if (!isInsideFunctionScope(node)) return;
				const value = node.value;
				if (!value || !isNodeOfType(value, "JSXExpressionContainer")) return;
				const expression = value.expression;
				if (!expression || expression.type === "JSXEmptyExpression") return;
				const expressionNode = expression;
				if (!isArrayProducingExpression(expressionNode) && !followsRenderLocalArrayBinding(expressionNode, node)) return;
				context.report({
					node,
					message: MESSAGE$38
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-new-function-as-prop-tables.ts
/**
* Handler / render-prop names that conventionally fire at most once
* per component lifecycle (mount / unmount / ready / error / load /
* destroy / completion / open / close) OR are render-prop slots
* called per-render but only when the slot is mounted (one-shot
* fallbacks, render-as-function patterns, custom UI hooks).
*
* For these, a new function reference per render has zero measurable
* perf impact — the handler isn't called in a hot interaction path,
* and even if the surrounding component is memoized and re-renders,
* the handler still fires the same number of times.
*/
const ONE_SHOT_LIFECYCLE_HANDLER_NAMES = new Set([
	"onMount",
	"onUnmount",
	"onReady",
	"onInit",
	"onLoad",
	"onDestroy",
	"onBeforeMount",
	"onAfterMount",
	"onBeforeUnmount",
	"onAfterUnmount",
	"onError",
	"onComplete",
	"onCompleted",
	"onFinish",
	"onFinished",
	"onSuccess",
	"onAbort",
	"onOpen",
	"onClose",
	"onDismiss",
	"onCancel",
	"onConfirm",
	"onSave",
	"onSubmit",
	"onCommit",
	"onApply",
	"onRemove",
	"onDelete",
	"onDuplicate",
	"onReset",
	"onRetry",
	"onRefresh",
	"onAdd",
	"onCreate",
	"onUpdate",
	"onConfirmClick",
	"onAcceptClick",
	"onCancelClick",
	"onSaveClick",
	"onClickOutside",
	"onPressEnter",
	"onEnter",
	"onEscape",
	"onLeave",
	"onDragStart",
	"onDragEnd",
	"onDrop",
	"onSort",
	"fallback",
	"fallbackRender",
	"render",
	"renderItem",
	"renderRow",
	"renderCell",
	"renderEmpty",
	"renderError",
	"renderLoading",
	"renderHeader",
	"renderFooter",
	"renderName",
	"renderContent",
	"renderTrigger",
	"renderOption",
	"renderItemActions",
	"children",
	"useCustom",
	"Icon",
	"Trigger",
	"Header",
	"Footer",
	"Label",
	"Content",
	"Adornment",
	"Indicator",
	"Tooltip",
	"Badge",
	"Panel",
	"Overlay",
	"Section",
	"Button",
	"Action",
	"onValueChange",
	"onCheckedChange",
	"onOpenChange",
	"onSelectionChange",
	"onPressedChange",
	"onToggleChange",
	"onSearch",
	"onSearchChange",
	"onClear",
	"onCopy",
	"onPaste",
	"onPick",
	"onActiveChange",
	"onExpandedChange",
	"onSortChange",
	"onFilterChange",
	"onSelectChange",
	"onSelect",
	"onToggle",
	"onTab",
	"onShiftTab",
	"onBack",
	"onForward",
	"onPrev",
	"onNext",
	"onSkip",
	"onContinue",
	"onPressCmdEnter",
	"onPressCmdK",
	"onCloseRequest",
	"onCloseRequested",
	"onRowClick",
	"onCellClick",
	"onHeaderClick",
	"onToggleExpand",
	"onToggleCollapse",
	"onVisibilityChange",
	"onVariableSelect",
	"onSelectColor",
	"action",
	"onEdit",
	"onView",
	"onApprove",
	"onReject",
	"onArchive",
	"onUnarchive",
	"onPin",
	"onUnpin",
	"onShare",
	"onDownload",
	"onUpload",
	"onPrint",
	"onExport",
	"onImport",
	"onMove",
	"onRename",
	"rowKey",
	"onRow",
	"onCell",
	"onHeader",
	"onHeaderRow",
	"onHeaderCell",
	"onPageChange",
	"onTabChange",
	"onNameChange",
	"onDescriptionChange",
	"onInputChange",
	"onLabelChange",
	"onValueCommit"
]);
/**
* Render-prop / slot / customization suffix conventions — `render*`,
* `*Render`, `*Renderer`, `*Slot`, `*Component`, `*Element`, plus
* PascalCase-suffix slot props (`actionButton`, `closeIcon`, etc.).
*/
const ONE_SHOT_HANDLER_SUFFIXES = [
	"Render",
	"Renderer",
	"Slot",
	"Component",
	"Element",
	"Icon",
	"Trigger",
	"Header",
	"Footer",
	"Label",
	"Content",
	"Adornment",
	"Indicator",
	"Tooltip",
	"Badge",
	"Panel",
	"Overlay",
	"Section",
	"Button",
	"Action",
	"Override",
	"Fallback"
];
/**
* `get*`, `format*`, `parse*`, `validate*`, `is*`, `should*`,
* `match*`, `select*`, `to*` — pure-ish accessor / predicate /
* formatter function props called on demand, not on every render.
* New identity is OK.
*/
const ACCESSOR_PREDICATE_PREFIXES = [
	"get",
	"format",
	"parse",
	"validate",
	"is",
	"should",
	"match",
	"select",
	"filter",
	"compare"
];
/**
* Receivers whose method calls don't typically pass derived state —
* navigation, routing, telemetry, dialog management. Used as a "fully
* stable" gate: any args to these calls are accepted as stable even
* if they're themselves call expressions, because the outer wrapper
* is doing a fire-and-forget side effect.
*/
const SAFE_RECEIVER_NAMES = new Set([
	"router",
	"navigate",
	"navigation",
	"history",
	"console",
	"window",
	"document",
	"location",
	"localStorage",
	"sessionStorage",
	"analytics",
	"telemetry",
	"logger",
	"log",
	"posthog",
	"Sentry",
	"Math",
	"Number",
	"String",
	"Boolean",
	"Array",
	"Object",
	"JSON",
	"Date",
	"Promise",
	"Map",
	"Set",
	"Symbol"
]);
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-new-function-as-prop.ts
const MESSAGE$37 = "This child redraws every render because the prop gets a brand new function each time.";
const isAccessorPredicateName = (propName) => {
	for (const prefix of ACCESSOR_PREDICATE_PREFIXES) {
		if (propName.length <= prefix.length) continue;
		if (!propName.startsWith(prefix)) continue;
		const nextChar = propName.charCodeAt(prefix.length);
		if (nextChar >= 65 && nextChar <= 90) return true;
	}
	return false;
};
const isOneShotHandlerName = (propName) => {
	if (ONE_SHOT_LIFECYCLE_HANDLER_NAMES.has(propName)) return true;
	if (propName.startsWith("render") && propName.length > 6) {
		const fourthCharCode = propName.charCodeAt(6);
		if (fourthCharCode >= 65 && fourthCharCode <= 90) return true;
	}
	if (isAccessorPredicateName(propName)) return true;
	for (const suffix of ONE_SHOT_HANDLER_SUFFIXES) if (propName.length > suffix.length && propName.endsWith(suffix)) return true;
	return false;
};
const isFunctionProducingExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "ArrowFunctionExpression") || isNodeOfType(stripped, "FunctionExpression") || isNodeOfType(stripped, "FunctionDeclaration")) return true;
	if (isNodeOfType(stripped, "NewExpression")) return isNodeOfType(stripped.callee, "Identifier") && stripped.callee.name === "Function";
	if (isNodeOfType(stripped, "CallExpression")) {
		if (isNodeOfType(stripped.callee, "Identifier") && stripped.callee.name === "Function") return true;
		if (isNodeOfType(stripped.callee, "MemberExpression") && isNodeOfType(stripped.callee.property, "Identifier") && stripped.callee.property.name === "bind") return true;
		return false;
	}
	if (isNodeOfType(stripped, "LogicalExpression")) return isFunctionProducingExpression(stripped.left) || isFunctionProducingExpression(stripped.right);
	if (isNodeOfType(stripped, "ConditionalExpression")) return isFunctionProducingExpression(stripped.consequent) || isFunctionProducingExpression(stripped.alternate);
	return false;
};
const followsRenderLocalFunctionBinding = (expression, jsxAttribute) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "Identifier")) return false;
	const binding = findVariableInitializer(stripped, stripped.name);
	if (!binding || !binding.initializer) return false;
	let walker = jsxAttribute;
	while (walker) {
		if (walker === binding.scopeOwner) {
			if (binding.scopeOwner.type === "Program") return false;
			break;
		}
		walker = walker.parent ?? null;
	}
	if (!isFunctionProducingExpression(binding.initializer)) return false;
	if (isParameterBindingWrapper(binding.initializer)) return false;
	const init = binding.initializer;
	if (isNodeOfType(init, "CallExpression")) {
		const callee = init.callee;
		if (isNodeOfType(callee, "Identifier") && callee.name.startsWith("use")) return false;
		if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && callee.property.name.startsWith("use")) return false;
	}
	return true;
};
const isStableArgumentValue = (node) => {
	if (isNodeOfType(node, "Literal")) return true;
	if (isNodeOfType(node, "TemplateLiteral")) return (node.expressions ?? []).every((expression) => isStableArgumentValue(expression));
	if (isNodeOfType(node, "Identifier")) return true;
	if (isNodeOfType(node, "MemberExpression")) return true;
	if (isNodeOfType(node, "UnaryExpression")) return isStableArgumentValue(node.argument);
	if (isNodeOfType(node, "ChainExpression")) return isStableArgumentValue(node.expression);
	if (node.type === "TSAsExpression" || node.type === "TSTypeAssertion" || node.type === "TSNonNullExpression" || node.type === "TSSatisfiesExpression") return isStableArgumentValue(node.expression);
	if (isNodeOfType(node, "ConditionalExpression")) return isStableArgumentValue(node.test) && isStableArgumentValue(node.consequent) && isStableArgumentValue(node.alternate);
	if (isNodeOfType(node, "LogicalExpression") && (node.operator === "??" || node.operator === "||" || node.operator === "&&")) return isStableArgumentValue(node.left) && isStableArgumentValue(node.right);
	if (isNodeOfType(node, "BinaryExpression") && isStableArgumentValue(node.left) && isStableArgumentValue(node.right)) return true;
	if (isNodeOfType(node, "ObjectExpression")) {
		for (const property of node.properties ?? []) {
			if (isNodeOfType(property, "SpreadElement")) {
				if (!isStableArgumentValue(property.argument)) return false;
				continue;
			}
			if (!isNodeOfType(property, "Property")) return false;
			if (property.shorthand) continue;
			if (!isStableArgumentValue(property.value)) return false;
		}
		return true;
	}
	if (isNodeOfType(node, "ArrayExpression")) {
		for (const element of node.elements ?? []) {
			if (!element) continue;
			if (isNodeOfType(element, "SpreadElement")) {
				if (!isStableArgumentValue(element.argument)) return false;
				continue;
			}
			if (!isStableArgumentValue(element)) return false;
		}
		return true;
	}
	if (isNodeOfType(node, "CallExpression")) return isStableCallExpression(node);
	if (node.type === "TaggedTemplateExpression") return true;
	return false;
};
const calleeReceiverName = (callee) => {
	let cursor = callee;
	while (isNodeOfType(cursor, "MemberExpression")) cursor = cursor.object;
	return isNodeOfType(cursor, "Identifier") ? cursor.name : null;
};
const isStableCallExpression = (node) => {
	let inner = node;
	if (isNodeOfType(inner, "ChainExpression")) inner = inner.expression;
	if (!isNodeOfType(inner, "CallExpression")) return false;
	const callee = inner.callee;
	if (!isNodeOfType(callee, "Identifier") && !isNodeOfType(callee, "MemberExpression")) return false;
	const receiverName = calleeReceiverName(callee);
	if (receiverName && SAFE_RECEIVER_NAMES.has(receiverName)) return true;
	for (const argument of inner.arguments ?? []) if (!isStableArgumentValue(argument)) return false;
	return true;
};
const isLightweightBodyExpression = (body) => {
	if (isStableCallExpression(body)) return true;
	if (isNodeOfType(body, "ChainExpression")) return isLightweightBodyExpression(body.expression);
	if (isNodeOfType(body, "LogicalExpression") && (body.operator === "&&" || body.operator === "||" || body.operator === "??")) {
		const leftStable = isStableArgumentValue(body.left) || isLightweightBodyExpression(body.left);
		const rightStable = isStableArgumentValue(body.right) || isLightweightBodyExpression(body.right);
		if (!leftStable || !rightStable) return false;
		const leftIsCall = isNodeOfType(body.left, "CallExpression") || isNodeOfType(body.left, "ChainExpression") && isNodeOfType(body.left.expression, "CallExpression");
		const rightIsCall = isNodeOfType(body.right, "CallExpression") || isNodeOfType(body.right, "ChainExpression") && isNodeOfType(body.right.expression, "CallExpression");
		return leftIsCall || rightIsCall;
	}
	if (isNodeOfType(body, "ConditionalExpression")) return isLightweightBodyExpression(body.consequent) && isLightweightBodyExpression(body.alternate);
	if (isNodeOfType(body, "UnaryExpression") && (body.operator === "void" || body.operator === "!")) return isLightweightBodyExpression(body.argument);
	if (body.type === "AwaitExpression") return isLightweightBodyExpression(body.argument);
	if (body.type === "TSAsExpression" || body.type === "TSTypeAssertion" || body.type === "TSNonNullExpression" || body.type === "TSSatisfiesExpression") return isLightweightBodyExpression(body.expression);
	return false;
};
const MAX_STABLE_STATEMENTS_IN_BLOCK = 8;
const isStableStatement = (statement) => {
	if (isNodeOfType(statement, "ExpressionStatement")) return isLightweightBodyExpression(statement.expression);
	if (isNodeOfType(statement, "ReturnStatement")) {
		if (!statement.argument) return true;
		return isLightweightBodyExpression(statement.argument);
	}
	if (isNodeOfType(statement, "VariableDeclaration")) {
		for (const declarator of statement.declarations ?? []) {
			if (!isNodeOfType(declarator, "VariableDeclarator")) return false;
			if (!declarator.init) continue;
			if (!isStableArgumentValue(declarator.init)) return false;
		}
		return true;
	}
	if (isNodeOfType(statement, "IfStatement")) {
		if (!isStableArgumentValue(statement.test)) return false;
		if (!isStableStatement(statement.consequent)) return false;
		if (statement.alternate && !isStableStatement(statement.alternate)) return false;
		return true;
	}
	if (isNodeOfType(statement, "BlockStatement")) return isStableStatementBlock(statement.body ?? []);
	return false;
};
const isStableStatementBlock = (statements) => {
	if (statements.length === 0) return true;
	if (statements.length > MAX_STABLE_STATEMENTS_IN_BLOCK) return false;
	for (const statement of statements) if (!isStableStatement(statement)) return false;
	return true;
};
const isNullishOrStableWrapper = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "Literal")) return stripped.value === null;
	if (isNodeOfType(stripped, "Identifier")) return stripped.name === "undefined";
	if (isNodeOfType(stripped, "ArrowFunctionExpression")) return isParameterBindingWrapper(stripped);
	if (isNodeOfType(stripped, "ConditionalExpression")) return isNullishOrStableWrapper(stripped.consequent) && isNullishOrStableWrapper(stripped.alternate);
	if (isNodeOfType(stripped, "LogicalExpression") && (stripped.operator === "??" || stripped.operator === "||" || stripped.operator === "&&")) return isNullishOrStableWrapper(stripped.left) && isNullishOrStableWrapper(stripped.right);
	return false;
};
const isParameterBindingWrapper = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "ConditionalExpression")) return isNullishOrStableWrapper(stripped.consequent) && isNullishOrStableWrapper(stripped.alternate) && (isNodeOfType(stripped.consequent, "ArrowFunctionExpression") || isNodeOfType(stripped.alternate, "ArrowFunctionExpression"));
	if (!isNodeOfType(stripped, "ArrowFunctionExpression")) return false;
	const body = stripped.body;
	if (!isNodeOfType(body, "BlockStatement")) return isLightweightBodyExpression(body);
	return isStableStatementBlock(body.body ?? []);
};
const jsxNoNewFunctionAsProp = defineRule({
	id: "jsx-no-new-function-as-prop",
	title: "New function passed as a prop",
	tags: ["react-jsx-only"],
	severity: "warn",
	disabledBy: ["react-compiler"],
	recommendation: "Wrap the callback in `useCallback`, or move it outside the component.",
	category: "Performance",
	create: (context) => {
		const isTestlikeFile = isTestlikeFilename(context.filename);
		let memoRegistry = null;
		return {
			Program(node) {
				memoRegistry = buildSameFileMemoRegistry(node);
			},
			JSXAttribute(node) {
				if (isTestlikeFile) return;
				if (isJsxAttributeOnIntrinsicHtmlElement(node)) return;
				const parentJsxOpening = node.parent;
				const openingName = parentJsxOpening && isNodeOfType(parentJsxOpening, "JSXOpeningElement") ? parentJsxOpening.name : null;
				if (memoStatusForJsxOpeningName(memoRegistry, openingName) !== "memoised") return;
				if (isNodeOfType(node.name, "JSXIdentifier") && isOneShotHandlerName(node.name.name)) return;
				if (!isInsideFunctionScope(node)) return;
				const value = node.value;
				if (!value || !isNodeOfType(value, "JSXExpressionContainer")) return;
				const expression = value.expression;
				if (!expression || expression.type === "JSXEmptyExpression") return;
				const expressionNode = expression;
				if (isParameterBindingWrapper(expressionNode)) return;
				if (!isFunctionProducingExpression(expressionNode) && !followsRenderLocalFunctionBinding(expressionNode, node)) return;
				context.report({
					node,
					message: MESSAGE$37
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-new-object-as-prop-tables.ts
const ALWAYS_FRESH_OBJECT_PROPS = new Set(["dangerouslySetInnerHTML", "style"]);
const CONFIG_OBJECT_PROP_NAMES = new Set([
	"options",
	"config",
	"settings",
	"params",
	"input",
	"value",
	"values",
	"data",
	"metadata",
	"components",
	"customComponents",
	"slots",
	"elements",
	"classNames",
	"theme",
	"styles",
	"sx",
	"css",
	"margin",
	"padding",
	"viewport",
	"viewBox",
	"bounds",
	"extent",
	"domain",
	"range",
	"animate",
	"initial",
	"exit",
	"transition",
	"variants",
	"whileHover",
	"whileTap",
	"whileFocus",
	"whileInView",
	"drag",
	"dragConstraints",
	"UIOptions",
	"renderConfig",
	"shape",
	"shapes",
	"user",
	"users",
	"args",
	"avatar",
	"dot",
	"action",
	"expandable",
	"defaultSort",
	"resourceType",
	"truncateText",
	"formatters",
	"label",
	"context",
	"query",
	"props",
	"pagination",
	"filters",
	"person",
	"command",
	"cursor",
	"payload",
	"tooltip",
	"properties",
	"metadataSource",
	"queryParams",
	"extraQueryParams",
	"selectedOption",
	"emptyOption",
	"pinnedOption",
	"excludedProperties",
	"disabledReasons",
	"fallbackApplicationData",
	"fieldMetadataItem",
	"contextDescription",
	"emptyMessage",
	"defaultSorting",
	"defaultValue",
	"dropdown",
	"sideAction",
	"dropdownOffset",
	"collisionPadding",
	"forceBackTo",
	"resource",
	"field",
	"menu",
	"survey",
	"legend",
	"defaultFilters",
	"introOverride",
	"searchParams",
	"commandMenuContextApi",
	"nodeTypes",
	"forceParams",
	"callToActionButton",
	"a",
	"b",
	"edgeTypes",
	"axisBottom",
	"axisLeft",
	"axisRight",
	"axisTop",
	"xScale",
	"yScale",
	"xAxis",
	"yAxis",
	"xData",
	"yData",
	"activeDot",
	"defaultViewport",
	"effectiveValueRange",
	"arcLinkLabelsColor",
	"applicationInfo",
	"developerLinks",
	"permission",
	"modifier",
	"modifiers",
	"animationDurations",
	"targetRecordIdentifier",
	"web",
	"item",
	"overflow",
	"button"
]);
const CONFIG_OBJECT_PROP_SUFFIXES = [
	"Props",
	"Config",
	"Configuration",
	"Options",
	"Settings",
	"Style",
	"Styles",
	"ClassName",
	"ClassNames",
	"Theme",
	"Sort",
	"Sorting",
	"Filter",
	"Pagination",
	"Format",
	"Locale",
	"Validator",
	"Args",
	"Type",
	"Item",
	"Option",
	"Record",
	"Metadata",
	"Context",
	"Query",
	"Source",
	"Target",
	"Action",
	"Properties",
	"Property",
	"Reasons",
	"Reason",
	"Padding",
	"Margin",
	"Offset",
	"Position",
	"Placement",
	"Value",
	"Defaults",
	"Default",
	"Schema",
	"Payload",
	"Cursor",
	"Tooltip",
	"Scale",
	"Axis",
	"Range",
	"Domain",
	"Tick",
	"Bar",
	"Line",
	"Area",
	"Mark",
	"Point",
	"Dot",
	"Label",
	"Color",
	"Stroke",
	"Fill",
	"Bottom",
	"Top",
	"Left",
	"Right",
	"Layer",
	"Viewport",
	"ViewBox",
	"Bounds",
	"Date",
	"Time",
	"Period",
	"Window",
	"Interval",
	"Duration",
	"Identifier",
	"Permission",
	"Info",
	"Link",
	"Links",
	"Animation",
	"Modifier",
	"Modifiers",
	"Strategy"
];
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-new-object-as-prop.ts
const MESSAGE$36 = "This child redraws every render because the prop gets a brand new object each time.";
const isConfigObjectPropName = (propName) => {
	if (CONFIG_OBJECT_PROP_NAMES.has(propName)) return true;
	for (const suffix of CONFIG_OBJECT_PROP_SUFFIXES) if (propName.length > suffix.length && propName.endsWith(suffix)) return true;
	return false;
};
const OBJECT_CONSTRUCTOR_NAMES = new Set(["Object"]);
const OBJECT_PRODUCING_METHODS = new Set([
	"assign",
	"create",
	"fromEntries",
	"groupBy",
	"freeze",
	"seal"
]);
const isEmptyObjectLiteralExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	return isNodeOfType(stripped, "ObjectExpression") && (stripped.properties ?? []).length === 0;
};
const isObjectProducingExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "ObjectExpression")) return true;
	if (isNodeOfType(stripped, "NewExpression")) return isNodeOfType(stripped.callee, "Identifier") && OBJECT_CONSTRUCTOR_NAMES.has(stripped.callee.name);
	if (isNodeOfType(stripped, "CallExpression")) {
		if (isNodeOfType(stripped.callee, "Identifier") && OBJECT_CONSTRUCTOR_NAMES.has(stripped.callee.name)) return true;
		if (isNodeOfType(stripped.callee, "MemberExpression") && isNodeOfType(stripped.callee.object, "Identifier") && stripped.callee.object.name === "Object" && isNodeOfType(stripped.callee.property, "Identifier") && OBJECT_PRODUCING_METHODS.has(stripped.callee.property.name)) return true;
		return false;
	}
	if (isNodeOfType(stripped, "LogicalExpression")) {
		if (stripped.operator === "??" || stripped.operator === "||") {
			const leftIsEmptyFallback = isEmptyObjectLiteralExpression(stripped.left);
			const rightIsEmptyFallback = isEmptyObjectLiteralExpression(stripped.right);
			if (leftIsEmptyFallback) return isObjectProducingExpression(stripped.right);
			if (rightIsEmptyFallback) return isObjectProducingExpression(stripped.left);
		}
		return isObjectProducingExpression(stripped.left) || isObjectProducingExpression(stripped.right);
	}
	if (isNodeOfType(stripped, "ConditionalExpression")) return isObjectProducingExpression(stripped.consequent) || isObjectProducingExpression(stripped.alternate);
	return false;
};
const followsRenderLocalObjectBinding = (expression, jsxAttribute) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "Identifier")) return false;
	const binding = findVariableInitializer(stripped, stripped.name);
	if (!binding || !binding.initializer) return false;
	let walker = jsxAttribute;
	while (walker) {
		if (walker === binding.scopeOwner) {
			if (binding.scopeOwner.type === "Program") return false;
			break;
		}
		walker = walker.parent ?? null;
	}
	return isObjectProducingExpression(binding.initializer);
};
const jsxNoNewObjectAsProp = defineRule({
	id: "jsx-no-new-object-as-prop",
	title: "New object passed as a prop",
	tags: ["react-jsx-only"],
	severity: "warn",
	disabledBy: ["react-compiler"],
	recommendation: "Wrap the object in `useMemo`, or move it outside the component.",
	category: "Performance",
	create: (context) => {
		const isTestlikeFile = isTestlikeFilename(context.filename);
		let memoRegistry = null;
		return {
			Program(node) {
				memoRegistry = buildSameFileMemoRegistry(node);
			},
			JSXAttribute(node) {
				if (isTestlikeFile) return;
				if (isJsxAttributeOnIntrinsicHtmlElement(node)) return;
				const parentJsxOpening = node.parent;
				const openingName = parentJsxOpening && isNodeOfType(parentJsxOpening, "JSXOpeningElement") ? parentJsxOpening.name : null;
				if (memoStatusForJsxOpeningName(memoRegistry, openingName) !== "memoised") return;
				if (!isInsideFunctionScope(node)) return;
				if (!isNodeOfType(node.name, "JSXIdentifier")) return;
				if (ALWAYS_FRESH_OBJECT_PROPS.has(node.name.name)) return;
				if (isConfigObjectPropName(node.name.name)) return;
				const value = node.value;
				if (!value || !isNodeOfType(value, "JSXExpressionContainer")) return;
				const expression = value.expression;
				if (!expression || expression.type === "JSXEmptyExpression") return;
				const expressionNode = expression;
				if (!isObjectProducingExpression(expressionNode) && !followsRenderLocalObjectBinding(expressionNode, node)) return;
				context.report({
					node,
					message: MESSAGE$36
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-script-url.ts
const MESSAGE$35 = "A `javascript:` URL is an XSS hole that runs injected input as code.";
const JAVASCRIPT_URL_PATTERN = /j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;
const resolveSettings$29 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	if (typeof reactDoctor !== "object" || reactDoctor === null) return {};
	return reactDoctor.jsxNoScriptUrl ?? {};
};
const getElementName = (node) => {
	if (isNodeOfType(node.name, "JSXIdentifier")) return node.name.name;
	return null;
};
const isLinkPropForElement = (elementName, attributeName, options) => {
	if (elementName === "a" && attributeName === "href") return true;
	const explicit = options.components?.[elementName];
	if (explicit && explicit.includes(attributeName)) return true;
	if (options.includeFromSettings && options.linkComponents) {
		const settingsAttrs = options.linkComponents[elementName];
		if (settingsAttrs && settingsAttrs.includes(attributeName)) return true;
	}
	return false;
};
const jsxNoScriptUrl = defineRule({
	id: "jsx-no-script-url",
	title: "javascript: URL in JSX",
	severity: "error",
	recommendation: "Replace `javascript:` URLs with `onClick` or `onSubmit` handlers. React 19 blocks them anyway.",
	category: "Security",
	create: (context) => {
		const options = resolveSettings$29(context.settings);
		return { JSXOpeningElement(node) {
			const elementName = getElementName(node);
			if (!elementName) return;
			for (const attribute of node.attributes) {
				if (!isNodeOfType(attribute, "JSXAttribute")) continue;
				if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
				const attributeName = attribute.name.name;
				if (!isLinkPropForElement(elementName, attributeName, options)) continue;
				const value = attribute.value;
				if (!value || !isNodeOfType(value, "Literal") || typeof value.value !== "string") continue;
				if (JAVASCRIPT_URL_PATTERN.test(value.value)) context.report({
					node: attribute,
					message: MESSAGE$35
				});
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-target-blank.ts
const NOREFERRER_MESSAGE = "`target=\"_blank\"` without `rel=\"noreferrer\"` lets the linked page hijack your tab to a phishing site.";
const NOOPENER_MESSAGE = "`target=\"_blank\"` without `rel` lets the linked page hijack your tab to a phishing site.";
const SPREAD_MESSAGE = "A spread here can add `target=\"_blank\"`, letting the linked page hijack your tab to a phishing site.";
const resolveSettings$28 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxNoTargetBlank ?? {} : {};
	const reactSettings = typeof settings?.react === "object" && settings.react !== null ? settings.react : {};
	const linkComponents = /* @__PURE__ */ new Map();
	for (const entry of reactSettings.linkComponents ?? []) if (typeof entry === "string") linkComponents.set(entry, ["href"]);
	else if (typeof entry === "object" && entry !== null) {
		const linkAttribute = entry.linkAttribute ?? "href";
		linkComponents.set(entry.name, Array.isArray(linkAttribute) ? linkAttribute : [linkAttribute]);
	}
	const formComponents = /* @__PURE__ */ new Map();
	for (const entry of reactSettings.formComponents ?? []) if (typeof entry === "string") formComponents.set(entry, ["action"]);
	else if (typeof entry === "object" && entry !== null) {
		const formAttribute = entry.formAttribute ?? "action";
		formComponents.set(entry.name, Array.isArray(formAttribute) ? formAttribute : [formAttribute]);
	}
	return {
		enforceDynamicLinks: ruleSettings.enforceDynamicLinks ?? "always",
		warnOnSpreadAttributes: ruleSettings.warnOnSpreadAttributes ?? false,
		allowReferrer: ruleSettings.allowReferrer ?? false,
		links: ruleSettings.links ?? true,
		forms: ruleSettings.forms ?? false,
		linkComponents,
		formComponents
	};
};
const isExternalLink = (href) => href.includes("//");
const matchHrefExpression = (expression, state) => {
	if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") {
		if (isExternalLink(expression.value)) state.isExternal = true;
		return;
	}
	if (isNodeOfType(expression, "Identifier")) {
		state.isDynamic = true;
		return;
	}
	if (isNodeOfType(expression, "ConditionalExpression")) {
		matchHrefExpression(expression.consequent, state);
		matchHrefExpression(expression.alternate, state);
	}
};
const checkHref = (attributeValue, enforceDynamicLinks) => {
	const state = {
		isExternal: false,
		isDynamic: false
	};
	if (isNodeOfType(attributeValue, "Literal") && typeof attributeValue.value === "string") state.isExternal = isExternalLink(attributeValue.value);
	else if (isNodeOfType(attributeValue, "JSXExpressionContainer")) matchHrefExpression(attributeValue.expression, state);
	if (enforceDynamicLinks === "never") return !state.isExternal || state.isDynamic;
	return !(state.isExternal || state.isDynamic);
};
const checkRelValue = (text, allowReferrer) => {
	const tokens = text.split(/\s+/);
	if (allowReferrer) return tokens.includes("noopener") || tokens.includes("noreferrer");
	return tokens.some((token) => token.toLowerCase() === "noreferrer");
};
const matchRelExpression = (expression, allowReferrer) => {
	const empty = {
		combined: false,
		testName: "",
		consequent: false,
		alternate: false
	};
	if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") return {
		combined: checkRelValue(expression.value, allowReferrer),
		testName: "",
		consequent: false,
		alternate: false
	};
	if (isNodeOfType(expression, "ConditionalExpression")) {
		const consequent = matchRelExpression(expression.consequent, allowReferrer);
		const alternate = matchRelExpression(expression.alternate, allowReferrer);
		const test = expression.test;
		if (isNodeOfType(test, "Identifier")) return {
			combined: consequent.combined && alternate.combined,
			testName: test.name,
			consequent: consequent.combined,
			alternate: alternate.combined
		};
		return {
			combined: consequent.combined && alternate.combined,
			testName: "",
			consequent: consequent.combined,
			alternate: alternate.combined
		};
	}
	return empty;
};
const checkRel = (attributeValue, allowReferrer) => {
	const empty = {
		combined: false,
		testName: "",
		consequent: false,
		alternate: false
	};
	if (isNodeOfType(attributeValue, "Literal") && typeof attributeValue.value === "string") return {
		combined: checkRelValue(attributeValue.value, allowReferrer),
		testName: "",
		consequent: false,
		alternate: false
	};
	if (isNodeOfType(attributeValue, "JSXExpressionContainer")) {
		const expression = attributeValue.expression;
		if (expression.type === "JSXEmptyExpression") return empty;
		return matchRelExpression(expression, allowReferrer);
	}
	return empty;
};
const matchTargetExpression = (expression) => {
	const empty = {
		combined: false,
		testName: "",
		consequent: false,
		alternate: false
	};
	if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") return {
		combined: expression.value.toLowerCase() === "_blank",
		testName: "",
		consequent: false,
		alternate: false
	};
	if (isNodeOfType(expression, "ConditionalExpression")) {
		const consequent = matchTargetExpression(expression.consequent);
		const alternate = matchTargetExpression(expression.alternate);
		const test = expression.test;
		const combined = consequent.combined || alternate.combined;
		if (isNodeOfType(test, "Identifier")) return {
			combined,
			testName: test.name,
			consequent: consequent.combined,
			alternate: alternate.combined
		};
		return {
			combined,
			testName: "",
			consequent: consequent.combined,
			alternate: alternate.combined
		};
	}
	return empty;
};
const checkTarget = (attributeValue) => {
	if (isNodeOfType(attributeValue, "Literal") && typeof attributeValue.value === "string") return {
		combined: attributeValue.value.toLowerCase() === "_blank",
		testName: "",
		consequent: false,
		alternate: false
	};
	if (isNodeOfType(attributeValue, "JSXExpressionContainer")) {
		const expression = attributeValue.expression;
		if (expression.type === "JSXEmptyExpression") return {
			combined: false,
			testName: "",
			consequent: false,
			alternate: false
		};
		return matchTargetExpression(expression);
	}
	return {
		combined: false,
		testName: "",
		consequent: false,
		alternate: false
	};
};
const getOpeningElementName = (node) => {
	const name = node.name;
	if (isNodeOfType(name, "JSXIdentifier")) return name.name;
	return null;
};
const jsxNoTargetBlank = defineRule({
	id: "jsx-no-target-blank",
	title: "Unsafe target=_blank link",
	severity: "warn",
	recommendation: "Add `rel=\"noreferrer\"` (or `\"noopener\"`) when using `target=\"_blank\"`.",
	category: "Security",
	create: (context) => {
		const settings = resolveSettings$28(context.settings);
		const isLink = (tagName) => {
			if (!settings.links) return false;
			if (tagName === "a") return true;
			return settings.linkComponents.has(tagName);
		};
		const isForm = (tagName) => {
			if (!settings.forms) return false;
			if (tagName === "form") return true;
			return settings.formComponents.has(tagName);
		};
		return { JSXOpeningElement(node) {
			const tagName = getOpeningElementName(node);
			if (!tagName) return;
			if (!isLink(tagName) && !isForm(tagName)) return;
			const linkAttributeNames = settings.linkComponents.get(tagName) ?? ["href"];
			const formAttributeNames = settings.formComponents.get(tagName) ?? ["action"];
			let targetTuple = {
				combined: false,
				testName: "",
				consequent: false,
				alternate: false
			};
			let relTuple = {
				combined: false,
				testName: "",
				consequent: false,
				alternate: false
			};
			let isHrefValid = true;
			let hasHrefValue = false;
			let warnSpread = false;
			let targetReportNode = node.name;
			let spreadReportNode = null;
			for (const attribute of node.attributes) {
				if (isNodeOfType(attribute, "JSXSpreadAttribute")) {
					if (settings.warnOnSpreadAttributes) {
						warnSpread = true;
						spreadReportNode = attribute;
						targetTuple = {
							combined: false,
							testName: "",
							consequent: false,
							alternate: false
						};
						relTuple = {
							combined: false,
							testName: "",
							consequent: false,
							alternate: false
						};
						isHrefValid = false;
						hasHrefValue = true;
					}
					continue;
				}
				if (!isNodeOfType(attribute, "JSXAttribute")) continue;
				const attributeName = attribute.name;
				if (!isNodeOfType(attributeName, "JSXIdentifier")) continue;
				const propName = attributeName.name;
				const value = attribute.value;
				if (propName === "target") {
					if (value) {
						targetTuple = checkTarget(value);
						targetReportNode = value;
					}
				} else if (propName === "href" || propName === "action" || linkAttributeNames.includes(propName) || formAttributeNames.includes(propName)) {
					if (value) {
						hasHrefValue = true;
						isHrefValid = checkHref(value, settings.enforceDynamicLinks);
					}
				} else if (propName === "rel" && value) relTuple = checkRel(value, settings.allowReferrer);
			}
			if (warnSpread) {
				if (hasHrefValue && isHrefValid || relTuple.combined) return;
				context.report({
					node: spreadReportNode ?? node,
					message: SPREAD_MESSAGE
				});
				return;
			}
			if (!isHrefValid) {
				if (targetTuple.testName !== "" && targetTuple.testName === relTuple.testName) {
					const consequentBad = targetTuple.consequent && !relTuple.consequent;
					const alternateBad = targetTuple.alternate && !relTuple.alternate;
					if (consequentBad || alternateBad) context.report({
						node: targetReportNode,
						message: settings.allowReferrer ? NOOPENER_MESSAGE : NOREFERRER_MESSAGE
					});
					return;
				}
				if (targetTuple.combined && !relTuple.combined) context.report({
					node: targetReportNode,
					message: settings.allowReferrer ? NOOPENER_MESSAGE : NOREFERRER_MESSAGE
				});
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-undef.ts
const buildMessage$17 = (name) => `\`${name}\` crashes at runtime because it isn't defined here.`;
const KNOWN_GLOBALS = new Set([
	"globalThis",
	"window",
	"document",
	"console",
	"React",
	"self",
	"this"
]);
const getRootIdentifier = (elementName) => {
	if (isNodeOfType(elementName, "JSXIdentifier")) {
		const firstCharacter = elementName.name.charCodeAt(0);
		if (firstCharacter >= 97 && firstCharacter <= 122) return null;
		return elementName.name;
	}
	if (isNodeOfType(elementName, "JSXMemberExpression")) {
		let current = elementName;
		while (isNodeOfType(current, "JSXMemberExpression")) current = current.object;
		if (isNodeOfType(current, "JSXIdentifier")) return current.name;
	}
	return null;
};
const jsxNoUndef = defineRule({
	id: "jsx-no-undef",
	title: "Undefined JSX component",
	severity: "error",
	recommendation: "Import the component or check for typos.",
	create: (context) => ({ JSXOpeningElement(node) {
		const rootIdentifier = getRootIdentifier(node.name);
		if (!rootIdentifier) return;
		if (KNOWN_GLOBALS.has(rootIdentifier)) return;
		if (!findProgramRoot(node)) return;
		if (findVariableInitializer(node, rootIdentifier)) return;
		context.report({
			node: node.name,
			message: buildMessage$17(rootIdentifier)
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-no-useless-fragment.ts
const NEEDS_MORE_CHILDREN = "This fragment wraps a single child & does nothing.";
const CHILD_OF_HTML_ELEMENT = "This fragment does nothing inside an HTML tag that can hold the children directly.";
const resolveSettings$27 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { allowExpressions: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxNoUselessFragment ?? {} : {}).allowExpressions ?? false };
};
const isPaddingChild = (child) => {
	if (!isNodeOfType(child, "JSXText")) return false;
	return child.value.trim().length === 0 && child.value.includes("\n");
};
const stripWhitespaceOnlyText = (children) => {
	const filtered = [];
	for (const child of children) {
		if (isPaddingChild(child)) continue;
		filtered.push(child);
	}
	return filtered;
};
const hasCallExpressionChild = (children) => {
	for (const child of children) {
		if (!isNodeOfType(child, "JSXExpressionContainer")) continue;
		if (isNodeOfType(child.expression, "CallExpression")) return true;
	}
	return false;
};
const isLowercaseHtmlTag = (node) => {
	if (!isNodeOfType(node, "JSXOpeningElement")) return false;
	const elementName = node.name;
	if (!isNodeOfType(elementName, "JSXIdentifier")) return false;
	const firstCharacter = elementName.name.charCodeAt(0);
	return firstCharacter >= 97 && firstCharacter <= 122;
};
const isChildOfHtmlElement = (node) => {
	const parent = node.parent;
	if (!parent) return false;
	if (!isNodeOfType(parent, "JSXElement")) return false;
	return isLowercaseHtmlTag(parent.openingElement);
};
const isFragmentWithSingleExpression = (children) => {
	const meaningful = stripWhitespaceOnlyText(children);
	if (meaningful.length !== 1) return false;
	return isNodeOfType(meaningful[0], "JSXExpressionContainer");
};
const isFragmentWithOnlyTextAndNotJsxChild = (fragmentNode, children) => {
	if (children.length !== 1) return false;
	if (!isNodeOfType(children[0], "JSXText")) return false;
	const parent = fragmentNode.parent;
	if (!parent) return false;
	return !isNodeOfType(parent, "JSXElement") && !isNodeOfType(parent, "JSXFragment");
};
const jsxNoUselessFragment = defineRule({
	id: "jsx-no-useless-fragment",
	title: "Unnecessary React fragment",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Drop the fragment when it wraps a single child or sits directly under an HTML tag.",
	category: "Architecture",
	create: (context) => {
		const { allowExpressions } = resolveSettings$27(context.settings);
		const checkChildren = (fragmentNode, reportNode, children) => {
			const meaningful = stripWhitespaceOnlyText(children);
			if (meaningful.length >= 2) return false;
			if (allowExpressions && isFragmentWithSingleExpression(children)) return false;
			if (hasCallExpressionChild(meaningful)) return false;
			if (isFragmentWithOnlyTextAndNotJsxChild(fragmentNode, meaningful)) return false;
			context.report({
				node: reportNode,
				message: NEEDS_MORE_CHILDREN
			});
			return true;
		};
		return {
			JSXElement(node) {
				const openingElement = node.openingElement;
				if (!isJsxFragmentElement(openingElement)) return;
				if (hasJsxKeyAttribute(openingElement)) return;
				if (checkChildren(node, openingElement, node.children)) return;
				if (isChildOfHtmlElement(node)) context.report({
					node: openingElement,
					message: CHILD_OF_HTML_ELEMENT
				});
			},
			JSXFragment(node) {
				if (checkChildren(node, node.openingFragment, node.children)) return;
				if (isChildOfHtmlElement(node)) context.report({
					node: node.openingFragment,
					message: CHILD_OF_HTML_ELEMENT
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-pascal-case.ts
const buildMessage$16 = (componentName, allowAllCaps) => allowAllCaps ? `React can mistake \`${componentName}\` for an HTML tag unless it's PascalCase or SCREAMING_SNAKE_CASE.` : `React can mistake \`${componentName}\` for an HTML tag unless it's PascalCase.`;
const resolveSettings$26 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxPascalCase ?? {} : {};
	return {
		allowAllCaps: ruleSettings.allowAllCaps ?? false,
		allowNamespace: ruleSettings.allowNamespace ?? false,
		allowLeadingUnderscore: ruleSettings.allowLeadingUnderscore ?? true,
		ignore: ruleSettings.ignore ?? []
	};
};
const flattenMemberName = (node) => {
	if (isNodeOfType(node, "JSXIdentifier")) return node.name;
	if (isNodeOfType(node, "JSXMemberExpression")) {
		const objectName = flattenMemberName(node.object);
		if (!objectName) return null;
		return `${objectName}.${node.property.name}`;
	}
	return null;
};
const isAsciiUppercase = (charCode) => charCode >= 65 && charCode <= 90;
const isAsciiLowercase = (charCode) => charCode >= 97 && charCode <= 122;
const isAsciiDigit = (charCode) => charCode >= 48 && charCode <= 57;
const checkPascalCase = (name) => {
	if (name.length === 0) return false;
	const firstChar = name.charAt(0);
	if (firstChar.toUpperCase() !== firstChar || firstChar === firstChar.toLowerCase()) return false;
	let hasLowerOrDigit = false;
	for (let i = 1; i < name.length; i++) {
		const character = name.charAt(i);
		const code = name.charCodeAt(i);
		if (!/[\p{L}\p{N}]/u.test(character)) return false;
		if (isAsciiDigit(code) || character.toLowerCase() === character && character !== character.toUpperCase()) hasLowerOrDigit = true;
	}
	return hasLowerOrDigit;
};
const checkAllCaps = (name) => {
	if (name.length === 0) return false;
	for (let i = 0; i < name.length; i++) {
		const code = name.charCodeAt(i);
		if (i === 0 || i === name.length - 1) {
			if (!isAsciiUppercase(code) && !isAsciiDigit(code)) return false;
		} else if (!isAsciiUppercase(code) && !isAsciiDigit(code) && code !== 95) return false;
	}
	return true;
};
const STAR_PATTERN = /\*/;
const compiledGlobCache = /* @__PURE__ */ new Map();
const compileGlobPattern = (pattern) => {
	const cached = compiledGlobCache.get(pattern);
	if (cached) return cached;
	let result = "";
	for (let cursor = 0; cursor < pattern.length; cursor += 1) {
		const character = pattern[cursor];
		if (character === "*") result += ".*";
		else if (character === "?") result += ".";
		else if (character === "[") {
			const closingIndex = pattern.indexOf("]", cursor + 1);
			if (closingIndex === -1) {
				result += "\\[";
				continue;
			}
			const charClassBody = pattern.slice(cursor + 1, closingIndex).replaceAll(",", "");
			result += `[${charClassBody}]`;
			cursor = closingIndex;
		} else if (/[.+?^${}()|\\]/.test(character)) result += `\\${character}`;
		else result += character;
	}
	const compiled = new RegExp(`^${result}$`);
	compiledGlobCache.set(pattern, compiled);
	return compiled;
};
const matchesIgnoreGlob = (ignoreSet, ignorePatternsWithStar, candidate) => {
	if (ignoreSet.has(candidate)) return true;
	for (const pattern of ignorePatternsWithStar) if (compileGlobPattern(pattern).test(candidate)) return true;
	return false;
};
const jsxPascalCase = defineRule({
	id: "jsx-pascal-case",
	title: "Component name not PascalCase",
	severity: "warn",
	defaultEnabled: false,
	tags: ["test-noise"],
	recommendation: "Rename custom JSX components to PascalCase.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$26(context.settings);
		const ignoreSet = /* @__PURE__ */ new Set();
		const ignorePatternsWithStar = [];
		for (const pattern of settings.ignore) if (STAR_PATTERN.test(pattern)) ignorePatternsWithStar.push(pattern);
		else ignoreSet.add(pattern);
		return { JSXOpeningElement(node) {
			const elementName = node.name;
			let isNamespaced = false;
			let isMember = false;
			let fullName = null;
			if (isNodeOfType(elementName, "JSXIdentifier")) {
				if (isAsciiLowercase(elementName.name.charCodeAt(0))) return;
				fullName = elementName.name;
			} else if (isNodeOfType(elementName, "JSXNamespacedName")) {
				isNamespaced = true;
				fullName = `${elementName.namespace.name}:${elementName.name.name}`;
			} else if (isNodeOfType(elementName, "JSXMemberExpression")) {
				isMember = true;
				fullName = flattenMemberName(elementName);
			}
			if (!fullName) return;
			if (isAsciiLowercase(fullName.charCodeAt(0))) return;
			const segments = isNamespaced ? fullName.split(":") : isMember ? fullName.split(".") : [fullName];
			for (const segment of segments) {
				if (segment.length <= 1) continue;
				if (matchesIgnoreGlob(ignoreSet, ignorePatternsWithStar, segment)) continue;
				const checkName = settings.allowLeadingUnderscore && segment.startsWith("_") ? segment.slice(1) : segment;
				const isPascal = checkPascalCase(checkName);
				const isAllCaps = settings.allowAllCaps && checkAllCaps(checkName);
				if (!isPascal && !isAllCaps) {
					context.report({
						node,
						message: buildMessage$16(segment, settings.allowAllCaps)
					});
					return;
				}
				if (settings.allowNamespace) return;
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-props-no-spread-multi.ts
const buildIdentifierMessage = (name) => `The later spread of \`${name}\` silently overrides the earlier one.`;
const buildMemberMessage = (name) => `The later spread of \`${name}\` silently overrides the earlier one.`;
const flattenMemberExpressionName = (node) => {
	const stripped = stripParenExpression(node);
	if (isNodeOfType(stripped, "Identifier")) return stripped.name;
	if (isNodeOfType(stripped, "ThisExpression")) return "this";
	if (isNodeOfType(stripped, "MemberExpression")) {
		const objectName = flattenMemberExpressionName(stripped.object);
		if (!objectName) return null;
		if (isNodeOfType(stripped.property, "Identifier") && !stripped.computed) return `${objectName}.${stripped.property.name}`;
		if (isNodeOfType(stripped.property, "Literal") && typeof stripped.property.value === "string") return `${objectName}.${stripped.property.value}`;
	}
	return null;
};
const jsxPropsNoSpreadMulti = defineRule({
	id: "jsx-props-no-spread-multi",
	title: "Same prop spread multiple times",
	severity: "warn",
	recommendation: "Spread the same value at most once per element.",
	create: (context) => ({ JSXOpeningElement(node) {
		const seenNames = /* @__PURE__ */ new Map();
		const reportedNames = /* @__PURE__ */ new Set();
		for (const attribute of node.attributes) {
			if (!isNodeOfType(attribute, "JSXSpreadAttribute")) continue;
			const argument = stripParenExpression(attribute.argument);
			const isIdentifier = isNodeOfType(argument, "Identifier");
			const name = flattenMemberExpressionName(argument);
			if (!name) continue;
			if (seenNames.has(name)) {
				if (!reportedNames.has(name)) {
					context.report({
						node: attribute,
						message: isIdentifier ? buildIdentifierMessage(name) : buildMemberMessage(name)
					});
					reportedNames.add(name);
				}
			} else seenNames.set(name, attribute);
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/jsx-props-no-spreading.ts
const MESSAGE$34 = "You can't tell what props reach this element when you spread them.";
const resolveSettings$25 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.jsxPropsNoSpreading ?? {} : {};
	return {
		html: ruleSettings.html ?? "enforce",
		custom: ruleSettings.custom ?? "enforce",
		explicitSpread: ruleSettings.explicitSpread ?? "enforce",
		exceptions: ruleSettings.exceptions ?? []
	};
};
const jsxPropsNoSpreading = defineRule({
	id: "jsx-props-no-spreading",
	title: "Props spread onto element",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "List each prop explicitly so consumers can see what's being passed.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$25(context.settings);
		return { JSXOpeningElement(node) {
			const tagName = flattenJsxName(node.name);
			if (!tagName) return;
			const isCustom = isReactComponentName(tagName) || tagName.includes(".");
			const isHtml = !isCustom;
			const isException = settings.exceptions.includes(tagName);
			const ignoreHtml = settings.html === "ignore";
			const ignoreCustom = settings.custom === "ignore";
			for (const attribute of node.attributes) {
				if (!isNodeOfType(attribute, "JSXSpreadAttribute")) continue;
				const argument = stripParenExpression(attribute.argument);
				if (settings.explicitSpread === "ignore" && isNodeOfType(argument, "ObjectExpression")) {
					if (!argument.properties.some((property) => isNodeOfType(property, "SpreadElement"))) continue;
				}
				if (isHtml) {
					if (!(ignoreHtml ? isException : !isException)) continue;
				} else if (isCustom) {
					if (!(ignoreCustom ? isException : !isException)) continue;
				}
				context.report({
					node: attribute,
					message: MESSAGE$34
				});
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/a11y/label-has-associated-control.ts
const MESSAGE_NO_LABEL = "Blind users can't identify this field because screen readers find no label text, so add visible text, `aria-label`, or `aria-labelledby`.";
const MESSAGE_NO_CONTROL = "Screen reader users can't tell which input this label names because it's tied to none, so add `htmlFor` or wrap the input inside it.";
const DEFAULT_CONTROL_COMPONENTS = [
	"input",
	"meter",
	"output",
	"progress",
	"select",
	"textarea"
];
const DEFAULT_LABEL_ATTRIBUTES = [
	"alt",
	"aria-label",
	"aria-labelledby"
];
const resolveSettings$24 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.labelHasAssociatedControl ?? {} : {};
	const jsxA11y = settings?.["jsx-a11y"];
	const forAttributes = (typeof jsxA11y === "object" && jsxA11y !== null ? jsxA11y : {}).attributes?.for ?? ["htmlFor"];
	return {
		labelComponents: ["label", ...ruleSettings.labelComponents ?? []].sort(),
		labelAttributes: [...new Set([...DEFAULT_LABEL_ATTRIBUTES, ...ruleSettings.labelAttributes ?? []])].sort(),
		controlComponents: ruleSettings.controlComponents ?? [],
		assert: ruleSettings.assert ?? "either",
		depth: Math.min(ruleSettings.depth ?? 5, 25),
		forAttributes
	};
};
const isControlComponent = (tagName, controlComponents) => {
	if (DEFAULT_CONTROL_COMPONENTS.includes(tagName)) return true;
	return controlComponents.some((pattern) => compileGlob(pattern).test(tagName));
};
const searchForNestedControl = (child, currentDepth, searchContext) => {
	if (currentDepth > searchContext.depth) return false;
	if (isNodeOfType(child, "JSXExpressionContainer")) return true;
	if (isNodeOfType(child, "JSXFragment")) return child.children.some((nestedChild) => searchForNestedControl(nestedChild, currentDepth + 1, searchContext));
	if (isNodeOfType(child, "JSXElement")) {
		if (isControlComponent(getElementType(child.openingElement, searchContext.settings), searchContext.controlComponents)) return true;
		for (const nestedChild of child.children) if (searchForNestedControl(nestedChild, currentDepth + 1, searchContext)) return true;
	}
	return false;
};
const searchForAccessibleLabel = (child, currentDepth, searchContext) => {
	if (currentDepth > searchContext.depth) return false;
	if (isNodeOfType(child, "JSXExpressionContainer")) return true;
	if (isNodeOfType(child, "JSXText")) return child.value.trim().length > 0;
	if (isNodeOfType(child, "JSXFragment")) return child.children.some((nestedChild) => searchForAccessibleLabel(nestedChild, currentDepth + 1, searchContext));
	if (isNodeOfType(child, "JSXElement")) {
		const opening = child.openingElement;
		for (const attribute of opening.attributes) {
			if (isNodeOfType(attribute, "JSXSpreadAttribute")) return true;
			if (!isNodeOfType(attribute, "JSXAttribute")) continue;
			const attributeName = attribute.name;
			if (!isNodeOfType(attributeName, "JSXIdentifier")) continue;
			const propName = getJsxAttributeName(attributeName);
			if (!propName || !searchContext.labelAttributes.includes(propName)) continue;
			const attributeValue = attribute.value;
			if (!attributeValue) continue;
			if (isNodeOfType(attributeValue, "Literal") && typeof attributeValue.value === "string") {
				if (attributeValue.value.trim().length > 0) return true;
			} else return true;
		}
		if (child.children.length === 0) {
			const tagName = getElementType(opening, searchContext.settings);
			if (isReactComponentName(tagName) && !isControlComponent(tagName, searchContext.controlComponents)) return true;
		}
		for (const nestedChild of child.children) if (searchForAccessibleLabel(nestedChild, currentDepth + 1, searchContext)) return true;
	}
	return false;
};
const hasAccessibleLabel = (element, searchContext) => {
	for (const attribute of element.openingElement.attributes) {
		if (isNodeOfType(attribute, "JSXSpreadAttribute")) return true;
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		const attributeName = attribute.name;
		if (!isNodeOfType(attributeName, "JSXIdentifier")) continue;
		const propName = getJsxAttributeName(attributeName);
		if (propName && searchContext.labelAttributes.includes(propName)) return true;
	}
	for (const child of element.children) if (searchForAccessibleLabel(child, 1, searchContext)) return true;
	return false;
};
const hasNestedControl = (element, searchContext) => {
	for (const child of element.children) if (searchForNestedControl(child, 1, searchContext)) return true;
	return false;
};
const labelHasAssociatedControl = defineRule({
	id: "label-has-associated-control",
	title: "Label missing associated control",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Tie every label to a control with `htmlFor`, or by nesting the input.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$24(context.settings);
		const isTestlikeFile = isTestlikeFilename(context.filename);
		return { JSXElement(node) {
			if (isTestlikeFile) return;
			const opening = node.openingElement;
			const tagName = getElementType(opening, context.settings);
			if (!settings.labelComponents.includes(tagName)) return;
			const hasHtmlFor = settings.forAttributes.some((attributeName) => Boolean(hasJsxPropIgnoreCase(opening.attributes, attributeName)));
			const searchContext = {
				depth: settings.depth,
				labelAttributes: settings.labelAttributes,
				controlComponents: settings.controlComponents,
				settings: context.settings
			};
			const hasControl = hasNestedControl(node, searchContext);
			if (!hasAccessibleLabel(node, searchContext)) {
				context.report({
					node: opening,
					message: MESSAGE_NO_LABEL
				});
				return;
			}
			switch (settings.assert) {
				case "htmlFor":
					if (!hasHtmlFor) context.report({
						node: opening,
						message: MESSAGE_NO_CONTROL
					});
					return;
				case "nesting":
					if (!hasControl) context.report({
						node: opening,
						message: MESSAGE_NO_CONTROL
					});
					return;
				case "both":
					if (!hasHtmlFor || !hasControl) context.report({
						node: opening,
						message: MESSAGE_NO_CONTROL
					});
					return;
				default:
					if (!hasHtmlFor && !hasControl) context.report({
						node: opening,
						message: MESSAGE_NO_CONTROL
					});
					return;
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/a11y/lang.ts
const MESSAGE$33 = "Screen readers can't pick the right voice because this `lang` isn't a real language code, so use a valid one like `en` or `en-US`.";
const COMMON_LANGUAGE_PRIMARY_TAGS = new Set([
	"aa",
	"ab",
	"ae",
	"af",
	"ak",
	"am",
	"an",
	"ar",
	"as",
	"av",
	"ay",
	"az",
	"ba",
	"be",
	"bg",
	"bh",
	"bi",
	"bm",
	"bn",
	"bo",
	"br",
	"bs",
	"ca",
	"ce",
	"ch",
	"co",
	"cr",
	"cs",
	"cu",
	"cv",
	"cy",
	"da",
	"de",
	"dv",
	"dz",
	"ee",
	"el",
	"en",
	"eo",
	"es",
	"et",
	"eu",
	"fa",
	"ff",
	"fi",
	"fj",
	"fo",
	"fr",
	"fy",
	"ga",
	"gd",
	"gl",
	"gn",
	"gu",
	"gv",
	"ha",
	"he",
	"hi",
	"ho",
	"hr",
	"ht",
	"hu",
	"hy",
	"hz",
	"ia",
	"id",
	"ie",
	"ig",
	"ii",
	"ik",
	"io",
	"is",
	"it",
	"iu",
	"ja",
	"jv",
	"ka",
	"kg",
	"ki",
	"kj",
	"kk",
	"kl",
	"km",
	"kn",
	"ko",
	"kr",
	"ks",
	"ku",
	"kv",
	"kw",
	"ky",
	"la",
	"lb",
	"lg",
	"li",
	"ln",
	"lo",
	"lt",
	"lu",
	"lv",
	"mg",
	"mh",
	"mi",
	"mk",
	"ml",
	"mn",
	"mr",
	"ms",
	"mt",
	"my",
	"na",
	"nb",
	"nd",
	"ne",
	"ng",
	"nl",
	"nn",
	"no",
	"nr",
	"nv",
	"ny",
	"oc",
	"oj",
	"om",
	"or",
	"os",
	"pa",
	"pi",
	"pl",
	"ps",
	"pt",
	"qu",
	"rm",
	"rn",
	"ro",
	"ru",
	"rw",
	"sa",
	"sc",
	"sd",
	"se",
	"sg",
	"si",
	"sk",
	"sl",
	"sm",
	"sn",
	"so",
	"sq",
	"sr",
	"ss",
	"st",
	"su",
	"sv",
	"sw",
	"ta",
	"te",
	"tg",
	"th",
	"ti",
	"tk",
	"tl",
	"tn",
	"to",
	"tr",
	"ts",
	"tt",
	"tw",
	"ty",
	"ug",
	"uk",
	"ur",
	"uz",
	"ve",
	"vi",
	"vo",
	"wa",
	"wo",
	"xh",
	"yi",
	"yo",
	"za",
	"zh",
	"zu"
]);
const isValidLangTag = (value) => {
	if (value.length === 0) return false;
	const parts = value.split(/[-_]/);
	if (parts.length === 0) return false;
	const primary = parts[0].toLowerCase();
	if (!COMMON_LANGUAGE_PRIMARY_TAGS.has(primary)) return false;
	return parts.every((part) => /^[A-Za-z0-9]+$/.test(part));
};
const lang = defineRule({
	id: "lang",
	title: "Invalid lang attribute value",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Use a valid language code, like `en` or `en-US`.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		if (getElementType(node, context.settings) !== "html") return;
		const langAttr = hasJsxPropIgnoreCase(node.attributes, "lang");
		if (!langAttr) return;
		const attributeValue = langAttr.value;
		if (attributeValue && attributeValue.type === "JSXExpressionContainer") {
			const expression = attributeValue.expression;
			if (expression.type === "Identifier" && expression.name === "undefined" || expression.type === "Literal" && expression.value === null) {
				context.report({
					node: langAttr,
					message: MESSAGE$33
				});
				return;
			}
		}
		const value = getJsxPropStringValue(langAttr);
		if (value === null) return;
		if (!isValidLangTag(value)) context.report({
			node: langAttr,
			message: MESSAGE$33
		});
	} })
});
//#endregion
//#region src/plugin/rules/a11y/media-has-caption.ts
const MESSAGE$32 = "Deaf & hard-of-hearing users miss this media without captions, so add a `<track kind=\"captions\">` inside the `<audio>` or `<video>`.";
const DEFAULT_AUDIO = ["audio"];
const DEFAULT_VIDEO = ["video"];
const DEFAULT_TRACK = ["track"];
const resolveSettings$23 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.mediaHasCaption ?? {} : {};
	return {
		audio: [...DEFAULT_AUDIO, ...ruleSettings.audio ?? []],
		video: [...DEFAULT_VIDEO, ...ruleSettings.video ?? []],
		track: [...DEFAULT_TRACK, ...ruleSettings.track ?? []]
	};
};
const evaluateMuted = (attribute) => {
	if (!attribute) return false;
	const value = attribute.value;
	if (!value) return true;
	if (isNodeOfType(value, "Literal") && typeof value.value === "string") return value.value === "true";
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal") && typeof expression.value === "boolean") return expression.value;
	}
	return false;
};
const mediaHasCaption = defineRule({
	id: "media-has-caption",
	title: "Media missing captions",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Add `<track kind=\"captions\">` inside every `<audio>` / `<video>`.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$23(context.settings);
		return { JSXOpeningElement(node) {
			const tag = getElementType(node, context.settings);
			if (!(settings.audio.includes(tag) || settings.video.includes(tag))) return;
			if (evaluateMuted(hasJsxPropIgnoreCase(node.attributes, "muted")) === true) return;
			const parent = node.parent;
			if (!parent || !isNodeOfType(parent, "JSXElement")) {
				context.report({
					node: node.name,
					message: MESSAGE$32
				});
				return;
			}
			if (!parent.children.some((child) => {
				if (!isNodeOfType(child, "JSXElement")) return false;
				const opening = child.openingElement;
				const childTag = getElementType(opening, context.settings);
				if (!settings.track.includes(childTag)) return false;
				const kindAttribute = hasJsxPropIgnoreCase(opening.attributes, "kind");
				if (!kindAttribute) return false;
				const kindValue = kindAttribute.value;
				if (!kindValue || !isNodeOfType(kindValue, "Literal")) return false;
				if (typeof kindValue.value !== "string") return false;
				return kindValue.value.toLowerCase() === "captions";
			})) context.report({
				node: node.name,
				message: MESSAGE$32
			});
		} };
	}
});
//#endregion
//#region src/plugin/utils/has-jsx-prop.ts
const hasJsxProp = (attributes, targetProp) => {
	for (const attribute of attributes) {
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		if (getJsxAttributeName(attribute.name) === targetProp) return attribute;
	}
};
//#endregion
//#region src/plugin/rules/a11y/mouse-events-have-key-events.ts
const buildMissingFocusMessage = (handler) => `Keyboard users miss this \`${handler}\` because it only fires with a mouse, so add an \`onFocus\` handler too.`;
const buildMissingBlurMessage = (handler) => `Keyboard users miss this \`${handler}\` because it only fires with a mouse, so add an \`onBlur\` handler too.`;
const resolveSettings$22 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.mouseEventsHaveKeyEvents ?? {} : {};
	return {
		hoverInHandlers: ruleSettings.hoverInHandlers ?? ["onMouseOver"],
		hoverOutHandlers: ruleSettings.hoverOutHandlers ?? ["onMouseOut"]
	};
};
const isExplicitUndefinedExpression = (value) => {
	if (!isNodeOfType(value, "JSXExpressionContainer")) return false;
	const expression = value.expression;
	return isNodeOfType(expression, "Identifier") && expression.name === "undefined";
};
const mouseEventsHaveKeyEvents = defineRule({
	id: "mouse-events-have-key-events",
	title: "Mouse handler missing focus handler",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Pair mouse events with keyboard ones so keyboard users are not left out.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$22(context.settings);
		return { JSXOpeningElement(node) {
			const tag = getElementType(node, context.settings);
			if (!HTML_TAGS.has(tag)) return;
			for (const handler of settings.hoverInHandlers) {
				const handlerAttribute = hasJsxProp(node.attributes, handler);
				if (!handlerAttribute) continue;
				if (!handlerAttribute.value) continue;
				const focusAttribute = hasJsxProp(node.attributes, "onFocus");
				if (!focusAttribute) context.report({
					node: handlerAttribute,
					message: buildMissingFocusMessage(handler)
				});
				else if (focusAttribute.value && isExplicitUndefinedExpression(focusAttribute.value)) context.report({
					node: handlerAttribute,
					message: buildMissingFocusMessage(handler)
				});
				break;
			}
			for (const handler of settings.hoverOutHandlers) {
				const handlerAttribute = hasJsxProp(node.attributes, handler);
				if (!handlerAttribute) continue;
				if (!handlerAttribute.value) continue;
				const blurAttribute = hasJsxProp(node.attributes, "onBlur");
				if (!blurAttribute) context.report({
					node: handlerAttribute,
					message: buildMissingBlurMessage(handler)
				});
				else if (blurAttribute.value && isExplicitUndefinedExpression(blurAttribute.value)) context.report({
					node: handlerAttribute,
					message: buildMissingBlurMessage(handler)
				});
				break;
			}
		} };
	}
});
//#endregion
//#region src/plugin/utils/has-directive.ts
const hasDirective = (programNode, directive) => {
	if (!isNodeOfType(programNode, "Program")) return false;
	return Boolean(programNode.body?.some((statement) => isNodeOfType(statement, "ExpressionStatement") && isNodeOfType(statement.expression, "Literal") && statement.expression.value === directive));
};
//#endregion
//#region src/plugin/utils/is-uppercase-name.ts
const isUppercaseName = (name) => UPPERCASE_PATTERN.test(name);
//#endregion
//#region src/plugin/utils/is-component-assignment.ts
const isComponentAssignment = (node) => isNodeOfType(node, "VariableDeclarator") && isNodeOfType(node.id, "Identifier") && isUppercaseName(node.id.name) && Boolean(node.init) && (isNodeOfType(node.init, "ArrowFunctionExpression") || isNodeOfType(node.init, "FunctionExpression"));
//#endregion
//#region src/plugin/rules/nextjs/nextjs-async-client-component.ts
const nextjsAsyncClientComponent = defineRule({
	id: "nextjs-async-client-component",
	title: "Async client component",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "error",
	recommendation: "Fetch data in a parent Server Component and pass it as props, or use useQuery/useSWR in the client component",
	create: (context) => {
		let fileHasUseClient = false;
		return {
			Program(programNode) {
				fileHasUseClient = hasDirective(programNode, "use client");
			},
			FunctionDeclaration(node) {
				if (!fileHasUseClient || !node.async) return;
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				context.report({
					node,
					message: `Async client component "${node.id.name}" fails to render because client components can't be async.`
				});
			},
			VariableDeclarator(node) {
				if (!fileHasUseClient) return;
				if (!isComponentAssignment(node)) return;
				if (!isInlineFunctionExpression(node.init)) return;
				if (!node.init.async) return;
				if (!isNodeOfType(node.id, "Identifier")) return;
				context.report({
					node,
					message: `Async client component "${node.id.name}" fails to render because client components can't be async.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/constants/nextjs.ts
const PAGE_FILE_PATTERN = /\/page\.(tsx?|jsx?)$/;
const PAGE_OR_LAYOUT_FILE_PATTERN = /\/(page|layout)\.(tsx?|jsx?)$/;
const INTERNAL_PAGE_PATH_PATTERN = /\/(?:(?:\((?:dashboard|admin|settings|account|internal|manage|console|portal|auth|onboarding|app|ee|protected)\))|(?:dashboard|admin|settings|account|internal|manage|console|portal))\//i;
const OG_ROUTE_PATTERN = /\/og\b/i;
const PAGES_DIRECTORY_PATTERN = /\/pages\//;
const NEXTJS_NAVIGATION_FUNCTIONS = new Set([
	"redirect",
	"permanentRedirect",
	"notFound",
	"forbidden",
	"unauthorized"
]);
const GOOGLE_FONTS_PATTERN = /fonts\.googleapis\.com/;
const POLYFILL_SCRIPT_PATTERN = /polyfill\.io|polyfill\.min\.js|cdn\.polyfill/;
const APP_DIRECTORY_PATTERN = /\/app\//;
const ROUTE_HANDLER_FILE_PATTERN = /\/route\.(tsx?|jsx?)$/;
const CRON_ROUTE_PATTERN = /\/(?:cron|jobs\/cron)(?:\/|$)/i;
const MUTATING_ROUTE_SEGMENTS = new Set([
	"logout",
	"log-out",
	"signout",
	"sign-out",
	"unsubscribe",
	"delete",
	"remove",
	"revoke",
	"cancel",
	"deactivate"
]);
const ERROR_BOUNDARY_FILE_PATTERN = /\/(error|global-error)\.(tsx?|jsx?)$/;
const GLOBAL_ERROR_FILE_PATTERN = /\/global-error\.(tsx?|jsx?)$/;
const ROUTE_HANDLER_HTTP_METHODS = new Set([
	"GET",
	"POST",
	"PUT",
	"PATCH",
	"DELETE",
	"OPTIONS",
	"HEAD"
]);
const GOOGLE_ANALYTICS_SCRIPT_PATTERN = /google-analytics\.com|googletagmanager\.com\/gtag/;
const OG_IMAGE_FILE_PATTERN = /\/(opengraph-image|twitter-image)\d*\.(tsx?|jsx?)$/;
//#endregion
//#region src/plugin/rules/nextjs/nextjs-error-boundary-missing-use-client.ts
const nextjsErrorBoundaryMissingUseClient = defineRule({
	id: "nextjs-error-boundary-missing-use-client",
	title: "Error boundary missing 'use client'",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "error",
	recommendation: "Add `'use client'` at the top of this file. Error boundaries must be Client Components to catch and render fallback UI",
	create: (context) => ({ Program(programNode) {
		const filename = normalizeFilename$1(context.filename ?? "");
		if (!APP_DIRECTORY_PATTERN.test(filename)) return;
		if (!ERROR_BOUNDARY_FILE_PATTERN.test(filename)) return;
		if (hasDirective(programNode, "use client")) return;
		context.report({
			node: programNode,
			message: "This error boundary silently does nothing without 'use client'. Next.js requires error.tsx to be a Client Component."
		});
	} })
});
//#endregion
//#region src/plugin/utils/file-contains-jsx-elements.ts
const fileContainsJsxElements = (programNode, tagNames) => {
	const targetTagNames = new Set(tagNames);
	const foundTagNames = /* @__PURE__ */ new Set();
	walkAst(programNode, (child) => {
		if (foundTagNames.size === targetTagNames.size) return false;
		if (isNodeOfType(child, "JSXOpeningElement") && isNodeOfType(child.name, "JSXIdentifier") && targetTagNames.has(child.name.name)) foundTagNames.add(child.name.name);
	});
	return foundTagNames;
};
//#endregion
//#region src/plugin/rules/nextjs/nextjs-global-error-missing-html-body.ts
const REQUIRED_HTML_TAGS = ["html", "body"];
const nextjsGlobalErrorMissingHtmlBody = defineRule({
	id: "nextjs-global-error-missing-html-body",
	title: "global-error.tsx missing <html>/<body>",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "error",
	recommendation: "Wrap your error UI in `<html><body>...</body></html>`. The root layout is unmounted when global-error renders",
	create: (context) => ({ Program(programNode) {
		const filename = normalizeFilename$1(context.filename ?? "");
		if (!APP_DIRECTORY_PATTERN.test(filename)) return;
		if (!GLOBAL_ERROR_FILE_PATTERN.test(filename)) return;
		const foundTags = fileContainsJsxElements(programNode, REQUIRED_HTML_TAGS);
		const missingTags = REQUIRED_HTML_TAGS.filter((tagName) => !foundTags.has(tagName)).map((tagName) => `<${tagName}>`);
		if (missingTags.length > 0) context.report({
			node: programNode,
			message: `global-error.tsx is missing ${missingTags.join(" and ")}. The root layout unmounts on error, so this page renders broken HTML.`
		});
	} })
});
//#endregion
//#region src/plugin/utils/has-jsx-attribute.ts
const hasJsxAttribute = (attributes, attributeName) => Boolean(findJsxAttribute(attributes, attributeName));
//#endregion
//#region src/plugin/rules/nextjs/nextjs-image-missing-sizes.ts
const nextjsImageMissingSizes = defineRule({
	id: "nextjs-image-missing-sizes",
	title: "Image fill missing sizes",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Add sizes for responsive behavior: `sizes=\"(max-width: 768px) 100vw, 50vw\"` matching your layout breakpoints",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "Image") return;
		const attributes = node.attributes ?? [];
		if (!hasJsxAttribute(attributes, "fill")) return;
		if (hasJsxAttribute(attributes, "sizes")) return;
		context.report({
			node,
			message: "next/image uses fill without sizes, so your users download the largest image."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-inline-script-missing-id.ts
const nextjsInlineScriptMissingId = defineRule({
	id: "nextjs-inline-script-missing-id",
	title: "Inline script missing id",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Add `id=\"descriptive-name\"` so Next.js can track, deduplicate, and re-execute the script correctly",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "Script") return;
		const attributes = node.attributes ?? [];
		if (hasJsxAttribute(attributes, "src")) return;
		if (hasJsxAttribute(attributes, "id")) return;
		context.report({
			node,
			message: "Without an id, Next.js can't track this inline <Script> & may execute it twice."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-missing-metadata.ts
const nextjsMissingMetadata = defineRule({
	id: "nextjs-missing-metadata",
	title: "Page missing metadata",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Add `export const metadata = { title: '...', description: '...' }` or `export async function generateMetadata()`",
	create: (context) => ({ Program(programNode) {
		const filename = normalizeFilename$1(context.filename ?? "");
		if (!PAGE_FILE_PATTERN.test(filename)) return;
		if (INTERNAL_PAGE_PATH_PATTERN.test(filename)) return;
		if (!programNode.body?.some((statement) => {
			if (!isNodeOfType(statement, "ExportNamedDeclaration")) return false;
			const declaration = statement.declaration;
			if (isNodeOfType(declaration, "VariableDeclaration")) return declaration.declarations?.some((declarator) => isNodeOfType(declarator.id, "Identifier") && (declarator.id.name === "metadata" || declarator.id.name === "generateMetadata"));
			if (isNodeOfType(declaration, "FunctionDeclaration")) return declaration.id?.name === "generateMetadata";
			return false;
		})) context.report({
			node: programNode,
			message: "This page has no metadata, so search engines & social previews get no title or description."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-a-element.ts
const nextjsNoAElement = defineRule({
	id: "nextjs-no-a-element",
	title: "Plain anchor for internal link",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "`import Link from 'next/link'` for client-side navigation, prefetching, and preserved scroll position",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "a") return;
		const hrefAttribute = findJsxAttribute(node.attributes ?? [], "href");
		if (!hrefAttribute?.value) return;
		let hrefValue = null;
		if (isNodeOfType(hrefAttribute.value, "Literal")) hrefValue = hrefAttribute.value.value;
		else if (isNodeOfType(hrefAttribute.value, "JSXExpressionContainer") && isNodeOfType(hrefAttribute.value.expression, "Literal")) hrefValue = hrefAttribute.value.expression.value;
		if (typeof hrefValue === "string" && hrefValue.startsWith("/")) context.report({
			node,
			message: "Plain <a> reloads the whole page on internal links."
		});
	} })
});
//#endregion
//#region src/plugin/utils/contains-fetch-call.ts
const containsFetchCall = (node) => {
	let didFindFetchCall = false;
	walkAst(node, (child) => {
		if (didFindFetchCall || !isNodeOfType(child, "CallExpression")) return;
		if (isNodeOfType(child.callee, "Identifier") && FETCH_CALLEE_NAMES.has(child.callee.name)) didFindFetchCall = true;
		if (isNodeOfType(child.callee, "MemberExpression") && isNodeOfType(child.callee.object, "Identifier") && FETCH_MEMBER_OBJECTS.has(child.callee.object.name)) didFindFetchCall = true;
	});
	return didFindFetchCall;
};
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-client-fetch-for-server-data.ts
const nextjsNoClientFetchForServerData = defineRule({
	id: "nextjs-no-client-fetch-for-server-data",
	title: "Client fetch for server data",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Remove 'use client' and fetch directly in the Server Component. No API round-trip, and secrets stay on the server.",
	create: (context) => {
		let fileHasUseClient = false;
		return {
			Program(programNode) {
				fileHasUseClient = hasDirective(programNode, "use client");
			},
			CallExpression(node) {
				if (!fileHasUseClient || !isHookCall$1(node, EFFECT_HOOK_NAMES$1)) return;
				const callback = getEffectCallback(node);
				if (!callback || !containsFetchCall(callback)) return;
				const filename = normalizeFilename$1(context.filename ?? "");
				if (PAGE_OR_LAYOUT_FILE_PATTERN.test(filename) || PAGES_DIRECTORY_PATTERN.test(filename)) context.report({
					node,
					message: "useEffect + fetch in a page/layout makes your users wait through an extra round trip & loading spinner."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-client-side-redirect.ts
const describeClientSideNavigation = (node) => {
	if (isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "MemberExpression")) {
		const objectName = isNodeOfType(node.callee.object, "Identifier") ? node.callee.object.name : null;
		const methodName = isNodeOfType(node.callee.property, "Identifier") ? node.callee.property.name : null;
		if (objectName === "router" && (methodName === "push" || methodName === "replace")) return `router.${methodName}() in useEffect flashes the wrong page before redirecting.`;
	}
	if (isNodeOfType(node, "AssignmentExpression") && isNodeOfType(node.left, "MemberExpression")) {
		const objectName = isNodeOfType(node.left.object, "Identifier") ? node.left.object.name : null;
		const propertyName = isNodeOfType(node.left.property, "Identifier") ? node.left.property.name : null;
		if (objectName === "window" && propertyName === "location") return `window.location assignment in useEffect flashes the wrong page before redirecting.`;
		if (objectName === "location" && propertyName === "href") return `location.href assignment in useEffect flashes the wrong page before redirecting.`;
	}
	return null;
};
const nextjsNoClientSideRedirect = defineRule({
	id: "nextjs-no-client-side-redirect",
	title: "Client-side redirect for navigation",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Avoid redirects inside useEffect. Use an event handler, middleware, or server-side redirect (App Router: redirect() from next/navigation; Pages Router: getServerSideProps redirect)",
	create: (context) => {
		return { CallExpression(node) {
			if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1)) return;
			const callback = getEffectCallback(node);
			if (!callback) return;
			walkAst(callback, (child) => {
				const navigationDescription = describeClientSideNavigation(child);
				if (navigationDescription) context.report({
					node: child,
					message: navigationDescription
				});
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-css-link.ts
const nextjsNoCssLink = defineRule({
	id: "nextjs-no-css-link",
	title: "Stylesheet loaded via link",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Import CSS directly: `import './styles.css'` or use CSS Modules: `import styles from './Button.module.css'`",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "link") return;
		const attributes = node.attributes ?? [];
		const relAttribute = findJsxAttribute(attributes, "rel");
		if (!relAttribute?.value) return;
		if ((isNodeOfType(relAttribute.value, "Literal") ? relAttribute.value.value : null) !== "stylesheet") return;
		const hrefAttribute = findJsxAttribute(attributes, "href");
		if (!hrefAttribute?.value) return;
		const hrefValue = isNodeOfType(hrefAttribute.value, "Literal") ? hrefAttribute.value.value : null;
		if (typeof hrefValue === "string" && GOOGLE_FONTS_PATTERN.test(hrefValue)) return;
		context.report({
			node,
			message: "This <link rel=\"stylesheet\"> loads unbundled, unoptimized CSS."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-default-export-in-route-handler.ts
const programHasNamedHttpMethodExport = (programNode) => {
	for (const statement of programNode.body ?? []) {
		if (!isNodeOfType(statement, "ExportNamedDeclaration")) continue;
		const declaration = statement.declaration;
		if (isNodeOfType(declaration, "FunctionDeclaration") && declaration.id?.name && ROUTE_HANDLER_HTTP_METHODS.has(declaration.id.name)) return true;
		if (isNodeOfType(declaration, "VariableDeclaration")) {
			for (const declarator of declaration.declarations ?? []) if (isNodeOfType(declarator.id, "Identifier") && ROUTE_HANDLER_HTTP_METHODS.has(declarator.id.name)) return true;
		}
		for (const specifier of statement.specifiers ?? []) if (isNodeOfType(specifier, "ExportSpecifier") && isNodeOfType(specifier.exported, "Identifier") && ROUTE_HANDLER_HTTP_METHODS.has(specifier.exported.name)) return true;
	}
	return false;
};
const nextjsNoDefaultExportInRouteHandler = defineRule({
	id: "nextjs-no-default-export-in-route-handler",
	title: "Default export in route handler",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "error",
	recommendation: "Replace `export default` with named HTTP method exports: `export async function GET(request) { … }`",
	create: (context) => {
		let isAppRouteHandler = false;
		let programNode = null;
		return {
			Program(node) {
				const filename = normalizeFilename$1(context.filename ?? "");
				isAppRouteHandler = APP_DIRECTORY_PATTERN.test(filename) && ROUTE_HANDLER_FILE_PATTERN.test(filename);
				programNode = node;
			},
			ExportDefaultDeclaration(node) {
				if (!isAppRouteHandler || !programNode) return;
				if (programHasNamedHttpMethodExport(programNode)) return;
				context.report({
					node,
					message: "Default exports in route.ts are silently ignored. Next.js only recognizes named HTTP method exports (GET, POST, etc.)."
				});
			},
			ExportNamedDeclaration(node) {
				if (!isAppRouteHandler || !programNode) return;
				if (!(node.specifiers ?? []).some((specifier) => isNodeOfType(specifier, "ExportSpecifier") && isNodeOfType(specifier.exported, "Identifier") && specifier.exported.name === "default")) return;
				if (programHasNamedHttpMethodExport(programNode)) return;
				context.report({
					node,
					message: "Default exports in route.ts are silently ignored. Next.js only recognizes named HTTP method exports (GET, POST, etc.)."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-edge-og-runtime.ts
const nextjsNoEdgeOgRuntime = defineRule({
	id: "nextjs-no-edge-og-runtime",
	title: "Edge runtime in OG image route",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Remove `export const runtime = 'edge'` from OG image files. The default Node.js runtime supports more fonts and APIs",
	create: (context) => {
		let isOgImageFile = false;
		return {
			Program() {
				const filename = normalizeFilename$1(context.filename ?? "");
				isOgImageFile = OG_IMAGE_FILE_PATTERN.test(filename);
			},
			ExportNamedDeclaration(node) {
				if (!isOgImageFile) return;
				const declaration = node.declaration;
				if (!isNodeOfType(declaration, "VariableDeclaration")) return;
				for (const declarator of declaration.declarations ?? []) {
					if (!isNodeOfType(declarator, "VariableDeclarator")) continue;
					if (!isNodeOfType(declarator.id, "Identifier")) continue;
					if (declarator.id.name !== "runtime") continue;
					if ((isNodeOfType(declarator.init, "Literal") ? declarator.init.value : null) === "edge") context.report({
						node,
						message: "Edge runtime limits OG image generation. Node.js runtime supports more fonts, filesystem access, and larger response sizes."
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-font-link.ts
const nextjsNoFontLink = defineRule({
	id: "nextjs-no-font-link",
	title: "Google Fonts loaded via link",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "`import { Inter } from \"next/font/google\"` for self-hosting, zero layout shift, and no render-blocking requests",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "link") return;
		const hrefAttribute = findJsxAttribute(node.attributes ?? [], "href");
		if (!hrefAttribute?.value) return;
		const hrefValue = isNodeOfType(hrefAttribute.value, "Literal") ? hrefAttribute.value.value : null;
		if (typeof hrefValue === "string" && GOOGLE_FONTS_PATTERN.test(hrefValue)) context.report({
			node,
			message: "Loading Google Fonts with <link> blocks rendering & shifts layout."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-google-analytics-script.ts
const nextjsNoGoogleAnalyticsScript = defineRule({
	id: "nextjs-no-google-analytics-script",
	title: "Manual Google Analytics script",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Use `import { GoogleAnalytics } from '@next/third-parties/google'` for automatic optimization & smaller bundles",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		if (node.name.name !== "script" && node.name.name !== "Script") return;
		const srcAttribute = findJsxAttribute(node.attributes ?? [], "src");
		if (!srcAttribute?.value) return;
		const srcValue = isNodeOfType(srcAttribute.value, "Literal") ? srcAttribute.value.value : null;
		if (typeof srcValue === "string" && GOOGLE_ANALYTICS_SCRIPT_PATTERN.test(srcValue)) context.report({
			node,
			message: "Manual Google Analytics script blocks rendering. Use @next/third-parties for optimal loading strategy."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-head-import.ts
const nextjsNoHeadImport = defineRule({
	id: "nextjs-no-head-import",
	title: "next/head in App Router",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "error",
	recommendation: "Use the Metadata API instead. Add `export const metadata = { title: '...' }` or `export async function generateMetadata()`",
	create: (context) => ({ ImportDeclaration(node) {
		if (node.source?.value !== "next/head") return;
		const filename = normalizeFilename$1(context.filename ?? "");
		if (!APP_DIRECTORY_PATTERN.test(filename)) return;
		context.report({
			node,
			message: "next/head silently does nothing in the App Router, so your meta tags never render."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-img-element.ts
const nextjsNoImgElement = defineRule({
	id: "nextjs-no-img-element",
	title: "Plain img element",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "`import Image from 'next/image'` for automatic WebP/AVIF, lazy loading, and responsive srcset",
	create: (context) => {
		const filename = normalizeFilename$1(context.filename ?? "");
		const isOgRoute = OG_ROUTE_PATTERN.test(filename);
		const isMetadataImageRoute = isNextjsMetadataImageRouteFilename(filename);
		return { JSXOpeningElement(node) {
			if (isOgRoute || isMetadataImageRoute) return;
			if (isNodeOfType(node.name, "JSXIdentifier") && node.name.name === "img") context.report({
				node,
				message: "Plain <img> ships unoptimized, oversized images to your users."
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-native-script.ts
const nextjsNoNativeScript = defineRule({
	id: "nextjs-no-native-script",
	title: "Plain script tag",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "`import Script from \"next/script\"`. Use `strategy=\"afterInteractive\"` for analytics or `\"lazyOnload\"` for widgets",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "script") return;
		const typeAttribute = findJsxAttribute(node.attributes ?? [], "type");
		const typeValue = isNodeOfType(typeAttribute?.value, "Literal") ? typeAttribute.value.value : null;
		if (typeof typeValue === "string" && !EXECUTABLE_SCRIPT_TYPES.has(typeValue)) return;
		context.report({
			node,
			message: "Plain <script> blocks rendering with no loading strategy."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-polyfill-script.ts
const nextjsNoPolyfillScript = defineRule({
	id: "nextjs-no-polyfill-script",
	title: "Redundant polyfill script",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Next.js includes polyfills for fetch, Promise, Object.assign, Array.from, and 50+ others automatically",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		if (node.name.name !== "script" && node.name.name !== "Script") return;
		const srcAttribute = findJsxAttribute(node.attributes ?? [], "src");
		if (!srcAttribute?.value) return;
		const srcValue = isNodeOfType(srcAttribute.value, "Literal") ? srcAttribute.value.value : null;
		if (typeof srcValue === "string" && POLYFILL_SCRIPT_PATTERN.test(srcValue)) context.report({
			node,
			message: "This polyfill CDN script makes your users download polyfills Next.js already includes."
		});
	} })
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-redirect-in-try-catch.ts
const nextjsNoRedirectInTryCatch = defineRule({
	id: "nextjs-no-redirect-in-try-catch",
	title: "redirect() inside try-catch",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Move the redirect/notFound call outside the try block, or add `unstable_rethrow(error)` in the catch",
	create: (context) => {
		let tryCatchDepth = 0;
		return {
			TryStatement() {
				tryCatchDepth++;
			},
			"TryStatement:exit"() {
				tryCatchDepth--;
			},
			CallExpression(node) {
				if (tryCatchDepth === 0) return;
				if (!isNodeOfType(node.callee, "Identifier")) return;
				if (!NEXTJS_NAVIGATION_FUNCTIONS.has(node.callee.name)) return;
				context.report({
					node,
					message: `${node.callee.name}() inside try-catch gets swallowed, so the redirect silently fails.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-script-in-head.ts
const nextjsNoScriptInHead = defineRule({
	id: "nextjs-no-script-in-head",
	title: "next/script inside next/head",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "error",
	recommendation: "Move `<Script>` outside of `<Head>`. next/script manages its own placement and ignores head context",
	create: (context) => {
		let insideHeadDepth = 0;
		return {
			JSXOpeningElement(node) {
				if (!isNodeOfType(node.name, "JSXIdentifier")) return;
				if (node.name.name === "Head" && !node.selfClosing) insideHeadDepth++;
				if (node.name.name === "Script" && insideHeadDepth > 0) context.report({
					node,
					message: "next/script inside next/head is silently ignored. Move <Script> outside <Head> so it actually loads."
				});
			},
			JSXClosingElement(node) {
				if (isNodeOfType(node.name, "JSXIdentifier") && node.name.name === "Head") insideHeadDepth = Math.max(0, insideHeadDepth - 1);
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/is-cookies-call.ts
const isCookiesCall = (node) => isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "Identifier") && node.callee.name === "cookies";
//#endregion
//#region src/plugin/utils/is-cookies-or-awaited-cookies-call.ts
const isCookiesOrAwaitedCookiesCall = (node) => {
	if (isCookiesCall(node)) return true;
	if (isNodeOfType(node, "AwaitExpression") && node.argument) return isCookiesCall(node.argument);
	return false;
};
//#endregion
//#region src/plugin/utils/collect-locally-scoped-cookie-bindings.ts
const collectLocallyScopedCookieBindings = (handlerBody) => {
	const cookieBindingNames = /* @__PURE__ */ new Set();
	walkInsideStatementBlocks(handlerBody, (node) => {
		if (!isNodeOfType(node, "VariableDeclarator")) return;
		if (!node.init) return;
		if (!isCookiesOrAwaitedCookiesCall(node.init)) return;
		collectPatternNames(node.id, cookieBindingNames);
	});
	return cookieBindingNames;
};
//#endregion
//#region src/plugin/utils/is-safe-mutable-receiver-source.ts
const SAFE_INTRINSIC_PROPERTY_NAMES = new Set(["headers", "searchParams"]);
const unwrapAwait = (node) => isNodeOfType(node, "AwaitExpression") && node.argument ? node.argument : node;
const isSafeConstructorNew = (node) => isNodeOfType(node, "NewExpression") && isNodeOfType(node.callee, "Identifier") && SAFE_MUTABLE_CONSTRUCTOR_NAMES.has(node.callee.name);
const isResponseFactoryCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (!isNodeOfType(node.callee, "MemberExpression")) return false;
	if (!isNodeOfType(node.callee.object, "Identifier")) return false;
	if (!isNodeOfType(node.callee.property, "Identifier")) return false;
	return RESPONSE_FACTORY_OBJECTS.has(node.callee.object.name) && RESPONSE_FACTORY_METHODS.has(node.callee.property.name);
};
const isHeadersFunctionCall = (node) => isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "Identifier") && node.callee.name === "headers";
const isSafeIntrinsicMemberAccess = (node) => isNodeOfType(node, "MemberExpression") && isNodeOfType(node.property, "Identifier") && SAFE_INTRINSIC_PROPERTY_NAMES.has(node.property.name);
const isSafeMutableReceiverSource = (initNode) => {
	const unwrapped = unwrapAwait(initNode);
	if (isSafeConstructorNew(unwrapped)) return true;
	if (isResponseFactoryCall(unwrapped)) return true;
	if (isSafeIntrinsicMemberAccess(unwrapped)) return true;
	if (isHeadersFunctionCall(unwrapped)) return true;
	return false;
};
const isSafeReceiverChainNode = (node, locallyScopedSafeBindings) => {
	if (isSafeConstructorNew(node)) return true;
	if (isResponseFactoryCall(node)) return true;
	if (isSafeIntrinsicMemberAccess(node)) return true;
	if (isHeadersFunctionCall(node)) return true;
	if (isNodeOfType(node, "Identifier") && locallyScopedSafeBindings.has(node.name)) return true;
	return false;
};
//#endregion
//#region src/plugin/utils/collect-locally-scoped-safe-bindings.ts
const collectLocallyScopedSafeBindings = (handlerBody) => {
	const safeBindingNames = /* @__PURE__ */ new Set();
	walkInsideStatementBlocks(handlerBody, (node) => {
		if (!isNodeOfType(node, "VariableDeclarator")) return;
		if (!node.init) return;
		if (!isSafeMutableReceiverSource(node.init)) return;
		collectPatternNames(node.id, safeBindingNames);
	});
	return safeBindingNames;
};
//#endregion
//#region src/plugin/utils/is-safe-receiver-chain.ts
const isSafeReceiverChain = (receiverNode, locallyScopedSafeBindings) => {
	let current = receiverNode;
	while (current) {
		if (isSafeReceiverChainNode(current, locallyScopedSafeBindings)) return true;
		if (isNodeOfType(current, "MemberExpression")) {
			current = current.object;
			continue;
		}
		if (isNodeOfType(current, "ChainExpression")) {
			current = current.expression;
			continue;
		}
		if (isNodeOfType(current, "AwaitExpression")) {
			current = current.argument;
			continue;
		}
		if (isNodeOfType(current, "TSNonNullExpression") || isNodeOfType(current, "TSAsExpression")) {
			current = current.expression;
			continue;
		}
		return false;
	}
	return false;
};
//#endregion
//#region src/plugin/utils/find-side-effect.ts
const EMPTY_BINDING_SET = /* @__PURE__ */ new Set();
const COOKIE_MUTATION_METHOD_NAMES = new Set([
	"set",
	"append",
	"delete"
]);
const isMutatingMethodProperty = (property) => isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "method" && isNodeOfType(property.value, "Literal") && typeof property.value.value === "string" && MUTATING_HTTP_METHODS.has(property.value.value.toUpperCase());
const isCookieReceiver = (receiverNode, locallyScopedCookieBindings) => {
	if (isCookiesOrAwaitedCookiesCall(receiverNode)) return true;
	if (isNodeOfType(receiverNode, "Identifier")) return locallyScopedCookieBindings.has(receiverNode.name);
	return false;
};
const getCookieMutationMethodName = (node, locallyScopedCookieBindings) => {
	if (!isNodeOfType(node, "CallExpression")) return null;
	if (!isNodeOfType(node.callee, "MemberExpression")) return null;
	if (!isNodeOfType(node.callee.property, "Identifier")) return null;
	if (!COOKIE_MUTATION_METHOD_NAMES.has(node.callee.property.name)) return null;
	if (!isCookieReceiver(node.callee.object, locallyScopedCookieBindings)) return null;
	return node.callee.property.name;
};
const isMutatingFetchCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (!isNodeOfType(node.callee, "Identifier") || node.callee.name !== "fetch") return false;
	const optionsArgument = node.arguments?.[1];
	if (!optionsArgument || !isNodeOfType(optionsArgument, "ObjectExpression")) return false;
	return Boolean(optionsArgument.properties?.some(isMutatingMethodProperty));
};
const isMutatingDbCall = (node, locallyScopedSafeBindings) => {
	if (!isNodeOfType(node, "CallExpression") || !isNodeOfType(node.callee, "MemberExpression")) return false;
	const { property, object } = node.callee;
	if (!isNodeOfType(property, "Identifier") || !MUTATION_METHOD_NAMES.has(property.name)) return false;
	if (isSafeReceiverChain(object, locallyScopedSafeBindings)) return false;
	return true;
};
const getDbCallDescription = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return ".unknown()";
	if (!isNodeOfType(node.callee, "MemberExpression")) return ".unknown()";
	if (!isNodeOfType(node.callee.property, "Identifier")) return ".unknown()";
	const methodName = node.callee.property.name;
	const rootObjectName = isNodeOfType(node.callee.object, "Identifier") ? node.callee.object.name : null;
	return rootObjectName ? `${rootObjectName}.${methodName}()` : `.${methodName}()`;
};
const findSideEffect = (node, options = {}) => {
	const locallyScopedSafeBindings = options.locallyScopedSafeBindings ?? EMPTY_BINDING_SET;
	const locallyScopedCookieBindings = options.locallyScopedCookieBindings ?? EMPTY_BINDING_SET;
	let sideEffectDescription = null;
	walkAst(node, (child) => {
		if (sideEffectDescription) return;
		const cookieMethodName = getCookieMutationMethodName(child, locallyScopedCookieBindings);
		if (cookieMethodName) {
			sideEffectDescription = `cookies().${cookieMethodName}()`;
			return;
		}
		if (isMutatingFetchCall(child) && isNodeOfType(child, "CallExpression")) {
			const optionsArgument = child.arguments[1];
			if (!isNodeOfType(optionsArgument, "ObjectExpression")) return;
			const methodProperty = optionsArgument.properties.find(isMutatingMethodProperty);
			if (!methodProperty || !isNodeOfType(methodProperty, "Property") || !isNodeOfType(methodProperty.value, "Literal")) return;
			sideEffectDescription = `fetch() with method ${methodProperty.value.value}`;
			return;
		}
		if (isMutatingDbCall(child, locallyScopedSafeBindings)) sideEffectDescription = getDbCallDescription(child);
	});
	return sideEffectDescription;
};
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-side-effect-in-get-handler.ts
const extractMutatingRouteSegment = (rawFilename) => {
	const segments = rawFilename.replaceAll("\\", "/").split("/");
	for (const segment of segments) {
		const cleaned = segment.replace(/^\[.*\]$/, "");
		if (MUTATING_ROUTE_SEGMENTS.has(cleaned)) return cleaned;
	}
	return null;
};
const buildProgramBindingLookup = (programNode) => {
	const topLevelBindings = /* @__PURE__ */ new Map();
	if (!isNodeOfType(programNode, "Program")) return () => null;
	const collectFromStatements = (statements) => {
		for (const statement of statements) {
			if (isNodeOfType(statement, "VariableDeclaration")) {
				for (const declarator of statement.declarations ?? []) {
					if (!isNodeOfType(declarator.id, "Identifier")) continue;
					if (!declarator.init) continue;
					topLevelBindings.set(declarator.id.name, declarator.init);
				}
				continue;
			}
			if (isNodeOfType(statement, "FunctionDeclaration") && isNodeOfType(statement.id, "Identifier") && statement.body) {
				topLevelBindings.set(statement.id.name, statement);
				continue;
			}
			if (isNodeOfType(statement, "ExportNamedDeclaration") && statement.declaration) collectFromStatements([statement.declaration]);
		}
	};
	collectFromStatements(programNode.body ?? []);
	return (identifierName) => topLevelBindings.get(identifierName) ?? null;
};
const isExportedGetHandler = (node) => {
	if (!isNodeOfType(node, "ExportNamedDeclaration")) return false;
	const declaration = node.declaration;
	if (!declaration) return false;
	if (isNodeOfType(declaration, "FunctionDeclaration") && declaration.id?.name === "GET") return true;
	if (isNodeOfType(declaration, "VariableDeclaration")) {
		for (const declarator of declaration.declarations ?? []) if (isNodeOfType(declarator?.id, "Identifier") && declarator.id.name === "GET") return true;
	}
	return false;
};
const isGetMethodCall = (callExpression) => isNodeOfType(callExpression, "CallExpression") && isNodeOfType(callExpression.callee, "MemberExpression") && isNodeOfType(callExpression.callee.property, "Identifier") && callExpression.callee.property.name === "get";
const isStringLikeNode = (node) => isNodeOfType(node, "Literal") && typeof node.value === "string" || isNodeOfType(node, "TemplateLiteral");
const getHandlerCallbackBody = (callExpression) => {
	const callArguments = callExpression.arguments ?? [];
	if (callArguments.length < 2) return null;
	const routePatternArgument = callArguments[0];
	if (!isStringLikeNode(routePatternArgument)) return null;
	const handlerArgument = callArguments[callArguments.length - 1];
	if ((isNodeOfType(handlerArgument, "ArrowFunctionExpression") || isNodeOfType(handlerArgument, "FunctionExpression")) && handlerArgument.body) return handlerArgument.body;
	return null;
};
const collectChainedGetHandlerBodies = (initNode) => {
	const chainedBodies = [];
	let cursor = initNode;
	while (cursor && isNodeOfType(cursor, "CallExpression")) {
		if (isGetMethodCall(cursor)) {
			const body = getHandlerCallbackBody(cursor);
			if (body) chainedBodies.push(body);
		}
		cursor = isNodeOfType(cursor.callee, "MemberExpression") ? cursor.callee.object : null;
	}
	return chainedBodies;
};
const resolveBodiesFromExpression = (expression, resolveBinding, remainingDepth) => {
	if (remainingDepth <= 0) return [];
	if (isFunctionLike$2(expression)) return expression.body ? [expression.body] : [];
	if (isNodeOfType(expression, "CallExpression")) {
		for (const callArgument of expression.arguments ?? []) {
			if (isNodeOfType(callArgument, "ArrowFunctionExpression") || isNodeOfType(callArgument, "FunctionExpression")) {
				if (callArgument.body) return [callArgument.body];
			}
			if (!isNodeOfType(callArgument, "Identifier")) continue;
			const argumentInit = resolveBinding(callArgument.name);
			if (!argumentInit) continue;
			const resolvedBodies = resolveBodiesFromExpression(argumentInit, resolveBinding, remainingDepth - 1);
			if (resolvedBodies.length > 0) return resolvedBodies;
			const chainedBodies = collectChainedGetHandlerBodies(argumentInit);
			if (chainedBodies.length > 0) return chainedBodies;
		}
		return [];
	}
	if (isNodeOfType(expression, "Identifier")) {
		const boundInit = resolveBinding(expression.name);
		if (!boundInit) return [];
		return resolveBodiesFromExpression(boundInit, resolveBinding, remainingDepth - 1);
	}
	return [];
};
const resolveGetHandlerBodies = (exportNode, resolveBinding) => {
	if (!isNodeOfType(exportNode, "ExportNamedDeclaration")) return [];
	const declaration = exportNode.declaration;
	if (!declaration) return [];
	if (isNodeOfType(declaration, "FunctionDeclaration") && declaration.id?.name === "GET") return declaration.body ? [declaration.body] : [];
	if (!isNodeOfType(declaration, "VariableDeclaration")) return [];
	for (const declarator of declaration.declarations ?? []) {
		if (!isNodeOfType(declarator.id, "Identifier") || declarator.id.name !== "GET") continue;
		if (!declarator.init) return [];
		return resolveBodiesFromExpression(declarator.init, resolveBinding, 3);
	}
	return [];
};
const nextjsNoSideEffectInGetHandler = defineRule({
	id: "nextjs-no-side-effect-in-get-handler",
	title: "Side effect in GET handler",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "error",
	category: "Security",
	recommendation: "GET requests can be prefetched and are open to CSRF. Move the side effect to a POST handler.",
	create: (context) => {
		let resolveBinding = () => null;
		return {
			Program(node) {
				resolveBinding = buildProgramBindingLookup(node);
			},
			ExportNamedDeclaration(node) {
				const filename = normalizeFilename$1(context.filename ?? "");
				if (!ROUTE_HANDLER_FILE_PATTERN.test(filename)) return;
				if (CRON_ROUTE_PATTERN.test(filename)) return;
				if (!isExportedGetHandler(node)) return;
				const mutatingSegment = extractMutatingRouteSegment(filename);
				if (mutatingSegment) {
					context.report({
						node,
						message: `This GET handler on the "/${mutatingSegment}" route is prone to CSRF vulnerabilities, since prefetching or a forged request can trigger it.`
					});
					return;
				}
				const handlerBodies = resolveGetHandlerBodies(node, resolveBinding);
				for (const handlerBody of handlerBodies) {
					const sideEffect = findSideEffect(handlerBody, {
						locallyScopedSafeBindings: collectLocallyScopedSafeBindings(handlerBody),
						locallyScopedCookieBindings: collectLocallyScopedCookieBindings(handlerBody)
					});
					if (!sideEffect) continue;
					context.report({
						node,
						message: `This GET handler's side effect (${sideEffect}) is prone to CSRF vulnerabilities, since prefetching or a forged request can trigger it.`
					});
					return;
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/find-exported-function-body.ts
const isFunctionLike = (node) => {
	if (!node) return false;
	return isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression");
};
const findExportedFunctionBody = (programRoot, exportedName) => {
	if (!isNodeOfType(programRoot, "Program")) return null;
	const localBindings = /* @__PURE__ */ new Map();
	const namedExports = /* @__PURE__ */ new Map();
	let defaultExport = null;
	let defaultExportIdentifierName = null;
	const recordVariableDeclaration = (declaration) => {
		for (const declarator of declaration.declarations ?? []) {
			if (!isNodeOfType(declarator, "VariableDeclarator")) continue;
			if (!isNodeOfType(declarator.id, "Identifier")) continue;
			const initializer = declarator.init ? stripParenExpression(declarator.init) : null;
			if (initializer && isFunctionLike(initializer)) localBindings.set(declarator.id.name, initializer);
		}
	};
	for (const statement of programRoot.body ?? []) {
		if (isNodeOfType(statement, "VariableDeclaration")) {
			recordVariableDeclaration(statement);
			continue;
		}
		if (isNodeOfType(statement, "FunctionDeclaration") && statement.id) {
			localBindings.set(statement.id.name, statement);
			continue;
		}
		if (isNodeOfType(statement, "ExportNamedDeclaration")) {
			const declaration = statement.declaration;
			if (declaration && isNodeOfType(declaration, "VariableDeclaration")) {
				recordVariableDeclaration(declaration);
				for (const declarator of declaration.declarations ?? []) {
					if (!isNodeOfType(declarator, "VariableDeclarator")) continue;
					if (!isNodeOfType(declarator.id, "Identifier")) continue;
					namedExports.set(declarator.id.name, declarator.id.name);
				}
			} else if (declaration && isNodeOfType(declaration, "FunctionDeclaration") && declaration.id) {
				localBindings.set(declaration.id.name, declaration);
				namedExports.set(declaration.id.name, declaration.id.name);
			}
			for (const specifier of statement.specifiers ?? []) {
				if (!isNodeOfType(specifier, "ExportSpecifier")) continue;
				const local = specifier.local;
				const exported = specifier.exported;
				if (!isNodeOfType(local, "Identifier")) continue;
				const exportedNameSpec = isNodeOfType(exported, "Identifier") ? exported.name : isNodeOfType(exported, "Literal") && typeof exported.value === "string" ? exported.value : null;
				if (!exportedNameSpec) continue;
				namedExports.set(exportedNameSpec, local.name);
			}
			continue;
		}
		if (isNodeOfType(statement, "ExportDefaultDeclaration")) {
			const declaration = statement.declaration;
			if (!declaration) continue;
			if (isNodeOfType(declaration, "FunctionDeclaration") && declaration.id) {
				localBindings.set(declaration.id.name, declaration);
				defaultExport = declaration;
				continue;
			}
			if (isFunctionLike(declaration)) {
				defaultExport = declaration;
				continue;
			}
			if (isNodeOfType(declaration, "Identifier")) {
				defaultExportIdentifierName = declaration.name;
				continue;
			}
		}
	}
	if (exportedName === "default") {
		if (defaultExport) return defaultExport;
		if (defaultExportIdentifierName) {
			const binding = localBindings.get(defaultExportIdentifierName);
			if (binding) return binding;
		}
	}
	const localName = namedExports.get(exportedName);
	if (!localName) return null;
	return localBindings.get(localName) ?? null;
};
const resolveImportedExportName = (importSpecifier) => {
	if (isNodeOfType(importSpecifier, "ImportSpecifier")) {
		const imported = importSpecifier.imported;
		if (isNodeOfType(imported, "Identifier")) return imported.name;
		if (isNodeOfType(imported, "Literal") && typeof imported.value === "string") return imported.value;
		return null;
	}
	if (isNodeOfType(importSpecifier, "ImportDefaultSpecifier")) return "default";
	return null;
};
const findReExportSourcesForName = (programRoot, exportedName) => {
	if (!isNodeOfType(programRoot, "Program")) return [];
	const exportAllSources = [];
	for (const statement of programRoot.body ?? []) {
		if (isNodeOfType(statement, "ExportNamedDeclaration") && statement.source) {
			const sourceValue = statement.source.value;
			if (typeof sourceValue !== "string") continue;
			for (const specifier of statement.specifiers ?? []) {
				if (!isNodeOfType(specifier, "ExportSpecifier")) continue;
				const exported = specifier.exported;
				if ((isNodeOfType(exported, "Identifier") ? exported.name : isNodeOfType(exported, "Literal") && typeof exported.value === "string" ? exported.value : null) === exportedName) return [sourceValue];
			}
		}
		if (isNodeOfType(statement, "ExportAllDeclaration") && statement.source) {
			const sourceValue = statement.source.value;
			if (typeof sourceValue === "string") exportAllSources.push(sourceValue);
		}
	}
	return exportAllSources;
};
//#endregion
//#region src/plugin/utils/attach-parent-references.ts
const attachParentReferences = (root) => {
	const visit = (node, parent) => {
		const writableNode = node;
		writableNode.parent = parent;
		const nodeRecord = node;
		for (const key of Object.keys(nodeRecord)) {
			if (key === "parent") continue;
			const child = nodeRecord[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item, node);
			} else if (isAstNode(child)) visit(child, node);
		}
	};
	visit(root, null);
};
//#endregion
//#region src/plugin/utils/parse-source-file.ts
const FILENAME_TO_LANG = {
	".ts": "ts",
	".tsx": "tsx",
	".js": "js",
	".jsx": "jsx",
	".mjs": "js",
	".cjs": "js",
	".mts": "ts",
	".cts": "ts"
};
const resolveLang = (filename) => {
	return FILENAME_TO_LANG[path.extname(filename).toLowerCase()] ?? "tsx";
};
const parseCache = /* @__PURE__ */ new Map();
const parseSourceFile = (absoluteFilePath) => {
	let fileStat;
	try {
		fileStat = fs.statSync(absoluteFilePath);
	} catch {
		return null;
	}
	if (!fileStat.isFile()) return null;
	if (fileStat.size > 2e6) return null;
	const cached = parseCache.get(absoluteFilePath);
	if (cached && cached.mtimeMs === fileStat.mtimeMs && cached.size === fileStat.size) return cached.program;
	if (absoluteFilePath.endsWith(".d.ts") || absoluteFilePath.endsWith(".d.mts") || absoluteFilePath.endsWith(".d.cts")) {
		parseCache.set(absoluteFilePath, {
			mtimeMs: fileStat.mtimeMs,
			size: fileStat.size,
			program: null
		});
		return null;
	}
	let sourceText;
	try {
		sourceText = fs.readFileSync(absoluteFilePath, "utf8");
	} catch {
		parseCache.set(absoluteFilePath, {
			mtimeMs: fileStat.mtimeMs,
			size: fileStat.size,
			program: null
		});
		return null;
	}
	let parsedProgram = null;
	try {
		const result = parseSync(absoluteFilePath, sourceText, {
			astType: "ts",
			lang: resolveLang(absoluteFilePath)
		});
		if (!result.errors.some((parseError) => parseError.severity === "Error")) {
			parsedProgram = result.program;
			attachParentReferences(parsedProgram);
		}
	} catch {
		parsedProgram = null;
	}
	parseCache.set(absoluteFilePath, {
		mtimeMs: fileStat.mtimeMs,
		size: fileStat.size,
		program: parsedProgram
	});
	return parsedProgram;
};
//#endregion
//#region src/plugin/utils/parse-export-specifiers.ts
const getSpecifierName = (rawName) => rawName.replace(/^type\s+/, "").trim();
const parseExportSpecifiers = (specifiersText, declarationIsTypeOnly) => specifiersText.split(",").map((specifierText) => specifierText.trim()).filter(Boolean).map((specifierText) => {
	const isTypeOnly = declarationIsTypeOnly || specifierText.startsWith("type ");
	const [rawLocalName, rawExportedName] = specifierText.split(/\s+as\s+/);
	const localName = getSpecifierName(rawLocalName ?? "");
	return {
		localName,
		exportedName: getSpecifierName(rawExportedName ?? localName),
		isTypeOnly
	};
});
//#endregion
//#region src/plugin/utils/strip-js-comments.ts
const BLOCK_COMMENT_PATTERN = /\/\*[\s\S]*?\*\//g;
const LINE_COMMENT_PATTERN = /^\s*\/\/.*$/gm;
const stripJsComments = (sourceText) => sourceText.replace(BLOCK_COMMENT_PATTERN, "").replace(LINE_COMMENT_PATTERN, "");
//#endregion
//#region src/plugin/utils/does-module-export-name.ts
const DEFAULT_EXPORT_DECLARATION_PATTERN = /^\s*export\s+default\b/m;
const NAMED_EXPORT_DECLARATION_PATTERN = /^\s*export\s+(?:declare\s+)?(?:(?:async\s+)?function|(?:abstract\s+)?class|const|let|var|enum|interface|type)\s+([\w$]+)/gm;
const LOCAL_EXPORT_SPECIFIER_DECLARATION_PATTERN$1 = /^\s*export\s+(?:type\s+)?\{([\s\S]*?)\}(?:\s+from\s+["'][^"']+["'])?\s*;?\s*(?:(?:\/\/[^\n]*)?\s*)/gm;
const doesSourceTextExportName = (sourceText, exportedName) => {
	const strippedSource = stripJsComments(sourceText);
	if (exportedName === "default" && DEFAULT_EXPORT_DECLARATION_PATTERN.test(strippedSource)) return true;
	for (const match of strippedSource.matchAll(NAMED_EXPORT_DECLARATION_PATTERN)) if (match[1] === exportedName) return true;
	for (const match of strippedSource.matchAll(LOCAL_EXPORT_SPECIFIER_DECLARATION_PATTERN$1)) if (parseExportSpecifiers(match[1] ?? "", false).map((specifier) => specifier.exportedName).includes(exportedName)) return true;
	return false;
};
const doesModuleExportName = (filePath, exportedName) => {
	try {
		return doesSourceTextExportName(fs.readFileSync(filePath, "utf8"), exportedName);
	} catch {
		return false;
	}
};
//#endregion
//#region src/plugin/utils/is-barrel-index-module.ts
const INDEX_MODULE_FILE_PATTERN = /^index\.(?:[cm]?[jt]sx?|mjs)$/;
const BINDING_IMPORT_DECLARATION_PATTERN = /^\s*import\s+(type\s+)?(?!["'])([^;]*?)\s+from\s+["']([^"']+)["']\s*;?\s*(?:(?:\/\/[^\n]*)?\s*)/gm;
const BARREL_REEXPORT_DECLARATION_PATTERN = /^\s*export\s+(type\s+)?(?:\*(?:\s+as\s+([\w$]+))?|\{([\s\S]*?)\})\s+from\s+["']([^"']+)["']\s*;?\s*(?:(?:\/\/[^\n]*)?\s*)/gm;
const LOCAL_EXPORT_SPECIFIER_DECLARATION_PATTERN = /^\s*export\s+(type\s+)?\{([\s\S]*?)\}\s*;?\s*(?:(?:\/\/[^\n]*)?\s*)/gm;
const barrelIndexModuleInfoCache = /* @__PURE__ */ new Map();
const isIndexModuleFilePath = (filePath) => INDEX_MODULE_FILE_PATTERN.test(path.basename(filePath));
const createNonBarrelInfo = () => ({
	isBarrel: false,
	exportsByName: /* @__PURE__ */ new Map(),
	starExportSources: []
});
const addImportedBinding = (importedBindings, binding) => {
	importedBindings.set(binding.localName, {
		...binding,
		didExport: false
	});
};
const collectNamedImportBindings = (namedSpecifiersText, source, declarationIsTypeOnly, importedBindings) => {
	for (const specifier of parseExportSpecifiers(namedSpecifiersText, declarationIsTypeOnly)) addImportedBinding(importedBindings, {
		localName: specifier.exportedName,
		importedName: specifier.localName,
		source,
		isTypeOnly: specifier.isTypeOnly
	});
};
const collectImportBindings = (importClause, source, declarationIsTypeOnly, importedBindings) => {
	const trimmedImportClause = importClause.trim();
	const namespaceMatch = trimmedImportClause.match(/(?:^|,\s*)\*\s+as\s+([\w$]+)/);
	if (namespaceMatch?.[1]) addImportedBinding(importedBindings, {
		localName: namespaceMatch[1],
		importedName: "*",
		source,
		isTypeOnly: declarationIsTypeOnly
	});
	const namedImportMatch = trimmedImportClause.match(/\{([\s\S]*?)\}/);
	if (namedImportMatch?.[1]) collectNamedImportBindings(namedImportMatch[1], source, declarationIsTypeOnly, importedBindings);
	const defaultImportName = trimmedImportClause.split(",")[0]?.trim();
	if (defaultImportName && !defaultImportName.startsWith("{") && !defaultImportName.startsWith("*")) addImportedBinding(importedBindings, {
		localName: defaultImportName,
		importedName: "default",
		source,
		isTypeOnly: declarationIsTypeOnly
	});
};
const replaceKnownDeclarations = (sourceText, importedBindings, exportsByName, starExportSources) => {
	let withoutKnownDeclarations = sourceText.replace(BINDING_IMPORT_DECLARATION_PATTERN, (_match, typeKeyword, importClause, source) => {
		collectImportBindings(importClause, source, Boolean(typeKeyword), importedBindings);
		return "";
	});
	withoutKnownDeclarations = withoutKnownDeclarations.replace(BARREL_REEXPORT_DECLARATION_PATTERN, (_match, typeKeyword, namespaceExportName, specifiersText, source) => {
		const isTypeOnly = Boolean(typeKeyword);
		if (namespaceExportName) {
			exportsByName.set(namespaceExportName, {
				exportedName: namespaceExportName,
				importedName: "*",
				source,
				isTypeOnly
			});
			return "";
		}
		if (specifiersText) {
			for (const specifier of parseExportSpecifiers(specifiersText, isTypeOnly)) exportsByName.set(specifier.exportedName, {
				exportedName: specifier.exportedName,
				importedName: specifier.localName,
				source,
				isTypeOnly: specifier.isTypeOnly
			});
			return "";
		}
		starExportSources.push(source);
		return "";
	});
	withoutKnownDeclarations = withoutKnownDeclarations.replace(LOCAL_EXPORT_SPECIFIER_DECLARATION_PATTERN, (_match, typeKeyword, specifiersText) => {
		for (const specifier of parseExportSpecifiers(specifiersText, Boolean(typeKeyword))) {
			const importedBinding = importedBindings.get(specifier.localName);
			if (!importedBinding) return _match;
			importedBinding.didExport = true;
			exportsByName.set(specifier.exportedName, {
				exportedName: specifier.exportedName,
				importedName: importedBinding.importedName,
				source: importedBinding.source,
				isTypeOnly: specifier.isTypeOnly || importedBinding.isTypeOnly
			});
		}
		return "";
	});
	return withoutKnownDeclarations;
};
const hasUnexportedRuntimeImport = (importedBindings) => {
	for (const binding of importedBindings.values()) if (!binding.isTypeOnly && !binding.didExport) return true;
	return false;
};
const classifyBarrelModule = (sourceText) => {
	const strippedSource = stripJsComments(sourceText).trim();
	if (!strippedSource) return createNonBarrelInfo();
	const importedBindings = /* @__PURE__ */ new Map();
	const exportsByName = /* @__PURE__ */ new Map();
	const starExportSources = [];
	if (replaceKnownDeclarations(strippedSource, importedBindings, exportsByName, starExportSources).trim() || hasUnexportedRuntimeImport(importedBindings)) return createNonBarrelInfo();
	return {
		isBarrel: exportsByName.size > 0 || starExportSources.length > 0,
		exportsByName,
		starExportSources
	};
};
const getBarrelIndexModuleInfo = (filePath) => {
	if (!isIndexModuleFilePath(filePath)) return createNonBarrelInfo();
	const cachedResult = barrelIndexModuleInfoCache.get(filePath);
	if (cachedResult !== void 0) return cachedResult;
	let moduleInfo = createNonBarrelInfo();
	try {
		moduleInfo = classifyBarrelModule(fs.readFileSync(filePath, "utf8"));
	} catch {
		moduleInfo = createNonBarrelInfo();
	}
	barrelIndexModuleInfoCache.set(filePath, moduleInfo);
	return moduleInfo;
};
const isBarrelIndexModule = (filePath) => getBarrelIndexModuleInfo(filePath).isBarrel;
//#endregion
//#region src/plugin/utils/resolve-relative-import-path.ts
const MODULE_FILE_EXTENSIONS = [
	".ts",
	".tsx",
	".js",
	".jsx",
	".mjs",
	".cjs",
	".mts",
	".cts"
];
const PACKAGE_EXPORT_CONDITIONS = [
	"import",
	"default",
	"module",
	"browser",
	"require"
];
const PACKAGE_ENTRY_FIELDS = [
	"module",
	"main",
	"browser"
];
const getExistingFilePath = (filePath) => {
	try {
		return fs.statSync(filePath).isFile() ? filePath : null;
	} catch {
		return null;
	}
};
const getExistingDirectoryPath = (directoryPath) => {
	try {
		return fs.statSync(directoryPath).isDirectory() ? directoryPath : null;
	} catch {
		return null;
	}
};
const getModuleFilePathCandidates = (modulePath) => {
	const extension = path.extname(modulePath);
	if (!extension) return MODULE_FILE_EXTENSIONS.map((moduleExtension) => `${modulePath}${moduleExtension}`);
	const modulePathWithoutExtension = modulePath.slice(0, -extension.length);
	if (extension === ".js") return [
		modulePath,
		`${modulePathWithoutExtension}.ts`,
		`${modulePathWithoutExtension}.tsx`,
		`${modulePathWithoutExtension}.jsx`
	];
	if (extension === ".jsx") return [modulePath, `${modulePathWithoutExtension}.tsx`];
	if (extension === ".mjs") return [modulePath, `${modulePathWithoutExtension}.mts`];
	if (extension === ".cjs") return [modulePath, `${modulePathWithoutExtension}.cts`];
	return [modulePath];
};
const isObjectRecord$1 = (value) => typeof value === "object" && value !== null;
const getConditionalExportEntry = (exportEntry) => {
	if (typeof exportEntry === "string") return exportEntry;
	if (Array.isArray(exportEntry)) {
		for (const fallbackEntry of exportEntry) {
			const resolvedFallbackEntry = getConditionalExportEntry(fallbackEntry);
			if (resolvedFallbackEntry) return resolvedFallbackEntry;
		}
		return null;
	}
	if (!isObjectRecord$1(exportEntry)) return null;
	for (const condition of PACKAGE_EXPORT_CONDITIONS) {
		const nestedEntry = getConditionalExportEntry(exportEntry[condition]);
		if (nestedEntry) return nestedEntry;
	}
	return null;
};
const getPackageExportEntry = (packageJson) => {
	const exportsField = packageJson.exports;
	if (!exportsField) return null;
	const directExportEntry = getConditionalExportEntry(exportsField);
	if (directExportEntry) return directExportEntry;
	if (!isObjectRecord$1(exportsField)) return null;
	return getConditionalExportEntry(exportsField["."]);
};
const resolveModulePathWithIndexFallback = (modulePath) => {
	const filePath = resolveModuleFilePath(modulePath);
	if (filePath) return filePath;
	return resolveModuleFilePath(path.join(modulePath, "index"));
};
const resolvePackageDirectoryEntry = (directoryPath) => {
	const existingDirectoryPath = getExistingDirectoryPath(directoryPath);
	if (!existingDirectoryPath) return null;
	const packageJsonPath = path.join(existingDirectoryPath, "package.json");
	try {
		const packageJson = JSON.parse(fs.readFileSync(packageJsonPath, "utf8"));
		const packageEntry = getPackageExportEntry(packageJson) ?? PACKAGE_ENTRY_FIELDS.map((fieldName) => packageJson[fieldName]).find((value) => typeof value === "string");
		if (!packageEntry) return null;
		return resolveModulePathWithIndexFallback(path.resolve(existingDirectoryPath, packageEntry));
	} catch {
		return null;
	}
};
const resolveModuleFilePath = (modulePath) => {
	const exactFilePath = getExistingFilePath(modulePath);
	if (exactFilePath) return exactFilePath;
	for (const candidateFilePath of getModuleFilePathCandidates(modulePath)) {
		const filePath = getExistingFilePath(candidateFilePath);
		if (filePath) return filePath;
	}
	return null;
};
const resolveModuleFileFromAbsolutePath = (importPath) => {
	const directFilePath = resolveModuleFilePath(importPath);
	if (directFilePath) return directFilePath;
	const packageEntryFilePath = resolvePackageDirectoryEntry(importPath);
	if (packageEntryFilePath) return packageEntryFilePath;
	return resolveModuleFilePath(path.join(importPath, "index"));
};
const resolveRelativeImportPath = (filename, source) => resolveModuleFileFromAbsolutePath(path.resolve(path.dirname(filename), source));
//#endregion
//#region src/plugin/utils/resolve-barrel-export-file-path.ts
const getUniqueFilePath = (filePaths) => {
	const uniqueFilePaths = new Set(filePaths);
	if (uniqueFilePaths.size !== 1) return null;
	const [filePath] = uniqueFilePaths;
	return filePath ?? null;
};
const resolveStarExportFilePath = (barrelFilePath, exportedName, source, visitedFilePaths) => {
	const resolvedTargetPath = resolveRelativeImportPath(barrelFilePath, source);
	if (!resolvedTargetPath) return null;
	const nestedTargetPath = resolveBarrelExportFilePath(resolvedTargetPath, exportedName, new Set(visitedFilePaths));
	if (nestedTargetPath) return nestedTargetPath;
	return doesModuleExportName(resolvedTargetPath, exportedName) ? resolvedTargetPath : null;
};
const resolveBarrelExportFilePath = (barrelFilePath, exportedName, visitedFilePaths = /* @__PURE__ */ new Set()) => {
	if (visitedFilePaths.has(barrelFilePath)) return null;
	visitedFilePaths.add(barrelFilePath);
	const moduleInfo = getBarrelIndexModuleInfo(barrelFilePath);
	if (!moduleInfo.isBarrel) return null;
	const target = moduleInfo.exportsByName.get(exportedName);
	if (target) {
		const resolvedTargetPath = resolveRelativeImportPath(barrelFilePath, target.source);
		if (!resolvedTargetPath) return null;
		return resolveBarrelExportFilePath(resolvedTargetPath, target.importedName, visitedFilePaths) ?? resolvedTargetPath;
	}
	if (exportedName === "default") return null;
	return getUniqueFilePath(moduleInfo.starExportSources.map((source) => resolveStarExportFilePath(barrelFilePath, exportedName, source, visitedFilePaths)).filter((filePath) => Boolean(filePath)));
};
//#endregion
//#region src/plugin/utils/resolve-tsconfig-alias.ts
const TSCONFIG_FILE_NAMES = ["tsconfig.json", "jsconfig.json"];
const isObjectRecord = (value) => typeof value === "object" && value !== null;
const stripJsonComments = (text) => {
	let output = "";
	let inString = false;
	let inLineComment = false;
	let inBlockComment = false;
	for (let index = 0; index < text.length; index++) {
		const character = text[index];
		const nextCharacter = text[index + 1];
		if (inLineComment) {
			if (character === "\n") {
				inLineComment = false;
				output += character;
			}
			continue;
		}
		if (inBlockComment) {
			if (character === "*" && nextCharacter === "/") {
				inBlockComment = false;
				index++;
			}
			continue;
		}
		if (inString) {
			output += character;
			if (character === "\\") {
				output += nextCharacter ?? "";
				index++;
			} else if (character === "\"") inString = false;
			continue;
		}
		if (character === "\"") {
			inString = true;
			output += character;
			continue;
		}
		if (character === "/" && nextCharacter === "/") {
			inLineComment = true;
			index++;
			continue;
		}
		if (character === "/" && nextCharacter === "*") {
			inBlockComment = true;
			index++;
			continue;
		}
		output += character;
	}
	return output.replace(/,(\s*[}\]])/g, "$1");
};
const parseTsconfigFile = (configFilePath) => {
	let sourceText;
	try {
		sourceText = fs.readFileSync(configFilePath, "utf8");
	} catch {
		return null;
	}
	try {
		const parsed = JSON.parse(stripJsonComments(sourceText));
		return isObjectRecord(parsed) ? parsed : null;
	} catch {
		return null;
	}
};
const resolveExtendsPath = (extendsValue, fromConfigDirectory) => {
	const withExtension = extendsValue.endsWith(".json") ? extendsValue : `${extendsValue}.json`;
	if (extendsValue.startsWith("./") || extendsValue.startsWith("../")) return path.resolve(fromConfigDirectory, withExtension);
	return path.join(fromConfigDirectory, "node_modules", withExtension);
};
const parsePathsField = (pathsField) => {
	const paths = /* @__PURE__ */ new Map();
	if (!isObjectRecord(pathsField)) return paths;
	for (const [pattern, targets] of Object.entries(pathsField)) {
		if (!Array.isArray(targets)) continue;
		const stringTargets = targets.filter((target) => typeof target === "string");
		if (stringTargets.length > 0) paths.set(pattern, stringTargets);
	}
	return paths;
};
const readResolvedTsconfig = (configFilePath, extendsDepth) => {
	const parsed = parseTsconfigFile(configFilePath);
	if (!parsed) return null;
	const configDirectory = path.dirname(configFilePath);
	const compilerOptions = isObjectRecord(parsed.compilerOptions) ? parsed.compilerOptions : {};
	const baseUrlValue = typeof compilerOptions.baseUrl === "string" ? compilerOptions.baseUrl : null;
	const hasExplicitBaseUrl = baseUrlValue !== null;
	const baseAbsolutePath = baseUrlValue !== null ? path.resolve(configDirectory, baseUrlValue) : configDirectory;
	if (isObjectRecord(compilerOptions.paths)) return {
		baseAbsolutePath,
		hasExplicitBaseUrl,
		paths: parsePathsField(compilerOptions.paths)
	};
	if (typeof parsed.extends === "string" && extendsDepth < 8) {
		const parentPath = resolveExtendsPath(parsed.extends, configDirectory);
		const inherited = parentPath ? readResolvedTsconfig(parentPath, extendsDepth + 1) : null;
		if (inherited) return inherited;
	}
	return hasExplicitBaseUrl ? {
		baseAbsolutePath,
		hasExplicitBaseUrl,
		paths: /* @__PURE__ */ new Map()
	} : null;
};
const configByFilePath = /* @__PURE__ */ new Map();
const loadTsconfigCached = (configFilePath) => {
	let fileStat;
	try {
		fileStat = fs.statSync(configFilePath);
	} catch {
		return null;
	}
	const cached = configByFilePath.get(configFilePath);
	if (cached && cached.mtimeMs === fileStat.mtimeMs) return cached.config;
	const config = readResolvedTsconfig(configFilePath, 0);
	configByFilePath.set(configFilePath, {
		mtimeMs: fileStat.mtimeMs,
		config
	});
	return config;
};
const findNearestTsconfig = (fromDirectory) => {
	let currentDirectory = fromDirectory;
	for (let level = 0; level < 30; level++) {
		for (const fileName of TSCONFIG_FILE_NAMES) {
			const candidate = loadTsconfigCached(path.join(currentDirectory, fileName));
			if (candidate) return candidate;
		}
		const parentDirectory = path.dirname(currentDirectory);
		if (parentDirectory === currentDirectory) break;
		currentDirectory = parentDirectory;
	}
	return null;
};
const matchPathPattern = (source, pattern) => {
	const starIndex = pattern.indexOf("*");
	if (starIndex === -1) return source === pattern ? "" : null;
	const prefix = pattern.slice(0, starIndex);
	const suffix = pattern.slice(starIndex + 1);
	if (source.length >= prefix.length + suffix.length && source.startsWith(prefix) && source.endsWith(suffix)) return source.slice(prefix.length, source.length - suffix.length);
	return null;
};
const resolveTsconfigAliasPath = (fromFilename, source) => {
	const config = findNearestTsconfig(path.dirname(fromFilename));
	if (!config) return null;
	let bestPattern = null;
	let bestCapture = "";
	let bestPrefixLength = -1;
	for (const pattern of config.paths.keys()) {
		const capture = matchPathPattern(source, pattern);
		if (capture === null) continue;
		const starIndex = pattern.indexOf("*");
		const prefixLength = starIndex === -1 ? pattern.length : starIndex;
		if (prefixLength > bestPrefixLength) {
			bestPattern = pattern;
			bestCapture = capture;
			bestPrefixLength = prefixLength;
		}
	}
	if (bestPattern) for (const target of config.paths.get(bestPattern) ?? []) {
		const substituted = target.replaceAll("*", bestCapture);
		const resolved = resolveModuleFileFromAbsolutePath(path.resolve(config.baseAbsolutePath, substituted));
		if (resolved) return resolved;
	}
	if (config.hasExplicitBaseUrl) return resolveModuleFileFromAbsolutePath(path.resolve(config.baseAbsolutePath, source));
	return null;
};
//#endregion
//#region src/plugin/utils/resolve-module-path.ts
const resolveModulePath = (fromFilename, source) => resolveRelativeImportPath(fromFilename, source) ?? resolveTsconfigAliasPath(fromFilename, source);
//#endregion
//#region src/plugin/utils/resolve-cross-file-function-export.ts
const resolveFunctionExportInFile = (filePath, exportedName, visitedFilePaths) => {
	if (visitedFilePaths.size >= 4) return null;
	if (visitedFilePaths.has(filePath)) return null;
	visitedFilePaths.add(filePath);
	const actualFilePath = resolveBarrelExportFilePath(filePath, exportedName) ?? filePath;
	const programRoot = parseSourceFile(actualFilePath);
	if (!programRoot) return null;
	const exported = findExportedFunctionBody(programRoot, exportedName);
	if (exported) return exported;
	for (const reExportSource of findReExportSourcesForName(programRoot, exportedName)) {
		const nextFilePath = resolveModulePath(actualFilePath, reExportSource);
		if (!nextFilePath) continue;
		const resolved = resolveFunctionExportInFile(nextFilePath, exportedName, visitedFilePaths);
		if (resolved) return resolved;
	}
	return null;
};
const resolveCrossFileFunctionExport = (fromFilename, source, exportedName) => {
	const resolvedFilePath = resolveModulePath(fromFilename, source);
	if (!resolvedFilePath) return null;
	return resolveFunctionExportInFile(resolvedFilePath, exportedName, /* @__PURE__ */ new Set());
};
//#endregion
//#region src/plugin/utils/ast-mentions-suspense.ts
const isSuspenseJsxOpeningName = (name) => {
	if (isNodeOfType(name, "JSXIdentifier")) return name.name === "Suspense";
	return isNodeOfType(name, "JSXMemberExpression") && isNodeOfType(name.property, "JSXIdentifier") && name.property.name === "Suspense";
};
const astMentionsSuspense = (programNode) => {
	let didDetect = false;
	walkAst(programNode, (child) => {
		if (didDetect) return false;
		if (isNodeOfType(child, "JSXOpeningElement") && isSuspenseJsxOpeningName(child.name)) {
			didDetect = true;
			return false;
		}
		if (isNodeOfType(child, "ImportDeclaration") && child.source?.value === "react") {
			if ((child.specifiers ?? []).some((specifier) => isNodeOfType(specifier, "ImportSpecifier") && getImportedName$1(specifier) === "Suspense")) {
				didDetect = true;
				return false;
			}
		}
	});
	return didDetect;
};
//#endregion
//#region src/plugin/utils/find-ancestor-suspense-layout.ts
const LAYOUT_FILE_NAMES = [
	"layout.tsx",
	"layout.jsx",
	"layout.ts",
	"layout.js"
];
const hasAncestorSuspenseLayout = (pageFilename) => {
	const normalizedPage = pageFilename.replaceAll("\\", "/");
	let currentDirectory = path.dirname(normalizedPage);
	for (let level = 0; level < 30; level++) {
		for (const layoutFileName of LAYOUT_FILE_NAMES) {
			const layoutPath = path.join(currentDirectory, layoutFileName);
			if (layoutPath.replaceAll("\\", "/") === normalizedPage) continue;
			const programRoot = parseSourceFile(layoutPath);
			if (programRoot && astMentionsSuspense(programRoot)) return true;
		}
		if (path.basename(currentDirectory) === "app") break;
		const parentDirectory = path.dirname(currentDirectory);
		if (parentDirectory === currentDirectory) break;
		currentDirectory = parentDirectory;
	}
	return false;
};
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-use-search-params-without-suspense.ts
const astContainsUseSearchParams = (root) => {
	let didFind = false;
	walkAst(root, (child) => {
		if (didFind) return false;
		if (isHookCall$1(child, "useSearchParams")) {
			didFind = true;
			return false;
		}
	});
	return didFind;
};
const isSuspenseJsxName = (name, suspenseLocalNames) => {
	if (isNodeOfType(name, "JSXIdentifier")) return name.name === "Suspense" || suspenseLocalNames.has(name.name);
	return isNodeOfType(name, "JSXMemberExpression") && isNodeOfType(name.property, "JSXIdentifier") && name.property.name === "Suspense";
};
const isInsideSuspenseBoundary = (node, suspenseLocalNames) => {
	let ancestor = node.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "JSXElement") && isSuspenseJsxName(ancestor.openingElement?.name, suspenseLocalNames)) return true;
		ancestor = ancestor.parent ?? null;
	}
	return false;
};
const collectSuspenseLocalNames = (programNode) => {
	const names = /* @__PURE__ */ new Set();
	for (const statement of programNode.body ?? []) {
		if (!isNodeOfType(statement, "ImportDeclaration")) continue;
		if (statement.source?.value !== "react") continue;
		for (const specifier of statement.specifiers ?? []) if (isNodeOfType(specifier, "ImportSpecifier") && getImportedName$1(specifier) === "Suspense" && specifier.local?.name) names.add(specifier.local.name);
	}
	return names;
};
const collectImportedComponents = (programNode) => {
	const entries = /* @__PURE__ */ new Map();
	for (const statement of programNode.body ?? []) {
		if (!isNodeOfType(statement, "ImportDeclaration")) continue;
		if (typeof statement.source?.value !== "string") continue;
		const source = statement.source.value;
		for (const specifier of statement.specifiers ?? []) {
			const localName = specifier.local?.name;
			if (!localName) continue;
			const exportedName = resolveImportedExportName(specifier);
			if (!exportedName) continue;
			entries.set(localName, {
				source,
				exportedName
			});
		}
	}
	return entries;
};
const nextjsNoUseSearchParamsWithoutSuspense = defineRule({
	id: "nextjs-no-use-search-params-without-suspense",
	title: "useSearchParams without Suspense",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Wrap the component using useSearchParams: `<Suspense fallback={<Skeleton />}><SearchComponent /></Suspense>`",
	create: (context) => {
		let isPageOrLayoutFile = false;
		let hasAncestorLayoutSuspense = false;
		let hasSuspenseInFile = false;
		let importedComponents = /* @__PURE__ */ new Map();
		let suspenseLocalNames = /* @__PURE__ */ new Set();
		return {
			Program(programNode) {
				const filename = normalizeFilename$1(context.filename ?? "");
				isPageOrLayoutFile = PAGE_OR_LAYOUT_FILE_PATTERN.test(filename);
				if (!isPageOrLayoutFile) return;
				hasAncestorLayoutSuspense = hasAncestorSuspenseLayout(context.filename ?? "");
				if (hasAncestorLayoutSuspense) return;
				hasSuspenseInFile = astMentionsSuspense(programNode);
				importedComponents = collectImportedComponents(programNode);
				suspenseLocalNames = collectSuspenseLocalNames(programNode);
			},
			CallExpression(node) {
				if (!isPageOrLayoutFile || hasAncestorLayoutSuspense || hasSuspenseInFile) return;
				if (!isHookCall$1(node, "useSearchParams")) return;
				context.report({
					node,
					message: "useSearchParams() without a <Suspense> boundary forces the whole page into client-side rendering."
				});
			},
			JSXOpeningElement(node) {
				if (!isPageOrLayoutFile || hasAncestorLayoutSuspense) return;
				if (!isNodeOfType(node.name, "JSXIdentifier")) return;
				const importEntry = importedComponents.get(node.name.name);
				if (!importEntry) return;
				const jsxElement = node.parent;
				if (!jsxElement) return;
				if (isInsideSuspenseBoundary(jsxElement, suspenseLocalNames)) return;
				const componentBody = resolveCrossFileFunctionExport(context.filename ?? "", importEntry.source, importEntry.exportedName);
				if (!componentBody || !astContainsUseSearchParams(componentBody)) return;
				context.report({
					node,
					message: `<${node.name.name}> uses useSearchParams() but is not wrapped in a <Suspense> boundary.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/nextjs/nextjs-no-vercel-og-import.ts
const nextjsNoVercelOgImport = defineRule({
	id: "nextjs-no-vercel-og-import",
	title: "@vercel/og import instead of next/og",
	tags: ["test-noise"],
	requires: ["nextjs"],
	severity: "warn",
	recommendation: "Use `import { ImageResponse } from \"next/og\"`. The `@vercel/og` package is built into Next.js and should not be imported directly",
	create: (context) => ({ ImportDeclaration(node) {
		if (node.source?.value !== "@vercel/og") return;
		context.report({
			node,
			message: "@vercel/og is bundled into Next.js. Import from \"next/og\" instead to avoid duplicate code and version mismatch."
		});
	} })
});
//#endregion
//#region src/plugin/rules/a11y/no-access-key.ts
const MESSAGE$31 = "Screen reader users can lose their shortcuts because `accessKey` clashes with them, so remove it.";
const isUndefinedIdentifier = (expression) => isNodeOfType(expression, "Identifier") && expression.name === "undefined";
const noAccessKey = defineRule({
	id: "no-access-key",
	title: "accessKey attribute used",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Do not use `accessKey`. It conflicts with assistive tech shortcuts.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const accessKey = hasJsxPropIgnoreCase(node.attributes, "accessKey");
		if (!accessKey) return;
		const attributeValue = accessKey.value;
		if (!attributeValue) return;
		if (isNodeOfType(attributeValue, "Literal") && typeof attributeValue.value === "string") {
			context.report({
				node: accessKey,
				message: MESSAGE$31
			});
			return;
		}
		if (isNodeOfType(attributeValue, "JSXExpressionContainer")) {
			const expression = attributeValue.expression;
			if (!expression || expression.type === "JSXEmptyExpression") return;
			if (isUndefinedIdentifier(expression)) return;
			context.report({
				node: accessKey,
				message: MESSAGE$31
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/utils/effect/constants.ts
const TYPESCRIPT_VISITOR_KEYS = {
	TSAsExpression: ["expression", "typeAnnotation"],
	TSNonNullExpression: ["expression"],
	TSSatisfiesExpression: ["expression", "typeAnnotation"],
	TSTypeAssertion: ["typeAnnotation", "expression"],
	TSInstantiationExpression: ["expression", "typeArguments"]
};
const VISITOR_KEYS = {
	...eslintVisitorKeys.KEYS,
	...TYPESCRIPT_VISITOR_KEYS
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/effect/get-program-analysis.ts
const programToAnalysis = /* @__PURE__ */ new WeakMap();
const stripAndRecordParents = (root) => {
	const restorations = [];
	const seen = /* @__PURE__ */ new WeakSet();
	const visit = (value) => {
		if (!value || typeof value !== "object") return;
		if (seen.has(value)) return;
		seen.add(value);
		if (Array.isArray(value)) {
			for (const item of value) visit(item);
			return;
		}
		const record = value;
		if (!("type" in record)) return;
		if ("parent" in record) {
			restorations.push({
				node: record,
				originalParent: record.parent
			});
			record.parent = null;
		}
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			visit(record[key]);
		}
	};
	visit(root);
	return restorations;
};
const restoreParents = (restorations) => {
	for (const restoration of restorations) restoration.node.parent = restoration.originalParent;
};
const getProgramAnalysis = (anyNode) => {
	const programNode = findProgramRoot(anyNode);
	if (!programNode) return null;
	const cached = programToAnalysis.get(programNode);
	if (cached) return cached;
	const restorations = stripAndRecordParents(programNode);
	let scopeManager;
	try {
		scopeManager = analyze(programNode, {
			ecmaVersion: 2024,
			sourceType: "module",
			childVisitorKeys: VISITOR_KEYS,
			fallback: "iteration"
		});
	} finally {
		restoreParents(restorations);
	}
	const analysis = {
		programNode,
		scopeManager
	};
	programToAnalysis.set(programNode, analysis);
	return analysis;
};
const getScopeForNode = (node, manager) => {
	if (!node.range) return null;
	let bestScope = null;
	let bestSize = Infinity;
	for (const scope of manager.scopes) {
		const block = scope.block;
		if (!block?.range) continue;
		if (node.range[0] < block.range[0] || node.range[1] > block.range[1]) continue;
		const size = block.range[1] - block.range[0];
		if (size <= bestSize) {
			bestSize = size;
			bestScope = scope;
		}
	}
	return bestScope;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/effect/ast.ts
const getChildKeys = (node) => VISITOR_KEYS[node.type] ?? Object.keys(node).filter((key) => key !== "parent");
const ascend = (analysis, ref, visit, visited = /* @__PURE__ */ new Set()) => {
	if (visited.has(ref)) return;
	const result = visit(ref);
	visited.add(ref);
	if (result === false) return;
	const defs = ref.resolved?.defs ?? [];
	for (const def of defs) {
		if (def.type === "ImportBinding") continue;
		if (def.type === "Parameter") continue;
		const defNode = def.node;
		const next = defNode.init ?? defNode.body;
		if (!next) continue;
		for (const innerRef of getDownstreamRefs(analysis, next)) ascend(analysis, innerRef, visit, visited);
	}
};
const descend = (node, visit, visited = /* @__PURE__ */ new Set()) => {
	if (visited.has(node)) return;
	visit(node);
	visited.add(node);
	const keys = getChildKeys(node);
	const record = node;
	for (const key of keys) {
		const child = record[key];
		if (!child) continue;
		if (Array.isArray(child)) {
			for (const item of child) if (item && isAstNode(item)) descend(item, visit, visited);
		} else if (isAstNode(child)) descend(child, visit, visited);
	}
};
const getUpstreamRefs = (analysis, ref) => {
	const refs = [];
	ascend(analysis, ref, (upRef) => {
		refs.push(upRef);
	});
	return refs;
};
const findDownstreamNodes = (topNode, type) => {
	const nodes = [];
	descend(topNode, (node) => {
		if (node.type === type) nodes.push(node);
	});
	return nodes;
};
const getRef = (analysis, identifier) => {
	const scope = getScopeForNode(identifier, analysis.scopeManager);
	if (!scope) return null;
	for (const reference of scope.references) if (reference.identifier === identifier) return reference;
	return null;
};
const downstreamRefsCache = /* @__PURE__ */ new WeakMap();
const getDownstreamRefs = (analysis, node) => {
	let perNode = downstreamRefsCache.get(analysis);
	if (!perNode) {
		perNode = /* @__PURE__ */ new WeakMap();
		downstreamRefsCache.set(analysis, perNode);
	}
	const cached = perNode.get(node);
	if (cached) return cached;
	const refs = [];
	for (const identifier of findDownstreamNodes(node, "Identifier")) {
		const ref = getRef(analysis, identifier);
		if (ref) refs.push(ref);
	}
	perNode.set(node, refs);
	return refs;
};
const getCallExpr = (ref, current = ref.identifier.parent) => {
	if (!current) return null;
	if (isNodeOfType(current, "CallExpression")) {
		let node = ref.identifier;
		let parent = node.parent;
		while (parent && isNodeOfType(parent, "MemberExpression")) {
			node = parent;
			parent = node.parent;
		}
		if (current.callee === node) return current;
	}
	if (isNodeOfType(current, "MemberExpression")) return getCallExpr(ref, current.parent);
	return null;
};
const getArgsUpstreamRefs = (analysis, ref) => {
	const result = [];
	for (const upRef of getUpstreamRefs(analysis, ref)) {
		const callExpr = getCallExpr(upRef);
		if (!callExpr || !isNodeOfType(callExpr, "CallExpression")) continue;
		for (const argument of callExpr.arguments ?? []) for (const argRef of getDownstreamRefs(analysis, argument)) for (const innerRef of getUpstreamRefs(analysis, argRef)) result.push(innerRef);
	}
	return result;
};
const isSynchronous = (node, within) => {
	if (!node) return false;
	if (node === within) return true;
	if (node.async === true) return false;
	if (isNodeOfType(node, "AwaitExpression") || isNodeOfType(node, "UnaryExpression") && node.operator === "void" || isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression")) return false;
	return isSynchronous(node.parent, within);
};
const isEventualCallTo = (analysis, ref, predicate) => {
	const callExprRefs = [];
	ascend(analysis, ref, (upRef) => {
		if (getCallExpr(upRef)) callExprRefs.push(upRef);
		else return false;
	});
	return callExprRefs.some(predicate);
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/effect/react.ts
const getOuterScopeContaining = (analysis, node) => {
	if (!node.range) return null;
	let best = null;
	let bestSize = Infinity;
	for (const scope of analysis.scopeManager.scopes) {
		const block = scope.block;
		if (!block?.range) continue;
		if (node.range[0] < block.range[0] || node.range[1] > block.range[1]) continue;
		const size = block.range[1] - block.range[0];
		if (size <= bestSize) {
			bestSize = size;
			best = scope;
		}
	}
	return best;
};
const KNOWN_PURE_HOC_NAMES = new Set(["memo", "forwardRef"]);
const startsWithUppercase = (name) => Boolean(name && name.length > 0 && name[0] >= "A" && name[0] <= "Z");
const isReactFunctionalComponent = (node) => {
	if (!node) return false;
	if (isNodeOfType(node, "FunctionDeclaration")) return Boolean(node.id && startsWithUppercase(node.id.name));
	if (isNodeOfType(node, "VariableDeclarator")) {
		if (!isNodeOfType(node.id, "Identifier")) return false;
		if (!startsWithUppercase(node.id.name)) return false;
		const init = node.init;
		if (!init) return false;
		return isNodeOfType(init, "ArrowFunctionExpression") || isNodeOfType(init, "CallExpression");
	}
	return false;
};
const isReactFunctionalHOC = (analysis, node) => {
	if (!isReactFunctionalComponent(node)) return false;
	if (!isNodeOfType(node, "VariableDeclarator")) return false;
	const init = node.init;
	if (!init) return false;
	const isWrappedInline = () => {
		if (!isNodeOfType(init, "CallExpression")) return false;
		if (!isNodeOfType(init.callee, "Identifier")) return false;
		if (KNOWN_PURE_HOC_NAMES.has(init.callee.name)) return false;
		const firstArg = init.arguments?.[0];
		if (!firstArg) return false;
		return isNodeOfType(firstArg, "ArrowFunctionExpression") || isNodeOfType(firstArg, "FunctionExpression");
	};
	const isWrappedSeparately = () => {
		if (!isNodeOfType(node.id, "Identifier")) return false;
		const bindingName = node.id.name;
		const containingScope = getOuterScopeContaining(analysis, node);
		if (!containingScope) return false;
		const variable = containingScope.variables.find((v) => v.name === bindingName);
		if (!variable) return false;
		for (const reference of variable.references) {
			const parent = reference.identifier.parent;
			if (!parent || !isNodeOfType(parent, "CallExpression")) continue;
			const args = parent.arguments ?? [];
			const refId = reference.identifier;
			if (!args.includes(refId)) continue;
			const callee = parent.callee;
			const calleeName = isNodeOfType(callee, "Identifier") ? callee.name : isNodeOfType(callee, "CallExpression") && isNodeOfType(callee.callee, "Identifier") ? callee.callee.name : null;
			if (calleeName != null && !KNOWN_PURE_HOC_NAMES.has(calleeName)) return true;
		}
		return false;
	};
	return isWrappedInline() || isWrappedSeparately();
};
const isCustomHook = (node) => {
	if (!node) return false;
	if (isNodeOfType(node, "FunctionDeclaration")) {
		const name = node.id?.name;
		if (!name) return false;
		return name.startsWith("use") && name.length > 3 && name[3] >= "A" && name[3] <= "Z";
	}
	if (isNodeOfType(node, "VariableDeclarator")) {
		if (!isNodeOfType(node.id, "Identifier")) return false;
		const name = node.id.name;
		const init = node.init;
		if (!init) return false;
		if (!isNodeOfType(init, "ArrowFunctionExpression") && !isNodeOfType(init, "FunctionExpression")) return false;
		return name.startsWith("use") && name.length > 3 && name[3] >= "A" && name[3] <= "Z";
	}
	return false;
};
const isReactNamedImportReference = (ref, importedName) => Boolean(ref?.resolved?.defs.some((def) => {
	if (def.type !== "ImportBinding") return false;
	const declarationNode = def.node;
	if (!isNodeOfType(declarationNode, "ImportSpecifier")) return false;
	const imported = declarationNode.imported;
	if (!isNodeOfType(imported, "Identifier")) return false;
	if (imported.name !== importedName) return false;
	const importDeclaration = declarationNode.parent;
	return Boolean(importDeclaration && isNodeOfType(importDeclaration, "ImportDeclaration") && isNodeOfType(importDeclaration.source, "Literal") && importDeclaration.source.value === "react");
}));
const isHookCallee = (analysis, node, hookName) => {
	if (!node) return false;
	if (isNodeOfType(node, "Identifier")) {
		if (node.name === hookName) return true;
		if (isReactNamedImportReference(getRef(analysis, node), hookName)) return true;
		const parent = node.parent;
		if (parent && isNodeOfType(parent, "MemberExpression") && isNodeOfType(parent.object, "Identifier") && parent.object.name === "React" && isNodeOfType(parent.property, "Identifier") && parent.property.name === hookName) return true;
		return false;
	}
	if (isNodeOfType(node, "MemberExpression")) return isNodeOfType(node.object, "Identifier") && node.object.name === "React" && isNodeOfType(node.property, "Identifier") && node.property.name === hookName;
	return false;
};
const isUseEffect = (node) => {
	if (!node || !isNodeOfType(node, "CallExpression")) return false;
	const callee = node.callee;
	if (isNodeOfType(callee, "Identifier") && callee.name === "useEffect") return true;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && callee.object.name === "React" && isNodeOfType(callee.property, "Identifier") && callee.property.name === "useEffect") return true;
	return false;
};
const getEffectFn = (analysis, node) => {
	if (!isNodeOfType(node, "CallExpression")) return null;
	const fn = node.arguments?.[0];
	if (!fn) return null;
	if (isNodeOfType(fn, "ArrowFunctionExpression") || isNodeOfType(fn, "FunctionExpression")) return fn;
	if (isNodeOfType(fn, "Identifier")) {
		const definitionNode = getRef(analysis, fn)?.resolved?.defs[0]?.node;
		if (definitionNode && isFunctionLike$2(definitionNode)) return definitionNode;
		if (definitionNode && isNodeOfType(definitionNode, "VariableDeclarator")) {
			const initializer = definitionNode.init;
			if (isNodeOfType(initializer, "ArrowFunctionExpression") || isNodeOfType(initializer, "FunctionExpression")) return initializer;
		}
	}
	return null;
};
const getEffectFnRefs = (analysis, node) => {
	const fn = getEffectFn(analysis, node);
	if (!fn) return null;
	return getDownstreamRefs(analysis, fn);
};
const getEffectDepsRefs = (analysis, node) => {
	if (!isNodeOfType(node, "CallExpression")) return null;
	const deps = node.arguments?.[1];
	if (!deps || !isNodeOfType(deps, "ArrayExpression")) return null;
	return getDownstreamRefs(analysis, deps);
};
const isState = (analysis, ref) => Boolean(ref.resolved?.defs.some((def) => {
	const node = def.node;
	if (!isNodeOfType(node, "VariableDeclarator")) return false;
	if (!isNodeOfType(node.init, "CallExpression")) return false;
	if (!isHookCallee(analysis, node.init.callee, "useState")) return false;
	if (!isNodeOfType(node.id, "ArrayPattern")) return false;
	const elements = node.id.elements ?? [];
	if (elements.length !== 1 && elements.length !== 2) return false;
	const first = elements[0];
	return Boolean(first && isNodeOfType(first, "Identifier") && first.name === ref.identifier.name);
}));
const isStateSetter = (analysis, ref) => Boolean(ref.resolved?.defs.some((def) => {
	const node = def.node;
	if (!isNodeOfType(node, "VariableDeclarator")) return false;
	if (!isNodeOfType(node.init, "CallExpression")) return false;
	if (!isHookCallee(analysis, node.init.callee, "useState")) return false;
	if (!isNodeOfType(node.id, "ArrayPattern")) return false;
	const elements = node.id.elements ?? [];
	if (elements.length !== 2) return false;
	const second = elements[1];
	return Boolean(second && isNodeOfType(second, "Identifier") && second.name === ref.identifier.name);
}));
const isProp = (analysis, ref) => Boolean(ref.resolved?.defs.some((def) => {
	if (def.type !== "Parameter") return false;
	const defNode = def.node;
	let declaringNode = defNode;
	if (isNodeOfType(defNode, "ArrowFunctionExpression")) {
		const parent = defNode.parent;
		if (parent && isNodeOfType(parent, "CallExpression")) declaringNode = parent.parent;
		else declaringNode = parent;
	}
	if (!declaringNode) return false;
	return isReactFunctionalComponent(declaringNode) && !isReactFunctionalHOC(analysis, declaringNode) || isCustomHook(declaringNode);
}));
const isIdentifierOrMemberExpression = (node) => isNodeOfType(node, "Identifier") || isNodeOfType(node, "MemberExpression");
const isPropAlias = (analysis, ref) => {
	if (isProp(analysis, ref)) return true;
	return Boolean(ref.resolved?.defs.some((def) => {
		const node = def.node;
		if (!isNodeOfType(node, "VariableDeclarator")) return false;
		const initializer = node.init;
		if (!initializer) return false;
		if (!isNodeOfType(node.id, "ObjectPattern") && !isIdentifierOrMemberExpression(initializer)) return false;
		return getDownstreamRefs(analysis, initializer).some((initializerRef) => getUpstreamRefs(analysis, initializerRef).some((upstreamRef) => isProp(analysis, upstreamRef)));
	}));
};
const isConstant = (ref) => Boolean((ref.resolved?.defs ?? []).some((def) => {
	const node = def.node;
	if (!isNodeOfType(node, "VariableDeclarator")) return false;
	const init = node.init;
	if (!init) return false;
	return isNodeOfType(init, "Literal") || isNodeOfType(init, "TemplateLiteral") || isNodeOfType(init, "ArrayExpression") || isNodeOfType(init, "ObjectExpression");
}));
const isRef = (analysis, ref) => Boolean(ref.resolved?.defs.some((def) => {
	const node = def.node;
	if (!isNodeOfType(node, "VariableDeclarator")) return false;
	if (!isNodeOfType(node.init, "CallExpression")) return false;
	return isHookCallee(analysis, node.init.callee, "useRef");
}));
const isRefCurrent = (ref) => {
	const parent = ref.identifier.parent;
	if (!parent || !isNodeOfType(parent, "MemberExpression")) return false;
	if (!isNodeOfType(parent.property, "Identifier")) return false;
	return parent.property.name === "current";
};
const isStateSetterCall = (analysis, ref) => isEventualCallTo(analysis, ref, (innerRef) => isStateSetter(analysis, innerRef));
const isPropCall = (analysis, ref) => isEventualCallTo(analysis, ref, (innerRef) => isPropAlias(analysis, innerRef));
const isRefCall = (analysis, ref) => isEventualCallTo(analysis, ref, (innerRef) => isRefCurrent(innerRef) || isRef(analysis, innerRef));
const getUseStateDecl = (analysis, ref) => {
	let node = getUpstreamRefs(analysis, ref).find((upRef) => isHookCallee(analysis, upRef.identifier, "useState"))?.identifier;
	while (node && !isNodeOfType(node, "VariableDeclarator")) node = node.parent;
	return node ?? null;
};
const isCleanupReturnArgument = (analysis, node) => {
	if (isFunctionLike$2(node)) return true;
	if (isNodeOfType(node, "MemberExpression")) return true;
	if (isNodeOfType(node, "Identifier")) {
		const definitionNode = getRef(analysis, node)?.resolved?.defs[0]?.node;
		if (definitionNode && isFunctionLike$2(definitionNode)) return true;
		if (definitionNode && isNodeOfType(definitionNode, "VariableDeclarator")) {
			const initializer = definitionNode.init;
			return isFunctionLike$2(initializer);
		}
	}
	if (isNodeOfType(node, "ConditionalExpression")) return isCleanupReturnArgument(analysis, node.consequent) || isCleanupReturnArgument(analysis, node.alternate);
	return false;
};
const hasCleanupReturn = (analysis, node, visited = /* @__PURE__ */ new WeakSet()) => {
	if (visited.has(node)) return false;
	visited.add(node);
	if (isNodeOfType(node, "ReturnStatement") && node.argument != null) return isCleanupReturnArgument(analysis, node.argument);
	if (!isNodeOfType(node, "BlockStatement") && isFunctionLike$2(node)) return false;
	const record = node;
	for (const [key, value] of Object.entries(record)) {
		if (key === "parent") continue;
		if (Array.isArray(value)) {
			if (value.some((item) => isAstNode(item) && hasCleanupReturn(analysis, item, visited))) return true;
		} else if (isAstNode(value) && hasCleanupReturn(analysis, value, visited)) return true;
	}
	return false;
};
const hasCleanup = (analysis, node) => {
	const fn = getEffectFn(analysis, node);
	if (!isFunctionLike$2(fn)) return false;
	if (!isNodeOfType(fn.body, "BlockStatement")) return false;
	return hasCleanupReturn(analysis, fn.body);
};
const findContainingNode = (analysis, node) => {
	if (!node) return null;
	if (isReactFunctionalComponent(node) || isReactFunctionalHOC(analysis, node) || isCustomHook(node)) return node;
	const parent = node.parent;
	return findContainingNode(analysis, parent);
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-adjust-state-on-prop-change.ts
const noAdjustStateOnPropChange = defineRule({
	id: "no-adjust-state-on-prop-change",
	title: "State synced to a prop inside an effect",
	severity: "error",
	tags: ["test-noise"],
	recommendation: "Adjust the state inline during render with a `prev`-prop comparison (`if (prop !== prevProp) { setPrevProp(prop); setX(...); }`), or refactor to remove the duplicated state. Routing the adjustment through a useEffect forces an extra render with a stale UI between the two commits. See https://react.dev/learn/you-might-not-need-an-effect#adjusting-some-state-when-a-prop-changes",
	create: (context) => ({ CallExpression(node) {
		if (!isUseEffect(node)) return;
		const analysis = getProgramAnalysis(node);
		if (!analysis) return;
		const effectFnRefs = getEffectFnRefs(analysis, node);
		const depsRefs = getEffectDepsRefs(analysis, node);
		if (!effectFnRefs || !depsRefs) return;
		const effectFn = getEffectFn(analysis, node);
		if (!effectFn) return;
		if (!depsRefs.flatMap((ref) => getUpstreamRefs(analysis, ref)).some((ref) => isProp(analysis, ref))) return;
		for (const ref of effectFnRefs) {
			if (!isStateSetterCall(analysis, ref)) continue;
			if (!isSynchronous(ref.identifier, effectFn)) continue;
			const callExpr = getCallExpr(ref);
			if (!callExpr) continue;
			if (getArgsUpstreamRefs(analysis, ref).some((argRef) => isProp(analysis, argRef))) continue;
			context.report({
				node: callExpr,
				message: "Your users briefly see the wrong value when the prop changes."
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/a11y/no-aria-hidden-on-focusable.ts
const MESSAGE$30 = "Screen reader users tab to this focusable element but hear nothing because `aria-hidden` skips it, so remove `aria-hidden` or stop it being focusable.";
const noAriaHiddenOnFocusable = defineRule({
	id: "no-aria-hidden-on-focusable",
	title: "aria-hidden on focusable element",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Remove `aria-hidden` from focusable elements, or stop them being focusable.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const ariaHidden = hasJsxPropIgnoreCase(node.attributes, "aria-hidden");
		if (!ariaHidden) return;
		const value = ariaHidden.value;
		if (value) {
			if (isNodeOfType(value, "Literal") && value.value !== "true") return;
			if (isNodeOfType(value, "JSXExpressionContainer")) {
				const expression = value.expression;
				if (isNodeOfType(expression, "Literal") && !expression.value) return;
			}
		}
		const tag = getElementType(node, context.settings);
		const tabIndex = hasJsxPropIgnoreCase(node.attributes, "tabIndex");
		const tabIndexValue = tabIndex ? parseJsxValue(tabIndex.value ?? null) : null;
		if (tabIndexValue !== null && tabIndexValue < 0) return;
		const isExplicitlyFocusable = tabIndexValue !== null && tabIndexValue >= 0;
		const isImplicitlyFocusable = isInteractiveElement(tag, node);
		if (isExplicitlyFocusable || isImplicitlyFocusable) context.report({
			node: ariaHidden,
			message: MESSAGE$30
		});
	} })
});
//#endregion
//#region src/plugin/utils/is-all-literal-array-expression.ts
/**
* True when the node is an `ArrayExpression` whose elements are all
* primitive literals (string / number / boolean). Used by the two
* "no array-index key" rule variants to detect a `keys={[1, 2, 3]}`
* style stable-id array.
*/
const isAllLiteralArrayExpression = (node) => {
	if (!isNodeOfType(node, "ArrayExpression")) return false;
	const elements = node.elements ?? [];
	if (elements.length < 1) return false;
	for (const element of elements) {
		if (!element) return false;
		if (!isNodeOfType(element, "Literal")) return false;
		const value = element.value;
		if (typeof value !== "string" && typeof value !== "number" && typeof value !== "boolean") return false;
	}
	return true;
};
//#endregion
//#region src/plugin/utils/jsx-stateless-leaf.ts
const PURE_SVG_PRIMITIVE_TAGS = new Set([
	"circle",
	"ellipse",
	"g",
	"line",
	"path",
	"polygon",
	"polyline",
	"rect",
	"stop",
	"text",
	"tspan",
	"defs",
	"use",
	"mask",
	"marker",
	"linearGradient",
	"radialGradient",
	"clipPath",
	"filter",
	"feGaussianBlur",
	"feOffset",
	"feMerge",
	"feMergeNode",
	"feColorMatrix",
	"feFlood",
	"feComposite",
	"title",
	"desc"
]);
const STATELESS_HTML_LEAF_TAGS = new Set([
	"div",
	"span",
	"p",
	"h1",
	"h2",
	"h3",
	"h4",
	"h5",
	"h6",
	"header",
	"footer",
	"section",
	"article",
	"aside",
	"main",
	"nav",
	"li",
	"ul",
	"ol",
	"dl",
	"dt",
	"dd",
	"tr",
	"td",
	"th",
	"tbody",
	"thead",
	"tfoot",
	"table",
	"caption",
	"colgroup",
	"col",
	"strong",
	"em",
	"small",
	"b",
	"i",
	"u",
	"s",
	"mark",
	"del",
	"ins",
	"sub",
	"sup",
	"abbr",
	"cite",
	"code",
	"kbd",
	"samp",
	"pre",
	"blockquote",
	"q",
	"br",
	"hr",
	"wbr",
	"img",
	"picture",
	"figure",
	"figcaption",
	"label",
	"legend",
	"fieldset",
	"address",
	"time",
	"data",
	"var",
	"ruby",
	"rt",
	"rp",
	"bdi",
	"bdo"
]);
const STATEFUL_HTML_DESCENDANT_TAGS = new Set([
	"input",
	"textarea",
	"select",
	"option",
	"optgroup",
	"button",
	"form",
	"output",
	"progress",
	"meter",
	"video",
	"audio",
	"source",
	"track",
	"iframe",
	"embed",
	"object",
	"a",
	"details",
	"summary",
	"dialog",
	"canvas"
]);
const STATEFUL_DESCENDANT_SCAN_BUDGET = 200;
const containsStatefulDescendant = (jsxElement) => {
	let budget = STATEFUL_DESCENDANT_SCAN_BUDGET;
	const stack = [jsxElement];
	while (stack.length > 0) {
		if (budget <= 0) return true;
		budget -= 1;
		const node = stack.pop();
		if (isNodeOfType(node, "JSXElement")) {
			const name = node.openingElement.name;
			if (name && isNodeOfType(name, "JSXIdentifier")) {
				const tagName = name.name;
				const firstChar = tagName.charCodeAt(0);
				if (firstChar >= 65 && firstChar <= 90) return true;
				if (STATEFUL_HTML_DESCENDANT_TAGS.has(tagName)) return true;
			}
			if (name && isNodeOfType(name, "JSXMemberExpression")) return true;
			const children = node.children ?? [];
			for (const child of children) stack.push(child);
			continue;
		}
		if (isNodeOfType(node, "JSXFragment")) {
			const children = node.children ?? [];
			for (const child of children) stack.push(child);
			continue;
		}
		if (isNodeOfType(node, "JSXExpressionContainer")) {
			const expression = node.expression;
			if (isNodeOfType(expression, "CallExpression") || isNodeOfType(expression, "Identifier") || isNodeOfType(expression, "MemberExpression")) return true;
			stack.push(expression);
			continue;
		}
		if (isNodeOfType(node, "ConditionalExpression")) {
			stack.push(node.consequent, node.alternate);
			continue;
		}
		if (isNodeOfType(node, "LogicalExpression")) {
			stack.push(node.left, node.right);
			continue;
		}
	}
	return false;
};
//#endregion
//#region src/plugin/rules/correctness/no-array-index-as-key.ts
const STRING_COERCION_FUNCTIONS = new Set(["String", "Number"]);
const extractIndexName = (node) => {
	if (isNodeOfType(node, "Identifier") && INDEX_PARAMETER_NAMES.has(node.name)) return node.name;
	if (isNodeOfType(node, "TemplateLiteral")) {
		for (const expression of node.expressions ?? []) if (isNodeOfType(expression, "Identifier") && INDEX_PARAMETER_NAMES.has(expression.name)) return expression.name;
	}
	if (isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.object, "Identifier") && INDEX_PARAMETER_NAMES.has(node.callee.object.name) && isNodeOfType(node.callee.property, "Identifier") && node.callee.property.name === "toString") return node.callee.object.name;
	if (isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "Identifier") && STRING_COERCION_FUNCTIONS.has(node.callee.name) && isNodeOfType(node.arguments?.[0], "Identifier") && INDEX_PARAMETER_NAMES.has(node.arguments[0].name)) return node.arguments[0].name;
	if (isNodeOfType(node, "BinaryExpression") && node.operator === "+" && (isNodeOfType(node.left, "Identifier") && INDEX_PARAMETER_NAMES.has(node.left.name) && isNodeOfType(node.right, "Literal") && node.right.value === "" || isNodeOfType(node.right, "Identifier") && INDEX_PARAMETER_NAMES.has(node.right.name) && isNodeOfType(node.left, "Literal") && node.left.value === "")) {
		if (isNodeOfType(node.left, "Identifier")) return node.left.name;
		if (isNodeOfType(node.right, "Identifier")) return node.right.name;
		return null;
	}
	return null;
};
const isNumericLiteralOrUndefined = (node) => {
	if (!node) return false;
	if (isNodeOfType(node, "Literal") && typeof node.value === "number") return true;
	if (isNodeOfType(node, "Identifier") && node.name === "undefined") return true;
	return false;
};
const isArrayConstructorCallWithNumericLength = (node) => {
	if (!node) return false;
	if (isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "Identifier") && node.callee.name === "Array" && isNumericLiteralOrUndefined(node.arguments?.[0])) return true;
	if (isNodeOfType(node, "NewExpression") && isNodeOfType(node.callee, "Identifier") && node.callee.name === "Array" && isNumericLiteralOrUndefined(node.arguments?.[0])) return true;
	return false;
};
const isArrayFromCall = (node) => {
	if (!node) return false;
	if (!isNodeOfType(node, "CallExpression")) return false;
	const callee = node.callee;
	return Boolean(isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && callee.object.name === "Array" && isNodeOfType(callee.property, "Identifier") && callee.property.name === "from");
};
/**
* True if every element of an ArrayExpression is a primitive constant
* (number/string/boolean literal) — `[1, 2, 3]`, `['a', 'b']`. Such arrays
* have a fixed order at every render, so an index key is stable.
*/
/**
* True if the call expression looks like a placeholder constructor whose
* elements have no identity beyond their position — i.e. `Array.from(...)`,
* `Array(N)`, `new Array(N)`, `Array(N).fill(...)`, `[...Array(N)]`, or
* a literal-only array `[1, 2, 3]` / `['a', 'b']`.
*
* Used both for `<receiver>.map(...)` and for `Array.from(<length>, fn)`.
*/
const isStaticPlaceholderReceiver = (receiver) => {
	if (isArrayFromCall(receiver)) return true;
	if (isArrayConstructorCallWithNumericLength(receiver)) return true;
	if (isAllLiteralArrayExpression(receiver)) return true;
	if (isNodeOfType(receiver, "CallExpression")) {
		const callee = receiver.callee;
		if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && callee.property.name === "fill" && isArrayConstructorCallWithNumericLength(callee.object)) return true;
	}
	if (isNodeOfType(receiver, "ArrayExpression") && receiver.elements?.length === 1) {
		const only = receiver.elements[0];
		if (only && isNodeOfType(only, "SpreadElement")) {
			const arg = only.argument;
			if (isArrayConstructorCallWithNumericLength(arg)) return true;
			if (isArrayFromCall(arg)) return true;
		}
	}
	return false;
};
const isArrayFromLengthObjectCall = (node) => {
	if (!isArrayFromCall(node)) return false;
	if (!isNodeOfType(node, "CallExpression")) return false;
	const first = node.arguments?.[0];
	if (!first || !isNodeOfType(first, "ObjectExpression")) return false;
	for (const prop of first.properties ?? []) {
		if (!isNodeOfType(prop, "Property")) continue;
		const key = prop.key;
		if (!(isNodeOfType(key, "Identifier") && key.name === "length" || isNodeOfType(key, "Literal") && key.value === "length")) continue;
		if (isNumericLiteralOrUndefined(prop.value)) return true;
		if (isNodeOfType(prop.value, "Identifier")) return true;
	}
	return false;
};
const isInsideStaticPlaceholderMap = (node) => {
	let current = node;
	while (current.parent) {
		const parent = current.parent;
		if (isFunctionLike$2(current) && isNodeOfType(parent, "CallExpression") && parent.arguments.includes(current)) {
			const callee = parent.callee;
			if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && (callee.property.name === "map" || callee.property.name === "flatMap" || callee.property.name === "forEach")) return isStaticPlaceholderReceiver(callee.object);
			if (isArrayFromCall(parent) && parent.arguments.length >= 2 && parent.arguments[1] === current) return isArrayFromLengthObjectCall(parent);
		}
		current = parent;
	}
	return false;
};
/**
* Walk up from a JSXAttribute node looking for the enclosing iterator
* callback (`.map(cb)`, `.flatMap(cb)`, `.forEach(cb)`, `Array.from(_, cb)`)
* and return the first parameter's name. The first param is the per-item
* value, e.g. `item` in `arr.map((item, index) => …)`.
*/
const findIteratorItemName$1 = (node) => {
	let current = node;
	while (current.parent) {
		const parent = current.parent;
		if (isFunctionLike$2(current) && isNodeOfType(parent, "CallExpression") && parent.arguments.includes(current)) {
			const callee = parent.callee;
			const isIteratorMethodCall = isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && (callee.property.name === "map" || callee.property.name === "flatMap" || callee.property.name === "forEach");
			const isArrayFromCallback = isArrayFromCall(parent) && parent.arguments.length >= 2 && parent.arguments[1] === current;
			if (isIteratorMethodCall || isArrayFromCallback) {
				const first = (current.params ?? [])[0];
				if (first && isNodeOfType(first, "Identifier")) return first.name;
				return null;
			}
		}
		current = parent;
	}
	return null;
};
const templateLiteralHasIteratorIdentity = (template, itemName) => {
	for (const expression of template.expressions ?? []) {
		if (isNodeOfType(expression, "Identifier") && expression.name === itemName) return true;
		if (isNodeOfType(expression, "MemberExpression") && isNodeOfType(expression.object, "Identifier") && expression.object.name === itemName) return true;
	}
	return false;
};
/**
* True when the JSX key value is a template literal mixing an index with at
* least one stable per-item identifier (e.g. `${item.id}-${index}`). Common
* defensive pattern in user code — the index is just a uniqueness fallback,
* the real identity is `item.id`.
*/
const isCompositeKeyWithIteratorIdentity = (keyExpression, attributeNode) => {
	if (!isNodeOfType(keyExpression, "TemplateLiteral")) return false;
	if ((keyExpression.expressions ?? []).length < 2) return false;
	const itemName = findIteratorItemName$1(attributeNode);
	if (!itemName) return false;
	return templateLiteralHasIteratorIdentity(keyExpression, itemName);
};
const noArrayIndexAsKey = defineRule({
	id: "no-array-index-as-key",
	title: "Array index used as a key",
	severity: "warn",
	recommendation: "Use a stable id from the item, like `key={item.id}` or `key={item.slug}`. Index keys break when the list reorders or filters.",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "key") return;
		if (!node.value || !isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const indexName = extractIndexName(node.value.expression);
		if (!indexName) return;
		if (isInsideStaticPlaceholderMap(node)) return;
		if (isCompositeKeyWithIteratorIdentity(node.value.expression, node)) return;
		const openingElement = node.parent;
		if (openingElement && isNodeOfType(openingElement, "JSXOpeningElement")) {
			const elementName = openingElement.name;
			if (isNodeOfType(elementName, "JSXIdentifier")) {
				if (elementName.name === "Fragment") return;
				if (PURE_SVG_PRIMITIVE_TAGS.has(elementName.name)) return;
				if (STATELESS_HTML_LEAF_TAGS.has(elementName.name)) {
					const jsxElement = openingElement.parent;
					if (jsxElement && isNodeOfType(jsxElement, "JSXElement")) {
						if (!containsStatefulDescendant(jsxElement)) return;
					}
				}
			}
			if (isNodeOfType(elementName, "JSXMemberExpression") && isNodeOfType(elementName.object, "JSXIdentifier") && isNodeOfType(elementName.property, "JSXIdentifier") && elementName.object.name === "React" && elementName.property.name === "Fragment") return;
		}
		context.report({
			node,
			message: `Your users can see & submit the wrong data when this list reorders or filters, so use a stable id like \`key={item.id}\`, not the array index "${indexName}".`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-array-index-key.ts
const MESSAGE$29 = "Your users can see & submit the wrong data when this list reorders.";
const SECOND_INDEX_METHODS = new Set([
	"every",
	"filter",
	"find",
	"findIndex",
	"flatMap",
	"forEach",
	"map",
	"some"
]);
const THIRD_INDEX_METHODS = new Set(["reduce", "reduceRight"]);
const isPositionallyStableIterationReceiver = (receiver) => {
	if (isAllLiteralArrayExpression(receiver)) return true;
	if (isNodeOfType(receiver, "ArrayExpression") && receiver.elements?.length === 1) {
		const only = receiver.elements[0];
		if (only && isNodeOfType(only, "SpreadElement")) {
			const arg = only.argument;
			if (arg && isPositionallyStableIterationReceiver(arg)) return true;
		}
	}
	if (!isNodeOfType(receiver, "CallExpression")) return false;
	const callee = receiver.callee;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && callee.object.name === "Array" && isNodeOfType(callee.property, "Identifier") && callee.property.name === "from" && receiver.arguments.length >= 1 && isNodeOfType(receiver.arguments[0], "ObjectExpression")) return true;
	if (isNodeOfType(callee, "Identifier") && callee.name === "Array") return true;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && callee.property.name === "split") return true;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && (callee.property.name === "fill" || callee.property.name === "flat")) return isPositionallyStableIterationReceiver(callee.object);
	return false;
};
const templateHasIteratorMember = (templateLiteral, iteratorName) => {
	for (const expression of templateLiteral.expressions ?? []) {
		if (isNodeOfType(expression, "Identifier") && expression.name === iteratorName) return true;
		if (isNodeOfType(expression, "MemberExpression") && isNodeOfType(expression.object, "Identifier") && expression.object.name === iteratorName) return true;
	}
	return false;
};
const isArrayFromMapperCallback = (parentCall, callback) => {
	if (parentCall.arguments[1] !== callback) return false;
	const callee = parentCall.callee;
	return isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && callee.object.name === "Array" && isNodeOfType(callee.property, "Identifier") && callee.property.name === "from";
};
const isArrayFromSourcePositionallyStable = (source) => {
	if (isNodeOfType(source, "ObjectExpression")) {
		for (const property of source.properties ?? []) {
			if (!isNodeOfType(property, "Property")) continue;
			const key = property.key;
			if (isNodeOfType(key, "Identifier") && key.name === "length" || isNodeOfType(key, "Literal") && key.value === "length") return true;
		}
		return false;
	}
	return isPositionallyStableIterationReceiver(source);
};
const findIteratorItemName = (node) => {
	let current = node;
	while (current) {
		if (isNodeOfType(current, "ArrowFunctionExpression") || isNodeOfType(current, "FunctionExpression")) {
			const parent = current.parent;
			const callbackItemName = readIteratorItemFromCallback(current, parent);
			if (callbackItemName !== void 0) return callbackItemName;
			if (current.params.length > 0) return null;
		}
		current = current.parent ?? null;
	}
	return null;
};
const readIteratorItemFromCallback = (callback, parent) => {
	if (!parent || !isNodeOfType(parent, "CallExpression")) return void 0;
	const callee = parent.callee;
	if (parent.arguments[0] === callback && isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) {
		const methodName = callee.property.name;
		if (SECOND_INDEX_METHODS.has(methodName)) {
			const item = callback.params[0];
			return item && isNodeOfType(item, "Identifier") ? item.name : null;
		}
		if (THIRD_INDEX_METHODS.has(methodName)) {
			const item = callback.params[1];
			return item && isNodeOfType(item, "Identifier") ? item.name : null;
		}
	}
	if (isArrayFromMapperCallback(parent, callback)) {
		const item = callback.params[0];
		return item && isNodeOfType(item, "Identifier") ? item.name : null;
	}
};
const findIndexParameterBinding = (node) => {
	let current = node.parent;
	while (current) {
		if (isNodeOfType(current, "ArrowFunctionExpression") || isNodeOfType(current, "FunctionExpression")) {
			const indexParam = readIteratorIndexFromCallback(current, current.parent);
			if (indexParam !== void 0) return indexParam;
			if (current.params.length > 0) return null;
		}
		current = current.parent ?? null;
	}
	return null;
};
const readIteratorIndexFromCallback = (callback, parent) => {
	if (!parent || !isNodeOfType(parent, "CallExpression")) return void 0;
	const callee = parent.callee;
	if (parent.arguments[0] === callback && isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) {
		const methodName = callee.property.name;
		let indexParamPosition = null;
		if (SECOND_INDEX_METHODS.has(methodName)) indexParamPosition = 1;
		else if (THIRD_INDEX_METHODS.has(methodName)) indexParamPosition = 2;
		if (indexParamPosition !== null) {
			const receiver = callee.object;
			if (isPositionallyStableIterationReceiver(receiver)) return null;
			const indexParam = callback.params[indexParamPosition];
			return indexParam && isNodeOfType(indexParam, "Identifier") ? indexParam : null;
		}
	}
	if (isArrayFromMapperCallback(parent, callback)) {
		const source = parent.arguments[0];
		if (source && isArrayFromSourcePositionallyStable(source)) return null;
		const indexParam = callback.params[1];
		return indexParam && isNodeOfType(indexParam, "Identifier") ? indexParam : null;
	}
};
const isIndexReference = (expression, paramName) => isNodeOfType(expression, "Identifier") && expression.name === paramName;
const expressionUsesIndex = (expression, paramName) => {
	if (isIndexReference(expression, paramName)) return true;
	if (isNodeOfType(expression, "TemplateLiteral")) return expression.expressions.some((innerExpression) => isIndexReference(innerExpression, paramName));
	if (isNodeOfType(expression, "BinaryExpression")) {
		const usesInLeft = isIndexReference(expression.left, paramName);
		const usesInRight = isIndexReference(expression.right, paramName);
		if (usesInLeft || usesInRight) return true;
		if (isNodeOfType(expression.left, "BinaryExpression") && expressionUsesIndex(expression.left, paramName)) return true;
		if (isNodeOfType(expression.right, "BinaryExpression") && expressionUsesIndex(expression.right, paramName)) return true;
		return false;
	}
	if (isNodeOfType(expression, "CallExpression")) {
		if (isNodeOfType(expression.callee, "MemberExpression") && isNodeOfType(expression.callee.property, "Identifier") && expression.callee.property.name === "toString" && isIndexReference(expression.callee.object, paramName)) return true;
		if (isNodeOfType(expression.callee, "Identifier") && expression.callee.name === "String" && expression.arguments.length > 0 && isIndexReference(expression.arguments[0], paramName)) return true;
	}
	return false;
};
const isReactCloneElement = (callExpression) => {
	const callee = callExpression.callee;
	if (!isNodeOfType(callee, "MemberExpression")) return false;
	if (!isNodeOfType(callee.property, "Identifier")) return false;
	if (callee.property.name !== "cloneElement") return false;
	return isNodeOfType(callee.object, "Identifier") && callee.object.name === "React";
};
const isPureSvgPrimitiveJsxName = (jsxOpeningName) => isNodeOfType(jsxOpeningName, "JSXIdentifier") && PURE_SVG_PRIMITIVE_TAGS.has(jsxOpeningName.name);
const isStatelessLeafJsxName = (jsxOpeningName) => isNodeOfType(jsxOpeningName, "JSXIdentifier") && STATELESS_HTML_LEAF_TAGS.has(jsxOpeningName.name);
const isFragmentJsxName = (jsxOpeningName) => {
	if (isNodeOfType(jsxOpeningName, "JSXIdentifier")) return jsxOpeningName.name === "Fragment";
	if (isNodeOfType(jsxOpeningName, "JSXMemberExpression") && isNodeOfType(jsxOpeningName.object, "JSXIdentifier") && isNodeOfType(jsxOpeningName.property, "JSXIdentifier") && jsxOpeningName.object.name === "React" && jsxOpeningName.property.name === "Fragment") return true;
	return false;
};
const noArrayIndexKey = defineRule({
	id: "no-array-index-key",
	title: "Array index used as a key",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Use a stable `key` from your data instead of the array index.",
	category: "Performance",
	create: (context) => ({
		JSXOpeningElement(node) {
			const keyAttribute = hasJsxPropIgnoreCase(node.attributes, "key");
			if (!keyAttribute) return;
			if (!keyAttribute.value || !isNodeOfType(keyAttribute.value, "JSXExpressionContainer")) return;
			const expression = keyAttribute.value.expression;
			if (expression.type === "JSXEmptyExpression") return;
			if (isFragmentJsxName(node.name)) return;
			if (isPureSvgPrimitiveJsxName(node.name)) return;
			const indexBinding = findIndexParameterBinding(node);
			if (!indexBinding) return;
			if (!expressionUsesIndex(expression, indexBinding.name)) return;
			if (isNodeOfType(expression, "TemplateLiteral")) {
				const itemName = findIteratorItemName(node);
				if (itemName && templateHasIteratorMember(expression, itemName)) return;
				const interpolations = expression.expressions ?? [];
				let interpolationsBeyondIndex = 0;
				for (const interpolation of interpolations) {
					if (isIndexReference(interpolation, indexBinding.name)) continue;
					interpolationsBeyondIndex += 1;
					if (interpolationsBeyondIndex >= 1) break;
				}
				if (interpolationsBeyondIndex >= 1) return;
			}
			if (isNodeOfType(expression, "BinaryExpression") && expression.operator === "+") {
				let interpolationsBeyondIndex = 0;
				const walkOperand = (operand) => {
					if (isNodeOfType(operand, "BinaryExpression") && operand.operator === "+") {
						walkOperand(operand.left);
						walkOperand(operand.right);
						return;
					}
					if (isIndexReference(operand, indexBinding.name)) return;
					if (isNodeOfType(operand, "Literal") && typeof operand.value === "string") return;
					interpolationsBeyondIndex += 1;
				};
				walkOperand(expression);
				if (interpolationsBeyondIndex >= 1) return;
			}
			if (isStatelessLeafJsxName(node.name)) {
				const jsxElement = node.parent;
				if (jsxElement && isNodeOfType(jsxElement, "JSXElement")) {
					if (!containsStatefulDescendant(jsxElement)) return;
				}
			}
			context.report({
				node: keyAttribute,
				message: MESSAGE$29
			});
		},
		CallExpression(node) {
			if (!isReactCloneElement(node)) return;
			if (node.arguments.length < 2 || node.arguments.length > 3) return;
			const propsArgument = node.arguments[1];
			if (!isNodeOfType(propsArgument, "ObjectExpression")) return;
			const indexBinding = findIndexParameterBinding(node);
			if (!indexBinding) return;
			for (const property of propsArgument.properties) {
				if (!isNodeOfType(property, "Property")) continue;
				if (property.computed) continue;
				const propKey = property.key;
				let propName = null;
				if (isNodeOfType(propKey, "Identifier")) propName = propKey.name;
				else if (isNodeOfType(propKey, "Literal") && typeof propKey.value === "string") propName = propKey.value;
				if (propName !== "key") continue;
				if (expressionUsesIndex(property.value, indexBinding.name)) context.report({
					node: property,
					message: MESSAGE$29
				});
			}
		}
	})
});
//#endregion
//#region src/plugin/rules/a11y/no-autofocus.ts
const MESSAGE$28 = "Screen reader & keyboard users get disoriented because `autoFocus` jumps focus on load, so remove it and let people choose where to focus.";
const resolveSettings$21 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { ignoreNonDOM: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noAutofocus ?? {} : {}).ignoreNonDOM ?? true };
};
const innerExpression = (expression) => {
	if (expression.type === "ParenthesizedExpression" && "expression" in expression) return innerExpression(expression.expression);
	return expression;
};
const isSameNameIdentifierForward = (attributeName, value) => {
	if (!value || !isNodeOfType(value, "JSXExpressionContainer")) return false;
	const expression = innerExpression(value.expression);
	if (isNodeOfType(expression, "Identifier") && expression.name === attributeName) return true;
	if (isNodeOfType(expression, "MemberExpression") && !expression.computed && isNodeOfType(expression.property, "Identifier") && expression.property.name === attributeName) return true;
	return false;
};
const isFalseAttributeValue = (value) => {
	if (isNodeOfType(value, "Literal")) return typeof value.value === "string" ? value.value === "false" : value.value === false;
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = innerExpression(value.expression);
		if (isNodeOfType(expression, "Literal")) {
			if (typeof expression.value === "boolean") return !expression.value;
			if (typeof expression.value === "string") return expression.value === "false";
			return false;
		}
		if (isNodeOfType(expression, "TemplateLiteral")) return getStaticTemplateLiteralValue(expression) === "false";
	}
	return false;
};
const noAutofocus = defineRule({
	id: "no-autofocus",
	title: "Autofocus on an element",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Do not use `autoFocus`. It disorients users on load.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$21(context.settings);
		const isTestlikeFile = isTestlikeFilename(context.filename);
		return { JSXOpeningElement(node) {
			if (isTestlikeFile) return;
			const autoFocusAttribute = node.attributes.find((attribute) => {
				if (!isNodeOfType(attribute, "JSXAttribute")) return false;
				const attributeName = attribute.name;
				return isNodeOfType(attributeName, "JSXIdentifier") && attributeName.name === "autoFocus";
			});
			if (!autoFocusAttribute) return;
			const attributeValue = autoFocusAttribute.value;
			if (attributeValue && isFalseAttributeValue(attributeValue)) return;
			if (isSameNameIdentifierForward("autoFocus", attributeValue)) return;
			if (settings.ignoreNonDOM) {
				const tag = getElementType(node, context.settings);
				if (!HTML_TAGS.has(tag)) return;
			}
			context.report({
				node: autoFocusAttribute,
				message: MESSAGE$28
			});
		} };
	}
});
//#endregion
//#region src/plugin/utils/create-relative-import-source.ts
const createRelativeImportSource = (filename, targetFilePath) => {
	const targetPathWithoutExtension = targetFilePath.slice(0, targetFilePath.length - path.extname(targetFilePath).length);
	const targetModulePath = path.basename(targetPathWithoutExtension) === "index" ? path.dirname(targetPathWithoutExtension) : targetPathWithoutExtension;
	const relativePath = path.relative(path.dirname(filename), targetModulePath).split(path.sep).join("/");
	return relativePath.startsWith(".") ? relativePath : `./${relativePath}`;
};
//#endregion
//#region src/plugin/rules/bundle-size/no-barrel-import.ts
const getLiteralName = (node) => {
	if (node.type === "Identifier" && typeof node.name === "string") return node.name;
	if (node.type === "Literal" && typeof node.value === "string") return node.value;
	return null;
};
const getRuntimeImportRequests = (node) => {
	if (node.importKind === "type") return [];
	return node.specifiers.flatMap((specifier) => {
		if (specifier.type === "ImportSpecifier") {
			if (specifier.importKind === "type") return [];
			return [{ importedName: getLiteralName(specifier.imported) }];
		}
		if (specifier.type === "ImportDefaultSpecifier") return [{ importedName: "default" }];
		return [{ importedName: null }];
	});
};
const buildReportMessage = (filename, barrelFilePath, importRequests) => {
	const directImportSources = /* @__PURE__ */ new Set();
	for (const request of importRequests) {
		if (!request.importedName) continue;
		const directFilePath = resolveBarrelExportFilePath(barrelFilePath, request.importedName);
		if (directFilePath) directImportSources.add(createRelativeImportSource(filename, directFilePath));
	}
	if (directImportSources.size === 1) {
		const [directImportSource] = directImportSources;
		return `This ships extra code to your users & slows page load. Import directly from "${directImportSource}".`;
	}
	if (directImportSources.size > 1) return `This ships extra code to your users & slows page load. Import directly from: ${[...directImportSources].map((source) => `"${source}"`).join(", ")}.`;
	return "Importing from an index file pulls in extra code. Import directly from the source file instead.";
};
const noBarrelImport = defineRule({
	id: "no-barrel-import",
	title: "Import from a barrel file",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Import from the direct path: `import { Button } from './components/Button'` instead of `./components`",
	create: (context) => {
		let didReportForFile = false;
		return { ImportDeclaration(node) {
			if (didReportForFile) return;
			const source = node.source?.value;
			if (typeof source !== "string" || !source.startsWith(".")) return;
			const filename = normalizeFilename$1(context.filename ?? "");
			if (!filename) return;
			const importRequests = getRuntimeImportRequests(node);
			if (importRequests.length === 0) return;
			const resolvedImportPath = resolveRelativeImportPath(filename, source);
			if (resolvedImportPath && isBarrelIndexModule(resolvedImportPath)) {
				didReportForFile = true;
				context.report({
					node,
					message: buildReportMessage(filename, resolvedImportPath, importRequests)
				});
			}
		} };
	}
});
//#endregion
//#region src/plugin/utils/is-setter-identifier.ts
const isSetterIdentifier = (name) => SETTER_PATTERN.test(name);
//#endregion
//#region src/plugin/utils/is-setter-call.ts
const isSetterCall = (node) => isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "Identifier") && isSetterIdentifier(node.callee.name);
//#endregion
//#region src/plugin/utils/is-hook-binding-in-scope.ts
const isHookBindingInScope = (node, query) => {
	const { bindingName, hookName, destructureIndex } = query;
	let cursor = node;
	while (cursor) {
		if (isNodeOfType(cursor, "BlockStatement") || isNodeOfType(cursor, "Program")) for (const statement of cursor.body ?? []) {
			if (!isNodeOfType(statement, "VariableDeclaration")) continue;
			for (const declarator of statement.declarations ?? []) {
				if (!isNodeOfType(declarator.init, "CallExpression")) continue;
				if (!isHookCall$1(declarator.init, hookName)) continue;
				if (destructureIndex !== void 0) {
					if (!isNodeOfType(declarator.id, "ArrayPattern")) continue;
					const elements = declarator.id.elements ?? [];
					if (elements.length <= destructureIndex) continue;
					const element = elements[destructureIndex];
					if (isNodeOfType(element, "Identifier") && element.name === bindingName) return true;
				} else if (isNodeOfType(declarator.id, "Identifier") && declarator.id.name === bindingName) return true;
			}
		}
		cursor = cursor.parent ?? null;
	}
	return false;
};
//#endregion
//#region src/plugin/utils/is-use-state-setter-in-scope.ts
const isUseStateSetterInScope = (node, setterName) => isHookBindingInScope(node, {
	bindingName: setterName,
	hookName: "useState",
	destructureIndex: 1
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-cascading-set-state.ts
const isAsyncFunctionLike = (node) => {
	if (isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "FunctionDeclaration")) return Boolean(node.async);
	return false;
};
const isTerminatingStatement = (statement) => isNodeOfType(statement, "BreakStatement") || isNodeOfType(statement, "ReturnStatement") || isNodeOfType(statement, "ThrowStatement") || isNodeOfType(statement, "ContinueStatement");
const countMaxPathSetStateCalls = (node) => {
	if (!node || typeof node !== "object") return 0;
	if (isAsyncFunctionLike(node)) return 0;
	if (isNodeOfType(node, "IfStatement")) {
		const thenCount = countMaxPathSetStateCalls(node.consequent);
		const elseCount = node.alternate ? countMaxPathSetStateCalls(node.alternate) : 0;
		return Math.max(thenCount, elseCount);
	}
	if (isNodeOfType(node, "ConditionalExpression")) return Math.max(countMaxPathSetStateCalls(node.consequent), countMaxPathSetStateCalls(node.alternate));
	if (isNodeOfType(node, "SwitchStatement")) {
		let maxRunSetters = 0;
		let currentRunSetters = 0;
		for (const switchCase of node.cases ?? []) {
			const consequent = switchCase.consequent ?? [];
			let caseSetters = 0;
			let runEnds = false;
			for (const statement of consequent) {
				caseSetters += countMaxPathSetStateCalls(statement);
				if (isTerminatingStatement(statement)) runEnds = true;
			}
			currentRunSetters += caseSetters;
			if (runEnds) {
				if (currentRunSetters > maxRunSetters) maxRunSetters = currentRunSetters;
				currentRunSetters = 0;
			}
		}
		if (currentRunSetters > maxRunSetters) maxRunSetters = currentRunSetters;
		return maxRunSetters;
	}
	if (isNodeOfType(node, "TryStatement")) {
		const tryCount = countMaxPathSetStateCalls(node.block);
		const catchCount = node.handler ? countMaxPathSetStateCalls(node.handler.body) : 0;
		const finallyCount = node.finalizer ? countMaxPathSetStateCalls(node.finalizer) : 0;
		return Math.max(tryCount, catchCount) + finallyCount;
	}
	if (isNodeOfType(node, "CallExpression") && isSetterCall(node) && isNodeOfType(node.callee, "Identifier") && isUseStateSetterInScope(node, node.callee.name)) {
		let nestedSettersInArgs = 0;
		for (const argument of node.arguments ?? []) nestedSettersInArgs += countMaxPathSetStateCalls(argument);
		return 1 + nestedSettersInArgs;
	}
	let total = 0;
	const nodeRecord = node;
	for (const key of Object.keys(nodeRecord)) {
		if (key === "parent") continue;
		const child = nodeRecord[key];
		if (Array.isArray(child)) {
			for (const item of child) if (item && typeof item === "object" && "type" in item) total += countMaxPathSetStateCalls(item);
		} else if (child && typeof child === "object" && "type" in child) total += countMaxPathSetStateCalls(child);
	}
	return total;
};
const isInitOnlyEffect = (node) => {
	const depsArg = node.arguments?.[1];
	if (!depsArg) return false;
	if (!isNodeOfType(depsArg, "ArrayExpression")) return false;
	return (depsArg.elements ?? []).length === 0;
};
const noCascadingSetState = defineRule({
	id: "no-cascading-set-state",
	title: "Multiple setState calls in one effect",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Combine into useReducer: `const [state, dispatch] = useReducer(reducer, initialState)`",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1)) return;
		if (isInitOnlyEffect(node)) return;
		const callback = getEffectCallback(node);
		if (!callback) return;
		const setStateCallCount = countMaxPathSetStateCalls(callback);
		if (setStateCallCount >= 3) context.report({
			node,
			message: `${setStateCallCount} setState calls in one useEffect redraw your screen each time they run together.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-chain-state-updates.ts
const noChainStateUpdates = defineRule({
	id: "no-chain-state-updates",
	title: "State updates chained through effects",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Set all the related state together in the event handler that starts it, instead of having one useEffect react to a state change and set more state. See https://react.dev/learn/you-might-not-need-an-effect#chains-of-computations",
	create: (context) => ({ CallExpression(node) {
		if (!isUseEffect(node)) return;
		const analysis = getProgramAnalysis(node);
		if (!analysis) return;
		if (hasCleanup(analysis, node)) return;
		const effectFnRefs = getEffectFnRefs(analysis, node);
		const depsRefs = getEffectDepsRefs(analysis, node);
		if (!effectFnRefs || !depsRefs) return;
		const effectFn = getEffectFn(analysis, node);
		if (!effectFn) return;
		if (!depsRefs.flatMap((ref) => getUpstreamRefs(analysis, ref)).some((ref) => isState(analysis, ref))) return;
		for (const ref of effectFnRefs) {
			if (!isStateSetterCall(analysis, ref)) continue;
			if (!isSynchronous(ref.identifier, effectFn)) continue;
			const callExpr = getCallExpr(ref);
			if (!callExpr) continue;
			if (getArgsUpstreamRefs(analysis, ref).some((argRef) => isState(analysis, argRef))) continue;
			context.report({
				node: callExpr,
				message: "Chaining state updates triggers an extra render each step."
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-children-prop.ts
const MESSAGE$27 = "Your component can render the wrong children when you pass them through a `children` prop.";
const noChildrenProp = defineRule({
	id: "no-children-prop",
	title: "Children passed as a prop",
	severity: "warn",
	recommendation: "Nest children between the tags instead of passing a `children` prop.",
	create: (context) => ({
		JSXAttribute(node) {
			if (!isNodeOfType(node.name, "JSXIdentifier")) return;
			if (node.name.name !== "children") return;
			context.report({
				node: node.name,
				message: MESSAGE$27
			});
		},
		CallExpression(node) {
			if (!isCreateElementCall(node)) return;
			const propsArgument = node.arguments[1];
			if (!propsArgument) return;
			if (!isNodeOfType(propsArgument, "ObjectExpression")) return;
			for (const property of propsArgument.properties) {
				if (!isNodeOfType(property, "Property")) continue;
				const propertyKey = property.key;
				if (isNodeOfType(propertyKey, "Identifier") && propertyKey.name === "children" || isNodeOfType(propertyKey, "Literal") && propertyKey.value === "children") context.report({
					node: propertyKey,
					message: MESSAGE$27
				});
			}
		}
	})
});
//#endregion
//#region src/plugin/rules/react-builtins/no-clone-element.ts
const MESSAGE$26 = "`React.cloneElement` breaks easily when the cloned element's props change.";
const noCloneElement = defineRule({
	id: "no-clone-element",
	title: "Use of cloneElement",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Pass children, render props, or use `Children.map` instead of cloning elements.",
	category: "Architecture",
	create: (context) => ({ CallExpression(node) {
		const callee = stripParenExpression(node.callee);
		if (isNodeOfType(callee, "Identifier") && callee.name === "cloneElement") {
			if (isImportedFromModule(node, "cloneElement", "react")) context.report({
				node: callee,
				message: MESSAGE$26
			});
			return;
		}
		if (isNodeOfType(callee, "MemberExpression")) {
			let propertyName = null;
			if (isNodeOfType(callee.property, "Identifier")) propertyName = callee.property.name;
			else if (isNodeOfType(callee.property, "Literal") && typeof callee.property.value === "string") propertyName = callee.property.value;
			if (propertyName !== "cloneElement") return;
			if (!isNodeOfType(callee.object, "Identifier")) return;
			if (!isImportedFromModule(node, callee.object.name, "react")) return;
			context.report({
				node: callee,
				message: MESSAGE$26
			});
		}
	} })
});
//#endregion
//#region src/plugin/utils/component-or-hook-display-name.ts
const hocWrapperCalleeName = (callee) => {
	if (isNodeOfType(callee, "Identifier")) return callee.name;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) return callee.property.name;
	return null;
};
const displayNameFromFunctionBinding = (functionNode) => {
	let current = functionNode;
	for (;;) {
		const parent = current.parent;
		if (parent && isNodeOfType(parent, "CallExpression") && parent.arguments?.[0] === current) {
			const calleeName = hocWrapperCalleeName(parent.callee);
			if (calleeName && COMPONENT_HOC_WRAPPER_NAMES.has(calleeName)) {
				current = parent;
				continue;
			}
		}
		break;
	}
	const binding = current.parent;
	if (binding && isNodeOfType(binding, "VariableDeclarator") && isNodeOfType(binding.id, "Identifier") && binding.init === current) return isReactComponentOrHookName(binding.id.name) ? binding.id.name : null;
	return null;
};
const componentOrHookDisplayNameForFunction = (functionNode) => {
	if ((isNodeOfType(functionNode, "FunctionDeclaration") || isNodeOfType(functionNode, "FunctionExpression")) && functionNode.id) return isReactComponentOrHookName(functionNode.id.name) ? functionNode.id.name : null;
	return displayNameFromFunctionBinding(functionNode);
};
const nearestEnclosingFunction = (node) => {
	let cursor = node.parent;
	while (cursor) {
		if (isFunctionLike$2(cursor)) return cursor;
		cursor = cursor.parent ?? null;
	}
	return null;
};
//#endregion
//#region src/plugin/utils/enclosing-component-or-hook-name.ts
const enclosingComponentOrHookName = (node) => {
	const functionNode = nearestEnclosingFunction(node);
	return functionNode ? componentOrHookDisplayNameForFunction(functionNode) : null;
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-create-context-in-render.ts
const MESSAGE$25 = "createContext() builds a new context every render, so every consumer gets cut off & resets.";
const CONTEXT_MODULES = [
	"react",
	"use-context-selector",
	"react-tracked"
];
const isCreateContextCallee = (callee) => {
	if (isNodeOfType(callee, "Identifier")) {
		for (const moduleName of CONTEXT_MODULES) if (getImportedNameFromModule(callee, callee.name, moduleName) === "createContext") return true;
		return false;
	}
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed) {
		const namespaceIdentifier = callee.object;
		const propertyIdentifier = callee.property;
		if (!isNodeOfType(namespaceIdentifier, "Identifier")) return false;
		if (!isNodeOfType(propertyIdentifier, "Identifier")) return false;
		if (propertyIdentifier.name !== "createContext") return false;
		const namespaceName = namespaceIdentifier.name;
		if (isCanonicalReactNamespaceName(namespaceName)) return true;
		for (const moduleName of CONTEXT_MODULES) if (isImportedFromModule(namespaceIdentifier, namespaceName, moduleName)) return true;
		return false;
	}
	return false;
};
const noCreateContextInRender = defineRule({
	id: "no-create-context-in-render",
	title: "createContext called during render",
	severity: "error",
	category: "Correctness",
	recommendation: "Move `createContext(...)` outside the component, to the top level of the file, so it stays the same on every render.",
	create: (context) => ({ CallExpression(node) {
		if (!isCreateContextCallee(node.callee)) return;
		const componentOrHookName = enclosingComponentOrHookName(node);
		if (!componentOrHookName) return;
		context.report({
			node,
			message: `${MESSAGE$25} (called inside "${componentOrHookName}")`
		});
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-create-store-in-render.ts
const STORE_FACTORIES = [
	{
		module: "zustand",
		exportedName: "create",
		humanLabel: "zustand.create"
	},
	{
		module: "zustand",
		exportedName: "createStore",
		humanLabel: "zustand.createStore"
	},
	{
		module: "zustand/vanilla",
		exportedName: "createStore",
		humanLabel: "zustand.createStore"
	},
	{
		module: "zustand/vanilla",
		exportedName: "create",
		humanLabel: "zustand.create"
	},
	{
		module: "redux",
		exportedName: "createStore",
		humanLabel: "redux.createStore"
	},
	{
		module: "@reduxjs/toolkit",
		exportedName: "configureStore",
		humanLabel: "@reduxjs/toolkit.configureStore"
	},
	{
		module: "@reduxjs/toolkit",
		exportedName: "createSlice",
		humanLabel: "createSlice"
	},
	{
		module: "jotai",
		exportedName: "atom",
		humanLabel: "jotai.atom"
	},
	{
		module: "jotai/vanilla",
		exportedName: "atom",
		humanLabel: "jotai.atom"
	},
	{
		module: "jotai",
		exportedName: "createStore",
		humanLabel: "jotai.createStore"
	},
	{
		module: "valtio",
		exportedName: "proxy",
		humanLabel: "valtio.proxy"
	},
	{
		module: "valtio/vanilla",
		exportedName: "proxy",
		humanLabel: "valtio.proxy"
	},
	{
		module: "mobx",
		exportedName: "observable",
		humanLabel: "mobx.observable"
	},
	{
		module: "mobx",
		exportedName: "makeAutoObservable",
		humanLabel: "mobx.makeAutoObservable"
	},
	{
		module: "mobx",
		exportedName: "makeObservable",
		humanLabel: "mobx.makeObservable"
	},
	{
		module: "nanostores",
		exportedName: "atom",
		humanLabel: "nanostores.atom"
	},
	{
		module: "nanostores",
		exportedName: "map",
		humanLabel: "nanostores.map"
	},
	{
		module: "@xstate/store",
		exportedName: "createStore",
		humanLabel: "@xstate/store.createStore"
	}
];
const STORE_FACTORY_LOOKUP = /* @__PURE__ */ new Map();
for (const factory of STORE_FACTORIES) {
	const bucket = STORE_FACTORY_LOOKUP.get(factory.exportedName) ?? [];
	STORE_FACTORY_LOOKUP.set(factory.exportedName, [...bucket, factory]);
}
const resolveStoreFactoryForCallee = (callee) => {
	if (isNodeOfType(callee, "Identifier")) {
		const localName = callee.name;
		for (const factoryBucket of STORE_FACTORY_LOOKUP.values()) for (const factory of factoryBucket) if (getImportedNameFromModule(callee, localName, factory.module) === factory.exportedName) return factory;
		return null;
	}
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed) {
		const namespaceIdentifier = callee.object;
		const propertyIdentifier = callee.property;
		if (!isNodeOfType(namespaceIdentifier, "Identifier")) return null;
		if (!isNodeOfType(propertyIdentifier, "Identifier")) return null;
		const propertyName = propertyIdentifier.name;
		const factoryBucket = STORE_FACTORY_LOOKUP.get(propertyName);
		if (!factoryBucket) return null;
		for (const factory of factoryBucket) if (isImportedFromModule(namespaceIdentifier, namespaceIdentifier.name, factory.module)) return factory;
		return null;
	}
	return null;
};
const noCreateStoreInRender = defineRule({
	id: "no-create-store-in-render",
	title: "Store created during render",
	severity: "error",
	category: "Correctness",
	recommendation: "Create the store, atom, or observable at the top level of the file, not inside a component or hook.",
	create: (context) => ({ CallExpression(node) {
		const factory = resolveStoreFactoryForCallee(node.callee);
		if (!factory) return;
		if (!enclosingComponentOrHookName(node)) return;
		context.report({
			node,
			message: `\`${factory.humanLabel}(...)\` builds a new store every render, so subscribers get cut off & saved state resets.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-danger.ts
const MESSAGE$24 = "`dangerouslySetInnerHTML` is an XSS hole that runs attacker-controlled HTML in your users' browsers.";
const noDanger = defineRule({
	id: "no-danger",
	title: "Use of dangerouslySetInnerHTML",
	severity: "warn",
	category: "Security",
	recommendation: "Render trusted content as React children rather than injecting raw HTML.",
	create: (context) => ({
		JSXOpeningElement(node) {
			const propAttribute = hasJsxProp(node.attributes, "dangerouslySetInnerHTML");
			if (!propAttribute) return;
			context.report({
				node: propAttribute.name,
				message: MESSAGE$24
			});
		},
		CallExpression(node) {
			if (!isCreateElementCall(node)) return;
			const propsArgument = node.arguments[1];
			if (!propsArgument || !isNodeOfType(propsArgument, "ObjectExpression")) return;
			for (const property of propsArgument.properties) {
				if (!isNodeOfType(property, "Property")) continue;
				const propertyKey = property.key;
				if (isNodeOfType(propertyKey, "Identifier") && propertyKey.name === "dangerouslySetInnerHTML" || isNodeOfType(propertyKey, "Literal") && propertyKey.value === "dangerouslySetInnerHTML") context.report({
					node: propertyKey,
					message: MESSAGE$24
				});
			}
		}
	})
});
//#endregion
//#region src/plugin/rules/react-builtins/no-danger-with-children.ts
const MESSAGE$23 = "React throws an error when you set both children & `dangerouslySetInnerHTML`.";
const isLineBreak = (child) => {
	if (!isNodeOfType(child, "JSXText")) return false;
	return child.value.trim().length === 0 && child.value.includes("\n");
};
const mergePropsShape = (target, source) => {
	target.hasDangerously ||= source.hasDangerously;
	target.hasChildren ||= source.hasChildren;
};
const getStaticPropName = (key) => {
	if (isNodeOfType(key, "Identifier")) return key.name;
	if (isNodeOfType(key, "Literal") && typeof key.value === "string") return key.value;
	return null;
};
const resolvePropsShape = (expression, scopes, visitedSymbolIds = /* @__PURE__ */ new Set()) => {
	if (isNodeOfType(expression, "Identifier")) {
		const symbol = scopes.symbolFor(expression);
		if (!symbol || visitedSymbolIds.has(symbol.id) || !symbol.initializer) return {
			hasDangerously: false,
			hasChildren: false
		};
		visitedSymbolIds.add(symbol.id);
		return resolvePropsShape(symbol.initializer, scopes, visitedSymbolIds);
	}
	if (!isNodeOfType(expression, "ObjectExpression")) return {
		hasDangerously: false,
		hasChildren: false
	};
	const shape = {
		hasDangerously: false,
		hasChildren: false
	};
	for (const property of expression.properties) {
		if (isNodeOfType(property, "SpreadElement")) {
			mergePropsShape(shape, resolvePropsShape(property.argument, scopes, visitedSymbolIds));
			continue;
		}
		if (!isNodeOfType(property, "Property") || property.computed) continue;
		const propName = getStaticPropName(property.key);
		if (propName === "dangerouslySetInnerHTML") shape.hasDangerously = true;
		if (propName === "children") shape.hasChildren = true;
	}
	return shape;
};
const resolveJsxSpreadPropsShape = (attributes, scopes) => {
	const shape = {
		hasDangerously: false,
		hasChildren: false
	};
	for (const attribute of attributes) {
		if (!isNodeOfType(attribute, "JSXSpreadAttribute")) continue;
		mergePropsShape(shape, resolvePropsShape(attribute.argument, scopes));
	}
	return shape;
};
const noDangerWithChildren = defineRule({
	id: "no-danger-with-children",
	title: "dangerouslySetInnerHTML with children",
	severity: "error",
	recommendation: "Use either `children` or `dangerouslySetInnerHTML`, never both.",
	category: "Correctness",
	create: (context) => ({
		JSXElement(node) {
			const opening = node.openingElement;
			const spreadPropsShape = resolveJsxSpreadPropsShape(opening.attributes, context.scopes);
			const hasChildrenProp = Boolean(hasJsxPropIgnoreCase(opening.attributes, "children")) || spreadPropsShape.hasChildren;
			const hasNestedChildren = node.children.length > 0 && !isLineBreak(node.children[0]);
			if (!hasChildrenProp && !hasNestedChildren) return;
			if (hasJsxPropIgnoreCase(opening.attributes, "dangerouslySetInnerHTML") || spreadPropsShape.hasDangerously) context.report({
				node: opening,
				message: MESSAGE$23
			});
		},
		CallExpression(node) {
			if (node.arguments.length <= 1) return;
			if (!isCreateElementCall(node)) return;
			const propsArgument = node.arguments[1];
			if (!propsArgument) return;
			const propsShape = resolvePropsShape(propsArgument, context.scopes);
			if (!propsShape.hasDangerously) return;
			if (node.arguments.length >= 3 || propsShape.hasChildren) context.report({
				node,
				message: MESSAGE$23
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/design/utils/get-inline-style-expression.ts
const getInlineStyleExpression = (node) => {
	if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "style") return null;
	if (!isNodeOfType(node.value, "JSXExpressionContainer")) return null;
	const expression = node.value.expression;
	if (!isNodeOfType(expression, "ObjectExpression")) return null;
	return expression;
};
//#endregion
//#region src/plugin/rules/design/utils/get-style-property-string-value.ts
const getStylePropertyStringValue = (property) => {
	if (!isNodeOfType(property, "Property")) return null;
	if (isNodeOfType(property.value, "Literal") && typeof property.value.value === "string") return property.value.value;
	return null;
};
//#endregion
//#region src/plugin/rules/design/utils/get-style-property-key.ts
const getStylePropertyKey = (property) => {
	if (!isNodeOfType(property, "Property")) return null;
	if (isNodeOfType(property.key, "Identifier")) return property.key.name;
	if (isNodeOfType(property.key, "Literal") && typeof property.key.value === "string") return property.key.value;
	return null;
};
//#endregion
//#region src/plugin/rules/design/utils/parse-color-to-rgb.ts
const parseColorToRgb = (value) => {
	const trimmed = value.trim().toLowerCase();
	const hex8Match = trimmed.match(/^#([0-9a-f]{2})([0-9a-f]{2})([0-9a-f]{2})[0-9a-f]{2}$/);
	if (hex8Match) return {
		red: parseInt(hex8Match[1], 16),
		green: parseInt(hex8Match[2], 16),
		blue: parseInt(hex8Match[3], 16)
	};
	const hex6Match = trimmed.match(/^#([0-9a-f]{2})([0-9a-f]{2})([0-9a-f]{2})$/);
	if (hex6Match) return {
		red: parseInt(hex6Match[1], 16),
		green: parseInt(hex6Match[2], 16),
		blue: parseInt(hex6Match[3], 16)
	};
	const hex4Match = trimmed.match(/^#([0-9a-f])([0-9a-f])([0-9a-f])[0-9a-f]$/);
	if (hex4Match) return {
		red: parseInt(hex4Match[1] + hex4Match[1], 16),
		green: parseInt(hex4Match[2] + hex4Match[2], 16),
		blue: parseInt(hex4Match[3] + hex4Match[3], 16)
	};
	const hex3Match = trimmed.match(/^#([0-9a-f])([0-9a-f])([0-9a-f])$/);
	if (hex3Match) return {
		red: parseInt(hex3Match[1] + hex3Match[1], 16),
		green: parseInt(hex3Match[2] + hex3Match[2], 16),
		blue: parseInt(hex3Match[3] + hex3Match[3], 16)
	};
	const rgbMatch = trimmed.match(/rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/);
	if (rgbMatch) return {
		red: parseInt(rgbMatch[1], 10),
		green: parseInt(rgbMatch[2], 10),
		blue: parseInt(rgbMatch[3], 10)
	};
	return null;
};
//#endregion
//#region src/plugin/rules/design/utils/has-color-chroma.ts
const hasColorChroma = (parsed) => Math.max(parsed.red, parsed.green, parsed.blue) - Math.min(parsed.red, parsed.green, parsed.blue) >= 30;
//#endregion
//#region src/plugin/rules/design/utils/is-pure-black-color.ts
const isPureBlackColor = (value) => {
	const trimmed = value.trim().toLowerCase();
	if (trimmed === "#000" || trimmed === "#000000") return true;
	if (/^rgb\(\s*0\s*,\s*0\s*,\s*0\s*\)$/.test(trimmed)) return true;
	return false;
};
//#endregion
//#region src/plugin/rules/design/no-dark-mode-glow.ts
const splitShadowLayers = (shadowValue) => shadowValue.split(/,(?![^(]*\))/);
const extractColorFromShadowLayer = (layer) => {
	const rgbMatch = layer.match(/rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/);
	if (rgbMatch) return {
		red: parseInt(rgbMatch[1], 10),
		green: parseInt(rgbMatch[2], 10),
		blue: parseInt(rgbMatch[3], 10)
	};
	const hexMatch = layer.match(/#([0-9a-f]{3,6})\b/i);
	if (hexMatch) return parseColorToRgb(`#${hexMatch[1]}`);
	return null;
};
const parseShadowLayerBlur = (layer) => {
	const numericTokens = [...layer.replace(/rgba?\([^)]*\)/g, "").replace(/#[0-9a-f]{3,8}\b/gi, "").matchAll(/(\d+(?:\.\d+)?)(px)?/g)].map((match) => parseFloat(match[1]));
	return numericTokens.length >= 3 ? numericTokens[2] : 0;
};
const hasColoredGlowShadow = (shadowValue) => {
	for (const layer of splitShadowLayers(shadowValue)) {
		const color = extractColorFromShadowLayer(layer);
		if (color && hasColorChroma(color) && parseShadowLayerBlur(layer) > 4) return true;
	}
	return false;
};
const isBackgroundDark = (bgValue) => {
	const trimmed = bgValue.trim().toLowerCase();
	if (isPureBlackColor(trimmed)) return true;
	const parsed = parseColorToRgb(trimmed);
	if (!parsed) return false;
	return parsed.red <= 35 && parsed.green <= 35 && parsed.blue <= 35;
};
const noDarkModeGlow = defineRule({
	id: "no-dark-mode-glow",
	title: "Colored glow on dark background",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Use a subtle `box-shadow` in neutral colors for depth, or a faint `border`. Colored glows on dark backgrounds look overdone.",
	create: (context) => ({ JSXAttribute(node) {
		const expression = getInlineStyleExpression(node);
		if (!expression) return;
		let hasDarkBackground = false;
		let shadowProperty = null;
		let shadowValue = null;
		for (const property of expression.properties ?? []) {
			const key = getStylePropertyKey(property);
			if (!key) continue;
			if (key === "backgroundColor" || key === "background") {
				const value = getStylePropertyStringValue(property);
				if (value && isBackgroundDark(value)) hasDarkBackground = true;
			}
			if (key === "boxShadow") {
				shadowProperty = property;
				shadowValue = getStylePropertyStringValue(property);
			}
		}
		if (!hasDarkBackground || !shadowValue || !shadowProperty) return;
		if (hasColoredGlowShadow(shadowValue)) context.report({
			node: shadowProperty,
			message: "Your users see a cheap, overdone colored glow on the dark background, so use a subtle, neutral shadow instead."
		});
	} })
});
//#endregion
//#region src/plugin/rules/architecture/no-default-props.ts
const noDefaultProps = defineRule({
	id: "no-default-props",
	title: "defaultProps removed in React 19",
	requires: ["react:19"],
	tags: ["test-noise"],
	severity: "warn",
	defaultEnabled: false,
	recommendation: "React 19 drops `Component.defaultProps` for function components. Set the defaults in the destructured props instead: `function Foo({ size = \"md\", variant = \"primary\" })` instead of `Foo.defaultProps = { size: \"md\", variant: \"primary\" }`.",
	create: (context) => ({ AssignmentExpression(node) {
		if (node.operator !== "=") return;
		const left = node.left;
		if (!isNodeOfType(left, "MemberExpression")) return;
		if (left.computed) return;
		if (!isNodeOfType(left.property, "Identifier") || left.property.name !== "defaultProps") return;
		if (!isNodeOfType(left.object, "Identifier")) return;
		if (!isUppercaseName(left.object.name)) return;
		context.report({
			node: left,
			message: `${left.object.name}.defaultProps stops applying in React 19, so your users see missing defaults. Set them in the destructured props parameter instead, like \`function ${left.object.name}({ size = "md" })\`.`
		});
	} })
});
//#endregion
//#region src/plugin/utils/is-initial-only-prop-name.ts
const isInitialOnlyPropName = (propName) => {
	if (propName === "initialValue" || propName === "defaultValue" || propName === "seedValue") return true;
	return /^initial[A-Z]/.test(propName) || /^default[A-Z]/.test(propName) || /^seed[A-Z]/.test(propName) || /^starting[A-Z]/.test(propName) || /^baseline[A-Z]/.test(propName) || /^preset[A-Z]/.test(propName);
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-derived-state.ts
const countSetterCallSites = (ref) => {
	if (!ref.resolved) return 0;
	let count = 0;
	for (const reference of ref.resolved.references) {
		const parent = reference.identifier.parent;
		if (parent && isNodeOfType(parent, "CallExpression")) count += 1;
	}
	return count;
};
const getStateNameForUseStateDecl = (useStateNode) => {
	if (!useStateNode || !isNodeOfType(useStateNode, "VariableDeclarator")) return null;
	if (!isNodeOfType(useStateNode.id, "ArrayPattern")) return null;
	const elements = useStateNode.id.elements ?? [];
	const candidate = elements[0] ?? elements[1];
	if (!candidate) return null;
	return isNodeOfType(candidate, "Identifier") ? candidate.name : null;
};
const noDerivedState = defineRule({
	id: "no-derived-state",
	title: "Derived value copied into state",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Work out the value while rendering (or with useMemo if it's expensive) instead of copying it into useState through a useEffect. See https://react.dev/learn/you-might-not-need-an-effect#updating-state-based-on-props-or-state",
	create: (context) => ({ CallExpression(node) {
		if (!isUseEffect(node)) return;
		const analysis = getProgramAnalysis(node);
		if (!analysis) return;
		if (hasCleanup(analysis, node)) return;
		const effectFnRefs = getEffectFnRefs(analysis, node);
		const depsRefs = getEffectDepsRefs(analysis, node);
		if (!effectFnRefs || !depsRefs) return;
		const effectFn = getEffectFn(analysis, node);
		if (!effectFn) return;
		for (const ref of effectFnRefs) {
			if (!isStateSetterCall(analysis, ref)) continue;
			if (!isSynchronous(ref.identifier, effectFn)) continue;
			const callExpr = getCallExpr(ref);
			if (!callExpr) continue;
			const stateName = getStateNameForUseStateDecl(getUseStateDecl(analysis, ref)) ?? "<state>";
			const argsUpstreamRefs = getArgsUpstreamRefs(analysis, ref);
			const depsUpstreamRefs = depsRefs.flatMap((depRef) => getUpstreamRefs(analysis, depRef));
			if (isInitialOnlySetterCall(callExpr)) continue;
			const isSomeArgsInternal = argsUpstreamRefs.some((argRef) => isState(analysis, argRef) || isProp(analysis, argRef));
			const isValueAlwaysInSync = argsUpstreamRefs.length > 0 && argsUpstreamRefs.every((argRef) => depsUpstreamRefs.some((depRef) => argRef.resolved === depRef.resolved)) && countSetterCallSites(ref) === 1;
			if (isSomeArgsInternal) context.report({
				node: callExpr,
				message: `Storing "${stateName}" in state when you can derive it from other values costs an extra render.`
			});
			else if (isValueAlwaysInSync) context.report({
				node: callExpr,
				message: `"${stateName}" is only set here from other values, so storing it costs an extra render.`
			});
		}
	} })
});
const isInitialOnlySetterCall = (callExpr) => {
	if (!isNodeOfType(callExpr, "CallExpression")) return false;
	const args = callExpr.arguments ?? [];
	if (args.length !== 1) return false;
	const arg = args[0];
	if (!isNodeOfType(arg, "Identifier")) return false;
	return isInitialOnlyPropName(arg.name);
};
//#endregion
//#region src/plugin/utils/get-callback-statements.ts
const getCallbackStatements = (callback) => {
	if (!isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression") && !isNodeOfType(callback, "FunctionDeclaration")) return [];
	if (isNodeOfType(callback.body, "BlockStatement")) return callback.body.body ?? [];
	return callback.body ? [callback.body] : [];
};
//#endregion
//#region src/plugin/utils/get-root-identifier-name.ts
const getRootIdentifierName = (node, options) => {
	if (!node) return null;
	if (isNodeOfType(node, "Identifier")) return node.name;
	const followCallChains = options?.followCallChains === true;
	let cursor = node;
	while (cursor) {
		if (isNodeOfType(cursor, "ChainExpression")) {
			cursor = cursor.expression;
			continue;
		}
		if (isNodeOfType(cursor, "MemberExpression")) {
			cursor = cursor.object;
			continue;
		}
		if (followCallChains && isNodeOfType(cursor, "CallExpression")) {
			const callee = cursor.callee;
			if (!isNodeOfType(callee, "MemberExpression")) return null;
			cursor = callee.object;
			continue;
		}
		break;
	}
	return isNodeOfType(cursor, "Identifier") ? cursor.name : null;
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-derived-state-effect.ts
const collectValueIdentifierNames = (node, into) => {
	if (!node || typeof node !== "object") return;
	if (isNodeOfType(node, "CallExpression")) {
		if (isNodeOfType(node.callee, "MemberExpression")) {
			const rootName = getRootIdentifierName(node.callee);
			if (!rootName || !BUILTIN_GLOBAL_NAMESPACE_NAMES.has(rootName)) collectValueIdentifierNames(node.callee.object, into);
		}
		for (const argument of node.arguments ?? []) collectValueIdentifierNames(argument, into);
		return;
	}
	if (isNodeOfType(node, "MemberExpression")) {
		const rootName = getRootIdentifierName(node);
		if (!rootName || !BUILTIN_GLOBAL_NAMESPACE_NAMES.has(rootName)) collectValueIdentifierNames(node.object, into);
		if (node.computed) collectValueIdentifierNames(node.property, into);
		return;
	}
	if (isNodeOfType(node, "Identifier")) {
		into.push(node.name);
		return;
	}
	const nodeRecord = node;
	for (const key of Object.keys(nodeRecord)) {
		if (key === "parent" || key === "type") continue;
		const child = nodeRecord[key];
		if (Array.isArray(child)) {
			for (const item of child) if (item && typeof item === "object" && "type" in item) collectValueIdentifierNames(item, into);
		} else if (child && typeof child === "object" && "type" in child) collectValueIdentifierNames(child, into);
	}
};
const noDerivedStateEffect = defineRule({
	id: "no-derived-state-effect",
	title: "Derived state stored in an effect",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Work out derived values while rendering: `const x = fn(dep)`. To reset a component's state when a prop changes, give it a key prop: `<Component key={prop} />`. See https://react.dev/learn/you-might-not-need-an-effect",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1) || (node.arguments?.length ?? 0) < 2) return;
		const callback = getEffectCallback(node);
		if (!callback) return;
		const depsNode = node.arguments[1];
		if (!isNodeOfType(depsNode, "ArrayExpression") || !depsNode.elements?.length) return;
		const dependencyNames = /* @__PURE__ */ new Set();
		for (const element of depsNode.elements ?? []) if (isNodeOfType(element, "Identifier")) dependencyNames.add(element.name);
		if (dependencyNames.size === 0) return;
		let allDepsAreInitialOnly = true;
		let sawAnyDep = false;
		for (const name of dependencyNames) {
			sawAnyDep = true;
			if (!isInitialOnlyPropName(name)) {
				allDepsAreInitialOnly = false;
				break;
			}
		}
		if (sawAnyDep && allDepsAreInitialOnly) return;
		const statements = getCallbackStatements(callback);
		if (statements.length === 0) return;
		if (!statements.every((statement) => {
			if (!isNodeOfType(statement, "ExpressionStatement")) return false;
			const expression = statement.expression;
			if (!isSetterCall(expression)) return false;
			if (!isNodeOfType(expression, "CallExpression")) return false;
			if (!isNodeOfType(expression.callee, "Identifier")) return false;
			return isUseStateSetterInScope(expression, expression.callee.name);
		})) return;
		let allArgumentsDeriveFromDeps = true;
		let hasAnyDependencyReference = false;
		let hasExpensiveDerivation = false;
		for (const statement of statements) {
			if (!isNodeOfType(statement, "ExpressionStatement")) continue;
			if (!isNodeOfType(statement.expression, "CallExpression")) continue;
			const setStateArguments = statement.expression.arguments;
			if (!setStateArguments?.length) continue;
			const valueIdentifierNames = [];
			collectValueIdentifierNames(setStateArguments[0], valueIdentifierNames);
			walkAst(setStateArguments[0], (child) => {
				if (!isNodeOfType(child, "CallExpression")) return;
				if (isNodeOfType(child.callee, "MemberExpression")) {
					const rootName = getRootIdentifierName(child.callee);
					if (rootName && BUILTIN_GLOBAL_NAMESPACE_NAMES.has(rootName)) return;
					hasExpensiveDerivation = true;
					return;
				}
				if (isNodeOfType(child.callee, "Identifier")) {
					const calleeName = child.callee.name;
					if (!TRIVIAL_DERIVATION_CALLEE_NAMES.has(calleeName) && !isSetterIdentifier(calleeName)) hasExpensiveDerivation = true;
				}
			});
			const nonSetterIdentifiers = valueIdentifierNames.filter((name) => !isSetterIdentifier(name));
			if (nonSetterIdentifiers.some((name) => dependencyNames.has(name))) hasAnyDependencyReference = true;
			if (nonSetterIdentifiers.some((name) => !dependencyNames.has(name))) {
				allArgumentsDeriveFromDeps = false;
				break;
			}
		}
		if (!allArgumentsDeriveFromDeps) return;
		if (hasExpensiveDerivation) hasAnyDependencyReference = true;
		let message;
		if (!hasAnyDependencyReference) message = "Your users briefly see stale state on every prop change.";
		else if (hasExpensiveDerivation) message = "You pay an extra render for state derived from other values.";
		else message = "You pay an extra render for state you can derive from other values.";
		context.report({
			node,
			message
		});
	} })
});
//#endregion
//#region src/plugin/utils/create-component-prop-stack-tracker.ts
const extractDestructuredPropNames = (params) => {
	const propNames = /* @__PURE__ */ new Set();
	for (const param of params) collectPatternNames(param, propNames);
	return propNames;
};
const getInlineFunctionNode = (node) => {
	if (!node) return null;
	if (isFunctionLike$2(node)) return node;
	if (!isNodeOfType(node, "CallExpression")) return null;
	for (const argument of node.arguments ?? []) {
		const inlineFunctionNode = getInlineFunctionNode(argument);
		if (inlineFunctionNode) return inlineFunctionNode;
	}
	return null;
};
const getNearestComponentFunction = (node) => {
	let cursor = node.parent ?? null;
	while (cursor) {
		if (isFunctionLike$2(cursor)) return cursor;
		cursor = cursor.parent ?? null;
	}
	return null;
};
const isFunctionAssignedToComponent = (functionNode) => {
	let cursor = functionNode.parent ?? null;
	while (isNodeOfType(cursor, "CallExpression")) cursor = cursor.parent ?? null;
	if (isNodeOfType(cursor, "VariableDeclarator") && isNodeOfType(cursor.id, "Identifier") && isUppercaseName(cursor.id.name)) return true;
	return isNodeOfType(cursor, "ExportDefaultDeclaration");
};
const isComponentFunction = (functionNode) => {
	if (isNodeOfType(functionNode, "FunctionDeclaration")) return !functionNode.id || functionNode.id.name === "default" || isUppercaseName(functionNode.id.name) || isNodeOfType(functionNode.parent, "ExportDefaultDeclaration");
	return isFunctionAssignedToComponent(functionNode);
};
const isFunctionLikeVariableDeclarator = (node) => {
	if (!isNodeOfType(node, "VariableDeclarator")) return false;
	return isNodeOfType(node.init, "ArrowFunctionExpression") || isNodeOfType(node.init, "FunctionExpression");
};
const createComponentPropStackTracker = (callbacks) => {
	const propParamStack = [];
	const exportDefaultDeclarationsWithFrames = /* @__PURE__ */ new WeakSet();
	const functionDeclarationsWithFrames = /* @__PURE__ */ new WeakSet();
	const variableDeclaratorsWithFrames = /* @__PURE__ */ new WeakSet();
	const isPropName = (name, referenceNode) => {
		for (let frameIndex = propParamStack.length - 1; frameIndex >= 0; frameIndex--) {
			const frame = propParamStack[frameIndex];
			if (frame.size === 0) break;
			if (frame.has(name)) return true;
		}
		if (referenceNode) {
			const componentFunctionNode = getNearestComponentFunction(referenceNode);
			if (!componentFunctionNode || !isComponentFunction(componentFunctionNode)) return false;
			return extractDestructuredPropNames(componentFunctionNode.params ?? []).has(name);
		}
		return false;
	};
	const getCurrentPropNames = () => {
		for (let frameIndex = propParamStack.length - 1; frameIndex >= 0; frameIndex--) {
			const frame = propParamStack[frameIndex];
			if (frame.size === 0) return /* @__PURE__ */ new Set();
			return frame;
		}
		return /* @__PURE__ */ new Set();
	};
	const pushFunctionPropFrame = (functionNode) => {
		propParamStack.push(extractDestructuredPropNames(functionNode.params ?? []));
		callbacks?.onComponentEnter?.(functionNode.body);
	};
	return {
		isPropName,
		getCurrentPropNames,
		visitors: {
			ExportDefaultDeclaration(node) {
				if (!isNodeOfType(node, "ExportDefaultDeclaration")) return;
				const inlineFunctionNode = getInlineFunctionNode(node.declaration);
				if (!inlineFunctionNode) return;
				if (isNodeOfType(inlineFunctionNode, "FunctionDeclaration")) return;
				pushFunctionPropFrame(inlineFunctionNode);
				exportDefaultDeclarationsWithFrames.add(node);
			},
			"ExportDefaultDeclaration:exit"(node) {
				if (!isNodeOfType(node, "ExportDefaultDeclaration")) return;
				if (!exportDefaultDeclarationsWithFrames.has(node)) return;
				propParamStack.pop();
			},
			FunctionDeclaration(node) {
				if (!isNodeOfType(node, "FunctionDeclaration")) return;
				if (!node.id || node.id.name === "default") {
					propParamStack.push(extractDestructuredPropNames(node.params ?? []));
					callbacks?.onComponentEnter?.(node.body);
					functionDeclarationsWithFrames.add(node);
					return;
				}
				if (!isUppercaseName(node.id.name)) {
					propParamStack.push(/* @__PURE__ */ new Set());
					functionDeclarationsWithFrames.add(node);
					return;
				}
				propParamStack.push(extractDestructuredPropNames(node.params ?? []));
				callbacks?.onComponentEnter?.(node.body);
				functionDeclarationsWithFrames.add(node);
			},
			"FunctionDeclaration:exit"(node) {
				if (!isNodeOfType(node, "FunctionDeclaration")) return;
				if (!functionDeclarationsWithFrames.has(node)) return;
				propParamStack.pop();
			},
			VariableDeclarator(node) {
				if (!isNodeOfType(node, "VariableDeclarator")) return;
				if (isNodeOfType(node.id, "Identifier") && isUppercaseName(node.id.name)) {
					const inlineFunctionNode = getInlineFunctionNode(node.init);
					if (!inlineFunctionNode) return;
					pushFunctionPropFrame(inlineFunctionNode);
					variableDeclaratorsWithFrames.add(node);
					return;
				}
				if (isFunctionLikeVariableDeclarator(node)) {
					propParamStack.push(/* @__PURE__ */ new Set());
					variableDeclaratorsWithFrames.add(node);
				}
			},
			"VariableDeclarator:exit"(node) {
				if (!isNodeOfType(node, "VariableDeclarator")) return;
				if (!variableDeclaratorsWithFrames.has(node)) return;
				propParamStack.pop();
			}
		}
	};
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-derived-use-state.ts
const noDerivedUseState = defineRule({
	id: "no-derived-useState",
	title: "Prop derived into useState",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Remove useState and compute the value inline: `const value = transform(propName)`",
	create: (context) => {
		const propStackTracker = createComponentPropStackTracker();
		return {
			...propStackTracker.visitors,
			CallExpression(node) {
				if (!isHookCall$1(node, "useState") || !node.arguments?.length) return;
				const initializer = node.arguments[0];
				if (isNodeOfType(initializer, "Identifier") && propStackTracker.isPropName(initializer.name)) {
					if (isInitialOnlyPropName(initializer.name)) return;
					context.report({
						node,
						message: `Your users see a stale value when prop "${initializer.name}" changes because useState copies it once.`
					});
					return;
				}
				if (isNodeOfType(initializer, "MemberExpression") && !initializer.computed) {
					const rootIdentifierName = getRootIdentifierName(initializer);
					if (rootIdentifierName && propStackTracker.isPropName(rootIdentifierName)) {
						if (isNodeOfType(initializer.property, "Identifier") && isInitialOnlyPropName(initializer.property.name)) return;
						context.report({
							node,
							message: `Your users see a stale value when prop "${rootIdentifierName}" changes because useState copies it once.`
						});
					}
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/is-set-state-in-lifecycle.ts
const FUNCTION_NODE_TYPES = new Set([
	"FunctionDeclaration",
	"FunctionExpression",
	"ArrowFunctionExpression"
]);
const isLifecycleMember = (node, lifecycleNames) => {
	if (isNodeOfType(node, "MethodDefinition") || isNodeOfType(node, "Property") || isNodeOfType(node, "PropertyDefinition")) {
		const key = node.key;
		if (!key) return false;
		if (isNodeOfType(key, "Identifier")) return lifecycleNames.has(key.name);
		if (isNodeOfType(key, "Literal") && typeof key.value === "string") return lifecycleNames.has(key.value);
	}
	return false;
};
const isSetStateCallInLifecycle = (setStateCall, lifecycleNames, options = {}) => {
	let lifecycleMember = null;
	let nestedFunctionCount = 0;
	let ancestor = setStateCall.parent;
	while (ancestor) {
		if (!lifecycleMember) {
			if (FUNCTION_NODE_TYPES.has(ancestor.type)) nestedFunctionCount += 1;
			if (isLifecycleMember(ancestor, lifecycleNames)) lifecycleMember = ancestor;
		} else if (isEs5Component(ancestor) || isEs6Component(ancestor)) {
			if (nestedFunctionCount > 1 && !options.disallowInNestedFunctions) return false;
			return true;
		}
		ancestor = ancestor.parent ?? null;
	}
	return false;
};
//#endregion
//#region src/plugin/rules/react-builtins/no-did-mount-set-state.ts
const LIFECYCLE_NAMES$2 = new Set(["componentDidMount"]);
const MESSAGE$22 = "Your users see an extra render right after mount when you call `setState` in `componentDidMount`.";
const resolveSettings$20 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { mode: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noDidMountSetState ?? {} : {}).mode ?? "allowed" };
};
const noDidMountSetState = defineRule({
	id: "no-did-mount-set-state",
	title: "setState in componentDidMount",
	severity: "warn",
	recommendation: "Setting state in `componentDidMount` triggers an extra render. Use `getDerivedStateFromProps` or initial state instead.",
	create: (context) => {
		const { mode } = resolveSettings$20(context.settings);
		return { CallExpression(node) {
			if (!isNodeOfType(node.callee, "MemberExpression")) return;
			if (!isNodeOfType(node.callee.object, "ThisExpression")) return;
			if (!isNodeOfType(node.callee.property, "Identifier") || node.callee.property.name !== "setState") return;
			if (!isSetStateCallInLifecycle(node, LIFECYCLE_NAMES$2, { disallowInNestedFunctions: mode === "disallow-in-func" })) return;
			context.report({
				node: node.callee,
				message: MESSAGE$22
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/no-did-update-set-state.ts
const LIFECYCLE_NAMES$1 = new Set(["componentDidUpdate"]);
const MESSAGE$21 = "This can loop forever & freeze the component.";
const resolveSettings$19 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { mode: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noDidUpdateSetState ?? {} : {}).mode ?? "allowed" };
};
const noDidUpdateSetState = defineRule({
	id: "no-did-update-set-state",
	title: "setState in componentDidUpdate",
	severity: "warn",
	recommendation: "Setting state in `componentDidUpdate` causes another render and can loop. Use `getDerivedStateFromProps` instead.",
	create: (context) => {
		const { mode } = resolveSettings$19(context.settings);
		return { CallExpression(node) {
			if (!isNodeOfType(node.callee, "MemberExpression")) return;
			if (!isNodeOfType(node.callee.object, "ThisExpression")) return;
			if (!isNodeOfType(node.callee.property, "Identifier") || node.callee.property.name !== "setState") return;
			if (!isSetStateCallInLifecycle(node, LIFECYCLE_NAMES$1, { disallowInNestedFunctions: mode === "disallow-in-func" })) return;
			context.report({
				node: node.callee,
				message: MESSAGE$21
			});
		} };
	}
});
//#endregion
//#region src/plugin/utils/get-outer-member-expression.ts
const getOuterMemberExpression = (node) => {
	let current = node;
	while (isNodeOfType(current, "MemberExpression") && isNodeOfType(current.object, "MemberExpression")) current = current.object;
	if (!isNodeOfType(current, "MemberExpression")) return null;
	return current;
};
//#endregion
//#region src/plugin/utils/is-state-member-expression.ts
const isStateMemberExpression = (node) => {
	if (!isNodeOfType(node, "MemberExpression")) return false;
	if (!isNodeOfType(node.object, "ThisExpression")) return false;
	if (!isNodeOfType(node.property, "Identifier")) return false;
	return node.property.name === "state";
};
//#endregion
//#region src/plugin/rules/react-builtins/no-direct-mutation-state.ts
const MESSAGE$20 = "Your users see stale data because mutating `this.state` by hand never redraws & gets overwritten.";
const shouldIgnoreMutation = (node) => {
	let isConstructor = false;
	let isInsideCallExpression = false;
	let isInsideComponent = false;
	let ancestor = node.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "MethodDefinition") && "kind" in ancestor && ancestor.kind === "constructor") isConstructor = true;
		if (isNodeOfType(ancestor, "CallExpression")) isInsideCallExpression = true;
		if (isEs5Component(ancestor) || isEs6Component(ancestor)) isInsideComponent = true;
		if (isNodeOfType(ancestor, "ClassDeclaration") || isNodeOfType(ancestor, "ClassExpression")) break;
		ancestor = ancestor.parent ?? null;
	}
	return isConstructor && !isInsideCallExpression || !isInsideComponent;
};
const reportIfStateMutation = (context, reportNode, target) => {
	const outerMember = getOuterMemberExpression(target);
	if (!outerMember) return;
	if (!isStateMemberExpression(outerMember)) return;
	if (shouldIgnoreMutation(reportNode)) return;
	context.report({
		node: reportNode,
		message: MESSAGE$20
	});
};
const noDirectMutationState = defineRule({
	id: "no-direct-mutation-state",
	title: "Direct mutation of this.state",
	severity: "error",
	recommendation: "Don't change `this.state` by hand. `setState()` overwrites it anyway, so always go through `setState()`.",
	create: (context) => ({
		AssignmentExpression(node) {
			reportIfStateMutation(context, node, node.left);
		},
		UpdateExpression(node) {
			reportIfStateMutation(context, node, node.argument);
		}
	})
});
//#endregion
//#region src/plugin/rules/state-and-effects/utils/collect-use-state-bindings.ts
const collectUseStateBindings = (componentBody) => {
	const bindings = [];
	if (!isNodeOfType(componentBody, "BlockStatement")) return bindings;
	for (const statement of componentBody.body ?? []) {
		if (!isNodeOfType(statement, "VariableDeclaration")) continue;
		for (const declarator of statement.declarations ?? []) {
			if (!isNodeOfType(declarator.id, "ArrayPattern")) continue;
			const elements = declarator.id.elements ?? [];
			if (elements.length < 2) continue;
			const valueElement = elements[0];
			const setterElement = elements[1];
			if (!isNodeOfType(valueElement, "Identifier") || !isNodeOfType(setterElement, "Identifier") || !isSetterIdentifier(setterElement.name)) continue;
			if (!isNodeOfType(declarator.init, "CallExpression")) continue;
			if (!isHookCall$1(declarator.init, "useState")) continue;
			bindings.push({
				valueName: valueElement.name,
				setterName: setterElement.name,
				declarator
			});
		}
	}
	return bindings;
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-direct-state-mutation.ts
const collectFunctionLocalBindings = (functionNode) => {
	const localBindings = /* @__PURE__ */ new Set();
	if (!isNodeOfType(functionNode, "FunctionDeclaration") && !isNodeOfType(functionNode, "FunctionExpression") && !isNodeOfType(functionNode, "ArrowFunctionExpression")) return localBindings;
	for (const param of functionNode.params ?? []) collectPatternNames(param, localBindings);
	if (isNodeOfType(functionNode.body, "BlockStatement")) for (const statement of functionNode.body.body ?? []) {
		if (!isNodeOfType(statement, "VariableDeclaration")) continue;
		for (const declarator of statement.declarations ?? []) collectPatternNames(declarator.id, localBindings);
	}
	return localBindings;
};
const walkComponentRespectingShadows = (node, shadowedStateNames, visit) => {
	if (!node || typeof node !== "object") return;
	let nextShadowedStateNames = shadowedStateNames;
	if (isFunctionLike$2(node)) {
		const localBindings = collectFunctionLocalBindings(node);
		if (localBindings.size > 0) {
			const merged = new Set(shadowedStateNames);
			for (const localName of localBindings) merged.add(localName);
			nextShadowedStateNames = merged;
		}
	}
	visit(node, shadowedStateNames);
	const nodeRecord = node;
	for (const key of Object.keys(nodeRecord)) {
		if (key === "parent") continue;
		const child = nodeRecord[key];
		if (Array.isArray(child)) {
			for (const item of child) if (item && typeof item === "object" && "type" in item) walkComponentRespectingShadows(item, nextShadowedStateNames, visit);
		} else if (child && typeof child === "object" && "type" in child) walkComponentRespectingShadows(child, nextShadowedStateNames, visit);
	}
};
const noDirectStateMutation = defineRule({
	id: "no-direct-state-mutation",
	title: "State mutated in place",
	severity: "warn",
	recommendation: "Call the setter with a brand new value instead: `setItems([...items, newItem])`, `setItems(items.filter(x => x !== target))`, or `setItems(items.toSorted(...))`. React only redraws when the value is new, so changing it in place does nothing.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const bindings = collectUseStateBindings(componentBody);
			if (bindings.length === 0) return;
			const stateValueToSetter = new Map(bindings.map((binding) => [binding.valueName, binding.setterName]));
			walkComponentRespectingShadows(componentBody, /* @__PURE__ */ new Set(), (child, currentlyShadowed) => {
				if (isNodeOfType(child, "AssignmentExpression")) {
					if (!isNodeOfType(child.left, "MemberExpression")) return;
					const rootName = getRootIdentifierName(child.left);
					if (!rootName || !stateValueToSetter.has(rootName)) return;
					if (currentlyShadowed.has(rootName)) return;
					context.report({
						node: child,
						message: `Your screen won't update because you change "${rootName}" in place.`
					});
					return;
				}
				if (isNodeOfType(child, "CallExpression")) {
					const callee = child.callee;
					if (!isNodeOfType(callee, "MemberExpression")) return;
					if (!isNodeOfType(callee.property, "Identifier")) return;
					const methodName = callee.property.name;
					if (!MUTATING_ARRAY_METHODS.has(methodName)) return;
					const rootName = getRootIdentifierName(callee.object);
					if (!rootName || !stateValueToSetter.has(rootName)) return;
					if (currentlyShadowed.has(rootName)) return;
					context.report({
						node: child,
						message: `Your screen won't update because .${methodName}() changes "${rootName}" in place.`
					});
				}
			});
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/design/no-disabled-zoom.ts
const noDisabledZoom = defineRule({
	id: "no-disabled-zoom",
	title: "Zoom disabled on viewport",
	severity: "error",
	tags: ["test-noise"],
	category: "Accessibility",
	recommendation: "Remove `user-scalable=no` and `maximum-scale` from the viewport meta tag. If the layout breaks at 200% zoom, fix the layout instead.",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "meta") return;
		const nameAttr = findJsxAttribute(node.attributes ?? [], "name");
		if (!nameAttr?.value) return;
		if ((isNodeOfType(nameAttr.value, "Literal") ? nameAttr.value.value : null) !== "viewport") return;
		const contentAttr = findJsxAttribute(node.attributes ?? [], "content");
		if (!contentAttr?.value) return;
		const contentValue = isNodeOfType(contentAttr.value, "Literal") && typeof contentAttr.value.value === "string" ? contentAttr.value.value : null;
		if (!contentValue) return;
		const hasUserScalableNo = /user-scalable\s*=\s*no/i.test(contentValue);
		const maxScaleMatch = contentValue.match(/maximum-scale\s*=\s*([\d.]+)/i);
		const hasRestrictiveMaxScale = maxScaleMatch !== null && parseFloat(maxScaleMatch[1]) < 2;
		if (hasUserScalableNo && hasRestrictiveMaxScale) context.report({
			node,
			message: `Your users can't pinch to zoom because user-scalable=no & maximum-scale=${maxScaleMatch[1]} block it, which fails accessibility (WCAG 1.4.4). Remove both & fix the layout if it breaks at 200%.`
		});
		else if (hasUserScalableNo) context.report({
			node,
			message: "Your users can't pinch to zoom because user-scalable=no blocks it, which fails accessibility (WCAG 1.4.4). Remove it & fix the layout if it breaks at 200%."
		});
		else if (hasRestrictiveMaxScale) context.report({
			node,
			message: `Your users can't zoom past 200% because maximum-scale=${maxScaleMatch[1]} blocks it, which fails accessibility (WCAG 1.4.4). Use maximum-scale=5 or remove it.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/a11y/no-distracting-elements.ts
const buildMessage$15 = (tag) => `Users with attention or motion sensitivity struggle because \`<${tag}>\` animates on its own, so use normal, accessible markup instead.`;
const DEFAULT_DISTRACTING = ["marquee", "blink"];
const resolveSettings$18 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const elements = (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noDistractingElements ?? {} : {}).elements ?? DEFAULT_DISTRACTING;
	return { distractingTags: new Set(elements) };
};
const noDistractingElements = defineRule({
	id: "no-distracting-elements",
	title: "Distracting marquee or blink element",
	tags: ["react-jsx-only"],
	severity: "error",
	recommendation: "Replace `<marquee>` and `<blink>` with normal, accessible markup.",
	category: "Accessibility",
	create: (context) => {
		const { distractingTags } = resolveSettings$18(context.settings);
		return { JSXOpeningElement(node) {
			if (distractingTags.size === 0) return;
			const tag = getElementType(node, context.settings);
			if (distractingTags.has(tag)) context.report({
				node: node.name,
				message: buildMessage$15(tag)
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/view-transitions/no-document-start-view-transition.ts
const noDocumentStartViewTransition = defineRule({
	id: "no-document-start-view-transition",
	title: "Direct document.startViewTransition call",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Render a <ViewTransition> component and update inside startTransition or useDeferredValue, and React calls startViewTransition for you.",
	create: (context) => ({ CallExpression(node) {
		const callee = node.callee;
		if (!isNodeOfType(callee, "MemberExpression")) return;
		if (!isNodeOfType(callee.object, "Identifier") || callee.object.name !== "document") return;
		if (!isNodeOfType(callee.property, "Identifier") || callee.property.name !== "startViewTransition") return;
		context.report({
			node,
			message: "Your users lose React's <ViewTransition> animations when document.startViewTransition() runs directly."
		});
	} })
});
//#endregion
//#region src/plugin/rules/bundle-size/no-dynamic-import-path.ts
const noDynamicImportPath = defineRule({
	id: "no-dynamic-import-path",
	title: "Non-static dynamic import path",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use a plain string path: `import('./feature/heavy.js')` so the bundler can split this into its own chunk.",
	create: (context) => ({
		ImportExpression(node) {
			const source = node.source;
			if (source && !isNodeOfType(source, "Literal") && !isNodeOfType(source, "TemplateLiteral")) {
				context.report({
					node,
					message: "This ships in the main bundle & slows page load, since the bundler can't code-split a dynamic import path. Use a plain string path instead."
				});
				return;
			}
			if (isNodeOfType(source, "TemplateLiteral") && (source.expressions?.length ?? 0) > 0) context.report({
				node,
				message: "This ships in the main bundle & slows page load, since the bundler can't code-split a dynamic import path. Use a plain string path instead of one with `${...}`."
			});
		},
		CallExpression(node) {
			if (!isNodeOfType(node.callee, "Identifier") || node.callee.name !== "require") return;
			const arg = node.arguments?.[0];
			if (!arg) return;
			if (!isNodeOfType(arg, "Literal") && !isNodeOfType(arg, "TemplateLiteral")) {
				context.report({
					node,
					message: "This ships in the main bundle & slows page load, since the bundler can't trace a dynamic require() path. Use a plain string path instead."
				});
				return;
			}
			if (isNodeOfType(arg, "TemplateLiteral") && (arg.expressions?.length ?? 0) > 0) context.report({
				node,
				message: "This ships in the main bundle & slows page load, since the bundler can't trace a dynamic require() path. Use a plain string path instead of one with `${...}`."
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-effect-chain.ts
const findTopLevelEffectCalls = (componentBody) => {
	const effectCalls = [];
	if (!isNodeOfType(componentBody, "BlockStatement")) return effectCalls;
	for (const statement of componentBody.body ?? []) {
		if (!isNodeOfType(statement, "ExpressionStatement")) continue;
		const expression = statement.expression;
		if (!isNodeOfType(expression, "CallExpression")) continue;
		if (!isHookCall$1(expression, EFFECT_HOOK_NAMES$1)) continue;
		effectCalls.push(expression);
	}
	return effectCalls;
};
const collectDepIdentifierNames = (effectNode) => {
	const depNames = /* @__PURE__ */ new Set();
	if (!isNodeOfType(effectNode, "CallExpression")) return depNames;
	const depsNode = effectNode.arguments?.[1];
	if (!isNodeOfType(depsNode, "ArrayExpression")) return depNames;
	for (const element of depsNode.elements ?? []) if (isNodeOfType(element, "Identifier")) depNames.add(element.name);
	return depNames;
};
const collectWrittenStateNamesInEffect = (effectCallback, setterToStateName) => {
	const writtenStateNames = /* @__PURE__ */ new Set();
	if (!isNodeOfType(effectCallback, "ArrowFunctionExpression") && !isNodeOfType(effectCallback, "FunctionExpression")) return writtenStateNames;
	walkInsideStatementBlocks(effectCallback.body, (child) => {
		if (!isNodeOfType(child, "CallExpression")) return;
		if (!isNodeOfType(child.callee, "Identifier")) return;
		const stateName = setterToStateName.get(child.callee.name);
		if (stateName) writtenStateNames.add(stateName);
	});
	return writtenStateNames;
};
const isFunctionShapedReturn = (returnedValue) => {
	if (isNodeOfType(returnedValue, "ArrowFunctionExpression") || isNodeOfType(returnedValue, "FunctionExpression")) return true;
	if (isNodeOfType(returnedValue, "CallExpression")) return true;
	if (isNodeOfType(returnedValue, "Identifier")) return true;
	return false;
};
const isExternalSyncEffect = (effectCallback) => {
	if (!isNodeOfType(effectCallback, "ArrowFunctionExpression") && !isNodeOfType(effectCallback, "FunctionExpression")) return false;
	if (isNodeOfType(effectCallback.body, "BlockStatement")) {
		const statements = effectCallback.body.body ?? [];
		for (const statement of statements) if (isNodeOfType(statement, "ReturnStatement") && statement.argument && isFunctionShapedReturn(statement.argument)) return true;
	}
	let didFindExternalCall = false;
	walkAst(effectCallback, (child) => {
		if (didFindExternalCall) return false;
		if (isNodeOfType(child, "NewExpression")) {
			const constructor = child.callee;
			if (isNodeOfType(constructor, "Identifier") && EXTERNAL_SYNC_OBSERVER_CONSTRUCTORS.has(constructor.name)) didFindExternalCall = true;
			return;
		}
		if (isNodeOfType(child, "AssignmentExpression")) {
			if (isNodeOfType(child.left, "MemberExpression") && isNodeOfType(child.left.property, "Identifier") && child.left.property.name === "current") didFindExternalCall = true;
			return;
		}
		if (!isNodeOfType(child, "CallExpression")) return;
		if (isNodeOfType(child.callee, "Identifier") && EXTERNAL_SYNC_DIRECT_CALLEE_NAMES.has(child.callee.name)) {
			didFindExternalCall = true;
			return;
		}
		if (isNodeOfType(child.callee, "MemberExpression") && isNodeOfType(child.callee.property, "Identifier")) {
			const propertyName = child.callee.property.name;
			if (EXTERNAL_SYNC_MEMBER_METHOD_NAMES.has(propertyName)) {
				didFindExternalCall = true;
				return;
			}
			if (EXTERNAL_SYNC_AMBIGUOUS_HTTP_METHOD_NAMES.has(propertyName)) {
				const receiverRootName = getRootIdentifierName(child.callee.object);
				if (receiverRootName !== null && EXTERNAL_SYNC_HTTP_CLIENT_RECEIVERS.has(receiverRootName)) didFindExternalCall = true;
			}
		}
	});
	return didFindExternalCall;
};
const noEffectChain = defineRule({
	id: "no-effect-chain",
	title: "Effects chained together",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Compute as much as possible during render (e.g. `const isGameOver = round > 5`) and write all related state inside the event handler that originally fires the chain. Each effect link adds an extra render and makes the code rigid as requirements evolve",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const useStateBindings = collectUseStateBindings(componentBody);
			if (useStateBindings.length === 0) return;
			const setterToStateName = /* @__PURE__ */ new Map();
			for (const binding of useStateBindings) setterToStateName.set(binding.setterName, binding.valueName);
			const effectInfos = [];
			for (const effectCall of findTopLevelEffectCalls(componentBody)) {
				const callback = getEffectCallback(effectCall);
				if (!callback) continue;
				effectInfos.push({
					node: effectCall,
					depNames: collectDepIdentifierNames(effectCall),
					writtenStateNames: collectWrittenStateNamesInEffect(callback, setterToStateName),
					isExternalSync: isExternalSyncEffect(callback)
				});
			}
			if (effectInfos.length < 2) return;
			const reportedNodes = /* @__PURE__ */ new Set();
			for (const writerEffect of effectInfos) {
				if (writerEffect.isExternalSync) continue;
				if (writerEffect.writtenStateNames.size === 0) continue;
				for (const readerEffect of effectInfos) {
					if (readerEffect === writerEffect) continue;
					if (readerEffect.isExternalSync) continue;
					if (readerEffect.depNames.size === 0) continue;
					let chainedStateName = null;
					for (const writtenName of writerEffect.writtenStateNames) if (readerEffect.depNames.has(writtenName)) {
						chainedStateName = writtenName;
						break;
					}
					if (!chainedStateName) continue;
					if (reportedNodes.has(readerEffect.node)) continue;
					reportedNodes.add(readerEffect.node);
					context.report({
						node: readerEffect.node,
						message: `Your screen redraws several times from a single action because one useEffect changes "${chainedStateName}", which sets off this one.`
					});
				}
			}
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/utils/find-triggered-side-effect-callee-name.ts
const findTriggeredSideEffectCalleeName = (consequentNode) => {
	let foundCalleeName = null;
	walkAst(consequentNode, (child) => {
		if (foundCalleeName) return false;
		if (!isNodeOfType(child, "CallExpression")) return;
		const callee = child.callee;
		if (isNodeOfType(callee, "Identifier") && EVENT_TRIGGERED_SIDE_EFFECT_CALLEES.has(callee.name)) {
			foundCalleeName = callee.name;
			return;
		}
		if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) {
			const propertyName = callee.property.name;
			const isUnambiguousMethod = EVENT_TRIGGERED_SIDE_EFFECT_MEMBER_METHODS.has(propertyName);
			const isNavigationMethod = EVENT_TRIGGERED_NAVIGATION_METHOD_NAMES.has(propertyName);
			if (!isUnambiguousMethod && !isNavigationMethod) return;
			const rootName = getRootIdentifierName(callee);
			if (isNavigationMethod && (rootName === null || !NAVIGATION_RECEIVER_NAMES.has(rootName))) return;
			foundCalleeName = rootName ? `${rootName}.${propertyName}` : propertyName;
		}
	});
	return foundCalleeName;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/has-document-class-list-mutation.ts
const DOCUMENT_CLASS_LIST_MUTATION_METHOD_NAMES = new Set([
	"add",
	"remove",
	"toggle"
]);
const DOCUMENT_CLASS_LIST_TARGET_NAMES = new Set(["body", "documentElement"]);
const hasDocumentClassListMutation = (node) => {
	let didFindMutation = false;
	walkAst(node, (child) => {
		if (didFindMutation) return false;
		if (!isNodeOfType(child, "CallExpression")) return;
		const callee = child.callee;
		if (!isNodeOfType(callee, "MemberExpression") || !isNodeOfType(callee.property, "Identifier") || !DOCUMENT_CLASS_LIST_MUTATION_METHOD_NAMES.has(callee.property.name)) return;
		const classListExpression = callee.object;
		if (!isNodeOfType(classListExpression, "MemberExpression") || !isNodeOfType(classListExpression.property, "Identifier") || classListExpression.property.name !== "classList") return;
		const documentElementExpression = classListExpression.object;
		if (!isNodeOfType(documentElementExpression, "MemberExpression") || !isNodeOfType(documentElementExpression.object, "Identifier") || documentElementExpression.object.name !== "document" || !isNodeOfType(documentElementExpression.property, "Identifier") || !DOCUMENT_CLASS_LIST_TARGET_NAMES.has(documentElementExpression.property.name)) return;
		didFindMutation = true;
	});
	return didFindMutation;
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-effect-event-handler.ts
const hasEventLikeNode = (node) => findTriggeredSideEffectCalleeName(node) !== null || hasDocumentClassListMutation(node);
const unwrapChainExpression = (node) => {
	if (!node) return null;
	if (isNodeOfType(node, "ChainExpression")) return node.expression;
	return node;
};
const collectGuardExpressions = (node, into) => {
	if (!node) return;
	const unwrappedNode = unwrapChainExpression(node);
	if (!unwrappedNode) return;
	const rootIdentifierName = getRootIdentifierName(unwrappedNode);
	if (rootIdentifierName) {
		into.push({
			expression: unwrappedNode,
			rootIdentifierName
		});
		return;
	}
	if (isNodeOfType(unwrappedNode, "UnaryExpression")) {
		collectGuardExpressions(unwrappedNode.argument, into);
		return;
	}
	if (isNodeOfType(unwrappedNode, "BinaryExpression") || isNodeOfType(unwrappedNode, "LogicalExpression")) {
		collectGuardExpressions(unwrappedNode.left, into);
		collectGuardExpressions(unwrappedNode.right, into);
		return;
	}
	if (isNodeOfType(unwrappedNode, "ConditionalExpression")) {
		collectGuardExpressions(unwrappedNode.test, into);
		collectGuardExpressions(unwrappedNode.consequent, into);
		collectGuardExpressions(unwrappedNode.alternate, into);
	}
};
const isReturnOnlyStatement = (node) => {
	if (isNodeOfType(node, "ReturnStatement")) return true;
	return isNodeOfType(node, "BlockStatement") && (node.body?.length ?? 0) === 1 && isNodeOfType(node.body?.[0], "ReturnStatement");
};
const hasEventLikeRemainingStatements = (statements) => statements.some((statement) => !isNodeOfType(statement, "ReturnStatement") && hasEventLikeNode(statement));
const doesGuardMatchDependency = (guardExpression, dependencyExpression) => {
	const unwrappedDependencyExpression = unwrapChainExpression(dependencyExpression);
	if (!unwrappedDependencyExpression) return false;
	if (areExpressionsStructurallyEqual(guardExpression.expression, unwrappedDependencyExpression)) return true;
	return isNodeOfType(unwrappedDependencyExpression, "Identifier") && unwrappedDependencyExpression.name === guardExpression.rootIdentifierName;
};
const hasDependencyMatch = (guardExpression, dependencyExpressions) => dependencyExpressions.some((dependencyExpression) => doesGuardMatchDependency(guardExpression, dependencyExpression));
const isStandaloneIdentifier = (node) => isNodeOfType(node, "Identifier") && !(isNodeOfType(node.parent, "MemberExpression") && node.parent.property === node && node.parent.computed !== true);
const doesNodeReferenceAnyRoot = (node, rootIdentifierNames) => {
	let didFindReference = false;
	const visit = (child) => {
		if (didFindReference) return false;
		if (isNodeOfType(child, "MemberExpression")) {
			const rootIdentifierName = getRootIdentifierName(child);
			if (rootIdentifierName && rootIdentifierNames.has(rootIdentifierName)) {
				didFindReference = true;
				return false;
			}
		}
		if (isStandaloneIdentifier(child) && rootIdentifierNames.has(child.name)) {
			didFindReference = true;
			return false;
		}
	};
	walkAst(node, visit);
	return didFindReference;
};
const doesEventLikeCallReferenceAnyRoot = (node, rootIdentifierNames) => {
	let didFindReference = false;
	walkAst(node, (child) => {
		if (didFindReference) return false;
		if (!isNodeOfType(child, "CallExpression")) return;
		if (findTriggeredSideEffectCalleeName(child) === null && !hasDocumentClassListMutation(child)) return;
		if (doesNodeReferenceAnyRoot(child, rootIdentifierNames)) {
			didFindReference = true;
			return false;
		}
	});
	return didFindReference;
};
const doesAnyEventLikeCallReferenceAnyRoot = (nodes, rootIdentifierNames) => nodes.some((node) => doesEventLikeCallReferenceAnyRoot(node, rootIdentifierNames));
const noEffectEventHandler = defineRule({
	id: "no-effect-event-handler",
	title: "Effect used as an event handler",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Move the conditional logic into onClick, onChange, or onSubmit handlers directly",
	create: (context) => {
		const propStackTracker = createComponentPropStackTracker();
		return {
			...propStackTracker.visitors,
			CallExpression(node) {
				if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1) || (node.arguments?.length ?? 0) < 2) return;
				const callback = getEffectCallback(node);
				if (!callback) return;
				const depsNode = node.arguments[1];
				if (!isNodeOfType(depsNode, "ArrayExpression") || !depsNode.elements?.length) return;
				const dependencyExpressions = depsNode.elements ?? [];
				const statements = getCallbackStatements(callback);
				if (statements.length === 0) return;
				const soleStatement = statements[0];
				if (!isNodeOfType(soleStatement, "IfStatement")) return;
				const guardExpressions = [];
				collectGuardExpressions(soleStatement.test, guardExpressions);
				const matchingPropGuardExpressions = guardExpressions.filter((guardExpression) => hasDependencyMatch(guardExpression, dependencyExpressions) && propStackTracker.isPropName(guardExpression.rootIdentifierName, node));
				if (matchingPropGuardExpressions.length === 0) return;
				const isSingleGuardedEventLikeStatement = statements.length === 1 && hasEventLikeNode(soleStatement.consequent);
				const isEarlyReturnGuardedEventLikeBody = statements.length > 1 && !soleStatement.alternate && isReturnOnlyStatement(soleStatement.consequent) && hasEventLikeRemainingStatements(statements.slice(1));
				if (!isSingleGuardedEventLikeStatement && !isEarlyReturnGuardedEventLikeBody) return;
				if (guardExpressions.some((guardExpression) => !matchingPropGuardExpressions.some((matchingGuardExpression) => matchingGuardExpression.expression === guardExpression.expression))) {
					const matchingPropRootNames = new Set(matchingPropGuardExpressions.map((guardExpression) => guardExpression.rootIdentifierName));
					if (!(isSingleGuardedEventLikeStatement ? doesEventLikeCallReferenceAnyRoot(soleStatement.consequent, matchingPropRootNames) : doesAnyEventLikeCallReferenceAnyRoot(statements.slice(1), matchingPropRootNames))) return;
				}
				context.report({
					node,
					message: "This useEffect is simulating an event handler, which costs an extra render & runs late."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-effect-event-in-deps.ts
const createComponentBindingStackTracker = (callbacks) => {
	const componentBindingStack = [];
	const isInsideComponent = () => componentBindingStack.length > 0;
	const isBoundName = (name) => {
		for (let frameIndex = componentBindingStack.length - 1; frameIndex >= 0; frameIndex--) if (componentBindingStack[frameIndex].has(name)) return true;
		return false;
	};
	const addBindingToCurrentFrame = (name) => {
		if (componentBindingStack.length === 0) return;
		componentBindingStack[componentBindingStack.length - 1].add(name);
	};
	return {
		isInsideComponent,
		isBoundName,
		addBindingToCurrentFrame,
		visitors: {
			FunctionDeclaration(node) {
				if (!isNodeOfType(node, "FunctionDeclaration")) return;
				if (!node.id || !isUppercaseName(node.id.name)) return;
				componentBindingStack.push(/* @__PURE__ */ new Set());
			},
			"FunctionDeclaration:exit"(node) {
				if (!isNodeOfType(node, "FunctionDeclaration")) return;
				if (!node.id || !isUppercaseName(node.id.name)) return;
				componentBindingStack.pop();
			},
			VariableDeclarator(node) {
				if (isComponentAssignment(node)) {
					componentBindingStack.push(/* @__PURE__ */ new Set());
					return;
				}
				callbacks?.onVariableDeclarator?.(node);
			},
			"VariableDeclarator:exit"(node) {
				if (isComponentAssignment(node)) componentBindingStack.pop();
			}
		}
	};
};
const noEffectEventInDeps = defineRule({
	id: "no-effect-event-in-deps",
	title: "Effect Event listed in effect deps",
	tags: ["test-noise"],
	severity: "error",
	recommendation: "Call the useEffectEvent function inside the effect body and don't list it in the deps. It changes on every render on purpose.",
	create: (context) => {
		const componentBindings = createComponentBindingStackTracker({ onVariableDeclarator: (declaratorNode) => {
			if (!isNodeOfType(declaratorNode, "VariableDeclarator")) return;
			if (!isNodeOfType(declaratorNode.id, "Identifier")) return;
			const initializer = declaratorNode.init;
			if (!initializer || !isNodeOfType(initializer, "CallExpression")) return;
			if (!isHookCall$1(initializer, "useEffectEvent")) return;
			componentBindings.addBindingToCurrentFrame(declaratorNode.id.name);
		} });
		return {
			...componentBindings.visitors,
			CallExpression(node) {
				if (!isHookCall$1(node, HOOKS_WITH_DEPS) || node.arguments.length < 2) return;
				if (!componentBindings.isInsideComponent()) return;
				const depsNode = node.arguments[1];
				if (!isNodeOfType(depsNode, "ArrayExpression")) return;
				for (const element of depsNode.elements ?? []) {
					if (!isNodeOfType(element, "Identifier")) continue;
					if (componentBindings.isBoundName(element.name)) context.report({
						node: element,
						message: `Listing "${element.name}" in the deps re-runs your effect every render & defeats useEffectEvent.`
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-effect-with-fresh-deps.ts
const classifyFreshDependency = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "ObjectExpression")) return "object";
	if (isNodeOfType(stripped, "ArrayExpression")) return "array";
	if (isNodeOfType(stripped, "ArrowFunctionExpression") || isNodeOfType(stripped, "FunctionExpression")) return "function";
	if (isNodeOfType(stripped, "JSXElement") || isNodeOfType(stripped, "JSXFragment")) return "JSX";
	if (isNodeOfType(stripped, "NewExpression")) return "instance";
	return null;
};
const resolveDependencyFreshness = (dep) => {
	const directKind = classifyFreshDependency(dep);
	if (directKind) return {
		kind: directKind,
		viaBindingName: null
	};
	const stripped = stripParenExpression(dep);
	if (!isNodeOfType(stripped, "Identifier")) return null;
	const binding = findVariableInitializer(stripped, stripped.name);
	if (!binding || !binding.initializer) return null;
	if (binding.scopeOwner.type === "Program") return null;
	const declarator = binding.bindingIdentifier.parent;
	if (!declarator || !isNodeOfType(declarator, "VariableDeclarator") || declarator.init !== binding.initializer) return null;
	const indirectKind = classifyFreshDependency(binding.initializer);
	if (!indirectKind) return null;
	return {
		kind: indirectKind,
		viaBindingName: stripped.name
	};
};
const noEffectWithFreshDeps = defineRule({
	id: "no-effect-with-fresh-deps",
	title: "Effect dependency recreated every render",
	severity: "error",
	category: "State & Effects",
	recommendation: "Move the value inside the hook body and depend on its simple inputs instead, or wrap it in useMemo / useCallback so it stays the same between renders.",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, HOOKS_WITH_DEPS)) return;
		const args = node.arguments ?? [];
		if (args.length < 2) return;
		const depsNode = args[1];
		if (!depsNode) return;
		const stripped = stripParenExpression(depsNode);
		if (!isNodeOfType(stripped, "ArrayExpression")) return;
		const calleeNode = node.callee;
		let hookName;
		if (isNodeOfType(calleeNode, "Identifier")) hookName = calleeNode.name;
		else if (isNodeOfType(calleeNode, "MemberExpression") && isNodeOfType(calleeNode.property, "Identifier")) hookName = calleeNode.property.name;
		else hookName = "hook";
		const elements = stripped.elements ?? [];
		for (const element of elements) {
			if (!element) continue;
			if (isNodeOfType(element, "SpreadElement")) continue;
			const freshness = resolveDependencyFreshness(element);
			if (!freshness) continue;
			const message = freshness.viaBindingName ? `Your ${hookName} runs every render because dep \`${freshness.viaBindingName}\` is a new ${freshness.kind} built fresh each time, so \`===\` always fails.` : `Your ${hookName} runs every render because its deps include a new ${freshness.kind} built fresh each time, so \`===\` always fails.`;
			context.report({
				node: element,
				message
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/security/no-eval.ts
const noEval = defineRule({
	id: "no-eval",
	title: "Use of eval()",
	severity: "error",
	recommendation: "Use `JSON.parse` for data, or rewrite the code so it doesn't build and run code from strings.",
	create: (context) => ({
		CallExpression(node) {
			if (isNodeOfType(node.callee, "Identifier") && node.callee.name === "eval") {
				context.report({
					node,
					message: "eval() is a code-injection vulnerability: it runs any string as code."
				});
				return;
			}
			if (isNodeOfType(node.callee, "Identifier") && (node.callee.name === "setTimeout" || node.callee.name === "setInterval") && isNodeOfType(node.arguments?.[0], "Literal") && typeof node.arguments[0].value === "string") context.report({
				node,
				message: `Passing a string to ${node.callee.name}() is a code-injection vulnerability, since it runs that string as code.`
			});
		},
		NewExpression(node) {
			if (isNodeOfType(node.callee, "Identifier") && node.callee.name === "Function") context.report({
				node,
				message: "new Function() is a code-injection vulnerability: it builds & runs code from a string."
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-event-handler.ts
const SETTER_NAME_PATTERN = /^set[A-Z]/;
const isSetterCallExpressionStatement = (node) => {
	if (!isNodeOfType(node, "ExpressionStatement")) return false;
	let expression = node.expression;
	if (expression && isNodeOfType(expression, "ChainExpression")) expression = expression.expression;
	if (!expression) return false;
	if (isNodeOfType(expression, "CallExpression")) {
		const callee = expression.callee;
		if (isNodeOfType(callee, "Identifier")) return SETTER_NAME_PATTERN.test(callee.name);
		if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && SETTER_NAME_PATTERN.test(callee.property.name)) return true;
		return false;
	}
	if (isNodeOfType(expression, "AssignmentExpression")) {
		const left = expression.left;
		if (isNodeOfType(left, "MemberExpression") && !left.computed && isNodeOfType(left.property, "Identifier") && left.property.name === "current" && isNodeOfType(left.object, "Identifier")) return true;
	}
	return false;
};
const REF_GUARD_SCAN_BUDGET = 50;
const containsRefGuard = (testNode) => {
	const stack = [testNode];
	let budget = REF_GUARD_SCAN_BUDGET;
	while (stack.length > 0 && budget-- > 0) {
		const node = stack.pop();
		if (isNodeOfType(node, "MemberExpression") && !node.computed && isNodeOfType(node.property, "Identifier") && node.property.name === "current" && isNodeOfType(node.object, "Identifier")) {
			const name = node.object.name;
			if (name === "ref" || name.endsWith("Ref") || name.endsWith("ref")) return true;
		}
		if (isNodeOfType(node, "LogicalExpression") || isNodeOfType(node, "BinaryExpression")) stack.push(node.left, node.right);
		else if (isNodeOfType(node, "UnaryExpression")) stack.push(node.argument);
		else if (isNodeOfType(node, "ConditionalExpression")) stack.push(node.test, node.consequent, node.alternate);
		else if (isNodeOfType(node, "MemberExpression")) stack.push(node.object);
		else if (isNodeOfType(node, "ChainExpression")) stack.push(node.expression);
	}
	return false;
};
const isSideEffectFreeExit = (statement) => {
	if (isNodeOfType(statement, "ContinueStatement")) return true;
	if (isNodeOfType(statement, "BreakStatement")) return true;
	if (!isNodeOfType(statement, "ReturnStatement")) return false;
	const argument = statement.argument;
	if (!argument) return true;
	if (isNodeOfType(argument, "Literal")) return true;
	if (isNodeOfType(argument, "Identifier")) return true;
	if (isNodeOfType(argument, "UnaryExpression") && argument.operator === "void") return true;
	return false;
};
const isPureEarlyExitConsequent = (consequent) => {
	if (isNodeOfType(consequent, "ReturnStatement") || isNodeOfType(consequent, "ContinueStatement") || isNodeOfType(consequent, "BreakStatement")) return isSideEffectFreeExit(consequent);
	if (isNodeOfType(consequent, "BlockStatement")) {
		const body = consequent.body ?? [];
		if (body.length === 0) return false;
		const last = body[body.length - 1];
		if (!isSideEffectFreeExit(last)) return false;
		for (let i = 0; i < body.length - 1; i++) if (!isSetterCallExpressionStatement(body[i])) return false;
		return true;
	}
	return false;
};
const noEventHandler = defineRule({
	id: "no-event-handler",
	title: "Event logic handled in an effect",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Run the side effect in the event handler that triggers it, instead of watching its state from a useEffect. See https://react.dev/learn/you-might-not-need-an-effect#sharing-logic-between-event-handlers",
	create: (context) => ({ CallExpression(node) {
		if (!isUseEffect(node)) return;
		const analysis = getProgramAnalysis(node);
		if (!analysis) return;
		if (hasCleanup(analysis, node)) return;
		if (!getEffectFnRefs(analysis, node)) return;
		const ifTestRefs = findDownstreamNodes(node, "IfStatement").filter((ifNode) => isNodeOfType(ifNode, "IfStatement") && !ifNode.alternate && !isPureEarlyExitConsequent(ifNode.consequent) && !containsRefGuard(ifNode.test)).flatMap((ifNode) => {
			if (!isNodeOfType(ifNode, "IfStatement")) return [];
			return getDownstreamRefs(analysis, ifNode.test).flatMap((ref) => getUpstreamRefs(analysis, ref));
		});
		const seenBindings = /* @__PURE__ */ new Set();
		const seenIdentifiers = /* @__PURE__ */ new Set();
		const dedupedRefs = ifTestRefs.filter((ref) => {
			const identifier = ref.identifier;
			if (!identifier) return false;
			const resolved = ref.resolved;
			if (resolved && seenBindings.has(resolved)) return false;
			if (resolved) seenBindings.add(resolved);
			if (seenIdentifiers.has(identifier)) return false;
			seenIdentifiers.add(identifier);
			return true;
		});
		for (const ref of dedupedRefs) if (isState(analysis, ref)) context.report({
			node: ref.identifier,
			message: "Faking an event handler with state plus a useEffect costs an extra render & runs late."
		});
		for (const ref of dedupedRefs) if (isProp(analysis, ref)) context.report({
			node: ref.identifier,
			message: "Faking an event handler with a prop plus a useEffect costs an extra render & runs late."
		});
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/utils/static-member-property-name.ts
const getStaticMemberPropertyName = (node) => {
	if (!node) return null;
	const unwrappedNode = stripParenExpression(node);
	if (!isNodeOfType(unwrappedNode, "MemberExpression")) return null;
	if (!unwrappedNode.computed && isNodeOfType(unwrappedNode.property, "Identifier")) return unwrappedNode.property.name;
	if (unwrappedNode.computed && isNodeOfType(unwrappedNode.property, "Literal") && typeof unwrappedNode.property.value === "string") return unwrappedNode.property.value;
	return null;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/event-handler-reference.ts
const isEventHandlerName = (name) => /^on[A-Z]/.test(name);
const getStaticMemberReferenceName = (node, resolveName = (name) => name) => {
	if (!isNodeOfType(node, "MemberExpression")) return null;
	if (!isNodeOfType(node.object, "Identifier")) return null;
	const propertyName = getStaticMemberPropertyName(node);
	return propertyName ? `${resolveName(node.object.name)}.${propertyName}` : null;
};
const getStaticPropertyKeyName = (node) => {
	if (!isNodeOfType(node, "Property")) return null;
	if (node.computed) return null;
	if (isNodeOfType(node.key, "Identifier")) return node.key.name;
	if (isNodeOfType(node.key, "Literal")) return String(node.key.value);
	return null;
};
const isEventHandlerValue = (node, eventHandlerReferenceNames, resolveName = (name) => name) => {
	if (isInlineFunctionExpression(node)) return true;
	if (isNodeOfType(node, "Identifier")) return eventHandlerReferenceNames.has(resolveName(node.name));
	const memberReferenceName = getStaticMemberReferenceName(node, resolveName);
	return Boolean(memberReferenceName && eventHandlerReferenceNames.has(memberReferenceName));
};
const isIntrinsicJsxAttribute = (node) => {
	if (!isNodeOfType(node, "JSXAttribute")) return false;
	const openingElement = node.parent;
	if (!isNodeOfType(openingElement, "JSXOpeningElement")) return false;
	const elementName = openingElement.name;
	if (!isNodeOfType(elementName, "JSXIdentifier")) return false;
	return /^[a-z]/.test(elementName.name);
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/scope-aware-reference-names.ts
const createComponentBindingScope = () => {
	const scope = {
		bindings: /* @__PURE__ */ new Map(),
		functionScope: null,
		isComponentScope: true,
		parent: null
	};
	scope.functionScope = scope;
	return scope;
};
const createBlockBindingScope = (parent) => ({
	bindings: /* @__PURE__ */ new Map(),
	functionScope: parent.functionScope ?? parent,
	isComponentScope: false,
	parent
});
const createFunctionBindingScope = (parent) => {
	const scope = {
		bindings: /* @__PURE__ */ new Map(),
		functionScope: null,
		isComponentScope: false,
		parent
	};
	scope.functionScope = scope;
	return scope;
};
const getBindingKey = (name, pattern, scope) => {
	if (scope.isComponentScope) return name;
	const rangeStart = pattern.range?.[0];
	if (rangeStart !== void 0) return `${name}@${rangeStart}`;
	const locationStart = pattern.loc?.start;
	return locationStart ? `${name}@${locationStart.line}:${locationStart.column}` : name;
};
const resolveBindingName = (scope, name) => {
	let currentScope = scope;
	while (currentScope) {
		const bindingName = currentScope.bindings.get(name);
		if (bindingName) return bindingName;
		currentScope = currentScope.parent;
	}
	return name;
};
const addPatternBindings = (pattern, scope) => {
	const bindingNames = /* @__PURE__ */ new Set();
	if (!pattern) return bindingNames;
	collectPatternNames(pattern, bindingNames);
	const resolvedBindingNames = /* @__PURE__ */ new Set();
	for (const bindingName of bindingNames) {
		const resolvedBindingName = getBindingKey(bindingName, pattern, scope);
		scope.bindings.set(bindingName, resolvedBindingName);
		resolvedBindingNames.add(resolvedBindingName);
	}
	return resolvedBindingNames;
};
const getVariableDeclarationScope = (declaration, scope) => {
	if (isNodeOfType(declaration, "VariableDeclaration") && declaration.kind === "var") return scope.functionScope ?? scope;
	return scope;
};
const addNames$1 = (into, names) => {
	for (const name of names) into.add(name);
};
const collectPatternDefaultNames = (pattern, names, scope, eventHandlerReferenceNames) => {
	if (!pattern) return;
	if (isNodeOfType(pattern, "AssignmentPattern")) {
		addNames$1(names, collectScopedReferenceNames(pattern.right, scope, eventHandlerReferenceNames));
		collectPatternDefaultNames(pattern.left, names, scope, eventHandlerReferenceNames);
		return;
	}
	if (isNodeOfType(pattern, "RestElement")) {
		collectPatternDefaultNames(pattern.argument, names, scope, eventHandlerReferenceNames);
		return;
	}
	if (isNodeOfType(pattern, "ArrayPattern")) {
		for (const element of pattern.elements ?? []) collectPatternDefaultNames(element, names, scope, eventHandlerReferenceNames);
		return;
	}
	if (isNodeOfType(pattern, "ObjectPattern")) for (const property of pattern.properties ?? []) {
		if (isNodeOfType(property, "RestElement")) {
			collectPatternDefaultNames(property.argument, names, scope, eventHandlerReferenceNames);
			continue;
		}
		if (isNodeOfType(property, "Property")) collectPatternDefaultNames(property.value, names, scope, eventHandlerReferenceNames);
	}
};
const collectScopedPatternDefaultReferenceNames = (pattern, scope, eventHandlerReferenceNames) => {
	const names = /* @__PURE__ */ new Set();
	collectPatternDefaultNames(pattern, names, scope, eventHandlerReferenceNames);
	return names;
};
const collectPatternAssignmentNames = (pattern, scope) => {
	const names = /* @__PURE__ */ new Set();
	collectPatternNames(pattern, names);
	const resolvedNames = /* @__PURE__ */ new Set();
	for (const name of names) resolvedNames.add(resolveBindingName(scope, name));
	return resolvedNames;
};
const collectChildrenReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	const names = /* @__PURE__ */ new Set();
	const nodeRecord = node;
	for (const key of Object.keys(nodeRecord)) {
		if (key === "parent") continue;
		const child = nodeRecord[key];
		if (Array.isArray(child)) {
			for (const item of child) if (isAstNode(item)) addNames$1(names, collectScopedReferenceNames(item, scope, eventHandlerReferenceNames));
			continue;
		}
		if (isAstNode(child)) addNames$1(names, collectScopedReferenceNames(child, scope, eventHandlerReferenceNames));
	}
	return names;
};
const collectFunctionReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "FunctionDeclaration") && !isNodeOfType(node, "FunctionExpression") && !isNodeOfType(node, "ArrowFunctionExpression")) return /* @__PURE__ */ new Set();
	const functionScope = createFunctionBindingScope(scope);
	if (isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression")) {
		if (node.id) functionScope.bindings.set(node.id.name, getBindingKey(node.id.name, node.id, functionScope));
	}
	const names = /* @__PURE__ */ new Set();
	for (const param of node.params ?? []) {
		addNames$1(names, collectScopedPatternDefaultReferenceNames(param, functionScope, eventHandlerReferenceNames));
		addPatternBindings(param, functionScope);
	}
	addNames$1(names, collectScopedReferenceNames(node.body, functionScope, eventHandlerReferenceNames));
	return names;
};
const collectVariableDeclarationReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "VariableDeclaration")) return /* @__PURE__ */ new Set();
	const names = /* @__PURE__ */ new Set();
	const declarationScope = getVariableDeclarationScope(node, scope);
	for (const declarator of node.declarations ?? []) {
		if (declarator.init) addNames$1(names, collectScopedReferenceNames(declarator.init, scope, eventHandlerReferenceNames));
		addNames$1(names, collectScopedPatternDefaultReferenceNames(declarator.id, scope, eventHandlerReferenceNames));
		addPatternBindings(declarator.id, declarationScope);
	}
	return names;
};
const collectPropertyReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "Property")) return /* @__PURE__ */ new Set();
	const propertyName = getStaticPropertyKeyName(node);
	if (propertyName && isEventHandlerName(propertyName) && isEventHandlerValue(node.value, eventHandlerReferenceNames, (name) => resolveBindingName(scope, name))) return /* @__PURE__ */ new Set();
	const names = /* @__PURE__ */ new Set();
	if (node.computed) addNames$1(names, collectScopedReferenceNames(node.key, scope, eventHandlerReferenceNames));
	addNames$1(names, collectScopedReferenceNames(node.value, scope, eventHandlerReferenceNames));
	return names;
};
const collectMemberExpressionReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "MemberExpression")) return /* @__PURE__ */ new Set();
	const names = collectScopedReferenceNames(node.object, scope, eventHandlerReferenceNames);
	if (node.computed) addNames$1(names, collectScopedReferenceNames(node.property, scope, eventHandlerReferenceNames));
	return names;
};
const collectBlockStatementReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "BlockStatement")) return /* @__PURE__ */ new Set();
	const names = /* @__PURE__ */ new Set();
	const blockScope = createBlockBindingScope(scope);
	for (const statement of node.body ?? []) addNames$1(names, collectScopedReferenceNames(statement, blockScope, eventHandlerReferenceNames));
	return names;
};
const collectForStatementReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "ForStatement")) return /* @__PURE__ */ new Set();
	const names = /* @__PURE__ */ new Set();
	const loopScope = createBlockBindingScope(scope);
	if (node.init) addNames$1(names, collectScopedReferenceNames(node.init, loopScope, eventHandlerReferenceNames));
	if (node.test) addNames$1(names, collectScopedReferenceNames(node.test, loopScope, eventHandlerReferenceNames));
	if (node.update) addNames$1(names, collectScopedReferenceNames(node.update, loopScope, eventHandlerReferenceNames));
	addNames$1(names, collectScopedReferenceNames(node.body, loopScope, eventHandlerReferenceNames));
	return names;
};
const collectForInOrOfStatementReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "ForInStatement") && !isNodeOfType(node, "ForOfStatement")) return /* @__PURE__ */ new Set();
	const names = /* @__PURE__ */ new Set();
	const loopScope = createBlockBindingScope(scope);
	addNames$1(names, collectScopedReferenceNames(node.right, loopScope, eventHandlerReferenceNames));
	if (isNodeOfType(node.left, "VariableDeclaration")) {
		const declarationScope = getVariableDeclarationScope(node.left, loopScope);
		for (const declarator of node.left.declarations ?? []) addPatternBindings(declarator.id, declarationScope);
	} else addPatternBindings(node.left, loopScope);
	addNames$1(names, collectScopedReferenceNames(node.body, loopScope, eventHandlerReferenceNames));
	return names;
};
const collectCatchClauseReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "CatchClause")) return /* @__PURE__ */ new Set();
	const catchScope = createBlockBindingScope(scope);
	addPatternBindings(node.param, catchScope);
	return collectScopedReferenceNames(node.body, catchScope, eventHandlerReferenceNames);
};
const collectJsxAttributeReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(node, "JSXAttribute")) return /* @__PURE__ */ new Set();
	const attributeName = isNodeOfType(node.name, "JSXIdentifier") ? node.name.name : null;
	if (!node.value) return /* @__PURE__ */ new Set();
	if (attributeName && isEventHandlerName(attributeName) && isIntrinsicJsxAttribute(node)) return /* @__PURE__ */ new Set();
	if (attributeName && isEventHandlerName(attributeName) && isNodeOfType(node.value, "JSXExpressionContainer") && isEventHandlerValue(node.value.expression, eventHandlerReferenceNames, (name) => resolveBindingName(scope, name))) return /* @__PURE__ */ new Set();
	return collectScopedReferenceNames(node.value, scope, eventHandlerReferenceNames);
};
const collectScopedReferenceNames = (node, scope, eventHandlerReferenceNames) => {
	if (isNodeOfType(node, "Identifier")) return new Set([resolveBindingName(scope, node.name)]);
	if (isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression")) return collectFunctionReferenceNames(node, scope, eventHandlerReferenceNames);
	if (isNodeOfType(node, "BlockStatement")) return collectBlockStatementReferenceNames(node, scope, eventHandlerReferenceNames);
	if (isNodeOfType(node, "VariableDeclaration")) return collectVariableDeclarationReferenceNames(node, scope, eventHandlerReferenceNames);
	if (isNodeOfType(node, "Property")) return collectPropertyReferenceNames(node, scope, eventHandlerReferenceNames);
	if (isNodeOfType(node, "MemberExpression")) return collectMemberExpressionReferenceNames(node, scope, eventHandlerReferenceNames);
	if (isNodeOfType(node, "ForStatement")) return collectForStatementReferenceNames(node, scope, eventHandlerReferenceNames);
	if (isNodeOfType(node, "ForInStatement") || isNodeOfType(node, "ForOfStatement")) return collectForInOrOfStatementReferenceNames(node, scope, eventHandlerReferenceNames);
	if (isNodeOfType(node, "CatchClause")) return collectCatchClauseReferenceNames(node, scope, eventHandlerReferenceNames);
	if (isNodeOfType(node, "JSXAttribute")) return collectJsxAttributeReferenceNames(node, scope, eventHandlerReferenceNames);
	return collectChildrenReferenceNames(node, scope, eventHandlerReferenceNames);
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/build-local-dependency-graph.ts
const addDependencies = (graph, declaredName, dependencyNames) => {
	const existing = graph.get(declaredName);
	if (existing === void 0) {
		graph.set(declaredName, new Set(dependencyNames));
		return;
	}
	for (const dependencyName of dependencyNames) existing.add(dependencyName);
};
const addDependencyNames = (into, dependencyNames) => {
	for (const dependencyName of dependencyNames) into.add(dependencyName);
};
const getPatternDefaultReferenceNames = (pattern, scope, eventHandlerReferenceNames) => collectScopedPatternDefaultReferenceNames(pattern, scope, eventHandlerReferenceNames);
const addVariableDeclarationDependencies = (graph, statement, scope, eventHandlerReferenceNames) => {
	if (!isNodeOfType(statement, "VariableDeclaration")) return;
	const declarationScope = getVariableDeclarationScope(statement, scope);
	for (const declarator of statement.declarations ?? []) {
		const dependencyNames = declarator.init ? collectScopedReferenceNames(declarator.init, scope, eventHandlerReferenceNames) : /* @__PURE__ */ new Set();
		addDependencyNames(dependencyNames, getPatternDefaultReferenceNames(declarator.id, scope, eventHandlerReferenceNames));
		const declaredNames = addPatternBindings(declarator.id, declarationScope);
		for (const declaredName of declaredNames) addDependencies(graph, declaredName, dependencyNames);
	}
};
const addAssignmentExpressionDependencies = (graph, expression, scope, eventHandlerReferenceNames, controlDependencyNames) => {
	if (!isNodeOfType(expression, "AssignmentExpression")) return;
	const dependencyNames = collectScopedReferenceNames(expression.right, scope, eventHandlerReferenceNames);
	addDependencyNames(dependencyNames, getPatternDefaultReferenceNames(expression.left, scope, eventHandlerReferenceNames));
	addDependencyNames(dependencyNames, controlDependencyNames);
	if (expression.operator !== "=") addDependencyNames(dependencyNames, collectScopedReferenceNames(expression.left, scope, eventHandlerReferenceNames));
	const assignedNames = collectPatternAssignmentNames(expression.left, scope);
	for (const assignedName of assignedNames) addDependencies(graph, assignedName, dependencyNames);
};
const collectExpressionDependencies = (graph, expression, scope, eventHandlerReferenceNames, controlDependencyNames) => {
	if (isNodeOfType(expression, "AssignmentExpression")) {
		addAssignmentExpressionDependencies(graph, expression, scope, eventHandlerReferenceNames, controlDependencyNames);
		return;
	}
	if (isNodeOfType(expression, "SequenceExpression")) {
		for (const subExpression of expression.expressions ?? []) collectExpressionDependencies(graph, subExpression, scope, eventHandlerReferenceNames, controlDependencyNames);
		return;
	}
	if (isNodeOfType(expression, "ConditionalExpression")) {
		collectExpressionDependencies(graph, expression.test, scope, eventHandlerReferenceNames, controlDependencyNames);
		const branchControlDependencyNames = new Set(controlDependencyNames);
		addDependencyNames(branchControlDependencyNames, collectScopedReferenceNames(expression.test, scope, eventHandlerReferenceNames));
		collectExpressionDependencies(graph, expression.consequent, scope, eventHandlerReferenceNames, branchControlDependencyNames);
		collectExpressionDependencies(graph, expression.alternate, scope, eventHandlerReferenceNames, branchControlDependencyNames);
		return;
	}
	if (isNodeOfType(expression, "LogicalExpression")) {
		collectExpressionDependencies(graph, expression.left, scope, eventHandlerReferenceNames, controlDependencyNames);
		const rightControlDependencyNames = new Set(controlDependencyNames);
		addDependencyNames(rightControlDependencyNames, collectScopedReferenceNames(expression.left, scope, eventHandlerReferenceNames));
		collectExpressionDependencies(graph, expression.right, scope, eventHandlerReferenceNames, rightControlDependencyNames);
	}
};
const collectStatementDependencies = (graph, statement, scope, eventHandlerReferenceNames, controlDependencyNames) => {
	if (isNodeOfType(statement, "VariableDeclaration")) {
		addVariableDeclarationDependencies(graph, statement, scope, eventHandlerReferenceNames);
		return;
	}
	if (isNodeOfType(statement, "FunctionDeclaration")) {
		if (!statement.id?.name) return;
		const declaredName = addPatternBindings(statement.id, scope).values().next().value;
		if (!declaredName) return;
		addDependencies(graph, declaredName, collectScopedReferenceNames(statement, scope, eventHandlerReferenceNames));
		return;
	}
	if (isNodeOfType(statement, "ExpressionStatement")) {
		collectExpressionDependencies(graph, statement.expression, scope, eventHandlerReferenceNames, controlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "AssignmentExpression")) {
		addAssignmentExpressionDependencies(graph, statement, scope, eventHandlerReferenceNames, controlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "BlockStatement")) {
		const blockScope = createBlockBindingScope(scope);
		collectStatementListDependencies(graph, statement.body, blockScope, eventHandlerReferenceNames, controlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "IfStatement")) {
		const branchControlDependencyNames = new Set(controlDependencyNames);
		addDependencyNames(branchControlDependencyNames, collectScopedReferenceNames(statement.test, scope, eventHandlerReferenceNames));
		collectStatementDependencies(graph, statement.consequent, scope, eventHandlerReferenceNames, branchControlDependencyNames);
		if (statement.alternate) collectStatementDependencies(graph, statement.alternate, scope, eventHandlerReferenceNames, branchControlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "SwitchStatement")) {
		for (const switchCase of statement.cases ?? []) {
			const caseControlDependencyNames = new Set(controlDependencyNames);
			addDependencyNames(caseControlDependencyNames, collectScopedReferenceNames(statement.discriminant, scope, eventHandlerReferenceNames));
			if (switchCase.test) addDependencyNames(caseControlDependencyNames, collectScopedReferenceNames(switchCase.test, scope, eventHandlerReferenceNames));
			const caseScope = createBlockBindingScope(scope);
			collectStatementListDependencies(graph, switchCase.consequent, caseScope, eventHandlerReferenceNames, caseControlDependencyNames);
		}
		return;
	}
	if (isNodeOfType(statement, "TryStatement")) {
		collectStatementDependencies(graph, statement.block, scope, eventHandlerReferenceNames, controlDependencyNames);
		if (statement.handler) collectStatementDependencies(graph, statement.handler, scope, eventHandlerReferenceNames, controlDependencyNames);
		if (statement.finalizer) collectStatementDependencies(graph, statement.finalizer, scope, eventHandlerReferenceNames, controlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "CatchClause")) {
		const catchScope = createBlockBindingScope(scope);
		addPatternBindings(statement.param, catchScope);
		collectStatementDependencies(graph, statement.body, catchScope, eventHandlerReferenceNames, controlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "ForStatement")) {
		const loopScope = createBlockBindingScope(scope);
		if (statement.init) collectStatementDependencies(graph, statement.init, loopScope, eventHandlerReferenceNames, controlDependencyNames);
		const loopControlDependencyNames = new Set(controlDependencyNames);
		if (statement.test) addDependencyNames(loopControlDependencyNames, collectScopedReferenceNames(statement.test, loopScope, eventHandlerReferenceNames));
		collectStatementDependencies(graph, statement.body, loopScope, eventHandlerReferenceNames, loopControlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "ForInStatement") || isNodeOfType(statement, "ForOfStatement")) {
		const loopControlDependencyNames = new Set(controlDependencyNames);
		addDependencyNames(loopControlDependencyNames, collectScopedReferenceNames(statement.right, scope, eventHandlerReferenceNames));
		const loopScope = createBlockBindingScope(scope);
		if (isNodeOfType(statement.left, "VariableDeclaration")) addVariableDeclarationDependencies(graph, statement.left, loopScope, eventHandlerReferenceNames);
		collectStatementDependencies(graph, statement.body, loopScope, eventHandlerReferenceNames, loopControlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "WhileStatement") || isNodeOfType(statement, "DoWhileStatement")) {
		const loopControlDependencyNames = new Set(controlDependencyNames);
		addDependencyNames(loopControlDependencyNames, collectScopedReferenceNames(statement.test, scope, eventHandlerReferenceNames));
		collectStatementDependencies(graph, statement.body, scope, eventHandlerReferenceNames, loopControlDependencyNames);
		return;
	}
	if (isNodeOfType(statement, "LabeledStatement")) collectStatementDependencies(graph, statement.body, scope, eventHandlerReferenceNames, controlDependencyNames);
};
const collectStatementListDependencies = (graph, statements, scope, eventHandlerReferenceNames, controlDependencyNames) => {
	for (const statement of statements ?? []) collectStatementDependencies(graph, statement, scope, eventHandlerReferenceNames, controlDependencyNames);
};
const buildLocalDependencyGraph = (componentBody, eventHandlerReferenceNames = /* @__PURE__ */ new Set()) => {
	const graph = /* @__PURE__ */ new Map();
	if (!isNodeOfType(componentBody, "BlockStatement")) return graph;
	const scope = createComponentBindingScope();
	collectStatementListDependencies(graph, componentBody.body, scope, eventHandlerReferenceNames, /* @__PURE__ */ new Set());
	return graph;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/collect-render-reachable-names.ts
const addNames = (into, names) => {
	for (const name of names) into.add(name);
};
const addDeclarationBindings = (statement, scope) => {
	if (isNodeOfType(statement, "VariableDeclaration")) {
		const declarationScope = getVariableDeclarationScope(statement, scope);
		for (const declarator of statement.declarations ?? []) addPatternBindings(declarator.id, declarationScope);
		return;
	}
	if (isNodeOfType(statement, "FunctionDeclaration") && statement.id) addPatternBindings(statement.id, scope);
};
const collectRenderReachableNamesFromStatements = (statements, names, scope, eventHandlerReferenceNames = /* @__PURE__ */ new Set()) => {
	let hasReturn = false;
	for (const statement of statements ?? []) if (collectRenderReachableNamesFromStatement(statement, names, scope, eventHandlerReferenceNames)) hasReturn = true;
	else addDeclarationBindings(statement, scope);
	return hasReturn;
};
const collectRenderReachableNamesFromStatement = (statement, names, scope, eventHandlerReferenceNames) => {
	if (isNodeOfType(statement, "ReturnStatement")) {
		if (statement.argument) addNames(names, collectScopedReferenceNames(statement.argument, scope, eventHandlerReferenceNames));
		return true;
	}
	if (isNodeOfType(statement, "BlockStatement")) return collectRenderReachableNamesFromStatements(statement.body, names, createBlockBindingScope(scope), eventHandlerReferenceNames);
	if (isNodeOfType(statement, "IfStatement")) {
		const consequentHasReturn = collectRenderReachableNamesFromStatement(statement.consequent, names, scope, eventHandlerReferenceNames);
		const alternateHasReturn = statement.alternate ? collectRenderReachableNamesFromStatement(statement.alternate, names, scope, eventHandlerReferenceNames) : false;
		if (consequentHasReturn || alternateHasReturn) addNames(names, collectScopedReferenceNames(statement.test, scope, eventHandlerReferenceNames));
		return consequentHasReturn || alternateHasReturn;
	}
	if (isNodeOfType(statement, "SwitchStatement")) {
		let hasReturn = false;
		for (const switchCase of statement.cases ?? []) {
			const caseScope = createBlockBindingScope(scope);
			if (!collectRenderReachableNamesFromStatements(switchCase.consequent, names, caseScope, eventHandlerReferenceNames)) continue;
			hasReturn = true;
			if (switchCase.test) addNames(names, collectScopedReferenceNames(switchCase.test, scope, eventHandlerReferenceNames));
		}
		if (hasReturn) addNames(names, collectScopedReferenceNames(statement.discriminant, scope, eventHandlerReferenceNames));
		return hasReturn;
	}
	if (isNodeOfType(statement, "TryStatement")) {
		const blockHasReturn = collectRenderReachableNamesFromStatement(statement.block, names, scope, eventHandlerReferenceNames);
		const handlerHasReturn = statement.handler ? collectRenderReachableNamesFromStatement(statement.handler, names, scope, eventHandlerReferenceNames) : false;
		const finalizerHasReturn = statement.finalizer ? collectRenderReachableNamesFromStatement(statement.finalizer, names, scope, eventHandlerReferenceNames) : false;
		return blockHasReturn || handlerHasReturn || finalizerHasReturn;
	}
	if (isNodeOfType(statement, "CatchClause")) {
		const catchScope = createBlockBindingScope(scope);
		addPatternBindings(statement.param, catchScope);
		return collectRenderReachableNamesFromStatement(statement.body, names, catchScope, eventHandlerReferenceNames);
	}
	if (isNodeOfType(statement, "WhileStatement") || isNodeOfType(statement, "DoWhileStatement")) {
		const bodyHasReturn = collectRenderReachableNamesFromStatement(statement.body, names, scope, eventHandlerReferenceNames);
		if (bodyHasReturn) addNames(names, collectScopedReferenceNames(statement.test, scope, eventHandlerReferenceNames));
		return bodyHasReturn;
	}
	if (isNodeOfType(statement, "ForStatement")) {
		const loopScope = createBlockBindingScope(scope);
		if (statement.init) addDeclarationBindings(statement.init, loopScope);
		if (!collectRenderReachableNamesFromStatement(statement.body, names, loopScope, eventHandlerReferenceNames)) return false;
		if (statement.init) addNames(names, collectScopedReferenceNames(statement.init, loopScope, eventHandlerReferenceNames));
		if (statement.test) addNames(names, collectScopedReferenceNames(statement.test, loopScope, eventHandlerReferenceNames));
		if (statement.update) addNames(names, collectScopedReferenceNames(statement.update, loopScope, eventHandlerReferenceNames));
		return true;
	}
	if (isNodeOfType(statement, "ForInStatement") || isNodeOfType(statement, "ForOfStatement")) {
		const rightNames = collectScopedReferenceNames(statement.right, scope, eventHandlerReferenceNames);
		const loopScope = createBlockBindingScope(scope);
		if (isNodeOfType(statement.left, "VariableDeclaration")) addDeclarationBindings(statement.left, loopScope);
		if (!collectRenderReachableNamesFromStatement(statement.body, names, loopScope, eventHandlerReferenceNames)) return false;
		addNames(names, rightNames);
		return true;
	}
	if (isNodeOfType(statement, "LabeledStatement")) return collectRenderReachableNamesFromStatement(statement.body, names, scope, eventHandlerReferenceNames);
	if (isNodeOfType(statement, "WithStatement")) {
		const bodyHasReturn = collectRenderReachableNamesFromStatement(statement.body, names, scope, eventHandlerReferenceNames);
		if (bodyHasReturn) addNames(names, collectScopedReferenceNames(statement.object, scope, eventHandlerReferenceNames));
		return bodyHasReturn;
	}
	return false;
};
const collectRenderReachableNames = (componentBody, eventHandlerReferenceNames = /* @__PURE__ */ new Set()) => {
	const names = /* @__PURE__ */ new Set();
	if (!isNodeOfType(componentBody, "BlockStatement")) return names;
	collectRenderReachableNamesFromStatements(componentBody.body, names, createComponentBindingScope(), eventHandlerReferenceNames);
	return names;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/expand-transitive-dependencies.ts
const expandTransitiveDependencies = (seedNames, dependencyGraph) => {
	const reachable = new Set(seedNames);
	const queue = Array.from(seedNames);
	while (queue.length > 0) {
		const currentName = queue.pop();
		if (currentName === void 0) continue;
		const dependencyNames = dependencyGraph.get(currentName);
		if (!dependencyNames) continue;
		for (const dependencyName of dependencyNames) {
			if (reachable.has(dependencyName)) continue;
			reachable.add(dependencyName);
			queue.push(dependencyName);
		}
	}
	return reachable;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/collect-function-like-local-names.ts
const isUseCallbackCall = (node) => isNodeOfType(node, "CallExpression") && getCalleeName(node.callee) === "useCallback";
const getCalleeName = (node) => {
	if (isNodeOfType(node, "Identifier")) return node.name;
	if (isNodeOfType(node, "MemberExpression")) return getStaticMemberPropertyName(node);
	return null;
};
const isFunctionLikeReference = (node, functionLikeLocalNames, scope) => {
	if (isInlineFunctionExpression(node) || isUseCallbackCall(node)) return true;
	if (isNodeOfType(node, "Identifier")) return functionLikeLocalNames.has(resolveBindingName(scope, node.name));
	const memberReferenceName = getStaticMemberReferenceName(node, (name) => resolveBindingName(scope, name));
	return Boolean(memberReferenceName && functionLikeLocalNames.has(memberReferenceName));
};
const addObjectPropertyFunctionNames = (objectBindingName, node, functionLikeLocalNames, scope) => {
	if (!isNodeOfType(node, "ObjectExpression")) return;
	for (const property of node.properties ?? []) {
		if (!isNodeOfType(property, "Property")) continue;
		const propertyName = getStaticPropertyKeyName(property);
		if (!propertyName) continue;
		if (!isFunctionLikeReference(property.value, functionLikeLocalNames, scope)) continue;
		functionLikeLocalNames.add(`${objectBindingName}.${propertyName}`);
	}
};
const addVariableDeclarationFunctionNames = (statement, functionLikeLocalNames, scope) => {
	if (!isNodeOfType(statement, "VariableDeclaration")) return;
	const declarationScope = getVariableDeclarationScope(statement, scope);
	for (const declarator of statement.declarations ?? []) {
		const declaredBindingNames = addPatternBindings(declarator.id, declarationScope);
		if (!declarator.init) continue;
		const isFunctionReference = isFunctionLikeReference(declarator.init, functionLikeLocalNames, scope);
		for (const declaredBindingName of declaredBindingNames) {
			if (isFunctionReference) functionLikeLocalNames.add(declaredBindingName);
			addObjectPropertyFunctionNames(declaredBindingName, declarator.init, functionLikeLocalNames, scope);
		}
	}
};
const collectStatementFunctionNames = (statement, functionLikeLocalNames, scope) => {
	if (isNodeOfType(statement, "FunctionDeclaration")) {
		if (statement.id) {
			const declaredBindingNames = addPatternBindings(statement.id, scope);
			for (const declaredBindingName of declaredBindingNames) functionLikeLocalNames.add(declaredBindingName);
		}
		return;
	}
	if (isNodeOfType(statement, "VariableDeclaration")) {
		addVariableDeclarationFunctionNames(statement, functionLikeLocalNames, scope);
		return;
	}
	if (isNodeOfType(statement, "BlockStatement")) {
		collectStatementListFunctionNames(statement.body, functionLikeLocalNames, createBlockBindingScope(scope));
		return;
	}
	if (isNodeOfType(statement, "IfStatement")) {
		collectStatementFunctionNames(statement.consequent, functionLikeLocalNames, scope);
		if (statement.alternate) collectStatementFunctionNames(statement.alternate, functionLikeLocalNames, scope);
		return;
	}
	if (isNodeOfType(statement, "SwitchStatement")) {
		for (const switchCase of statement.cases ?? []) collectStatementListFunctionNames(switchCase.consequent, functionLikeLocalNames, createBlockBindingScope(scope));
		return;
	}
	if (isNodeOfType(statement, "TryStatement")) {
		collectStatementFunctionNames(statement.block, functionLikeLocalNames, scope);
		if (statement.handler) {
			const catchScope = createBlockBindingScope(scope);
			addPatternBindings(statement.handler.param, catchScope);
			collectStatementFunctionNames(statement.handler.body, functionLikeLocalNames, catchScope);
		}
		if (statement.finalizer) collectStatementFunctionNames(statement.finalizer, functionLikeLocalNames, scope);
		return;
	}
	if (isNodeOfType(statement, "ForStatement")) {
		const loopScope = createBlockBindingScope(scope);
		if (statement.init && isNodeOfType(statement.init, "VariableDeclaration")) addVariableDeclarationFunctionNames(statement.init, functionLikeLocalNames, loopScope);
		collectStatementFunctionNames(statement.body, functionLikeLocalNames, loopScope);
		return;
	}
	if (isNodeOfType(statement, "ForInStatement") || isNodeOfType(statement, "ForOfStatement")) {
		const loopScope = createBlockBindingScope(scope);
		if (isNodeOfType(statement.left, "VariableDeclaration")) addVariableDeclarationFunctionNames(statement.left, functionLikeLocalNames, loopScope);
		else addPatternBindings(statement.left, loopScope);
		collectStatementFunctionNames(statement.body, functionLikeLocalNames, loopScope);
		return;
	}
	if (isNodeOfType(statement, "WhileStatement") || isNodeOfType(statement, "DoWhileStatement")) {
		collectStatementFunctionNames(statement.body, functionLikeLocalNames, scope);
		return;
	}
	if (isNodeOfType(statement, "LabeledStatement")) collectStatementFunctionNames(statement.body, functionLikeLocalNames, scope);
};
const collectStatementListFunctionNames = (statements, functionLikeLocalNames, scope) => {
	for (const statement of statements ?? []) collectStatementFunctionNames(statement, functionLikeLocalNames, scope);
};
const collectFunctionLikeLocalNames = (componentBody) => {
	const functionLikeLocalNames = /* @__PURE__ */ new Set();
	if (!isNodeOfType(componentBody, "BlockStatement")) return functionLikeLocalNames;
	let previousSize = -1;
	while (previousSize !== functionLikeLocalNames.size) {
		previousSize = functionLikeLocalNames.size;
		collectStatementListFunctionNames(componentBody.body, functionLikeLocalNames, createComponentBindingScope());
	}
	return functionLikeLocalNames;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/collect-handler-binding-names.ts
const collectHandlerBindingNames = (componentBody) => {
	const handlerNames = /* @__PURE__ */ new Set();
	walkAst(componentBody, (child) => {
		if (!isNodeOfType(child, "JSXAttribute")) return;
		if (!isNodeOfType(child.name, "JSXIdentifier")) return;
		if (!/^on[A-Z]/.test(child.name.name)) return;
		if (!isNodeOfType(child.value, "JSXExpressionContainer")) return;
		const expression = child.value.expression;
		if (isNodeOfType(expression, "Identifier")) handlerNames.add(expression.name);
	});
	return handlerNames;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/is-inside-event-handler.ts
const isInsideEventHandler = (node, handlerBindingNames) => {
	let cursor = node.parent ?? null;
	while (cursor) {
		if (isNodeOfType(cursor, "ArrowFunctionExpression") || isNodeOfType(cursor, "FunctionExpression") || isNodeOfType(cursor, "FunctionDeclaration")) {
			let outer = cursor.parent ?? null;
			while (outer) {
				if (isNodeOfType(outer, "JSXAttribute")) {
					const attrName = isNodeOfType(outer.name, "JSXIdentifier") ? outer.name.name : null;
					if (attrName && /^on[A-Z]/.test(attrName)) return true;
					return false;
				}
				if (isNodeOfType(outer, "VariableDeclarator")) {
					const declaredName = isNodeOfType(outer.id, "Identifier") ? outer.id.name : null;
					return Boolean(declaredName && handlerBindingNames.has(declaredName));
				}
				if (isNodeOfType(outer, "Program")) return false;
				outer = outer.parent ?? null;
			}
			return false;
		}
		cursor = cursor.parent ?? null;
	}
	return false;
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-event-trigger-state.ts
const SENTINEL_IDENTIFIER_NAMES = new Set([
	"undefined",
	"NaN",
	"null"
]);
const isSentinelIdentifier = (node) => isNodeOfType(node, "Identifier") && SENTINEL_IDENTIFIER_NAMES.has(node.name);
const getTriggerGuardRootName = (testNode) => {
	if (!testNode) return null;
	if (isNodeOfType(testNode, "Identifier")) return testNode.name;
	if (isNodeOfType(testNode, "BinaryExpression")) {
		if (![
			"!==",
			"===",
			"!=",
			"=="
		].includes(testNode.operator)) return null;
		for (const side of [testNode.left, testNode.right]) if (isNodeOfType(side, "Identifier") && !isSentinelIdentifier(side)) return side.name;
		return null;
	}
	if (isNodeOfType(testNode, "MemberExpression") && isNodeOfType(testNode.property, "Identifier") && testNode.property.name === "length") {
		if (isNodeOfType(testNode.object, "Identifier")) return testNode.object.name;
	}
	if (isNodeOfType(testNode, "UnaryExpression") && testNode.operator === "!") return getTriggerGuardRootName(testNode.argument);
	return null;
};
const collectHandlerOnlyWriteStateNames = (componentBody, useStateBindings, handlerBindingNames) => {
	const handlerOnlyWriteStateNames = /* @__PURE__ */ new Set();
	for (const binding of useStateBindings) {
		let didFindAnySetterCall = false;
		let areAllSetterCallsInHandlers = true;
		walkAst(componentBody, (child) => {
			if (!areAllSetterCallsInHandlers) return false;
			if (!isNodeOfType(child, "CallExpression")) return;
			if (!isNodeOfType(child.callee, "Identifier")) return;
			if (child.callee.name !== binding.setterName) return;
			didFindAnySetterCall = true;
			if (!isInsideEventHandler(child, handlerBindingNames)) areAllSetterCallsInHandlers = false;
		});
		if (didFindAnySetterCall && areAllSetterCallsInHandlers) handlerOnlyWriteStateNames.add(binding.valueName);
	}
	return handlerOnlyWriteStateNames;
};
const noEventTriggerState = defineRule({
	id: "no-event-trigger-state",
	title: "State exists only to trigger an effect",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Delete the trigger state (`useState(null)` plus the `useEffect` that watches it) and call the side effect like `post(...)`, `navigate(...)`, or `track(...)` directly in the event handler that set it. State shouldn't exist just to kick off an effect.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const useStateBindings = collectUseStateBindings(componentBody);
			if (useStateBindings.length === 0) return;
			const handlerOnlyWriteStateNames = collectHandlerOnlyWriteStateNames(componentBody, useStateBindings, collectHandlerBindingNames(componentBody));
			if (handlerOnlyWriteStateNames.size === 0) return;
			const eventHandlerReferenceNames = collectFunctionLikeLocalNames(componentBody);
			const dependencyGraph = buildLocalDependencyGraph(componentBody, eventHandlerReferenceNames);
			const renderReachableNames = expandTransitiveDependencies(collectRenderReachableNames(componentBody, eventHandlerReferenceNames), dependencyGraph);
			walkAst(componentBody, (effectCall) => {
				if (!isNodeOfType(effectCall, "CallExpression")) return;
				if (!isHookCall$1(effectCall, EFFECT_HOOK_NAMES$1)) return;
				if ((effectCall.arguments?.length ?? 0) < 2) return;
				const depsNode = effectCall.arguments[1];
				if (!isNodeOfType(depsNode, "ArrayExpression")) return;
				if ((depsNode.elements?.length ?? 0) !== 1) return;
				const depElement = depsNode.elements[0];
				if (!isNodeOfType(depElement, "Identifier")) return;
				if (!handlerOnlyWriteStateNames.has(depElement.name)) return;
				if (renderReachableNames.has(depElement.name)) return;
				const callback = getEffectCallback(effectCall);
				if (!callback) return;
				const bodyStatements = getCallbackStatements(callback);
				if (bodyStatements.length !== 1) return;
				const soleStatement = bodyStatements[0];
				if (!isNodeOfType(soleStatement, "IfStatement")) return;
				if (getTriggerGuardRootName(soleStatement.test) !== depElement.name) return;
				const sideEffectCalleeName = findTriggeredSideEffectCalleeName(soleStatement.consequent);
				if (!sideEffectCalleeName) return;
				context.report({
					node: effectCall,
					message: `useState "${depElement.name}" forces an extra render just to fire "${sideEffectCalleeName}(...)" from a useEffect.`
				});
			});
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-fetch-in-effect.ts
const noFetchInEffect = defineRule({
	id: "no-fetch-in-effect",
	title: "Data fetching inside an effect",
	severity: "warn",
	recommendation: "Use `useQuery()` from @tanstack/react-query, `useSWR()`, or fetch in a Server Component instead",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1)) return;
		const callback = getEffectCallback(node);
		if (!callback) return;
		if (containsFetchCall(callback)) context.report({
			node,
			message: "fetch() inside useEffect races, double-fires & leaks for your users."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-find-dom-node.ts
const ALLOWED_NAMESPACES = new Set([
	"React",
	"ReactDOM",
	"ReactDom"
]);
const MESSAGE$19 = "`findDOMNode` crashes your app in React 19 because it was removed.";
const noFindDomNode = defineRule({
	id: "no-find-dom-node",
	title: "Use of findDOMNode",
	severity: "warn",
	recommendation: "Use a ref (`useRef` or `createRef`) to reach DOM nodes instead of `findDOMNode`.",
	create: (context) => ({ CallExpression(node) {
		const callee = node.callee;
		if (isNodeOfType(callee, "Identifier") && callee.name === "findDOMNode") {
			context.report({
				node: callee,
				message: MESSAGE$19
			});
			return;
		}
		if (isNodeOfType(callee, "MemberExpression")) {
			if (!isNodeOfType(callee.object, "Identifier")) return;
			if (!ALLOWED_NAMESPACES.has(callee.object.name)) return;
			if (!isNodeOfType(callee.property, "Identifier")) return;
			if (callee.property.name !== "findDOMNode") return;
			context.report({
				node: callee.property,
				message: MESSAGE$19
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/view-transitions/no-flush-sync.ts
const noFlushSync = defineRule({
	id: "no-flush-sync",
	title: "flushSync skips View Transitions",
	tags: ["test-noise"],
	severity: "warn",
	category: "Performance",
	recommendation: "flushSync forces an immediate update that skips View Transitions and concurrent rendering. Use startTransition for updates that are not urgent.",
	create: (context) => ({ ImportDeclaration(node) {
		if (node.source?.value !== "react-dom") return;
		for (const specifier of node.specifiers ?? []) {
			if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
			if (getImportedName$1(specifier) === "flushSync") context.report({
				node: specifier,
				message: "Your users lose View Transitions & concurrent rendering when flushSync from react-dom forces an immediate update."
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/bundle-size/no-full-lodash-import.ts
const noFullLodashImport = defineRule({
	id: "no-full-lodash-import",
	title: "Full lodash import",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Import just the function you need: `import debounce from 'lodash/debounce'`. Saves about 70kb.",
	create: (context) => ({ ImportDeclaration(node) {
		if (node.source?.value === "lodash") context.report({
			node,
			message: "Importing all of lodash ships the whole library to your users & slows page load. Import from 'lodash/functionName' instead."
		});
	} })
});
//#endregion
//#region src/plugin/rules/architecture/no-generic-handler-names.ts
const noGenericHandlerNames = defineRule({
	id: "no-generic-handler-names",
	title: "Vague event handler name",
	severity: "warn",
	defaultEnabled: false,
	tags: ["test-noise"],
	recommendation: "Rename it to say what it does. For example `handleSubmit` could be `saveUserProfile`, and `handleClick` could be `toggleSidebar`.",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || !node.name.name.startsWith("on")) return;
		if (!node.value || !isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const eventSuffix = node.name.name.slice(2);
		if (!GENERIC_EVENT_SUFFIXES.has(eventSuffix)) return;
		const mirroredHandlerName = `handle${eventSuffix}`;
		const expression = node.value.expression;
		if (isNodeOfType(expression, "Identifier") && expression.name === mirroredHandlerName) context.report({
			node,
			message: `The handler name "${expression.name}" says when it runs, not what it does, so name it after the action instead.`
		});
	} })
});
//#endregion
//#region src/plugin/utils/function-contains-react-render-output.ts
const NESTED_RENDER_EVIDENCE_BOUNDARY_TYPES = new Set([
	"FunctionDeclaration",
	"FunctionExpression",
	"ArrowFunctionExpression",
	"ClassDeclaration",
	"ClassExpression"
]);
const isReactImport$1 = (symbol) => {
	let importDeclaration = symbol.declarationNode?.parent;
	while (importDeclaration && !isNodeOfType(importDeclaration, "ImportDeclaration")) importDeclaration = importDeclaration.parent ?? null;
	if (!importDeclaration || !isNodeOfType(importDeclaration, "ImportDeclaration")) return false;
	return importDeclaration.source.value === "react";
};
const getImportedName = (symbol) => {
	if (symbol.kind !== "import") return null;
	if (!isReactImport$1(symbol)) return null;
	return getImportedName$1(symbol.declarationNode) ?? null;
};
const isReactNamespaceImport = (symbol) => {
	if (symbol.kind !== "import") return false;
	if (!isReactImport$1(symbol)) return false;
	return isNodeOfType(symbol.declarationNode, "ImportDefaultSpecifier") || isNodeOfType(symbol.declarationNode, "ImportNamespaceSpecifier");
};
const isReactCreateElementIdentifierCall = (callee, scopes) => {
	if (!isNodeOfType(callee, "Identifier")) return false;
	const symbol = scopes.symbolFor(callee);
	return Boolean(symbol && getImportedName(symbol) === "createElement");
};
const isReactCreateElementMemberCall = (callee, scopes) => {
	if (!isNodeOfType(callee, "MemberExpression")) return false;
	if (callee.computed) return false;
	if (!isNodeOfType(callee.object, "Identifier")) return false;
	if (!isNodeOfType(callee.property, "Identifier")) return false;
	if (callee.property.name !== "createElement") return false;
	const symbol = scopes.symbolFor(callee.object);
	return Boolean(symbol && isReactNamespaceImport(symbol));
};
const isReactCreateElementCall = (node, scopes) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	return isReactCreateElementIdentifierCall(node.callee, scopes) || isReactCreateElementMemberCall(node.callee, scopes);
};
const containsRenderOutput = (node, rootNode, scopes) => {
	if (node !== rootNode && NESTED_RENDER_EVIDENCE_BOUNDARY_TYPES.has(node.type)) return false;
	if (node.type === "JSXElement" || node.type === "JSXFragment") return true;
	if (isReactCreateElementCall(node, scopes)) return true;
	const nodeRecord = node;
	for (const key of Object.keys(nodeRecord)) {
		if (key === "parent") continue;
		const child = nodeRecord[key];
		if (Array.isArray(child)) {
			for (const innerChild of child) if (isAstNode(innerChild) && containsRenderOutput(innerChild, rootNode, scopes)) return true;
		} else if (isAstNode(child) && containsRenderOutput(child, rootNode, scopes)) return true;
	}
	return false;
};
const functionContainsReactRenderOutput = (functionNode, scopes) => containsRenderOutput(functionNode, functionNode, scopes);
//#endregion
//#region src/plugin/rules/architecture/no-giant-component.ts
const noGiantComponent = defineRule({
	id: "no-giant-component",
	title: "Component is too large",
	severity: "warn",
	tags: ["test-noise", "react-jsx-only"],
	recommendation: "Pull each section into its own component, like `<UserHeader />` and `<UserActions />`.",
	create: (context) => {
		const getOversizedComponentLineCount = (bodyNode) => {
			if (!bodyNode.loc) return null;
			const lineCount = bodyNode.loc.end.line - bodyNode.loc.start.line + 1;
			return lineCount > 300 ? lineCount : null;
		};
		const reportOversizedComponent = (nameNode, componentName, lineCount) => {
			context.report({
				node: nameNode,
				message: `Component "${componentName}" is ${lineCount} lines long, which is hard to read & change. Split it into a few smaller components.`
			});
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				const lineCount = getOversizedComponentLineCount(node);
				if (lineCount === null) return;
				if (!functionContainsReactRenderOutput(node, context.scopes)) return;
				reportOversizedComponent(node.id, node.id.name, lineCount);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.id, "Identifier") || !node.init) return;
				const lineCount = getOversizedComponentLineCount(node.init);
				if (lineCount === null) return;
				if (!functionContainsReactRenderOutput(node.init, context.scopes)) return;
				reportOversizedComponent(node.id, node.id.name, lineCount);
			}
		};
	}
});
//#endregion
//#region src/plugin/constants/style.ts
const LAYOUT_PROPERTIES = new Set([
	"width",
	"height",
	"top",
	"left",
	"right",
	"bottom",
	"padding",
	"paddingTop",
	"paddingRight",
	"paddingBottom",
	"paddingLeft",
	"margin",
	"marginTop",
	"marginRight",
	"marginBottom",
	"marginLeft",
	"borderWidth",
	"fontSize",
	"lineHeight",
	"gap"
]);
const MOTION_ANIMATE_PROPS = new Set([
	"animate",
	"initial",
	"exit",
	"whileHover",
	"whileTap",
	"whileFocus",
	"whileDrag",
	"whileInView"
]);
const BLUR_VALUE_PATTERN = /blur\((\d+(?:\.\d+)?)px\)/;
const ANIMATION_CALLBACK_NAMES = new Set(["requestAnimationFrame", "setInterval"]);
const MOTION_LIBRARY_PACKAGES = new Set(["framer-motion", "motion"]);
const BOUNCE_ANIMATION_NAMES = new Set([
	"bounce",
	"elastic",
	"wobble",
	"jiggle",
	"spring"
]);
//#endregion
//#region src/plugin/rules/performance/no-global-css-variable-animation.ts
const noGlobalCssVariableAnimation = defineRule({
	id: "no-global-css-variable-animation",
	title: "Animating a global CSS variable",
	tags: ["test-noise"],
	severity: "error",
	recommendation: "Set the variable on the element that needs it instead of a parent, or use `@property` with `inherits: false`. Better yet, update `element.style.transform` directly",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "Identifier")) return;
		if (!ANIMATION_CALLBACK_NAMES.has(node.callee.name)) return;
		const callback = node.arguments?.[0];
		if (!callback) return;
		const calleeName = node.callee.name;
		walkAst(callback, (child) => {
			if (!isNodeOfType(child, "CallExpression")) return;
			if (!isMemberProperty(child.callee, "setProperty")) return;
			if (!isNodeOfType(child.arguments?.[0], "Literal")) return;
			const variableName = child.arguments[0].value;
			if (typeof variableName !== "string" || !variableName.startsWith("--")) return;
			context.report({
				node: child,
				message: `This forces every element using "${variableName}" to recompute its styles each frame because ${calleeName} changes it every frame, so set it on just the element that needs it`
			});
		});
	} })
});
//#endregion
//#region src/plugin/rules/design/utils/get-string-from-class-name-attr.ts
const getStringFromClassNameAttr = (node) => {
	if (!isNodeOfType(node, "JSXOpeningElement")) return null;
	const classAttr = findJsxAttribute(node.attributes ?? [], "className");
	if (!classAttr?.value) return null;
	if (isNodeOfType(classAttr.value, "Literal") && typeof classAttr.value.value === "string") return classAttr.value.value;
	if (isNodeOfType(classAttr.value, "JSXExpressionContainer") && isNodeOfType(classAttr.value.expression, "Literal") && typeof classAttr.value.expression.value === "string") return classAttr.value.expression.value;
	if (isNodeOfType(classAttr.value, "JSXExpressionContainer") && isNodeOfType(classAttr.value.expression, "TemplateLiteral") && classAttr.value.expression.quasis?.length === 1) return classAttr.value.expression.quasis[0].value?.raw ?? null;
	return null;
};
//#endregion
//#region src/plugin/rules/design/no-gradient-text.ts
const noGradientText = defineRule({
	id: "no-gradient-text",
	title: "Gradient text is hard to read",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Use a solid text color so it stays readable. For emphasis, change the weight, size, or color instead of using a gradient.",
	create: (context) => ({
		JSXAttribute(node) {
			const expression = getInlineStyleExpression(node);
			if (!expression) return;
			let hasBackgroundClipText = false;
			let hasGradientBackground = false;
			for (const property of expression.properties ?? []) {
				const key = getStylePropertyKey(property);
				const value = getStylePropertyStringValue(property);
				if (!key || !value) continue;
				if ((key === "backgroundClip" || key === "WebkitBackgroundClip") && value === "text") hasBackgroundClipText = true;
				if ((key === "backgroundImage" || key === "background") && value.includes("gradient")) hasGradientBackground = true;
			}
			if (hasBackgroundClipText && hasGradientBackground) context.report({
				node,
				message: "Your users struggle to read gradient text (background-clip: text), so use a solid text color instead."
			});
		},
		JSXOpeningElement(node) {
			const classStr = getStringFromClassNameAttr(node);
			if (!classStr) return;
			if (/\bbg-clip-text\b/.test(classStr) && /\bbg-gradient-to-/.test(classStr)) context.report({
				node,
				message: "Your users struggle to read gradient text (bg-clip-text + bg-gradient), so use a solid text color instead."
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/design/no-gray-on-colored-background.ts
const noGrayOnColoredBackground = defineRule({
	id: "no-gray-on-colored-background",
	title: "Gray text on colored background",
	tags: ["test-noise"],
	severity: "warn",
	category: "Accessibility",
	recommendation: "Use white or near-white text, or a darker shade of the background color. Gray text on colored backgrounds looks washed out.",
	create: (context) => ({ JSXOpeningElement(node) {
		const classStr = getStringFromClassNameAttr(node);
		if (!classStr) return;
		const grayTextMatch = classStr.match(/\btext-(?:gray|slate|zinc|neutral|stone)-\d+\b/);
		const coloredBgMatch = classStr.match(/\bbg-(?:red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-\d+\b/);
		if (grayTextMatch && coloredBgMatch) context.report({
			node,
			message: `Your users see washed-out gray text (${grayTextMatch[0]}) on a colored background (${coloredBgMatch[0]}), so use white or a darker shade of the background color.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-initialize-state.ts
const noInitializeState = defineRule({
	id: "no-initialize-state",
	title: "State initialized from a mount effect",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Pass the initial value directly to useState() instead of setting it from a mount-only useEffect. For SSR hydration, prefer useSyncExternalStore().",
	create: (context) => ({ CallExpression(node) {
		if (!isUseEffect(node)) return;
		const analysis = getProgramAnalysis(node);
		if (!analysis) return;
		const effectFnRefs = getEffectFnRefs(analysis, node);
		const depsRefs = getEffectDepsRefs(analysis, node);
		if (!effectFnRefs || !depsRefs) return;
		const effectFn = getEffectFn(analysis, node);
		if (!effectFn) return;
		if (!(depsRefs.filter((ref) => !isStateSetter(analysis, ref)).length === 0)) return;
		for (const ref of effectFnRefs) {
			if (!isStateSetterCall(analysis, ref)) continue;
			if (!isSynchronous(ref.identifier, effectFn)) continue;
			const callExpr = getCallExpr(ref);
			if (!callExpr || !isNodeOfType(callExpr, "CallExpression")) continue;
			const useStateDecl = getUseStateDecl(analysis, ref);
			if (!useStateDecl || !isNodeOfType(useStateDecl, "VariableDeclarator")) continue;
			if (!isNodeOfType(useStateDecl.id, "ArrayPattern")) continue;
			const elements = useStateDecl.id.elements ?? [];
			const stateBinding = elements[0] ?? elements[1];
			const stateName = stateBinding && isNodeOfType(stateBinding, "Identifier") ? stateBinding.name : "<state>";
			context.report({
				node: callExpr,
				message: `Your users see an extra render with empty "${stateName}" because a useEffect sets its starting value.`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/design/no-inline-bounce-easing.ts
const isOvershootCubicBezier = (value) => {
	const match = value.match(/cubic-bezier\(\s*([\d.-]+)\s*,\s*([\d.-]+)\s*,\s*([\d.-]+)\s*,\s*([\d.-]+)\s*\)/);
	if (!match) return false;
	const controlY1 = parseFloat(match[2]);
	const controlY2 = parseFloat(match[4]);
	return controlY1 < -.1 || controlY1 > 1.1 || controlY2 < -.1 || controlY2 > 1.1;
};
const hasBounceAnimationName = (value) => {
	const lowerValue = value.toLowerCase();
	for (const name of BOUNCE_ANIMATION_NAMES) if (lowerValue.includes(name)) return true;
	return false;
};
const noInlineBounceEasing = defineRule({
	id: "no-inline-bounce-easing",
	title: "Bouncy easing animation",
	severity: "warn",
	tags: ["test-noise"],
	category: "Performance",
	recommendation: "Use `cubic-bezier(0.16, 1, 0.3, 1)` (ease-out-expo) for a natural finish. Real objects don't bounce.",
	create: (context) => ({
		JSXAttribute(node) {
			const expression = getInlineStyleExpression(node);
			if (!expression) return;
			for (const property of expression.properties ?? []) {
				const key = getStylePropertyKey(property);
				if (!key) continue;
				const value = getStylePropertyStringValue(property);
				if (!value) continue;
				if ((key === "transition" || key === "transitionTimingFunction" || key === "animation" || key === "animationTimingFunction") && isOvershootCubicBezier(value)) context.report({
					node: property,
					message: "Your users see a dated, bouncy animation, so use ease-out or cubic-bezier(0.16, 1, 0.3, 1) for a smooth finish."
				});
				if ((key === "animation" || key === "animationName") && hasBounceAnimationName(value)) context.report({
					node: property,
					message: "Your users see a tacky bounce animation, so use a smooth ease-out (like ease-out-quart or expo) for a natural finish."
				});
			}
		},
		JSXOpeningElement(node) {
			const classStr = getStringFromClassNameAttr(node);
			if (!classStr) return;
			if (/\banimate-bounce\b/.test(classStr)) context.report({
				node,
				message: "Your users see a dated, tacky animate-bounce, so use a subtle ease-out transform for a smoother finish."
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/design/no-inline-exhaustive-style.ts
const noInlineExhaustiveStyle = defineRule({
	id: "no-inline-exhaustive-style",
	title: "Too many inline style properties",
	severity: "warn",
	tags: ["test-noise", "react-jsx-only"],
	recommendation: "Move the styles to a CSS class, CSS module, Tailwind utilities, or a styled component. Big inline objects are hard to read and rebuild on every update.",
	create: (context) => ({ JSXAttribute(node) {
		const expression = getInlineStyleExpression(node);
		if (!expression) return;
		const propertyCount = expression.properties?.filter((property) => isNodeOfType(property, "Property")).length ?? 0;
		if (propertyCount >= 8) context.report({
			node: expression,
			message: `This inline style has ${propertyCount} properties, which is hard to read & rebuilds every render. Move it to a CSS class, CSS module, or styled component.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/performance/no-inline-prop-on-memo-component.ts
const isMemoCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (isNodeOfType(node.callee, "Identifier") && node.callee.name === "memo") return true;
	if (isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.object, "Identifier") && node.callee.object.name === "React" && isNodeOfType(node.callee.property, "Identifier") && node.callee.property.name === "memo") return true;
	return false;
};
const isInlineReference = (node) => {
	if (isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.property, "Identifier") && node.callee.property.name === "bind") return "functions";
	if (isNodeOfType(node, "ObjectExpression")) return "objects";
	if (isNodeOfType(node, "ArrayExpression")) return "Arrays";
	if (isNodeOfType(node, "JSXElement") || isNodeOfType(node, "JSXFragment")) return "JSX";
	return null;
};
const noInlinePropOnMemoComponent = defineRule({
	id: "no-inline-prop-on-memo-component",
	title: "Inline prop defeats memo()",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Move the inline `() => ...` / `[]` / `{}` to a stable value with useMemo, useCallback, or module scope, so the memoized child stops redrawing on every parent render",
	create: (context) => {
		const memoizedComponentNames = /* @__PURE__ */ new Set();
		return {
			VariableDeclarator(node) {
				if (!isNodeOfType(node.id, "Identifier") || !node.init) return;
				if (isMemoCall(node.init)) memoizedComponentNames.add(node.id.name);
			},
			ExportDefaultDeclaration(node) {
				if (node.declaration && isNodeOfType(node.declaration, "CallExpression") && isMemoCall(node.declaration)) {
					const innerArgument = node.declaration.arguments?.[0];
					if (isNodeOfType(innerArgument, "Identifier")) memoizedComponentNames.add(innerArgument.name);
				}
			},
			JSXAttribute(node) {
				if (!node.value || !isNodeOfType(node.value, "JSXExpressionContainer")) return;
				const openingElement = node.parent;
				if (!openingElement || !isNodeOfType(openingElement, "JSXOpeningElement")) return;
				let elementName = null;
				if (isNodeOfType(openingElement.name, "JSXIdentifier")) elementName = openingElement.name.name;
				if (!elementName || !memoizedComponentNames.has(elementName)) return;
				const propType = isInlineReference(node.value.expression);
				if (propType) context.report({
					node: node.value.expression,
					message: `This redraws ${elementName} on every render because the prop is ${propType} built right here, so memo() can't skip it. Move it to a stable value with useMemo, useCallback, or module scope`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/a11y/no-interactive-element-to-noninteractive-role.ts
const buildMessage$14 = (tag, role) => `Screen reader users can't operate this interactive \`<${tag}>\` because role \`${role}\` says it isn't, so remove the role or use a different element.`;
const PRESENTATION_ROLES = ["presentation", "none"];
const DEFAULT_ALLOWED_ROLES$1 = {
	tr: ["none", "presentation"],
	canvas: ["img"]
};
const resolveSettings$17 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	if (typeof reactDoctor !== "object" || reactDoctor === null) return DEFAULT_ALLOWED_ROLES$1;
	const reactDoctorBlock = reactDoctor;
	if (!("noInteractiveElementToNoninteractiveRole" in reactDoctorBlock)) return DEFAULT_ALLOWED_ROLES$1;
	const ruleSettings = reactDoctorBlock.noInteractiveElementToNoninteractiveRole ?? {};
	const result = {};
	for (const key of Object.keys(ruleSettings)) {
		const value = ruleSettings[key];
		if (Array.isArray(value)) result[key] = value;
	}
	return result;
};
const noInteractiveElementToNoninteractiveRole = defineRule({
	id: "no-interactive-element-to-noninteractive-role",
	title: "Interactive element given noninteractive role",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Do not give an interactive element a role that says it is not interactive.",
	category: "Accessibility",
	create: (context) => {
		const allowedRoles = resolveSettings$17(context.settings);
		return { JSXOpeningElement(node) {
			const elementType = getElementType(node, context.settings);
			if (!HTML_TAGS.has(elementType)) return;
			if (!(elementType === "input" || isInteractiveElement(elementType, node))) return;
			const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
			if (!roleAttribute) return;
			const value = roleAttribute.value;
			if (!value || !isNodeOfType(value, "Literal")) return;
			const literal = value;
			if (typeof literal.value !== "string") return;
			const firstRole = literal.value.trim().split(/\s+/)[0];
			if (!firstRole) return;
			const allowed = allowedRoles[elementType];
			if (allowed && allowed.includes(firstRole)) return;
			if (!isNonInteractiveRole(firstRole) && !PRESENTATION_ROLES.includes(firstRole)) return;
			context.report({
				node: roleAttribute,
				message: buildMessage$14(elementType, firstRole)
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/no-is-mounted.ts
const noIsMounted = defineRule({
	id: "no-is-mounted",
	title: "Use of isMounted",
	severity: "warn",
	recommendation: "`isMounted` doesn't work in modern React. Track mount state with a ref, or cancel the async work instead.",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		if (!isNodeOfType(node.callee.object, "ThisExpression")) return;
		if (!isNodeOfType(node.callee.property, "Identifier") || node.callee.property.name !== "isMounted") return;
		let ancestor = node.parent;
		while (ancestor) {
			if (ancestor.type === "MethodDefinition" || ancestor.type === "Property") {
				context.report({
					node,
					message: "`isMounted` is unreliable in modern React & leads to bugs."
				});
				return;
			}
			ancestor = ancestor.parent ?? null;
		}
	} })
});
//#endregion
//#region src/plugin/rules/correctness/no-jsx-element-type.ts
const MESSAGE$18 = "`JSX.Element` is too narrow: it excludes `null`, strings, numbers, and fragments that components commonly return. Use `React.ReactNode` instead.";
const isJsxElementTypeReference = (node) => {
	if (!isNodeOfType(node, "TSTypeReference")) return false;
	const typeName = node.typeName;
	if (!isNodeOfType(typeName, "TSQualifiedName")) return false;
	return isNodeOfType(typeName.left, "Identifier") && typeName.left.name === "JSX" && isNodeOfType(typeName.right, "Identifier") && typeName.right.name === "Element";
};
const extractReturnTypeAnnotation = (returnType) => {
	if (!returnType) return null;
	if (!isNodeOfType(returnType, "TSTypeAnnotation")) return null;
	return returnType.typeAnnotation ?? null;
};
const checkReturnType = (context, returnType) => {
	const typeAnnotation = extractReturnTypeAnnotation(returnType);
	if (!typeAnnotation) return;
	if (isJsxElementTypeReference(typeAnnotation)) context.report({
		node: typeAnnotation,
		message: MESSAGE$18
	});
};
const noJsxElementType = defineRule({
	id: "no-jsx-element-type",
	title: "No JSX.Element",
	severity: "error",
	recommendation: "Replace `JSX.Element` with `React.ReactNode`. `JSX.Element` is too narrow: it excludes `null`, strings, numbers, and fragments that components commonly return.",
	create: (context) => ({
		FunctionDeclaration(node) {
			checkReturnType(context, node.returnType);
		},
		ArrowFunctionExpression(node) {
			checkReturnType(context, node.returnType);
		},
		FunctionExpression(node) {
			checkReturnType(context, node.returnType);
		},
		TSDeclareFunction(node) {
			checkReturnType(context, node.returnType);
		},
		TSMethodSignature(node) {
			checkReturnType(context, node.returnType);
		}
	})
});
//#endregion
//#region src/plugin/rules/design/no-justified-text.ts
const noJustifiedText = defineRule({
	id: "no-justified-text",
	title: "Justified text without hyphens",
	tags: ["test-noise"],
	severity: "warn",
	defaultEnabled: false,
	category: "Accessibility",
	recommendation: "Use `text-align: left` for body text. If you must justify, add `hyphens: auto` and `overflow-wrap: break-word`.",
	create: (context) => ({ JSXAttribute(node) {
		const expression = getInlineStyleExpression(node);
		if (!expression) return;
		let isJustified = false;
		let hasHyphens = false;
		for (const property of expression.properties ?? []) {
			const key = getStylePropertyKey(property);
			const value = getStylePropertyStringValue(property);
			if (!key || !value) continue;
			if (key === "textAlign" && value === "justify") isJustified = true;
			if ((key === "hyphens" || key === "WebkitHyphens") && value === "auto") hasHyphens = true;
		}
		if (isJustified && !hasHyphens) context.report({
			node,
			message: "Your users read big uneven gaps between words because justified text has no hyphens, so use text-align: left, or add hyphens: auto."
		});
	} })
});
//#endregion
//#region src/plugin/rules/performance/no-large-animated-blur.ts
const noLargeAnimatedBlur = defineRule({
	id: "no-large-animated-blur",
	title: "Large animated blur",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Keep the blur under 10px, or blur a smaller element. Big blurs use a lot more GPU memory as the element grows",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		if (node.name.name !== "style" && !MOTION_ANIMATE_PROPS.has(node.name.name)) return;
		if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const expression = node.value.expression;
		if (!isNodeOfType(expression, "ObjectExpression")) return;
		for (const property of expression.properties ?? []) {
			if (!isNodeOfType(property, "Property")) continue;
			const key = isNodeOfType(property.key, "Identifier") ? property.key.name : null;
			if (key !== "filter" && key !== "backdropFilter" && key !== "WebkitBackdropFilter") continue;
			if (!isNodeOfType(property.value, "Literal") || typeof property.value.value !== "string") continue;
			const match = BLUR_VALUE_PATTERN.exec(property.value.value);
			if (!match) continue;
			const blurRadius = Number.parseFloat(match[1]);
			if (blurRadius > 10) context.report({
				node: property,
				message: `This can run out of GPU memory on phones because blur(${blurRadius}px) gets heavier as the blur & element grow, so use a smaller blur or a smaller element`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/performance/no-layout-property-animation.ts
const isMotionElement = (attributeNode) => {
	const openingElement = attributeNode.parent;
	if (!openingElement || !isNodeOfType(openingElement, "JSXOpeningElement")) return false;
	const elementName = openingElement.name;
	if (isNodeOfType(elementName, "JSXMemberExpression") && isNodeOfType(elementName.object, "JSXIdentifier") && (elementName.object.name === "motion" || elementName.object.name === "m")) return true;
	if (isNodeOfType(elementName, "JSXIdentifier") && elementName.name.startsWith("Motion")) return true;
	return false;
};
const noLayoutPropertyAnimation = defineRule({
	id: "no-layout-property-animation",
	title: "Animating a layout property",
	tags: ["test-noise"],
	severity: "error",
	recommendation: "Use `transform: translateX()` or `scale()` instead. They animate smoothly without making the browser redo layout or repaint",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || !MOTION_ANIMATE_PROPS.has(node.name.name)) return;
		if (!node.value || !isNodeOfType(node.value, "JSXExpressionContainer")) return;
		if (isMotionElement(node)) return;
		const expression = node.value.expression;
		if (!isNodeOfType(expression, "ObjectExpression")) return;
		for (const property of expression.properties ?? []) {
			if (!isNodeOfType(property, "Property")) continue;
			let propertyName = null;
			if (isNodeOfType(property.key, "Identifier")) propertyName = property.key.name;
			else if (isNodeOfType(property.key, "Literal") && typeof property.key.value === "string") propertyName = property.key.value;
			if (propertyName && LAYOUT_PROPERTIES.has(propertyName)) context.report({
				node: property,
				message: `This stutters because animating "${propertyName}" makes the browser redo page layout every frame, so animate transform or scale instead, or use the layout prop`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/design/no-layout-transition-inline.ts
const noLayoutTransitionInline = defineRule({
	id: "no-layout-transition-inline",
	title: "Animating layout properties",
	tags: ["test-noise"],
	severity: "warn",
	category: "Performance",
	recommendation: "Animate `transform` and `opacity` instead, since they're cheap for the browser. For height, animate `grid-template-rows` from `0fr` to `1fr`.",
	create: (context) => ({ JSXAttribute(node) {
		const expression = getInlineStyleExpression(node);
		if (!expression) return;
		for (const property of expression.properties ?? []) {
			const key = getStylePropertyKey(property);
			if (key !== "transition" && key !== "transitionProperty") continue;
			const value = getStylePropertyStringValue(property);
			if (!value) continue;
			const lower = value.toLowerCase();
			if (/\ball\b/.test(lower)) continue;
			const layoutMatch = lower.match(/\b(?:(?:max|min)-)?(?:width|height)\b|\bpadding(?:-(?:top|right|bottom|left))?\b|\bmargin(?:-(?:top|right|bottom|left))?\b/);
			if (layoutMatch) context.report({
				node: property,
				message: `Your users see janky, stuttering animation because "${layoutMatch[0]}" relayouts the page every frame, so animate transform & opacity instead.`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/architecture/no-legacy-class-lifecycles.ts
const LEGACY_LIFECYCLE_REPLACEMENTS = new Map([
	["componentWillMount", "Put side effects in `componentDidMount` & initial state in the `constructor`"],
	["componentWillReceiveProps", "Put side effects in `componentDidUpdate` & derived state in the static `getDerivedStateFromProps`"],
	["componentWillUpdate", "Read the DOM in `getSnapshotBeforeUpdate` & do other work in `componentDidUpdate`"]
]);
const stripUnsafePrefix = (name) => {
	if (name.startsWith("UNSAFE_")) return {
		baseName: name.slice(7),
		hasUnsafePrefix: true
	};
	return {
		baseName: name,
		hasUnsafePrefix: false
	};
};
const buildLegacyLifecycleMessage = (originalName) => {
	const { baseName, hasUnsafePrefix } = stripUnsafePrefix(originalName);
	const replacement = LEGACY_LIFECYCLE_REPLACEMENTS.get(baseName);
	if (!replacement) return null;
	return `${hasUnsafePrefix ? `\`${originalName}\` breaks under concurrent rendering & is gone in React 19, & the UNSAFE_ prefix only hides the warning.` : `\`${originalName}\` breaks under concurrent rendering, warns in React 18 & is gone in React 19.`} ${replacement}.`;
};
const noLegacyClassLifecycles = defineRule({
	id: "no-legacy-class-lifecycles",
	title: "Legacy class lifecycle methods",
	severity: "error",
	category: "Correctness",
	tags: ["migration-hint"],
	recommendation: "Move `componentWillMount` work to `componentDidMount`, `componentWillReceiveProps` to `componentDidUpdate` or the static `getDerivedStateFromProps`, and `componentWillUpdate` to `getSnapshotBeforeUpdate` plus `componentDidUpdate`. The `UNSAFE_` prefix only hides the warning. React 19 removes both.",
	create: (context) => {
		const checkMember = (memberNode) => {
			if (!memberNode) return;
			if (!isNodeOfType(memberNode, "MethodDefinition") && !isNodeOfType(memberNode, "PropertyDefinition")) return;
			if (!isNodeOfType(memberNode.key, "Identifier")) return;
			const message = buildLegacyLifecycleMessage(memberNode.key.name);
			if (message) context.report({
				node: memberNode.key,
				message
			});
		};
		return { ClassBody(node) {
			for (const member of node.body ?? []) checkMember(member);
		} };
	}
});
//#endregion
//#region src/plugin/rules/architecture/no-legacy-context-api.ts
const LEGACY_CONTEXT_NAMES = new Set([
	"childContextTypes",
	"contextTypes",
	"getChildContext"
]);
const buildLegacyContextMessage = (memberName) => {
	if (memberName === "childContextTypes" || memberName === "getChildContext") return `${memberName} uses the old context API that React 19 removes, so your provider stops passing data. Switch to \`createContext\` with \`<MyContext.Provider value={...}>\` & read it with \`useContext()\`, moving every consumer together.`;
	return "contextTypes uses the old context API that React 19 removes, so your component stops receiving context. Use `static contextType = MyContext` or `useContext()` in a function component, & update the provider too.";
};
const isInsideClassBody = (node) => {
	let current = node.parent;
	while (current) {
		if (isNodeOfType(current, "ClassBody")) return true;
		if (isFunctionLike$2(current)) return false;
		current = current.parent;
	}
	return false;
};
const noLegacyContextApi = defineRule({
	id: "no-legacy-context-api",
	title: "Legacy context API",
	severity: "error",
	category: "Correctness",
	tags: ["migration-hint"],
	recommendation: "Swap `childContextTypes` + `getChildContext` for `const MyContext = createContext(...)` and `<MyContext.Provider value={...}>`. Swap `contextTypes` for `static contextType = MyContext` or `useContext()` in a function component. Move the provider and every consumer together, or some consumers read the wrong context.",
	create: (context) => {
		const checkMember = (memberNode) => {
			if (!memberNode) return;
			if (!isNodeOfType(memberNode, "MethodDefinition") && !isNodeOfType(memberNode, "PropertyDefinition")) return;
			if (!isNodeOfType(memberNode.key, "Identifier")) return;
			if (!LEGACY_CONTEXT_NAMES.has(memberNode.key.name)) return;
			context.report({
				node: memberNode.key,
				message: buildLegacyContextMessage(memberNode.key.name)
			});
		};
		return {
			ClassBody(node) {
				for (const member of node.body ?? []) checkMember(member);
			},
			AssignmentExpression(node) {
				if (node.operator !== "=") return;
				const left = node.left;
				if (!isNodeOfType(left, "MemberExpression")) return;
				if (left.computed) return;
				if (!isNodeOfType(left.property, "Identifier")) return;
				if (!LEGACY_CONTEXT_NAMES.has(left.property.name)) return;
				if (!isNodeOfType(left.object, "Identifier")) return;
				if (!isUppercaseName(left.object.name)) return;
				if (isInsideClassBody(node)) return;
				context.report({
					node: left,
					message: buildLegacyContextMessage(left.property.name)
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/design/no-long-transition-duration.ts
const noLongTransitionDuration = defineRule({
	id: "no-long-transition-duration",
	title: "Transition duration too long",
	tags: ["test-noise"],
	severity: "warn",
	category: "Performance",
	recommendation: "Keep UI transitions under 1s. Use about 100 to 150ms for instant feedback, 200 to 300ms for state changes, and 300 to 500ms for layout. Save longer ones for big page-load animations.",
	create: (context) => ({ JSXAttribute(node) {
		const expression = getInlineStyleExpression(node);
		if (!expression) return;
		for (const property of expression.properties ?? []) {
			const key = getStylePropertyKey(property);
			if (!key) continue;
			const value = getStylePropertyStringValue(property);
			if (!value) continue;
			let durationMs = null;
			if (key === "transitionDuration" || key === "animationDuration") {
				let longestDurationPropertyMs = 0;
				for (const segment of value.split(",")) {
					const trimmedSegment = segment.trim();
					const msMatch = trimmedSegment.match(/^([\d.]+)ms$/);
					const secondsMatch = trimmedSegment.match(/^([\d.]+)s$/);
					if (msMatch) longestDurationPropertyMs = Math.max(longestDurationPropertyMs, parseFloat(msMatch[1]));
					else if (secondsMatch) longestDurationPropertyMs = Math.max(longestDurationPropertyMs, parseFloat(secondsMatch[1]) * 1e3);
				}
				if (longestDurationPropertyMs > 0) durationMs = longestDurationPropertyMs;
			}
			if (key === "transition" || key === "animation") {
				let longestDurationMs = 0;
				const segments = value.split(",");
				for (const segment of segments) {
					const firstTimeMatch = segment.match(/(?<![a-zA-Z\d])([\d.]+)(m?s)(?![a-zA-Z\d-])/);
					if (!firstTimeMatch) continue;
					const segmentDurationMs = firstTimeMatch[2] === "ms" ? parseFloat(firstTimeMatch[1]) : parseFloat(firstTimeMatch[1]) * 1e3;
					longestDurationMs = Math.max(longestDurationMs, segmentDurationMs);
				}
				if (longestDurationMs > 0) durationMs = longestDurationMs;
			}
			if (durationMs !== null && durationMs > 1e3) context.report({
				node: property,
				message: `Your users wait through a sluggish ${durationMs}ms transition, so keep UI transitions under ${LONG_TRANSITION_DURATION_THRESHOLD_MS}ms & save longer ones for big page-load animations.`
			});
		}
	} })
});
//#endregion
//#region src/plugin/utils/is-boolean-prefixed-prop-name.ts
const BOOLEAN_PROP_PREFIX_PATTERN = /^(?:is|has|should|can|show|hide|enable|disable|with)[A-Z]/;
const isBooleanPrefixedPropName = (propName) => BOOLEAN_PROP_PREFIX_PATTERN.test(propName);
//#endregion
//#region src/plugin/utils/is-component-declaration.ts
const isComponentDeclaration = (node) => isNodeOfType(node, "FunctionDeclaration") && node.id !== null && Boolean(node.id?.name) && isUppercaseName(node.id.name);
//#endregion
//#region src/plugin/rules/architecture/no-many-boolean-props.ts
const collectBooleanLikePropsFromBody = (componentBody, propsParamName) => {
	const found = /* @__PURE__ */ new Set();
	if (!componentBody) return found;
	walkAst(componentBody, (child) => {
		if (!isNodeOfType(child, "MemberExpression")) return;
		if (child.computed) return;
		if (!isNodeOfType(child.object, "Identifier")) return;
		if (child.object.name !== propsParamName) return;
		if (!isNodeOfType(child.property, "Identifier")) return;
		if (!isBooleanPrefixedPropName(child.property.name)) return;
		found.add(child.property.name);
	});
	return found;
};
const noManyBooleanProps = defineRule({
	id: "no-many-boolean-props",
	title: "Too many boolean props",
	severity: "warn",
	tags: ["test-noise", "react-jsx-only"],
	recommendation: "Split into smaller components or named variants, like `<Button.Primary />` and `<DialogConfirm />`, instead of stacking `isPrimary` and `isConfirm` flags.",
	create: (context) => {
		const reportIfMany = (booleanLikePropNames, componentName, reportNode) => {
			if (booleanLikePropNames.length >= 4) context.report({
				node: reportNode,
				message: `Component "${componentName}" takes ${booleanLikePropNames.length} on/off props (${booleanLikePropNames.slice(0, 3).join(", ")}…), which is hard to combine & test. Split it into smaller components or named variants.`
			});
		};
		const checkComponent = (param, body, componentName, reportNode) => {
			if (!param) return;
			if (isNodeOfType(param, "ObjectPattern")) {
				const booleanLikePropNames = [];
				for (const property of param.properties ?? []) {
					if (!isNodeOfType(property, "Property")) continue;
					const keyName = isNodeOfType(property.key, "Identifier") ? property.key.name : null;
					if (!keyName) continue;
					if (isBooleanPrefixedPropName(keyName)) booleanLikePropNames.push(keyName);
				}
				reportIfMany(booleanLikePropNames, componentName, reportNode);
				return;
			}
			if (isNodeOfType(param, "Identifier")) reportIfMany([...collectBooleanLikePropsFromBody(body, param.name)], componentName, reportNode);
		};
		return {
			FunctionDeclaration(node) {
				if (!isComponentDeclaration(node) || !node.id) return;
				checkComponent(node.params?.[0], node.body, node.id.name, node.id);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.id, "Identifier")) return;
				if (!isInlineFunctionExpression(node.init)) return;
				checkComponent(node.init.params?.[0], node.init.body, node.id.name, node.id);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-mirror-prop-effect.ts
const getPropRootName = (expression, propNames) => {
	const rootName = getRootIdentifierName(expression, { followCallChains: true });
	return rootName !== null && propNames.has(rootName) ? rootName : null;
};
const noMirrorPropEffect = defineRule({
	id: "no-mirror-prop-effect",
	title: "Prop mirrored into state via effect",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Delete both the `useState` and the `useEffect` and read the prop directly while rendering. Copying a prop into state shows the old value on the first render before the effect catches up.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const propNames = propStackTracker.getCurrentPropNames();
			if (propNames.size === 0) return;
			const mirrorBindings = [];
			for (const statement of componentBody.body ?? []) {
				if (!isNodeOfType(statement, "VariableDeclaration")) continue;
				for (const declarator of statement.declarations ?? []) {
					if (!isNodeOfType(declarator.id, "ArrayPattern")) continue;
					const elements = declarator.id.elements ?? [];
					if (elements.length < 2) continue;
					const valueElement = elements[0];
					const setterElement = elements[1];
					if (!isNodeOfType(valueElement, "Identifier") || !isNodeOfType(setterElement, "Identifier") || !isSetterIdentifier(setterElement.name)) continue;
					if (!isNodeOfType(declarator.init, "CallExpression")) continue;
					if (!isHookCall$1(declarator.init, "useState")) continue;
					const initializer = declarator.init.arguments?.[0];
					if (!initializer) continue;
					const propRootName = getPropRootName(initializer, propNames);
					if (!propRootName) continue;
					mirrorBindings.push({
						valueName: valueElement.name,
						setterName: setterElement.name,
						initializer,
						propRootName
					});
				}
			}
			if (mirrorBindings.length === 0) return;
			for (const statement of componentBody.body ?? []) {
				if (!isNodeOfType(statement, "ExpressionStatement")) continue;
				const effectCall = statement.expression;
				if (!isNodeOfType(effectCall, "CallExpression")) continue;
				if (!isHookCall$1(effectCall, EFFECT_HOOK_NAMES$1)) continue;
				if ((effectCall.arguments?.length ?? 0) < 2) continue;
				const depsNode = effectCall.arguments[1];
				if (!isNodeOfType(depsNode, "ArrayExpression")) continue;
				const depIdentifierNames = /* @__PURE__ */ new Set();
				for (const element of depsNode.elements ?? []) if (isNodeOfType(element, "Identifier")) depIdentifierNames.add(element.name);
				if (depIdentifierNames.size === 0) continue;
				const callback = getEffectCallback(effectCall);
				if (!callback) continue;
				const bodyStatements = getCallbackStatements(callback);
				if (bodyStatements.length !== 1) continue;
				const onlyStatement = bodyStatements[0];
				const expression = isNodeOfType(onlyStatement, "ExpressionStatement") ? onlyStatement.expression : onlyStatement;
				if (!isNodeOfType(expression, "CallExpression")) continue;
				if (!isNodeOfType(expression.callee, "Identifier")) continue;
				if (!isSetterIdentifier(expression.callee.name)) continue;
				if (!expression.arguments?.length) continue;
				const setterArgument = expression.arguments[0];
				const calleeName = expression.callee.name;
				const matchedBinding = mirrorBindings.find((binding) => binding.setterName === calleeName && depIdentifierNames.has(binding.propRootName) && areExpressionsStructurallyEqual(binding.initializer, setterArgument));
				if (!matchedBinding) continue;
				context.report({
					node: effectCall,
					message: `Your screen shows the old value first because useState "${matchedBinding.valueName}" copies prop "${matchedBinding.propRootName}" through this effect.`
				});
			}
		};
		const propStackTracker = createComponentPropStackTracker({ onComponentEnter: checkComponent });
		return propStackTracker.visitors;
	}
});
//#endregion
//#region src/plugin/rules/bundle-size/no-moment.ts
const noMoment = defineRule({
	id: "no-moment",
	title: "Using moment.js",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Switch to `import { format } from 'date-fns'` or `import dayjs from 'dayjs'` (2kb).",
	create: (context) => ({ ImportDeclaration(node) {
		if (node.source?.value === "moment") context.report({
			node,
			message: "moment.js ships 300 kb+ to your users & slows page load. Use \"date-fns\" or \"dayjs\" instead."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-multi-comp.ts
const MESSAGE$17 = "This file is harder to navigate with more than one component.";
const resolveSettings$16 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { ignoreStateless: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noMultiComp ?? {} : {}).ignoreStateless ?? false };
};
const isHocCall = (call, scopes) => {
	if (!isNodeOfType(call, "CallExpression")) return false;
	const calleeName = flattenCalleeName(call.callee);
	if (calleeName && REACT_HOC_NAMES.has(calleeName)) return true;
	if (!isNodeOfType(call.callee, "Identifier")) return false;
	const symbol = scopes.symbolFor(call.callee);
	if (!symbol) return false;
	return symbolMapsToHoc(symbol);
};
const symbolMapsToHoc = (symbol) => {
	if (REACT_HOC_NAMES.has(symbol.name)) {
		if (symbol.kind === "import") return true;
	}
	const init = symbol.initializer;
	if (!init) return false;
	if (isNodeOfType(init, "MemberExpression")) {
		const flat = flattenCalleeName(init);
		if (flat && REACT_HOC_NAMES.has(flat)) return true;
	}
	if (isNodeOfType(init, "Identifier") && REACT_HOC_NAMES.has(init.name)) return true;
	if (REACT_HOC_NAMES.has(symbol.name) && isNodeOfType(init, "Identifier") && init.name === "React") return true;
	return false;
};
const isTrivialPassthroughChild = (child) => {
	if (child.type === "JSXText") return true;
	if (child.type === "JSXExpressionContainer") return true;
	if (child.type === "JSXFragment") return true;
	if (isNodeOfType(child, "JSXElement")) {
		const open = child.openingElement;
		if (isNodeOfType(open.name, "JSXIdentifier")) {
			const first = open.name.name.charCodeAt(0);
			return first < 65 || first > 90;
		}
		return false;
	}
	return false;
};
const MAX_PASSTHROUGH_ATTRS = 6;
const isSimpleJsxPassthrough = (expression) => {
	if (!isNodeOfType(expression, "JSXElement")) return false;
	const opening = expression.openingElement;
	if (!isNodeOfType(opening.name, "JSXIdentifier")) return false;
	if (!isReactComponentName(opening.name.name)) return false;
	const attrs = opening.attributes;
	if (attrs.length > MAX_PASSTHROUGH_ATTRS) return false;
	if (!attrs.some((attr) => isNodeOfType(attr, "JSXSpreadAttribute"))) return false;
	for (const child of expression.children ?? []) if (!isTrivialPassthroughChild(child)) return false;
	return true;
};
const isSingleReturnPassthrough = (statements) => {
	if (statements.length !== 1) return false;
	const only = statements[0];
	if (!isNodeOfType(only, "ReturnStatement")) return false;
	if (!only.argument) return false;
	return isSimpleJsxPassthrough(only.argument);
};
const isPassthroughFunction = (fn) => {
	if (!isNodeOfType(fn, "FunctionExpression") && !isNodeOfType(fn, "FunctionDeclaration")) return false;
	const body = fn.body;
	if (!body || !isNodeOfType(body, "BlockStatement")) return false;
	return isSingleReturnPassthrough(body.body);
};
const isPassthroughArrow = (arrow) => {
	if (!isNodeOfType(arrow, "ArrowFunctionExpression")) return false;
	const body = arrow.body;
	if (isNodeOfType(body, "BlockStatement")) return isSingleReturnPassthrough(body.body);
	return isSimpleJsxPassthrough(body);
};
const isHocComponent = (call, scopes) => {
	if (!isHocCall(call, scopes)) return false;
	const arg = call.arguments[0];
	if (!arg) return false;
	if (isNodeOfType(arg, "FunctionExpression")) return !isPassthroughFunction(arg) && containsJsx(arg);
	if (isNodeOfType(arg, "ArrowFunctionExpression")) return !isPassthroughArrow(arg) && containsJsx(arg);
	return false;
};
const containsJsx = (root) => {
	let found = false;
	const visit = (node) => {
		if (found) return;
		if (node.type === "JSXElement" || node.type === "JSXFragment") {
			found = true;
			return;
		}
		if (node !== root) {
			if (node.type === "FunctionDeclaration" || node.type === "FunctionExpression" || node.type === "ArrowFunctionExpression" || node.type === "ClassDeclaration" || node.type === "ClassExpression") return;
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
			if (found) return;
		}
	};
	visit(root);
	return found;
};
const expressionContainsJsx = (expression) => {
	if (isNodeOfType(expression, "FunctionExpression") || isNodeOfType(expression, "FunctionDeclaration") || isNodeOfType(expression, "ArrowFunctionExpression")) {
		const body = expression.body;
		if (!body) return false;
		return containsJsx(body);
	}
	return false;
};
const isFunctionReturningNull = (expression) => {
	if (!isNodeOfType(expression, "ArrowFunctionExpression") && !isNodeOfType(expression, "FunctionExpression")) return false;
	const body = expression.body;
	if (isNodeOfType(body, "Literal")) return body.value === null;
	if (!isNodeOfType(body, "BlockStatement")) return false;
	for (const statement of body.body) if (isNodeOfType(statement, "ReturnStatement") && statement.argument && isNodeOfType(statement.argument, "Literal") && statement.argument.value === null) return true;
	return false;
};
const collectReExportedNames = (program) => {
	const names = /* @__PURE__ */ new Set();
	if (!isNodeOfType(program, "Program")) return names;
	for (const statement of program.body) {
		if (isNodeOfType(statement, "ExportDefaultDeclaration")) {
			if (isNodeOfType(statement.declaration, "Identifier")) names.add(statement.declaration.name);
			continue;
		}
		if (!isNodeOfType(statement, "ExportNamedDeclaration")) continue;
		if (!statement.declaration) {
			for (const specifier of statement.specifiers ?? []) {
				if (!isNodeOfType(specifier, "ExportSpecifier")) continue;
				const local = specifier.local;
				if (isNodeOfType(local, "Identifier")) names.add(local.name);
			}
			continue;
		}
		if (!isNodeOfType(statement.declaration, "VariableDeclaration")) continue;
		for (const declarator of statement.declaration.declarations ?? []) {
			if (!isNodeOfType(declarator, "VariableDeclarator")) continue;
			if (!isNodeOfType(declarator.init, "ObjectExpression")) continue;
			for (const property of declarator.init.properties ?? []) {
				if (!isNodeOfType(property, "Property")) continue;
				if (property.computed) continue;
				const value = property.value;
				if (isNodeOfType(value, "Identifier")) names.add(value.name);
			}
		}
	}
	return names;
};
const isExportedDeclaration = (node, reExportedNames) => {
	if (isNodeOfType(node, "Identifier") && reExportedNames.has(node.name)) return true;
	let current = node.parent;
	let didCrossOneBindingLayer = false;
	while (current) {
		if (isNodeOfType(current, "ExportNamedDeclaration") || isNodeOfType(current, "ExportDefaultDeclaration")) return true;
		if (isNodeOfType(current, "Program")) return false;
		if (isNodeOfType(current, "FunctionDeclaration") || isNodeOfType(current, "FunctionExpression") || isNodeOfType(current, "ArrowFunctionExpression") || isNodeOfType(current, "ClassDeclaration") || isNodeOfType(current, "ClassExpression")) {
			if (didCrossOneBindingLayer) return false;
			didCrossOneBindingLayer = true;
		}
		current = current.parent ?? null;
	}
	return false;
};
const detectVariableComponent = (declarator, scopes) => {
	if (!isNodeOfType(declarator, "VariableDeclarator")) return null;
	if (!isNodeOfType(declarator.id, "Identifier")) return null;
	const name = declarator.id.name;
	if (!isReactComponentName(name)) return null;
	let init = declarator.init;
	if (!init) return null;
	init = stripParenExpression(init);
	if (isPassthroughArrow(init) || isPassthroughFunction(init)) return null;
	if (expressionContainsJsx(init) || isFunctionReturningNull(init)) return {
		name,
		reportNode: declarator.id,
		isStateless: true
	};
	if (isNodeOfType(init, "SequenceExpression")) {
		const expressions = init.expressions;
		const last = expressions[expressions.length - 1];
		if (last && (expressionContainsJsx(last) || isFunctionReturningNull(last))) return {
			name,
			reportNode: declarator.id,
			isStateless: true
		};
	}
	if (isHocComponent(init, scopes)) return {
		name,
		reportNode: declarator.id,
		isStateless: true
	};
	return null;
};
const recordComponent = (context, name, reportNode, isStateless) => {
	if (context.componentDepth === 0) context.components.push({
		name,
		reportNode,
		isStateless
	});
};
const walkChildren = (node, context) => {
	const record = node;
	for (const key of Object.keys(record)) {
		if (key === "parent") continue;
		const child = record[key];
		if (Array.isArray(child)) {
			for (const item of child) if (isAstNode(item)) walkComponentSearch(item, context);
		} else if (isAstNode(child)) walkComponentSearch(child, context);
	}
};
const walkComponentSearch = (node, context) => {
	if (isNodeOfType(node, "ClassDeclaration") || isNodeOfType(node, "ClassExpression")) {
		if (isEs6Component(node)) {
			recordComponent(context, node.id ? node.id.name : "UnnamedComponent", node.id ?? node, false);
			context.componentDepth += 1;
			walkChildren(node, context);
			context.componentDepth -= 1;
			return;
		}
	}
	if (isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression")) {
		if (node.id && isReactComponentName(node.id.name) && containsJsx(node) && !isPassthroughFunction(node)) {
			recordComponent(context, node.id.name, node.id, true);
			context.componentDepth += 1;
			walkChildren(node, context);
			context.componentDepth -= 1;
			return;
		}
	}
	if (isNodeOfType(node, "VariableDeclarator")) {
		const detected = detectVariableComponent(node, context.scopes);
		if (detected) {
			recordComponent(context, detected.name, detected.reportNode, detected.isStateless);
			const previousName = context.currentVarName;
			context.currentVarName = detected.name;
			context.componentDepth += 1;
			walkChildren(node, context);
			context.componentDepth -= 1;
			context.currentVarName = previousName;
			return;
		}
		const previousName = context.currentVarName;
		if (isNodeOfType(node.id, "Identifier")) context.currentVarName = node.id.name;
		walkChildren(node, context);
		context.currentVarName = previousName;
		return;
	}
	if (isNodeOfType(node, "CallExpression") && isEs5Component(node)) {
		if (context.componentDepth === 0) {
			recordComponent(context, context.currentVarName ?? "UnnamedComponent", node, false);
			context.componentDepth += 1;
			walkChildren(node, context);
			context.componentDepth -= 1;
			return;
		}
	}
	if (isNodeOfType(node, "ExportDefaultDeclaration")) {
		const declaration = node.declaration;
		if (isNodeOfType(declaration, "CallExpression") && isHocComponent(declaration, context.scopes)) {
			recordComponent(context, "UnnamedComponent", node, true);
			context.componentDepth += 1;
			walkChildren(node, context);
			context.componentDepth -= 1;
			return;
		}
	}
	if (isNodeOfType(node, "Property")) {
		if (isNodeOfType(node.key, "Identifier") && isReactComponentName(node.key.name) && (isNodeOfType(node.value, "FunctionExpression") || isNodeOfType(node.value, "ArrowFunctionExpression")) && containsJsx(node.value)) {
			recordComponent(context, node.key.name, node.key, true);
			context.componentDepth += 1;
			walkChildren(node, context);
			context.componentDepth -= 1;
			return;
		}
	}
	if (isNodeOfType(node, "AssignmentExpression")) {
		if (isNodeOfType(node.left, "MemberExpression") && isNodeOfType(node.left.property, "Identifier") && !node.left.computed && isReactComponentName(node.left.property.name)) {
			const right = node.right;
			if (containsJsx(right) || (isNodeOfType(right, "FunctionExpression") || isNodeOfType(right, "ArrowFunctionExpression")) && containsJsx(right)) {
				recordComponent(context, node.left.property.name, node.left.property, true);
				context.componentDepth += 1;
				walkChildren(node, context);
				context.componentDepth -= 1;
				return;
			}
		}
	}
	walkChildren(node, context);
};
const noMultiComp = defineRule({
	id: "no-multi-comp",
	title: "Multiple components in one file",
	severity: "warn",
	recommendation: "Move secondary components into their own files.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$16(context.settings);
		const isTestlikeFile = isTestlikeFilename(context.filename);
		return { Program(node) {
			if (isTestlikeFile) return;
			const visitContext = {
				components: [],
				componentDepth: 0,
				currentVarName: null,
				scopes: context.scopes
			};
			for (const statement of node.body) walkComponentSearch(statement, visitContext);
			const flagged = settings.ignoreStateless ? visitContext.components.filter((component) => !component.isStateless) : visitContext.components;
			if (flagged.length <= 2) return;
			const reExportedNames = collectReExportedNames(node);
			const exportedCount = flagged.filter((component) => isExportedDeclaration(component.reportNode, reExportedNames)).length;
			if (flagged.length >= 4 && exportedCount >= Math.floor(flagged.length * .7) || flagged.length >= 8 && exportedCount >= Math.floor(flagged.length * .5)) return;
			const isSmallFeatureModule = exportedCount > 0 && exportedCount <= 2 && exportedCount < flagged.length;
			const isLargeFeatureModule = exportedCount > 0 && exportedCount <= 4 && flagged.length >= 8 && exportedCount * 2 < flagged.length;
			const isVeryLargeFeatureModule = exportedCount >= 5 && flagged.length >= 12 && (flagged.length - exportedCount) * 4 >= flagged.length;
			if (isSmallFeatureModule || isLargeFeatureModule || isVeryLargeFeatureModule) return;
			for (const component of flagged.slice(1)) context.report({
				node: component.reportNode,
				message: MESSAGE$17
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-mutable-in-deps.ts
const collectUseRefBindingNames = (componentBody) => {
	const useRefBindings = /* @__PURE__ */ new Set();
	if (!isNodeOfType(componentBody, "BlockStatement")) return useRefBindings;
	for (const statement of componentBody.body ?? []) {
		if (!isNodeOfType(statement, "VariableDeclaration")) continue;
		for (const declarator of statement.declarations ?? []) {
			if (!isNodeOfType(declarator.id, "Identifier")) continue;
			if (!isNodeOfType(declarator.init, "CallExpression")) continue;
			if (!isHookCall$1(declarator.init, "useRef")) continue;
			useRefBindings.add(declarator.id.name);
		}
	}
	return useRefBindings;
};
const findMutableDepIssue = (depElement, useRefBindingNames) => {
	if (!isNodeOfType(depElement, "MemberExpression")) return null;
	if (isNodeOfType(depElement.property, "Identifier") && depElement.property.name === "current" && !depElement.computed && isNodeOfType(depElement.object, "Identifier") && useRefBindingNames.has(depElement.object.name)) return {
		kind: "ref-current",
		rootName: depElement.object.name
	};
	const rootName = getRootIdentifierName(depElement);
	if (rootName !== null && MUTABLE_GLOBAL_ROOTS.has(rootName)) return {
		kind: "global",
		rootName
	};
	return null;
};
const noMutableInDeps = defineRule({
	id: "no-mutable-in-deps",
	title: "Mutable value in effect dependencies",
	severity: "error",
	recommendation: "Read mutable values like `location.pathname` or `ref.current` inside the effect body, or subscribe with `useSyncExternalStore`. Changing them doesn't redraw the screen, so listing them in deps won't make the effect run again.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const useRefBindingNames = collectUseRefBindingNames(componentBody);
			walkAst(componentBody, (child) => {
				if (!isNodeOfType(child, "CallExpression")) return;
				if (!isHookCall$1(child, HOOKS_WITH_DEPS)) return;
				if ((child.arguments?.length ?? 0) < 2) return;
				const depsNode = child.arguments[1];
				if (!isNodeOfType(depsNode, "ArrayExpression")) return;
				for (const element of depsNode.elements ?? []) {
					if (!element) continue;
					const issue = findMutableDepIssue(element, useRefBindingNames);
					if (!issue) continue;
					if (issue.kind === "ref-current") context.report({
						node: element,
						message: `Your effect silently never re-runs on "${issue.rootName}.current" because changing a ref doesn't redraw the screen.`
					});
					else context.report({
						node: element,
						message: `Your effect silently never re-runs on "${issue.rootName}.*" because values like \`location.pathname\` can change without redrawing the screen.`
					});
				}
			});
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/utils/lodash-mutator-call.ts
const LODASH_MUTATOR_NAMES = new Set([
	"set",
	"unset",
	"update",
	"merge",
	"defaults",
	"defaultsDeep",
	"assign",
	"assignIn",
	"pull",
	"pullAll",
	"pullAllBy",
	"pullAt",
	"remove",
	"fill"
]);
const LODASH_MUTATING_MODULES = new Set(["lodash", "lodash-es"]);
const isLodashMutatingImport = (sourceValue) => {
	if (LODASH_MUTATING_MODULES.has(sourceValue)) return true;
	if (sourceValue.startsWith("lodash/fp") || sourceValue.startsWith("lodash-es/fp")) return false;
	if (sourceValue.startsWith("lodash/") || sourceValue.startsWith("lodash-es/")) return true;
	return false;
};
const isLodashMutatorCall = (callExpression) => {
	if (!isNodeOfType(callExpression, "CallExpression")) return false;
	const callee = callExpression.callee;
	if (isNodeOfType(callee, "Identifier")) {
		if (!LODASH_MUTATOR_NAMES.has(callee.name)) return false;
		const initializer = findVariableInitializer(callee, callee.name)?.initializer;
		if (!initializer) return false;
		if (!isNodeOfType(initializer, "ImportSpecifier") && !isNodeOfType(initializer, "ImportDefaultSpecifier") && !isNodeOfType(initializer, "ImportNamespaceSpecifier")) return false;
		const importDeclaration = initializer.parent;
		if (!importDeclaration || !isNodeOfType(importDeclaration, "ImportDeclaration")) return false;
		const sourceValue = importDeclaration.source?.value;
		if (typeof sourceValue !== "string") return false;
		return isLodashMutatingImport(sourceValue);
	}
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed) {
		const propertyName = getStaticMemberPropertyName(callee);
		if (!propertyName || !LODASH_MUTATOR_NAMES.has(propertyName)) return false;
		const receiver = callee.object;
		if (!isNodeOfType(receiver, "Identifier")) return false;
		const initializer = findVariableInitializer(receiver, receiver.name)?.initializer;
		if (!initializer) return false;
		if (!isNodeOfType(initializer, "ImportNamespaceSpecifier") && !isNodeOfType(initializer, "ImportDefaultSpecifier")) return false;
		const importDeclaration = initializer.parent;
		if (!importDeclaration || !isNodeOfType(importDeclaration, "ImportDeclaration")) return false;
		const sourceValue = importDeclaration.source?.value;
		if (typeof sourceValue !== "string") return false;
		return isLodashMutatingImport(sourceValue);
	}
	return false;
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/resolve-reducer-function.ts
const resolveReducerFunction = (node, currentFilename) => {
	if (!node) return null;
	const unwrappedNode = stripParenExpression(node);
	if (isFunctionLike$2(unwrappedNode)) return {
		functionNode: unwrappedNode,
		crossFileSourceDisplay: null
	};
	if (!isNodeOfType(unwrappedNode, "Identifier")) return null;
	const initializer = findVariableInitializer(unwrappedNode, unwrappedNode.name)?.initializer;
	if (!initializer) return null;
	const unwrappedInitializer = stripParenExpression(initializer);
	if (isFunctionLike$2(unwrappedInitializer)) return {
		functionNode: unwrappedInitializer,
		crossFileSourceDisplay: null
	};
	if (isNodeOfType(initializer, "ImportSpecifier") || isNodeOfType(initializer, "ImportDefaultSpecifier")) {
		if (!currentFilename) return null;
		const importDeclaration = initializer.parent;
		if (!importDeclaration || !isNodeOfType(importDeclaration, "ImportDeclaration")) return null;
		const sourceValue = importDeclaration.source?.value;
		if (typeof sourceValue !== "string") return null;
		const exportedName = resolveImportedExportName(initializer);
		if (!exportedName) return null;
		const crossFileFunction = resolveCrossFileFunctionExport(currentFilename, sourceValue, exportedName);
		if (!crossFileFunction) return null;
		return {
			functionNode: crossFileFunction,
			crossFileSourceDisplay: sourceValue
		};
	}
	return null;
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-mutating-reducer-state.ts
const MESSAGE$16 = "This reducer changes state in place, so your update is silently skipped.";
const SAME_REFERENCE_ARRAY_RETURN_METHODS = new Set([
	"copyWithin",
	"fill",
	"reverse",
	"sort"
]);
const SAME_REFERENCE_COLLECTION_RETURN_METHODS = new Set(["add", "set"]);
const OBJECT_MUTATION_METHODS = new Set([
	"assign",
	"defineProperties",
	"defineProperty"
]);
const REFLECT_MUTATION_METHODS = new Set(["deleteProperty", "set"]);
const cloneReducerPathState = (state) => ({
	originalStateReferenceNames: new Set(state.originalStateReferenceNames),
	mutableStateSourceNames: new Set(state.mutableStateSourceNames),
	mutations: [...state.mutations]
});
const isSpecifierImportedFromReact = (node) => {
	const parent = node.parent ?? null;
	return parent !== null && isNodeOfType(parent, "ImportDeclaration") && parent.source.value === "react";
};
const isNamedReactUseReducerImportSpecifier = (node) => {
	if (!isNodeOfType(node, "ImportSpecifier")) return false;
	if (!isSpecifierImportedFromReact(node)) return false;
	const imported = node.imported;
	if (isNodeOfType(imported, "Identifier")) return imported.name === "useReducer";
	if (isNodeOfType(imported, "Literal")) return imported.value === "useReducer";
	return false;
};
const isReactNamespaceOrDefaultImportSpecifier = (node) => isSpecifierImportedFromReact(node) && (isNodeOfType(node, "ImportNamespaceSpecifier") || isNodeOfType(node, "ImportDefaultSpecifier"));
const isCallToImportedReactUseReducer = (node) => {
	const callee = node.callee;
	if (isNodeOfType(callee, "Identifier")) {
		const binding = findVariableInitializer(callee, callee.name);
		return Boolean(binding?.initializer && isNamedReactUseReducerImportSpecifier(binding.initializer));
	}
	if (!isNodeOfType(callee, "MemberExpression")) return false;
	if (!isNodeOfType(callee.object, "Identifier")) return false;
	if (!isNodeOfType(callee.property, "Identifier")) return false;
	if (callee.property.name !== "useReducer") return false;
	const binding = findVariableInitializer(callee.object, callee.object.name);
	return Boolean(binding?.initializer && isReactNamespaceOrDefaultImportSpecifier(binding.initializer));
};
const isStaticMethodCallOnNamedObject = (node, objectName, methodNames) => {
	const unwrappedNode = stripParenExpression(node);
	if (!isNodeOfType(unwrappedNode, "CallExpression")) return false;
	if (!isNodeOfType(unwrappedNode.callee, "MemberExpression")) return false;
	const calleeObject = unwrappedNode.callee.object;
	if (!isNodeOfType(calleeObject, "Identifier")) return false;
	if (calleeObject.name !== objectName) return false;
	if (!methodNames.has(getStaticMemberPropertyName(unwrappedNode.callee) ?? "")) return false;
	if (findVariableInitializer(calleeObject, calleeObject.name)) return false;
	return true;
};
const isExpressionRootedInMutableReducerStateSource = (node, state) => {
	let current = stripParenExpression(node);
	while (current && isNodeOfType(current, "MemberExpression")) current = stripParenExpression(current.object);
	return isNodeOfType(current, "Identifier") && state.mutableStateSourceNames.has(current.name);
};
const isExpressionOriginalReducerStateReference = (node, state) => {
	if (!node) return false;
	const unwrappedNode = stripParenExpression(node);
	return isNodeOfType(unwrappedNode, "Identifier") && state.originalStateReferenceNames.has(unwrappedNode.name);
};
const isExpressionReachableFromOriginalReducerState = (node, state) => {
	if (!node) return false;
	if (isExpressionOriginalReducerStateReference(node, state)) return true;
	const unwrappedNode = stripParenExpression(node);
	return isNodeOfType(unwrappedNode, "MemberExpression") && isExpressionRootedInMutableReducerStateSource(unwrappedNode, state);
};
const canExpressionReturnOriginalReducerStateReference = (node, state) => {
	if (!node) return false;
	const unwrappedNode = stripParenExpression(node);
	if (isExpressionOriginalReducerStateReference(unwrappedNode, state)) return true;
	if (isNodeOfType(unwrappedNode, "CallExpression")) {
		if (isNodeOfType(unwrappedNode.callee, "MemberExpression")) {
			const methodName = getStaticMemberPropertyName(unwrappedNode.callee);
			if (methodName === "assign" && isNodeOfType(unwrappedNode.callee.object, "Identifier") && unwrappedNode.callee.object.name === "Object") return isExpressionOriginalReducerStateReference(unwrappedNode.arguments?.[0], state);
			if (methodName && SAME_REFERENCE_ARRAY_RETURN_METHODS.has(methodName) && isExpressionOriginalReducerStateReference(unwrappedNode.callee.object, state)) return true;
			if (methodName && SAME_REFERENCE_COLLECTION_RETURN_METHODS.has(methodName) && isExpressionOriginalReducerStateReference(unwrappedNode.callee.object, state)) return true;
		}
	}
	if (isNodeOfType(unwrappedNode, "ConditionalExpression")) return canExpressionReturnOriginalReducerStateReference(unwrappedNode.consequent, state) || canExpressionReturnOriginalReducerStateReference(unwrappedNode.alternate, state);
	if (isNodeOfType(unwrappedNode, "LogicalExpression")) return canExpressionReturnOriginalReducerStateReference(unwrappedNode.left, state) || canExpressionReturnOriginalReducerStateReference(unwrappedNode.right, state);
	if (isNodeOfType(unwrappedNode, "SequenceExpression")) return canExpressionReturnOriginalReducerStateReference(unwrappedNode.expressions[unwrappedNode.expressions.length - 1], state);
	return false;
};
const collectReducerStateMutationsInExpressionOrStatement = (node, state) => {
	if (isFunctionLike$2(node)) return [];
	const mutations = [];
	walkAst(node, (child) => {
		const unwrappedChild = stripParenExpression(child);
		if (child !== node && isFunctionLike$2(unwrappedChild)) return false;
		if (isNodeOfType(unwrappedChild, "AssignmentExpression")) {
			if (isNodeOfType(stripParenExpression(unwrappedChild.left), "MemberExpression") && isExpressionRootedInMutableReducerStateSource(unwrappedChild.left, state)) mutations.push({ node: unwrappedChild });
			return;
		}
		if (isNodeOfType(unwrappedChild, "UpdateExpression")) {
			if (isNodeOfType(stripParenExpression(unwrappedChild.argument), "MemberExpression") && isExpressionRootedInMutableReducerStateSource(unwrappedChild.argument, state)) mutations.push({ node: unwrappedChild });
			return;
		}
		if (isNodeOfType(unwrappedChild, "UnaryExpression") && unwrappedChild.operator === "delete") {
			if (isNodeOfType(stripParenExpression(unwrappedChild.argument), "MemberExpression") && isExpressionRootedInMutableReducerStateSource(unwrappedChild.argument, state)) mutations.push({ node: unwrappedChild });
			return;
		}
		if (!isNodeOfType(unwrappedChild, "CallExpression")) return;
		const firstArgument = unwrappedChild.arguments?.[0];
		if (firstArgument && isExpressionRootedInMutableReducerStateSource(firstArgument, state) && (isStaticMethodCallOnNamedObject(unwrappedChild, "Object", OBJECT_MUTATION_METHODS) || isStaticMethodCallOnNamedObject(unwrappedChild, "Reflect", REFLECT_MUTATION_METHODS))) {
			mutations.push({ node: unwrappedChild });
			return;
		}
		if (firstArgument && isExpressionRootedInMutableReducerStateSource(firstArgument, state) && isLodashMutatorCall(unwrappedChild)) {
			mutations.push({ node: unwrappedChild });
			return;
		}
		if (!isNodeOfType(unwrappedChild.callee, "MemberExpression")) return;
		const methodName = getStaticMemberPropertyName(unwrappedChild.callee);
		if (!methodName || !MUTATING_ARRAY_METHODS.has(methodName) && !MUTATING_COLLECTION_METHODS.has(methodName)) return;
		if (isExpressionRootedInMutableReducerStateSource(unwrappedChild.callee.object, state)) mutations.push({ node: unwrappedChild });
	});
	return mutations;
};
const collectBlockScopedBindingNames = (blockStatement) => {
	const blockScopedBindingNames = /* @__PURE__ */ new Set();
	for (const statement of blockStatement.body ?? []) {
		if (!isNodeOfType(statement, "VariableDeclaration")) continue;
		if (statement.kind !== "let" && statement.kind !== "const") continue;
		for (const declarator of statement.declarations ?? []) collectPatternNames(declarator.id, blockScopedBindingNames);
	}
	return blockScopedBindingNames;
};
const restoreOuterIdentityForBlockScopedNames = (blockState, outerState, blockScopedBindingNames) => {
	const nextState = cloneReducerPathState(blockState);
	for (const name of blockScopedBindingNames) {
		if (outerState.originalStateReferenceNames.has(name)) nextState.originalStateReferenceNames.add(name);
		else nextState.originalStateReferenceNames.delete(name);
		if (outerState.mutableStateSourceNames.has(name)) nextState.mutableStateSourceNames.add(name);
		else nextState.mutableStateSourceNames.delete(name);
	}
	return nextState;
};
const recordDestructuredAliasNames = (pattern, state) => {
	if (isNodeOfType(pattern, "ObjectPattern")) {
		for (const property of pattern.properties ?? []) {
			if (!isNodeOfType(property, "Property")) continue;
			const valueNode = property.value;
			let leafIdentifier = null;
			if (isNodeOfType(valueNode, "Identifier")) leafIdentifier = valueNode;
			else if (isNodeOfType(valueNode, "AssignmentPattern") && isNodeOfType(valueNode.left, "Identifier")) leafIdentifier = valueNode.left;
			if (!leafIdentifier) continue;
			state.mutableStateSourceNames.add(leafIdentifier.name);
		}
		return;
	}
	if (isNodeOfType(pattern, "ArrayPattern")) for (const element of pattern.elements ?? []) {
		if (!element) continue;
		if (isNodeOfType(element, "Identifier")) {
			state.mutableStateSourceNames.add(element.name);
			continue;
		}
		if (isNodeOfType(element, "AssignmentPattern") && isNodeOfType(element.left, "Identifier")) {
			state.mutableStateSourceNames.add(element.left.name);
			continue;
		}
		if (isNodeOfType(element, "RestElement") && isNodeOfType(element.argument, "Identifier")) {}
	}
};
const updateReducerStateIdentityForVariableDeclaration = (declaration, state) => {
	for (const declarator of declaration.declarations ?? []) {
		if (isNodeOfType(declarator.id, "Identifier")) {
			const name = declarator.id.name;
			state.originalStateReferenceNames.delete(name);
			state.mutableStateSourceNames.delete(name);
			if (isExpressionOriginalReducerStateReference(declarator.init, state)) {
				state.originalStateReferenceNames.add(name);
				state.mutableStateSourceNames.add(name);
				continue;
			}
			if (isExpressionReachableFromOriginalReducerState(declarator.init, state)) state.mutableStateSourceNames.add(name);
			continue;
		}
		if ((isNodeOfType(declarator.id, "ObjectPattern") || isNodeOfType(declarator.id, "ArrayPattern")) && isExpressionReachableFromOriginalReducerState(declarator.init, state)) recordDestructuredAliasNames(declarator.id, state);
	}
};
const updateReducerStateIdentityForIdentifierAssignment = (assignment, state) => {
	if (!isNodeOfType(assignment.left, "Identifier")) return;
	const name = assignment.left.name;
	state.originalStateReferenceNames.delete(name);
	state.mutableStateSourceNames.delete(name);
	if (isExpressionOriginalReducerStateReference(assignment.right, state)) {
		state.originalStateReferenceNames.add(name);
		state.mutableStateSourceNames.add(name);
		return;
	}
	if (isExpressionReachableFromOriginalReducerState(assignment.right, state)) state.mutableStateSourceNames.add(name);
};
const analyzeReactUseReducerFunctionForStateMutation = (context, functionNode, reportedNodes, options) => {
	if (!isFunctionLike$2(functionNode) || !isNodeOfType(functionNode.body, "BlockStatement")) return;
	const firstParam = functionNode.params?.[0];
	const stateName = isNodeOfType(firstParam, "Identifier") ? firstParam.name : isNodeOfType(firstParam, "AssignmentPattern") && isNodeOfType(firstParam.left, "Identifier") ? firstParam.left.name : null;
	if (!stateName) return;
	const reportReducerStateMutations = (mutations) => {
		if (mutations.length === 0) return;
		if (options.crossFileConsumerCallSite && options.crossFileSourceDisplay) {
			if (reportedNodes.has(options.crossFileConsumerCallSite)) return;
			reportedNodes.add(options.crossFileConsumerCallSite);
			context.report({
				node: options.crossFileConsumerCallSite,
				message: `${MESSAGE$16} (mutation in imported reducer at \`${options.crossFileSourceDisplay}\`)`
			});
			return;
		}
		for (const mutation of mutations) {
			if (reportedNodes.has(mutation.node)) continue;
			reportedNodes.add(mutation.node);
			context.report({
				node: mutation.node,
				message: MESSAGE$16
			});
		}
	};
	let pathBudgetExceeded = false;
	const analyzeReducerStatementListByPath = (statements, initialState) => {
		if (pathBudgetExceeded) return [cloneReducerPathState(initialState)];
		let activeStates = [cloneReducerPathState(initialState)];
		for (const statement of statements) {
			if (activeStates.length > 1e3) {
				pathBudgetExceeded = true;
				break;
			}
			const nextStates = [];
			for (const activeState of activeStates) {
				if (isNodeOfType(statement, "ReturnStatement")) {
					const returnMutations = collectReducerStateMutationsInExpressionOrStatement(statement, activeState);
					const mutationsAtReturn = [...activeState.mutations, ...returnMutations];
					if (canExpressionReturnOriginalReducerStateReference(statement.argument, activeState)) reportReducerStateMutations(mutationsAtReturn);
					continue;
				}
				if (isNodeOfType(statement, "IfStatement")) {
					const conditionState = cloneReducerPathState(activeState);
					conditionState.mutations.push(...collectReducerStateMutationsInExpressionOrStatement(statement.test, conditionState));
					const consequentStates = analyzeReducerStatementListByPath(isNodeOfType(statement.consequent, "BlockStatement") ? statement.consequent.body : [statement.consequent], conditionState);
					const alternateStates = statement.alternate ? analyzeReducerStatementListByPath(isNodeOfType(statement.alternate, "BlockStatement") ? statement.alternate.body : [statement.alternate], conditionState) : [cloneReducerPathState(conditionState)];
					nextStates.push(...consequentStates, ...alternateStates);
					continue;
				}
				if (isNodeOfType(statement, "SwitchStatement")) {
					const discriminantState = cloneReducerPathState(activeState);
					discriminantState.mutations.push(...collectReducerStateMutationsInExpressionOrStatement(statement.discriminant, discriminantState));
					const switchCases = statement.cases ?? [];
					if (!switchCases.some((switchCase) => switchCase.test === null)) nextStates.push(cloneReducerPathState(discriminantState));
					for (let startIndex = 0; startIndex < switchCases.length; startIndex += 1) {
						const fallthroughStatements = [];
						for (let caseIndex = startIndex; caseIndex < switchCases.length; caseIndex += 1) {
							let didHitBreak = false;
							for (const caseStatement of switchCases[caseIndex].consequent ?? []) {
								if (isNodeOfType(caseStatement, "BreakStatement")) {
									didHitBreak = true;
									break;
								}
								fallthroughStatements.push(caseStatement);
							}
							if (didHitBreak) break;
						}
						nextStates.push(...analyzeReducerStatementListByPath(fallthroughStatements, discriminantState));
					}
					continue;
				}
				if (isNodeOfType(statement, "BlockStatement")) {
					const blockScopedBindingNames = collectBlockScopedBindingNames(statement);
					const blockStates = analyzeReducerStatementListByPath(statement.body, activeState);
					for (const blockState of blockStates) nextStates.push(restoreOuterIdentityForBlockScopedNames(blockState, activeState, blockScopedBindingNames));
					continue;
				}
				const nextState = cloneReducerPathState(activeState);
				nextState.mutations.push(...collectReducerStateMutationsInExpressionOrStatement(statement, nextState));
				if (isNodeOfType(statement, "VariableDeclaration")) updateReducerStateIdentityForVariableDeclaration(statement, nextState);
				else if (isNodeOfType(statement, "ExpressionStatement") && isNodeOfType(statement.expression, "AssignmentExpression")) updateReducerStateIdentityForIdentifierAssignment(statement.expression, nextState);
				nextStates.push(nextState);
			}
			activeStates = nextStates;
			if (activeStates.length === 0) break;
		}
		return activeStates;
	};
	analyzeReducerStatementListByPath(functionNode.body.body, {
		originalStateReferenceNames: new Set([stateName]),
		mutableStateSourceNames: new Set([stateName]),
		mutations: []
	});
};
const noMutatingReducerState = defineRule({
	id: "no-mutating-reducer-state",
	title: "Reducer mutates its state",
	severity: "error",
	recommendation: "Return a new state object from the reducer instead of changing the old one and returning it. React only notices the change when the object is new.",
	create: (context) => {
		const analyzedReducers = /* @__PURE__ */ new WeakSet();
		const reportedNodes = /* @__PURE__ */ new WeakSet();
		const currentFilename = context.filename;
		return { CallExpression(node) {
			if (!isCallToImportedReactUseReducer(node)) return;
			const resolved = resolveReducerFunction(node.arguments?.[0], currentFilename);
			if (!resolved) return;
			if (analyzedReducers.has(resolved.functionNode)) return;
			analyzedReducers.add(resolved.functionNode);
			analyzeReactUseReducerFunctionForStateMutation(context, resolved.functionNode, reportedNodes, {
				crossFileConsumerCallSite: resolved.crossFileSourceDisplay ? node : null,
				crossFileSourceDisplay: resolved.crossFileSourceDisplay
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/no-namespace.ts
const buildMessage$13 = (componentName) => `React can't render namespaced names like \`${componentName}\`.`;
const noNamespace = defineRule({
	id: "no-namespace",
	title: "Namespaced JSX element",
	severity: "warn",
	recommendation: "Drop the namespace and use a plain (Pascal-cased) component or DOM tag.",
	create: (context) => ({
		JSXOpeningElement(node) {
			if (!isNodeOfType(node.name, "JSXNamespacedName")) return;
			const namespaced = node.name;
			const fullName = `${namespaced.namespace.name}:${namespaced.name.name}`;
			context.report({
				node: namespaced,
				message: buildMessage$13(fullName)
			});
		},
		CallExpression(node) {
			if (!isCreateElementCall(node)) return;
			const firstArgument = node.arguments[0];
			if (!firstArgument) return;
			if (!isNodeOfType(firstArgument, "Literal") || typeof firstArgument.value !== "string") return;
			if (!firstArgument.value.includes(":")) return;
			context.report({
				node: firstArgument,
				message: buildMessage$13(firstArgument.value)
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/architecture/no-nested-component-definition.ts
const noNestedComponentDefinition = defineRule({
	id: "no-nested-component-definition",
	title: "Component defined inside another component",
	tags: ["test-noise", "react-jsx-only"],
	severity: "error",
	category: "Correctness",
	recommendation: "Move to a separate file or to module scope above the parent component",
	create: (context) => {
		const componentStack = [];
		return {
			FunctionDeclaration(node) {
				if (!isComponentDeclaration(node) || !node.id) return;
				if (componentStack.length > 0) context.report({
					node: node.id,
					message: `Your users lose all state in "${node.id.name}" on every render because it's defined inside "${componentStack[componentStack.length - 1]}", so move it out to the top of the file.`
				});
				componentStack.push(node.id.name);
			},
			"FunctionDeclaration:exit"(node) {
				if (isComponentDeclaration(node)) componentStack.pop();
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.id, "Identifier")) return;
				if (componentStack.length > 0) context.report({
					node: node.id,
					message: `Your users lose all state in "${node.id.name}" on every render because it's defined inside "${componentStack[componentStack.length - 1]}", so move it out to the top of the file.`
				});
				componentStack.push(node.id.name);
			},
			"VariableDeclarator:exit"(node) {
				if (isComponentAssignment(node)) componentStack.pop();
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/a11y/no-noninteractive-element-interactions.ts
const buildMessage$12 = (tag) => `Keyboard & screen reader users can't trigger this \`<${tag}>\` because it isn't interactive, so use a button or link or add an interactive role.`;
const INTERACTIVE_HANDLERS = [
	"onClick",
	"onMouseDown",
	"onMouseUp",
	"onKeyDown",
	"onKeyPress",
	"onKeyUp"
];
const noNoninteractiveElementInteractions = defineRule({
	id: "no-noninteractive-element-interactions",
	title: "Handler on non-interactive element",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Put interactions on a button or link, or add an interactive role.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const tag = getElementType(node, context.settings);
		if (!NON_INTERACTIVE_ELEMENTS.has(tag)) return;
		if (!INTERACTIVE_HANDLERS.some((handler) => hasJsxPropIgnoreCase(node.attributes, handler))) return;
		const roleAttr = hasJsxPropIgnoreCase(node.attributes, "role");
		if (roleAttr) {
			const role = getJsxPropStringValue(roleAttr);
			if (role && INTERACTIVE_ROLES.has(role)) return;
		}
		context.report({
			node: node.name,
			message: buildMessage$12(tag)
		});
	} })
});
//#endregion
//#region src/plugin/rules/a11y/no-noninteractive-element-to-interactive-role.ts
const buildMessage$11 = (tag, role) => `Screen reader users get confused because role \`${role}\` makes \`<${tag}>\` act interactive when it isn't, so use a real interactive element instead.`;
const DEFAULT_ALLOWED_ROLES = {
	ul: [
		"menu",
		"menubar",
		"radiogroup",
		"tablist",
		"tree",
		"treegrid"
	],
	ol: [
		"menu",
		"menubar",
		"radiogroup",
		"tablist",
		"tree",
		"treegrid"
	],
	li: [
		"menuitem",
		"menuitemcheckbox",
		"menuitemradio",
		"row",
		"tab",
		"treeitem"
	],
	fieldset: ["radiogroup", "presentation"]
};
const resolveSettings$15 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	if (typeof reactDoctor !== "object" || reactDoctor === null) return DEFAULT_ALLOWED_ROLES;
	const reactDoctorBlock = reactDoctor;
	if (!("noNoninteractiveElementToInteractiveRole" in reactDoctorBlock)) return DEFAULT_ALLOWED_ROLES;
	const ruleSettings = reactDoctorBlock.noNoninteractiveElementToInteractiveRole ?? {};
	const result = {};
	for (const key of Object.keys(ruleSettings)) {
		const value = ruleSettings[key];
		if (Array.isArray(value)) result[key] = value;
	}
	return result;
};
const noNoninteractiveElementToInteractiveRole = defineRule({
	id: "no-noninteractive-element-to-interactive-role",
	title: "Noninteractive element given interactive role",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Use a real interactive element instead of adding an interactive role to a static one.",
	category: "Accessibility",
	create: (context) => {
		const allowedRoles = resolveSettings$15(context.settings);
		return { JSXOpeningElement(node) {
			const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
			if (!roleAttribute) return;
			const value = roleAttribute.value;
			if (!value || !isNodeOfType(value, "Literal") || typeof value.value !== "string") return;
			const firstRole = value.value.trim().split(/\s+/)[0];
			if (!firstRole) return;
			const elementType = getElementType(node, context.settings);
			if (!HTML_TAGS.has(elementType)) return;
			const allowed = allowedRoles[elementType];
			if (allowed && allowed.includes(firstRole)) return;
			if (!isNonInteractiveElement(elementType, node)) return;
			if (!isInteractiveRole(firstRole)) return;
			context.report({
				node: roleAttribute,
				message: buildMessage$11(elementType, firstRole)
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/a11y/no-noninteractive-tabindex.ts
const MESSAGE$15 = "Keyboard users get stuck focusing this element they can't act on because `tabIndex` makes it tabbable, so remove it.";
const resolveSettings$14 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noNoninteractiveTabindex ?? {} : {};
	return {
		tags: ruleSettings.tags ?? [],
		roles: ruleSettings.roles ?? ["tabpanel"],
		allowExpressionValues: ruleSettings.allowExpressionValues ?? true
	};
};
const noNoninteractiveTabindex = defineRule({
	id: "no-noninteractive-tabindex",
	title: "Tabindex on non-interactive element",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Only add `tabIndex` to interactive elements or interactive roles.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$14(context.settings);
		return { JSXOpeningElement(node) {
			const tabIndex = hasJsxPropIgnoreCase(node.attributes, "tabIndex");
			if (!tabIndex) return;
			const tabIndexValue = tabIndex.value;
			if (!tabIndexValue) return;
			const numeric = parseJsxValue(tabIndexValue);
			if (numeric === null) {
				if (isNodeOfType(tabIndexValue, "JSXExpressionContainer") && !settings.allowExpressionValues) context.report({
					node: tabIndex,
					message: MESSAGE$15
				});
				return;
			}
			if (numeric < 0 || numeric % 1 !== 0) return;
			const elementType = getElementType(node, context.settings);
			if (settings.tags.includes(elementType)) return;
			if (!HTML_TAGS.has(elementType)) return;
			if (isInteractiveElement(elementType, node)) return;
			const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
			if (!roleAttribute) {
				context.report({
					node: tabIndex,
					message: MESSAGE$15
				});
				return;
			}
			const roleValue = roleAttribute.value;
			if (roleValue) {
				if (isNodeOfType(roleValue, "Literal") && typeof roleValue.value === "string") {
					const firstRole = roleValue.value.split(/\s+/)[0];
					if (firstRole && (isInteractiveRole(firstRole) || settings.roles.includes(firstRole))) return;
				}
				if (isNodeOfType(roleValue, "JSXExpressionContainer") && settings.allowExpressionValues) return;
			}
			context.report({
				node: tabIndex,
				message: MESSAGE$15
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/design/utils/get-style-property-number-value.ts
const getStylePropertyNumberValue = (property) => {
	if (!isNodeOfType(property, "Property")) return null;
	if (isNodeOfType(property.value, "Literal") && typeof property.value.value === "number") return property.value.value;
	if (isNodeOfType(property.value, "UnaryExpression") && property.value.operator === "-" && isNodeOfType(property.value.argument, "Literal") && typeof property.value.argument.value === "number") return -property.value.argument.value;
	return null;
};
//#endregion
//#region src/plugin/rules/design/no-outline-none.ts
const noOutlineNone = defineRule({
	id: "no-outline-none",
	title: "outline:none removes focus ring",
	severity: "warn",
	tags: ["test-noise"],
	category: "Accessibility",
	recommendation: "Style `:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px }` so the focus ring shows for keyboard users but not mouse clicks.",
	create: (context) => ({ JSXAttribute(node) {
		const expression = getInlineStyleExpression(node);
		if (!expression) return;
		let hasOutlineNone = false;
		let outlineProperty = null;
		for (const property of expression.properties ?? []) {
			if (getStylePropertyKey(property) !== "outline") continue;
			const strValue = getStylePropertyStringValue(property);
			const numValue = getStylePropertyNumberValue(property);
			if (strValue === "none" || strValue === "0" || numValue === 0) {
				hasOutlineNone = true;
				outlineProperty = property;
			}
		}
		if (!hasOutlineNone || !outlineProperty) return;
		if (!expression.properties?.some((property) => {
			return getStylePropertyKey(property) === "boxShadow";
		})) context.report({
			node: outlineProperty,
			message: "Your keyboard users can't tell where they are because outline: none hides the focus ring, so style :focus-visible instead, or add a box-shadow focus ring."
		});
	} })
});
//#endregion
//#region src/plugin/utils/is-namespaced-api-call.ts
const NAMESPACED_API_PROPERTY_NAMES = new Set([
	"commands",
	"actions",
	"api",
	"store",
	"service",
	"client",
	"controller",
	"manager",
	"registry",
	"dispatch",
	"queryClient",
	"fetcher",
	"loader",
	"editor",
	"model",
	"context",
	"transport",
	"channel",
	"session",
	"connection",
	"instance",
	"ref",
	"current",
	"value",
	"state",
	"vm",
	"viewModel",
	"logic",
	"selectors",
	"queries",
	"mutations",
	"effects",
	"utils",
	"helpers",
	"lib",
	"fonts",
	"shapes",
	"nodes",
	"layers",
	"users",
	"accounts",
	"events",
	"logs",
	"metrics",
	"telemetry",
	"tracker",
	"tracking",
	"analytics",
	"posthog",
	"sentry",
	"auth",
	"permissions",
	"roles",
	"features",
	"flags",
	"config",
	"settings",
	"preferences",
	"storage",
	"cache",
	"history",
	"navigation",
	"router",
	"navigator",
	"scheduler",
	"queue",
	"pipeline",
	"stream",
	"socket",
	"bridge",
	"io",
	"fs",
	"db",
	"kv",
	"blob",
	"buffer",
	"cells",
	"rows",
	"columns",
	"tabs",
	"panels",
	"windows",
	"elements",
	"selections",
	"selection",
	"clipboard",
	"viewport",
	"camera",
	"scene",
	"world",
	"physics",
	"renderer",
	"renderers",
	"rendering",
	"ports",
	"messages",
	"channels",
	"subscriptions",
	"observers",
	"watchers",
	"listeners",
	"handlers"
]);
const isNamespacedApiCallee = (callee) => {
	let cursor = callee;
	let hops = 0;
	while (cursor && hops < 16) {
		hops += 1;
		if (!isNodeOfType(cursor, "MemberExpression")) return false;
		if (!cursor.computed && isNodeOfType(cursor.property, "Identifier")) {
			if (NAMESPACED_API_PROPERTY_NAMES.has(cursor.property.name)) return true;
		}
		cursor = cursor.object;
	}
	return false;
};
//#endregion
//#region src/plugin/constants/data-sink-method-names.ts
/**
* Method names that conventionally "consume" or "sink" the value
* passed to them rather than handing it BACK to a parent — used by
* `no-pass-data-to-parent` and `no-pass-live-state-to-parent` to
* filter out call shapes that aren't actually the data-handoff
* anti-pattern they detect.
*
* Was duplicated verbatim in both rule files; promoted to this
* shared module so adding a new method name (a new EventEmitter
* variant, a new imperative-action verb) propagates to both
* detectors without drift.
*
* The previous name `ITERATOR_METHOD_NAMES` was misleading —
* iterators are only one of the seven categories covered.
*/
const DATA_SINK_METHOD_NAMES = new Set([
	"forEach",
	"map",
	"filter",
	"reduce",
	"reduceRight",
	"flatMap",
	"some",
	"every",
	"find",
	"findIndex",
	"findLast",
	"findLastIndex",
	"subscribe",
	"unsubscribe",
	"addEventListener",
	"addListener",
	"removeEventListener",
	"removeListener",
	"on",
	"once",
	"off",
	"emit",
	"dispatch",
	"publish",
	"notify",
	"trigger",
	"fire",
	"broadcast",
	"send",
	"then",
	"catch",
	"finally",
	"add",
	"delete",
	"has",
	"get",
	"set",
	"clear",
	"put",
	"push",
	"pop",
	"shift",
	"unshift",
	"log",
	"info",
	"warn",
	"error",
	"debug",
	"trace",
	"track",
	"capture",
	"start",
	"stop",
	"play",
	"pause",
	"resume",
	"cancel",
	"abort",
	"commit",
	"rollback",
	"reset",
	"focus",
	"blur",
	"scroll",
	"scrollTo",
	"scrollIntoView",
	"close",
	"open",
	"show",
	"hide",
	"expand",
	"collapse",
	"toggle",
	"refresh",
	"reload",
	"rerender",
	"refetch",
	"invalidate",
	"select",
	"deselect",
	"click",
	"press",
	"tap",
	"submit",
	"validate",
	"format",
	"parse",
	"serialize",
	"deserialize"
]);
//#endregion
//#region src/plugin/utils/get-call-method-name.ts
/**
* Returns the static method name of a call's callee when it's a
* non-computed MemberExpression (`obj.method` → `"method"`), or
* `null` otherwise. Used by `no-pass-data-to-parent` and
* `no-pass-live-state-to-parent` to match the receiver-bound
* method-call shape against an allow/block list.
*/
const getCallMethodName = (callee) => {
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed && isNodeOfType(callee.property, "Identifier")) return callee.property.name;
	return null;
};
//#endregion
//#region src/plugin/rules/state-and-effects/no-pass-data-to-parent.ts
const isUseStateIdentifier = (identifier) => {
	if (!isNodeOfType(identifier, "Identifier")) return false;
	if (identifier.name === "useState") return true;
	const parent = identifier.parent;
	if (parent && isNodeOfType(parent, "MemberExpression") && isNodeOfType(parent.object, "Identifier") && parent.object.name === "React" && isNodeOfType(parent.property, "Identifier") && parent.property.name === "useState") return true;
	return false;
};
const isUseRefIdentifier = (identifier) => {
	if (!isNodeOfType(identifier, "Identifier")) return false;
	if (identifier.name === "useRef") return true;
	const parent = identifier.parent;
	if (parent && isNodeOfType(parent, "MemberExpression") && isNodeOfType(parent.object, "Identifier") && parent.object.name === "React" && isNodeOfType(parent.property, "Identifier") && parent.property.name === "useRef") return true;
	return false;
};
const noPassDataToParent = defineRule({
	id: "no-pass-data-to-parent",
	title: "Data passed to parent via effect",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Fetch the data in the parent and pass it down as a prop (or return it from the hook), instead of handing it back up through a prop callback in a useEffect. See https://react.dev/learn/you-might-not-need-an-effect#passing-data-to-the-parent",
	create: (context) => ({ CallExpression(node) {
		if (!isUseEffect(node)) return;
		const analysis = getProgramAnalysis(node);
		if (!analysis) return;
		if (hasCleanup(analysis, node)) return;
		const effectFnRefs = getEffectFnRefs(analysis, node);
		if (!effectFnRefs) return;
		const effectFn = getEffectFn(analysis, node);
		if (!effectFn) return;
		for (const ref of effectFnRefs) {
			if (!isPropCall(analysis, ref)) continue;
			if (isRefCall(analysis, ref)) continue;
			if (!isSynchronous(ref.identifier, effectFn)) continue;
			const callExpr = getCallExpr(ref);
			if (!callExpr) continue;
			const calleeNode = callExpr.callee;
			const methodName = calleeNode ? getCallMethodName(calleeNode) : null;
			if (methodName && DATA_SINK_METHOD_NAMES.has(methodName)) continue;
			if (calleeNode && isNamespacedApiCallee(calleeNode)) continue;
			if (!getArgsUpstreamRefs(analysis, ref).filter((argRef) => getUpstreamRefs(analysis, argRef).length === 1).some((argRef) => {
				if (isUseStateIdentifier(argRef.identifier)) return false;
				if (isProp(analysis, argRef)) return false;
				if (isUseRefIdentifier(argRef.identifier)) return false;
				if (isRefCurrent(argRef)) return false;
				if (isConstant(argRef)) return false;
				return true;
			})) continue;
			context.report({
				node: callExpr,
				message: "Handing data back to a parent from a useEffect costs your users an extra render."
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-pass-live-state-to-parent.ts
const noPassLiveStateToParent = defineRule({
	id: "no-pass-live-state-to-parent",
	title: "Live state pushed to parent via effect",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Move the state up to the parent (or return it from the hook), instead of handing it back up through a prop callback in a useEffect. See https://react.dev/learn/you-might-not-need-an-effect#notifying-parent-components-about-state-changes",
	create: (context) => ({ CallExpression(node) {
		if (!isUseEffect(node)) return;
		const analysis = getProgramAnalysis(node);
		if (!analysis) return;
		const effectFnRefs = getEffectFnRefs(analysis, node);
		if (!effectFnRefs) return;
		const effectFn = getEffectFn(analysis, node);
		if (!effectFn) return;
		for (const ref of effectFnRefs) {
			if (!isPropCall(analysis, ref)) continue;
			if (!isSynchronous(ref.identifier, effectFn)) continue;
			const callExpr = getCallExpr(ref);
			if (!callExpr) continue;
			const calleeNode = callExpr.callee;
			const methodName = calleeNode ? getCallMethodName(calleeNode) : null;
			if (methodName && DATA_SINK_METHOD_NAMES.has(methodName)) continue;
			if (calleeNode && isNamespacedApiCallee(calleeNode)) continue;
			if (!getArgsUpstreamRefs(analysis, ref).some((argRef) => isState(analysis, argRef))) continue;
			context.report({
				node: callExpr,
				message: "Pushing state up to a parent from a useEffect costs your users an extra render."
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/performance/no-permanent-will-change.ts
const noPermanentWillChange = defineRule({
	id: "no-permanent-will-change",
	title: "Permanent will-change wastes GPU",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Add will-change when the animation starts (`onMouseEnter`) and remove it when it ends (`onAnimationEnd`). Leaving it on all the time wastes GPU memory and can slow things down",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "style") return;
		if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const expression = node.value.expression;
		if (!isNodeOfType(expression, "ObjectExpression")) return;
		for (const property of expression.properties ?? []) {
			if (!isNodeOfType(property, "Property")) continue;
			if ((isNodeOfType(property.key, "Identifier") ? property.key.name : null) !== "willChange") continue;
			context.report({
				node: property,
				message: "This wastes GPU memory because will-change is left on all the time, so add it right before the animation & remove it when the animation ends"
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/correctness/no-polymorphic-children.ts
const noPolymorphicChildren = defineRule({
	id: "no-polymorphic-children",
	title: "Children type checked at runtime",
	severity: "warn",
	category: "Architecture",
	recommendation: "Add clear subcomponents like `<Button.Text>` and `<Button.Icon>` so callers don't have to check `typeof children`.",
	create: (context) => ({ BinaryExpression(node) {
		if (node.operator !== "===" && node.operator !== "==") return;
		const isTypeofChildren = (operand) => isNodeOfType(operand, "UnaryExpression") && operand.operator === "typeof" && isNodeOfType(operand.argument, "Identifier") && operand.argument.name === "children";
		if (!isTypeofChildren(node.left) && !isTypeofChildren(node.right)) return;
		const isStringLiteral = (operand) => isNodeOfType(operand, "Literal") && operand.value === "string";
		if (!isStringLiteral(node.left) && !isStringLiteral(node.right)) return;
		context.report({
			node,
			message: "Your users hit inconsistent behavior because `typeof children === \"string\"` makes this component switch on what callers pass, so add clear subcomponents like `<Button.Text>` instead."
		});
	} })
});
//#endregion
//#region src/plugin/utils/get-react-doctor-setting.ts
const readReactDoctorSettingsBag = (settings) => {
	const reactDoctorSettings = settings?.["react-doctor"];
	if (typeof reactDoctorSettings !== "object" || reactDoctorSettings === null || Array.isArray(reactDoctorSettings)) return null;
	return reactDoctorSettings;
};
const readOwnPropertyValue = (bag, settingName) => Object.getOwnPropertyDescriptor(bag, settingName)?.value;
const getReactDoctorStringSetting = (settings, settingName) => {
	const bag = readReactDoctorSettingsBag(settings);
	if (!bag) return void 0;
	const settingValue = readOwnPropertyValue(bag, settingName);
	return typeof settingValue === "string" ? settingValue : void 0;
};
const getReactDoctorNumberSetting = (settings, settingName) => {
	const bag = readReactDoctorSettingsBag(settings);
	if (!bag) return void 0;
	const settingValue = readOwnPropertyValue(bag, settingName);
	return typeof settingValue === "number" && Number.isFinite(settingValue) ? settingValue : void 0;
};
const getReactDoctorStringArraySetting = (settings, settingName) => {
	const bag = readReactDoctorSettingsBag(settings);
	if (!bag) return [];
	const settingValue = readOwnPropertyValue(bag, settingName);
	if (!Array.isArray(settingValue)) return [];
	return settingValue.filter((entry) => typeof entry === "string" && entry.length > 0);
};
//#endregion
//#region src/plugin/rules/correctness/no-prevent-default.ts
const PREVENT_DEFAULT_ELEMENTS = new Map([["form", ["onSubmit"]], ["a", ["onClick"]]]);
const SERVER_CAPABLE_FRAMEWORKS = new Set([
	"nextjs",
	"tanstack-start",
	"remix"
]);
const CLIENT_ONLY_FRAMEWORKS = new Set([
	"vite",
	"cra",
	"gatsby",
	"react-native",
	"expo"
]);
const FORM_MESSAGE_SERVER_CAPABLE = "Your users can't submit this <form> without JavaScript because onSubmit calls preventDefault(), so use a server action like `<form action={serverAction}>` to make it work either way.";
const FORM_MESSAGE_GENERIC = "Your users can't submit this <form> when JavaScript is off, so the form won't work without JavaScript. Consider a form action so it still works.";
const ANCHOR_MESSAGE = "Your users click this <a> & nothing navigates because onClick calls preventDefault(), so use a <button> or a routing component instead.";
const containsPreventDefaultCall = (node) => {
	let didFindPreventDefault = false;
	walkAst(node, (child) => {
		if (didFindPreventDefault) return;
		if (isNodeOfType(child, "CallExpression") && isNodeOfType(child.callee, "MemberExpression") && isNodeOfType(child.callee.property, "Identifier") && child.callee.property.name === "preventDefault") didFindPreventDefault = true;
	});
	return didFindPreventDefault;
};
const selectFormMessage = (framework) => framework !== void 0 && SERVER_CAPABLE_FRAMEWORKS.has(framework) ? FORM_MESSAGE_SERVER_CAPABLE : FORM_MESSAGE_GENERIC;
const noPreventDefault = defineRule({
	id: "no-prevent-default",
	title: "preventDefault on a form or link",
	severity: "warn",
	recommendation: "Use `<form action>` where your framework supports it (it works without JS), or use a `<button>` instead of an `<a>` with preventDefault.",
	create: (context) => {
		const framework = getReactDoctorStringSetting(context.settings, "framework");
		const isClientOnlyFramework = framework !== void 0 && CLIENT_ONLY_FRAMEWORKS.has(framework);
		const formMessage = selectFormMessage(framework);
		return { JSXOpeningElement(node) {
			const elementName = isNodeOfType(node.name, "JSXIdentifier") ? node.name.name : null;
			if (!elementName) return;
			const targetEventProps = PREVENT_DEFAULT_ELEMENTS.get(elementName);
			if (!targetEventProps) return;
			if (elementName === "form" && isClientOnlyFramework) return;
			for (const targetEventProp of targetEventProps) {
				const eventAttribute = findJsxAttribute(node.attributes ?? [], targetEventProp);
				if (!eventAttribute?.value || !isNodeOfType(eventAttribute.value, "JSXExpressionContainer")) continue;
				const expression = eventAttribute.value.expression;
				if (!isInlineFunctionExpression(expression)) continue;
				if (!containsPreventDefaultCall(expression)) continue;
				context.report({
					node,
					message: elementName === "form" ? formMessage : ANCHOR_MESSAGE
				});
				return;
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-prop-callback-in-effect.ts
const noPropCallbackInEffect = defineRule({
	id: "no-prop-callback-in-effect",
	title: "Parent kept in sync with a callback effect",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Move the shared state into a Provider so both sides read the same value. Then you don't need a useEffect to keep them in sync.",
	create: (context) => {
		const propStackTracker = createComponentPropStackTracker();
		return {
			...propStackTracker.visitors,
			CallExpression(node) {
				if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1) || (node.arguments?.length ?? 0) < 2) return;
				const callback = getEffectCallback(node);
				if (!callback || !isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) return;
				const depsNode = node.arguments[1];
				if (!isNodeOfType(depsNode, "ArrayExpression") || !depsNode.elements?.length) return;
				if (!(depsNode.elements ?? []).some((element) => isNodeOfType(element, "Identifier") && !propStackTracker.isPropName(element.name))) return;
				const reportedNodes = /* @__PURE__ */ new Set();
				walkInsideStatementBlocks(callback.body, (child) => {
					if (!isNodeOfType(child, "CallExpression")) return;
					if (!isNodeOfType(child.callee, "Identifier")) return;
					const calleeName = child.callee.name;
					if (!propStackTracker.isPropName(calleeName)) return;
					if (reportedNodes.has(child)) return;
					reportedNodes.add(child);
					context.report({
						node: child,
						message: `Your parent re-renders on every local state change because this useEffect calls the prop "${calleeName}" just to stay in sync.`
					});
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/architecture/no-prop-types.ts
const PROP_TYPES_PROPERTY = "propTypes";
const isPropTypesKey = (key, computed) => {
	if (!key) return false;
	if (computed) return isNodeOfType(key, "Literal") && key.value === PROP_TYPES_PROPERTY;
	return isNodeOfType(key, "Identifier") && key.name === PROP_TYPES_PROPERTY;
};
const getComponentNameFromPropTypesAssignment = (left) => {
	if (!isNodeOfType(left, "MemberExpression")) return null;
	if (!isPropTypesKey(left.property, Boolean(left.computed))) return null;
	if (!isNodeOfType(left.object, "Identifier")) return null;
	if (!isUppercaseName(left.object.name)) return null;
	return left.object.name;
};
const getComponentNameFromClassProperty = (node) => {
	if (!node.static) return null;
	if (!isPropTypesKey(node.key, Boolean(node.computed))) return null;
	const classBody = node.parent;
	if (!isNodeOfType(classBody, "ClassBody")) return null;
	const classNode = classBody.parent;
	if (!classNode) return null;
	if ((isNodeOfType(classNode, "ClassDeclaration") || isNodeOfType(classNode, "ClassExpression")) && classNode.id?.name && isUppercaseName(classNode.id.name)) return classNode.id.name;
	if (!isNodeOfType(classNode, "ClassExpression")) return null;
	const declarator = classNode.parent;
	if (!isNodeOfType(declarator, "VariableDeclarator")) return null;
	if (!isNodeOfType(declarator.id, "Identifier")) return null;
	if (!isUppercaseName(declarator.id.name)) return null;
	return declarator.id.name;
};
const buildMessage$10 = (componentName) => `${componentName}.propTypes does nothing in React 19, so bad props reach your users with no warning. Describe props with TypeScript types & check risky data yourself.`;
const noPropTypes = defineRule({
	id: "no-prop-types",
	title: "propTypes ignored in React 19",
	requires: ["react:19"],
	tags: ["test-noise"],
	severity: "warn",
	defaultEnabled: false,
	recommendation: "React 19 ignores `Component.propTypes`, so invalid props pass silently. Describe props with TypeScript types and add real runtime checks (or schema parsing) only where data can actually be wrong. Only runs on React 19+ projects.",
	create: (context) => ({
		AssignmentExpression(node) {
			if (node.operator !== "=") return;
			const componentName = getComponentNameFromPropTypesAssignment(node.left);
			if (!componentName) return;
			context.report({
				node: node.left,
				message: buildMessage$10(componentName)
			});
		},
		PropertyDefinition(node) {
			const componentName = getComponentNameFromClassProperty(node);
			if (!componentName) return;
			context.report({
				node: node.key,
				message: buildMessage$10(componentName)
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/design/no-pure-black-background.ts
const noPureBlackBackground = defineRule({
	id: "no-pure-black-background",
	title: "Pure black background",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Nudge the background slightly toward your brand color, like `#0a0a0f` or Tailwind's `bg-gray-950`. Pure black looks harsh on modern screens.",
	create: (context) => ({
		JSXAttribute(node) {
			const expression = getInlineStyleExpression(node);
			if (!expression) return;
			for (const property of expression.properties ?? []) {
				const key = getStylePropertyKey(property);
				if (key !== "backgroundColor" && key !== "background") continue;
				const value = getStylePropertyStringValue(property);
				if (value && isPureBlackColor(value)) context.report({
					node: property,
					message: "Your users see a harsh pure #000 background, so nudge it toward your brand color, like #0a0a0f."
				});
			}
		},
		JSXOpeningElement(node) {
			const classStr = getStringFromClassNameAttr(node);
			if (!classStr) return;
			if (/\bbg-black\b(?!\/)/.test(classStr)) context.report({
				node,
				message: "Your users see a harsh pure black background (bg-black), so use a near-black with a hint of your brand color, like bg-gray-950."
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/correctness/no-random-key.ts
const ALWAYS_FRESH_DIRECT_CALLEES = new Set([
	"nanoid",
	"uuid",
	"uuidv4",
	"uuidV4",
	"v4",
	"cuid",
	"cuid2",
	"createId",
	"ulid",
	"objectid",
	"ObjectId",
	"shortid"
]);
const ALWAYS_FRESH_MEMBER_RECEIVERS = new Map([
	["Math", new Set(["random"])],
	["Date", new Set(["now"])],
	["performance", new Set(["now"])],
	["crypto", new Set([
		"randomUUID",
		"getRandomValues",
		"randomBytes"
	])]
]);
const isAlwaysFreshExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "NewExpression")) {
		if (isNodeOfType(stripped.callee, "Identifier") && stripped.callee.name === "Date") return "new Date()";
	}
	if (!isNodeOfType(stripped, "CallExpression")) return null;
	const callee = stripped.callee;
	if (isNodeOfType(callee, "Identifier")) {
		if (!ALWAYS_FRESH_DIRECT_CALLEES.has(callee.name)) return null;
		const binding = findVariableInitializer(callee, callee.name);
		if (binding?.initializer && !isNodeOfType(binding.initializer, "ImportSpecifier") && !isNodeOfType(binding.initializer, "ImportDefaultSpecifier") && !isNodeOfType(binding.initializer, "ImportNamespaceSpecifier")) return null;
		return `${callee.name}()`;
	}
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed) {
		const receiver = callee.object;
		const property = callee.property;
		if (!isNodeOfType(property, "Identifier")) return null;
		if (isNodeOfType(receiver, "Identifier")) {
			if (ALWAYS_FRESH_MEMBER_RECEIVERS.get(receiver.name)?.has(property.name)) return `${receiver.name}.${property.name}()`;
		}
	}
	return null;
};
const variableLabelForUpdateArgument = (argument) => {
	if (!argument) return "counter";
	const stripped = stripParenExpression(argument);
	if (isNodeOfType(stripped, "Identifier")) return stripped.name;
	if (isNodeOfType(stripped, "MemberExpression") && !stripped.computed && isNodeOfType(stripped.property, "Identifier")) return stripped.property.name;
	return "counter";
};
const looksLikeFreshUpdateExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "UpdateExpression")) {
		const label = variableLabelForUpdateArgument(stripped.argument);
		return stripped.prefix ? `${stripped.operator}${label}` : `${label}${stripped.operator}`;
	}
	if (isNodeOfType(stripped, "AssignmentExpression") && (stripped.operator === "+=" || stripped.operator === "-=")) return `${stripped.operator} side-effect`;
	return null;
};
const noRandomKey = defineRule({
	id: "no-random-key",
	title: "Random value used as a key",
	severity: "error",
	category: "Correctness",
	recommendation: "Use a stable id from the item itself, like `item.id`, a content hash, or the index when the order never changes. Don't build the key from something that changes every time.",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		if (node.name.name !== "key") return;
		if (!node.value) return;
		if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const inner = node.value.expression;
		if (!inner) return;
		if (inner.type === "JSXEmptyExpression") return;
		const freshDescription = isAlwaysFreshExpression(inner) ?? looksLikeFreshUpdateExpression(inner);
		if (!freshDescription) return;
		context.report({
			node: node.value,
			message: `Your users lose typed input, focus & scroll position because \`key={${freshDescription}}\` makes a new key every render, so React rebuilds every item. Use a stable id from the item.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-react-children.ts
const MESSAGE$14 = "`React.Children` breaks easily when the children change shape.";
const isChildrenIdentifier = (node, contextNode) => {
	if (!isNodeOfType(node, "Identifier") || node.name !== "Children") return false;
	return isImportedFromModule(contextNode, "Children", "react");
};
const isReactNamespaceMember = (node, contextNode) => {
	if (!isNodeOfType(node, "MemberExpression")) return false;
	if ((isNodeOfType(node.property, "Identifier") ? node.property.name : null) !== "Children") return false;
	const objectInner = stripParenExpression(node.object);
	if (!isNodeOfType(objectInner, "Identifier")) return false;
	return isImportedFromModule(contextNode, objectInner.name, "react");
};
const noReactChildren = defineRule({
	id: "no-react-children",
	title: "Use of React.Children",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Pass children as props or render them directly instead of using React.Children.",
	category: "Architecture",
	create: (context) => ({ CallExpression(node) {
		const calleeOuter = stripParenExpression(node.callee);
		if (!isNodeOfType(calleeOuter, "MemberExpression")) return;
		const memberObject = stripParenExpression(calleeOuter.object);
		if (isChildrenIdentifier(memberObject, node)) {
			context.report({
				node: calleeOuter,
				message: MESSAGE$14
			});
			return;
		}
		if (isReactNamespaceMember(memberObject, node)) context.report({
			node: calleeOuter,
			message: MESSAGE$14
		});
	} })
});
//#endregion
//#region src/plugin/rules/architecture/utils/create-deprecated-react-import-rule.ts
const createDeprecatedReactImportRule = ({ source, messages, handleExtraSource }) => ({ create: (context) => {
	const namespaceBindings = /* @__PURE__ */ new Set();
	return {
		ImportDeclaration(node) {
			const sourceValue = node.source?.value;
			if (typeof sourceValue !== "string") return;
			if (handleExtraSource?.(node, context)) return;
			if (sourceValue !== source) return;
			for (const specifier of node.specifiers ?? []) {
				if (isNodeOfType(specifier, "ImportSpecifier")) {
					const importedName = getImportedName$1(specifier);
					if (!importedName) continue;
					const message = messages.get(importedName);
					if (message) context.report({
						node: specifier,
						message
					});
					continue;
				}
				if (isNodeOfType(specifier, "ImportDefaultSpecifier") || isNodeOfType(specifier, "ImportNamespaceSpecifier")) {
					const localName = specifier.local?.name;
					if (localName) namespaceBindings.add(localName);
				}
			}
		},
		MemberExpression(node) {
			if (namespaceBindings.size === 0) return;
			if (node.computed) return;
			if (!isNodeOfType(node.object, "Identifier")) return;
			if (!namespaceBindings.has(node.object.name)) return;
			if (!isNodeOfType(node.property, "Identifier")) return;
			const message = messages.get(node.property.name);
			if (message) context.report({
				node,
				message
			});
		}
	};
} });
//#endregion
//#region src/plugin/rules/architecture/no-react-dom-deprecated-apis.ts
const REACT_DOM_DEPRECATED_MESSAGES = new Map([
	["render", "ReactDOM.render crashes your app in React 19 since it's gone, so import `createRoot` from `react-dom/client` & call `createRoot(container).render(...)`."],
	["hydrate", "ReactDOM.hydrate crashes hydration in React 19 since it's gone, so import `hydrateRoot` from `react-dom/client` & call `hydrateRoot(container, <App />)`."],
	["unmountComponentAtNode", "ReactDOM.unmountComponentAtNode won't unmount your tree in React 19 since it's gone, so keep the root you created & call `root.unmount()` instead."],
	["findDOMNode", "ReactDOM.findDOMNode crashes in React 19 since it's gone, & it breaks composition anyway, so pass a ref & read `ref.current` instead."]
]);
const REACT_DOM_TEST_UTILS_REPLACEMENTS = new Map([
	["act", "`import { act } from 'react'` instead"],
	["Simulate", "`fireEvent` from `@testing-library/react` instead"],
	["renderIntoDocument", "`render` from `@testing-library/react` instead"],
	["findRenderedDOMComponentWithTag", "`getByRole` / `getByTestId` from `@testing-library/react`"],
	["findRenderedDOMComponentWithClass", "`getByRole` or `container.querySelector` from RTL"],
	["scryRenderedDOMComponentsWithTag", "`getAllByRole` from `@testing-library/react`"]
]);
const buildTestUtilsMessage = (importedName) => {
	const replacement = REACT_DOM_TEST_UTILS_REPLACEMENTS.get(importedName);
	return `react-dom/test-utils is removed in React 19, so your tests break. ${replacement ? `Use ${replacement}.` : "Switch to `act` from `react` or the equivalent in `@testing-library/react`."}`;
};
const reportTestUtilsImports = (node, context) => {
	for (const specifier of node.specifiers ?? []) {
		if (isNodeOfType(specifier, "ImportSpecifier")) {
			const importedName = getImportedName$1(specifier) ?? "default";
			context.report({
				node: specifier,
				message: buildTestUtilsMessage(importedName)
			});
			continue;
		}
		context.report({
			node: specifier,
			message: "react-dom/test-utils is removed in React 19, so your tests break. Use `act` from `react` & `fireEvent` / `render` from `@testing-library/react` instead"
		});
	}
};
const noReactDomDeprecatedApis = defineRule({
	id: "no-react-dom-deprecated-apis",
	title: "Deprecated react-dom APIs",
	requires: ["react:18"],
	tags: ["test-noise", "migration-hint"],
	severity: "warn",
	recommendation: "Switch the old `react-dom` root API (`render` / `hydrate` / `unmountComponentAtNode`) to `createRoot` / `hydrateRoot` / `root.unmount()` from `react-dom/client`. Replace `findDOMNode` with a ref. `react-dom/test-utils` is gone in React 19, so use `act` from `react` and `fireEvent` / `render` from `@testing-library/react`. Only runs on React 18+ projects.",
	...createDeprecatedReactImportRule({
		source: "react-dom",
		messages: REACT_DOM_DEPRECATED_MESSAGES,
		handleExtraSource: (node, context) => {
			if (node.source?.value !== "react-dom/test-utils") return false;
			reportTestUtilsImports(node, context);
			return true;
		}
	})
});
const noReact19DeprecatedApis = defineRule({
	id: "no-react19-deprecated-apis",
	title: "Deprecated React 19 APIs",
	requires: ["react:19"],
	tags: ["test-noise", "migration-hint"],
	severity: "warn",
	recommendation: "Pass `ref` as a normal prop on function components, since `forwardRef` isn't needed in React 19. Replace `useContext(X)` with `use(X)`. Only runs on React 19+ projects.",
	...createDeprecatedReactImportRule({
		source: "react",
		messages: new Map([["forwardRef", "forwardRef is dead weight in React 19, since ref is a normal prop now, so drop it & pass ref straight through."], ["useContext", "useContext is replaced by `use()` in React 19, which reads context inside ifs & loops too, so switch to `import { use } from 'react'`."]])
	})
});
//#endregion
//#region src/plugin/constants/aria-element-roles.ts
const ELEMENT_ROLE_PAIRS = [
	["a", "link"],
	["address", "group"],
	["area", "link"],
	["article", "article"],
	["aside", "complementary"],
	["blockquote", "blockquote"],
	["button", "button"],
	["caption", "caption"],
	["code", "code"],
	["datalist", "listbox"],
	["del", "deletion"],
	["details", "group"],
	["dfn", "term"],
	["dialog", "dialog"],
	["em", "emphasis"],
	["fieldset", "group"],
	["figure", "figure"],
	["footer", "contentinfo"],
	["form", "form"],
	["h1", "heading"],
	["h2", "heading"],
	["h3", "heading"],
	["h4", "heading"],
	["h5", "heading"],
	["h6", "heading"],
	["header", "banner"],
	["hgroup", "group"],
	["hr", "separator"],
	["img", "img"],
	["img", "image"],
	["input", "checkbox"],
	["input", "combobox"],
	["input", "radio"],
	["input", "searchbox"],
	["input", "slider"],
	["input", "spinbutton"],
	["input", "textbox"],
	["ins", "insertion"],
	["li", "listitem"],
	["main", "main"],
	["math", "math"],
	["menu", "list"],
	["meter", "meter"],
	["nav", "navigation"],
	["ol", "list"],
	["optgroup", "group"],
	["option", "option"],
	["output", "status"],
	["p", "paragraph"],
	["progress", "progressbar"],
	["s", "deletion"],
	["search", "search"],
	["section", "region"],
	["select", "combobox"],
	["select", "listbox"],
	["strong", "strong"],
	["sub", "subscript"],
	["sup", "superscript"],
	["svg", "graphics-document"],
	["table", "table"],
	["tbody", "rowgroup"],
	["td", "cell"],
	["td", "gridcell"],
	["textarea", "textbox"],
	["tfoot", "rowgroup"],
	["th", "columnheader"],
	["th", "rowheader"],
	["th", "gridcell"],
	["thead", "rowgroup"],
	["time", "time"],
	["tr", "row"],
	["ul", "list"]
];
const getElementImplicitRoles = (tag) => {
	const out = [];
	for (const [element, role] of ELEMENT_ROLE_PAIRS) if (element === tag && !out.includes(role)) out.push(role);
	return out;
};
const getTagsForRole = (role) => {
	const out = [];
	for (const [element, r] of ELEMENT_ROLE_PAIRS) if (r === role && !out.includes(element)) out.push(element);
	return out;
};
//#endregion
//#region src/plugin/rules/a11y/no-redundant-roles.ts
const buildMessage$9 = (tag, role) => `Screen reader users gain nothing from this \`role\` because \`<${tag}>\` already acts as a \`${role}\`, so remove it.`;
const resolveSettings$13 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { exceptions: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noRedundantRoles ?? {} : {}).exceptions ?? {} };
};
const noRedundantRoles = defineRule({
	id: "no-redundant-roles",
	title: "Redundant ARIA role",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Remove `role` attributes that match what the element already does.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$13(context.settings);
		return { JSXOpeningElement(node) {
			const roleAttr = hasJsxPropIgnoreCase(node.attributes, "role");
			if (!roleAttr) return;
			const role = getJsxPropStringValue(roleAttr);
			if (role === null) return;
			const tag = getElementType(node, context.settings);
			const implicitRoles = getElementImplicitRoles(tag);
			const allowedHere = settings.exceptions[tag] ?? [];
			if (implicitRoles.includes(role) && !allowedHere.includes(role)) context.report({
				node: roleAttr,
				message: buildMessage$9(tag, role)
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/no-redundant-should-component-update.ts
const buildMessage$8 = (className) => `\`shouldComponentUpdate\` fights PureComponent's built-in check in \`${className}\` & can skip needed updates.`;
const isPureComponentSuper = (superClass) => {
	if (!superClass) return false;
	if (isNodeOfType(superClass, "Identifier")) return superClass.name === "PureComponent";
	if (isNodeOfType(superClass, "MemberExpression")) {
		if (!isNodeOfType(superClass.object, "Identifier")) return false;
		if (superClass.object.name !== "React") return false;
		return isNodeOfType(superClass.property, "Identifier") && superClass.property.name === "PureComponent";
	}
	return false;
};
const findShouldComponentUpdate = (classBody) => {
	for (const member of classBody.body) if (isNodeOfType(member, "MethodDefinition") || isNodeOfType(member, "PropertyDefinition")) {
		const key = member.key;
		if (isNodeOfType(key, "Identifier") && key.name === "shouldComponentUpdate") return key;
		if (isNodeOfType(key, "Literal") && key.value === "shouldComponentUpdate") return key;
	}
	return null;
};
const noRedundantShouldComponentUpdate = defineRule({
	id: "no-redundant-should-component-update",
	title: "Redundant shouldComponentUpdate",
	severity: "warn",
	recommendation: "Drop `shouldComponentUpdate` (PureComponent already shallow-compares) or extend `React.Component` if custom logic is needed.",
	category: "Architecture",
	create: (context) => {
		const checkClass = (classNode) => {
			if (!isPureComponentSuper(classNode.superClass ?? null)) return;
			const reportNode = findShouldComponentUpdate(classNode.body);
			if (!reportNode) return;
			const className = classNode.id?.name ?? "<anonymous class>";
			context.report({
				node: reportNode,
				message: buildMessage$8(className)
			});
		};
		return {
			ClassDeclaration: checkClass,
			ClassExpression: checkClass
		};
	}
});
//#endregion
//#region src/plugin/rules/architecture/no-render-in-render.ts
const noRenderInRender = defineRule({
	id: "no-render-in-render",
	title: "Component rendered by inline function call",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Make it a named component, like `const ListItem = ({ item }) => <div>{item.name}</div>`.",
	create: (context) => ({ JSXExpressionContainer(node) {
		const expression = node.expression;
		if (!isNodeOfType(expression, "CallExpression")) return;
		let calleeName = null;
		if (isNodeOfType(expression.callee, "Identifier")) calleeName = expression.callee.name;
		else if (isNodeOfType(expression.callee, "MemberExpression") && isNodeOfType(expression.callee.property, "Identifier")) calleeName = expression.callee.property.name;
		if (!calleeName || !RENDER_FUNCTION_PATTERN.test(calleeName)) return;
		context.report({
			node: expression,
			message: `Your users lose state because "${calleeName}()" builds UI from an inline call that React remounts, so pull it into its own component instead.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/architecture/no-render-prop-children.ts
const RENDER_PROP_PATTERN = /^render[A-Z]/;
const noRenderPropChildren = defineRule({
	id: "no-render-prop-children",
	title: "Too many render props",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Swap `renderXxx` props for child components like `<Modal.Header>` or plain `children`, so the parent doesn't control every slot.",
	create: (context) => ({ JSXOpeningElement(node) {
		const renderPropAttrs = [];
		for (const attr of node.attributes ?? []) {
			if (!isNodeOfType(attr, "JSXAttribute")) continue;
			if (!isNodeOfType(attr.name, "JSXIdentifier")) continue;
			const name = attr.name.name;
			if (!RENDER_PROP_PATTERN.test(name)) continue;
			renderPropAttrs.push({
				name,
				node: attr
			});
		}
		if (renderPropAttrs.length < 3) return;
		const propList = renderPropAttrs.slice(0, 3).map((entry) => entry.name).join(", ");
		context.report({
			node: renderPropAttrs[0].node,
			message: `This element takes ${renderPropAttrs.length} render props (${propList}…), which is hard to follow & extend. Use child components or \`children\` so callers don't wire up every slot.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-render-return-value.ts
const MESSAGE$13 = "Your app breaks in React 19 because `ReactDOM.render` returns nothing there.";
const isReactDomRenderCall = (node) => {
	if (!isNodeOfType(node.callee, "MemberExpression")) return false;
	if (!isNodeOfType(node.callee.object, "Identifier")) return false;
	if (node.callee.object.name !== "ReactDOM") return false;
	if (!isNodeOfType(node.callee.property, "Identifier")) return false;
	return node.callee.property.name === "render";
};
const isUsedAsReturnValue = (parent) => {
	if (!parent) return false;
	if (isNodeOfType(parent, "VariableDeclarator") || isNodeOfType(parent, "Property") || isNodeOfType(parent, "ReturnStatement") || isNodeOfType(parent, "AssignmentExpression")) return true;
	if (isNodeOfType(parent, "ArrowFunctionExpression") && parent.body && !isNodeOfType(parent.body, "BlockStatement")) return true;
	return false;
};
const noRenderReturnValue = defineRule({
	id: "no-render-return-value",
	title: "Using ReactDOM.render return value",
	severity: "warn",
	recommendation: "Don't use `ReactDOM.render`'s return value. It's legacy and was removed in React 19.",
	create: (context) => ({ CallExpression(node) {
		if (!isReactDomRenderCall(node)) return;
		if (!isUsedAsReturnValue(node.parent)) return;
		context.report({
			node: node.callee,
			message: MESSAGE$13
		});
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-reset-all-state-on-prop-change.ts
const isUndefinedNode = (node) => {
	if (node === null || node === void 0) return true;
	return isNodeOfType(node, "Identifier") && node.name === "undefined";
};
const getNodeText = (node) => {
	if (!node) return "";
	return JSON.stringify(node, (key, value) => {
		if (key === "parent" || key === "loc" || key === "range" || key === "start" || key === "end") return;
		return value;
	});
};
const isSetStateToInitialValue = (analysis, setterRef) => {
	const callExpr = getCallExpr(setterRef);
	if (!callExpr || !isNodeOfType(callExpr, "CallExpression")) return false;
	const setStateToValue = callExpr.arguments?.[0];
	const useStateDecl = getUseStateDecl(analysis, setterRef);
	if (!useStateDecl || !isNodeOfType(useStateDecl, "VariableDeclarator")) return false;
	if (!isNodeOfType(useStateDecl.init, "CallExpression")) return false;
	const stateInitialValue = useStateDecl.init.arguments?.[0];
	if (isUndefinedNode(setStateToValue) && isUndefinedNode(stateInitialValue)) return true;
	if (setStateToValue == null && stateInitialValue == null) return true;
	if (setStateToValue && !stateInitialValue || !setStateToValue && stateInitialValue) return false;
	return getNodeText(setStateToValue) === getNodeText(stateInitialValue);
};
const countUseStates = (analysis, componentNode) => {
	if (!componentNode) return 0;
	const stateVariables = /* @__PURE__ */ new Set();
	for (const ref of getDownstreamRefs(analysis, componentNode)) if (isState(analysis, ref)) stateVariables.add(ref.resolved);
	return stateVariables.size;
};
const findPropUsedToResetAllState = (analysis, effectFnRefs, depsRefs, useEffectNode) => {
	const stateSetterRefs = effectFnRefs.filter((ref) => isStateSetterCall(analysis, ref));
	if (stateSetterRefs.length === 0) return null;
	if (!stateSetterRefs.every((ref) => isSetStateToInitialValue(analysis, ref))) return null;
	const containing = findContainingNode(analysis, useEffectNode);
	if (stateSetterRefs.length !== countUseStates(analysis, containing)) return null;
	for (const depRef of depsRefs) for (const upRef of getUpstreamRefs(analysis, depRef)) if (isProp(analysis, upRef)) return upRef;
	return null;
};
const noResetAllStateOnPropChange = defineRule({
	id: "no-reset-all-state-on-prop-change",
	title: "All state reset on prop change",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Pass the prop as `key` so React resets the component for you when the prop changes, instead of clearing every state value by hand in a useEffect. See https://react.dev/learn/you-might-not-need-an-effect#resetting-all-state-when-a-prop-changes",
	create: (context) => ({ CallExpression(node) {
		if (!isUseEffect(node)) return;
		const analysis = getProgramAnalysis(node);
		if (!analysis) return;
		const effectFnRefs = getEffectFnRefs(analysis, node);
		const depsRefs = getEffectDepsRefs(analysis, node);
		if (!effectFnRefs || !depsRefs) return;
		const containing = findContainingNode(analysis, node);
		if (containing && isCustomHook(containing)) return;
		if (!findPropUsedToResetAllState(analysis, effectFnRefs, depsRefs, node)) return;
		context.report({
			node,
			message: `Your users briefly see stale state when a prop changes because this useEffect clears all state.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/performance/no-scale-from-zero.ts
const noScaleFromZero = defineRule({
	id: "no-scale-from-zero",
	title: "Animating scale from zero",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use `initial={{ scale: 0.95, opacity: 0 }}`. Elements should gently shrink and fade, not vanish into a point",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		if (node.name.name !== "initial" && node.name.name !== "exit") return;
		if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const expression = node.value.expression;
		if (!isNodeOfType(expression, "ObjectExpression")) return;
		for (const property of expression.properties ?? []) {
			if (!isNodeOfType(property, "Property")) continue;
			if ((isNodeOfType(property.key, "Identifier") ? property.key.name : null) !== "scale") continue;
			if (isNodeOfType(property.value, "Literal") && property.value.value === 0) context.report({
				node: property,
				message: "This looks abrupt to your users because scale: 0 pops the element in from a single point, so use scale: 0.95 with opacity: 0 for a smoother entrance"
			});
		}
	} })
});
//#endregion
//#region src/plugin/constants/security.ts
const AUTH_FUNCTION_NAMES = new Set([
	"auth",
	"getSession",
	"getServerSession",
	"getUser",
	"requireAuth",
	"checkAuth",
	"verifyAuth",
	"authenticate",
	"currentUser",
	"getAuth",
	"validateSession"
]);
const GENERIC_AUTH_METHOD_NAMES = new Set(["getUser"]);
const AUTH_OBJECT_PATTERN = /(?:^|[._])(?:auth|authn|authz|clerk|session|jwt|firebase|supabase|nextauth|kinde|workos|stytch|descope|cognito|propelauth|lucia)/i;
const SECRET_PATTERNS = [
	/^sk_live_/,
	/^sk_test_/,
	/^AKIA[0-9A-Z]{16}$/,
	/^ghp_[a-zA-Z0-9]{36}$/,
	/^gho_[a-zA-Z0-9]{36}$/,
	/^github_pat_/,
	/^glpat-/,
	/^xox[bporas]-/,
	/^sk-[a-zA-Z0-9]{32,}$/
];
const PUBLIC_CLIENT_KEY_PATTERNS = [
	/^appl_/,
	/^goog_/,
	/^amzn_/,
	/^strp_/,
	/^pk_(?:live|test)_/,
	/^sb_publishable_/,
	/^phc_/,
	/^public-token-(?:live|test)-/,
	/^pk\.eyJ/
];
const SECRET_VARIABLE_PATTERN = /(?:api_?key|secret|token|password|credential|auth)/i;
const SECRET_TOOLING_FILE_PATTERN = /(?:^|\/)[^/]+\.config\.[cm]?[jt]s$/;
const SECRET_TOOLING_RC_FILE_PATTERN = /(?:^|\/)(?:\.[a-z-]+rc|[a-z-]+\.rc)\.[cm]?[jt]s$/;
const SECRET_TEST_FILE_PATTERN = /(?:^|\/)[^/]+\.(?:test|spec|stories|story|fixture|fixtures)\.[cm]?[jt]sx?$/;
const SECRET_SERVER_FILE_SUFFIX_PATTERN = /(?:^|\/)[^/]+\.server\.[cm]?[jt]sx?$/;
const SECRET_SERVER_ENTRY_FILE_PATTERN = /(?:^|\/)(?:middleware|route)\.[cm]?[jt]sx?$/;
const SECRET_NEXT_PAGES_API_FILE_PATTERN = /(?:^|\/)pages\/api\/.+\.[cm]?[jt]sx?$/;
const SECRET_CLIENT_FILE_SUFFIX_PATTERN = /(?:^|\/)[^/]+\.(?:client|browser|web)\.[cm]?[jt]sx?$/;
const SECRET_CLIENT_ENTRY_FILE_PATTERN = /(?:^|\/)(?:src\/)?(?:main|index|[Aa]pp|client)\.[cm]?[jt]sx?$/;
const SECRET_SERVER_DIRECTORY_NAMES = new Set([
	"backend",
	"functions",
	"lambdas",
	"lambda",
	"middleware",
	"server",
	"servers"
]);
const SECRET_SERVER_SOURCE_ROOT_OWNER_NAMES = new Set([
	"api",
	"backend",
	"edge",
	"function",
	"functions",
	"lambda",
	"lambdas",
	"server",
	"servers",
	"worker",
	"workers"
]);
const SECRET_TEST_DIRECTORY_NAMES = new Set([
	"__fixtures__",
	"__mocks__",
	"__tests__",
	"fixtures",
	"mocks",
	"test",
	"tests"
]);
const SECRET_TOOLING_DIRECTORY_NAMES = new Set([
	"bin",
	"config",
	"configs",
	"script",
	"scripts",
	"tooling",
	"tools"
]);
const SECRET_CLIENT_SOURCE_DIRECTORY_NAMES = new Set([
	"components",
	"features",
	"hooks",
	"pages",
	"ui",
	"views",
	"widgets"
]);
const SECRET_FALSE_POSITIVE_SUFFIXES = new Set([
	"modal",
	"label",
	"text",
	"title",
	"name",
	"id",
	"url",
	"path",
	"route",
	"page",
	"param",
	"field",
	"column",
	"header",
	"placeholder",
	"prefix",
	"description",
	"type",
	"icon",
	"class",
	"style",
	"variant",
	"event",
	"action",
	"status",
	"state",
	"mode",
	"flag",
	"option",
	"config",
	"message",
	"error",
	"display",
	"view",
	"component",
	"element",
	"container",
	"wrapper",
	"button",
	"link",
	"input",
	"select",
	"dialog",
	"menu",
	"form",
	"step",
	"index",
	"count",
	"length",
	"role",
	"scope",
	"context",
	"provider",
	"ref",
	"handler",
	"query",
	"schema",
	"constant"
]);
//#endregion
//#region src/plugin/utils/classify-secret-file-exposure.ts
const SOURCE_FILE_EXTENSION_PATTERN = /\.[cm]?[jt]sx?$/;
const CLIENT_SOURCE_FILE_EXTENSION_PATTERN = /\.[cm]?[jt]sx$/;
const CLIENT_APP_DIRECTORY_FRAMEWORKS = new Set([
	"cra",
	"expo",
	"gatsby",
	"vite"
]);
const isInsideDirectory = (pathSegments, directoryNames) => pathSegments.some((pathSegment) => directoryNames.has(pathSegment));
const normalizeFilename = (filename) => filename.replaceAll("\\", "/");
const normalizeDirectory = (directory) => normalizeFilename(directory).replace(/\/+$/, "");
const getProjectRelativeFilename = (filename, rootDirectory) => {
	const normalizedFilename = normalizeFilename(filename);
	if (!rootDirectory) return normalizedFilename;
	const rootDirectoryPrefix = `${normalizeDirectory(rootDirectory)}/`;
	if (!normalizedFilename.startsWith(rootDirectoryPrefix)) return normalizedFilename;
	return normalizedFilename.slice(rootDirectoryPrefix.length);
};
const getClassifiablePathSegments = (pathSegments) => {
	const srcIndex = pathSegments.lastIndexOf("src");
	if (srcIndex === -1) return pathSegments;
	return pathSegments.slice(srcIndex + 1);
};
const isClientSourceFile = (normalizedFilename, pathSegments, classifiablePathSegments, options) => {
	if (!SOURCE_FILE_EXTENSION_PATTERN.test(normalizedFilename)) return false;
	if (!pathSegments.includes("src")) return false;
	if (classifiablePathSegments[0] === "app" && !CLIENT_APP_DIRECTORY_FRAMEWORKS.has(options.framework ?? "")) return false;
	if (CLIENT_SOURCE_FILE_EXTENSION_PATTERN.test(normalizedFilename)) return true;
	return classifiablePathSegments.some((pathSegment) => SECRET_CLIENT_SOURCE_DIRECTORY_NAMES.has(pathSegment));
};
const getSourceRootOwner = (pathSegments) => {
	const srcIndex = pathSegments.lastIndexOf("src");
	if (srcIndex <= 0) return null;
	return pathSegments[srcIndex - 1];
};
const isAppDirectoryClientSourceFile = (normalizedFilename, classifiablePathSegments, options) => {
	if (!SOURCE_FILE_EXTENSION_PATTERN.test(normalizedFilename)) return false;
	if (!CLIENT_APP_DIRECTORY_FRAMEWORKS.has(options.framework ?? "")) return false;
	return classifiablePathSegments.includes("app");
};
const isNextJsFramework = (options) => options.framework === "nextjs";
const classifySecretFileExposure = (filename, options = {}) => {
	if (filename.length === 0) return "unknown";
	const normalizedFilename = getProjectRelativeFilename(filename, options.rootDirectory);
	const pathSegments = normalizedFilename.split("/");
	const classifiablePathSegments = getClassifiablePathSegments(pathSegments);
	const sourceRootOwner = getSourceRootOwner(pathSegments);
	if (SECRET_TEST_FILE_PATTERN.test(normalizedFilename)) return "test";
	if (isInsideDirectory(classifiablePathSegments, SECRET_TEST_DIRECTORY_NAMES)) return "test";
	if (SECRET_TOOLING_FILE_PATTERN.test(normalizedFilename)) return "tooling";
	if (SECRET_TOOLING_RC_FILE_PATTERN.test(normalizedFilename)) return "tooling";
	if (sourceRootOwner && SECRET_TOOLING_DIRECTORY_NAMES.has(sourceRootOwner)) return "tooling";
	if (isInsideDirectory(classifiablePathSegments, SECRET_TOOLING_DIRECTORY_NAMES)) return "tooling";
	if (SECRET_SERVER_FILE_SUFFIX_PATTERN.test(normalizedFilename)) return "server";
	if (options.hasUseServerDirective === true) return "server";
	if (options.hasUseClientDirective === true) return "client";
	if (SECRET_CLIENT_FILE_SUFFIX_PATTERN.test(normalizedFilename)) return "client";
	if (isNextJsFramework(options) && SECRET_SERVER_ENTRY_FILE_PATTERN.test(normalizedFilename)) return "server";
	if (isNextJsFramework(options) && SECRET_NEXT_PAGES_API_FILE_PATTERN.test(normalizedFilename)) return "server";
	if (sourceRootOwner && SECRET_SERVER_SOURCE_ROOT_OWNER_NAMES.has(sourceRootOwner)) return "server";
	if (isInsideDirectory(classifiablePathSegments, SECRET_SERVER_DIRECTORY_NAMES)) return "server";
	if (SECRET_CLIENT_ENTRY_FILE_PATTERN.test(normalizedFilename)) return "client";
	if (isAppDirectoryClientSourceFile(normalizedFilename, classifiablePathSegments, options)) return "client";
	if (isClientSourceFile(normalizedFilename, pathSegments, classifiablePathSegments, options)) return "client";
	return "unknown";
};
//#endregion
//#region src/plugin/utils/get-identifier-trailing-word.ts
const getIdentifierTrailingWord = (identifierName) => {
	return identifierName.match(/[A-Z]+(?=[A-Z][a-z]|\b)|[A-Z]?[a-z]+|\d+/g)?.at(-1)?.toLowerCase() ?? identifierName.toLowerCase();
};
//#endregion
//#region src/plugin/constants/tanstack.ts
const TANSTACK_ROUTE_FILE_PATTERN = /\/routes\//;
const TANSTACK_ROOT_ROUTE_FILE_PATTERN = /__root\.(tsx?|jsx?)$/;
const TANSTACK_ROUTE_PROPERTY_ORDER = [
	"params",
	"validateSearch",
	"loaderDeps",
	"search.middlewares",
	"ssr",
	"context",
	"beforeLoad",
	"loader",
	"onEnter",
	"onStay",
	"onLeave",
	"head",
	"scripts",
	"headers",
	"remountDeps"
];
const TANSTACK_ROUTE_CREATION_FUNCTIONS = new Set([
	"createFileRoute",
	"createRoute",
	"createRootRoute",
	"createRootRouteWithContext"
]);
const TANSTACK_SERVER_FN_NAMES = new Set(["createServerFn"]);
const TANSTACK_MIDDLEWARE_METHOD_ORDER = [
	"middleware",
	"inputValidator",
	"client",
	"server",
	"handler"
];
const TANSTACK_REDIRECT_FUNCTIONS = new Set(["redirect", "notFound"]);
const TANSTACK_SERVER_FN_FILE_PATTERN = /\.functions(\.[jt]sx?)?$/;
const TANSTACK_QUERY_HOOKS = new Set([
	"useQuery",
	"useInfiniteQuery",
	"useSuspenseQuery",
	"useSuspenseInfiniteQuery"
]);
const TANSTACK_MUTATION_HOOKS = new Set(["useMutation"]);
const QUERY_CACHE_UPDATE_METHODS = new Set([
	"invalidateQueries",
	"setQueryData",
	"setQueriesData",
	"resetQueries",
	"refetchQueries",
	"removeQueries",
	"cancelQueries",
	"clear"
]);
//#endregion
//#region src/plugin/utils/has-use-server-directive.ts
const hasUseServerDirective = (node) => {
	if (!isNodeOfType(node, "FunctionDeclaration") && !isNodeOfType(node, "FunctionExpression") && !isNodeOfType(node, "ArrowFunctionExpression")) return false;
	if (!isNodeOfType(node.body, "BlockStatement")) return false;
	return Boolean(node.body.body?.some((statement) => isNodeOfType(statement, "ExpressionStatement") && statement.directive === "use server"));
};
//#endregion
//#region src/plugin/utils/is-inside-server-only-scope.ts
const isTanStackServerFnHandlerCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (!isNodeOfType(node.callee, "MemberExpression")) return false;
	if (!isNodeOfType(node.callee.property, "Identifier")) return false;
	if (node.callee.property.name !== "handler") return false;
	let currentNode = node.callee.object;
	while (isNodeOfType(currentNode, "CallExpression")) {
		const calleeName = getCalleeName$1(currentNode);
		if (calleeName && TANSTACK_SERVER_FN_NAMES.has(calleeName)) return true;
		if (!isNodeOfType(currentNode.callee, "MemberExpression")) return false;
		currentNode = currentNode.callee.object;
	}
	return false;
};
const isTanStackServerFnHandler = (node) => {
	const parent = node.parent;
	if (!parent || !isNodeOfType(parent, "CallExpression")) return false;
	if (!parent.arguments?.some((argument) => argument === node)) return false;
	return isTanStackServerFnHandlerCall(parent);
};
const isInsideServerOnlyScope = (node) => {
	let currentNode = node.parent ?? null;
	while (currentNode) {
		if (isFunctionLike$2(currentNode)) {
			if (hasUseServerDirective(currentNode) || isTanStackServerFnHandler(currentNode)) return true;
		}
		currentNode = currentNode.parent ?? null;
	}
	return false;
};
//#endregion
//#region src/plugin/rules/security/no-secrets-in-client-code.ts
const noSecretsInClientCode = defineRule({
	id: "no-secrets-in-client-code",
	title: "Secret in client code",
	severity: "warn",
	recommendation: "Move secrets to server-only code. Anything in client env variables gets shipped to the browser, so it can't hold secrets.",
	create: (context) => {
		const filename = normalizeFilename$1(context.filename ?? "");
		const framework = getReactDoctorStringSetting(context.settings, "framework");
		const rootDirectory = getReactDoctorStringSetting(context.settings, "rootDirectory");
		let shouldUseVariableNameHeuristic = classifySecretFileExposure(filename, {
			framework,
			rootDirectory
		}) === "client";
		return {
			Program(programNode) {
				shouldUseVariableNameHeuristic = classifySecretFileExposure(filename, {
					framework,
					hasUseClientDirective: hasDirective(programNode, "use client"),
					hasUseServerDirective: hasDirective(programNode, "use server"),
					rootDirectory
				}) === "client";
			},
			VariableDeclarator(node) {
				if (!isNodeOfType(node.id, "Identifier")) return;
				if (!isNodeOfType(node.init, "Literal") || typeof node.init.value !== "string") return;
				const variableName = node.id.name;
				const literalValue = node.init.value;
				if (PUBLIC_CLIENT_KEY_PATTERNS.some((pattern) => pattern.test(literalValue))) return;
				const isServerOnlyScope = isInsideServerOnlyScope(node);
				const trailingSuffix = getIdentifierTrailingWord(variableName);
				const isUiConstant = SECRET_FALSE_POSITIVE_SUFFIXES.has(trailingSuffix);
				if (shouldUseVariableNameHeuristic && !isServerOnlyScope && SECRET_VARIABLE_PATTERN.test(variableName) && !isUiConstant && literalValue.length > 24) {
					context.report({
						node,
						message: `Hardcoding "${variableName}" in client code is a security vulnerability: the secret ships to the browser where anyone can read it.`
					});
					return;
				}
				if (SECRET_PATTERNS.some((pattern) => pattern.test(literalValue))) context.report({
					node,
					message: "This hardcoded secret is a security vulnerability: it ships to the browser where anyone can read it."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-self-updating-effect.ts
const doesConstructFreshReference = (node) => isNodeOfType(node, "ArrayExpression") || isNodeOfType(node, "ObjectExpression") || isNodeOfType(node, "NewExpression") || isNodeOfType(node, "Literal") && "regex" in node;
const expressionReadsStateValue = (node, stateName) => {
	if (isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression")) return false;
	if (isNodeOfType(node, "Identifier")) return node.name === stateName;
	if (isNodeOfType(node, "MemberExpression")) {
		if (expressionReadsStateValue(node.object, stateName)) return true;
		return node.computed ? expressionReadsStateValue(node.property, stateName) : false;
	}
	if (isNodeOfType(node, "Property")) {
		if (node.computed && expressionReadsStateValue(node.key, stateName)) return true;
		return expressionReadsStateValue(node.value, stateName);
	}
	const nodeRecord = node;
	for (const childKey of Object.keys(nodeRecord)) {
		if (childKey === "parent" || childKey === "type") continue;
		const childValue = nodeRecord[childKey];
		if (Array.isArray(childValue)) {
			for (const childArrayItem of childValue) if (isAstNode(childArrayItem) && expressionReadsStateValue(childArrayItem, stateName)) return true;
		} else if (isAstNode(childValue) && expressionReadsStateValue(childValue, stateName)) return true;
	}
	return false;
};
const isNonSettlingSetterArgument = (setterCall, stateName) => {
	const firstArgument = setterCall.arguments?.[0];
	if (!firstArgument) return false;
	const argument = stripParenExpression(firstArgument);
	if (isNodeOfType(argument, "Identifier") && argument.name === stateName) return false;
	if (isNodeOfType(argument, "ArrowFunctionExpression") || isNodeOfType(argument, "FunctionExpression")) return true;
	if (doesConstructFreshReference(argument)) return true;
	return expressionReadsStateValue(argument, stateName);
};
const getUnconditionalSetterCall = (statement, setterNames) => {
	const expression = stripParenExpression(isNodeOfType(statement, "ExpressionStatement") ? statement.expression : statement);
	if (!isNodeOfType(expression, "CallExpression")) return null;
	if (!isNodeOfType(expression.callee, "Identifier")) return null;
	if (!setterNames.has(expression.callee.name)) return null;
	return expression;
};
const collectDependencyStateNames = (depsNode) => {
	const dependencyNames = /* @__PURE__ */ new Set();
	if (!isNodeOfType(depsNode, "ArrayExpression")) return dependencyNames;
	for (const element of depsNode.elements ?? []) if (isNodeOfType(element, "Identifier")) dependencyNames.add(element.name);
	return dependencyNames;
};
const isEarlyReturnGuard = (statement) => {
	if (!isNodeOfType(statement, "IfStatement")) return false;
	const consequent = statement.consequent;
	if (isNodeOfType(consequent, "ReturnStatement")) return true;
	if (isNodeOfType(consequent, "BlockStatement")) return (consequent.body ?? []).some((inner) => isNodeOfType(inner, "ReturnStatement"));
	return false;
};
const numericLiteralValue = (node) => {
	if (isNodeOfType(node, "Literal") && typeof node.value === "number") return node.value;
	if (isNodeOfType(node, "UnaryExpression") && node.operator === "-" && isNodeOfType(node.argument, "Literal") && typeof node.argument.value === "number") return -node.argument.value;
	return null;
};
const isStateLength = (node, stateName) => {
	const member = isNodeOfType(node, "ChainExpression") ? node.expression : node;
	return isNodeOfType(member, "MemberExpression") && !member.computed && isNodeOfType(member.property, "Identifier") && member.property.name === "length" && expressionReadsStateValue(member.object, stateName);
};
const isNullishLiteral = (node) => isNodeOfType(node, "Literal") && node.value === null || isNodeOfType(node, "Identifier") && node.name === "undefined";
const numericComparisonHolds = (operator, left, right) => {
	switch (operator) {
		case "<": return left < right;
		case "<=": return left <= right;
		case ">": return left > right;
		case ">=": return left >= right;
		case "===":
		case "==": return left === right;
		case "!==":
		case "!=": return left !== right;
		default: return false;
	}
};
const guardExitsWhenStateEmpty = (test, stateName) => {
	const node = isNodeOfType(test, "ChainExpression") ? test.expression : test;
	if (isNodeOfType(node, "UnaryExpression") && node.operator === "!") return isStateLength(node.argument, stateName);
	if (isNodeOfType(node, "LogicalExpression")) {
		if (node.operator === "||") return guardExitsWhenStateEmpty(node.left, stateName) || guardExitsWhenStateEmpty(node.right, stateName);
		if (node.operator === "&&") return guardExitsWhenStateEmpty(node.left, stateName) && guardExitsWhenStateEmpty(node.right, stateName);
		return false;
	}
	if (isNodeOfType(node, "BinaryExpression")) {
		const leftIsLength = isStateLength(node.left, stateName);
		const rightIsLength = isStateLength(node.right, stateName);
		if (leftIsLength || rightIsLength) {
			const other = numericLiteralValue(leftIsLength ? node.right : node.left);
			if (other === null) return false;
			return leftIsLength ? numericComparisonHolds(node.operator, 0, other) : numericComparisonHolds(node.operator, other, 0);
		}
		if (node.operator === "==" || node.operator === "===") {
			if (expressionReadsStateValue(node.left, stateName) && isNullishLiteral(node.right)) return true;
			if (expressionReadsStateValue(node.right, stateName) && isNullishLiteral(node.left)) return true;
		}
		return false;
	}
	return false;
};
const isEmptyOrFalsyValue = (node) => {
	if (isNodeOfType(node, "ArrayExpression")) return (node.elements ?? []).length === 0;
	if (isNodeOfType(node, "ObjectExpression")) return (node.properties ?? []).length === 0;
	if (isNodeOfType(node, "Literal")) return node.value === null || node.value === "" || node.value === 0 || node.value === false;
	if (isNodeOfType(node, "Identifier")) return node.name === "undefined";
	return false;
};
const functionReturnExpression = (fn) => {
	if (!isNodeOfType(fn, "ArrowFunctionExpression") && !isNodeOfType(fn, "FunctionExpression")) return null;
	if (!isNodeOfType(fn.body, "BlockStatement")) return fn.body ? stripParenExpression(fn.body) : null;
	for (const statement of fn.body.body ?? []) if (isNodeOfType(statement, "ReturnStatement") && statement.argument) return stripParenExpression(statement.argument);
	return null;
};
const isLengthReducingUpdater = (node) => {
	if (!isNodeOfType(node, "ArrowFunctionExpression") && !isNodeOfType(node, "FunctionExpression")) return false;
	const firstParameter = node.params?.[0];
	if (!firstParameter || !isNodeOfType(firstParameter, "Identifier")) return false;
	const returned = functionReturnExpression(node);
	if (!returned || !isNodeOfType(returned, "CallExpression")) return false;
	const callee = returned.callee;
	if (!isNodeOfType(callee, "MemberExpression") || callee.computed) return false;
	if (!isNodeOfType(callee.object, "Identifier") || callee.object.name !== firstParameter.name) return false;
	if (!isNodeOfType(callee.property, "Identifier") || callee.property.name !== "slice") return false;
	const sliceStart = numericLiteralValue(returned.arguments?.[0]);
	return sliceStart !== null && sliceStart >= 1;
};
const writeProvablyConverges = (setterArgument, stateName, earlyReturnGuardTests) => {
	if (!isEmptyOrFalsyValue(setterArgument) && !isLengthReducingUpdater(setterArgument)) return false;
	return earlyReturnGuardTests.some((test) => guardExitsWhenStateEmpty(test, stateName));
};
const SYMBOLIC_DEPTH_LIMIT = 16;
const unwrapChain = (node) => {
	let current = node;
	for (;;) {
		const withoutParens = stripParenExpression(current);
		if (withoutParens !== current) {
			current = withoutParens;
			continue;
		}
		if (isNodeOfType(current, "ChainExpression")) {
			current = current.expression;
			continue;
		}
		return current;
	}
};
const isUndefinedValue = (node) => isNodeOfType(node, "Identifier") && node.name === "undefined" || isNodeOfType(node, "Literal") && node.value === null;
const literalsEqual = (a, b) => isNodeOfType(a, "Literal") && isNodeOfType(b, "Literal") && a.value === b.value;
const resolveValueNode = (node, writes, depth, seen) => {
	if (depth > SYMBOLIC_DEPTH_LIMIT) return null;
	const current = unwrapChain(node);
	if (isNodeOfType(current, "Identifier")) {
		if (seen.has(current.name)) return null;
		const written = writes.get(current.name);
		if (written) return resolveValueNode(written, writes, depth + 1, new Set(seen).add(current.name));
		return current;
	}
	if (isNodeOfType(current, "Literal") || isNodeOfType(current, "ArrayExpression") || isNodeOfType(current, "ObjectExpression")) return current;
	if (isNodeOfType(current, "MemberExpression") && !current.computed && isNodeOfType(current.property, "Identifier")) {
		const objectValue = resolveValueNode(current.object, writes, depth + 1, seen);
		if (objectValue && isNodeOfType(objectValue, "ObjectExpression")) {
			const propertyKey = current.property.name;
			const properties = objectValue.properties ?? [];
			for (let index = properties.length - 1; index >= 0; index--) {
				const property = properties[index];
				if (isNodeOfType(property, "SpreadElement")) return null;
				if (isNodeOfType(property, "Property") && !property.computed && (isNodeOfType(property.key, "Identifier") && property.key.name === propertyKey || isNodeOfType(property.key, "Literal") && property.key.value === propertyKey)) return resolveValueNode(property.value, writes, depth + 1, seen);
			}
		}
		return null;
	}
	return null;
};
const resolveToNumber = (node, writes, depth, seen) => {
	const value = resolveValueNode(node, writes, depth, seen);
	if (value && isNodeOfType(value, "Literal") && typeof value.value === "number") return value.value;
	const current = unwrapChain(node);
	if (isNodeOfType(current, "MemberExpression") && !current.computed && isNodeOfType(current.property, "Identifier") && current.property.name === "length") {
		const objectValue = resolveValueNode(current.object, writes, depth, seen);
		if (objectValue && isNodeOfType(objectValue, "ArrayExpression")) {
			const elements = objectValue.elements ?? [];
			if (!elements.some((element) => element && isNodeOfType(element, "SpreadElement"))) return elements.length;
		}
	}
	return null;
};
const provablyEqualAfterWrites = (left, right, writes, depth, seen) => {
	const leftNumber = resolveToNumber(left, writes, depth, seen);
	const rightNumber = resolveToNumber(right, writes, depth, seen);
	if (leftNumber !== null && rightNumber !== null) return leftNumber === rightNumber;
	const a = resolveValueNode(left, writes, depth, seen);
	const b = resolveValueNode(right, writes, depth, seen);
	if (!a || !b) return false;
	if (literalsEqual(a, b)) return true;
	if (isUndefinedValue(a) && isUndefinedValue(b)) return true;
	return isNodeOfType(a, "Identifier") && isNodeOfType(b, "Identifier") && a.name === b.name;
};
const provablyFalsyAfterWrites = (node, writes, depth, seen) => {
	const value = resolveValueNode(node, writes, depth, seen);
	if (value) {
		if (isUndefinedValue(value)) return true;
		if (isNodeOfType(value, "Literal")) return value.value === null || value.value === 0 || value.value === false || value.value === "";
	}
	return resolveToNumber(node, writes, depth, seen) === 0;
};
const guardProvenAfterWrites = (test, writes, depth, seen) => {
	if (depth > SYMBOLIC_DEPTH_LIMIT) return false;
	const node = unwrapChain(test);
	if (isNodeOfType(node, "LogicalExpression")) {
		if (node.operator === "&&") return guardProvenAfterWrites(node.left, writes, depth + 1, seen) && guardProvenAfterWrites(node.right, writes, depth + 1, seen);
		if (node.operator === "||") return guardProvenAfterWrites(node.left, writes, depth + 1, seen) || guardProvenAfterWrites(node.right, writes, depth + 1, seen);
		return false;
	}
	if (isNodeOfType(node, "UnaryExpression") && node.operator === "!") return provablyFalsyAfterWrites(node.argument, writes, depth + 1, seen);
	if (isNodeOfType(node, "BinaryExpression")) {
		if (node.operator === "===" || node.operator === "==") return provablyEqualAfterWrites(node.left, node.right, writes, depth + 1, seen);
		const leftNumber = resolveToNumber(node.left, writes, depth + 1, seen);
		const rightNumber = resolveToNumber(node.right, writes, depth + 1, seen);
		if (leftNumber !== null && rightNumber !== null) return numericComparisonHolds(node.operator, leftNumber, rightNumber);
		return false;
	}
	const value = resolveValueNode(node, writes, depth + 1, seen);
	if (value) {
		if (isNodeOfType(value, "ArrayExpression") || isNodeOfType(value, "ObjectExpression")) return true;
		if (isNodeOfType(value, "Literal")) return Boolean(value.value);
	}
	return false;
};
const collectTopLevelWrites = (statements, setterNameToStateName, setterNames) => {
	const writes = /* @__PURE__ */ new Map();
	const setterCallNodes = /* @__PURE__ */ new Set();
	for (const statement of statements) {
		const setterCall = getUnconditionalSetterCall(statement, setterNames);
		if (!setterCall || !isNodeOfType(setterCall.callee, "Identifier")) continue;
		setterCallNodes.add(setterCall);
		const stateName = setterNameToStateName.get(setterCall.callee.name);
		if (!stateName) continue;
		const argument = setterCall.arguments?.[0];
		if (!argument) continue;
		const newValue = isNodeOfType(argument, "ArrowFunctionExpression") || isNodeOfType(argument, "FunctionExpression") ? functionReturnExpression(argument) : stripParenExpression(argument);
		if (newValue) writes.set(stateName, newValue);
	}
	return {
		writes,
		setterCallNodes
	};
};
const everySetterCall = (root, setterName, inspect) => {
	let ok = true;
	const visit = (node) => {
		if (!ok) return;
		if (isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "Identifier") && node.callee.name === setterName && !inspect(node)) {
			ok = false;
			return;
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent" || key === "type") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
			if (!ok) return;
		}
	};
	visit(root);
	return ok;
};
const everyWriteToStateDrivesTowardEmpty = (callbackBody, setterName) => everySetterCall(callbackBody, setterName, (call) => {
	const argument = call.arguments?.[0];
	if (!argument) return true;
	const value = stripParenExpression(argument);
	return isEmptyOrFalsyValue(value) || isLengthReducingUpdater(value);
});
const everySetterCallIsTopLevel = (callbackBody, setterNames, topLevelSetterCalls) => {
	let safe = true;
	const visit = (node) => {
		if (!safe) return;
		if (isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "Identifier") && setterNames.has(node.callee.name) && !topLevelSetterCalls.has(node)) {
			safe = false;
			return;
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent" || key === "type") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
			if (!safe) return;
		}
	};
	visit(callbackBody);
	return safe;
};
const noSelfUpdatingEffect = defineRule({
	id: "no-self-updating-effect",
	title: "Effect updates its own dependency",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "Break the loop: work the value out while rendering, set it in an event handler, or guard the update so it stops changing. See https://react.dev/learn/you-might-not-need-an-effect",
	create: (context) => {
		const checkFunctionScope = (functionBody) => {
			if (!functionBody || !isNodeOfType(functionBody, "BlockStatement")) return;
			const useStateBindings = collectUseStateBindings(functionBody);
			if (useStateBindings.length === 0) return;
			const setterNameToStateName = /* @__PURE__ */ new Map();
			for (const binding of useStateBindings) setterNameToStateName.set(binding.setterName, binding.valueName);
			const setterNames = new Set(setterNameToStateName.keys());
			for (const statement of functionBody.body ?? []) {
				if (!isNodeOfType(statement, "ExpressionStatement")) continue;
				const effectCall = statement.expression;
				if (!isNodeOfType(effectCall, "CallExpression")) continue;
				if (!isHookCall$1(effectCall, EFFECT_HOOK_NAMES$1)) continue;
				if ((effectCall.arguments?.length ?? 0) < 2) continue;
				const dependencyStateNames = collectDependencyStateNames(effectCall.arguments[1]);
				if (dependencyStateNames.size === 0) continue;
				const callback = getEffectCallback(effectCall);
				if (!callback) continue;
				const callbackStatements = getCallbackStatements(callback);
				const firstWriteIndex = callbackStatements.findIndex((candidate) => getUnconditionalSetterCall(candidate, setterNames) !== null);
				const guardCutoff = firstWriteIndex < 0 ? callbackStatements.length : firstWriteIndex;
				const earlyReturnGuardTests = callbackStatements.slice(0, guardCutoff).filter(isEarlyReturnGuard).map((guard) => guard.test);
				const { writes: topLevelWrites, setterCallNodes } = collectTopLevelWrites(callbackStatements, setterNameToStateName, setterNames);
				if (everySetterCallIsTopLevel(callback, setterNames, setterCallNodes) && earlyReturnGuardTests.some((test) => guardProvenAfterWrites(test, topLevelWrites, 0, /* @__PURE__ */ new Set()))) continue;
				const reportedStateNames = /* @__PURE__ */ new Set();
				for (const callbackStatement of callbackStatements) {
					const setterCall = getUnconditionalSetterCall(callbackStatement, setterNames);
					if (!setterCall || !isNodeOfType(setterCall.callee, "Identifier")) continue;
					const stateName = setterNameToStateName.get(setterCall.callee.name);
					if (!stateName || !dependencyStateNames.has(stateName)) continue;
					if (reportedStateNames.has(stateName)) continue;
					if (!isNonSettlingSetterArgument(setterCall, stateName)) continue;
					const firstArgument = setterCall.arguments?.[0];
					if (firstArgument && writeProvablyConverges(stripParenExpression(firstArgument), stateName, earlyReturnGuardTests) && everyWriteToStateDrivesTowardEmpty(callback, setterCall.callee.name)) continue;
					reportedStateNames.add(stateName);
					context.report({
						node: setterCall,
						message: `${setterCall.callee.name}() loops forever because it sets \`${stateName}\`, the same state this effect watches.`
					});
				}
			}
		};
		return {
			FunctionDeclaration(node) {
				const functionName = node.id?.name;
				if (!functionName || !isUppercaseName(functionName) && !isReactHookName(functionName)) return;
				checkFunctionScope(node.body);
			},
			VariableDeclarator(node) {
				const isHookAssignment = isNodeOfType(node.id, "Identifier") && isReactHookName(node.id.name) && (isNodeOfType(node.init, "ArrowFunctionExpression") || isNodeOfType(node.init, "FunctionExpression"));
				if (!isComponentAssignment(node) && !isHookAssignment) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkFunctionScope(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/get-parent-component.ts
const getParentComponent = (node) => {
	let ancestor = node.parent;
	while (ancestor) {
		if (isEs5Component(ancestor) || isEs6Component(ancestor)) return ancestor;
		ancestor = ancestor.parent ?? null;
	}
	return null;
};
//#endregion
//#region src/plugin/rules/react-builtins/no-set-state.ts
const MESSAGE$12 = "Your project discourages `this.setState` here.";
const noSetState = defineRule({
	id: "no-set-state",
	title: "Use of this.setState",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Lift state up or use an external store instead of `this.setState`.",
	category: "Architecture",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		if (!isNodeOfType(node.callee.object, "ThisExpression")) return;
		if (!isNodeOfType(node.callee.property, "Identifier") || node.callee.property.name !== "setState") return;
		if (!getParentComponent(node)) return;
		context.report({
			node: node.callee,
			message: MESSAGE$12
		});
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/no-set-state-in-render.ts
const isUnconditionalSetterCallStatement = (statement, setterNames) => {
	if (!isNodeOfType(statement, "ExpressionStatement")) return null;
	const expression = statement.expression;
	if (!isNodeOfType(expression, "CallExpression")) return null;
	const callee = expression.callee;
	if (!isNodeOfType(callee, "Identifier")) return null;
	if (!setterNames.has(callee.name)) return null;
	return expression;
};
const noSetStateInRender = defineRule({
	id: "no-set-state-in-render",
	title: "setState called during render",
	severity: "warn",
	recommendation: "Move the setter into a `useEffect` or an event handler, or compute the value while rendering. Calling a setter during render starts another render that calls it again, looping forever.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const setterNames = new Set(collectUseStateBindings(componentBody).map((binding) => binding.setterName));
			if (setterNames.size === 0) return;
			for (const statement of componentBody.body ?? []) {
				const setterCall = isUnconditionalSetterCallStatement(statement, setterNames);
				if (!setterCall) continue;
				if (!isNodeOfType(setterCall, "CallExpression")) continue;
				if (!isNodeOfType(setterCall.callee, "Identifier")) continue;
				const setterIdentifierName = setterCall.callee.name;
				context.report({
					node: setterCall,
					message: `${setterIdentifierName}() loops forever because it runs during render & triggers another render.`
				});
			}
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/design/no-side-tab-border.ts
const isNeutralBorderColor = (value) => {
	const trimmed = value.trim().toLowerCase();
	if ([
		"gray",
		"grey",
		"silver",
		"white",
		"black",
		"transparent",
		"currentcolor"
	].includes(trimmed)) return true;
	const parsed = parseColorToRgb(trimmed);
	if (parsed) return !hasColorChroma(parsed);
	return false;
};
const extractBorderColorFromShorthand = (shorthandValue) => {
	const afterSolid = shorthandValue.match(/solid\s+(.+)$/i);
	if (!afterSolid) return null;
	return afterSolid[1].trim();
};
const BORDER_SIDE_KEYS = new Map([
	["borderLeft", "left"],
	["borderRight", "right"],
	["borderInlineStart", "left"],
	["borderInlineEnd", "right"]
]);
const BORDER_SIDE_WIDTH_KEYS = new Set([
	"borderLeftWidth",
	"borderRightWidth",
	"borderInlineStartWidth",
	"borderInlineEndWidth"
]);
const noSideTabBorder = defineRule({
	id: "no-side-tab-border",
	title: "Thick one-sided border",
	tags: ["design", "test-noise"],
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Use a softer accent like an inset box-shadow, a background, or a thin border-bottom instead of a thick one-sided border.",
	create: (context) => ({
		JSXAttribute(node) {
			const expression = getInlineStyleExpression(node);
			if (!expression) return;
			let hasBorderRadius = false;
			for (const property of expression.properties ?? []) if (getStylePropertyKey(property) === "borderRadius") {
				const numValue = getStylePropertyNumberValue(property);
				const strValue = getStylePropertyStringValue(property);
				if (numValue !== null && numValue > 0 || strValue !== null && parseFloat(strValue) > 0) hasBorderRadius = true;
			}
			const threshold = hasBorderRadius ? 1 : 3;
			for (const property of expression.properties ?? []) {
				const key = getStylePropertyKey(property);
				if (!key) continue;
				const sideLabel = BORDER_SIDE_KEYS.get(key);
				if (sideLabel !== void 0) {
					const value = getStylePropertyStringValue(property);
					if (!value) continue;
					const widthMatch = value.match(/^(\d+)px\s+solid/);
					if (!widthMatch) continue;
					const borderColor = extractBorderColorFromShorthand(value);
					if (borderColor && isNeutralBorderColor(borderColor)) continue;
					const width = parseInt(widthMatch[1], 10);
					if (width >= threshold) context.report({
						node: property,
						message: `Your users see an off, dated thick border on one side (${sideLabel}: ${width}px), so use a softer accent or drop it.`
					});
				}
				if (BORDER_SIDE_WIDTH_KEYS.has(key)) {
					const numValue = getStylePropertyNumberValue(property);
					const strValue = getStylePropertyStringValue(property);
					const width = numValue ?? (strValue !== null ? parseFloat(strValue) : NaN);
					if (isNaN(width)) continue;
					const colorKey = key.replace("Width", "Color");
					if (!expression.properties?.some((colorProperty) => {
						if (getStylePropertyKey(colorProperty) !== colorKey) return false;
						const colorValue = getStylePropertyStringValue(colorProperty);
						return colorValue !== null && !isNeutralBorderColor(colorValue);
					})) continue;
					if (width >= threshold) context.report({
						node: property,
						message: `Your users see an off, dated thick border on one side (${width}px), so use a softer accent or drop it.`
					});
				}
			}
		},
		JSXOpeningElement(node) {
			const classStr = getStringFromClassNameAttr(node);
			if (!classStr) return;
			const sideMatch = classStr.match(/\bborder-[lrse]-(\d+)\b/);
			if (!sideMatch) return;
			if (/\bborder-(?:(?:gray|slate|zinc|neutral|stone)-\d+|white|black|transparent)\b/.test(classStr)) return;
			if (parseInt(sideMatch[1], 10) >= (/\brounded(?:-(?!none\b)\w+)?\b/.test(classStr) && !/\brounded-none\b/.test(classStr) ? 1 : 4)) context.report({
				node,
				message: `Your users see an off, dated thick border on one side (${sideMatch[0]}), so use a softer accent or drop it.`
			});
		}
	})
});
//#endregion
//#region src/plugin/utils/is-abstract-role.ts
const isAbstractRole = (openingElement, settings) => {
	const elementType = getElementType(openingElement, settings);
	if (!HTML_TAGS.has(elementType)) return false;
	const roleAttribute = hasJsxPropIgnoreCase(openingElement.attributes, "role");
	if (!roleAttribute) return false;
	const value = getJsxPropStringValue(roleAttribute);
	return value !== null && ABSTRACT_ROLES.has(value);
};
//#endregion
//#region src/plugin/rules/a11y/no-static-element-interactions.ts
const MESSAGE$11 = "Screen reader users can't tell this click handler is interactive because it has no `role`, so add a `role` or use a button or link.";
const DEFAULT_HANDLERS = [
	"onClick",
	"onMouseDown",
	"onMouseUp",
	"onKeyPress",
	"onKeyDown",
	"onKeyUp"
];
const resolveSettings$12 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noStaticElementInteractions ?? {} : {};
	return {
		handlers: ruleSettings.handlers ?? DEFAULT_HANDLERS,
		allowExpressionValues: ruleSettings.allowExpressionValues ?? false
	};
};
const isNullValue = (attribute) => {
	if (!attribute.value) return false;
	if (!isNodeOfType(attribute.value, "JSXExpressionContainer")) return false;
	const expression = attribute.value.expression;
	return isNodeOfType(expression, "Literal") && expression.value === null;
};
const noStaticElementInteractions = defineRule({
	id: "no-static-element-interactions",
	title: "Interaction on static element",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Give clickable static elements a `role`, or use a button or link.",
	category: "Accessibility",
	create: (context) => {
		const settings = resolveSettings$12(context.settings);
		const isTestlikeFile = isTestlikeFilename(context.filename);
		return { JSXOpeningElement(node) {
			if (isTestlikeFile) return;
			let hasNonBlockerHandler = false;
			let hasAnyHandler = false;
			for (const handler of settings.handlers) {
				const attribute = hasJsxPropIgnoreCase(node.attributes, handler);
				if (!attribute) continue;
				if (isNullValue(attribute)) continue;
				hasAnyHandler = true;
				if (!isPureEventBlockerHandler(attribute)) {
					hasNonBlockerHandler = true;
					break;
				}
			}
			if (!hasAnyHandler) return;
			if (!hasNonBlockerHandler) return;
			const elementType = getElementType(node, context.settings);
			if (!HTML_TAGS.has(elementType)) return;
			if (isHiddenFromScreenReader(node, context.settings)) return;
			if (isPresentationRole(node)) return;
			if (isInteractiveElement(elementType, node)) return;
			if (isNonInteractiveElement(elementType, node)) return;
			if (isAbstractRole(node, context.settings)) return;
			const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
			if (!roleAttribute || !roleAttribute.value) {
				context.report({
					node: node.name,
					message: MESSAGE$11
				});
				return;
			}
			const attributeValue = roleAttribute.value;
			if (isNodeOfType(attributeValue, "Literal") && typeof attributeValue.value === "string") {
				const firstRole = attributeValue.value.toLowerCase().trim().split(/\s+/)[0];
				if (firstRole && (isInteractiveRole(firstRole) || isNonInteractiveRole(firstRole))) return;
				context.report({
					node: node.name,
					message: MESSAGE$11
				});
				return;
			}
			if (isNodeOfType(attributeValue, "JSXExpressionContainer") && settings.allowExpressionValues) return;
			context.report({
				node: node.name,
				message: MESSAGE$11
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/no-string-refs.ts
const STRING_IN_REF_MESSAGE = "Your component can't reach this node because string refs don't work in modern React.";
const THIS_REFS_MESSAGE = "Your component can't reach its nodes because `this.refs` is empty in modern React.";
const isStringLiteralRefAttribute = (attribute, noTemplateLiterals) => {
	if (!isNodeOfType(attribute.name, "JSXIdentifier") || attribute.name.name !== "ref") return false;
	const value = attribute.value;
	if (!value) return false;
	if (isNodeOfType(value, "Literal") && typeof value.value === "string") return true;
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") return true;
		if (noTemplateLiterals && isNodeOfType(expression, "TemplateLiteral")) return true;
	}
	return false;
};
const resolveSettings$11 = (settings) => {
	const reactDoctorSettings = settings?.["react-doctor"];
	if (typeof reactDoctorSettings === "object" && reactDoctorSettings !== null && "noStringRefs" in reactDoctorSettings && typeof reactDoctorSettings.noStringRefs === "object" && reactDoctorSettings.noStringRefs !== null) return reactDoctorSettings.noStringRefs;
	return {};
};
const noStringRefs = defineRule({
	id: "no-string-refs",
	title: "Use of string refs",
	severity: "warn",
	recommendation: "Use a callback ref (`ref={(node) => { this.foo = node }}`) or `useRef` instead of string refs.",
	create: (context) => {
		const { noTemplateLiterals = false } = resolveSettings$11(context.settings);
		const isTestlikeFile = isTestlikeFilename(context.filename);
		return {
			JSXAttribute(node) {
				if (isTestlikeFile) return;
				if (isStringLiteralRefAttribute(node, noTemplateLiterals)) context.report({
					node,
					message: STRING_IN_REF_MESSAGE
				});
			},
			MemberExpression(node) {
				if (isTestlikeFile) return;
				if (!isNodeOfType(node.object, "ThisExpression")) return;
				if (!isNodeOfType(node.property, "Identifier") || node.property.name !== "refs") return;
				if (!getParentComponent(node)) return;
				context.report({
					node,
					message: THIS_REFS_MESSAGE
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/no-this-in-sfc.ts
const MESSAGE$10 = "This value is `undefined` because function components have no `this`.";
const isInsideClassMethod = (node, customClassFactoryNames) => {
	let ancestor = node.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "ClassDeclaration") || isNodeOfType(ancestor, "ClassExpression")) return true;
		if (isEs5Component(ancestor)) return true;
		if (customClassFactoryNames.size > 0 && isNodeOfType(ancestor, "CallExpression")) {
			const callee = ancestor.callee;
			if (isNodeOfType(callee, "Identifier") && customClassFactoryNames.has(callee.name)) return true;
			if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && customClassFactoryNames.has(callee.property.name)) return true;
		}
		ancestor = ancestor.parent ?? null;
	}
	return false;
};
const findEnclosingFunctionComponent = (node) => {
	let ancestor = node.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "FunctionDeclaration") || isNodeOfType(ancestor, "FunctionExpression") || isNodeOfType(ancestor, "ArrowFunctionExpression")) return ancestor;
		ancestor = ancestor.parent ?? null;
	}
	return null;
};
const hasExplicitThisParameter = (fn) => {
	const firstParameter = fn.params?.[0];
	if (!firstParameter) return false;
	if (!isNodeOfType(firstParameter, "Identifier")) return false;
	return firstParameter.name === "this";
};
const looksLikeFunctionComponent = (fn) => {
	if (hasExplicitThisParameter(fn)) return false;
	if (isNodeOfType(fn, "FunctionDeclaration") && fn.id) return isReactComponentName(fn.id.name);
	let parent = fn.parent;
	while (parent) {
		if (isNodeOfType(parent, "VariableDeclarator")) {
			if (isNodeOfType(parent.id, "Identifier")) return isReactComponentName(parent.id.name);
			return false;
		}
		if (isNodeOfType(parent, "AssignmentExpression")) {
			if (isNodeOfType(parent.left, "Identifier")) return isReactComponentName(parent.left.name);
			return false;
		}
		if (isNodeOfType(parent, "Property")) return false;
		if (isNodeOfType(parent, "FunctionDeclaration") || isNodeOfType(parent, "FunctionExpression") || isNodeOfType(parent, "ArrowFunctionExpression") || isNodeOfType(parent, "ClassDeclaration") || isNodeOfType(parent, "ClassExpression") || isNodeOfType(parent, "MethodDefinition")) return false;
		parent = parent.parent ?? null;
	}
	return false;
};
const noThisInSfc = defineRule({
	id: "no-this-in-sfc",
	title: "this used in function component",
	severity: "warn",
	recommendation: "Read from the `props` argument instead of `this.props`.",
	create: (context) => {
		const configured = (context.settings?.react)?.createClass;
		const customClassFactoryNames = /* @__PURE__ */ new Set();
		if (typeof configured === "string") customClassFactoryNames.add(configured);
		else if (Array.isArray(configured)) {
			for (const entry of configured) if (typeof entry === "string") customClassFactoryNames.add(entry);
		}
		return { ThisExpression(node) {
			const parent = node.parent;
			if (!parent || !isNodeOfType(parent, "MemberExpression")) return;
			if (isInsideClassMethod(node, customClassFactoryNames)) return;
			const enclosingFunction = findEnclosingFunctionComponent(node);
			if (!enclosingFunction) return;
			if (!looksLikeFunctionComponent(enclosingFunction)) return;
			context.report({
				node,
				message: MESSAGE$10
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/design/no-tiny-text.ts
const noTinyText = defineRule({
	id: "no-tiny-text",
	title: "Text is too small",
	severity: "warn",
	tags: ["test-noise"],
	category: "Accessibility",
	recommendation: "Use at least 12px for body text, and 16px is best. Small text is hard to read, especially on phones.",
	create: (context) => ({ JSXAttribute(node) {
		const expression = getInlineStyleExpression(node);
		if (!expression) return;
		for (const property of expression.properties ?? []) {
			if (getStylePropertyKey(property) !== "fontSize") continue;
			let pxValue = null;
			const numValue = getStylePropertyNumberValue(property);
			const strValue = getStylePropertyStringValue(property);
			if (numValue !== null) pxValue = numValue;
			else if (strValue !== null) {
				const pxMatch = strValue.match(/^([\d.]+)px$/);
				if (pxMatch) pxValue = parseFloat(pxMatch[1]);
				const remMatch = strValue.match(/^([\d.]+)rem$/);
				if (remMatch) pxValue = parseFloat(remMatch[1]) * 16;
			}
			if (pxValue !== null && pxValue > 0 && pxValue < 12) context.report({
				node: property,
				message: `Your users strain to read ${pxValue}px text, so use at least 12px for body text, & 16px is best.`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/performance/no-transition-all.ts
const noTransitionAll = defineRule({
	id: "no-transition-all",
	title: "transition: all animates everything",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "List the specific properties: `transition: \"opacity 200ms, transform 200ms\"`. In Tailwind, use `transition-colors`, `transition-opacity`, or `transition-transform`",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "style") return;
		if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const expression = node.value.expression;
		if (!isNodeOfType(expression, "ObjectExpression")) return;
		for (const property of expression.properties ?? []) {
			if (!isNodeOfType(property, "Property")) continue;
			if ((isNodeOfType(property.key, "Identifier") ? property.key.name : null) !== "transition") continue;
			if (isNodeOfType(property.value, "Literal") && typeof property.value.value === "string" && property.value.value.startsWith("all")) context.report({
				node: property,
				message: "This can stutter because transition: \"all\" animates every property, even slow layout ones, so list only the properties you actually change"
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/correctness/no-uncontrolled-input.ts
const UNCONTROLLED_INPUT_TAGS = new Set([
	"input",
	"textarea",
	"select"
]);
const VALUE_BYPASS_INPUT_TYPES = new Set([
	"hidden",
	"checkbox",
	"radio"
]);
const VALUE_PARTNER_ATTRIBUTES = ["onChange", "readOnly"];
const getInputTypeLiteral = (attributes) => {
	const typeAttribute = findJsxAttribute(attributes, "type");
	if (!typeAttribute || !isNodeOfType(typeAttribute.value, "Literal")) return null;
	const value = typeAttribute.value.value;
	return typeof value === "string" ? value : null;
};
const isUseStateUndefinedInitializer = (init) => {
	if (!init || !isNodeOfType(init, "CallExpression")) return false;
	if (!isHookCall$1(init, "useState")) return false;
	const args = init.arguments ?? [];
	if (args.length === 0) return true;
	const firstArgument = args[0];
	return isNodeOfType(firstArgument, "Identifier") && firstArgument.name === "undefined";
};
const collectUndefinedInitialStateNames = (componentBody) => {
	const stateNames = /* @__PURE__ */ new Set();
	if (!isNodeOfType(componentBody, "BlockStatement")) return stateNames;
	for (const statement of componentBody.body ?? []) {
		if (!isNodeOfType(statement, "VariableDeclaration")) continue;
		for (const declarator of statement.declarations ?? []) {
			if (!isNodeOfType(declarator.id, "ArrayPattern")) continue;
			const valueElement = declarator.id.elements?.[0];
			if (!isNodeOfType(valueElement, "Identifier")) continue;
			if (!isUseStateUndefinedInitializer(declarator.init)) continue;
			stateNames.add(valueElement.name);
		}
	}
	return stateNames;
};
const hasJsxSpreadAttribute = (attributes) => attributes.some((attribute) => isNodeOfType(attribute, "JSXSpreadAttribute"));
const noUncontrolledInput = defineRule({
	id: "no-uncontrolled-input",
	title: "Uncontrolled input value",
	severity: "warn",
	recommendation: "Give `useState` a starting value (e.g. `useState(\"\")` instead of `useState()`), add `onChange` (or `readOnly`) whenever you set `value`, and drop `defaultValue` on controlled inputs since React ignores it.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody) return;
			const undefinedInitialStateNames = isNodeOfType(componentBody, "BlockStatement") ? collectUndefinedInitialStateNames(componentBody) : /* @__PURE__ */ new Set();
			walkAst(componentBody, (child) => {
				if (!isNodeOfType(child, "JSXOpeningElement")) return;
				if (!isNodeOfType(child.name, "JSXIdentifier")) return;
				const tagName = child.name.name;
				if (!UNCONTROLLED_INPUT_TAGS.has(tagName)) return;
				const attributes = child.attributes ?? [];
				if (hasJsxSpreadAttribute(attributes)) return;
				const valueAttribute = findJsxAttribute(attributes, "value");
				if (!valueAttribute) return;
				if (tagName === "input") {
					const inputType = getInputTypeLiteral(attributes);
					if (inputType !== null && VALUE_BYPASS_INPUT_TYPES.has(inputType)) return;
				}
				const hasAllowedPartner = VALUE_PARTNER_ATTRIBUTES.some((partnerAttributeName) => findJsxAttribute(attributes, partnerAttributeName));
				if (isNodeOfType(valueAttribute.value, "JSXExpressionContainer") && isNodeOfType(valueAttribute.value.expression, "Identifier") && undefinedInitialStateNames.has(valueAttribute.value.expression.name)) {
					const stateName = valueAttribute.value.expression.name;
					const partnerHint = hasAllowedPartner ? "Give useState a starting value" : "Give useState a starting value & add onChange (or readOnly)";
					context.report({
						node: child,
						message: `Your users hit a console warning & a field that can reset because "${stateName}" starts undefined, so <${tagName} value={${stateName}}> flips from uncontrolled to controlled. ${partnerHint} (e.g. \`useState("")\`).`
					});
					return;
				}
				if (findJsxAttribute(attributes, "defaultValue")) {
					context.report({
						node: child,
						message: `Your users never see the \`defaultValue\` on this <${tagName}> because React ignores it once \`value\` is set, so remove one.`
					});
					return;
				}
				if (!hasAllowedPartner) context.report({
					node: child,
					message: `Your users can't type in this <${tagName} value={...}> because it has no \`onChange\` or \`readOnly\`, so add \`onChange\` (or \`readOnly\` if that's intended).`
				});
			});
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isInlineFunctionExpression(node.init)) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/bundle-size/no-undeferred-third-party.ts
const noUndeferredThirdParty = defineRule({
	id: "no-undeferred-third-party",
	title: "Render-blocking third-party script",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use `next/script` with `strategy=\"lazyOnload\"`, or add the `defer` attribute.",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "script") return;
		const attributes = node.attributes ?? [];
		if (!findJsxAttribute(attributes, "src")) return;
		if (!hasJsxAttribute(attributes, "defer") && !hasJsxAttribute(attributes, "async")) context.report({
			node,
			message: "This <script> blocks the page from showing to your users until it loads. Add defer or async so it loads in the background."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-unescaped-entities.ts
const ESCAPED_VERSIONS = {
	"\"": "`&quot;` / `&ldquo;` / `&#34;` / `&rdquo;`",
	"'": "`&apos;` / `&lsquo;` / `&#39;` / `&rsquo;`",
	">": "`&gt;` / `&#62;`",
	"}": "`&#125;` (or wrap the literal in `{'}'}`)"
};
const buildMessage$7 = (character) => `\`${character}\` in JSX text can read as markup & confuse readers.`;
const noUnescapedEntities = defineRule({
	id: "no-unescaped-entities",
	title: "Unescaped entities in JSX",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Replace bare `'` / `\"` / `>` / `}` characters in JSX text with HTML entities.",
	create: (context) => ({ JSXText(node) {
		const value = node.value;
		for (const character of value) if (character in ESCAPED_VERSIONS) {
			context.report({
				node,
				message: buildMessage$7(character)
			});
			return;
		}
	} })
});
//#endregion
//#region src/plugin/constants/dom-aria-properties.ts
const ARIA_PROPERTY_NAMES = new Set([
	"activedescendant",
	"atomic",
	"autocomplete",
	"braillelabel",
	"brailleroledescription",
	"busy",
	"checked",
	"colcount",
	"colindex",
	"colspan",
	"controls",
	"current",
	"describedby",
	"description",
	"details",
	"disabled",
	"dropeffect",
	"errormessage",
	"expanded",
	"flowto",
	"grabbed",
	"haspopup",
	"hidden",
	"invalid",
	"keyshortcuts",
	"label",
	"labelledby",
	"level",
	"live",
	"modal",
	"multiline",
	"multiselectable",
	"orientation",
	"owns",
	"placeholder",
	"posinset",
	"pressed",
	"readonly",
	"relevant",
	"required",
	"roledescription",
	"rowcount",
	"rowindex",
	"rowspan",
	"selected",
	"setsize",
	"sort",
	"valuemax",
	"valuemin",
	"valuenow",
	"valuetext"
]);
const isValidDomAriaProperty = (name) => {
	if (!name.startsWith("aria-")) return false;
	const suffix = name.slice(5).toLowerCase();
	return ARIA_PROPERTY_NAMES.has(suffix);
};
//#endregion
//#region src/plugin/constants/dom-property-names.ts
const DOM_PROPERTY_NAMES = new Set([
	"dir",
	"draggable",
	"hidden",
	"id",
	"lang",
	"nonce",
	"part",
	"popover",
	"slot",
	"style",
	"title",
	"translate",
	"inert",
	"accept",
	"action",
	"allow",
	"alt",
	"as",
	"async",
	"buffered",
	"capture",
	"challenge",
	"cite",
	"code",
	"cols",
	"content",
	"coords",
	"csp",
	"data",
	"decoding",
	"default",
	"defer",
	"disabled",
	"form",
	"headers",
	"height",
	"high",
	"href",
	"icon",
	"importance",
	"integrity",
	"kind",
	"label",
	"language",
	"loading",
	"list",
	"loop",
	"low",
	"manifest",
	"max",
	"media",
	"method",
	"min",
	"multiple",
	"muted",
	"name",
	"open",
	"optimum",
	"pattern",
	"ping",
	"placeholder",
	"poster",
	"preload",
	"profile",
	"rel",
	"required",
	"reversed",
	"role",
	"rows",
	"sandbox",
	"scope",
	"seamless",
	"selected",
	"shape",
	"size",
	"sizes",
	"span",
	"src",
	"start",
	"step",
	"summary",
	"target",
	"type",
	"value",
	"width",
	"wmode",
	"wrap",
	"accumulate",
	"additive",
	"alphabetic",
	"amplitude",
	"ascent",
	"azimuth",
	"bbox",
	"begin",
	"bias",
	"by",
	"clip",
	"color",
	"cursor",
	"cx",
	"cy",
	"d",
	"decelerate",
	"descent",
	"direction",
	"display",
	"divisor",
	"dur",
	"dx",
	"dy",
	"elevation",
	"end",
	"exponent",
	"fill",
	"filter",
	"format",
	"from",
	"fr",
	"fx",
	"fy",
	"g1",
	"g2",
	"hanging",
	"hreflang",
	"ideographic",
	"in",
	"in2",
	"intercept",
	"k",
	"k1",
	"k2",
	"k3",
	"k4",
	"kerning",
	"local",
	"mask",
	"mode",
	"offset",
	"opacity",
	"operator",
	"order",
	"orient",
	"orientation",
	"origin",
	"overflow",
	"path",
	"points",
	"r",
	"radius",
	"restart",
	"result",
	"rotate",
	"rx",
	"ry",
	"scale",
	"seed",
	"slope",
	"spacing",
	"speed",
	"stemh",
	"stemv",
	"string",
	"stroke",
	"to",
	"transform",
	"u1",
	"u2",
	"unicode",
	"values",
	"version",
	"visibility",
	"widths",
	"x",
	"x1",
	"x2",
	"xmlns",
	"y",
	"y1",
	"y2",
	"z",
	"property",
	"ref",
	"key",
	"children",
	"results",
	"security",
	"controls",
	"accessKey",
	"autoCapitalize",
	"autoFocus",
	"contentEditable",
	"enterKeyHint",
	"exportParts",
	"inputMode",
	"itemID",
	"itemRef",
	"itemProp",
	"itemScope",
	"itemType",
	"spellCheck",
	"tabIndex",
	"acceptCharset",
	"autoComplete",
	"autoPlay",
	"border",
	"cellPadding",
	"cellSpacing",
	"classID",
	"codeBase",
	"colSpan",
	"contextMenu",
	"dateTime",
	"encType",
	"formAction",
	"formEncType",
	"formMethod",
	"formNoValidate",
	"formTarget",
	"frameBorder",
	"hrefLang",
	"httpEquiv",
	"imageSizes",
	"imageSrcSet",
	"isMap",
	"keyParams",
	"keyType",
	"marginHeight",
	"marginWidth",
	"maxLength",
	"mediaGroup",
	"minLength",
	"noValidate",
	"onAnimationEnd",
	"onAnimationIteration",
	"onAnimationStart",
	"onBlur",
	"onChange",
	"onClick",
	"onContextMenu",
	"onCopy",
	"onCompositionEnd",
	"onCompositionStart",
	"onCompositionUpdate",
	"onCut",
	"onDoubleClick",
	"onDrag",
	"onDragEnd",
	"onDragEnter",
	"onDragExit",
	"onDragLeave",
	"onError",
	"onFocus",
	"onInput",
	"onKeyDown",
	"onKeyPress",
	"onKeyUp",
	"onLoad",
	"onWheel",
	"onDragOver",
	"onDragStart",
	"onDrop",
	"onMouseDown",
	"onMouseEnter",
	"onMouseLeave",
	"onMouseMove",
	"onMouseOut",
	"onMouseOver",
	"onMouseUp",
	"onPaste",
	"onScroll",
	"onScrollEnd",
	"onSelect",
	"onSubmit",
	"onBeforeToggle",
	"onToggle",
	"onTransitionEnd",
	"radioGroup",
	"readOnly",
	"referrerPolicy",
	"rowSpan",
	"srcDoc",
	"srcLang",
	"srcSet",
	"useMap",
	"crossOrigin",
	"accentHeight",
	"alignmentBaseline",
	"arabicForm",
	"attributeName",
	"attributeType",
	"baseFrequency",
	"baselineShift",
	"baseProfile",
	"calcMode",
	"capHeight",
	"clipPathUnits",
	"clipPath",
	"clipRule",
	"colorInterpolation",
	"colorInterpolationFilters",
	"colorProfile",
	"colorRendering",
	"contentScriptType",
	"contentStyleType",
	"diffuseConstant",
	"dominantBaseline",
	"edgeMode",
	"enableBackground",
	"fillOpacity",
	"fillRule",
	"filterRes",
	"filterUnits",
	"floodColor",
	"floodOpacity",
	"fontFamily",
	"fontSize",
	"fontSizeAdjust",
	"fontStretch",
	"fontStyle",
	"fontVariant",
	"fontWeight",
	"glyphName",
	"glyphOrientationHorizontal",
	"glyphOrientationVertical",
	"glyphRef",
	"gradientTransform",
	"gradientUnits",
	"horizAdvX",
	"horizOriginX",
	"imageRendering",
	"kernelMatrix",
	"kernelUnitLength",
	"keyPoints",
	"keySplines",
	"keyTimes",
	"lengthAdjust",
	"letterSpacing",
	"lightingColor",
	"limitingConeAngle",
	"markerEnd",
	"markerMid",
	"markerStart",
	"markerHeight",
	"markerUnits",
	"markerWidth",
	"maskContentUnits",
	"maskUnits",
	"mathematical",
	"numOctaves",
	"overlinePosition",
	"overlineThickness",
	"panose1",
	"paintOrder",
	"pathLength",
	"patternContentUnits",
	"patternTransform",
	"patternUnits",
	"pointerEvents",
	"pointsAtX",
	"pointsAtY",
	"pointsAtZ",
	"preserveAlpha",
	"preserveAspectRatio",
	"primitiveUnits",
	"refX",
	"refY",
	"rendering-intent",
	"repeatCount",
	"repeatDur",
	"requiredExtensions",
	"requiredFeatures",
	"shapeRendering",
	"specularConstant",
	"specularExponent",
	"spreadMethod",
	"startOffset",
	"stdDeviation",
	"stitchTiles",
	"stopColor",
	"stopOpacity",
	"strikethroughPosition",
	"strikethroughThickness",
	"strokeDasharray",
	"strokeDashoffset",
	"strokeLinecap",
	"strokeLinejoin",
	"strokeMiterlimit",
	"strokeOpacity",
	"strokeWidth",
	"surfaceScale",
	"systemLanguage",
	"tableValues",
	"targetX",
	"targetY",
	"textAnchor",
	"textDecoration",
	"textRendering",
	"textLength",
	"transformOrigin",
	"underlinePosition",
	"underlineThickness",
	"unicodeBidi",
	"unicodeRange",
	"unitsPerEm",
	"vAlphabetic",
	"vHanging",
	"vIdeographic",
	"vMathematical",
	"vectorEffect",
	"vertAdvY",
	"vertOriginX",
	"vertOriginY",
	"viewBox",
	"viewTarget",
	"wordSpacing",
	"writingMode",
	"xHeight",
	"xChannelSelector",
	"xlinkActuate",
	"xlinkArcrole",
	"xlinkHref",
	"xlinkRole",
	"xlinkShow",
	"xlinkTitle",
	"xlinkType",
	"xmlBase",
	"xmlLang",
	"xmlnsXlink",
	"xmlSpace",
	"yChannelSelector",
	"zoomAndPan",
	"autoCorrect",
	"autoSave",
	"className",
	"dangerouslySetInnerHTML",
	"defaultValue",
	"defaultChecked",
	"htmlFor",
	"onBeforeInput",
	"onInvalid",
	"onReset",
	"onTouchCancel",
	"onTouchEnd",
	"onTouchMove",
	"onTouchStart",
	"suppressContentEditableWarning",
	"suppressHydrationWarning",
	"onAbort",
	"onCanPlay",
	"onCanPlayThrough",
	"onDurationChange",
	"onEmptied",
	"onEncrypted",
	"onEnded",
	"onLoadedData",
	"onLoadedMetadata",
	"onLoadStart",
	"onPause",
	"onPlay",
	"onPlaying",
	"onProgress",
	"onRateChange",
	"onResize",
	"onSeeked",
	"onSeeking",
	"onStalled",
	"onSuspend",
	"onTimeUpdate",
	"onVolumeChange",
	"onWaiting",
	"onCopyCapture",
	"onCutCapture",
	"onPasteCapture",
	"onCompositionEndCapture",
	"onCompositionStartCapture",
	"onCompositionUpdateCapture",
	"onFocusCapture",
	"onBlurCapture",
	"onChangeCapture",
	"onBeforeInputCapture",
	"onInputCapture",
	"onResetCapture",
	"onSubmitCapture",
	"onInvalidCapture",
	"onLoadCapture",
	"onErrorCapture",
	"onKeyDownCapture",
	"onKeyPressCapture",
	"onKeyUpCapture",
	"onAbortCapture",
	"onCanPlayCapture",
	"onCanPlayThroughCapture",
	"onDurationChangeCapture",
	"onEmptiedCapture",
	"onEncryptedCapture",
	"onEndedCapture",
	"onLoadedDataCapture",
	"onLoadedMetadataCapture",
	"onLoadStartCapture",
	"onPauseCapture",
	"onPlayCapture",
	"onPlayingCapture",
	"onProgressCapture",
	"onRateChangeCapture",
	"onSeekedCapture",
	"onSeekingCapture",
	"onStalledCapture",
	"onSuspendCapture",
	"onTimeUpdateCapture",
	"onVolumeChangeCapture",
	"onWaitingCapture",
	"onSelectCapture",
	"onTouchCancelCapture",
	"onTouchEndCapture",
	"onTouchMoveCapture",
	"onTouchStartCapture",
	"onScrollCapture",
	"onScrollEndCapture",
	"onWheelCapture",
	"onAnimationEndCapture",
	"onAnimationStartCapture",
	"onTransitionEndCapture",
	"onAuxClick",
	"onAuxClickCapture",
	"onClickCapture",
	"onContextMenuCapture",
	"onDoubleClickCapture",
	"onDragCapture",
	"onDragEndCapture",
	"onDragEnterCapture",
	"onDragExitCapture",
	"onDragLeaveCapture",
	"onDragOverCapture",
	"onDragStartCapture",
	"onDropCapture",
	"onMouseDownCapture",
	"onMouseMoveCapture",
	"onMouseOutCapture",
	"onMouseOverCapture",
	"onMouseUpCapture",
	"autoPictureInPicture",
	"controlsList",
	"disablePictureInPicture",
	"disableRemotePlayback",
	"onGotPointerCapture",
	"onGotPointerCaptureCapture",
	"onLostPointerCapture",
	"onLostPointerCaptureCapture",
	"onPointerCancel",
	"onPointerCancelCapture",
	"onPointerDown",
	"onPointerDownCapture",
	"onPointerEnter",
	"onPointerEnterCapture",
	"onPointerLeave",
	"onPointerLeaveCapture",
	"onPointerMove",
	"onPointerMoveCapture",
	"onPointerOut",
	"onPointerOutCapture",
	"onPointerOver",
	"onPointerOverCapture",
	"onPointerUp",
	"onPointerUpCapture"
]);
const DOM_PROPERTY_NAMES_LOWER = (() => {
	const lookup = /* @__PURE__ */ new Map();
	for (const propertyName of DOM_PROPERTY_NAMES) lookup.set(propertyName.toLowerCase(), propertyName);
	return lookup;
})();
const DOM_ATTRIBUTES_TO_CAMEL = new Map([
	["accept-charset", "acceptCharset"],
	["class", "className"],
	["http-equiv", "httpEquiv"],
	["crossorigin", "crossOrigin"],
	["fetchpriority", "fetchPriority"],
	["for", "htmlFor"],
	["nomodule", "noModule"],
	["popovertarget", "popoverTarget"],
	["popovertargetaction", "popoverTargetAction"],
	["accent-height", "accentHeight"],
	["alignment-baseline", "alignmentBaseline"],
	["arabic-form", "arabicForm"],
	["baseline-shift", "baselineShift"],
	["cap-height", "capHeight"],
	["clip-path", "clipPath"],
	["clip-rule", "clipRule"],
	["color-interpolation", "colorInterpolation"],
	["color-interpolation-filters", "colorInterpolationFilters"],
	["color-profile", "colorProfile"],
	["color-rendering", "colorRendering"],
	["dominant-baseline", "dominantBaseline"],
	["enable-background", "enableBackground"],
	["fill-opacity", "fillOpacity"],
	["fill-rule", "fillRule"],
	["flood-color", "floodColor"],
	["flood-opacity", "floodOpacity"],
	["font-family", "fontFamily"],
	["font-size", "fontSize"],
	["font-size-adjust", "fontSizeAdjust"],
	["font-stretch", "fontStretch"],
	["font-style", "fontStyle"],
	["font-variant", "fontVariant"],
	["font-weight", "fontWeight"],
	["glyph-name", "glyphName"],
	["glyph-orientation-horizontal", "glyphOrientationHorizontal"],
	["glyph-orientation-vertical", "glyphOrientationVertical"],
	["horiz-adv-x", "horizAdvX"],
	["horiz-origin-x", "horizOriginX"],
	["image-rendering", "imageRendering"],
	["letter-spacing", "letterSpacing"],
	["lighting-color", "lightingColor"],
	["marker-end", "markerEnd"],
	["marker-mid", "markerMid"],
	["marker-start", "markerStart"],
	["overline-position", "overlinePosition"],
	["overline-thickness", "overlineThickness"],
	["paint-order", "paintOrder"],
	["panose-1", "panose1"],
	["pointer-events", "pointerEvents"],
	["rendering-intent", "renderingIntent"],
	["shape-rendering", "shapeRendering"],
	["stop-color", "stopColor"],
	["stop-opacity", "stopOpacity"],
	["strikethrough-position", "strikethroughPosition"],
	["strikethrough-thickness", "strikethroughThickness"],
	["stroke-dasharray", "strokeDasharray"],
	["stroke-dashoffset", "strokeDashoffset"],
	["stroke-linecap", "strokeLinecap"],
	["stroke-linejoin", "strokeLinejoin"],
	["stroke-miterlimit", "strokeMiterlimit"],
	["stroke-opacity", "strokeOpacity"],
	["stroke-width", "strokeWidth"],
	["text-anchor", "textAnchor"],
	["text-decoration", "textDecoration"],
	["text-rendering", "textRendering"],
	["underline-position", "underlinePosition"],
	["underline-thickness", "underlineThickness"],
	["unicode-bidi", "unicodeBidi"],
	["unicode-range", "unicodeRange"],
	["units-per-em", "unitsPerEm"],
	["v-alphabetic", "vAlphabetic"],
	["v-hanging", "vHanging"],
	["v-ideographic", "vIdeographic"],
	["v-mathematical", "vMathematical"],
	["vector-effect", "vectorEffect"],
	["vert-adv-y", "vertAdvY"],
	["vert-origin-x", "vertOriginX"],
	["vert-origin-y", "vertOriginY"],
	["word-spacing", "wordSpacing"],
	["writing-mode", "writingMode"],
	["x-height", "xHeight"],
	["xlink:actuate", "xlinkActuate"],
	["xlink:arcrole", "xlinkArcrole"],
	["xlink:href", "xlinkHref"],
	["xlink:role", "xlinkRole"],
	["xlink:show", "xlinkShow"],
	["xlink:title", "xlinkTitle"],
	["xlink:type", "xlinkType"],
	["xml:base", "xmlBase"],
	["xml:lang", "xmlLang"],
	["xml:space", "xmlSpace"]
]);
const DOM_PROPERTIES_IGNORE_CASE = [
	"charset",
	"allowFullScreen",
	"webkitAllowFullScreen",
	"mozAllowFullScreen",
	"webkitDirectory"
];
//#endregion
//#region src/plugin/constants/dom-property-tags.ts
const DOM_PROPERTY_TO_ALLOWED_TAGS = new Map([
	["abbr", new Set(["th", "td"])],
	["charset", new Set(["meta"])],
	["checked", new Set(["input"])],
	["closedby", new Set(["dialog"])],
	["crossOrigin", new Set([
		"script",
		"img",
		"video",
		"audio",
		"link",
		"image"
	])],
	["displaystyle", new Set(["math"])],
	["download", new Set(["a", "area"])],
	["fetchPriority", new Set([
		"img",
		"link",
		"script"
	])],
	["fill", new Set([
		"altGlyph",
		"circle",
		"ellipse",
		"g",
		"line",
		"marker",
		"mask",
		"path",
		"polygon",
		"polyline",
		"rect",
		"svg",
		"symbol",
		"text",
		"textPath",
		"tref",
		"tspan",
		"use",
		"animate",
		"animateColor",
		"animateMotion",
		"animateTransform",
		"set"
	])],
	["focusable", new Set(["svg"])],
	["imageSizes", new Set(["link"])],
	["imageSrcSet", new Set(["link"])],
	["property", new Set(["meta"])],
	["popoverTarget", new Set(["button", "input"])],
	["popoverTargetAction", new Set(["button", "input"])],
	["viewBox", new Set([
		"marker",
		"pattern",
		"svg",
		"symbol",
		"view"
	])],
	["as", new Set(["link"])],
	["align", new Set([
		"applet",
		"caption",
		"col",
		"colgroup",
		"hr",
		"iframe",
		"img",
		"table",
		"tbody",
		"td",
		"tfoot",
		"th",
		"thead",
		"tr"
	])],
	["valign", new Set([
		"tr",
		"td",
		"th",
		"thead",
		"tbody",
		"tfoot",
		"colgroup",
		"col"
	])],
	["noModule", new Set(["script"])],
	["onAbort", new Set(["audio", "video"])],
	["onCancel", new Set(["dialog"])],
	["onCanPlay", new Set(["audio", "video"])],
	["onCanPlayThrough", new Set(["audio", "video"])],
	["onClose", new Set(["dialog"])],
	["onDurationChange", new Set(["audio", "video"])],
	["onEmptied", new Set(["audio", "video"])],
	["onEncrypted", new Set(["audio", "video"])],
	["onEnded", new Set(["audio", "video"])],
	["onError", new Set([
		"audio",
		"video",
		"img",
		"link",
		"source",
		"script",
		"picture",
		"iframe"
	])],
	["onLoad", new Set([
		"script",
		"img",
		"link",
		"picture",
		"iframe",
		"object",
		"source",
		"body"
	])],
	["onLoadedData", new Set(["audio", "video"])],
	["onLoadedMetadata", new Set(["audio", "video"])],
	["onLoadStart", new Set(["audio", "video"])],
	["onPause", new Set(["audio", "video"])],
	["onPlay", new Set(["audio", "video"])],
	["onPlaying", new Set(["audio", "video"])],
	["onProgress", new Set(["audio", "video"])],
	["onRateChange", new Set(["audio", "video"])],
	["onResize", new Set(["audio", "video"])],
	["onSeeked", new Set(["audio", "video"])],
	["onSeeking", new Set(["audio", "video"])],
	["onStalled", new Set(["audio", "video"])],
	["onSuspend", new Set(["audio", "video"])],
	["onTimeUpdate", new Set(["audio", "video"])],
	["onVolumeChange", new Set(["audio", "video"])],
	["onWaiting", new Set(["audio", "video"])],
	["autoPictureInPicture", new Set(["video"])],
	["controls", new Set(["audio", "video"])],
	["controlsList", new Set(["audio", "video"])],
	["disablePictureInPicture", new Set(["video"])],
	["disableRemotePlayback", new Set(["audio", "video"])],
	["loop", new Set(["audio", "video"])],
	["muted", new Set(["audio", "video"])],
	["playsInline", new Set(["video"])],
	["allowFullScreen", new Set(["iframe", "video"])],
	["webkitAllowFullScreen", new Set(["iframe", "video"])],
	["mozAllowFullScreen", new Set(["iframe", "video"])],
	["poster", new Set(["video"])],
	["preload", new Set(["audio", "video"])],
	["scrolling", new Set(["iframe"])],
	["returnValue", new Set(["dialog"])],
	["webkitDirectory", new Set(["input"])],
	["shadowrootmode", new Set(["template"])],
	["shadowrootclonable", new Set(["template"])],
	["shadowrootdelegatesfocus", new Set(["template"])],
	["shadowrootserializable", new Set(["template"])],
	["transform-origin", new Set(["rect"])],
	["precedence", new Set(["style", "link"])]
]);
//#endregion
//#region src/plugin/rules/react-builtins/no-unknown-property.ts
const resolveSettings$10 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	if (typeof reactDoctor !== "object" || reactDoctor === null) return {};
	return reactDoctor.noUnknownProperty ?? {};
};
const isValidDataAttribute = (attributeName) => {
	if (!attributeName.startsWith("data-")) return false;
	if (attributeName.toLowerCase().startsWith("data-xml")) return false;
	const dataName = attributeName.slice(5);
	if (dataName.length === 0) return false;
	return !dataName.includes(":");
};
const matchesHtmlTagConventions = (tagName) => {
	if (tagName.length === 0) return false;
	const firstCharacter = tagName.charCodeAt(0);
	if (!(firstCharacter >= 97 && firstCharacter <= 122)) return false;
	return !tagName.includes("-");
};
const normalizeAttributeCase = (name) => {
	for (const ignoreCaseName of DOM_PROPERTIES_IGNORE_CASE) if (ignoreCaseName.toLowerCase() === name.toLowerCase()) return ignoreCaseName;
	return name;
};
const hasUppercaseChar = (input) => /[A-Z]/.test(input);
const INVALID_PROP_ON_TAG = (propName, allowedTags) => `React ignores \`${propName}\` here because it only works on these tags: ${allowedTags}.`;
const DATA_LOWERCASE_REQUIRED = () => `React drops this \`data-*\` prop because of its capital letters.`;
const UNKNOWN_PROP_WITH_STANDARD_NAME = () => `React ignores this prop because it doesn't recognize the name.`;
const UNKNOWN_PROP_GENERIC = "React ignores this prop because it doesn't recognize the name.";
const noUnknownProperty = defineRule({
	id: "no-unknown-property",
	title: "Unknown DOM property",
	severity: "warn",
	recommendation: "Use the prop name React expects, like `className`, `htmlFor`, or `tabIndex`.",
	create: (context) => {
		const { ignore = [], requireDataLowercase = false } = resolveSettings$10(context.settings);
		const ignoreSet = new Set(ignore);
		if (isNextjsMetadataImageRouteFilename(context.filename)) ignoreSet.add("tw");
		let fileIsNonReactJsx = false;
		return {
			Program(node) {
				fileIsNonReactJsx = fileImportsNonReactJsxDialect(node);
			},
			JSXOpeningElement(node) {
				if (!fileIsNonReactJsx) for (const attribute of node.attributes) {
					if (!isNodeOfType(attribute, "JSXAttribute")) continue;
					if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
					if (attribute.name.name !== "classList") continue;
					const value = attribute.value;
					if (!isNodeOfType(value, "JSXExpressionContainer")) continue;
					if (!isNodeOfType(value.expression, "ObjectExpression")) continue;
					fileIsNonReactJsx = true;
					break;
				}
				if (fileIsNonReactJsx) return;
				if (!isNodeOfType(node.name, "JSXIdentifier")) return;
				const elementType = node.name.name;
				const firstCharacter = elementType.charCodeAt(0);
				if (!(firstCharacter >= 97 && firstCharacter <= 122) || elementType === "fbt" || elementType === "fbs") return;
				let isValidHtmlTag = matchesHtmlTagConventions(elementType);
				if (isValidHtmlTag) for (const attribute of node.attributes) {
					if (!isNodeOfType(attribute, "JSXAttribute")) continue;
					if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
					if (attribute.name.name === "is") {
						isValidHtmlTag = false;
						break;
					}
				}
				for (const attribute of node.attributes) {
					if (!isNodeOfType(attribute, "JSXAttribute")) continue;
					const actualName = getJsxAttributeName(attribute.name);
					if (!actualName) continue;
					if (ignoreSet.has(actualName)) continue;
					if (isValidDataAttribute(actualName)) {
						if (requireDataLowercase && hasUppercaseChar(actualName)) context.report({
							node: attribute.name,
							message: DATA_LOWERCASE_REQUIRED()
						});
						continue;
					}
					if (isValidDomAriaProperty(actualName)) continue;
					if (!isValidHtmlTag) continue;
					const normalizedName = normalizeAttributeCase(actualName);
					const allowedTags = DOM_PROPERTY_TO_ALLOWED_TAGS.get(normalizedName);
					if (allowedTags) {
						if (!allowedTags.has(elementType)) context.report({
							node: attribute.name,
							message: INVALID_PROP_ON_TAG(actualName, [...allowedTags].join(", "))
						});
						continue;
					}
					if (DOM_PROPERTY_NAMES.has(normalizedName)) continue;
					const lowercased = normalizedName.toLowerCase();
					if (DOM_PROPERTY_NAMES_LOWER.get(lowercased) ?? DOM_ATTRIBUTES_TO_CAMEL.get(normalizedName)) context.report({
						node: attribute.name,
						message: UNKNOWN_PROP_WITH_STANDARD_NAME()
					});
					else context.report({
						node: attribute.name,
						message: UNKNOWN_PROP_GENERIC
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/no-unsafe.ts
const UNSAFE_PREFIXED = new Set([
	"UNSAFE_componentWillMount",
	"UNSAFE_componentWillReceiveProps",
	"UNSAFE_componentWillUpdate"
]);
const UNSAFE_ALIASES = new Set([
	"componentWillMount",
	"componentWillReceiveProps",
	"componentWillUpdate"
]);
const buildMessage$6 = (methodName) => `\`${methodName}\` causes subtle bugs & React is removing it.`;
const resolveSettings$9 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { checkAliases: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noUnsafe ?? {} : {}).checkAliases ?? false };
};
const getConfiguredReactMajorMinor = (settings) => {
	if (!settings || typeof settings !== "object") return null;
	const reactBlock = settings.react;
	if (!reactBlock || typeof reactBlock !== "object") return null;
	const version = reactBlock.version;
	if (typeof version !== "string") return null;
	const match = version.match(/^(\d+)\.(\d+)/);
	if (!match) return null;
	return {
		major: Number(match[1]),
		minor: Number(match[2])
	};
};
const isReactVersionAtLeast = (reactVersion, major, minor) => {
	if (!reactVersion) return true;
	if (reactVersion.major > major) return true;
	if (reactVersion.major < major) return false;
	return reactVersion.minor >= minor;
};
const getStaticKeyName = (key) => {
	if (isNodeOfType(key, "Identifier")) return key.name;
	if (isNodeOfType(key, "Literal") && typeof key.value === "string") return key.value;
	return null;
};
const noUnsafe = defineRule({
	id: "no-unsafe",
	title: "Unsafe legacy lifecycle method",
	severity: "warn",
	recommendation: "Replace `UNSAFE_componentWillMount` / `…WillReceiveProps` / `…WillUpdate` with the modern equivalents.",
	create: (context) => {
		const { checkAliases } = resolveSettings$9(context.settings);
		const flagsUnsafePrefix = isReactVersionAtLeast(getConfiguredReactMajorMinor(context.settings), 16, 3);
		const matchesActiveUnsafe = (name) => {
			if (UNSAFE_PREFIXED.has(name)) return flagsUnsafePrefix;
			if (UNSAFE_ALIASES.has(name)) return checkAliases;
			return false;
		};
		return {
			MethodDefinition(node) {
				const name = getStaticKeyName(node.key);
				if (!name || !matchesActiveUnsafe(name)) return;
				if (!getParentComponent(node)) return;
				context.report({
					node: node.key,
					message: buildMessage$6(name)
				});
			},
			Property(node) {
				const name = getStaticKeyName(node.key);
				if (!name || !matchesActiveUnsafe(name)) return;
				let ancestor = node.parent;
				while (ancestor) {
					if (isEs5Component(ancestor)) {
						context.report({
							node: node.key,
							message: buildMessage$6(name)
						});
						return;
					}
					ancestor = ancestor.parent ?? null;
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/no-unstable-nested-components.ts
const buildMessage$5 = (parentName) => {
	let message = "Your users lose this component's state on every render because it's defined inside another component";
	if (parentName) message += ` (\`${parentName}\`)`;
	message += ".";
	return message;
};
const NESTED_FUNCTION_TYPES = new Set([
	"FunctionDeclaration",
	"FunctionExpression",
	"ArrowFunctionExpression",
	"ClassDeclaration",
	"ClassExpression"
]);
const resolveSettings$8 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noUnstableNestedComponents ?? {} : {};
	return {
		allowAsProps: ruleSettings.allowAsProps ?? true,
		customValidators: ruleSettings.customValidators ?? [],
		propNamePattern: ruleSettings.propNamePattern ?? "render*"
	};
};
const expressionContainsJsxOrCreateElement = (root) => {
	let found = false;
	const visit = (node) => {
		if (found) return;
		if (node.type === "JSXElement" || node.type === "JSXFragment") {
			found = true;
			return;
		}
		if (isNodeOfType(node, "CallExpression") && isCreateElementCall(node)) {
			found = true;
			return;
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) for (const item of child) {
				if (!isAstNode(item)) continue;
				if (NESTED_FUNCTION_TYPES.has(item.type)) continue;
				visit(item);
				if (found) return;
			}
			else if (isAstNode(child)) {
				if (NESTED_FUNCTION_TYPES.has(child.type)) continue;
				visit(child);
			}
			if (found) return;
		}
	};
	visit(root);
	return found;
};
const classExtendsReactComponent$1 = (classNode) => {
	const superClass = classNode.superClass;
	if (!superClass) return false;
	if (isNodeOfType(superClass, "Identifier") && (superClass.name === "Component" || superClass.name === "PureComponent")) return true;
	if (isNodeOfType(superClass, "MemberExpression") && isNodeOfType(superClass.object, "Identifier") && superClass.object.name === "React" && isNodeOfType(superClass.property, "Identifier") && (superClass.property.name === "Component" || superClass.property.name === "PureComponent")) return true;
	return false;
};
const isReactClassComponent = (classNode) => {
	if (classExtendsReactComponent$1(classNode)) return true;
	return expressionContainsJsxOrCreateElement(classNode);
};
const findEnclosingComponent = (node) => {
	let walker = node.parent;
	while (walker) {
		if (isFunctionLike$2(walker)) {
			const componentName = inferFunctionLikeName(walker);
			if (componentName && isReactComponentName(componentName) && expressionContainsJsxOrCreateElement(walker)) return {
				component: walker,
				name: componentName
			};
			if (!componentName && expressionContainsJsxOrCreateElement(walker) && walker.parent && isNodeOfType(walker.parent, "ExportDefaultDeclaration")) return {
				component: walker,
				name: null
			};
		}
		if (isNodeOfType(walker, "ClassDeclaration") || isNodeOfType(walker, "ClassExpression")) {
			if (walker.id && isReactComponentName(walker.id.name) && isReactClassComponent(walker)) return {
				component: walker,
				name: walker.id.name
			};
		}
		walker = walker.parent ?? null;
	}
	return null;
};
const inferFunctionLikeName = (functionLike) => {
	if ((isNodeOfType(functionLike, "FunctionDeclaration") || isNodeOfType(functionLike, "FunctionExpression")) && functionLike.id) return functionLike.id.name;
	const parent = functionLike.parent;
	if (parent && isNodeOfType(parent, "VariableDeclarator")) {
		if (isNodeOfType(parent.id, "Identifier")) return parent.id.name;
	}
	if (parent && isNodeOfType(parent, "Property")) {
		if (isNodeOfType(parent.key, "Identifier")) return parent.key.name;
		if (isNodeOfType(parent.key, "Literal") && typeof parent.key.value === "string") return parent.key.value;
	}
	if (parent && isNodeOfType(parent, "AssignmentExpression")) {
		const left = parent.left;
		if (isNodeOfType(left, "Identifier")) return left.name;
		if (isNodeOfType(left, "MemberExpression") && isNodeOfType(left.property, "Identifier")) return left.property.name;
	}
	return null;
};
const isComponentDeclaredInProp = (candidateNode) => {
	let walker = candidateNode.parent;
	while (walker) {
		if (isNodeOfType(walker, "Property")) {
			const propName = isNodeOfType(walker.key, "Identifier") ? walker.key.name : isNodeOfType(walker.key, "Literal") && typeof walker.key.value === "string" ? walker.key.value : null;
			let propertyWalker = walker.parent;
			while (propertyWalker) {
				if (isNodeOfType(propertyWalker, "JSXExpressionContainer")) {
					const attribute = propertyWalker.parent;
					if (attribute && isNodeOfType(attribute, "JSXAttribute")) {
						const attributeName = attribute.name;
						if (isNodeOfType(attributeName, "JSXIdentifier")) return { propName: attributeName.name };
					}
					return propName ? { propName } : null;
				}
				if (isNodeOfType(propertyWalker, "CallExpression")) return propName ? { propName } : null;
				propertyWalker = propertyWalker.parent ?? null;
			}
			return propName ? { propName } : null;
		}
		if (isNodeOfType(walker, "JSXExpressionContainer")) {
			const grandparent = walker.parent;
			if (grandparent && isNodeOfType(grandparent, "JSXAttribute")) {
				const attributeName = grandparent.name;
				if (isNodeOfType(attributeName, "JSXIdentifier")) return { propName: attributeName.name };
			}
			return null;
		}
		if (isNodeOfType(walker, "CallExpression")) return null;
		walker = walker.parent ?? null;
	}
	return null;
};
const HOC_CALLEE_NAMES = new Set([
	"memo",
	"forwardRef",
	"createReactClass",
	"createClass",
	"lazy",
	"observer",
	"Observer",
	"compose"
]);
const isHocCallee$1 = (call) => {
	const callee = call.callee;
	if (isNodeOfType(callee, "Identifier")) return HOC_CALLEE_NAMES.has(callee.name);
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) return HOC_CALLEE_NAMES.has(callee.property.name);
	return false;
};
const isObjectCallbackCandidate = (node) => {
	const parent = node.parent;
	if (!parent || !isNodeOfType(parent, "Property")) return false;
	const keyName = isNodeOfType(parent.key, "Identifier") ? parent.key.name : isNodeOfType(parent.key, "Literal") && typeof parent.key.value === "string" ? parent.key.value : null;
	if (keyName && keyName.startsWith("render")) return false;
	let walker = parent.parent;
	while (walker) {
		if (isNodeOfType(walker, "JSXExpressionContainer")) return false;
		if (isNodeOfType(walker, "CallExpression")) return true;
		if (isNodeOfType(walker, "ArrayExpression")) return true;
		walker = walker.parent ?? null;
	}
	return false;
};
const hocCallContainsComponent = (call) => {
	const firstArgument = call.arguments[0];
	if (!firstArgument) return false;
	if (isNodeOfType(firstArgument, "FunctionExpression") || isNodeOfType(firstArgument, "ArrowFunctionExpression") || isNodeOfType(firstArgument, "ClassExpression")) return expressionContainsJsxOrCreateElement(firstArgument);
	if (isNodeOfType(firstArgument, "CallExpression") && isHocCallee$1(firstArgument)) return hocCallContainsComponent(firstArgument);
	return expressionContainsJsxOrCreateElement(firstArgument);
};
const isFirstArgumentOfHocCall = (node) => {
	const parent = node.parent;
	if (!parent) return false;
	if (!isNodeOfType(parent, "CallExpression")) return false;
	if (!isHocCallee$1(parent)) return false;
	return parent.arguments[0] === node;
};
const isReturnOfMapCallback = (node) => {
	const parent = node.parent;
	if (!parent) return false;
	if (isNodeOfType(parent, "CallExpression")) {
		const callee = parent.callee;
		if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) return [
			"map",
			"forEach",
			"filter",
			"flatMap",
			"reduce",
			"reduceRight"
		].includes(callee.property.name);
	}
	if (isNodeOfType(parent, "ArrowFunctionExpression") || isNodeOfType(parent, "FunctionExpression")) {
		const callbackParent = parent.parent;
		if (callbackParent && isNodeOfType(callbackParent, "CallExpression")) {
			const callee = callbackParent.callee;
			if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) return [
				"map",
				"forEach",
				"filter",
				"flatMap",
				"reduce",
				"reduceRight"
			].includes(callee.property.name);
		}
	}
	return false;
};
const noUnstableNestedComponents = defineRule({
	id: "no-unstable-nested-components",
	title: "Component defined inside a component",
	severity: "warn",
	recommendation: "Move nested components to the top of the file. Never define one inside another.",
	category: "Performance",
	create: (context) => {
		const settings = resolveSettings$8(context.settings);
		const renderPropRegex = compileGlob(settings.propNamePattern);
		const reportCandidate = (candidateNode, reportNode, candidateName) => {
			if (isFirstArgumentOfHocCall(candidateNode)) return;
			if (isReturnOfMapCallback(candidateNode)) return;
			const propInfo = isComponentDeclaredInProp(candidateNode);
			if (propInfo) {
				if (propInfo.propName === "children") return;
				if (renderPropRegex.test(propInfo.propName)) return;
				if (settings.allowAsProps) return;
			}
			const enclosing = findEnclosingComponent(candidateNode);
			if (!enclosing) return;
			context.report({
				node: reportNode,
				message: buildMessage$5(enclosing.name)
			});
		};
		const checkFunctionLike = (node) => {
			if (!expressionContainsJsxOrCreateElement(node)) return;
			const inferredName = inferFunctionLikeName(node);
			const propInfo = isComponentDeclaredInProp(node);
			if (!(inferredName !== null && isReactComponentName(inferredName) || propInfo !== null || isObjectCallbackCandidate(node))) return;
			reportCandidate(node, node, inferredName);
		};
		return {
			FunctionDeclaration: checkFunctionLike,
			FunctionExpression: checkFunctionLike,
			ArrowFunctionExpression: checkFunctionLike,
			ClassDeclaration(node) {
				if (!node.id) return;
				if (!isReactComponentName(node.id.name)) return;
				if (!isReactClassComponent(node)) return;
				reportCandidate(node, node, node.id.name);
			},
			ClassExpression(node) {
				const inferredName = node.id?.name ?? inferFunctionLikeName(node);
				if (!inferredName || !isReactComponentName(inferredName)) return;
				if (!isReactClassComponent(node)) return;
				reportCandidate(node, node, inferredName);
			},
			CallExpression(node) {
				if (!isHocCallee$1(node)) return;
				if (!hocCallContainsComponent(node)) return;
				reportCandidate(node, node, null);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/performance/no-usememo-simple-expression.ts
const isSimpleExpression = (node) => {
	if (!node) return false;
	switch (node.type) {
		case "Identifier":
		case "Literal":
		case "TemplateLiteral": return true;
		case "BinaryExpression": return isSimpleExpression(node.left) && isSimpleExpression(node.right);
		case "UnaryExpression": return isSimpleExpression(node.argument);
		case "MemberExpression": return !node.computed && isSimpleExpression(node.object);
		case "ConditionalExpression": return isSimpleExpression(node.test) && isSimpleExpression(node.consequent) && isSimpleExpression(node.alternate);
		default: return false;
	}
};
const isTriviallyCheapExpression = (node) => {
	if (!node) return false;
	if (!isSimpleExpression(node)) return false;
	if (isNodeOfType(node, "Identifier")) return false;
	if (isNodeOfType(node, "MemberExpression")) return false;
	return true;
};
const noUsememoSimpleExpression = defineRule({
	id: "no-usememo-simple-expression",
	title: "useMemo on a cheap value",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Remove the useMemo. Property reads, math, and ternaries are already fast, so wrapping them doesn't help",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, "useMemo")) return;
		if (isNodeOfType(node.callee, "MemberExpression")) {
			const namespaceIdentifier = node.callee.object;
			if (isNodeOfType(namespaceIdentifier, "Identifier")) {
				const namespaceName = namespaceIdentifier.name;
				if (!isCanonicalReactNamespaceName(namespaceName) && !isImportedFromModule(namespaceIdentifier, namespaceName, "react")) return;
			}
		}
		const callback = node.arguments?.[0];
		if (!callback) return;
		if (!isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) return;
		let returnExpression = null;
		if (!isNodeOfType(callback.body, "BlockStatement")) returnExpression = callback.body;
		else if (callback.body.body?.length === 1 && isNodeOfType(callback.body.body[0], "ReturnStatement")) returnExpression = callback.body.body[0].argument;
		if (returnExpression && isTriviallyCheapExpression(returnExpression)) context.report({
			node,
			message: "This costs more than it saves because useMemo is wrapping a value that's already cheap, so remove the useMemo"
		});
	} })
});
//#endregion
//#region src/plugin/rules/design/no-wide-letter-spacing.ts
const getJsxAttributeStringValue = (attributeValue) => {
	if (!attributeValue) return null;
	if (isNodeOfType(attributeValue, "Literal") && typeof attributeValue.value === "string") return attributeValue.value;
	if (isNodeOfType(attributeValue, "JSXExpressionContainer")) {
		const expression = attributeValue.expression;
		if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") return expression.value;
	}
	return null;
};
const isTruthyBooleanJsxAttribute = (attributeValue) => {
	if (attributeValue === null) return true;
	if (isNodeOfType(attributeValue, "JSXExpressionContainer")) {
		const expression = attributeValue.expression;
		return isNodeOfType(expression, "Literal") && expression.value === true;
	}
	return false;
};
const hasUppercaseSiblingProp = (styleAttribute) => {
	const openingElement = styleAttribute.parent;
	if (!openingElement || !isNodeOfType(openingElement, "JSXOpeningElement")) return false;
	for (const attribute of openingElement.attributes ?? []) {
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
		const attributeName = attribute.name.name;
		if (attributeName === "uppercase" && isTruthyBooleanJsxAttribute(attribute.value)) return true;
		if (attributeName === "textTransform" && getJsxAttributeStringValue(attribute.value) === "uppercase") return true;
	}
	return false;
};
const noWideLetterSpacing = defineRule({
	id: "no-wide-letter-spacing",
	title: "Wide letter spacing on body text",
	severity: "warn",
	defaultEnabled: false,
	tags: ["test-noise"],
	recommendation: "Save wide letter-spacing (over 0.05em) for short uppercase labels, nav items, and buttons, not body text.",
	create: (context) => ({ JSXAttribute(node) {
		const expression = getInlineStyleExpression(node);
		if (!expression) return;
		if (hasUppercaseSiblingProp(node)) return;
		let isUppercase = false;
		let letterSpacingProperty = null;
		let letterSpacingEm = null;
		for (const property of expression.properties ?? []) {
			const key = getStylePropertyKey(property);
			if (!key) continue;
			if (key === "textTransform") {
				if (getStylePropertyStringValue(property) === "uppercase") isUppercase = true;
			}
			if (key === "letterSpacing") {
				letterSpacingProperty = property;
				const strValue = getStylePropertyStringValue(property);
				const numValue = getStylePropertyNumberValue(property);
				if (strValue) {
					const emMatch = strValue.match(/^([\d.]+)em$/);
					if (emMatch) letterSpacingEm = parseFloat(emMatch[1]);
					const pxMatch = strValue.match(/^([\d.]+)px$/);
					if (pxMatch) letterSpacingEm = parseFloat(pxMatch[1]) / 16;
				}
				if (numValue !== null && numValue > 0) letterSpacingEm = numValue / 16;
			}
		}
		if (!isUppercase && letterSpacingProperty && letterSpacingEm !== null && letterSpacingEm > .05) context.report({
			node: letterSpacingProperty,
			message: `Your users find body text harder to read at ${letterSpacingEm.toFixed(2)}em letter spacing, so save wide spacing for short uppercase labels.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/no-will-update-set-state.ts
const LIFECYCLE_NAMES = new Set(["componentWillUpdate", "UNSAFE_componentWillUpdate"]);
const MESSAGE$9 = "This can loop forever & freeze the component.";
const resolveSettings$7 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { mode: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.noWillUpdateSetState ?? {} : {}).mode ?? "allowed" };
};
const isReactBelow16_3 = (settings) => {
	if (!settings || typeof settings !== "object") return false;
	const reactBlock = settings.react;
	if (!reactBlock || typeof reactBlock !== "object") return false;
	const version = reactBlock.version;
	if (typeof version !== "string") return false;
	const match = version.match(/^(\d+)\.(\d+)/);
	if (!match) return false;
	const major = Number(match[1]);
	const minor = Number(match[2]);
	if (major < 16) return true;
	if (major === 16 && minor < 3) return true;
	return false;
};
const noWillUpdateSetState = defineRule({
	id: "no-will-update-set-state",
	title: "setState in componentWillUpdate",
	severity: "warn",
	recommendation: "Don't set state in `componentWillUpdate`. Use `getDerivedStateFromProps` or `componentDidUpdate` instead.",
	create: (context) => {
		const { mode } = resolveSettings$7(context.settings);
		const activeLifecycleNames = isReactBelow16_3(context.settings) ? new Set(["componentWillUpdate"]) : LIFECYCLE_NAMES;
		return { CallExpression(node) {
			if (!isNodeOfType(node.callee, "MemberExpression")) return;
			if (!isNodeOfType(node.callee.object, "ThisExpression")) return;
			if (!isNodeOfType(node.callee.property, "Identifier") || node.callee.property.name !== "setState") return;
			if (!isSetStateCallInLifecycle(node, activeLifecycleNames, { disallowInNestedFunctions: mode === "disallow-in-func" })) return;
			context.report({
				node: node.callee,
				message: MESSAGE$9
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/design/no-z-index9999.ts
const noZIndex9999 = defineRule({
	id: "no-z-index-9999",
	title: "Excessively high z-index",
	tags: ["test-noise"],
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Pick a small z-index scale, like dropdown 10, modal 20, toast 30. To layer something on top, use `isolation: isolate` instead of bigger numbers.",
	create: (context) => ({
		JSXAttribute(node) {
			const expression = getInlineStyleExpression(node);
			if (!expression) return;
			for (const property of expression.properties ?? []) {
				if (getStylePropertyKey(property) !== "zIndex") continue;
				const zValue = getStylePropertyNumberValue(property);
				if (zValue !== null && Math.abs(zValue) >= 1e3) context.report({
					node: property,
					message: `z-index ${zValue} is way too high & usually hides a layering bug instead of fixing it, so use a small set scale, like 1 to 50.`
				});
			}
		},
		CallExpression(node) {
			if (!isNodeOfType(node.callee, "MemberExpression")) return;
			if (!isNodeOfType(node.callee.property, "Identifier") || node.callee.property.name !== "create") return;
			if (!isNodeOfType(node.callee.object, "Identifier") || node.callee.object.name !== "StyleSheet") return;
			const argument = node.arguments?.[0];
			if (!argument || !isNodeOfType(argument, "ObjectExpression")) return;
			walkAst(argument, (child) => {
				if (!isNodeOfType(child, "Property")) return;
				if (getStylePropertyKey(child) !== "zIndex") return;
				if (isNodeOfType(child.value, "Literal") && typeof child.value.value === "number") {
					const zValue = child.value.value;
					if (Math.abs(zValue) >= 1e3) context.report({
						node: child,
						message: `z-index ${zValue} is way too high & usually hides a layering bug instead of fixing it, so use a small set scale, like 1 to 50.`
					});
				}
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/react-builtins/only-export-components-tables.ts
const NOT_REACT_COMPONENT_EXPRESSION_TYPES = new Set([
	"ArrayExpression",
	"AwaitExpression",
	"BinaryExpression",
	"ChainExpression",
	"ConditionalExpression",
	"Literal",
	"LogicalExpression",
	"ObjectExpression",
	"TemplateLiteral",
	"ThisExpression",
	"UnaryExpression",
	"UpdateExpression"
]);
const NON_FAST_REFRESH_PATH_SEGMENTS = [
	"/test/",
	"/tests/",
	"/__tests__/",
	"/__test__/",
	"/__fixtures__/",
	"/fixtures/",
	"/__mocks__/",
	"/mocks/",
	"/cypress/",
	"/.storybook/",
	"/stories/",
	"/__stories__/",
	"/playground/",
	"/playgrounds/",
	"/examples/",
	"/example/",
	"/demo/",
	"/demos/",
	"/sandbox/",
	"/sandboxes/"
];
const ENTRY_POINT_BASENAMES = new Set([
	"main.tsx",
	"main.jsx",
	"main.js",
	"index.tsx",
	"index.jsx",
	"entry.tsx",
	"entry.jsx",
	"bootstrap.tsx",
	"bootstrap.jsx",
	"client.tsx",
	"client.jsx",
	"server.tsx",
	"server.jsx",
	"page.tsx",
	"page.jsx",
	"layout.tsx",
	"layout.jsx",
	"loading.tsx",
	"loading.jsx",
	"error.tsx",
	"error.jsx",
	"not-found.tsx",
	"not-found.jsx",
	"template.tsx",
	"template.jsx",
	"default.tsx",
	"default.jsx",
	"global-error.tsx",
	"global-error.jsx",
	"route.tsx",
	"route.jsx",
	"_layout.tsx",
	"_layout.jsx",
	"_app.tsx",
	"_app.jsx",
	"_document.tsx",
	"_document.jsx",
	"_error.tsx",
	"_error.jsx",
	"app.tsx",
	"app.jsx",
	"App.tsx",
	"App.jsx"
]);
const UTILITY_FILE_BASENAMES = new Set([
	"utils.tsx",
	"utils.jsx",
	"util.tsx",
	"util.jsx",
	"helpers.tsx",
	"helpers.jsx",
	"helper.tsx",
	"helper.jsx",
	"shared.tsx",
	"shared.jsx",
	"common.tsx",
	"common.jsx",
	"lib.tsx",
	"lib.jsx",
	"nodeTypes.tsx",
	"nodeTypes.jsx",
	"node-types.tsx",
	"node-types.jsx",
	"edgeTypes.tsx",
	"edgeTypes.jsx",
	"edge-types.tsx",
	"edge-types.jsx",
	"cellTypes.tsx",
	"cellTypes.jsx",
	"columnTypes.tsx",
	"columnTypes.jsx",
	"columnDefs.tsx",
	"columnDefs.jsx",
	"columnRenderers.tsx",
	"columnRenderers.jsx",
	"columns.tsx",
	"columns.jsx",
	"mappings.tsx",
	"mappings.jsx",
	"mapping.tsx",
	"mapping.jsx",
	"lookups.tsx",
	"lookups.jsx",
	"lookup.tsx",
	"lookup.jsx",
	"registry.tsx",
	"registry.jsx",
	"toast.tsx",
	"toast.jsx",
	"toaster.tsx",
	"toaster.jsx",
	"theme.tsx",
	"theme.jsx",
	"tokens.tsx",
	"tokens.jsx",
	"palette.tsx",
	"palette.jsx",
	"colors.tsx",
	"colors.jsx",
	"colours.tsx",
	"colours.jsx",
	"constants.tsx",
	"constants.jsx",
	"enums.tsx",
	"enums.jsx",
	"types.tsx",
	"types.jsx",
	"schemas.tsx",
	"schemas.jsx",
	"schema.tsx",
	"schema.jsx",
	"definitions.tsx",
	"definitions.jsx",
	"config.tsx",
	"config.jsx",
	"defaults.tsx",
	"defaults.jsx"
]);
//#endregion
//#region src/plugin/rules/react-builtins/only-export-components.ts
const NAMED_EXPORT_MESSAGE = "Fast Refresh stops working when a file exports non-components.";
const ANONYMOUS_MESSAGE = "Fast Refresh can't track an unnamed component & full-reloads instead.";
const EXPORT_ALL_MESSAGE = "`export *` hides what's exported, so Fast Refresh stops working.";
const REACT_CONTEXT_MESSAGE = "Fast Refresh stops working when a file exports a context too.";
const LOCAL_COMPONENT_MESSAGE = "Fast Refresh skips this component because it isn't exported.";
const NO_EXPORT_MESSAGE = "Fast Refresh can't track this component because the file exports nothing.";
const DEFAULT_REACT_HOCS = [
	"memo",
	"forwardRef",
	"lazy"
];
const resolveSettings$6 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.onlyExportComponents ?? {} : {};
	return {
		allowExportNames: ruleSettings.allowExportNames ?? [],
		allowConstantExport: ruleSettings.allowConstantExport ?? true,
		customHOCs: ruleSettings.customHOCs ?? [],
		checkJS: ruleSettings.checkJS ?? false
	};
};
const skipTsExpression = (expression) => {
	if (expression.type === "TSAsExpression" || expression.type === "TSSatisfiesExpression" || expression.type === "TSNonNullExpression") return skipTsExpression(expression.expression);
	return expression;
};
const classExtendsReactComponent = (classNode) => {
	const superClass = classNode.superClass;
	if (!superClass) return false;
	if (isNodeOfType(superClass, "Identifier") && (superClass.name === "Component" || superClass.name === "PureComponent")) return true;
	if (isNodeOfType(superClass, "MemberExpression") && isNodeOfType(superClass.object, "Identifier") && superClass.object.name === "React" && isNodeOfType(superClass.property, "Identifier") && (superClass.property.name === "Component" || superClass.property.name === "PureComponent")) return true;
	return false;
};
const isReactCreateContext = (initializer) => {
	if (!initializer) return false;
	const expression = skipTsExpression(initializer);
	if (!isNodeOfType(expression, "CallExpression")) return false;
	const callee = expression.callee;
	if (isNodeOfType(callee, "Identifier") && callee.name === "createContext") return true;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && callee.property.name === "createContext") return true;
	return false;
};
const isReactHocName = (name, state) => state.customHocs.has(name);
const isHocCallee = (callee, state) => {
	if (isNodeOfType(callee, "Identifier")) return isReactHocName(callee.name, state);
	if (isNodeOfType(callee, "MemberExpression")) {
		if (isNodeOfType(callee.property, "Identifier") && isReactHocName(callee.property.name, state)) return true;
		if (isNodeOfType(callee.object, "Identifier") && isReactHocName(callee.object.name, state)) return true;
		if (isNodeOfType(callee.object, "CallExpression") && isHocCallee(callee.object.callee, state)) return true;
		return false;
	}
	if (isNodeOfType(callee, "CallExpression")) {
		if (isNodeOfType(callee.callee, "Identifier") && callee.callee.name === "connect") return true;
		return isHocCallee(callee.callee, state);
	}
	return false;
};
const canBeReactFunctionComponent = (initializer, state) => {
	if (!initializer) return false;
	const expression = skipTsExpression(initializer);
	if (isNodeOfType(expression, "ArrowFunctionExpression") || isNodeOfType(expression, "FunctionExpression")) return true;
	if (isNodeOfType(expression, "CallExpression")) return isHocCallee(expression.callee, state);
	return false;
};
const isReactComponentInitializer = (expression, state) => {
	const stripped = skipTsExpression(expression);
	if (isNodeOfType(stripped, "ArrowFunctionExpression")) return true;
	if (isNodeOfType(stripped, "FunctionExpression")) return Boolean(stripped.id);
	if (isNodeOfType(stripped, "Identifier")) return isReactComponentName(stripped.name);
	if (isNodeOfType(stripped, "CallExpression") && isHocCallee(stripped.callee, state) && stripped.arguments.length > 0) return true;
	return false;
};
const classifyExport = (name, reportNode, isFunction, initializer, state) => {
	if (initializer) {
		const expression = skipTsExpression(initializer);
		if (isNodeOfType(expression, "CallExpression") && isHocCallee(expression.callee, state) && expression.arguments.length > 0 && isReactComponentName(name)) return { kind: "react-component" };
		if (isNodeOfType(expression, "ConditionalExpression") && isReactComponentName(name) && isReactComponentInitializer(expression.consequent, state) && isReactComponentInitializer(expression.alternate, state)) return { kind: "react-component" };
	}
	if (state.allowExportNames.has(name)) return { kind: "allowed" };
	if (/^use[A-Z]/.test(name)) return { kind: "allowed" };
	if (state.allowConstantExport && initializer) {
		const expression = skipTsExpression(initializer);
		if (isNodeOfType(expression, "Literal") || isNodeOfType(expression, "TemplateLiteral") || isNodeOfType(expression, "UnaryExpression") && isNodeOfType(expression.argument, "Literal") || isNodeOfType(expression, "BinaryExpression")) return { kind: "allowed" };
	}
	if (isFunction) return isReactComponentName(name) ? { kind: "react-component" } : {
		kind: "non-component",
		reportNode
	};
	if (initializer) {
		const stripped = skipTsExpression(initializer);
		if (isNodeOfType(stripped, "CallExpression")) {
			if (isReactCreateContext(stripped)) return {
				kind: "react-context",
				reportNode
			};
			return {
				kind: "non-component",
				reportNode
			};
		}
		if (NOT_REACT_COMPONENT_EXPRESSION_TYPES.has(stripped.type)) return {
			kind: "non-component",
			reportNode
		};
	}
	return isReactComponentName(name) ? { kind: "react-component" } : {
		kind: "non-component",
		reportNode
	};
};
const collectAllNodes = (programRoot) => {
	const out = [];
	const visit = (node) => {
		out.push(node);
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(programRoot);
	return out;
};
const isEntryPointFile = (filename) => {
	const lastSlash = Math.max(filename.lastIndexOf("/"), filename.lastIndexOf("\\"));
	const basename = lastSlash === -1 ? filename : filename.slice(lastSlash + 1);
	return ENTRY_POINT_BASENAMES.has(basename);
};
const ASSET_FILE_BASENAME_PATTERN = /^([A-Za-z][\w-]*[-._])?(icons?|svgs?|svg[-_]?sprites?|sprites?|emojis?|flags?|logos?|lockups?|illustrations?|glyphs?|stickers?|emotes?|avatars?|backgrounds?|patterns?|assets?|gradients?|countryVectors?|paymentVectors?|brandVectors?|brandLogos?)\.(t|j)sx?$/;
const UTILITY_BASENAME_SUFFIX_PATTERN = /^[A-Za-z][\w-]*(Utils|Util|Helpers|Helper|Shared|Constants|Constant|Types|Type|Mappings|Mapping|Lookups|Lookup|Registry|Renderers|Renderer|NodeTypes|EdgeTypes|CellTypes|ColumnDefs|ColumnTypes|ColumnRenderers|Schemas|Schema|Definitions|Definition|Config|Configuration|Defaults|Default|Tokens|Palette|Context|Provider|Providers|Logic|Scene|Page|Layout)\.(t|j)sx?$/;
const HOOK_FILE_BASENAME_PATTERN = /^use[A-Z][\w-]*\.(t|j)sx?$/;
const NODE_DEFINITION_BASENAME_PATTERN = /^[A-Z][\w-]*(NodeUtil|ShapeUtil|EdgeUtil|BindingUtil|InlineNode|BlockNode|NotebookNode)\.(t|j)sx?$/;
const isAssetOrUtilityFile = (filename) => {
	const lastSlash = Math.max(filename.lastIndexOf("/"), filename.lastIndexOf("\\"));
	const basename = lastSlash === -1 ? filename : filename.slice(lastSlash + 1);
	if (ASSET_FILE_BASENAME_PATTERN.test(basename)) return true;
	if (UTILITY_FILE_BASENAMES.has(basename)) return true;
	if (UTILITY_BASENAME_SUFFIX_PATTERN.test(basename)) return true;
	if (HOOK_FILE_BASENAME_PATTERN.test(basename)) return true;
	if (NODE_DEFINITION_BASENAME_PATTERN.test(basename)) return true;
	return false;
};
const isFileNameAllowed = (filename, checkJS) => {
	if (!filename) return true;
	if (filename.includes(".test.") || filename.includes(".spec.") || filename.includes(".cy.") || filename.includes(".stories.")) return false;
	for (const segment of NON_FAST_REFRESH_PATH_SEGMENTS) if (filename.includes(segment)) return false;
	if (isEntryPointFile(filename)) return false;
	if (isAssetOrUtilityFile(filename)) return false;
	if (filename.endsWith(".tsx") || filename.endsWith(".jsx")) return true;
	if (checkJS && filename.endsWith(".js")) return true;
	return false;
};
const onlyExportComponents = defineRule({
	id: "only-export-components",
	title: "Non-component export in component file",
	severity: "warn",
	recommendation: "Move non-component exports out of files that export components.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$6(context.settings);
		const state = {
			customHocs: new Set([...DEFAULT_REACT_HOCS, ...settings.customHOCs]),
			allowExportNames: new Set(settings.allowExportNames),
			allowConstantExport: settings.allowConstantExport
		};
		return { Program(node) {
			if (!isFileNameAllowed(normalizeFilename$1(context.filename ?? ""), settings.checkJS)) return;
			const allNodes = collectAllNodes(node);
			const exports = [];
			let hasReactExport = false;
			let hasAnyExports = false;
			const localComponents = [];
			const isExportedNodeIds = /* @__PURE__ */ new WeakSet();
			for (const child of allNodes) {
				if (isNodeOfType(child, "ExportAllDeclaration")) {
					if (child.exportKind === "type") continue;
					hasAnyExports = true;
					context.report({
						node: child,
						message: EXPORT_ALL_MESSAGE
					});
					continue;
				}
				if (isNodeOfType(child, "ExportDefaultDeclaration")) {
					hasAnyExports = true;
					const declaration = child.declaration;
					const stripped = skipTsExpression(declaration);
					if (isNodeOfType(stripped, "FunctionDeclaration") || isNodeOfType(stripped, "FunctionExpression")) {
						if (stripped.id) {
							const idNode = stripped.id;
							isExportedNodeIds.add(stripped);
							exports.push(classifyExport(idNode.name, idNode, true, null, state));
						} else {
							context.report({
								node: stripped,
								message: ANONYMOUS_MESSAGE
							});
							hasReactExport = true;
						}
						continue;
					}
					if (isNodeOfType(stripped, "ClassDeclaration") || isNodeOfType(stripped, "ClassExpression")) {
						if (stripped.id) {
							const idNode = stripped.id;
							isExportedNodeIds.add(stripped);
							if (isReactComponentName(idNode.name) && classExtendsReactComponent(stripped)) hasReactExport = true;
							else exports.push({
								kind: "non-component",
								reportNode: idNode
							});
						} else context.report({
							node: stripped,
							message: ANONYMOUS_MESSAGE
						});
						continue;
					}
					if (isNodeOfType(stripped, "Identifier")) {
						exports.push(classifyExport(stripped.name, stripped, false, null, state));
						continue;
					}
					if (isNodeOfType(stripped, "CallExpression")) {
						const isHoc = isHocCallee(stripped.callee, state);
						const firstArg = stripped.arguments[0];
						const firstArgIsValid = Boolean(firstArg) && (() => {
							if (!firstArg) return false;
							const expression = skipTsExpression(firstArg);
							if (isNodeOfType(expression, "Identifier")) return true;
							if (isNodeOfType(expression, "FunctionExpression") && expression.id) return true;
							if (isNodeOfType(expression, "CallExpression") && isHocCallee(expression.callee, state)) return true;
							return false;
						})();
						if (isHoc && firstArgIsValid) hasReactExport = true;
						else context.report({
							node: stripped,
							message: ANONYMOUS_MESSAGE
						});
						continue;
					}
					if (isNodeOfType(stripped, "ArrowFunctionExpression") || isNodeOfType(stripped, "ObjectExpression") || isNodeOfType(stripped, "Literal")) {
						context.report({
							node: stripped,
							message: ANONYMOUS_MESSAGE
						});
						continue;
					}
					context.report({
						node: child,
						message: ANONYMOUS_MESSAGE
					});
					continue;
				}
				if (isNodeOfType(child, "ExportNamedDeclaration")) {
					if (child.exportKind === "type") continue;
					hasAnyExports = true;
					if (child.declaration) {
						const declaration = child.declaration;
						if (isNodeOfType(declaration, "FunctionDeclaration") && declaration.id) {
							isExportedNodeIds.add(declaration);
							exports.push(classifyExport(declaration.id.name, declaration.id, true, null, state));
						} else if (isNodeOfType(declaration, "ClassDeclaration") && declaration.id) {
							isExportedNodeIds.add(declaration);
							if (isReactComponentName(declaration.id.name) && classExtendsReactComponent(declaration)) exports.push({ kind: "react-component" });
							else exports.push({
								kind: "non-component",
								reportNode: declaration.id
							});
						} else if (isNodeOfType(declaration, "VariableDeclaration")) for (const declarator of declaration.declarations) {
							if (!isNodeOfType(declarator.id, "Identifier")) continue;
							isExportedNodeIds.add(declarator);
							const isFunction = canBeReactFunctionComponent(declarator.init ?? null, state);
							exports.push(classifyExport(declarator.id.name, declarator.id, isFunction, declarator.init, state));
						}
						else if (declaration.type === "TSEnumDeclaration" || declaration.type === "TSInterfaceDeclaration" || declaration.type === "TSTypeAliasDeclaration") {
							if (declaration.type === "TSEnumDeclaration") exports.push({
								kind: "non-component",
								reportNode: declaration
							});
						}
					}
					for (const specifier of child.specifiers ?? []) {
						if (!isNodeOfType(specifier, "ExportSpecifier")) continue;
						const exported = specifier.exported;
						const local = specifier.local;
						let exportedName = null;
						if (exported && isNodeOfType(exported, "Identifier")) exportedName = exported.name;
						const localName = local && isNodeOfType(local, "Identifier") ? local.name : null;
						const reportNode = specifier;
						if (exportedName === "default" && localName) exports.push(classifyExport(localName, reportNode, false, null, state));
						else if (exportedName) exports.push(classifyExport(exportedName, reportNode, false, null, state));
						else exports.push({
							kind: "non-component",
							reportNode
						});
					}
				}
			}
			for (const entry of exports) if (entry.kind === "react-component") hasReactExport = true;
			const isInsideExport = (node) => {
				let walker = node.parent;
				while (walker) {
					if (isNodeOfType(walker, "ExportNamedDeclaration") || isNodeOfType(walker, "ExportDefaultDeclaration") || isNodeOfType(walker, "ExportAllDeclaration")) return true;
					walker = walker.parent ?? null;
				}
				return false;
			};
			for (const child of allNodes) {
				if (isNodeOfType(child, "FunctionDeclaration") && child.id) {
					if (isReactComponentName(child.id.name) && !isInsideExport(child)) localComponents.push(child.id);
				}
				if (isNodeOfType(child, "VariableDeclarator") && isNodeOfType(child.id, "Identifier")) {
					if (isReactComponentName(child.id.name) && canBeReactFunctionComponent(child.init, state) && !isInsideExport(child)) localComponents.push(child.id);
				}
			}
			if (hasAnyExports && hasReactExport) for (const entry of exports) {
				if (entry.kind === "non-component") context.report({
					node: entry.reportNode,
					message: NAMED_EXPORT_MESSAGE
				});
				if (entry.kind === "react-context") context.report({
					node: entry.reportNode,
					message: REACT_CONTEXT_MESSAGE
				});
			}
			else if (hasAnyExports && !hasReactExport && localComponents.length > 0) for (const local of localComponents) context.report({
				node: local,
				message: LOCAL_COMPONENT_MESSAGE
			});
			else if (!hasAnyExports && localComponents.length > 0) for (const local of localComponents) context.report({
				node: local,
				message: NO_EXPORT_MESSAGE
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/preact/preact-no-children-length.ts
const ARRAY_READ_METHOD_NAMES = new Set([
	"length",
	"map",
	"forEach",
	"filter",
	"find",
	"reduce",
	"some",
	"every",
	"flat",
	"flatMap",
	"indexOf",
	"includes",
	"slice",
	"concat",
	"join"
]);
const CHILDREN_ARRAY_MESSAGE = "Your users hit a crash when `props.children` is not an array in Preact, so use `toChildArray(children)` from `preact` before calling array methods or reading `.length`.";
const isDestructuredChildrenParam = (identifier) => {
	let cursor = identifier.parent;
	while (cursor) {
		if (isNodeOfType(cursor, "FunctionDeclaration") || isNodeOfType(cursor, "FunctionExpression") || isNodeOfType(cursor, "ArrowFunctionExpression")) {
			const firstParam = cursor.params[0];
			if (!firstParam || !isNodeOfType(firstParam, "ObjectPattern")) return false;
			return firstParam.properties.some((property) => isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "children");
		}
		cursor = cursor.parent ?? null;
	}
	return false;
};
const isChildrenMemberExpression = (node) => {
	const object = node.object;
	if (!isNodeOfType(object, "MemberExpression")) return isNodeOfType(object, "Identifier") && object.name === "children" && isDestructuredChildrenParam(object);
	if (!isNodeOfType(object.property, "Identifier") || object.property.name !== "children") return false;
	const propsObject = object.object;
	if (isNodeOfType(propsObject, "Identifier") && propsObject.name === "props") return true;
	if (isNodeOfType(propsObject, "MemberExpression") && isNodeOfType(propsObject.property, "Identifier") && propsObject.property.name === "props" && isNodeOfType(propsObject.object, "ThisExpression")) return true;
	return false;
};
const preactNoChildrenLength = defineRule({
	id: "preact-no-children-length",
	title: "Array methods on props.children",
	requires: ["preact"],
	severity: "warn",
	recommendation: "Wrap with `toChildArray(children)` from `preact` before accessing array methods or `.length`.",
	create: (context) => ({ MemberExpression(node) {
		if (node.computed) return;
		if (!isNodeOfType(node.property, "Identifier")) return;
		if (!ARRAY_READ_METHOD_NAMES.has(node.property.name)) return;
		if (!isChildrenMemberExpression(node)) return;
		context.report({
			node,
			message: CHILDREN_ARRAY_MESSAGE
		});
	} })
});
//#endregion
//#region src/plugin/rules/preact/preact-no-react-hooks-import.ts
const REACT_HOOK_NAMES = new Set([
	"useCallback",
	"useContext",
	"useDebugValue",
	"useDeferredValue",
	"useEffect",
	"useId",
	"useImperativeHandle",
	"useInsertionEffect",
	"useLayoutEffect",
	"useMemo",
	"useReducer",
	"useRef",
	"useState",
	"useSyncExternalStore",
	"useTransition"
]);
const buildMessage$4 = (importedNames) => `Your users hit \`__H\` undefined errors because importing ${importedNames.map((innerName) => `\`${innerName}\``).join(", ")} from \`react\` in a pure-Preact project loads a second copy of the hook state, so import from \`preact/hooks\` (or \`preact/compat\`) instead.`;
const preactNoReactHooksImport = defineRule({
	id: "preact-no-react-hooks-import",
	title: "Hooks imported from react",
	requires: ["pure-preact"],
	severity: "warn",
	recommendation: "Replace `from \"react\"` with `from \"preact/hooks\"` (or `from \"preact/compat\"` if other React API surface is needed).",
	create: (context) => ({ ImportDeclaration(node) {
		const source = node.source;
		if (!isNodeOfType(source, "Literal") || source.value !== "react") return;
		const reactHookSpecifiers = [];
		for (const specifier of node.specifiers) {
			if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
			const imported = specifier.imported;
			if (!isNodeOfType(imported, "Identifier")) continue;
			if (REACT_HOOK_NAMES.has(imported.name)) reactHookSpecifiers.push(specifier);
		}
		if (reactHookSpecifiers.length === 0) return;
		const importedNames = reactHookSpecifiers.map((specifier) => {
			const imported = specifier.imported;
			return isNodeOfType(imported, "Identifier") ? imported.name : "";
		});
		context.report({
			node,
			message: buildMessage$4(importedNames)
		});
	} })
});
//#endregion
//#region src/plugin/rules/preact/preact-no-render-arguments.ts
const PREACT_COMPONENT_NAMESPACES = new Set(["Preact"]);
const PREACT_COMPONENT_NAMES = new Set(["Component", "PureComponent"]);
const isPreactNamespaceComponentRef = (node) => {
	if (!isNodeOfType(node, "MemberExpression")) return false;
	if (!isNodeOfType(node.object, "Identifier")) return false;
	if (!PREACT_COMPONENT_NAMESPACES.has(node.object.name)) return false;
	if (!isNodeOfType(node.property, "Identifier")) return false;
	return PREACT_COMPONENT_NAMES.has(node.property.name);
};
const isPreactOrReactComponentClass = (node) => {
	if (isEs6Component(node)) return true;
	if (!isNodeOfType(node, "ClassDeclaration") && !isNodeOfType(node, "ClassExpression")) return false;
	const superClass = node.superClass;
	if (!superClass) return false;
	return isPreactNamespaceComponentRef(superClass);
};
const RENDER_ARGUMENTS_MESSAGE = "Your users hit `undefined` props under `preact/compat` when you read `render(props, state)` from arguments, since compat uses React's parameterless render, so read from `this.props` & `this.state` instead.";
const isInstanceMethodNamedRender = (node) => isNodeOfType(node, "MethodDefinition") && node.kind === "method" && node.static !== true && isNodeOfType(node.key, "Identifier") && node.key.name === "render";
const isInsideEs6Component$1 = (methodDefinition) => {
	const classBody = methodDefinition.parent;
	if (!classBody || !isNodeOfType(classBody, "ClassBody")) return false;
	const owningClass = classBody.parent;
	if (!owningClass) return false;
	return isPreactOrReactComponentClass(owningClass);
};
const stripThisParameter = (params) => {
	const first = params[0];
	if (!first) return params;
	if (isNodeOfType(first, "Identifier") && first.name === "this") return params.slice(1);
	return params;
};
const preactNoRenderArguments = defineRule({
	id: "preact-no-render-arguments",
	title: "render() reads props from arguments",
	requires: ["preact"],
	severity: "warn",
	recommendation: "Read state/props from `this.props` / `this.state` inside `render()` instead of declaring positional parameters.",
	create: (context) => ({ MethodDefinition(node) {
		if (!isInstanceMethodNamedRender(node)) return;
		if (!isInsideEs6Component$1(node)) return;
		const renderFunction = node.value;
		if (!renderFunction || !isNodeOfType(renderFunction, "FunctionExpression")) return;
		const firstParameter = stripThisParameter(renderFunction.params)[0];
		if (!firstParameter) return;
		context.report({
			node: firstParameter,
			message: RENDER_ARGUMENTS_MESSAGE
		});
	} })
});
//#endregion
//#region src/plugin/rules/preact/preact-prefer-ondblclick.ts
const MESSAGE$8 = "Your users get no response from `onDoubleClick` in Preact core, where it never fires, so use `onDblClick` instead, which matches the DOM event name.";
const preactPreferOndblclick = defineRule({
	id: "preact-prefer-ondblclick",
	title: "onDoubleClick instead of onDblClick",
	requires: ["pure-preact"],
	severity: "warn",
	recommendation: "Rename the handler from `onDoubleClick` to `onDblClick` to match the DOM event name.",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		const tagName = node.name.name;
		if (tagName.length === 0 || tagName[0] !== tagName[0].toLowerCase()) return;
		const onDoubleClickAttribute = findJsxAttribute(node.attributes, "onDoubleClick");
		if (!onDoubleClickAttribute) return;
		context.report({
			node: onDoubleClickAttribute,
			message: MESSAGE$8
		});
	} })
});
//#endregion
//#region src/plugin/rules/preact/preact-prefer-oninput.ts
const PREFER_ONINPUT_MESSAGE = "Your users see no live updates because `onChange` on text inputs in Preact core only fires on blur, so use `onInput` instead. `preact/compat` handles this for you.";
const COMPAT_EXEMPT_INPUT_TYPES = new Set([
	"checkbox",
	"radio",
	"file"
]);
const isTextLikeInput = (openingElement) => {
	if (!isNodeOfType(openingElement.name, "JSXIdentifier")) return false;
	const tagName = openingElement.name.name;
	if (tagName === "textarea") return true;
	if (tagName !== "input") return false;
	const typeAttribute = findJsxAttribute(openingElement.attributes, "type");
	if (!typeAttribute) return true;
	const typeValue = getJsxPropStringValue(typeAttribute);
	if (typeValue === null) return true;
	return !COMPAT_EXEMPT_INPUT_TYPES.has(typeValue);
};
const preactPreferOninput = defineRule({
	id: "preact-prefer-oninput",
	title: "onChange instead of onInput",
	requires: ["pure-preact"],
	severity: "warn",
	recommendation: "Replace `onChange` with `onInput` on text-like inputs, or use `preact/compat` which remaps `onChange` automatically.",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isTextLikeInput(node)) return;
		const onChangeAttribute = findJsxAttribute(node.attributes, "onChange");
		if (!onChangeAttribute) return;
		context.report({
			node: onChangeAttribute,
			message: PREFER_ONINPUT_MESSAGE
		});
	} })
});
//#endregion
//#region src/plugin/rules/bundle-size/prefer-dynamic-import.ts
const preferDynamicImport = defineRule({
	id: "prefer-dynamic-import",
	title: "Heavy library loaded eagerly",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Load it only when needed: `const Component = dynamic(() => import('library'), { ssr: false })` from next/dynamic, or React.lazy().",
	create: (context) => ({ ImportDeclaration(node) {
		const source = node.source?.value;
		if (typeof source !== "string" || !HEAVY_LIBRARIES.has(source)) return;
		if (node.importKind === "type") return;
		const specifiers = node.specifiers ?? [];
		if (specifiers.length === 0) {} else if (specifiers.every((specifier) => {
			return specifier.importKind === "type";
		})) return;
		context.report({
			node,
			message: `"${source}" ships extra code to your users up front & slows page load. Load it on demand with React.lazy() or next/dynamic.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/prefer-es6-class.ts
const ALWAYS_MESSAGE$1 = "`createReactClass` is legacy & adds a dependency.";
const NEVER_MESSAGE$1 = "This component is defined inconsistently.";
const resolveSettings$5 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	if (typeof reactDoctor !== "object" || reactDoctor === null) return {};
	return reactDoctor.preferEs6Class ?? {};
};
const preferEs6Class = defineRule({
	id: "prefer-es6-class",
	title: "createClass instead of ES6 class",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Pick one component style for the whole codebase: `class extends React.Component` (default) or `createReactClass` (legacy).",
	category: "Architecture",
	create: (context) => {
		const { mode = "always" } = resolveSettings$5(context.settings);
		return {
			CallExpression(node) {
				if (mode !== "always") return;
				const callExpressionNode = node;
				if (!isEs5Component(callExpressionNode)) return;
				context.report({
					node: callExpressionNode,
					message: ALWAYS_MESSAGE$1
				});
			},
			ClassDeclaration(node) {
				if (mode === "always") return;
				const classNode = node;
				if (!isEs6Component(classNode)) return;
				context.report({
					node: node.id ?? classNode,
					message: NEVER_MESSAGE$1
				});
			},
			ClassExpression(node) {
				if (mode === "always") return;
				const classNode = node;
				if (!isEs6Component(classNode)) return;
				context.report({
					node: node.id ?? classNode,
					message: NEVER_MESSAGE$1
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/utils/is-jsx-element-or-fragment.ts
/**
* Type-guard for the two single-node JSX output forms: `JSXElement`
* (`<Foo />`) and `JSXFragment` (`<>…</>`). Canonical home for the
* `isNodeOfType(x, "JSXElement") || isNodeOfType(x, "JSXFragment")` check
* that many rules otherwise inline. Does NOT unwrap parens / TS wrappers —
* callers that need the semantic expression should `stripParenExpression`
* first.
*/
const isJsxElementOrFragment = (node) => Boolean(node && (isNodeOfType(node, "JSXElement") || isNodeOfType(node, "JSXFragment")));
//#endregion
//#region src/plugin/rules/architecture/prefer-explicit-variants.ts
const resolveBooleanPropTestName = (testNode, booleanPropBindings) => {
	let identifierNode = stripParenExpression(testNode);
	if (isNodeOfType(identifierNode, "UnaryExpression") && identifierNode.operator === "!") identifierNode = stripParenExpression(identifierNode.argument);
	if (!isNodeOfType(identifierNode, "Identifier")) return null;
	return booleanPropBindings.has(identifierNode.name) ? identifierNode.name : null;
};
const CROSS_CUTTING_STATE_BOOLEAN_NAMES = new Set([
	"isLoading",
	"isPending",
	"isFetching",
	"isRefetching",
	"isSubmitting",
	"isError",
	"isSuccess",
	"isEmpty",
	"isReady",
	"isDirty",
	"isValid",
	"isInvalid",
	"isOpen",
	"isClosed",
	"isVisible",
	"isHidden",
	"isActive",
	"isInactive",
	"isExpanded",
	"isCollapsed",
	"isSelected",
	"isChecked",
	"isDisabled",
	"isEnabled",
	"isFocused",
	"isHovered",
	"isDragging",
	"isFullscreen",
	"isMobile",
	"isDesktop",
	"isTablet",
	"isOnline",
	"isOffline",
	"isLoggedIn",
	"isAuthenticated",
	"isAuthorized",
	"isDark",
	"isLight"
]);
const collectBooleanPropBindings = (param) => {
	const bindings = /* @__PURE__ */ new Set();
	if (!param || !isNodeOfType(param, "ObjectPattern")) return bindings;
	for (const property of param.properties ?? []) {
		if (!isNodeOfType(property, "Property")) continue;
		if (property.computed) continue;
		if (!isNodeOfType(property.key, "Identifier")) continue;
		if (!isBooleanPrefixedPropName(property.key.name)) continue;
		if (CROSS_CUTTING_STATE_BOOLEAN_NAMES.has(property.key.name)) continue;
		const propertyValue = property.value;
		if (isNodeOfType(propertyValue, "Identifier")) bindings.add(propertyValue.name);
		else if (isNodeOfType(propertyValue, "AssignmentPattern") && isNodeOfType(propertyValue.left, "Identifier")) bindings.add(propertyValue.left.name);
	}
	return bindings;
};
const collectVariantBranchProps = (body, booleanPropBindings) => {
	const variantBranchProps = /* @__PURE__ */ new Set();
	if (!body) return variantBranchProps;
	walkAst(body, (current) => {
		if (isNodeOfType(current, "FunctionDeclaration") || isInlineFunctionExpression(current)) return false;
		if (!isNodeOfType(current, "ConditionalExpression")) return;
		const propName = resolveBooleanPropTestName(current.test, booleanPropBindings);
		if (!propName) return;
		const consequent = stripParenExpression(current.consequent);
		const alternate = stripParenExpression(current.alternate);
		if (!isJsxElementOrFragment(consequent) || !isJsxElementOrFragment(alternate)) return;
		variantBranchProps.add(propName);
	});
	return variantBranchProps;
};
const preferExplicitVariants = defineRule({
	id: "prefer-explicit-variants",
	title: "Prefer explicit variant components",
	severity: "warn",
	tags: ["test-noise", "react-jsx-only"],
	recommendation: "Replace boolean props that switch whole subtrees with explicit variant components, like `<ThreadComposer />` and `<EditMessageComposer />`, so each variant renders one clear path.",
	create: (context) => {
		const checkComponent = (param, body, componentName, reportNode) => {
			const booleanPropBindings = collectBooleanPropBindings(param);
			if (booleanPropBindings.size < 2) return;
			const variantBranchProps = collectVariantBranchProps(body, booleanPropBindings);
			if (variantBranchProps.size < 2) return;
			const propList = [...variantBranchProps].slice(0, 3).join(", ");
			const overflow = variantBranchProps.size > 3 ? "…" : "";
			context.report({
				node: reportNode,
				message: `Component "${componentName}" picks which component to render from ${variantBranchProps.size} boolean props (${propList}${overflow}), which multiplies untestable variants. Split it into explicit variant components so each renders one clear path.`
			});
		};
		return {
			FunctionDeclaration(node) {
				if (!isComponentDeclaration(node) || !node.id) return;
				checkComponent(node.params?.[0], node.body, node.id.name, node.id);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.id, "Identifier")) return;
				if (!isInlineFunctionExpression(node.init)) return;
				checkComponent(node.init.params?.[0], node.init.body, node.id.name, node.id);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/prefer-function-component.ts
const MESSAGE$7 = "This class component is harder to maintain than a function component.";
const resolveSettings$4 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.preferFunctionComponent ?? {} : {};
	return {
		allowErrorBoundary: ruleSettings.allowErrorBoundary ?? true,
		allowJsxUtilityClass: ruleSettings.allowJsxUtilityClass ?? false
	};
};
const ERROR_BOUNDARY_METHODS = new Set(["componentDidCatch", "getDerivedStateFromError"]);
const isErrorBoundaryClass = (classNode) => {
	const classBody = classNode.body;
	if (!classBody) return false;
	const members = classBody.body ?? [];
	for (const member of members) {
		if (!isNodeOfType(member, "MethodDefinition")) continue;
		const key = member.key;
		if (isNodeOfType(key, "Identifier") && ERROR_BOUNDARY_METHODS.has(key.name)) return true;
	}
	return false;
};
const preferFunctionComponent = defineRule({
	id: "prefer-function-component",
	title: "Class component instead of function",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Re-write the class component as a function component using hooks.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$4(context.settings);
		const checkClass = (node) => {
			if (!isEs6Component(node)) {
				if (!settings.allowJsxUtilityClass) return;
				if (!containsJsxElement(node)) return;
			}
			if (settings.allowErrorBoundary && isErrorBoundaryClass(node)) return;
			const reportNode = node.id ?? node;
			context.report({
				node: reportNode,
				message: MESSAGE$7
			});
		};
		return {
			ClassDeclaration(node) {
				checkClass(node);
			},
			ClassExpression(node) {
				checkClass(node);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/a11y/prefer-html-dialog.ts
const ROLE_DIALOG_VALUES = new Set(["dialog", "alertdialog"]);
const ROLE_DIALOG_MESSAGE = "Keyboard users can tab out of this `role=\"dialog\"` modal because it has no built-in focus trapping, so use the native `<dialog>`, which gives you focus trapping, `Escape` to close, and the backdrop for free.";
const ARIA_MODAL_MESSAGE = "Keyboard users can tab out of this modal because `aria-modal=\"true\"` only hints to screen readers without trapping focus or blocking the page, so use the native `<dialog>` with `dialog.showModal()` instead.";
const isAriaModalTrue = (attribute) => {
	const stringValue = getJsxPropStringValue(attribute);
	if (stringValue !== null) return stringValue === "true";
	const value = attribute.value;
	if (!value) return true;
	if (isNodeOfType(value, "JSXExpressionContainer")) {
		const expression = value.expression;
		if (isNodeOfType(expression, "Literal") && expression.value === true) return true;
	}
	return false;
};
const preferHtmlDialog = defineRule({
	id: "prefer-html-dialog",
	title: "Custom modal instead of dialog",
	severity: "warn",
	recommendation: "Replace the wrapper with `<dialog>` and open it with `dialog.showModal()`. For the trigger, prefer `<button commandfor=\"id\" command=\"show-modal\">` (Chrome 135+), or a `useRef` with `dialogRef.current?.showModal()`.",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier")) return;
		const tagName = node.name.name;
		if (tagName === "dialog") return;
		if (tagName.length === 0 || tagName[0] !== tagName[0].toLowerCase()) return;
		const roleAttribute = findJsxAttribute(node.attributes, "role");
		if (roleAttribute) {
			const roleValue = getJsxPropStringValue(roleAttribute);
			if (roleValue !== null && ROLE_DIALOG_VALUES.has(roleValue)) {
				context.report({
					node: roleAttribute,
					message: ROLE_DIALOG_MESSAGE
				});
				return;
			}
		}
		const ariaModalAttribute = findJsxAttribute(node.attributes, "aria-modal");
		if (ariaModalAttribute && isAriaModalTrue(ariaModalAttribute)) context.report({
			node: ariaModalAttribute,
			message: ARIA_MODAL_MESSAGE
		});
	} })
});
//#endregion
//#region src/plugin/utils/function-returns-object-literal.ts
const unwrapExpression = (node) => {
	let current = node;
	for (;;) {
		if ((current.type === "TSAsExpression" || current.type === "TSSatisfiesExpression" || current.type === "TSNonNullExpression") && "expression" in current && isAstNode(current.expression)) {
			current = current.expression;
			continue;
		}
		return current;
	}
};
const doesFunctionReturnsObjectLiteral = (functionNode) => {
	if (functionNode.type === "ArrowFunctionExpression" && "body" in functionNode) {
		const body = functionNode.body;
		if (body && body.type !== "BlockStatement") return unwrapExpression(body).type === "ObjectExpression";
	}
	const body = functionNode.body;
	if (!body || body.type !== "BlockStatement") return false;
	let returnsObject = false;
	const visit = (node) => {
		if (returnsObject) return;
		if (node.type === "ReturnStatement" && "argument" in node && node.argument != null) {
			if (unwrapExpression(node.argument).type === "ObjectExpression") returnsObject = true;
			return;
		}
		const nodeRecord = node;
		for (const key of Object.keys(nodeRecord)) {
			if (key === "parent") continue;
			const child = nodeRecord[key];
			if (Array.isArray(child)) for (const item of child) {
				if (!isAstNode(item)) continue;
				if (FUNCTION_LIKE_TYPES$1.has(item.type)) continue;
				visit(item);
				if (returnsObject) return;
			}
			else if (isAstNode(child)) {
				if (FUNCTION_LIKE_TYPES$1.has(child.type)) continue;
				visit(child);
			}
		}
	};
	visit(body);
	return returnsObject;
};
//#endregion
//#region src/plugin/utils/enclosing-component-or-hook-scope.ts
const enclosingComponentOrHookScope = (startNode, ownScopeFor) => {
	const functionNode = nearestEnclosingFunction(startNode);
	if (!functionNode) return null;
	const displayName = componentOrHookDisplayNameForFunction(functionNode);
	if (!displayName) return null;
	if (!isReactHookName(displayName) && doesFunctionReturnsObjectLiteral(functionNode)) return null;
	const bodyScope = ownScopeFor(functionNode);
	if (!bodyScope) return null;
	return {
		functionNode,
		bodyScope,
		displayName
	};
};
//#endregion
//#region src/plugin/rules/architecture/prefer-module-scope-pure-function.ts
const isAssignedToComponentMember = (functionNode) => {
	const parent = functionNode.parent;
	if (!parent) return false;
	return isNodeOfType(parent, "AssignmentExpression") && isNodeOfType(parent.left, "MemberExpression");
};
const hasComponentLocalCaptures = (functionNode, bodyScope, scopes) => {
	const captures = closureCaptures(functionNode, scopes);
	for (const capture of captures) {
		const symbol = capture.resolvedSymbol;
		if (!symbol) continue;
		if (isDescendantScope(symbol.scope, bodyScope)) return true;
	}
	return false;
};
const preferModuleScopePureFunction = defineRule({
	id: "prefer-module-scope-pure-function",
	title: "Pure function rebuilt every render",
	tags: ["test-noise"],
	severity: "warn",
	category: "Architecture",
	recommendation: "Move the function above the component, at the top of the file. It doesn't use local state, so rebuilding it each update is wasted work.",
	create: (context) => {
		const report = (functionNode, name, componentName) => {
			context.report({
				node: functionNode,
				message: `\`${name}\` inside \`${componentName}\` uses no local state but is rebuilt on every render, so it wastes work & breaks memoized children. Move it to the top of the file, outside the component.`
			});
		};
		const checkNamedFunction = (functionNode, bindingName) => {
			if (isAssignedToComponentMember(functionNode)) return;
			const component = enclosingComponentOrHookScope(functionNode, context.scopes.ownScopeFor);
			if (!component) return;
			const ownScope = context.scopes.ownScopeFor(functionNode);
			if (!ownScope) return;
			if (ownScope === component.bodyScope) return;
			if (!isDescendantScope(ownScope, component.bodyScope)) return;
			if (hasComponentLocalCaptures(functionNode, component.bodyScope, context.scopes)) return;
			report(functionNode, bindingName, component.displayName);
		};
		return {
			VariableDeclarator(node) {
				if (!isNodeOfType(node.id, "Identifier")) return;
				const initializer = node.init;
				if (!initializer) return;
				if (!isNodeOfType(initializer, "ArrowFunctionExpression") && !isNodeOfType(initializer, "FunctionExpression")) return;
				const bindingName = node.id.name;
				if (/^[A-Z]/.test(bindingName)) return;
				checkNamedFunction(initializer, bindingName);
			},
			FunctionDeclaration(node) {
				if (!node.id?.name) return;
				const bindingName = node.id.name;
				if (/^[A-Z]/.test(bindingName)) return;
				checkNamedFunction(node, bindingName);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/architecture/prefer-module-scope-static-value.ts
const MUTATING_RECEIVER_METHOD_NAMES = new Set([...MUTATING_ARRAY_METHODS, ...MUTATING_COLLECTION_METHODS]);
const isMutationContext = (referenceIdentifier) => {
	const parent = referenceIdentifier.parent;
	if (!parent) return false;
	if (isNodeOfType(parent, "AssignmentExpression") && parent.left === referenceIdentifier) return true;
	if (isNodeOfType(parent, "UpdateExpression") && parent.argument === referenceIdentifier) return true;
	if (isNodeOfType(parent, "MemberExpression") && parent.object === referenceIdentifier) {
		const grandparent = parent.parent;
		if (!grandparent) return false;
		if (isNodeOfType(grandparent, "AssignmentExpression") && grandparent.left === parent) return true;
		if (isNodeOfType(grandparent, "UpdateExpression") && grandparent.argument === parent) return true;
		if (isNodeOfType(grandparent, "UnaryExpression") && grandparent.operator === "delete" && grandparent.argument === parent) return true;
		if (isNodeOfType(grandparent, "CallExpression") && grandparent.callee === parent && !parent.computed && isNodeOfType(parent.property, "Identifier") && MUTATING_RECEIVER_METHOD_NAMES.has(parent.property.name)) return true;
	}
	return false;
};
const isBindingMutatedAfterInit = (declaratorNode, bodyScope, scopes) => {
	if (!isNodeOfType(declaratorNode.id, "Identifier")) return false;
	const symbol = scopes.symbolFor(declaratorNode.id);
	if (!symbol) return false;
	for (const reference of symbol.references) {
		if (reference.identifier === declaratorNode.id) continue;
		if (reference.identifier === declaratorNode.init) continue;
		if (!isDescendantScope(reference.scope, bodyScope) && reference.scope !== bodyScope) continue;
		if (isMutationContext(reference.identifier)) return true;
	}
	return false;
};
const hasComponentLocalReferences = (expression, bodyScope, scopes) => {
	let foundLocal = false;
	walkAst(expression, (node) => {
		if (foundLocal) return false;
		if (isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression")) {
			foundLocal = true;
			return false;
		}
		const reference = scopes.referenceFor(node);
		if (reference?.resolvedSymbol && isDescendantScope(reference.resolvedSymbol.scope, bodyScope)) {
			foundLocal = true;
			return false;
		}
	});
	return foundLocal;
};
const isHoistableValueExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	return isNodeOfType(stripped, "ArrayExpression") || isNodeOfType(stripped, "ObjectExpression");
};
const preferModuleScopeStaticValue = defineRule({
	id: "prefer-module-scope-static-value",
	title: "Static value rebuilt every render",
	tags: ["test-noise"],
	severity: "warn",
	category: "Architecture",
	disabledBy: ["react-compiler"],
	recommendation: "Move the value above the component, at the top of the file. It doesn't use local state, so rebuilding it each update is wasted and makes it look new every time.",
	create: (context) => ({ VariableDeclarator(node) {
		if (!isNodeOfType(node.id, "Identifier")) return;
		const initializer = node.init;
		if (!initializer) return;
		if (!isHoistableValueExpression(initializer)) return;
		const component = enclosingComponentOrHookScope(node, context.scopes.ownScopeFor);
		if (!component) return;
		if (hasComponentLocalReferences(initializer, component.bodyScope, context.scopes)) return;
		if (isBindingMutatedAfterInit(node, component.bodyScope, context.scopes)) return;
		const bindingName = node.id.name;
		context.report({
			node,
			message: `\`${bindingName}\` inside \`${component.displayName}\` uses no local state but is rebuilt every render, so it looks new each time & breaks memoized children. Move it to the top of the file, outside the component.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/performance/prefer-stable-empty-fallback.ts
const isEmptyArrayLiteral$1 = (expression) => {
	const stripped = stripParenExpression(expression);
	return isNodeOfType(stripped, "ArrayExpression") && (stripped.elements ?? []).length === 0;
};
const isEmptyObjectLiteral = (expression) => {
	const stripped = stripParenExpression(expression);
	return isNodeOfType(stripped, "ObjectExpression") && (stripped.properties ?? []).length === 0;
};
const isStableNonEmptyExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "Identifier")) return true;
	if (isNodeOfType(stripped, "ThisExpression")) return true;
	if (isNodeOfType(stripped, "MemberExpression")) {
		if (stripped.computed) return false;
		const object = stripped.object;
		if (!object) return false;
		return isStableNonEmptyExpression(object);
	}
	return false;
};
const matchEmptyFallbackInLogicalExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "LogicalExpression")) return null;
	if (stripped.operator !== "||" && stripped.operator !== "??") return null;
	const left = stripped.left;
	const right = stripped.right;
	if (!left || !right) return null;
	if (isEmptyArrayLiteral$1(right) && isStableNonEmptyExpression(left)) return {
		emptyKind: "array",
		emptyNode: right,
		nonEmptyExpression: left
	};
	if (isEmptyObjectLiteral(right) && isStableNonEmptyExpression(left)) return {
		emptyKind: "object",
		emptyNode: right,
		nonEmptyExpression: left
	};
	return null;
};
const buildMessage$3 = (emptyKind) => {
	return `This redraws the memo child anyway because fallback \`${emptyKind === "array" ? "[]" : "{}"}\` builds a brand new ${emptyKind} each render when the left value is empty, so the child sees a different value. Hoist a constant (e.g. \`${emptyKind === "array" ? "const EMPTY_ITEMS: Item[] = []" : "const EMPTY_CONFIG: Config = {}"}\`) & use that as the fallback.`;
};
const preferStableEmptyFallback = defineRule({
	id: "prefer-stable-empty-fallback",
	title: "Empty fallback rebuilt each render",
	tags: ["react-jsx-only", "test-noise"],
	severity: "warn",
	category: "Performance",
	disabledBy: ["react-compiler"],
	recommendation: "Make a `const EMPTY = []` (or `{}`) at module scope and use it as the `||` / `??` fallback, so the value stays the same each render.",
	create: (context) => {
		let memoRegistry = null;
		return {
			Program(node) {
				memoRegistry = buildSameFileMemoRegistry(node);
			},
			JSXAttribute(node) {
				if (!isInsideFunctionScope(node)) return;
				if (isJsxAttributeOnIntrinsicHtmlElement(node)) return;
				if (!node.value) return;
				if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
				const innerExpression = node.value.expression;
				if (!innerExpression) return;
				if (innerExpression.type === "JSXEmptyExpression") return;
				const parentJsxOpening = node.parent;
				const openingName = parentJsxOpening && isNodeOfType(parentJsxOpening, "JSXOpeningElement") ? parentJsxOpening.name : null;
				if (memoStatusForJsxOpeningName(memoRegistry, openingName) !== "memoised") return;
				const fallback = matchEmptyFallbackInLogicalExpression(innerExpression);
				if (!fallback) return;
				context.report({
					node: fallback.emptyNode,
					message: buildMessage$3(fallback.emptyKind)
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/a11y/prefer-tag-over-role.ts
const buildMessage$2 = (role, tag) => `Screen reader users get more reliable semantics from \`<${tag}>\` than \`role="${role}"\`, so use \`<${tag}>\` instead.`;
const preferTagOverRole = defineRule({
	id: "prefer-tag-over-role",
	title: "Role used instead of HTML tag",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Replace `role` with the matching HTML element when one exists.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const tag = getElementType(node, context.settings);
		if (tag !== "div" && tag !== "span") return;
		const roleAttr = hasJsxPropIgnoreCase(node.attributes, "role");
		if (!roleAttr) return;
		const role = getJsxPropStringValue(roleAttr);
		if (!role) return;
		const matchingTags = getTagsForRole(role);
		if (matchingTags.length === 0) return;
		const preferred = matchingTags[0];
		context.report({
			node: roleAttr,
			message: buildMessage$2(role, preferred)
		});
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/prefer-use-effect-event.ts
const collectFunctionTypedLocalBindings = (componentBody) => {
	const functionTypedLocals = /* @__PURE__ */ new Set();
	if (!isNodeOfType(componentBody, "BlockStatement")) return functionTypedLocals;
	for (const statement of componentBody.body ?? []) {
		if (!isNodeOfType(statement, "VariableDeclaration")) continue;
		for (const declarator of statement.declarations ?? []) {
			if (!isNodeOfType(declarator.id, "Identifier")) continue;
			if (!isNodeOfType(declarator.init, "CallExpression")) continue;
			if (!isHookCall$1(declarator.init, "useCallback")) continue;
			functionTypedLocals.add(declarator.id.name);
		}
	}
	return functionTypedLocals;
};
const findEnclosingFunctionInsideEffect = (identifierNode, effectCallback) => {
	let cursor = identifierNode.parent ?? null;
	while (cursor && cursor !== effectCallback) {
		if (isNodeOfType(cursor, "ArrowFunctionExpression") || isNodeOfType(cursor, "FunctionExpression") || isNodeOfType(cursor, "FunctionDeclaration")) return cursor;
		cursor = cursor.parent ?? null;
	}
	return null;
};
const isCallExpressionWithSubHandlerCallee = (callExpression) => {
	if (!isNodeOfType(callExpression, "CallExpression")) return false;
	const callee = callExpression.callee;
	if (isNodeOfType(callee, "Identifier") && TIMER_AND_SCHEDULER_DIRECT_CALLEE_NAMES.has(callee.name)) return true;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && SUBSCRIPTION_METHOD_NAMES.has(callee.property.name)) return true;
	return false;
};
const getSubHandlerCalleeName = (callExpression) => {
	if (!isNodeOfType(callExpression, "CallExpression")) return null;
	const callee = callExpression.callee;
	if (isNodeOfType(callee, "Identifier")) return callee.name;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) return callee.property.name;
	return null;
};
const getEnclosingFunctionBindingName = (enclosingFunction) => {
	if (isNodeOfType(enclosingFunction, "FunctionDeclaration") && isNodeOfType(enclosingFunction.id, "Identifier")) return enclosingFunction.id.name;
	const directParent = enclosingFunction.parent;
	if (isNodeOfType(directParent, "VariableDeclarator") && isNodeOfType(directParent.id, "Identifier")) return directParent.id.name;
	if (isNodeOfType(directParent, "AssignmentExpression") && directParent.right === enclosingFunction && isNodeOfType(directParent.left, "Identifier")) return directParent.left.name;
	return null;
};
const findSubHandlerForEnclosingFunction = (enclosingFunction, effectCallback) => {
	const directParent = enclosingFunction.parent;
	if (isNodeOfType(directParent, "CallExpression") && (directParent.arguments ?? []).some((arg) => arg === enclosingFunction) && isCallExpressionWithSubHandlerCallee(directParent)) return directParent;
	const localName = getEnclosingFunctionBindingName(enclosingFunction);
	if (localName === null) return null;
	let matchingSubHandlerCall = null;
	walkAst(effectCallback, (child) => {
		if (matchingSubHandlerCall) return false;
		if (!isNodeOfType(child, "CallExpression")) return;
		if (!isCallExpressionWithSubHandlerCallee(child)) return;
		for (const argument of child.arguments ?? []) if (isNodeOfType(argument, "Identifier") && argument.name === localName) {
			matchingSubHandlerCall = child;
			return false;
		}
	});
	return matchingSubHandlerCall;
};
const classifyCallableReadsInsideEffect = (callableName, effectCallback) => {
	let hasAnyRead = false;
	let allReadsAreInSubHandlers = true;
	let firstSubHandlerName = null;
	walkAst(effectCallback, (child) => {
		if (!isNodeOfType(child, "Identifier")) return;
		if (child.name !== callableName) return;
		const parent = child.parent;
		if (isNodeOfType(parent, "ArrayExpression")) return;
		if (isNodeOfType(parent, "MemberExpression") && !parent.computed && parent.property === child) return;
		if (isNodeOfType(parent, "Property") && !parent.computed && !parent.shorthand && parent.key === child) return;
		hasAnyRead = true;
		const enclosingFunction = findEnclosingFunctionInsideEffect(child, effectCallback);
		if (!enclosingFunction) {
			allReadsAreInSubHandlers = false;
			return;
		}
		const subHandlerCall = findSubHandlerForEnclosingFunction(enclosingFunction, effectCallback);
		if (!subHandlerCall) {
			allReadsAreInSubHandlers = false;
			return;
		}
		if (firstSubHandlerName === null) firstSubHandlerName = getSubHandlerCalleeName(subHandlerCall);
	});
	return {
		hasAnyRead,
		allReadsAreInSubHandlers,
		firstSubHandlerName
	};
};
const preferUseEffectEvent = defineRule({
	id: "prefer-use-effect-event",
	title: "Effect re-subscribes on a changing callback",
	requires: ["react:19"],
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Wrap the callback with `useEffectEvent(callback)` (React 19+) and call it inside the sub-handler. An Effect Event always sees the latest props and state but isn't a dependency, so the effect won't re-subscribe every time the parent redraws. See https://react.dev/reference/react/useEffectEvent",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const functionTypedLocalBindings = collectFunctionTypedLocalBindings(componentBody);
			for (const statement of componentBody.body ?? []) {
				if (!isNodeOfType(statement, "ExpressionStatement")) continue;
				const effectCall = statement.expression;
				if (!isNodeOfType(effectCall, "CallExpression")) continue;
				if (!isHookCall$1(effectCall, EFFECT_HOOK_NAMES$1)) continue;
				if ((effectCall.arguments?.length ?? 0) < 2) continue;
				const depsNode = effectCall.arguments[1];
				if (!isNodeOfType(depsNode, "ArrayExpression")) continue;
				const depElements = depsNode.elements ?? [];
				if (depElements.length < 2) continue;
				if (!depElements.every((element) => isNodeOfType(element, "Identifier"))) continue;
				const callback = getEffectCallback(effectCall);
				if (!callback) continue;
				for (const depElement of depElements) {
					if (!isNodeOfType(depElement, "Identifier")) continue;
					const depName = depElement.name;
					const isFunctionTypedPropDep = propStackTracker.isPropName(depName) && REACT_HANDLER_PROP_PATTERN.test(depName);
					const isFunctionTypedLocalDep = functionTypedLocalBindings.has(depName);
					if (!isFunctionTypedPropDep && !isFunctionTypedLocalDep) continue;
					const classification = classifyCallableReadsInsideEffect(depName, callback);
					if (!classification.hasAnyRead) continue;
					if (!classification.allReadsAreInSubHandlers) continue;
					const subHandlerLabel = classification.firstSubHandlerName ? `\`${classification.firstSubHandlerName}\`` : "an async sub-handler";
					context.report({
						node: depElement,
						message: `Your effect re-subscribes whenever "${depName}" changes, even though it's only used inside ${subHandlerLabel}.`
					});
				}
			}
		};
		const propStackTracker = createComponentPropStackTracker({ onComponentEnter: checkComponent });
		return propStackTracker.visitors;
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/prefer-use-sync-external-store.ts
const findUseEffectsInComponent = (componentBody) => {
	const effectCalls = [];
	if (!isNodeOfType(componentBody, "BlockStatement")) return effectCalls;
	for (const statement of componentBody.body ?? []) walkAst(statement, (child) => {
		if (isNodeOfType(child, "CallExpression") && isHookCall$1(child, EFFECT_HOOK_NAMES$1)) effectCalls.push(child);
	});
	return effectCalls;
};
const findSubscriptionCall = (effectBodyStatements) => {
	for (const statement of effectBodyStatements) {
		if (isNodeOfType(statement, "VariableDeclaration")) for (const declarator of statement.declarations ?? []) {
			const init = declarator.init;
			if (!isNodeOfType(init, "CallExpression")) continue;
			if (!isNodeOfType(init.callee, "MemberExpression")) continue;
			if (!isNodeOfType(init.callee.property, "Identifier")) continue;
			if (!SUBSCRIPTION_METHOD_NAMES.has(init.callee.property.name)) continue;
			const boundSubscriptionName = isNodeOfType(declarator.id, "Identifier") ? declarator.id.name : null;
			return {
				call: init,
				boundReleaseName: boundSubscriptionName && isCleanupReturningSubscribeLikeCallExpression(init) ? boundSubscriptionName : null,
				boundSubscriptionName
			};
		}
		if (isNodeOfType(statement, "ExpressionStatement")) {
			const expression = statement.expression;
			if (!isNodeOfType(expression, "CallExpression")) continue;
			if (!isNodeOfType(expression.callee, "MemberExpression")) continue;
			if (!isNodeOfType(expression.callee.property, "Identifier")) continue;
			if (!SUBSCRIPTION_METHOD_NAMES.has(expression.callee.property.name)) continue;
			return {
				call: expression,
				boundReleaseName: null,
				boundSubscriptionName: null
			};
		}
	}
	return null;
};
const getSubscriptionHandlerArgument = (subscribeCall, effectBodyStatements) => {
	if (!isNodeOfType(subscribeCall, "CallExpression")) return null;
	for (const argument of subscribeCall.arguments ?? []) {
		if (isNodeOfType(argument, "ArrowFunctionExpression") || isNodeOfType(argument, "FunctionExpression")) return argument;
		if (isNodeOfType(argument, "Identifier")) for (const statement of effectBodyStatements) {
			if (!isNodeOfType(statement, "VariableDeclaration")) continue;
			for (const declarator of statement.declarations ?? []) {
				if (!isNodeOfType(declarator.id, "Identifier")) continue;
				if (declarator.id.name !== argument.name) continue;
				const init = declarator.init;
				if (isNodeOfType(init, "ArrowFunctionExpression") || isNodeOfType(init, "FunctionExpression")) return init;
			}
		}
	}
	return null;
};
const getSingleSetterCallFromHandler = (handler) => {
	const handlerStatements = getCallbackStatements(handler);
	if (handlerStatements.length !== 1) return null;
	const onlyStatement = handlerStatements[0];
	const expression = isNodeOfType(onlyStatement, "ExpressionStatement") ? onlyStatement.expression : onlyStatement;
	if (!isNodeOfType(expression, "CallExpression")) return null;
	if (!isNodeOfType(expression.callee, "Identifier")) return null;
	if (!isSetterIdentifier(expression.callee.name)) return null;
	if (!expression.arguments?.length) return null;
	return {
		setterName: expression.callee.name,
		setterArgument: expression.arguments[0]
	};
};
const cleanupReleasesSubscription = (effectBodyStatements, boundReleaseName, boundSubscriptionName) => {
	const lastStatement = effectBodyStatements[effectBodyStatements.length - 1];
	if (!isNodeOfType(lastStatement, "ReturnStatement")) return false;
	const knownBoundReleaseNames = /* @__PURE__ */ new Set();
	const knownBoundSubscriptionNames = /* @__PURE__ */ new Set();
	if (boundReleaseName) knownBoundReleaseNames.add(boundReleaseName);
	if (boundSubscriptionName) knownBoundSubscriptionNames.add(boundSubscriptionName);
	return isCleanupReturn(lastStatement.argument, knownBoundReleaseNames, knownBoundSubscriptionNames);
};
const preferUseSyncExternalStore = defineRule({
	id: "prefer-use-sync-external-store",
	title: "Hand-rolled external store subscription",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Replace the `useState(getSnapshot())` + `useEffect(() => store.subscribe(() => setSnapshot(getSnapshot())))` pair with `useSyncExternalStore(store.subscribe, getSnapshot)`. The hook gets this right during concurrent rendering and on the server; the hand-rolled version doesn't.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const useStateBindings = collectUseStateBindings(componentBody);
			if (useStateBindings.length === 0) return;
			const useStateInitializerByValueName = /* @__PURE__ */ new Map();
			for (const binding of useStateBindings) {
				const useStateCall = binding.declarator.init;
				if (!useStateCall || !isNodeOfType(useStateCall, "CallExpression")) continue;
				const initializerArgument = useStateCall.arguments?.[0];
				if (!initializerArgument) continue;
				if ((isNodeOfType(initializerArgument, "ArrowFunctionExpression") || isNodeOfType(initializerArgument, "FunctionExpression")) && !isNodeOfType(initializerArgument.body, "BlockStatement")) useStateInitializerByValueName.set(binding.valueName, initializerArgument.body);
				else useStateInitializerByValueName.set(binding.valueName, initializerArgument);
			}
			const setterNameToValueName = /* @__PURE__ */ new Map();
			for (const binding of useStateBindings) setterNameToValueName.set(binding.setterName, binding.valueName);
			for (const effectCall of findUseEffectsInComponent(componentBody)) {
				if (!isNodeOfType(effectCall, "CallExpression")) continue;
				if ((effectCall.arguments?.length ?? 0) < 2) continue;
				const depsNode = effectCall.arguments[1];
				if (!isNodeOfType(depsNode, "ArrayExpression")) continue;
				if ((depsNode.elements?.length ?? 0) !== 0) continue;
				const callback = getEffectCallback(effectCall);
				if (!callback || !isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) continue;
				if (!isNodeOfType(callback.body, "BlockStatement")) continue;
				const effectBodyStatements = callback.body.body ?? [];
				if (effectBodyStatements.length < 2) continue;
				const subscription = findSubscriptionCall(effectBodyStatements);
				if (!subscription) continue;
				const handler = getSubscriptionHandlerArgument(subscription.call, effectBodyStatements);
				if (!handler) continue;
				const setterPayload = getSingleSetterCallFromHandler(handler);
				if (!setterPayload) continue;
				const valueName = setterNameToValueName.get(setterPayload.setterName);
				if (!valueName) continue;
				const useStateInitializer = useStateInitializerByValueName.get(valueName);
				if (!useStateInitializer) continue;
				if (!areExpressionsStructurallyEqual(useStateInitializer, setterPayload.setterArgument)) continue;
				if (!cleanupReleasesSubscription(effectBodyStatements, subscription.boundReleaseName, subscription.boundSubscriptionName)) continue;
				const matchingBinding = useStateBindings.find((binding) => binding.valueName === valueName);
				context.report({
					node: matchingBinding?.declarator ?? effectCall,
					message: `Your users can see stale or torn values because useState "${valueName}" syncs an outside store through a useEffect.`
				});
			}
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/prefer-use-reducer.ts
const preferUseReducer = defineRule({
	id: "prefer-useReducer",
	title: "Many related useState calls",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Group related state: `const [state, dispatch] = useReducer(reducer, { field1, field2, ... })`",
	create: (context) => {
		const reportExcessiveUseState = (body, componentName) => {
			if (!isNodeOfType(body, "BlockStatement")) return;
			let useStateCount = 0;
			for (const statement of body.body ?? []) {
				if (!isNodeOfType(statement, "VariableDeclaration")) continue;
				for (const declarator of statement.declarations ?? []) if (declarator.init && isHookCall$1(declarator.init, "useState")) useStateCount++;
			}
			if (useStateCount >= 5) context.report({
				node: body,
				message: `${useStateCount} useState calls in "${componentName}" can each trigger a separate render.`
			});
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				reportExcessiveUseState(node.body, node.id.name);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				if (!isNodeOfType(node.id, "Identifier")) return;
				reportExcessiveUseState(node.init.body, node.id.name);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/tanstack-query/query-destructure-result.ts
const queryDestructureResult = defineRule({
	id: "query-destructure-result",
	title: "Destructure TanStack Query result",
	tags: ["test-noise"],
	requires: ["tanstack-query"],
	severity: "error",
	recommendation: "Destructure only the fields you need, like `const { data, isLoading } = useQuery(...)`. Assigning the whole object bypasses TanStack Query's tracked-property optimization and subscribes to every field.",
	create: (context) => ({ VariableDeclarator(node) {
		if (!isNodeOfType(node.id, "Identifier")) return;
		if (!node.init || !isNodeOfType(node.init, "CallExpression")) return;
		const calleeName = isNodeOfType(node.init.callee, "Identifier") ? node.init.callee.name : null;
		if (!calleeName || !TANSTACK_QUERY_HOOKS.has(calleeName)) return;
		context.report({
			node: node.id,
			message: `Destructure ${calleeName}() results instead of assigning the whole query object, so TanStack Query only subscribes to the fields you use.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-query/query-mutation-missing-invalidation.ts
const queryMutationMissingInvalidation = defineRule({
	id: "query-mutation-missing-invalidation",
	title: "Mutation without cache invalidation",
	tags: ["test-noise"],
	requires: ["tanstack-query"],
	severity: "warn",
	recommendation: "Add `onSuccess: () => queryClient.invalidateQueries({ queryKey: ['...'] })` so cached data stays in sync after the mutation",
	create: (context) => ({ CallExpression(node) {
		const calleeName = isNodeOfType(node.callee, "Identifier") ? node.callee.name : null;
		if (!calleeName || !TANSTACK_MUTATION_HOOKS.has(calleeName)) return;
		const optionsArgument = node.arguments?.[0];
		if (!optionsArgument || !isNodeOfType(optionsArgument, "ObjectExpression")) return;
		if (!optionsArgument.properties?.some((property) => isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "mutationFn")) return;
		let hasCacheUpdate = false;
		walkAst(optionsArgument, (child) => {
			if (hasCacheUpdate) return false;
			if (isNodeOfType(child, "CallExpression") && isNodeOfType(child.callee, "MemberExpression") && isNodeOfType(child.callee.property, "Identifier") && QUERY_CACHE_UPDATE_METHODS.has(child.callee.property.name)) {
				hasCacheUpdate = true;
				return false;
			}
		});
		if (!hasCacheUpdate) context.report({
			node,
			message: "useMutation with no cache update leaves your users looking at stale data after it runs."
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-query/query-no-query-in-effect.ts
const queryNoQueryInEffect = defineRule({
	id: "query-no-query-in-effect",
	title: "Query refetch inside useEffect",
	tags: ["test-noise"],
	requires: ["tanstack-query"],
	severity: "warn",
	recommendation: "React Query refetches automatically via queryKey changes and the `enabled` option. A manual refetch() in useEffect is usually unnecessary.",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1)) return;
		const callback = getEffectCallback(node);
		if (!callback) return;
		walkAst(callback, (child) => {
			if (!isNodeOfType(child, "CallExpression")) return;
			if ((isNodeOfType(child.callee, "Identifier") ? child.callee.name : null) === "refetch") context.report({
				node: child,
				message: "refetch() inside useEffect duplicates work React Query already does, causing extra fetches."
			});
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-query/query-no-rest-destructuring.ts
const queryNoRestDestructuring = defineRule({
	id: "query-no-rest-destructuring",
	title: "Rest destructuring on query result",
	tags: ["test-noise"],
	requires: ["tanstack-query"],
	severity: "warn",
	recommendation: "Destructure only the fields you need, like `const { data, isLoading } = useQuery(...)`. Rest destructuring subscribes to every field and adds re-renders.",
	create: (context) => ({ VariableDeclarator(node) {
		if (!isNodeOfType(node.id, "ObjectPattern")) return;
		if (!node.init || !isNodeOfType(node.init, "CallExpression")) return;
		const calleeName = isNodeOfType(node.init.callee, "Identifier") ? node.init.callee.name : null;
		if (!calleeName || !TANSTACK_QUERY_HOOKS.has(calleeName)) return;
		if (node.id.properties?.some((property) => isNodeOfType(property, "RestElement"))) context.report({
			node: node.id,
			message: `Rest-destructuring ${calleeName}() subscribes to every field, so it re-renders on each change.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-query/query-no-use-query-for-mutation.ts
const queryNoUseQueryForMutation = defineRule({
	id: "query-no-usequery-for-mutation",
	title: "useQuery used for mutation",
	tags: ["test-noise"],
	requires: ["tanstack-query"],
	severity: "warn",
	recommendation: "Use `useMutation()` for POST/PUT/DELETE. It gives onSuccess/onError callbacks, doesn't auto-refetch, and models writes correctly.",
	create: (context) => ({ CallExpression(node) {
		const calleeName = isNodeOfType(node.callee, "Identifier") ? node.callee.name : null;
		if (!calleeName || !TANSTACK_QUERY_HOOKS.has(calleeName)) return;
		const optionsArgument = node.arguments?.[0];
		if (!optionsArgument || !isNodeOfType(optionsArgument, "ObjectExpression")) return;
		const queryFnProperty = optionsArgument.properties?.find((property) => isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "queryFn");
		if (!queryFnProperty || !isNodeOfType(queryFnProperty, "Property") || !queryFnProperty.value) return;
		let hasMutatingFetch = false;
		walkAst(queryFnProperty.value, (child) => {
			if (hasMutatingFetch) return;
			if (!isNodeOfType(child, "CallExpression")) return;
			if (!isNodeOfType(child.callee, "Identifier") || child.callee.name !== "fetch") return;
			const optionsArg = child.arguments?.[1];
			if (!optionsArg || !isNodeOfType(optionsArg, "ObjectExpression")) return;
			if (optionsArg.properties?.find((property) => isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "method" && isNodeOfType(property.value, "Literal") && typeof property.value.value === "string" && MUTATING_HTTP_METHODS.has(property.value.value.toUpperCase()))) hasMutatingFetch = true;
		});
		if (hasMutatingFetch) context.report({
			node,
			message: `${calleeName}() auto-refetches, so this mutating fetch (POST/PUT/DELETE) can fire repeatedly.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-query/query-no-void-query-fn.ts
const queryNoVoidQueryFn = defineRule({
	id: "query-no-void-query-fn",
	title: "queryFn returns no value",
	tags: ["test-noise"],
	requires: ["tanstack-query"],
	severity: "warn",
	recommendation: "queryFn must return a value for the cache. Use the `enabled` option to conditionally disable the query instead of returning undefined",
	create: (context) => ({ CallExpression(node) {
		const calleeName = isNodeOfType(node.callee, "Identifier") ? node.callee.name : null;
		if (!calleeName || !TANSTACK_QUERY_HOOKS.has(calleeName)) return;
		const optionsArgument = node.arguments?.[0];
		if (!optionsArgument || !isNodeOfType(optionsArgument, "ObjectExpression")) return;
		const queryFnProperty = optionsArgument.properties?.find((property) => isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "queryFn");
		if (!queryFnProperty || !isNodeOfType(queryFnProperty, "Property") || !queryFnProperty.value) return;
		const queryFnValue = queryFnProperty.value;
		if (isNodeOfType(queryFnValue, "ArrowFunctionExpression") && !isNodeOfType(queryFnValue.body, "BlockStatement")) return;
		if (isNodeOfType(queryFnValue, "ArrowFunctionExpression") || isNodeOfType(queryFnValue, "FunctionExpression")) {
			const body = queryFnValue.body;
			if (!isNodeOfType(body, "BlockStatement")) return;
			if ((body.body ?? []).length === 0) context.report({
				node: queryFnProperty,
				message: "This empty queryFn caches undefined, so the component never gets data."
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-query/query-stable-query-client.ts
const queryStableQueryClient = defineRule({
	id: "query-stable-query-client",
	title: "Unstable QueryClient in component",
	tags: ["test-noise"],
	requires: ["tanstack-query"],
	severity: "warn",
	recommendation: "Move `new QueryClient()` to module scope, or wrap it in `useState(() => new QueryClient())`. Recreating it each render wipes the cache.",
	create: (context) => {
		let componentDepth = 0;
		let stableHookDepth = 0;
		return {
			FunctionDeclaration(node) {
				if (node.id?.name && UPPERCASE_PATTERN.test(node.id.name)) componentDepth++;
			},
			"FunctionDeclaration:exit"(node) {
				if (isNodeOfType(node, "FunctionDeclaration") && node.id?.name && UPPERCASE_PATTERN.test(node.id.name)) componentDepth--;
			},
			VariableDeclarator(node) {
				if (isNodeOfType(node.id, "Identifier") && UPPERCASE_PATTERN.test(node.id.name) && (isNodeOfType(node.init, "ArrowFunctionExpression") || isNodeOfType(node.init, "FunctionExpression"))) componentDepth++;
			},
			"VariableDeclarator:exit"(node) {
				if (isNodeOfType(node, "VariableDeclarator") && isNodeOfType(node.id, "Identifier") && UPPERCASE_PATTERN.test(node.id.name) && (isNodeOfType(node.init, "ArrowFunctionExpression") || isNodeOfType(node.init, "FunctionExpression"))) componentDepth--;
			},
			CallExpression(node) {
				if (isHookCall$1(node, STABLE_HOOK_WRAPPERS)) stableHookDepth++;
			},
			"CallExpression:exit"(node) {
				if (isHookCall$1(node, STABLE_HOOK_WRAPPERS)) stableHookDepth = Math.max(0, stableHookDepth - 1);
			},
			NewExpression(node) {
				if (componentDepth <= 0) return;
				if (stableHookDepth > 0) return;
				if (!isNodeOfType(node.callee, "Identifier") || node.callee.name !== "QueryClient") return;
				context.report({
					node,
					message: "new QueryClient() inside a component wipes your cache on every render."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/architecture/react-compiler-no-manual-memoization.ts
const REMOVAL_MESSAGE_BY_REACT_API_NAME = new Map([
	["useMemo", "This `useMemo` is dead weight, since React Compiler already caches every value here. Delete it."],
	["useCallback", "This `useCallback` is dead weight, since React Compiler already caches every function here. Delete it."],
	["memo", "This `memo()` is dead weight, since React Compiler already caches the component's output. Delete it."]
]);
const resolveReactApiNameForIdentifier = (callee) => {
	if (!isNodeOfType(callee, "Identifier")) return null;
	const importedName = getImportedNameFromModule(callee, callee.name, "react");
	if (importedName && REMOVAL_MESSAGE_BY_REACT_API_NAME.has(importedName)) return importedName;
	return null;
};
const resolveReactApiNameForMemberExpression = (callee) => {
	if (!isNodeOfType(callee, "MemberExpression")) return null;
	if (callee.computed) return null;
	const namespaceIdentifier = callee.object;
	const propertyIdentifier = callee.property;
	if (!isNodeOfType(namespaceIdentifier, "Identifier")) return null;
	if (!isNodeOfType(propertyIdentifier, "Identifier")) return null;
	if (!REMOVAL_MESSAGE_BY_REACT_API_NAME.has(propertyIdentifier.name)) return null;
	const namespaceName = namespaceIdentifier.name;
	if (isCanonicalReactNamespaceName(namespaceName)) return propertyIdentifier.name;
	if (isImportedFromModule(namespaceIdentifier, namespaceName, "react")) return propertyIdentifier.name;
	return null;
};
const resolveRemovalMessageForCallee = (callee) => {
	const apiName = resolveReactApiNameForIdentifier(callee) ?? resolveReactApiNameForMemberExpression(callee);
	if (!apiName) return null;
	return REMOVAL_MESSAGE_BY_REACT_API_NAME.get(apiName) ?? null;
};
const reactCompilerNoManualMemoization = defineRule({
	id: "react-compiler-no-manual-memoization",
	title: "Redundant manual memoization",
	severity: "warn",
	requires: ["react-compiler"],
	recommendation: "Delete the `useMemo` / `useCallback` / `memo` call and use the plain value or component. React Compiler caches it for you.",
	create: (context) => ({ CallExpression(node) {
		const removalMessage = resolveRemovalMessageForCallee(node.callee);
		if (!removalMessage) return;
		context.report({
			node,
			message: removalMessage
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/react-in-jsx-scope.ts
const MESSAGE$6 = "This JSX crashes because `React` isn't in scope.";
const reactInJsxScope = defineRule({
	id: "react-in-jsx-scope",
	title: "React not in scope for JSX",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "If you're on React 17+ with the new JSX transform, disable this rule. Otherwise import `React` at the top of the file.",
	create: (context) => ({
		JSXOpeningElement(node) {
			if (findVariableInitializer(node, "React")) return;
			context.report({
				node: node.name,
				message: MESSAGE$6
			});
		},
		JSXFragment(node) {
			if (findVariableInitializer(node, "React")) return;
			context.report({
				node: node.openingFragment,
				message: MESSAGE$6
			});
		}
	})
});
//#endregion
//#region src/plugin/utils/collect-react-redux-selector-aliases.ts
const REACT_REDUX_MODULE = "react-redux";
const collectReactReduxSelectorAliases = (programRoot) => {
	const aliases = /* @__PURE__ */ new Set();
	if (!isNodeOfType(programRoot, "Program")) return aliases;
	for (const topLevel of programRoot.body ?? []) {
		if (!isNodeOfType(topLevel, "ImportDeclaration")) continue;
		if (typeof topLevel.source?.value !== "string") continue;
		if (topLevel.source.value !== REACT_REDUX_MODULE) continue;
		for (const specifier of topLevel.specifiers ?? []) {
			if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
			const imported = specifier.imported;
			if ((imported && "name" in imported && typeof imported.name === "string" ? imported.name : imported && "value" in imported && typeof imported.value === "string" ? imported.value : null) !== "useSelector") continue;
			const local = specifier.local;
			if (isNodeOfType(local, "Identifier")) aliases.add(local.name);
		}
	}
	const collectDeclarations = (node) => {
		if (!isNodeOfType(node, "VariableDeclaration")) return;
		for (const declarator of node.declarations ?? []) {
			if (!isNodeOfType(declarator, "VariableDeclarator")) continue;
			if (!isNodeOfType(declarator.id, "Identifier")) continue;
			if (!declarator.init) continue;
			const initialiser = stripParenExpression(declarator.init);
			if (!isNodeOfType(initialiser, "Identifier")) continue;
			if (!aliases.has(initialiser.name)) continue;
			aliases.add(declarator.id.name);
		}
	};
	for (const topLevel of programRoot.body ?? []) if (isNodeOfType(topLevel, "VariableDeclaration")) collectDeclarations(topLevel);
	else if (isNodeOfType(topLevel, "ExportNamedDeclaration") && topLevel.declaration) collectDeclarations(topLevel.declaration);
	return aliases;
};
const isUseSelectorIdentifier = (calleeNode, aliases) => {
	if (!isNodeOfType(calleeNode, "Identifier")) return false;
	if (aliases.has(calleeNode.name)) return true;
	if (calleeNode.name !== "useSelector") return false;
	return isImportedFromModule(calleeNode, calleeNode.name, REACT_REDUX_MODULE);
};
//#endregion
//#region src/plugin/rules/state-and-effects/utils/inline-use-selector-function.ts
const inlineUseSelectorFunction = (callNode, aliases) => {
	if (!isUseSelectorIdentifier(callNode.callee, aliases)) return null;
	const args = callNode.arguments ?? [];
	if (args.length === 0 || args.length >= 2) return null;
	const selectorArgument = stripParenExpression(args[0]);
	if (isNodeOfType(selectorArgument, "ArrowFunctionExpression") || isNodeOfType(selectorArgument, "FunctionExpression")) return selectorArgument;
	return null;
};
//#endregion
//#region src/plugin/rules/state-and-effects/redux-useselector-inline-derivation.ts
const ALLOCATING_ARRAY_METHODS = new Set([
	"filter",
	"map",
	"flatMap",
	"slice",
	"concat",
	"toSorted",
	"toReversed",
	"toSpliced",
	"with"
]);
const ALLOCATING_NAMESPACE_CALLS = new Map([["Object", new Set([
	"keys",
	"values",
	"entries",
	"fromEntries",
	"assign"
])], ["Array", new Set(["from", "of"])]]);
const MESSAGE_DERIVATION = (methodName) => `\`.${methodName}(...)\` returns a new array every render, so your component redraws on every action.`;
const MESSAGE_NAMESPACE = (namespace, methodName) => `\`${namespace}.${methodName}(...)\` returns a new collection every render, so your component redraws on every action.`;
const getAllocatingCallSiteDescription = (expression) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "CallExpression")) return null;
	const callee = stripped.callee;
	if (!isNodeOfType(callee, "MemberExpression")) return null;
	if (callee.computed) return null;
	if (!isNodeOfType(callee.property, "Identifier")) return null;
	const methodName = callee.property.name;
	if (isNodeOfType(callee.object, "Identifier")) {
		const namespaceName = callee.object.name;
		if (ALLOCATING_NAMESPACE_CALLS.get(namespaceName)?.has(methodName)) return {
			kind: "namespace",
			namespace: namespaceName,
			method: methodName
		};
	}
	if (ALLOCATING_ARRAY_METHODS.has(methodName)) return {
		kind: "method",
		method: methodName
	};
	return null;
};
const findReturnedAllocatingCall = (expression) => {
	const stripped = stripParenExpression(expression);
	const direct = getAllocatingCallSiteDescription(stripped);
	if (direct) return {
		...direct,
		node: stripped
	};
	if (isNodeOfType(stripped, "ConditionalExpression")) return findReturnedAllocatingCall(stripped.consequent) ?? findReturnedAllocatingCall(stripped.alternate);
	if (isNodeOfType(stripped, "LogicalExpression")) return findReturnedAllocatingCall(stripped.left) ?? findReturnedAllocatingCall(stripped.right);
	if (isNodeOfType(stripped, "SequenceExpression")) {
		const lastExpression = stripped.expressions[stripped.expressions.length - 1];
		return lastExpression ? findReturnedAllocatingCall(lastExpression) : null;
	}
	return null;
};
const reduxUseselectorInlineDerivation = defineRule({
	id: "redux-useselector-inline-derivation",
	title: "useSelector derives data inline",
	severity: "warn",
	category: "Performance",
	disabledBy: ["react-compiler"],
	recommendation: "Select the raw slice and derive with `useMemo`, or use `createSelector` from `reselect`.",
	create: (context) => {
		let aliases = /* @__PURE__ */ new Set();
		return {
			Program(node) {
				aliases = collectReactReduxSelectorAliases(node);
			},
			CallExpression(node) {
				const selectorArgument = inlineUseSelectorFunction(node, aliases);
				if (!selectorArgument) return;
				const body = selectorArgument.body;
				if (!body) return;
				const returnedExpressions = [];
				if (isNodeOfType(body, "BlockStatement")) walkAst(body, (node) => {
					if (node !== body && isFunctionLike$2(node)) return false;
					if (isNodeOfType(node, "ReturnStatement")) {
						if (node.argument) returnedExpressions.push(node.argument);
						return false;
					}
				});
				else returnedExpressions.push(body);
				for (const returnedExpression of returnedExpressions) {
					const allocatingCall = findReturnedAllocatingCall(returnedExpression);
					if (!allocatingCall) continue;
					const reportMessage = allocatingCall.kind === "method" ? MESSAGE_DERIVATION(allocatingCall.method) : MESSAGE_NAMESPACE(allocatingCall.namespace, allocatingCall.method);
					context.report({
						node: allocatingCall.node,
						message: reportMessage
					});
					return;
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/redux-useselector-returns-new-collection.ts
const MESSAGE$5 = "Your component re-renders on every dispatched action when useSelector returns a new object or array.";
const isConciseBodyReturningCollection = (functionNode) => {
	if (!isNodeOfType(functionNode, "ArrowFunctionExpression") && !isNodeOfType(functionNode, "FunctionExpression")) return false;
	const rawBody = functionNode.body;
	if (!rawBody) return false;
	if (!isNodeOfType(rawBody, "BlockStatement")) {
		const conciseExpression = stripParenExpression(rawBody);
		return isNodeOfType(conciseExpression, "ObjectExpression") || isNodeOfType(conciseExpression, "ArrayExpression");
	}
	const statements = rawBody.body ?? [];
	if (statements.length === 0) return false;
	const lastStatement = statements[statements.length - 1];
	if (!isNodeOfType(lastStatement, "ReturnStatement")) return false;
	if (!lastStatement.argument) return false;
	const returnedExpression = stripParenExpression(lastStatement.argument);
	return isNodeOfType(returnedExpression, "ObjectExpression") || isNodeOfType(returnedExpression, "ArrayExpression");
};
const reduxUseselectorReturnsNewCollection = defineRule({
	id: "redux-useselector-returns-new-collection",
	title: "useSelector returns a new collection",
	severity: "warn",
	category: "Performance",
	disabledBy: ["react-compiler"],
	recommendation: "Return a primitive, split into multiple useSelector calls, or pass `shallowEqual` from `react-redux` as the second argument.",
	create: (context) => {
		let aliases = /* @__PURE__ */ new Set();
		return {
			Program(node) {
				aliases = collectReactReduxSelectorAliases(node);
			},
			CallExpression(node) {
				const selectorArgument = inlineUseSelectorFunction(node, aliases);
				if (!selectorArgument) return;
				if (!isConciseBodyReturningCollection(selectorArgument)) return;
				context.report({
					node,
					message: MESSAGE$5
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/performance/rendering-animate-svg-wrapper.ts
const renderingAnimateSvgWrapper = defineRule({
	id: "rendering-animate-svg-wrapper",
	title: "Animating an SVG directly",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Wrap the SVG: `<motion.div animate={...}><svg>...</svg></motion.div>`",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "svg") return;
		if (node.attributes?.some((attribute) => isNodeOfType(attribute, "JSXAttribute") && isNodeOfType(attribute.name, "JSXIdentifier") && MOTION_ANIMATE_PROPS.has(attribute.name.name))) context.report({
			node,
			message: "This is slow to render because you animate <svg> directly, so wrap it in a <div> or <motion.div> & animate that instead"
		});
	} })
});
//#endregion
//#region src/plugin/rules/correctness/rendering-conditional-render.ts
const NUMERIC_NAME_HINTS$1 = [
	"count",
	"length",
	"total",
	"size",
	"num"
];
const isNumericName$1 = (name) => {
	for (const hint of NUMERIC_NAME_HINTS$1) {
		if (name === hint) return true;
		const camelSuffix = hint.charAt(0).toUpperCase() + hint.slice(1);
		if (name.endsWith(camelSuffix)) return true;
		if (name.endsWith(`_${hint}`)) return true;
		if (name.endsWith(`_${hint.toUpperCase()}`)) return true;
	}
	return false;
};
const renderingConditionalRender = defineRule({
	id: "rendering-conditional-render",
	title: "Number before && renders stray 0",
	severity: "warn",
	recommendation: "Use `{items.length > 0 && <List />}`, or a ternary like `{items.length ? <List /> : null}`.",
	create: (context) => ({ LogicalExpression(node) {
		if (node.operator !== "&&") return;
		if (!(isNodeOfType(node.right, "JSXElement") || isNodeOfType(node.right, "JSXFragment"))) return;
		const left = node.left;
		if (!left) return;
		const isLengthMemberAccess = isNodeOfType(left, "MemberExpression") && isNodeOfType(left.property, "Identifier") && left.property.name === "length";
		const isNumericIdentifier = isNodeOfType(left, "Identifier") && isNumericName$1(left.name);
		if (isLengthMemberAccess || isNumericIdentifier) context.report({
			node,
			message: "Your users see a stray '0' on screen when a number before `&&` is zero, so use `value > 0`, `Boolean(value)`, or a ternary instead."
		});
	} })
});
//#endregion
//#region src/plugin/rules/performance/rendering-hoist-jsx.ts
const jsxReferencesLocalScope = (jsxNode) => {
	let referencesScope = false;
	walkAst(jsxNode, (child) => {
		if (referencesScope) return;
		if (isNodeOfType(child, "JSXExpressionContainer") && !isNodeOfType(child.expression, "JSXEmptyExpression")) referencesScope = true;
		if (isNodeOfType(child, "JSXSpreadAttribute")) referencesScope = true;
	});
	return referencesScope;
};
const renderingHoistJsx = defineRule({
	id: "rendering-hoist-jsx",
	title: "Constant JSX rebuilt each render",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Move the static JSX out to the top of the file: `const ICON = <svg>...</svg>`, so it isn't rebuilt on every render",
	create: (context) => {
		let componentDepth = 0;
		const isComponentLike = (node) => {
			if (isNodeOfType(node, "FunctionDeclaration") && node.id?.name && isUppercaseName(node.id.name)) return true;
			if (isNodeOfType(node, "VariableDeclarator") && isComponentAssignment(node)) return true;
			return false;
		};
		const enter = (node) => {
			if (isComponentLike(node)) componentDepth++;
		};
		const exit = (node) => {
			if (isComponentLike(node)) componentDepth = Math.max(0, componentDepth - 1);
		};
		return {
			FunctionDeclaration: enter,
			"FunctionDeclaration:exit": exit,
			VariableDeclarator: enter,
			"VariableDeclarator:exit": exit,
			VariableDeclaration(node) {
				if (componentDepth === 0) return;
				if (node.kind !== "const") return;
				for (const declarator of node.declarations ?? []) {
					const init = declarator.init;
					if (!init) continue;
					if (!isNodeOfType(init, "JSXElement") && !isNodeOfType(init, "JSXFragment")) continue;
					if (jsxReferencesLocalScope(init)) continue;
					const name = isNodeOfType(declarator.id, "Identifier") ? declarator.id.name : "<unnamed>";
					context.report({
						node: declarator,
						message: `This rebuilds on every render because static JSX "${name}" is built inside the component, so move it to the top of the file to make it just once`
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/performance/rendering-hydration-mismatch-time.ts
const NONDETERMINISTIC_RENDER_PATTERNS = [
	{
		display: "new Date()",
		matches: (node) => isNodeOfType(node, "NewExpression") && isNodeOfType(node.callee, "Identifier") && node.callee.name === "Date" && (node.arguments?.length ?? 0) === 0
	},
	{
		display: "Date.now()",
		matches: (node) => isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.object, "Identifier") && node.callee.object.name === "Date" && isNodeOfType(node.callee.property, "Identifier") && node.callee.property.name === "now"
	},
	{
		display: "Math.random()",
		matches: (node) => isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.object, "Identifier") && node.callee.object.name === "Math" && isNodeOfType(node.callee.property, "Identifier") && node.callee.property.name === "random"
	},
	{
		display: "performance.now()",
		matches: (node) => isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.object, "Identifier") && node.callee.object.name === "performance" && isNodeOfType(node.callee.property, "Identifier") && node.callee.property.name === "now"
	},
	{
		display: "crypto.randomUUID()",
		matches: (node) => isNodeOfType(node, "CallExpression") && isNodeOfType(node.callee, "MemberExpression") && isNodeOfType(node.callee.object, "Identifier") && node.callee.object.name === "crypto" && isNodeOfType(node.callee.property, "Identifier") && node.callee.property.name === "randomUUID"
	}
];
const findOpeningElementOfChild = (jsxNode) => {
	let cursor = jsxNode.parent ?? null;
	while (cursor) {
		if (isNodeOfType(cursor, "JSXElement")) return cursor.openingElement;
		if (isNodeOfType(cursor, "JSXFragment")) return null;
		cursor = cursor.parent ?? null;
	}
	return null;
};
const hasSuppressHydrationWarningAttribute = (openingElement) => {
	if (!openingElement || !isNodeOfType(openingElement, "JSXOpeningElement")) return false;
	for (const attr of openingElement.attributes ?? []) if (isNodeOfType(attr, "JSXAttribute") && isNodeOfType(attr.name, "JSXIdentifier") && attr.name.name === "suppressHydrationWarning") return true;
	return false;
};
const renderingHydrationMismatchTime = defineRule({
	id: "rendering-hydration-mismatch-time",
	title: "Time or random value in JSX",
	severity: "warn",
	category: "Correctness",
	recommendation: "Move time or random values into useEffect+useState so they only run in the browser, or add suppressHydrationWarning to the parent if it's intentional",
	create: (context) => ({ JSXExpressionContainer(node) {
		if (!node.expression) return;
		const matched = NONDETERMINISTIC_RENDER_PATTERNS.find((pattern) => pattern.matches(node.expression));
		if (matched) {
			if (hasSuppressHydrationWarningAttribute(findOpeningElementOfChild(node))) return;
			context.report({
				node,
				message: `This breaks hydration because ${matched.display} in JSX gives a different value on the server than in the browser, so move it into useEffect+useState to run only in the browser, or add suppressHydrationWarning to the parent if it's on purpose`
			});
			return;
		}
		walkAst(node.expression, (child) => {
			for (const pattern of NONDETERMINISTIC_RENDER_PATTERNS) if (pattern.matches(child)) {
				if (hasSuppressHydrationWarningAttribute(findOpeningElementOfChild(node))) return;
				context.report({
					node: child,
					message: `This breaks hydration because ${pattern.display} reached from JSX gives a different value on the server than in the browser, so move it into useEffect+useState to run only in the browser, or add suppressHydrationWarning to the parent if it's on purpose`
				});
				return;
			}
		});
	} })
});
//#endregion
//#region src/plugin/rules/performance/rendering-hydration-no-flicker.ts
const renderingHydrationNoFlicker = defineRule({
	id: "rendering-hydration-no-flicker",
	title: "useEffect setState flashes on mount",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use `useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot)` or add `suppressHydrationWarning` to the element",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1) || (node.arguments?.length ?? 0) < 2) return;
		const depsNode = node.arguments[1];
		if (!isNodeOfType(depsNode, "ArrayExpression") || depsNode.elements?.length !== 0) return;
		const callback = getEffectCallback(node);
		if (!callback || !isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) return;
		const bodyStatements = isNodeOfType(callback.body, "BlockStatement") ? callback.body.body : [callback.body];
		if (!bodyStatements || bodyStatements.length !== 1) return;
		const soleStatement = bodyStatements[0];
		if (!isNodeOfType(soleStatement, "ExpressionStatement")) return;
		const expression = soleStatement.expression;
		if (isSetterCall(expression) && isNodeOfType(expression, "CallExpression") && isNodeOfType(expression.callee, "Identifier") && isUseStateSetterInScope(expression, expression.callee.name)) context.report({
			node,
			message: "This flashes for your users because useEffect(setState, []) runs after the first paint, so use useSyncExternalStore, or add suppressHydrationWarning"
		});
	} })
});
//#endregion
//#region src/plugin/rules/performance/rendering-script-defer-async.ts
const renderingScriptDeferAsync = defineRule({
	id: "rendering-script-defer-async",
	title: "Script without defer or async",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Add `defer` for scripts that need the page, or `async` for standalone ones like analytics. In Next.js, use `<Script strategy=\"afterInteractive\" />`",
	create: (context) => ({ JSXOpeningElement(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "script") return;
		const attributes = node.attributes ?? [];
		if (!attributes.some((attr) => isNodeOfType(attr, "JSXAttribute") && isNodeOfType(attr.name, "JSXIdentifier") && attr.name.name === "src")) return;
		const typeAttribute = attributes.find((attr) => isNodeOfType(attr, "JSXAttribute") && isNodeOfType(attr.name, "JSXIdentifier") && attr.name.name === "type");
		const typeAttributeValue = typeAttribute && isNodeOfType(typeAttribute, "JSXAttribute") ? typeAttribute.value : null;
		const typeValue = isNodeOfType(typeAttributeValue, "Literal") ? typeAttributeValue.value : null;
		if (typeof typeValue === "string" && !EXECUTABLE_SCRIPT_TYPES.has(typeValue)) return;
		if (typeValue === "module") return;
		if (!attributes.some((attr) => isNodeOfType(attr, "JSXAttribute") && isNodeOfType(attr.name, "JSXIdentifier") && SCRIPT_LOADING_ATTRIBUTES.has(attr.name.name))) context.report({
			node,
			message: "This blocks the page from loading until the script downloads because <script src> has no defer or async, so add defer for scripts that need the page, or async for standalone ones"
		});
	} })
});
//#endregion
//#region src/plugin/rules/correctness/rendering-svg-precision.ts
const SVG_PATH_HIGH_PRECISION_PATTERN = /\d+\.\d{4,}/;
const SVG_PATH_ATTRIBUTES = new Set([
	"d",
	"points",
	"transform"
]);
const AUTO_GENERATED_PATH_SEGMENTS = [
	"/__generated__/",
	"/generated/",
	"/codegen/",
	"/figma-export/",
	"/sketch-export/"
];
const isAutoGeneratedSvgFile = (filename) => {
	if (!filename) return false;
	for (const segment of AUTO_GENERATED_PATH_SEGMENTS) if (filename.includes(segment)) return true;
	return false;
};
const renderingSvgPrecision = defineRule({
	id: "rendering-svg-precision",
	title: "Overly precise SVG path values",
	severity: "warn",
	category: "Performance",
	recommendation: "Round path, points, and transform decimals to 1 or 2 digits. The extra precision adds bytes with no visible difference.",
	create: (context) => {
		const filename = context.filename;
		const isAutoGenerated = isAutoGeneratedSvgFile(filename ? normalizeFilename$1(filename) : void 0);
		return { JSXAttribute(node) {
			if (isAutoGenerated) return;
			if (!isNodeOfType(node.name, "JSXIdentifier")) return;
			if (!SVG_PATH_ATTRIBUTES.has(node.name.name)) return;
			if (!isNodeOfType(node.value, "Literal")) return;
			const value = node.value.value;
			if (typeof value !== "string") return;
			if (!SVG_PATH_HIGH_PRECISION_PATTERN.test(value)) return;
			context.report({
				node,
				message: `Your users download extra bytes for SVG ${node.name.name} precision they can't see, so round it to 1 or 2 decimals.`
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/performance/rendering-usetransition-loading.ts
const enclosingFunctionBody = (node) => {
	let cursor = node.parent;
	while (cursor) {
		if (isNodeOfType(cursor, "FunctionDeclaration") || isNodeOfType(cursor, "FunctionExpression") || isNodeOfType(cursor, "ArrowFunctionExpression")) return cursor.body ?? null;
		cursor = cursor.parent ?? null;
	}
	return null;
};
const hasOwnAwait = (functionBody) => {
	if (!functionBody) return false;
	let found = false;
	walkAst(functionBody, (child) => {
		if (found) return;
		if (child !== functionBody && isFunctionLike$2(child)) return false;
		if (isNodeOfType(child, "AwaitExpression")) found = true;
	});
	return found;
};
const callsIdentifier = (root, identifierName) => {
	if (!root) return false;
	let found = false;
	walkAst(root, (child) => {
		if (found) return;
		if (isNodeOfType(child, "CallExpression") && isNodeOfType(child.callee, "Identifier") && child.callee.name === identifierName) found = true;
	});
	return found;
};
const setterIsCalledInAsyncContext = (componentBody, setterName) => {
	if (!componentBody) return false;
	let found = false;
	walkAst(componentBody, (child) => {
		if (found) return;
		if (!isFunctionLike$2(child)) return;
		const functionBody = child.body;
		if (!(Boolean(child.async) || hasOwnAwait(functionBody))) return;
		if (callsIdentifier(functionBody, setterName)) found = true;
	});
	return found;
};
const ASYNC_DATA_CALLEE_NAMES = new Set([
	"useApolloClient",
	"useMutation",
	"useQuery",
	"useLazyQuery",
	"useSubscription",
	"useSWR",
	"useSWRMutation",
	"useSWRInfinite",
	"fetch",
	"axios"
]);
const referencesAsyncDataApi = (body) => {
	if (!body) return false;
	let found = false;
	walkAst(body, (child) => {
		if (found) return;
		if (isNodeOfType(child, "CallExpression")) {
			const callee = child.callee;
			if (isNodeOfType(callee, "Identifier") && ASYNC_DATA_CALLEE_NAMES.has(callee.name)) {
				found = true;
				return;
			}
			if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier") && ASYNC_DATA_CALLEE_NAMES.has(callee.property.name)) {
				found = true;
				return;
			}
		}
	});
	return found;
};
const renderingUsetransitionLoading = defineRule({
	id: "rendering-usetransition-loading",
	title: "Loading useState forces extra render",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Replace with `const [isPending, startTransition] = useTransition()`, which skips the extra render for the loading flag",
	create: (context) => ({ VariableDeclarator(node) {
		if (!isNodeOfType(node.id, "ArrayPattern") || !node.id.elements?.length) return;
		if (!node.init || !isHookCall$1(node.init, "useState")) return;
		if (!isNodeOfType(node.init, "CallExpression")) return;
		if (!node.init.arguments?.length) return;
		const initializer = node.init.arguments[0];
		if (!isNodeOfType(initializer, "Literal") || initializer.value !== false) return;
		const firstBinding = node.id.elements[0];
		const stateVariableName = isNodeOfType(firstBinding, "Identifier") ? firstBinding.name : null;
		if (!stateVariableName || !LOADING_STATE_PATTERN.test(stateVariableName)) return;
		const secondBinding = node.id.elements[1];
		const setterName = isNodeOfType(secondBinding, "Identifier") ? secondBinding.name : null;
		const fnBody = enclosingFunctionBody(node);
		if (fnBody && (setterName && setterIsCalledInAsyncContext(fnBody, setterName) || referencesAsyncDataApi(fnBody))) return;
		context.report({
			node: node.init,
			message: `This adds an extra render because useState for "${stateVariableName}" re-renders just for the loading flag, so if it's a state change & not a data fetch, use useTransition instead`
		});
	} })
});
//#endregion
//#region src/plugin/utils/function-body-has-return-with-value.ts
const functionBodyHasReturnWithValue = (functionNode) => {
	if (functionNode.type === "ArrowFunctionExpression" && "body" in functionNode) {
		if (functionNode.body && functionNode.body.type !== "BlockStatement") return true;
	}
	const body = functionNode.body;
	if (!body || body.type !== "BlockStatement") return false;
	let didFindReturn = false;
	const visit = (node) => {
		if (didFindReturn) return;
		if (node.type === "ReturnStatement" && "argument" in node && node.argument != null) {
			didFindReturn = true;
			return;
		}
		const nodeRecord = node;
		for (const key of Object.keys(nodeRecord)) {
			if (key === "parent") continue;
			const child = nodeRecord[key];
			if (Array.isArray(child)) for (const item of child) {
				if (!isAstNode(item)) continue;
				if (FUNCTION_LIKE_TYPES$1.has(item.type)) continue;
				visit(item);
				if (didFindReturn) return;
			}
			else if (isAstNode(child)) {
				if (FUNCTION_LIKE_TYPES$1.has(child.type)) continue;
				visit(child);
			}
		}
	};
	visit(body);
	return didFindReturn;
};
//#endregion
//#region src/plugin/rules/react-builtins/require-render-return.ts
const RENDER_METHOD_NAME = "render";
const MESSAGE$4 = "Your users see nothing because this `render` method returns nothing.";
const isStaticRenderKey = (key) => {
	if (isNodeOfType(key, "Identifier")) return key.name === RENDER_METHOD_NAME;
	if (isNodeOfType(key, "Literal")) return key.value === RENDER_METHOD_NAME;
	return false;
};
const isFunctionExpressionLike = (node) => isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression");
const resolveRenderHost = (functionNode) => {
	const parent = functionNode.parent;
	if (!parent) return null;
	if (isNodeOfType(parent, "MethodDefinition") && isStaticRenderKey(parent.key)) return {
		reportNode: parent.key,
		isObjectPropertyRender: false,
		isClassRender: true
	};
	if (isNodeOfType(parent, "PropertyDefinition") && parent.value === functionNode && isStaticRenderKey(parent.key)) return {
		reportNode: parent.key,
		isObjectPropertyRender: false,
		isClassRender: true
	};
	if (isNodeOfType(parent, "Property") && parent.value === functionNode && isStaticRenderKey(parent.key)) return {
		reportNode: parent.key,
		isObjectPropertyRender: true,
		isClassRender: false
	};
	return null;
};
const isInsideEs5Component = (renderHost) => {
	const objectExpression = renderHost.parent;
	if (!objectExpression || !isNodeOfType(objectExpression, "ObjectExpression")) return false;
	const callExpression = objectExpression.parent;
	if (!callExpression) return false;
	return isEs5Component(callExpression);
};
const isInsideEs6Component = (renderHost) => {
	const classBody = renderHost.parent;
	if (!classBody || !isNodeOfType(classBody, "ClassBody")) return false;
	const classNode = classBody.parent;
	if (!classNode) return false;
	return isEs6Component(classNode);
};
const requireRenderReturn = defineRule({
	id: "require-render-return",
	title: "Render method does not return",
	severity: "error",
	recommendation: "Return JSX from your component's `render` method.",
	create: (context) => {
		const checkFunction = (functionNode) => {
			const host = resolveRenderHost(functionNode);
			if (!host) return;
			const renderHostNode = functionNode.parent ?? null;
			if (!renderHostNode) return;
			if (host.isObjectPropertyRender) {
				if (!isInsideEs5Component(renderHostNode)) return;
			} else if (host.isClassRender) {
				if (!isInsideEs6Component(renderHostNode)) return;
			} else return;
			if (functionBodyHasReturnWithValue(functionNode)) return;
			context.report({
				node: host.reportNode,
				message: MESSAGE$4
			});
		};
		return {
			FunctionExpression(node) {
				checkFunction(node);
			},
			ArrowFunctionExpression(node) {
				checkFunction(node);
			},
			FunctionDeclaration(node) {
				if (isFunctionExpressionLike(node)) return;
				checkFunction(node);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/rerender-defer-reads-hook.ts
const DEFERRABLE_HOOK_NAMES = new Set([
	"useSearchParams",
	"useParams",
	"usePathname"
]);
const findHookCallBindings = (componentBody) => {
	const bindings = [];
	if (!isNodeOfType(componentBody, "BlockStatement")) return bindings;
	for (const statement of componentBody.body ?? []) {
		if (!isNodeOfType(statement, "VariableDeclaration")) continue;
		for (const declarator of statement.declarations ?? []) {
			if (!isNodeOfType(declarator.id, "Identifier")) continue;
			if (!isNodeOfType(declarator.init, "CallExpression")) continue;
			const callee = declarator.init.callee;
			if (!isNodeOfType(callee, "Identifier")) continue;
			if (!DEFERRABLE_HOOK_NAMES.has(callee.name)) continue;
			bindings.push({
				valueName: declarator.id.name,
				hookName: callee.name,
				declarator
			});
		}
	}
	return bindings;
};
const rerenderDeferReadsHook = defineRule({
	id: "rerender-defer-reads-hook",
	title: "URL hook value only read in handlers",
	tags: ["test-noise"],
	severity: "warn",
	category: "Performance",
	recommendation: "Read the URL inside the handler (e.g. `new URL(window.location.href).searchParams`) so the component doesn't redraw every time the URL changes.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const bindings = findHookCallBindings(componentBody);
			if (bindings.length === 0) return;
			const handlerBindingNames = collectHandlerBindingNames(componentBody);
			for (const binding of bindings) {
				const referenceLocations = [];
				walkAst(componentBody, (child) => {
					if (isNodeOfType(binding.declarator, "VariableDeclarator") && child === binding.declarator.id) return;
					if (isNodeOfType(child, "Identifier") && child.name === binding.valueName) referenceLocations.push(child);
				});
				if (referenceLocations.length === 0) continue;
				if (!referenceLocations.every((ref) => isInsideEventHandler(ref, handlerBindingNames))) continue;
				context.report({
					node: binding.declarator,
					message: `${binding.hookName}() redraws your component on every URL change even though it's only read inside event handlers.`
				});
			}
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/rerender-dependencies.ts
const rerenderDependencies = defineRule({
	id: "rerender-dependencies",
	title: "Unstable value recreated every render",
	tags: ["test-noise"],
	severity: "error",
	recommendation: "Move it into a useMemo, useRef, or a constant outside the component so it stays the same between renders.",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, HOOKS_WITH_DEPS) || node.arguments.length < 2) return;
		const depsNode = node.arguments[1];
		if (!isNodeOfType(depsNode, "ArrayExpression")) return;
		for (const element of depsNode.elements ?? []) {
			if (!element) continue;
			if (isNodeOfType(element, "ObjectExpression")) context.report({
				node: element,
				message: "Your effect re-runs every render because a new object in its useEffect deps is rebuilt each time."
			});
			if (isNodeOfType(element, "ArrayExpression")) context.report({
				node: element,
				message: "Your effect re-runs every render because a new array in its useEffect deps is rebuilt each time."
			});
			if (isNodeOfType(element, "ArrowFunctionExpression") || isNodeOfType(element, "FunctionExpression")) context.report({
				node: element,
				message: "Your effect re-runs every render because the Inline function in its useEffect deps is rebuilt each time."
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/performance/rerender-derived-state-from-hook.ts
const CONTINUOUS_VALUE_HOOK_PATTERN = /^use(?:Window(?:Width|Height|Dimensions)|Scroll(?:Position|Y|X)|MousePosition|ResizeObserver|IntersectionObserver)/;
const isThresholdComparison = (node, valueName) => {
	if (!isNodeOfType(node, "BinaryExpression")) return false;
	if (![
		"<",
		"<=",
		">",
		">=",
		"===",
		"!==",
		"==",
		"!="
	].includes(node.operator)) return false;
	if (!(isNodeOfType(node.left, "Identifier") && node.left.name === valueName || isNodeOfType(node.right, "Identifier") && node.right.name === valueName)) return false;
	return isNodeOfType(node.left, "Literal") || isNodeOfType(node.right, "Literal");
};
const findThresholdDerivedBindings = (componentBody) => {
	const out = [];
	if (!isNodeOfType(componentBody, "BlockStatement")) return out;
	const statements = componentBody.body ?? [];
	for (let outerIndex = 0; outerIndex < statements.length; outerIndex++) {
		const outerStatement = statements[outerIndex];
		if (!isNodeOfType(outerStatement, "VariableDeclaration")) continue;
		for (const declarator of outerStatement.declarations ?? []) {
			if (!isNodeOfType(declarator.id, "Identifier")) continue;
			const init = declarator.init;
			if (!isNodeOfType(init, "CallExpression")) continue;
			if (!isNodeOfType(init.callee, "Identifier")) continue;
			if (!CONTINUOUS_VALUE_HOOK_PATTERN.test(init.callee.name)) continue;
			const continuousName = declarator.id.name;
			const hookName = init.callee.name;
			for (let innerIndex = outerIndex + 1; innerIndex < statements.length; innerIndex++) {
				const innerStatement = statements[innerIndex];
				if (!isNodeOfType(innerStatement, "VariableDeclaration")) break;
				let foundThreshold = false;
				for (const innerDecl of innerStatement.declarations ?? []) if (innerDecl.init && isThresholdComparison(innerDecl.init, continuousName)) {
					foundThreshold = true;
					break;
				}
				if (foundThreshold) {
					out.push({
						continuousName,
						hookName,
						declarator
					});
					break;
				}
			}
		}
	}
	return out;
};
const rerenderDerivedStateFromHook = defineRule({
	id: "rerender-derived-state-from-hook",
	title: "Continuous hook value over-renders",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use a threshold hook like `useMediaQuery(\"(max-width: 767px)\")`, so the screen only redraws when the answer changes, not on every pixel",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const bindings = findThresholdDerivedBindings(componentBody);
			for (const binding of bindings) context.report({
				node: binding.declarator,
				message: `This redraws the screen far more than needed because ${binding.hookName}() changes constantly but you only check it against a cutoff, so use a threshold hook like \`useMediaQuery("(max-width: 767px)")\` to redraw only when the answer changes`
			});
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/rerender-functional-setstate.ts
const STATE_ARITHMETIC_OPERATORS = new Set([
	"+",
	"-",
	"*",
	"/",
	"%",
	"**"
]);
const deriveStateVariableName = (setterName) => {
	if (!setterName.startsWith("set") || setterName.length < 4) return null;
	return setterName.charAt(3).toLowerCase() + setterName.slice(4);
};
const DEFERRED_EXECUTION_CALLEE_NAMES = new Set([
	"setTimeout",
	"setInterval",
	"setImmediate",
	"queueMicrotask",
	"requestAnimationFrame",
	"requestIdleCallback",
	"then",
	"catch",
	"finally",
	"subscribe",
	"addEventListener",
	"addListener",
	"on",
	"once",
	"useEffect",
	"useLayoutEffect",
	"useInsertionEffect"
]);
const isInsideDeferredCallback = (node) => {
	let current = node;
	while (current) {
		const parent = current.parent;
		if (!parent) return false;
		if ((isNodeOfType(current, "ArrowFunctionExpression") || isNodeOfType(current, "FunctionExpression") || isNodeOfType(current, "FunctionDeclaration")) && isNodeOfType(parent, "CallExpression")) {
			const callee = parent.callee;
			let calleeName = null;
			if (isNodeOfType(callee, "Identifier")) calleeName = callee.name;
			else if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.property, "Identifier")) calleeName = callee.property.name;
			if (calleeName && DEFERRED_EXECUTION_CALLEE_NAMES.has(calleeName)) return true;
		}
		current = parent;
	}
	return false;
};
const rerenderFunctionalSetstate = defineRule({
	id: "rerender-functional-setstate",
	title: "setState reads a stale value",
	severity: "warn",
	tags: ["test-noise"],
	category: "Performance",
	recommendation: "Use the callback form: `setState(prev => prev + 1)` to always read the latest value",
	create: (context) => ({ CallExpression(node) {
		if (!isSetterCall(node)) return;
		if (!node.arguments?.length) return;
		if (!isNodeOfType(node.callee, "Identifier")) return;
		if (!isUseStateSetterInScope(node, node.callee.name)) return;
		const calleeName = node.callee.name;
		const argument = node.arguments[0];
		const expectedStateName = deriveStateVariableName(calleeName);
		if (isNodeOfType(argument, "BinaryExpression") && STATE_ARITHMETIC_OPERATORS.has(argument.operator) && expectedStateName) {
			const matchesExpected = (operand) => isNodeOfType(operand, "Identifier") && operand.name === expectedStateName;
			const stateIdentifier = matchesExpected(argument.left) ? argument.left : matchesExpected(argument.right) ? argument.right : null;
			if (isNodeOfType(stateIdentifier, "Identifier")) {
				context.report({
					node,
					message: `You can lose this update because ${calleeName}(${stateIdentifier.name} ${argument.operator} ...) reads a stale value.`
				});
				return;
			}
		}
		if (isNodeOfType(argument, "UpdateExpression") && (argument.operator === "++" || argument.operator === "--") && isNodeOfType(argument.argument, "Identifier") && argument.argument.name === expectedStateName) {
			const display = argument.prefix ? `${argument.operator}${argument.argument.name}` : `${argument.argument.name}${argument.operator}`;
			context.report({
				node,
				message: `You can lose this update because ${calleeName}(${display}) reads a stale value & ++ grabs the wrong one.`
			});
			return;
		}
		if (!isInsideDeferredCallback(node)) return;
		if (expectedStateName && isNodeOfType(argument, "ArrayExpression")) {
			if ((argument.elements ?? []).some((element) => isNodeOfType(element, "SpreadElement") && isNodeOfType(element.argument, "Identifier") && element.argument.name === expectedStateName)) {
				context.report({
					node,
					message: `You can lose this update because ${calleeName}([...${expectedStateName}, ...]) reads a stale value.`
				});
				return;
			}
		}
		if (expectedStateName && isNodeOfType(argument, "ObjectExpression")) {
			if ((argument.properties ?? []).some((property) => isNodeOfType(property, "SpreadElement") && isNodeOfType(property.argument, "Identifier") && property.argument.name === expectedStateName)) {
				context.report({
					node,
					message: `You can lose this update because ${calleeName}({ ...${expectedStateName}, ... }) reads a stale value.`
				});
				return;
			}
		}
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/rerender-lazy-ref-init.ts
const rerenderLazyRefInit = defineRule({
	id: "rerender-lazy-ref-init",
	title: "Ref initializer runs on every render",
	tags: ["test-noise"],
	severity: "warn",
	category: "Performance",
	recommendation: "Set it up only once: `const ref = useRef<T | null>(null); if (ref.current === null) ref.current = expensiveCall();`",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, "useRef") || !node.arguments?.length) return;
		const initializer = node.arguments[0];
		const isPlainCall = isNodeOfType(initializer, "CallExpression");
		const isNewCall = isNodeOfType(initializer, "NewExpression");
		if (!isPlainCall && !isNewCall) return;
		const callee = initializer.callee;
		const memberPropertyName = isNodeOfType(callee, "MemberExpression") && (isNodeOfType(callee.property, "Identifier") || isNodeOfType(callee.property, "PrivateIdentifier")) ? callee.property.name : null;
		const calleeName = isNodeOfType(callee, "Identifier") ? callee.name : memberPropertyName ?? "fn";
		if (TRIVIAL_INITIALIZER_NAMES.has(calleeName)) return;
		if (isPlainCall && isReactHookName(calleeName)) return;
		const callShape = isNewCall ? `new ${calleeName}()` : `${calleeName}()`;
		context.report({
			node: initializer,
			message: `useRef(${callShape}) rebuilds this value on every render & throws it away.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/state-and-effects/rerender-lazy-state-init.ts
const rerenderLazyStateInit = defineRule({
	id: "rerender-lazy-state-init",
	title: "State initializer runs on every render",
	tags: ["test-noise"],
	severity: "warn",
	category: "Performance",
	recommendation: "Wrap in an arrow function so it only runs once: `useState(() => expensiveComputation())`",
	create: (context) => ({ CallExpression(node) {
		if (!isHookCall$1(node, "useState") || !node.arguments?.length) return;
		const initializer = node.arguments[0];
		if (!isNodeOfType(initializer, "CallExpression")) return;
		const callee = initializer.callee;
		const memberPropertyName = isNodeOfType(callee, "MemberExpression") && (isNodeOfType(callee.property, "Identifier") || isNodeOfType(callee.property, "PrivateIdentifier")) ? callee.property.name : null;
		const calleeName = isNodeOfType(callee, "Identifier") ? callee.name : memberPropertyName ?? "fn";
		if (TRIVIAL_INITIALIZER_NAMES.has(calleeName)) return;
		context.report({
			node: initializer,
			message: `useState(${calleeName}()) re-runs ${calleeName}() on every render & throws the result away.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/performance/rerender-memo-before-early-return.ts
const callbackReturnsJsx = (callback) => {
	if (!callback) return false;
	if (!isNodeOfType(callback, "ArrowFunctionExpression") && !isNodeOfType(callback, "FunctionExpression")) return false;
	const body = callback.body;
	if (isNodeOfType(body, "JSXElement") || isNodeOfType(body, "JSXFragment")) return true;
	if (!isNodeOfType(body, "BlockStatement")) return false;
	for (const stmt of body.body ?? []) if (isNodeOfType(stmt, "ReturnStatement") && (isNodeOfType(stmt.argument, "JSXElement") || isNodeOfType(stmt.argument, "JSXFragment"))) return true;
	return false;
};
const containsEarlyReturn = (ifStatement) => {
	if (!isNodeOfType(ifStatement, "IfStatement")) return false;
	const consequent = ifStatement.consequent;
	if (!consequent) return false;
	if (isNodeOfType(consequent, "ReturnStatement")) return true;
	if (!isNodeOfType(consequent, "BlockStatement")) return false;
	for (const stmt of consequent.body ?? []) if (isNodeOfType(stmt, "ReturnStatement")) return true;
	return false;
};
const rerenderMemoBeforeEarlyReturn = defineRule({
	id: "rerender-memo-before-early-return",
	title: "useMemo before an early return",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Move the JSX into a child component wrapped in memo, so the parent's early return skips it",
	create: (context) => {
		const inspectFunctionBody = (statements) => {
			let memoNode = null;
			for (const stmt of statements) {
				if (!memoNode) {
					if (!isNodeOfType(stmt, "VariableDeclaration")) continue;
					for (const declarator of stmt.declarations ?? []) {
						const init = declarator.init;
						if (isNodeOfType(init, "CallExpression") && isHookCall$1(init, "useMemo") && callbackReturnsJsx(init.arguments?.[0])) {
							memoNode = declarator;
							break;
						}
					}
					continue;
				}
				if (isNodeOfType(stmt, "IfStatement") && containsEarlyReturn(stmt)) {
					context.report({
						node: memoNode,
						message: "This runs even when the component bails out because the useMemo builds JSX before an early return, so move the JSX into a child wrapped in memo to skip it on the early return"
					});
					return;
				}
			}
		};
		return {
			FunctionDeclaration(node) {
				if (!isUppercaseName(node.id?.name ?? "")) return;
				if (!isNodeOfType(node.body, "BlockStatement")) return;
				inspectFunctionBody(node.body.body ?? []);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				const body = node.init.body;
				if (!isNodeOfType(body, "BlockStatement")) return;
				inspectFunctionBody(body.body ?? []);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/performance/rerender-memo-with-default-value.ts
const rerenderMemoWithDefaultValue = defineRule({
	id: "rerender-memo-with-default-value",
	title: "Empty default prop breaks memo",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Move it to the top of the file: `const EMPTY_ITEMS: Item[] = []`, then use that as the default value",
	create: (context) => {
		const checkDefaultProps = (params) => {
			for (const param of params) {
				if (!isNodeOfType(param, "ObjectPattern")) continue;
				for (const property of param.properties ?? []) {
					if (!isNodeOfType(property, "Property") || !isNodeOfType(property.value, "AssignmentPattern")) continue;
					const defaultValue = property.value.right;
					if (isNodeOfType(defaultValue, "ObjectExpression") && defaultValue.properties?.length === 0) context.report({
						node: defaultValue,
						message: "This keeps redrawing children that compare props because default prop value {} makes a brand new object every render, so move it to a constant at the top of the file"
					});
					if (isNodeOfType(defaultValue, "ArrayExpression") && defaultValue.elements?.length === 0) context.report({
						node: defaultValue,
						message: "This keeps redrawing children that compare props because default prop value [] makes a brand new array every render, so move it to a constant at the top of the file"
					});
				}
			}
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkDefaultProps(node.params ?? []);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkDefaultProps(node.init.params ?? []);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/state-and-effects/utils/collect-render-reachable-expressions.ts
const collectRenderReachableExpressionsFromStatements = (statements, renderReachableExpressions) => {
	let hasReturn = false;
	for (const statement of statements ?? []) if (collectRenderReachableExpressionsFromStatement(statement, renderReachableExpressions)) hasReturn = true;
	return hasReturn;
};
const collectRenderReachableExpressionsFromStatement = (statement, renderReachableExpressions) => {
	if (isNodeOfType(statement, "ReturnStatement")) {
		if (statement.argument) renderReachableExpressions.push(statement.argument);
		return true;
	}
	if (isNodeOfType(statement, "BlockStatement")) return collectRenderReachableExpressionsFromStatements(statement.body, renderReachableExpressions);
	if (isNodeOfType(statement, "IfStatement")) {
		const consequentHasReturn = collectRenderReachableExpressionsFromStatement(statement.consequent, renderReachableExpressions);
		const alternateHasReturn = statement.alternate ? collectRenderReachableExpressionsFromStatement(statement.alternate, renderReachableExpressions) : false;
		if (consequentHasReturn || alternateHasReturn) renderReachableExpressions.push(statement.test);
		return consequentHasReturn || alternateHasReturn;
	}
	if (isNodeOfType(statement, "SwitchStatement")) {
		let hasReturn = false;
		for (const switchCase of statement.cases ?? []) {
			if (!collectRenderReachableExpressionsFromStatements(switchCase.consequent, renderReachableExpressions)) continue;
			hasReturn = true;
			if (switchCase.test) renderReachableExpressions.push(switchCase.test);
		}
		if (hasReturn) renderReachableExpressions.push(statement.discriminant);
		return hasReturn;
	}
	if (isNodeOfType(statement, "TryStatement")) {
		const blockHasReturn = collectRenderReachableExpressionsFromStatement(statement.block, renderReachableExpressions);
		const handlerHasReturn = statement.handler ? collectRenderReachableExpressionsFromStatement(statement.handler.body, renderReachableExpressions) : false;
		const finalizerHasReturn = statement.finalizer ? collectRenderReachableExpressionsFromStatement(statement.finalizer, renderReachableExpressions) : false;
		return blockHasReturn || handlerHasReturn || finalizerHasReturn;
	}
	if (isNodeOfType(statement, "WhileStatement") || isNodeOfType(statement, "DoWhileStatement")) {
		const bodyHasReturn = collectRenderReachableExpressionsFromStatement(statement.body, renderReachableExpressions);
		if (bodyHasReturn) renderReachableExpressions.push(statement.test);
		return bodyHasReturn;
	}
	if (isNodeOfType(statement, "ForStatement")) {
		if (!collectRenderReachableExpressionsFromStatement(statement.body, renderReachableExpressions)) return false;
		if (statement.init) renderReachableExpressions.push(statement.init);
		if (statement.test) renderReachableExpressions.push(statement.test);
		if (statement.update) renderReachableExpressions.push(statement.update);
		return true;
	}
	if (isNodeOfType(statement, "ForInStatement") || isNodeOfType(statement, "ForOfStatement")) {
		if (!collectRenderReachableExpressionsFromStatement(statement.body, renderReachableExpressions)) return false;
		renderReachableExpressions.push(statement.right);
		return true;
	}
	if (isNodeOfType(statement, "LabeledStatement")) return collectRenderReachableExpressionsFromStatement(statement.body, renderReachableExpressions);
	if (isNodeOfType(statement, "WithStatement")) {
		const bodyHasReturn = collectRenderReachableExpressionsFromStatement(statement.body, renderReachableExpressions);
		if (bodyHasReturn) renderReachableExpressions.push(statement.object);
		return bodyHasReturn;
	}
	return false;
};
const collectRenderReachableExpressions = (componentBody) => {
	if (!isNodeOfType(componentBody, "BlockStatement")) return [];
	const renderReachableExpressions = [];
	collectRenderReachableExpressionsFromStatements(componentBody.body, renderReachableExpressions);
	return renderReachableExpressions;
};
//#endregion
//#region src/plugin/rules/state-and-effects/rerender-state-only-in-handlers.ts
const rerenderStateOnlyInHandlers = defineRule({
	id: "rerender-state-only-in-handlers",
	title: "State only used in handlers",
	severity: "warn",
	tags: ["test-noise"],
	category: "Performance",
	recommendation: "Use useRef instead of useState when the value is only set and never shown on screen. `ref.current = ...` updates it without redrawing the component.",
	create: (context) => {
		const checkComponent = (componentBody) => {
			if (!componentBody || !isNodeOfType(componentBody, "BlockStatement")) return;
			const bindings = collectUseStateBindings(componentBody);
			if (bindings.length === 0) return;
			if (collectRenderReachableExpressions(componentBody).length === 0) return;
			const eventHandlerReferenceNames = collectFunctionLikeLocalNames(componentBody);
			const dependencyGraph = buildLocalDependencyGraph(componentBody, eventHandlerReferenceNames);
			const renderReachableNames = expandTransitiveDependencies(collectRenderReachableNames(componentBody, eventHandlerReferenceNames), dependencyGraph);
			for (const binding of bindings) {
				if (renderReachableNames.has(binding.valueName)) continue;
				if (binding.valueName === "_" || binding.valueName.startsWith("_")) continue;
				const setterSuffix = binding.setterName.slice(3);
				if (/^(TriggerRender|ForceUpdate|Rerender|ForceRender|Tick|Bump|BumpVersion|InvalidateRender|Refresh|Repaint)$/i.test(setterSuffix)) continue;
				let setterCalled = false;
				walkAst(componentBody, (child) => {
					if (setterCalled) return;
					if (isNodeOfType(child, "CallExpression") && isNodeOfType(child.callee, "Identifier") && child.callee.name === binding.setterName) setterCalled = true;
				});
				if (!setterCalled) continue;
				context.report({
					node: binding.declarator,
					message: `Each update to "${binding.valueName}" redraws your component for nothing because this useState is set but never shown on screen.`
				});
			}
		};
		return {
			FunctionDeclaration(node) {
				if (!node.id?.name || !isUppercaseName(node.id.name)) return;
				checkComponent(node.body);
			},
			VariableDeclarator(node) {
				if (!isComponentAssignment(node)) return;
				if (!isNodeOfType(node.init, "ArrowFunctionExpression") && !isNodeOfType(node.init, "FunctionExpression")) return;
				checkComponent(node.init.body);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/performance/rerender-transitions-scroll.ts
const HIGH_FREQUENCY_DOM_EVENTS = new Set([
	"scroll",
	"mousemove",
	"wheel",
	"pointermove",
	"touchmove",
	"drag"
]);
const isAddEventListenerCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (!isNodeOfType(node.callee, "MemberExpression")) return false;
	if (!isNodeOfType(node.callee.property, "Identifier")) return false;
	if (node.callee.property.name !== "addEventListener") return false;
	return true;
};
const handlerCallsSetState = (handler) => {
	if (!isNodeOfType(handler, "ArrowFunctionExpression") && !isNodeOfType(handler, "FunctionExpression")) return null;
	let setStateCall = null;
	walkAst(handler.body, (child) => {
		if (setStateCall) return;
		if (isNodeOfType(child, "CallExpression") && isNodeOfType(child.callee, "Identifier") && /^set[A-Z]/.test(child.callee.name) && isUseStateSetterInScope(child, child.callee.name)) setStateCall = child;
	});
	return setStateCall;
};
const rerenderTransitionsScroll = defineRule({
	id: "rerender-transitions-scroll",
	title: "setState in a scroll handler",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Wrap the setState in startTransition, use useDeferredValue, or keep the value in a ref and throttle with requestAnimationFrame, so these events don't redraw the screen every time",
	create: (context) => ({ CallExpression(node) {
		if (!isAddEventListenerCall(node)) return;
		const eventArg = node.arguments?.[0];
		if (!isNodeOfType(eventArg, "Literal")) return;
		const eventName = eventArg.value;
		if (typeof eventName !== "string" || !HIGH_FREQUENCY_DOM_EVENTS.has(eventName)) return;
		const handler = node.arguments?.[1];
		if (!handler) return;
		const setStateCall = handlerCallsSetState(handler);
		if (!setStateCall) return;
		let cursor = setStateCall.parent ?? null;
		while (cursor && cursor !== handler) {
			if (isNodeOfType(cursor, "CallExpression") && isNodeOfType(cursor.callee, "Identifier") && (cursor.callee.name === "startTransition" || cursor.callee.name === "requestAnimationFrame" || cursor.callee.name === "requestIdleCallback")) return;
			cursor = cursor.parent ?? null;
		}
		context.report({
			node: setStateCall,
			message: `This causes jank because setState in a "${eventName}" handler redraws the screen many times a second, so wrap it in startTransition, use useDeferredValue, or keep the value in a ref & throttle with requestAnimationFrame`
		});
	} })
});
//#endregion
//#region src/plugin/utils/define-retired-rule.ts
const defineRetiredRule = (rule) => defineRule({
	...rule,
	defaultEnabled: false,
	lifecycle: "retired",
	create: () => ({})
});
//#endregion
//#region src/plugin/rules/react-native/rn-animate-layout-property.ts
const rnAnimateLayoutProperty = defineRetiredRule({
	id: "rn-animate-layout-property",
	title: "Animating a layout property",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Reanimated useAnimatedStyle runs on the UI thread; layout-affecting properties driven by animation helpers, interpolate, or shared values are valid."
});
//#endregion
//#region src/plugin/rules/react-native/rn-animation-reaction-as-derived.ts
const rnAnimationReactionAsDerived = defineRule({
	id: "rn-animation-reaction-as-derived",
	title: "useAnimatedReaction just copies a value",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "This useAnimatedReaction just copies one value to another. Replace it with `useDerivedValue(() => ..., [deps])`, which is shorter and tracks changes for you.",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "Identifier") || node.callee.name !== "useAnimatedReaction") return;
		const reactionFn = node.arguments?.[1];
		if (!reactionFn) return;
		if (!isNodeOfType(reactionFn, "ArrowFunctionExpression") && !isNodeOfType(reactionFn, "FunctionExpression")) return;
		const body = reactionFn.body;
		let singleAssignment = null;
		if (isNodeOfType(body, "BlockStatement")) {
			const statements = body.body ?? [];
			if (statements.length !== 1) return;
			const onlyStatement = statements[0];
			if (!isNodeOfType(onlyStatement, "ExpressionStatement")) return;
			singleAssignment = onlyStatement.expression;
		} else if (body) singleAssignment = body;
		if (!singleAssignment) return;
		const isValueAssignment = isNodeOfType(singleAssignment, "AssignmentExpression") && isNodeOfType(singleAssignment.left, "MemberExpression") && isNodeOfType(singleAssignment.left.property, "Identifier") && singleAssignment.left.property.name === "value";
		const isSetCall = isNodeOfType(singleAssignment, "CallExpression") && isNodeOfType(singleAssignment.callee, "MemberExpression") && isNodeOfType(singleAssignment.callee.property, "Identifier") && singleAssignment.callee.property.name === "set" && (singleAssignment.arguments?.length ?? 0) === 1;
		if (!isValueAssignment && !isSetCall) return;
		context.report({
			node,
			message: "Your users can see a stale value when this useAnimatedReaction only copies one value to another."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-bottom-sheet-prefer-native.ts
const JS_BOTTOM_SHEET_PACKAGES = new Set([
	"react-native-bottom-sheet",
	"react-native-modal-bottom-sheet",
	"react-native-raw-bottom-sheet",
	"react-native-modalize",
	"react-native-actions-sheet",
	"react-native-bottomsheet-reanimated",
	"@discord/bottom-sheet"
]);
const rnBottomSheetPreferNative = defineRule({
	id: "rn-bottom-sheet-prefer-native",
	title: "JS bottom sheet over native Modal",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "On RN v7+, use `<Modal presentationStyle=\"formSheet\">` for native gestures and snap points.",
	create: (context) => ({ ImportDeclaration(node) {
		const source = node.source?.value;
		if (typeof source !== "string" || !JS_BOTTOM_SHEET_PACKAGES.has(source)) return;
		context.report({
			node,
			message: `Your users feel a less native bottom sheet with ${source}.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-detox-missing-await.ts
const EMPTY_VISITORS$2 = {};
const DETOX_TEST_FILE = /(\.e2e\.[cm]?[jt]sx?$)|((^|\/)e2e\/)/;
const DETOX_ELEMENT_ACTIONS = new Set([
	"tap",
	"multiTap",
	"longPress",
	"longPressAndDrag",
	"swipe",
	"scroll",
	"scrollTo",
	"scrollToIndex",
	"scrollToElement",
	"typeText",
	"replaceText",
	"clearText",
	"tapReturnKey",
	"tapBackspaceKey",
	"pinch",
	"setColumnToValue",
	"setDatePickerDate",
	"performAccessibilityAction",
	"adjustSliderToPosition"
]);
const PROMISE_SETTLE_METHODS = new Set([
	"then",
	"catch",
	"finally"
]);
const findChainRoot = (node) => {
	if (isNodeOfType(node, "CallExpression")) {
		if (isNodeOfType(node.callee, "Identifier")) return {
			calleeName: node.callee.name,
			rootCall: node
		};
		if (isNodeOfType(node.callee, "MemberExpression")) return findChainRoot(node.callee.object);
		return null;
	}
	if (isNodeOfType(node, "MemberExpression")) return findChainRoot(node.object);
	return null;
};
const isDetoxExpectSubject = (rootCall) => {
	const firstArgument = rootCall.arguments?.[0];
	if (!firstArgument || !isNodeOfType(firstArgument, "CallExpression")) return false;
	if (!isNodeOfType(firstArgument.callee, "Identifier")) return false;
	return firstArgument.callee.name === "element" || firstArgument.callee.name === "web";
};
const getTerminalMethodName = (callExpression) => {
	const callee = callExpression.callee;
	if (!isNodeOfType(callee, "MemberExpression")) return null;
	if (callee.computed || !isNodeOfType(callee.property, "Identifier")) return null;
	return callee.property.name;
};
const rnDetoxMissingAwait = defineRule({
	id: "rn-detox-missing-await",
	title: "Un-awaited Detox action",
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Prepend `await` to Detox actions, `waitFor(...)` chains, and `expect(element(...))` assertions. They return promises tied to Detox's synchronization, so a missing await runs steps out of order.",
	create: (context) => {
		const filename = normalizeFilename$1(context.filename ?? "");
		if (!filename || !DETOX_TEST_FILE.test(filename)) return EMPTY_VISITORS$2;
		return { ExpressionStatement(node) {
			const expression = node.expression;
			if (!isNodeOfType(expression, "CallExpression")) return;
			const terminalMethod = getTerminalMethodName(expression);
			if (terminalMethod === null) return;
			if (PROMISE_SETTLE_METHODS.has(terminalMethod)) return;
			const root = findChainRoot(expression);
			if (!root) return;
			if (root.calleeName === "element") {
				if (!DETOX_ELEMENT_ACTIONS.has(terminalMethod)) return;
				context.report({
					node,
					message: `This Detox action (\`${terminalMethod}\`) isn't awaited, so it runs out of order and can race. Prepend \`await\`.`
				});
				return;
			}
			if (root.calleeName === "waitFor") {
				context.report({
					node,
					message: "This Detox `waitFor(...)` chain isn't awaited. Prepend `await`."
				});
				return;
			}
			if (root.calleeName === "expect" && isDetoxExpectSubject(root.rootCall)) context.report({
				node,
				message: "This Detox `expect(element(...))` assertion isn't awaited. Prepend `await`."
			});
		} };
	}
});
//#endregion
//#region src/plugin/constants/react-native.ts
const REACT_NATIVE_TEXT_COMPONENTS = new Set([
	"Text",
	"TextInput",
	"Typography",
	"Paragraph",
	"Span",
	"H1",
	"H2",
	"H3",
	"H4",
	"H5",
	"H6"
]);
const REACT_NATIVE_TEXT_COMPONENT_KEYWORDS = new Set([
	"Text",
	"Title",
	"Label",
	"Heading",
	"Caption",
	"Subtitle",
	"Typography",
	"Paragraph",
	"Description",
	"Body"
]);
const REACT_NATIVE_TEXT_TRANSPARENT_COMPONENTS = new Set(["fbt", "fbs"]);
const DEPRECATED_RN_MODULE_REPLACEMENTS = new Map([
	["AsyncStorage", "@react-native-async-storage/async-storage"],
	["Picker", "@react-native-picker/picker"],
	["PickerIOS", "@react-native-picker/picker"],
	["DatePickerIOS", "@react-native-community/datetimepicker"],
	["DatePickerAndroid", "@react-native-community/datetimepicker"],
	["ProgressBarAndroid", "a community alternative"],
	["ProgressViewIOS", "a community alternative"],
	["SafeAreaView", "react-native-safe-area-context"],
	["Slider", "@react-native-community/slider"],
	["ViewPagerAndroid", "react-native-pager-view"],
	["WebView", "react-native-webview"],
	["NetInfo", "@react-native-community/netinfo"],
	["CameraRoll", "@react-native-camera-roll/camera-roll"],
	["Clipboard", "@react-native-clipboard/clipboard"],
	["ImageEditor", "@react-native-community/image-editor"],
	["MaskedViewIOS", "@react-native-masked-view/masked-view"]
]);
const LEGACY_EXPO_PACKAGE_REPLACEMENTS = new Map([
	["expo-av", "expo-audio for audio and expo-video for video"],
	["expo-permissions", "the permissions API in each module (e.g. Camera.requestPermissionsAsync())"],
	["expo-app-loading", "expo-splash-screen"],
	["react-native-fast-image", "expo-image (drop-in with caching, placeholders, and crossfades)"]
]);
const EXPO_UI_MODULE_SOURCES = new Set([
	"@expo/ui",
	"@expo/ui/swift-ui",
	"@expo/ui/jetpack-compose"
]);
const REACT_NATIVE_LIST_COMPONENTS = new Set([
	"FlatList",
	"SectionList",
	"VirtualizedList",
	"FlashList",
	"LegendList"
]);
const RENDER_ITEM_PROP_NAMES = new Set([
	"renderItem",
	"renderSectionHeader",
	"renderSectionFooter"
]);
const LEGACY_SHADOW_STYLE_PROPERTIES = new Set([
	"shadowColor",
	"shadowOffset",
	"shadowOpacity",
	"shadowRadius",
	"elevation"
]);
//#endregion
//#region src/plugin/rules/react-native/rn-list-callback-per-row.ts
const LIST_ROW_PRESS_HANDLER_PROPS = new Set([
	"onPress",
	"onLongPress",
	"onPressIn",
	"onPressOut",
	"onSelect",
	"onClick"
]);
const detectInlineRowHandlers = (renderItemFn) => {
	const inlineHandlers = [];
	if (!isNodeOfType(renderItemFn, "ArrowFunctionExpression") && !isNodeOfType(renderItemFn, "FunctionExpression")) return inlineHandlers;
	walkAst(renderItemFn.body, (child) => {
		if (!isNodeOfType(child, "JSXAttribute")) return;
		if (!isNodeOfType(child.name, "JSXIdentifier")) return;
		if (!LIST_ROW_PRESS_HANDLER_PROPS.has(child.name.name)) return;
		if (!isNodeOfType(child.value, "JSXExpressionContainer")) return;
		const expression = child.value.expression;
		if (isNodeOfType(expression, "ArrowFunctionExpression") || isNodeOfType(expression, "FunctionExpression")) inlineHandlers.push(child);
	});
	return inlineHandlers;
};
const isRenderItemJsxAttribute = (parent) => {
	if (!isNodeOfType(parent, "JSXAttribute")) return false;
	const attrName = isNodeOfType(parent.name, "JSXIdentifier") ? parent.name.name : null;
	return attrName ? RENDER_ITEM_PROP_NAMES.has(attrName) : false;
};
const isRenderItemFunction = (node) => {
	const parent = node.parent;
	if (!isNodeOfType(parent, "JSXExpressionContainer")) return false;
	return isRenderItemJsxAttribute(parent.parent);
};
const rnListCallbackPerRow = defineRule({
	id: "rn-list-callback-per-row",
	title: "Inline handler in list renderItem",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Move the handler out with useCallback at list scope and pass the row id as a prop. Then memo() rows skip redrawing when their data has not changed.",
	create: (context) => {
		const inspect = (node) => {
			if (!isRenderItemFunction(node)) return;
			const inlineHandlers = detectInlineRowHandlers(node);
			for (const handler of inlineHandlers) {
				const handlerName = isNodeOfType(handler, "JSXAttribute") && isNodeOfType(handler.name, "JSXIdentifier") ? handler.name.name : "<handler>";
				context.report({
					node: handler,
					message: `This ${handlerName} is rebuilt for every row, so your memo() rows still redraw.`
				});
			}
		};
		return {
			ArrowFunctionExpression: inspect,
			FunctionExpression: inspect
		};
	}
});
//#endregion
//#region src/plugin/rules/react-native/utils/resolve-jsx-element-name.ts
const resolveJsxElementName = (openingElement) => {
	if (!isNodeOfType(openingElement, "JSXOpeningElement")) return null;
	const elementName = openingElement.name;
	if (!elementName) return null;
	if (isNodeOfType(elementName, "JSXIdentifier")) return elementName.name;
	if (isNodeOfType(elementName, "JSXMemberExpression")) return isNodeOfType(elementName.property, "JSXIdentifier") ? elementName.property.name : null;
	return null;
};
//#endregion
//#region src/plugin/rules/react-native/rn-list-data-mapped.ts
const FRESH_ARRAY_METHODS = new Set([
	"map",
	"filter",
	"toSorted",
	"slice",
	"toReversed",
	"concat",
	"flat",
	"flatMap",
	"toSpliced"
]);
const isFreshArrayExpression = (node) => {
	if (isNodeOfType(node, "ArrayExpression")) return "[...spread]";
	if (isNodeOfType(node, "CallExpression")) {
		const callee = node.callee;
		if (isNodeOfType(callee, "MemberExpression")) {
			if (isNodeOfType(callee.property, "Identifier")) {
				const methodName = callee.property.name;
				if (FRESH_ARRAY_METHODS.has(methodName)) return `.${methodName}(…)`;
				if (methodName === "from" && isNodeOfType(callee.object, "Identifier") && callee.object.name === "Array") return "Array.from(…)";
			}
			return isFreshArrayExpression(callee.object);
		}
		if (isNodeOfType(callee, "Identifier") && callee.name === "Array") return "Array(…)";
	}
	return null;
};
const rnListDataMapped = defineRule({
	id: "rn-list-data-mapped",
	title: "List data rebuilt every render",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "This builds a new array each time the parent redraws, so every row redraws too. Wrap it in `useMemo(() => items.map(...), [items])` to keep the same array.",
	create: (context) => ({ JSXOpeningElement(node) {
		const elementName = resolveJsxElementName(node);
		if (!elementName || !REACT_NATIVE_LIST_COMPONENTS.has(elementName)) return;
		for (const attr of node.attributes ?? []) {
			if (!isNodeOfType(attr, "JSXAttribute")) continue;
			if (!isNodeOfType(attr.name, "JSXIdentifier") || attr.name.name !== "data") continue;
			if (!isNodeOfType(attr.value, "JSXExpressionContainer")) continue;
			const expression = attr.value.expression;
			if (!isFreshArrayExpression(expression)) continue;
			context.report({
				node: attr,
				message: `Your users see every row redraw when <${elementName}> gets a new data array each render.`
			});
			return;
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-list-missing-estimated-item-size.ts
const RECYCLABLE_LIST_PACKAGES = {
	FlashList: ["@shopify/flash-list"],
	LegendList: ["@legendapp/list"]
};
const SIZING_HINT_ATTRIBUTE_NAMES = new Set(["estimatedItemSize", "estimatedListSize"]);
const isFlashListV2OrNewer = (context) => {
	const flashListMajorVersion = getReactDoctorNumberSetting(context.settings, "shopifyFlashListMajorVersion");
	return flashListMajorVersion !== void 0 && flashListMajorVersion >= 2;
};
const isEmptyArrayLiteral = (node) => {
	if (!isNodeOfType(node.value, "JSXExpressionContainer")) return false;
	const expression = node.value.expression;
	return isNodeOfType(expression, "ArrayExpression") && (expression.elements?.length ?? 0) === 0;
};
const rnListMissingEstimatedItemSize = defineRule({
	id: "rn-list-missing-estimated-item-size",
	title: "List missing estimatedItemSize",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Without `estimatedItemSize` the list guesses row height and can flash blank cells on fast scroll. Add `estimatedItemSize={<avg-row-height-in-px>}` so it matches your rows.",
	create: (context) => ({ JSXOpeningElement(node) {
		const localElementName = resolveJsxElementName(node);
		if (!localElementName) return;
		let canonicalRecyclerName = null;
		for (const [canonicalName, packageSources] of Object.entries(RECYCLABLE_LIST_PACKAGES)) if (packageSources.some((packageSource) => getImportedNameFromModule(node, localElementName, packageSource) === canonicalName)) {
			canonicalRecyclerName = canonicalName;
			break;
		}
		if (!canonicalRecyclerName) return;
		if (canonicalRecyclerName === "FlashList" && isFlashListV2OrNewer(context)) return;
		let hasSizingHint = false;
		let dataIsEmptyLiteral = false;
		let hasDataProp = false;
		for (const attribute of node.attributes ?? []) {
			if (!isNodeOfType(attribute, "JSXAttribute")) continue;
			if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
			const attributeName = attribute.name.name;
			if (SIZING_HINT_ATTRIBUTE_NAMES.has(attributeName)) hasSizingHint = true;
			if (attributeName === "data") {
				hasDataProp = true;
				if (isEmptyArrayLiteral(attribute)) dataIsEmptyLiteral = true;
			}
		}
		if (hasSizingHint) return;
		if (dataIsEmptyLiteral) return;
		if (!hasDataProp) return;
		context.report({
			node,
			message: `Your users see blank cells flash on fast scroll when <${localElementName}> has no \`estimatedItemSize\`.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-list-recyclable-without-types.ts
const RECYCLABLE_LIST_NAMES = new Set(["FlashList", "LegendList"]);
const rnListRecyclableWithoutTypes = defineRule({
	id: "rn-list-recyclable-without-types",
	title: "Recyclable list missing getItemType",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "When rows have different shapes, reused cells can show the wrong layout. Add `getItemType={item => item.kind}` so FlashList keeps a separate pool per row type.",
	create: (context) => ({ JSXOpeningElement(node) {
		const elementName = resolveJsxElementName(node);
		if (!elementName || !RECYCLABLE_LIST_NAMES.has(elementName)) return;
		let hasRecycleItemsEnabled = false;
		let hasGetItemType = false;
		for (const attr of node.attributes ?? []) {
			if (!isNodeOfType(attr, "JSXAttribute")) continue;
			if (!isNodeOfType(attr.name, "JSXIdentifier")) continue;
			if (attr.name.name === "recycleItems") if (!attr.value) hasRecycleItemsEnabled = true;
			else if (isNodeOfType(attr.value, "JSXExpressionContainer") && isNodeOfType(attr.value.expression, "Literal")) hasRecycleItemsEnabled = attr.value.expression.value === true;
			else hasRecycleItemsEnabled = true;
			if (attr.name.name === "getItemType") hasGetItemType = true;
		}
		if (hasRecycleItemsEnabled && !hasGetItemType) context.report({
			node,
			message: `Your users see rows of different shapes reuse the wrong cells when <${elementName} recycleItems> has no \`getItemType\`.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-deep-imports.ts
const DEEP_IMPORT_PREFIX = "react-native/Libraries/";
const NEW_APP_SCREEN_PATH = "react-native/Libraries/NewAppScreen";
const PUBLIC_RN_ROOT_EXPORTS = new Set([
	"View",
	"Text",
	"Image",
	"ImageBackground",
	"ScrollView",
	"FlatList",
	"SectionList",
	"VirtualizedList",
	"TextInput",
	"Pressable",
	"TouchableOpacity",
	"TouchableHighlight",
	"TouchableWithoutFeedback",
	"TouchableNativeFeedback",
	"Button",
	"Switch",
	"Modal",
	"ActivityIndicator",
	"RefreshControl",
	"KeyboardAvoidingView",
	"StyleSheet",
	"Alert",
	"Animated",
	"Platform",
	"Dimensions",
	"AppRegistry",
	"AppState",
	"Linking",
	"Appearance",
	"Keyboard",
	"StatusBar",
	"PixelRatio",
	"PanResponder",
	"BackHandler",
	"InteractionManager"
]);
const lastPathSegment = (source) => {
	const segments = source.split("/");
	return segments[segments.length - 1] ?? "";
};
const classifyDeepImport = (source) => {
	if (typeof source !== "string") return null;
	if (!source.startsWith(DEEP_IMPORT_PREFIX)) return null;
	if (source === NEW_APP_SCREEN_PATH || source.startsWith(`${NEW_APP_SCREEN_PATH}/`)) return { message: "`react-native/Libraries/NewAppScreen` was moved out of core in React Native 0.80; import from `@react-native/new-app-screen` instead." };
	const exportName = lastPathSegment(source);
	if (PUBLIC_RN_ROOT_EXPORTS.has(exportName)) return { message: `Deep import from "${source}" is a deprecated React Native internal subpath (RFC 0894) and breaks on upgrade. Import from "react-native" instead.` };
	return null;
};
const isEverySpecifierInlineType = (specifiers, specifierType, kindField) => {
	if (!specifiers || specifiers.length === 0) return false;
	return specifiers.every((specifier) => isNodeOfType(specifier, specifierType) && specifier[kindField] === "type");
};
const rnNoDeepImports = defineRule({
	id: "rn-no-deep-imports",
	title: "Deep import into react-native internals",
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Import the symbol from `react-native` (the package root) instead of the deprecated `react-native/Libraries/...` subpath, which RFC 0894 removes on upgrade.",
	create: (context) => {
		const reportFinding = (node, source) => {
			const finding = classifyDeepImport(source);
			if (finding) context.report({
				node,
				message: finding.message
			});
		};
		return {
			ImportDeclaration(node) {
				if (node.importKind === "type") return;
				if (isEverySpecifierInlineType(node.specifiers, "ImportSpecifier", "importKind")) return;
				reportFinding(node, node.source?.value);
			},
			ExportNamedDeclaration(node) {
				if (node.exportKind === "type") return;
				if (!node.source) return;
				if (isEverySpecifierInlineType(node.specifiers, "ExportSpecifier", "exportKind")) return;
				reportFinding(node, node.source.value);
			},
			ExportAllDeclaration(node) {
				if (node.exportKind === "type") return;
				reportFinding(node, node.source?.value);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-deprecated-modules.ts
const rnNoDeprecatedModules = defineRule({
	id: "rn-no-deprecated-modules",
	title: "Module removed from react-native core",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "error",
	recommendation: "These modules were removed from react-native core. Import them from the community package instead.",
	create: (context) => ({ ImportDeclaration(node) {
		if (node.source?.value !== "react-native") return;
		for (const specifier of node.specifiers ?? []) {
			if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
			const importedName = getImportedName$1(specifier);
			if (!importedName) continue;
			if (!DEPRECATED_RN_MODULE_REPLACEMENTS.get(importedName)) continue;
			context.report({
				node: specifier,
				message: `Your users hit a crash from "${importedName}", which was removed from react-native.`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-dimensions-get.ts
const rnNoDimensionsGet = defineRule({
	id: "rn-no-dimensions-get",
	title: "Dimensions.get over useWindowDimensions",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `const { width, height } = useWindowDimensions()` so the size updates automatically on rotation and resize.",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		if (!isNodeOfType(node.callee.object, "Identifier") || node.callee.object.name !== "Dimensions") return;
		if (isMemberProperty(node.callee, "get")) context.report({
			node,
			message: "Your users see a stale layout on rotation or resize because Dimensions.get() does not update."
		});
		if (isMemberProperty(node.callee, "addEventListener")) context.report({
			node,
			message: "Your users hit a crash from Dimensions.addEventListener(), which was removed in React Native 0.72."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-falsy-and-render.ts
const NUMERIC_NAME_HINTS = [
	"count",
	"length",
	"total",
	"size",
	"num",
	"index",
	"amount",
	"quantity",
	"offset",
	"width",
	"height",
	"duration",
	"progress",
	"score",
	"rank",
	"level",
	"step",
	"max",
	"min",
	"sum",
	"avg",
	"depth",
	"balance",
	"age",
	"weight",
	"volume",
	"distance",
	"speed",
	"rate",
	"ratio",
	"percent",
	"percentage"
];
const BOOLEAN_PREFIXES = [
	"is",
	"has",
	"can",
	"should",
	"did",
	"will",
	"show",
	"hide",
	"enable",
	"disable"
];
const isNumericName = (name) => {
	const lower = name.toLowerCase();
	for (const prefix of BOOLEAN_PREFIXES) if (lower.startsWith(prefix) && name.length > prefix.length && name[prefix.length] === name[prefix.length].toUpperCase()) return false;
	for (const hint of NUMERIC_NAME_HINTS) {
		if (lower === hint) return true;
		const camelSuffix = hint.charAt(0).toUpperCase() + hint.slice(1);
		if (name.endsWith(camelSuffix)) return true;
		if (lower.endsWith(`_${hint}`)) return true;
	}
	return false;
};
const isLikelyNumericExpression = (node) => {
	if (isNodeOfType(node, "MemberExpression") && isNodeOfType(node.property, "Identifier") && node.property.name === "length") return true;
	if (isNodeOfType(node, "Identifier") && isNumericName(node.name)) return true;
	if (isNodeOfType(node, "MemberExpression") && isNodeOfType(node.property, "Identifier") && isNumericName(node.property.name)) return true;
	return false;
};
const rnNoFalsyAndRender = defineRule({
	id: "rn-no-falsy-and-render",
	title: "Numeric && renders bare zero",
	requires: ["react-native"],
	severity: "error",
	recommendation: "When the number is 0, this shows a bare `0` as text, which crashes on RN. Use `{value > 0 && <X />}`, `{Boolean(value) && <X />}`, or `{value ? <X /> : null}`.",
	create: (context) => {
		let isDomComponentFile = false;
		return {
			Program(programNode) {
				isDomComponentFile = hasDirective(programNode, "use dom");
			},
			LogicalExpression(node) {
				if (isDomComponentFile) return;
				if (node.operator !== "&&") return;
				if (!(isNodeOfType(node.right, "JSXElement") || isNodeOfType(node.right, "JSXFragment"))) return;
				const parent = node.parent;
				if (!(isNodeOfType(parent, "JSXExpressionContainer") || isNodeOfType(parent, "LogicalExpression") && parent.operator === "&&")) return;
				const left = node.left;
				if (!left) return;
				if (!isLikelyNumericExpression(left)) return;
				context.report({
					node: left,
					message: "Your users hit a crash when this value is 0 & renders a bare `0` as text."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-image-children.ts
const isMeaningfulImageChild = (child) => {
	if (isNodeOfType(child, "JSXElement") || isNodeOfType(child, "JSXFragment")) return true;
	if (isNodeOfType(child, "JSXText")) return (child.value ?? "").trim().length > 0;
	if (isNodeOfType(child, "JSXExpressionContainer")) {
		const expression = child.expression;
		if (isNodeOfType(expression, "JSXEmptyExpression")) return false;
		if (isNodeOfType(expression, "Literal") && (expression.value === null || expression.value === false)) return false;
		if (isNodeOfType(expression, "Identifier") && expression.name === "undefined") return false;
		return true;
	}
	return false;
};
const rnNoImageChildren = defineRule({
	id: "rn-no-image-children",
	title: "Children inside react-native <Image>",
	requires: ["react-native"],
	severity: "error",
	recommendation: "React Native's `<Image>` can't render children. Use `<ImageBackground>` (same `source`/`style` props) to layer content over an image.",
	create: (context) => ({ JSXElement(node) {
		const openingElement = node.openingElement;
		if (!openingElement) return;
		const localName = resolveJsxElementName(openingElement);
		if (!localName) return;
		if (getImportedNameFromModule(openingElement, localName, "react-native") !== "Image") return;
		if (!(node.children ?? []).some(isMeaningfulImageChild)) return;
		context.report({
			node: openingElement,
			message: "React Native's <Image> does not render children, so this content silently disappears. Use <ImageBackground> to layer content over an image."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-inline-flatlist-renderitem.ts
const rnNoInlineFlatlistRenderitem = defineRule({
	id: "rn-no-inline-flatlist-renderitem",
	title: "Inline renderItem on list",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Move renderItem to a named function or wrap it in useCallback so it is not rebuilt every time the screen redraws.",
	create: (context) => ({ JSXAttribute(node) {
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "renderItem") return;
		if (!node.value || !isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const openingElement = node.parent;
		if (!openingElement || !isNodeOfType(openingElement, "JSXOpeningElement")) return;
		const listComponentName = resolveJsxElementName(openingElement);
		if (!listComponentName || !REACT_NATIVE_LIST_COMPONENTS.has(listComponentName)) return;
		const expression = node.value.expression;
		if (!isNodeOfType(expression, "ArrowFunctionExpression") && !isNodeOfType(expression, "FunctionExpression")) return;
		context.report({
			node: expression,
			message: `Your users see extra row work when renderItem on <${listComponentName}> is rebuilt every time the screen redraws.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-inline-object-in-list-item.ts
const rnNoInlineObjectInListItem = defineRule({
	id: "rn-no-inline-object-in-list-item",
	title: "Inline object in list renderItem",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Move style and object props out of renderItem (StyleSheet.create, useMemo at list scope, or pass primitives) so memo() rows stop redrawing when their data has not changed.",
	create: (context) => {
		const renderPropStack = [];
		const resolveRenderPropName = (node) => {
			if (!isNodeOfType(node, "ArrowFunctionExpression") && !isNodeOfType(node, "FunctionExpression")) return null;
			const expressionContainer = node.parent;
			if (!isNodeOfType(expressionContainer, "JSXExpressionContainer")) return null;
			const attr = expressionContainer.parent;
			if (!isNodeOfType(attr, "JSXAttribute")) return null;
			const attrName = isNodeOfType(attr.name, "JSXIdentifier") ? attr.name.name : null;
			return attrName && RENDER_ITEM_PROP_NAMES.has(attrName) ? attrName : null;
		};
		const enter = (node) => {
			const renderPropName = resolveRenderPropName(node);
			if (renderPropName) renderPropStack.push(renderPropName);
		};
		const exit = (node) => {
			if (resolveRenderPropName(node)) renderPropStack.pop();
		};
		return {
			ArrowFunctionExpression: enter,
			"ArrowFunctionExpression:exit": exit,
			FunctionExpression: enter,
			"FunctionExpression:exit": exit,
			JSXAttribute(node) {
				if (renderPropStack.length === 0) return;
				if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
				const expression = node.value.expression;
				const isInlineObject = isNodeOfType(expression, "ObjectExpression");
				const isInlineArray = isNodeOfType(expression, "ArrayExpression");
				if (!isInlineObject && !isInlineArray) return;
				const literalKind = isInlineArray ? "array" : "object";
				context.report({
					node,
					message: `This ${literalKind} is rebuilt for every row, so your memo() rows still redraw.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-legacy-expo-packages.ts
const rnNoLegacyExpoPackages = defineRule({
	id: "rn-no-legacy-expo-packages",
	title: "Unmaintained legacy Expo package",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "These Expo packages are no longer maintained. Switch to the recommended replacement package.",
	create: (context) => ({ ImportDeclaration(node) {
		const source = node.source?.value;
		if (typeof source !== "string") return;
		for (const [packageName] of LEGACY_EXPO_PACKAGE_REPLACEMENTS) if (source === packageName || source.startsWith(`${packageName}/`)) {
			context.report({
				node,
				message: `Your users are exposed to unfixed bugs when "${packageName}" is no longer maintained.`
			});
			return;
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-legacy-shadow-styles.ts
const reportLegacyShadowProperties = (objectExpression, context) => {
	const legacyShadowPropertyNames = [];
	for (const property of objectExpression.properties ?? []) {
		if (!isNodeOfType(property, "Property")) continue;
		const propertyName = isNodeOfType(property.key, "Identifier") ? property.key.name : null;
		if (propertyName && LEGACY_SHADOW_STYLE_PROPERTIES.has(propertyName)) legacyShadowPropertyNames.push(propertyName);
	}
	if (legacyShadowPropertyNames.length === 0) return;
	const quotedPropertyNames = legacyShadowPropertyNames.map((name) => `"${name}"`).join(", ");
	context.report({
		node: objectExpression,
		message: `Shadow style${legacyShadowPropertyNames.length > 1 ? "s" : ""} ${quotedPropertyNames} only work on one platform, so your users on the other see no shadow.`
	});
};
const rnNoLegacyShadowStyles = defineRule({
	id: "rn-no-legacy-shadow-styles",
	title: "Legacy platform-specific shadow styles",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `boxShadow` for shadows that work on both platforms on the new architecture, instead of platform-specific shadow properties.",
	create: (context) => ({
		JSXAttribute(node) {
			if (!isNodeOfType(node.name, "JSXIdentifier")) return;
			const attrName = node.name.name;
			if (attrName !== "style" && !attrName.endsWith("Style")) return;
			if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
			const expression = node.value.expression;
			if (isNodeOfType(expression, "ObjectExpression")) reportLegacyShadowProperties(expression, context);
			else if (isNodeOfType(expression, "ArrayExpression")) {
				for (const element of expression.elements ?? []) if (isNodeOfType(element, "ObjectExpression")) reportLegacyShadowProperties(element, context);
			}
		},
		CallExpression(node) {
			if (!isNodeOfType(node.callee, "MemberExpression")) return;
			if (!isNodeOfType(node.callee.object, "Identifier") || node.callee.object.name !== "StyleSheet") return;
			if (!isMemberProperty(node.callee, "create")) return;
			const stylesArgument = node.arguments?.[0];
			if (!isNodeOfType(stylesArgument, "ObjectExpression")) return;
			for (const styleDefinition of stylesArgument.properties ?? []) {
				if (!isNodeOfType(styleDefinition, "Property")) continue;
				if (!isNodeOfType(styleDefinition.value, "ObjectExpression")) continue;
				reportLegacyShadowProperties(styleDefinition.value, context);
			}
		}
	})
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-non-native-navigator.ts
const NON_NATIVE_NAVIGATOR_PACKAGES = new Map([
	["@react-navigation/stack", "@react-navigation/native-stack"],
	["@react-navigation/drawer", "expo-router Drawer (no native equivalent exists for standalone React Navigation)"],
	["@react-navigation/bottom-tabs", "@react-navigation/native-tabs (v7+) or expo-router NativeTabs"]
]);
const rnNoNonNativeNavigator = defineRule({
	id: "rn-no-non-native-navigator",
	title: "Non-native JS navigator",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `@react-navigation/native-stack` (or `native-tabs` in v7+) for real native transitions and gestures.",
	create: (context) => ({ ImportDeclaration(node) {
		const source = node.source?.value;
		if (typeof source !== "string") return;
		if (!NON_NATIVE_NAVIGATOR_PACKAGES.get(source)) return;
		context.report({
			node,
			message: `Your users feel less native transitions when ${source} uses a JS navigator.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-panresponder.ts
const rnNoPanresponder = defineRule({
	id: "rn-no-panresponder",
	title: "PanResponder over react-native-gesture-handler",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `react-native-gesture-handler` (`Gesture.Pan()`) instead of `PanResponder`. It runs gestures on the native UI thread, so they stay smooth even when the JS thread is busy.",
	create: (context) => ({ ImportDeclaration(node) {
		if (node.source?.value !== "react-native") return;
		if (node.importKind === "type") return;
		for (const specifier of node.specifiers ?? []) {
			if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
			if (specifier.importKind === "type") continue;
			if (getImportedName$1(specifier) !== "PanResponder") continue;
			context.report({
				node: specifier,
				message: "PanResponder runs gesture handling on the JS thread, which stutters under load. Use react-native-gesture-handler (`Gesture.Pan()`) so gestures run on the native UI thread."
			});
		}
	} })
});
//#endregion
//#region src/plugin/utils/is-inside-platform-os-web-branch.ts
const unwrapAccessor = (node) => {
	let current = node;
	while (current) {
		if (isNodeOfType(current, "TSNonNullExpression")) {
			current = current.expression;
			continue;
		}
		if (isNodeOfType(current, "ChainExpression")) {
			current = current.expression;
			continue;
		}
		if (isNodeOfType(current, "TSAsExpression") || isNodeOfType(current, "TSSatisfiesExpression")) {
			current = current.expression;
			continue;
		}
		return current;
	}
	return null;
};
const isPlatformOsMemberExpression = (node) => {
	const unwrapped = unwrapAccessor(node);
	if (!unwrapped || !isNodeOfType(unwrapped, "MemberExpression")) return false;
	if (unwrapped.computed) return false;
	const objectExpression = unwrapAccessor(unwrapped.object);
	if (!objectExpression || !isNodeOfType(objectExpression, "Identifier")) return false;
	if (objectExpression.name !== "Platform") return false;
	if (!isNodeOfType(unwrapped.property, "Identifier") || unwrapped.property.name !== "OS") return false;
	return true;
};
const isPlatformSelectCallee = (callee) => {
	const unwrapped = unwrapAccessor(callee);
	if (!unwrapped || !isNodeOfType(unwrapped, "MemberExpression")) return false;
	if (unwrapped.computed) return false;
	const objectExpression = unwrapAccessor(unwrapped.object);
	if (!objectExpression || !isNodeOfType(objectExpression, "Identifier")) return false;
	if (objectExpression.name !== "Platform") return false;
	if (!isNodeOfType(unwrapped.property, "Identifier") || unwrapped.property.name !== "select") return false;
	return true;
};
const isStringLiteralEqualTo = (node, expected) => {
	if (!node) return false;
	if (isNodeOfType(node, "Literal") && node.value === expected) return true;
	if (isNodeOfType(node, "TemplateLiteral")) return getStaticTemplateLiteralValue(node) === expected;
	return false;
};
const readStaticPropertyKeyName = (property) => {
	if (!isNodeOfType(property, "Property")) return null;
	if (property.computed) {
		if (isStringLiteralEqualTo(property.key, "web")) return "web";
		return null;
	}
	if (isNodeOfType(property.key, "Identifier")) return property.key.name;
	if (isNodeOfType(property.key, "Literal") && typeof property.key.value === "string") return property.key.value;
	return null;
};
const classifyPlatformOsBinaryTest = (testNode) => {
	if (!testNode || !isNodeOfType(testNode, "BinaryExpression")) return {
		isWebBranch: false,
		isNonWebBranch: false
	};
	if (testNode.operator !== "===" && testNode.operator !== "!==") return {
		isWebBranch: false,
		isNonWebBranch: false
	};
	const matchesLeft = isPlatformOsMemberExpression(testNode.left) && isStringLiteralEqualTo(testNode.right, "web");
	const matchesRight = isPlatformOsMemberExpression(testNode.right) && isStringLiteralEqualTo(testNode.left, "web");
	if (!matchesLeft && !matchesRight) return {
		isWebBranch: false,
		isNonWebBranch: false
	};
	return {
		isWebBranch: testNode.operator === "===",
		isNonWebBranch: testNode.operator === "!=="
	};
};
const isScopeBoundaryNode = (node) => isNodeOfType(node, "FunctionDeclaration") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "Program");
const isInsidePlatformOsWebBranch = (node) => {
	let child = node;
	let parent = node.parent;
	while (parent) {
		if (isNodeOfType(parent, "IfStatement")) {
			const classification = classifyPlatformOsBinaryTest(parent.test);
			if (classification.isWebBranch && parent.consequent === child) return true;
			if (classification.isNonWebBranch && parent.alternate === child) return true;
		} else if (isNodeOfType(parent, "ConditionalExpression")) {
			const classification = classifyPlatformOsBinaryTest(parent.test);
			if (classification.isWebBranch && parent.consequent === child) return true;
			if (classification.isNonWebBranch && parent.alternate === child) return true;
		} else if (isNodeOfType(parent, "LogicalExpression")) {
			const classification = classifyPlatformOsBinaryTest(parent.left);
			if (parent.right === child) {
				if (parent.operator === "&&" && classification.isWebBranch) return true;
				if (parent.operator === "||" && classification.isNonWebBranch) return true;
			}
		} else if (isNodeOfType(parent, "SwitchCase")) {
			const switchStatement = parent.parent;
			if (isNodeOfType(switchStatement, "SwitchStatement") && isPlatformOsMemberExpression(switchStatement.discriminant) && isStringLiteralEqualTo(parent.test, "web") && parent.test !== child) return true;
		} else if (isNodeOfType(parent, "Property")) {
			const objectExpression = parent.parent;
			if (isNodeOfType(objectExpression, "ObjectExpression")) {
				const callExpression = objectExpression.parent;
				if (isNodeOfType(callExpression, "CallExpression") && callExpression.arguments[0] === objectExpression && isPlatformSelectCallee(callExpression.callee) && readStaticPropertyKeyName(parent) === "web" && parent.value === child) return true;
			}
		}
		if (isScopeBoundaryNode(parent)) return false;
		child = parent;
		parent = parent.parent ?? null;
	}
	return false;
};
//#endregion
//#region src/plugin/rules/react-native/utils/collect-text-wrapper-components.ts
const isFunctionNode = (node) => isNodeOfType(node, "ArrowFunctionExpression") || isNodeOfType(node, "FunctionExpression") || isNodeOfType(node, "FunctionDeclaration");
const resolveReturnedRootElementName = (functionNode) => {
	const { body } = functionNode;
	if (!body) return null;
	if (!isNodeOfType(body, "BlockStatement")) return isNodeOfType(body, "JSXElement") ? resolveJsxElementName(body.openingElement) : null;
	for (const statement of body.body) {
		if (!isNodeOfType(statement, "ReturnStatement")) continue;
		const argument = statement.argument;
		if (argument && isNodeOfType(argument, "JSXElement")) return resolveJsxElementName(argument.openingElement);
	}
	return null;
};
const recordWrapperFromDeclaration = (componentName, functionNode, isTextHandlingRoot, wrappers) => {
	if (!componentName || !isReactComponentName(componentName)) return;
	if (!functionNode || !isFunctionNode(functionNode)) return;
	const rootName = resolveReturnedRootElementName(functionNode);
	if (rootName && isTextHandlingRoot(rootName)) wrappers.add(componentName);
};
const collectTextWrapperComponents = (programNode, isTextHandlingRoot) => {
	const wrappers = /* @__PURE__ */ new Set();
	walkAst(programNode, (node) => {
		if (isNodeOfType(node, "VariableDeclarator")) recordWrapperFromDeclaration(node.id && isNodeOfType(node.id, "Identifier") ? node.id.name : null, node.init, isTextHandlingRoot, wrappers);
		else if (isNodeOfType(node, "FunctionDeclaration")) recordWrapperFromDeclaration(node.id && isNodeOfType(node.id, "Identifier") ? node.id.name : null, node, isTextHandlingRoot, wrappers);
	});
	return wrappers;
};
//#endregion
//#region src/plugin/rules/react-native/utils/is-expo-ui-component-element.ts
const isNamedImportOf = (contextNode, localName, componentName) => {
	for (const moduleSource of EXPO_UI_MODULE_SOURCES) if (getImportedNameFromModule(contextNode, localName, moduleSource) === componentName) return true;
	return false;
};
const isExpoUiNamespaceImport = (contextNode, localName) => {
	for (const moduleSource of EXPO_UI_MODULE_SOURCES) if (isNamespaceImportFromModule(contextNode, localName, moduleSource)) return true;
	return false;
};
const isExpoUiComponentElement = (openingElement, contextNode, componentName) => {
	if (!openingElement.name) return false;
	const dottedName = flattenJsxName(openingElement.name);
	if (!dottedName) return false;
	const [rootLocalName, secondName] = dottedName.split(".");
	if (isNamedImportOf(contextNode, rootLocalName, componentName)) return true;
	return secondName === componentName && isExpoUiNamespaceImport(contextNode, rootLocalName);
};
//#endregion
//#region src/plugin/rules/react-native/rn-no-raw-text.ts
const truncateText = (text) => text.length > 30 ? `${text.slice(0, 30)}...` : text;
const isRawTextContent = (child) => {
	if (isNodeOfType(child, "JSXText")) return Boolean(child.value?.trim());
	if (!isNodeOfType(child, "JSXExpressionContainer") || !child.expression) return false;
	const expression = child.expression;
	return isNodeOfType(expression, "Literal") && (typeof expression.value === "string" || typeof expression.value === "number") || isNodeOfType(expression, "TemplateLiteral");
};
const getRawTextDescription = (child) => {
	if (isNodeOfType(child, "JSXText")) return `"${truncateText(child.value.trim())}"`;
	if (isNodeOfType(child, "JSXExpressionContainer") && child.expression) {
		const expression = child.expression;
		if (isNodeOfType(expression, "Literal") && typeof expression.value === "string") return `"${truncateText(expression.value)}"`;
		if (isNodeOfType(expression, "Literal") && typeof expression.value === "number") return `{${expression.value}}`;
		if (isNodeOfType(expression, "TemplateLiteral")) return "template literal";
	}
	return "text content";
};
const resolveTextBoundaryName = (openingElement) => {
	if (isNodeOfType(openingElement.name, "JSXNamespacedName")) return openingElement.name.namespace.name;
	return resolveJsxElementName(openingElement);
};
const isTextHandlingComponent = (elementName) => {
	if (REACT_NATIVE_TEXT_COMPONENTS.has(elementName)) return true;
	return [...REACT_NATIVE_TEXT_COMPONENT_KEYWORDS].some((keyword) => elementName.includes(keyword));
};
const isTransparentTextWrapper = (elementName) => elementName !== null && REACT_NATIVE_TEXT_TRANSPARENT_COMPONENTS.has(elementName);
const isInsideTextHandlingComponent = (node) => {
	let parentNode = node.parent;
	while (parentNode) {
		if (!isNodeOfType(parentNode, "JSXElement")) {
			parentNode = parentNode.parent;
			continue;
		}
		const parentName = resolveTextBoundaryName(parentNode.openingElement);
		if (parentName && isTextHandlingComponent(parentName)) return true;
		if (!isTransparentTextWrapper(parentName)) return false;
		parentNode = parentNode.parent;
	}
	return false;
};
const rnNoRawText = defineRule({
	id: "rn-no-raw-text",
	title: "Raw text outside a Text component",
	requires: ["react-native"],
	severity: "error",
	recommendation: "Text outside a `<Text>` component crashes on React Native. Wrap it like `<Text>{value}</Text>`.",
	create: (context) => {
		let isDomComponentFile = false;
		let autoDetectedWrappers = /* @__PURE__ */ new Set();
		return {
			Program(programNode) {
				isDomComponentFile = hasDirective(programNode, "use dom");
				autoDetectedWrappers = collectTextWrapperComponents(programNode, isTextHandlingComponent);
			},
			JSXElement(node) {
				if (isDomComponentFile) return;
				const elementName = resolveTextBoundaryName(node.openingElement);
				if (elementName && (isTextHandlingComponent(elementName) || autoDetectedWrappers.has(elementName))) return;
				if (isExpoUiComponentElement(node.openingElement, node, "ListItem")) return;
				if (isInsidePlatformOsWebBranch(node)) return;
				if (isTransparentTextWrapper(elementName) && isInsideTextHandlingComponent(node)) return;
				for (const child of node.children ?? []) {
					if (!isRawTextContent(child)) continue;
					context.report({
						node: child,
						message: `Your users hit a crash when raw ${getRawTextDescription(child)} renders outside a <Text> component on React Native.`
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-renderitem-key.ts
const collectTopLevelReturnExpressions = (functionNode) => {
	if (isNodeOfType(functionNode, "ArrowFunctionExpression") && functionNode.body) {
		if (!isNodeOfType(functionNode.body, "BlockStatement")) return [functionNode.body];
	}
	const block = functionNode.body;
	if (!block || !isNodeOfType(block, "BlockStatement")) return [];
	const returnExpressions = [];
	const visit = (node) => {
		if (FUNCTION_LIKE_TYPES$1.has(node.type)) return;
		if (isNodeOfType(node, "ReturnStatement") && node.argument) returnExpressions.push(node.argument);
		const nodeRecord = node;
		for (const fieldName of Object.keys(nodeRecord)) {
			if (fieldName === "parent") continue;
			const child = nodeRecord[fieldName];
			if (Array.isArray(child)) {
				for (const childItem of child) if (isAstNode(childItem)) visit(childItem);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(block);
	return returnExpressions;
};
const collectReturnedJsxElements = (expression) => {
	const elements = [];
	const visit = (current) => {
		const unwrapped = stripParenExpression(current);
		if (isNodeOfType(unwrapped, "JSXElement")) {
			elements.push(unwrapped);
			return;
		}
		if (isNodeOfType(unwrapped, "ConditionalExpression")) {
			visit(unwrapped.consequent);
			visit(unwrapped.alternate);
			return;
		}
		if (isNodeOfType(unwrapped, "LogicalExpression")) {
			visit(unwrapped.right);
			if (unwrapped.operator === "||" || unwrapped.operator === "??") visit(unwrapped.left);
		}
	};
	visit(expression);
	return elements;
};
const rnNoRenderitemKey = defineRule({
	id: "rn-no-renderitem-key",
	title: "Useless key on renderItem JSX",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "A `key` on the JSX from renderItem does nothing, since lists get row keys from `keyExtractor` (or `item.key`). Remove it and set `keyExtractor` on the list instead.",
	create: (context) => ({ JSXAttribute(attributeNode) {
		if (!isNodeOfType(attributeNode.name, "JSXIdentifier") || !RENDER_ITEM_PROP_NAMES.has(attributeNode.name.name)) return;
		const openingElement = attributeNode.parent;
		if (!openingElement || !isNodeOfType(openingElement, "JSXOpeningElement")) return;
		const listComponentName = resolveJsxElementName(openingElement);
		if (!listComponentName || !REACT_NATIVE_LIST_COMPONENTS.has(listComponentName)) return;
		if (!attributeNode.value || !isNodeOfType(attributeNode.value, "JSXExpressionContainer")) return;
		const renderFunction = attributeNode.value.expression;
		if (!isNodeOfType(renderFunction, "ArrowFunctionExpression") && !isNodeOfType(renderFunction, "FunctionExpression")) return;
		const returnExpressions = collectTopLevelReturnExpressions(renderFunction);
		const renderPropName = attributeNode.name.name;
		for (const returnExpression of returnExpressions) {
			const returnedJsxElements = collectReturnedJsxElements(returnExpression);
			for (const jsxElement of returnedJsxElements) {
				if (!hasJsxKeyAttribute(jsxElement.openingElement)) continue;
				context.report({
					node: jsxElement.openingElement,
					message: `Your users get no benefit from \`key\` on the JSX from ${renderPropName}; the list ignores it.`
				});
			}
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-scroll-state.ts
const SET_STATE_PATTERN = /^set[A-Z]/;
const findSetStateInBody = (body) => {
	let setStateCallNode = null;
	walkAst(body, (child) => {
		if (setStateCallNode) return;
		if (isNodeOfType(child, "CallExpression") && isNodeOfType(child.callee, "Identifier") && SET_STATE_PATTERN.test(child.callee.name) && isUseStateSetterInScope(child, child.callee.name)) setStateCallNode = child;
	});
	return setStateCallNode;
};
const rnNoScrollState = defineRule({
	id: "rn-no-scroll-state",
	title: "setState in onScroll handler",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "error",
	recommendation: "`setState` on every scroll event redraws the screen dozens of times a second. Track the position with a Reanimated shared value (`useAnimatedScrollHandler`) or a ref.",
	create: (context) => {
		const stateSettersInHandlers = /* @__PURE__ */ new Map();
		return {
			VariableDeclarator(node) {
				if (!isNodeOfType(node.id, "Identifier")) return;
				const variableName = node.id.name;
				if (!/scroll/i.test(variableName)) return;
				const init = node.init;
				if (!isNodeOfType(init, "ArrowFunctionExpression") && !isNodeOfType(init, "FunctionExpression")) return;
				const setStateCall = findSetStateInBody(init.body);
				if (setStateCall) stateSettersInHandlers.set(variableName, setStateCall);
			},
			JSXAttribute(node) {
				if (!isNodeOfType(node.name, "JSXIdentifier")) return;
				if (node.name.name !== "onScroll") return;
				if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
				const expression = node.value.expression;
				if (isNodeOfType(expression, "Identifier")) {
					const tracked = stateSettersInHandlers.get(expression.name);
					if (tracked) context.report({
						node: tracked,
						message: "Your users get janky scrolling when setState in this onScroll handler redraws the screen on every scroll event."
					});
					return;
				}
				if (!isNodeOfType(expression, "ArrowFunctionExpression") && !isNodeOfType(expression, "FunctionExpression")) return;
				const setStateCallNode = findSetStateInBody(expression.body);
				if (setStateCallNode) context.report({
					node: setStateCallNode,
					message: "Your users get janky scrolling when setState in onScroll redraws the screen on every scroll event."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-scrollview-mapped-list.ts
const NON_VIRTUALIZED_SCROLL_CONTAINERS = new Set(["ScrollView"]);
const ARRAY_ITERATION_METHODS = new Set(["map", "flatMap"]);
const isArrayIterationExpression = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (!isNodeOfType(node.callee, "MemberExpression")) return false;
	if (!isNodeOfType(node.callee.property, "Identifier")) return false;
	if (ARRAY_ITERATION_METHODS.has(node.callee.property.name)) return true;
	if (node.callee.property.name === "filter" || node.callee.property.name === "slice" || node.callee.property.name === "sort" || node.callee.property.name === "reverse" || node.callee.property.name === "concat") return isArrayIterationExpression(node.callee.object);
	return false;
};
const rnNoScrollviewMappedList = defineRule({
	id: "rn-no-scrollview-mapped-list",
	title: "Non-virtualized mapped list in ScrollView",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "`<ScrollView>{items.map(...)}</ScrollView>` builds every row at once, which slows scrolling. Use FlashList, LegendList, or FlatList instead.",
	create: (context) => ({ JSXElement(node) {
		const elementName = resolveJsxElementName(node.openingElement);
		if (!elementName || !NON_VIRTUALIZED_SCROLL_CONTAINERS.has(elementName)) return;
		if (isExpoUiComponentElement(node.openingElement, node, "ScrollView")) return;
		for (const child of node.children ?? []) {
			if (!isNodeOfType(child, "JSXExpressionContainer")) continue;
			const expression = child.expression;
			if (isArrayIterationExpression(expression)) {
				context.report({
					node: child,
					message: `Your users get slow scrolling when <${elementName}> with items.map(...) builds every row at once.`
				});
				return;
			}
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-set-native-props.ts
const isStaticMemberNamed = (node, name) => isNodeOfType(node, "MemberExpression") && !node.computed && isNodeOfType(node.property, "Identifier") && node.property.name === name;
const rnNoSetNativeProps = defineRule({
	id: "rn-no-set-native-props",
	title: "Imperative setNativeProps (no-op under Fabric)",
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Drive the prop through React state, an `Animated.Value` (with `useNativeDriver: true`), or a Reanimated shared value. `setNativeProps` is a silent no-op under the New Architecture.",
	create: (context) => ({ CallExpression(node) {
		const callee = node.callee;
		if (!isStaticMemberNamed(callee, "setNativeProps")) return;
		if (!isNodeOfType(callee, "MemberExpression")) return;
		if (!isStaticMemberNamed(callee.object, "current")) return;
		context.report({
			node,
			message: "`setNativeProps` is a silent no-op under the New Architecture (Fabric), so this imperative update won't change the view. Drive the prop via state, an Animated.Value, or a Reanimated shared value."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-no-single-element-style-array.ts
const rnNoSingleElementStyleArray = defineRule({
	id: "rn-no-single-element-style-array",
	title: "Single-element style array",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `style={value}` instead of `style={[value]}`. A one-item array just adds extra work for nothing.",
	create: (context) => ({ JSXAttribute(node) {
		const propName = isNodeOfType(node.name, "JSXIdentifier") ? node.name.name : null;
		if (!propName) return;
		if (propName !== "style" && !propName.endsWith("Style")) return;
		if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
		const expression = node.value.expression;
		if (!isNodeOfType(expression, "ArrayExpression")) return;
		if (expression.elements?.length !== 1) return;
		context.report({
			node: expression,
			message: `Your users pay for an extra array allocation when "${propName}" wraps a single value for nothing.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-prefer-content-inset-adjustment.ts
const rnPreferContentInsetAdjustment = defineRetiredRule({
	id: "rn-prefer-content-inset-adjustment",
	title: "Manual safe-area inset adjustment",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Prefer native content inset adjustment only when it replaces manual inset plumbing; SafeAreaView wrappers are valid and intentionally ignored."
});
//#endregion
//#region src/react-native-dependency-names.ts
const EXPO_MANAGED_DEPENDENCY_NAMES = new Set([
	"expo",
	"expo-router",
	"@expo/cli",
	"@expo/metro-config",
	"@expo/metro-runtime"
]);
const REACT_NATIVE_DEPENDENCY_NAMES = new Set([
	"react-native",
	"react-native-tvos",
	...EXPO_MANAGED_DEPENDENCY_NAMES,
	"react-native-windows",
	"react-native-macos"
]);
const REACT_NATIVE_DEPENDENCY_PREFIXES = ["@react-native/", "@react-native-"];
const isExpoManagedDependencyName = (dependencyName) => EXPO_MANAGED_DEPENDENCY_NAMES.has(dependencyName);
const isReactNativeDependencyName = (dependencyName) => {
	if (REACT_NATIVE_DEPENDENCY_NAMES.has(dependencyName)) return true;
	for (const prefix of REACT_NATIVE_DEPENDENCY_PREFIXES) if (dependencyName.startsWith(prefix)) return true;
	return false;
};
//#endregion
//#region src/plugin/utils/classify-package-platform.ts
const WEB_FRAMEWORK_DEPENDENCY_NAMES = new Set([
	"next",
	"vite",
	"react-scripts",
	"gatsby",
	"@remix-run/react",
	"@remix-run/node",
	"@docusaurus/core",
	"@docusaurus/preset-classic",
	"@storybook/react",
	"@storybook/react-vite",
	"@storybook/react-webpack5",
	"@storybook/nextjs",
	"@storybook/web-components",
	"storybook",
	"react-dom",
	"@vitejs/plugin-react",
	"@vitejs/plugin-react-swc"
]);
const cachedPlatformByPackageDirectory = /* @__PURE__ */ new Map();
const cachedPackageDirectoryByFilename = /* @__PURE__ */ new Map();
const findNearestPackageDirectory = (filename) => {
	if (!filename) return null;
	const fromCache = cachedPackageDirectoryByFilename.get(filename);
	if (fromCache !== void 0) return fromCache;
	let currentDirectory = path.dirname(filename);
	while (true) {
		const candidatePackageJsonPath = path.join(currentDirectory, "package.json");
		let hasPackageJson = false;
		try {
			hasPackageJson = fs.statSync(candidatePackageJsonPath).isFile();
		} catch {
			hasPackageJson = false;
		}
		if (hasPackageJson) {
			cachedPackageDirectoryByFilename.set(filename, currentDirectory);
			return currentDirectory;
		}
		const parentDirectory = path.dirname(currentDirectory);
		if (parentDirectory === currentDirectory) {
			cachedPackageDirectoryByFilename.set(filename, null);
			return null;
		}
		currentDirectory = parentDirectory;
	}
};
const readPackageJsonSafe = (packageJsonPath) => {
	let rawContents;
	try {
		rawContents = fs.readFileSync(packageJsonPath, "utf-8");
	} catch {
		return null;
	}
	try {
		const parsed = JSON.parse(rawContents);
		if (typeof parsed === "object" && parsed !== null) return parsed;
		return null;
	} catch {
		return null;
	}
};
const DEPENDENCY_SECTION_NAMES = [
	"dependencies",
	"devDependencies",
	"peerDependencies",
	"optionalDependencies"
];
const iterateDependencyNames = function* (packageJson) {
	for (const sectionName of DEPENDENCY_SECTION_NAMES) {
		const section = packageJson[sectionName];
		if (!section) continue;
		for (const dependencyName of Object.keys(section)) yield dependencyName;
	}
};
const isReactNativeAware = (packageJson) => {
	if (typeof packageJson["react-native"] === "string") return true;
	for (const dependencyName of iterateDependencyNames(packageJson)) if (isReactNativeDependencyName(dependencyName)) return true;
	return false;
};
const isExpoManaged = (packageJson) => {
	for (const dependencyName of iterateDependencyNames(packageJson)) if (isExpoManagedDependencyName(dependencyName)) return true;
	return false;
};
const isWebFrameworkOnly = (packageJson) => {
	for (const dependencyName of iterateDependencyNames(packageJson)) if (WEB_FRAMEWORK_DEPENDENCY_NAMES.has(dependencyName)) return true;
	return false;
};
const classifyPackagePlatform = (filename) => {
	const packageDirectory = findNearestPackageDirectory(filename);
	if (!packageDirectory) return "unknown";
	const cached = cachedPlatformByPackageDirectory.get(packageDirectory);
	if (cached !== void 0) return cached;
	const packageJson = readPackageJsonSafe(path.join(packageDirectory, "package.json"));
	if (!packageJson) {
		cachedPlatformByPackageDirectory.set(packageDirectory, "unknown");
		return "unknown";
	}
	let result;
	if (isExpoManaged(packageJson)) result = "expo";
	else if (isReactNativeAware(packageJson)) result = "react-native";
	else if (isWebFrameworkOnly(packageJson)) result = "web";
	else result = "unknown";
	cachedPlatformByPackageDirectory.set(packageDirectory, result);
	return result;
};
//#endregion
//#region src/plugin/utils/is-expo-managed-file.ts
const isExpoManagedFileActive = (context) => {
	const rawFilename = context.filename;
	const filename = rawFilename ? normalizeFilename$1(rawFilename) : void 0;
	if (filename) {
		const packagePlatform = classifyPackagePlatform(filename);
		if (packagePlatform === "expo") return true;
		if (packagePlatform === "react-native" || packagePlatform === "web") return false;
	}
	return getReactDoctorStringSetting(context.settings, "framework") === "expo";
};
//#endregion
//#region src/plugin/rules/react-native/rn-prefer-expo-image.ts
const EMPTY_VISITORS$1 = {};
const rnPreferExpoImage = defineRule({
	id: "rn-prefer-expo-image",
	title: "react-native Image instead of expo-image",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `<Image>` from `expo-image` instead of `react-native`. Same props, plus caching, placeholders, and crossfades for faster image loading.",
	create: (context) => {
		if (!isExpoManagedFileActive(context)) return EMPTY_VISITORS$1;
		return { ImportDeclaration(node) {
			if (node.source?.value !== "react-native") return;
			for (const specifier of node.specifiers ?? []) {
				if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
				const importedName = getImportedName$1(specifier);
				if (importedName !== "Image" && importedName !== "ImageBackground") continue;
				context.report({
					node: specifier,
					message: `Your users watch images reload often because ${importedName} from react-native has no caching.`
				});
			}
		} };
	}
});
//#endregion
//#region src/plugin/rules/react-native/rn-prefer-pressable.ts
const TOUCHABLE_COMPONENTS = new Set([
	"TouchableOpacity",
	"TouchableHighlight",
	"TouchableWithoutFeedback",
	"TouchableNativeFeedback"
]);
const TOUCHABLE_SOURCES = new Set(["react-native", "react-native-gesture-handler"]);
const rnPreferPressable = defineRule({
	id: "rn-prefer-pressable",
	title: "Touchable components instead of Pressable",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `<Pressable>` from react-native (or react-native-gesture-handler) instead of the old Touchable* components.",
	create: (context) => ({ ImportDeclaration(node) {
		const source = node.source?.value;
		if (typeof source !== "string" || !TOUCHABLE_SOURCES.has(source)) return;
		for (const specifier of node.specifiers ?? []) {
			if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
			const importedName = getImportedName$1(specifier);
			if (!importedName || !TOUCHABLE_COMPONENTS.has(importedName)) continue;
			context.report({
				node: specifier,
				message: `Your users miss <Pressable>'s flexible press feedback when you use ${importedName}, which is old & frozen.`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-prefer-pressable-over-gesture-detector.ts
const COMPOSING_CHAIN_METHOD_NAMES = new Set([
	"simultaneousWithExternalGesture",
	"requireExternalGestureToFail",
	"blocksExternalGesture"
]);
const analyzeGestureChain = (expression) => {
	if (!isNodeOfType(expression, "CallExpression")) return null;
	const chainMethodNames = [];
	let numberOfTapsArgument = null;
	let cursor = expression;
	while (cursor && isNodeOfType(cursor, "CallExpression")) {
		const callExpression = cursor;
		const callee = callExpression.callee;
		if (!isNodeOfType(callee, "MemberExpression")) return null;
		if (!isNodeOfType(callee.property, "Identifier")) return null;
		const methodName = callee.property.name;
		if (isNodeOfType(callee.object, "Identifier") && callee.object.name === "Gesture") return {
			factoryName: methodName,
			chainMethodNames,
			numberOfTapsArgument
		};
		if (methodName === "numberOfTaps" && numberOfTapsArgument === null && callExpression.arguments?.length === 1) numberOfTapsArgument = callExpression.arguments[0] ?? null;
		chainMethodNames.push(methodName);
		cursor = callee.object;
	}
	return null;
};
const isTapChainEligibleForPressable = (chain) => {
	if (chain.factoryName !== "Tap") return false;
	for (const methodName of chain.chainMethodNames) if (COMPOSING_CHAIN_METHOD_NAMES.has(methodName)) return false;
	const tapsArg = chain.numberOfTapsArgument;
	if (tapsArg !== null) {
		if (!isNodeOfType(tapsArg, "Literal")) return false;
		if (typeof tapsArg.value !== "number") return false;
		if (tapsArg.value !== 1) return false;
	}
	return true;
};
const rnPreferPressableOverGestureDetector = defineRule({
	id: "rn-prefer-pressable-over-gesture-detector",
	title: "GestureDetector for a plain tap",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Every GestureDetector sets up a native handler when it appears, which slows the screen. For a plain tap, use `<Pressable>` (or `createCSSAnimatedComponent(Pressable)` from react-native-reanimated/css for animated press feedback).",
	create: (context) => ({ JSXOpeningElement(node) {
		if (resolveJsxElementName(node) !== "GestureDetector") return;
		if (!isImportedFromModule(node, "GestureDetector", "react-native-gesture-handler")) return;
		let gestureExpression = null;
		for (const attribute of node.attributes ?? []) {
			if (!isNodeOfType(attribute, "JSXAttribute")) continue;
			if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
			if (attribute.name.name !== "gesture") continue;
			if (!isNodeOfType(attribute.value, "JSXExpressionContainer")) continue;
			gestureExpression = attribute.value.expression;
			break;
		}
		if (!gestureExpression) return;
		const resolvedExpression = stripParenExpression(gestureExpression);
		let chainExpression = resolvedExpression;
		if (isNodeOfType(resolvedExpression, "Identifier")) {
			const binding = findVariableInitializer(node, resolvedExpression.name);
			if (!binding || !binding.initializer) return;
			chainExpression = stripParenExpression(binding.initializer);
		}
		const chain = analyzeGestureChain(chainExpression);
		if (!chain) return;
		if (!isTapChainEligibleForPressable(chain)) return;
		context.report({
			node,
			message: "Your users wait longer for the screen when <GestureDetector> handles a simple tap."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-prefer-reanimated.ts
const JS_THREAD_ANIMATION_IMPORTS = new Set(["Animated", "LayoutAnimation"]);
const rnPreferReanimated = defineRule({
	id: "rn-prefer-reanimated",
	title: "JS-thread animation instead of Reanimated",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `import Animated from 'react-native-reanimated'` so animations run on the UI thread instead of the JS thread, which keeps them smooth.",
	create: (context) => ({ ImportDeclaration(node) {
		if (node.source?.value !== "react-native") return;
		for (const specifier of node.specifiers ?? []) {
			if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
			const importedName = getImportedName$1(specifier);
			if (!importedName || !JS_THREAD_ANIMATION_IMPORTS.has(importedName)) continue;
			const suggestion = importedName === "LayoutAnimation" ? "Your users see stutter when LayoutAnimation runs on the JS thread." : "Your users see stutter when Animated from react-native runs on the JS thread.";
			context.report({
				node: specifier,
				message: suggestion
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-pressable-shared-value-mutation.ts
const PRESS_HANDLER_PROP_NAMES = new Set([
	"onPress",
	"onPressIn",
	"onPressOut",
	"onLongPress"
]);
const PRESSABLE_ELEMENT_NAMES = new Set([
	"Pressable",
	"TouchableOpacity",
	"TouchableHighlight",
	"TouchableWithoutFeedback",
	"TouchableNativeFeedback"
]);
const handlerMutatesIdentifier = (handler, sharedValueBindings) => {
	if (!isNodeOfType(handler, "ArrowFunctionExpression") && !isNodeOfType(handler, "FunctionExpression")) return false;
	if (sharedValueBindings.size === 0) return false;
	let didMutate = false;
	walkAst(handler.body, (child) => {
		if (didMutate) return;
		if (isNodeOfType(child, "AssignmentExpression") && isNodeOfType(child.left, "MemberExpression") && isNodeOfType(child.left.object, "Identifier") && sharedValueBindings.has(child.left.object.name) && isNodeOfType(child.left.property, "Identifier") && child.left.property.name === "value") didMutate = true;
		if (isNodeOfType(child, "CallExpression") && isNodeOfType(child.callee, "MemberExpression") && isNodeOfType(child.callee.object, "Identifier") && sharedValueBindings.has(child.callee.object.name) && isNodeOfType(child.callee.property, "Identifier") && (child.callee.property.name === "set" || child.callee.property.name === "value")) didMutate = true;
	});
	return didMutate;
};
const rnPressableSharedValueMutation = defineRule({
	id: "rn-pressable-shared-value-mutation",
	title: "Pressable animates on the JS thread",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "This press animation runs on the JS thread, so it can stutter. Wrap it in <GestureDetector gesture={Gesture.Tap()...}> to run it on the UI thread.",
	create: (context) => {
		const sharedValueBindingsByComponent = [];
		const enterScope = () => {
			sharedValueBindingsByComponent.push(/* @__PURE__ */ new Set());
		};
		const exitScope = () => {
			sharedValueBindingsByComponent.pop();
		};
		const trackSharedValueBinding = (declarator) => {
			if (sharedValueBindingsByComponent.length === 0) return;
			if (!isNodeOfType(declarator, "VariableDeclarator")) return;
			if (!isNodeOfType(declarator.id, "Identifier")) return;
			if (!isNodeOfType(declarator.init, "CallExpression")) return;
			const callee = declarator.init.callee;
			if (!isNodeOfType(callee, "Identifier")) return;
			if (callee.name !== "useSharedValue") return;
			sharedValueBindingsByComponent[sharedValueBindingsByComponent.length - 1].add(declarator.id.name);
		};
		return {
			FunctionDeclaration: enterScope,
			"FunctionDeclaration:exit": exitScope,
			FunctionExpression: enterScope,
			"FunctionExpression:exit": exitScope,
			ArrowFunctionExpression: enterScope,
			"ArrowFunctionExpression:exit": exitScope,
			VariableDeclarator(node) {
				trackSharedValueBinding(node);
			},
			JSXOpeningElement(node) {
				const name = resolveJsxElementName(node);
				if (!name || !PRESSABLE_ELEMENT_NAMES.has(name)) return;
				if (sharedValueBindingsByComponent.length === 0) return;
				const activeBindings = /* @__PURE__ */ new Set();
				for (const frame of sharedValueBindingsByComponent) for (const binding of frame) activeBindings.add(binding);
				if (activeBindings.size === 0) return;
				for (const attr of node.attributes ?? []) {
					if (!isNodeOfType(attr, "JSXAttribute")) continue;
					if (!isNodeOfType(attr.name, "JSXIdentifier")) continue;
					if (!PRESS_HANDLER_PROP_NAMES.has(attr.name.name)) continue;
					if (!isNodeOfType(attr.value, "JSXExpressionContainer")) continue;
					const handler = attr.value.expression;
					if (!handler) continue;
					if (!handlerMutatesIdentifier(handler, activeBindings)) continue;
					context.report({
						node: attr,
						message: `Your users feel a choppy press when <${name}> ${attr.name.name} animates on the JS thread.`
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-native/utils/scrollview_names.ts
const SCROLLVIEW_NAMES = new Set([
	"ScrollView",
	"FlatList",
	"SectionList",
	"VirtualizedList",
	"KeyboardAwareScrollView"
]);
//#endregion
//#region src/plugin/rules/react-native/rn-scrollview-dynamic-padding.ts
const rnScrollviewDynamicPadding = defineRule({
	id: "rn-scrollview-dynamic-padding",
	title: "Dynamic padding on contentContainerStyle",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "Use `contentInset={{ bottom: dynamicValue }}` so the OS shifts the content instead of relaying it out, which avoids the jump.",
	create: (context) => ({ JSXOpeningElement(node) {
		const elementName = resolveJsxElementName(node);
		if (!elementName) return;
		if (!SCROLLVIEW_NAMES.has(elementName) && elementName !== "FlashList") return;
		if (elementName === "KeyboardAwareScrollView") return;
		for (const attr of node.attributes ?? []) {
			if (!isNodeOfType(attr, "JSXAttribute")) continue;
			if (!isNodeOfType(attr.name, "JSXIdentifier") || attr.name.name !== "contentContainerStyle") continue;
			if (!isNodeOfType(attr.value, "JSXExpressionContainer")) continue;
			const expression = attr.value.expression;
			if (!isNodeOfType(expression, "ObjectExpression")) continue;
			for (const property of expression.properties ?? []) {
				if (!isNodeOfType(property, "Property")) continue;
				if (!isNodeOfType(property.key, "Identifier")) continue;
				const key = property.key.name;
				if (key !== "paddingBottom" && key !== "paddingTop") continue;
				const value = property.value;
				if (!value) continue;
				if (isNodeOfType(value, "Literal")) continue;
				context.report({
					node: property,
					message: `Your users see rows jump when a changing ${key} on contentContainerStyle shifts the whole list.`
				});
				return;
			}
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-scrollview-flex-in-content-container.ts
const VIRTUALIZED_LIST_NAMES = new Set(["FlashList", "LegendList"]);
const getStaticMemberKeyName = (expression) => {
	if (!expression.computed) {
		if (isNodeOfType(expression.property, "Identifier")) return expression.property.name;
		return null;
	}
	if (isNodeOfType(expression.property, "Literal") && typeof expression.property.value === "string") return expression.property.value;
	return null;
};
const isStyleSheetCreateCallExpression = (expression) => {
	if (!expression) return false;
	const callExpression = stripParenExpression(expression);
	if (!isNodeOfType(callExpression, "CallExpression")) return false;
	const callee = callExpression.callee;
	return isNodeOfType(callee, "MemberExpression") && !callee.computed && isNodeOfType(callee.object, "Identifier") && callee.object.name === "StyleSheet" && isNodeOfType(callee.property, "Identifier") && callee.property.name === "create";
};
const resolveContentContainerStyleObject = (attribute) => {
	if (!isNodeOfType(attribute.name, "JSXIdentifier")) return null;
	if (attribute.name.name !== "contentContainerStyle") return null;
	if (!isNodeOfType(attribute.value, "JSXExpressionContainer")) return null;
	const expression = stripParenExpression(attribute.value.expression);
	if (isNodeOfType(expression, "ObjectExpression")) return expression;
	if (isNodeOfType(expression, "MemberExpression")) {
		const styleObjectKeyName = getStaticMemberKeyName(expression);
		if (!styleObjectKeyName) return null;
		const styleObjectIdentifierName = getRootIdentifierName(expression);
		if (!styleObjectIdentifierName) return null;
		const binding = findVariableInitializer(expression, styleObjectIdentifierName);
		if (!binding || !binding.initializer) return null;
		if (!isStyleSheetCreateCallExpression(binding.initializer)) return null;
		const argument = stripParenExpression(binding.initializer).arguments?.[0];
		if (!isNodeOfType(argument, "ObjectExpression")) return null;
		for (const property of argument.properties ?? []) {
			if (!isNodeOfType(property, "Property")) continue;
			if (property.computed) continue;
			let matchesKey = false;
			if (isNodeOfType(property.key, "Identifier")) matchesKey = property.key.name === styleObjectKeyName;
			else if (isNodeOfType(property.key, "Literal") && typeof property.key.value === "string") matchesKey = property.key.value === styleObjectKeyName;
			if (!matchesKey) continue;
			const propertyValue = stripParenExpression(property.value);
			if (isNodeOfType(propertyValue, "ObjectExpression")) return propertyValue;
			return null;
		}
	}
	return null;
};
const collectStyleKeyNames = (objectExpression) => {
	const names = /* @__PURE__ */ new Set();
	for (const property of objectExpression.properties ?? []) {
		if (!isNodeOfType(property, "Property")) continue;
		if (property.computed) continue;
		if (isNodeOfType(property.key, "Identifier")) names.add(property.key.name);
		else if (isNodeOfType(property.key, "Literal") && typeof property.key.value === "string") names.add(property.key.value);
	}
	return names;
};
const findFlexShorthandProperty = (objectExpression) => {
	for (const property of objectExpression.properties ?? []) {
		if (!isNodeOfType(property, "Property")) continue;
		if (property.computed) continue;
		if (!isNodeOfType(property.key, "Identifier") || property.key.name !== "flex") continue;
		const value = property.value;
		if (!isNodeOfType(value, "Literal")) return null;
		if (typeof value.value !== "number" || value.value <= 0) return null;
		return property;
	}
	return null;
};
const rnScrollviewFlexInContentContainer = defineRule({
	id: "rn-scrollview-flex-in-content-container",
	title: "flex on contentContainerStyle collapses container",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "In RN, `flex: 1` on `contentContainerStyle` sets `flexBasis: 0` and can collapse the container on small screens. Use `flexGrow: 1` instead.",
	create: (context) => ({ JSXOpeningElement(node) {
		const elementName = resolveJsxElementName(node);
		if (!elementName) return;
		if (!SCROLLVIEW_NAMES.has(elementName) && !VIRTUALIZED_LIST_NAMES.has(elementName)) return;
		for (const attribute of node.attributes ?? []) {
			if (!isNodeOfType(attribute, "JSXAttribute")) continue;
			const objectExpression = resolveContentContainerStyleObject(attribute);
			if (!objectExpression) continue;
			const keyNames = collectStyleKeyNames(objectExpression);
			if (keyNames.has("flexGrow") || keyNames.has("flexBasis")) continue;
			const flexProperty = findFlexShorthandProperty(objectExpression);
			if (!flexProperty) continue;
			context.report({
				node: flexProperty,
				message: `\`flex\` on contentContainerStyle can collapse the container on small screens.`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-native/rn-style-prefer-box-shadow.ts
const LEGACY_SHADOW_KEYS = new Set([
	"shadowColor",
	"shadowOffset",
	"shadowOpacity",
	"shadowRadius",
	"elevation"
]);
const findLegacyShadowProperty = (objectExpression) => {
	for (const property of objectExpression.properties ?? []) {
		if (!isNodeOfType(property, "Property")) continue;
		if (!isNodeOfType(property.key, "Identifier")) continue;
		if (LEGACY_SHADOW_KEYS.has(property.key.name)) return {
			keyName: property.key.name,
			node: property
		};
	}
	return null;
};
const rnStylePreferBoxShadow = defineRule({
	id: "rn-style-prefer-boxshadow",
	title: "Platform-specific shadow keys over boxShadow",
	tags: ["test-noise"],
	requires: ["react-native"],
	severity: "warn",
	recommendation: "These shadow keys only work on one platform. On RN v7+, use the CSS `boxShadow` string instead, like `boxShadow: \"0 2px 8px rgba(0,0,0,0.1)\"`, which works on both.",
	create: (context) => ({
		JSXAttribute(node) {
			if (!isNodeOfType(node.name, "JSXIdentifier")) return;
			const attrName = node.name.name;
			if (attrName !== "style" && !attrName.endsWith("Style")) return;
			if (!isNodeOfType(node.value, "JSXExpressionContainer")) return;
			const expression = node.value.expression;
			if (isNodeOfType(expression, "ObjectExpression")) {
				const match = findLegacyShadowProperty(expression);
				if (match) context.report({
					node: match.node,
					message: `Your users on the other platform see no shadow when you use ${match.keyName}.`
				});
			} else if (isNodeOfType(expression, "ArrayExpression")) for (const element of expression.elements ?? []) {
				if (!isNodeOfType(element, "ObjectExpression")) continue;
				const match = findLegacyShadowProperty(element);
				if (match) {
					context.report({
						node: match.node,
						message: `Your users on the other platform see no shadow when you use ${match.keyName}.`
					});
					return;
				}
			}
		},
		CallExpression(node) {
			if (!isNodeOfType(node.callee, "MemberExpression")) return;
			if (!isNodeOfType(node.callee.object, "Identifier")) return;
			if (node.callee.object.name !== "StyleSheet") return;
			if (!isNodeOfType(node.callee.property, "Identifier")) return;
			if (node.callee.property.name !== "create") return;
			const arg = node.arguments?.[0];
			if (!isNodeOfType(arg, "ObjectExpression")) return;
			for (const property of arg.properties ?? []) {
				if (!isNodeOfType(property, "Property")) continue;
				if (!isNodeOfType(property.value, "ObjectExpression")) continue;
				const match = findLegacyShadowProperty(property.value);
				if (!match) continue;
				context.report({
					node: match.node,
					message: `Your users on the other platform see no shadow when you use ${match.keyName}.`
				});
			}
		}
	})
});
//#endregion
//#region src/plugin/rules/a11y/role-has-required-aria-props.ts
const buildMessage$1 = (role, missingProps) => `Screen reader users can't tell the state of this \`${role}\` without its required ARIA props, so add \`${missingProps.join("`, `")}\`.`;
const ROLE_REQUIRED_PROPS = new Map([
	["checkbox", ["aria-checked"]],
	["combobox", ["aria-controls", "aria-expanded"]],
	["heading", ["aria-level"]],
	["menuitemcheckbox", ["aria-checked"]],
	["menuitemradio", ["aria-checked"]],
	["meter", ["aria-valuenow"]],
	["option", ["aria-selected"]],
	["radio", ["aria-checked"]],
	["scrollbar", ["aria-controls", "aria-valuenow"]],
	["slider", ["aria-valuenow"]],
	["switch", ["aria-checked"]]
]);
const roleHasRequiredAriaProps = defineRule({
	id: "role-has-required-aria-props",
	title: "Role missing required ARIA props",
	tags: ["react-jsx-only"],
	severity: "error",
	recommendation: "Add every required `aria-*` attribute when you set an interactive role.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
		if (!roleAttribute) return;
		const roleValue = getJsxPropStringValue(roleAttribute);
		if (roleValue === null) return;
		const roles = roleValue.split(/\s+/).filter((token) => token.length > 0);
		for (const role of roles) {
			const required = ROLE_REQUIRED_PROPS.get(role);
			if (!required) continue;
			const missing = required.filter((property) => !hasJsxPropIgnoreCase(node.attributes, property));
			if (missing.length > 0) context.report({
				node: roleAttribute,
				message: buildMessage$1(role, missing)
			});
		}
	} })
});
//#endregion
//#region src/plugin/constants/role-supports-aria-props.ts
const ROLE_SUPPORTS_ARIA_PROPS = {
	alert: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-modal",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	alertdialog: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-modal",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	application: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	article: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-setsize"
	]),
	banner: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-modal",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	blockquote: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-modal",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	button: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-pressed",
		"aria-relevant",
		"aria-roledescription"
	]),
	caption: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan"
	]),
	cell: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan"
	]),
	checkbox: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-checked",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription"
	]),
	code: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan"
	]),
	columnheader: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan",
		"aria-selected",
		"aria-sort"
	]),
	combobox: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-autocomplete",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription"
	]),
	command: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-modal",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	complementary: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-modal",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	composite: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	contentinfo: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	definition: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	deletion: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan"
	]),
	dialog: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-modal",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	directory: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-abstract": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-acknowledgments": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-afterword": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-appendix": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-backlink": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-biblioentry": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-level",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-setsize"
	]),
	"doc-bibliography": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-biblioref": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-chapter": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-colophon": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-conclusion": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-cover": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-credit": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-credits": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-dedication": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-endnote": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-level",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-setsize"
	]),
	"doc-endnotes": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-epigraph": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-epilogue": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-errata": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-example": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-footnote": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-foreword": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-glossary": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-glossref": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-index": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-introduction": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-noteref": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-notice": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-pagebreak": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-pagelist": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-part": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-preface": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-prologue": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-pullquote": /* @__PURE__ */ new Set([]),
	"doc-qna": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-subtitle": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-tip": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"doc-toc": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	document: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	emphasis: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan"
	]),
	feed: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	figure: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	form: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	generic: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan"
	]),
	"graphics-document": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"graphics-object": new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	"graphics-symbol": new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	grid: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-colcount",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowcount"
	]),
	gridcell: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan",
		"aria-selected"
	]),
	group: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	heading: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-level",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	img: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	input: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	insertion: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	landmark: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	link: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	list: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	listbox: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription"
	]),
	listitem: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-level",
		"aria-live",
		"aria-posinset",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-setsize"
	]),
	log: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	main: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	mark: new Set([
		"aria-atomic",
		"aria-braillelabel",
		"aria-brailleroledescription",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-description",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	marquee: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	math: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	menu: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	menubar: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	menuitem: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-setsize"
	]),
	menuitemcheckbox: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-checked",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-required",
		"aria-roledescription",
		"aria-setsize"
	]),
	menuitemradio: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-checked",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-required",
		"aria-roledescription",
		"aria-setsize"
	]),
	meter: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-valuemax",
		"aria-valuemin",
		"aria-valuenow",
		"aria-valuetext"
	]),
	navigation: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	none: /* @__PURE__ */ new Set([]),
	note: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	option: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-checked",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-selected",
		"aria-setsize"
	]),
	paragraph: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	presentation: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	progressbar: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-valuemax",
		"aria-valuemin",
		"aria-valuenow",
		"aria-valuetext"
	]),
	radio: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-checked",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-setsize"
	]),
	radiogroup: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription"
	]),
	range: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-valuemax",
		"aria-valuemin",
		"aria-valuenow"
	]),
	region: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	roletype: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	row: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-level",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowindex",
		"aria-selected",
		"aria-setsize"
	]),
	rowgroup: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	rowheader: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colindex",
		"aria-colspan",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription",
		"aria-rowindex",
		"aria-rowspan",
		"aria-selected",
		"aria-sort"
	]),
	scrollbar: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-valuemax",
		"aria-valuemin",
		"aria-valuenow",
		"aria-valuetext"
	]),
	search: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	searchbox: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-autocomplete",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-multiline",
		"aria-owns",
		"aria-placeholder",
		"aria-relevant",
		"aria-required",
		"aria-roledescription"
	]),
	section: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	sectionhead: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	select: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	separator: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-valuemax",
		"aria-valuemin",
		"aria-valuenow",
		"aria-valuetext"
	]),
	slider: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-valuemin",
		"aria-valuemax",
		"aria-valuenow",
		"aria-valuetext"
	]),
	spinbutton: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription",
		"aria-valuemax",
		"aria-valuemin",
		"aria-valuenow",
		"aria-valuetext"
	]),
	status: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	strong: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	structure: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	subscript: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	superscript: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	switch: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-checked",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription"
	]),
	tab: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-selected",
		"aria-setsize"
	]),
	table: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-colcount",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription",
		"aria-rowcount"
	]),
	tablist: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-level",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	tabpanel: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	term: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	textbox: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-autocomplete",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-multiline",
		"aria-owns",
		"aria-placeholder",
		"aria-relevant",
		"aria-required",
		"aria-roledescription"
	]),
	time: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	timer: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	toolbar: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	tooltip: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	tree: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-errormessage",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription"
	]),
	treegrid: new Set([
		"aria-activedescendant",
		"aria-atomic",
		"aria-busy",
		"aria-colcount",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-invalid",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-orientation",
		"aria-owns",
		"aria-relevant",
		"aria-required",
		"aria-roledescription",
		"aria-rowcount"
	]),
	treeitem: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-checked",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-disabled",
		"aria-dropeffect",
		"aria-expanded",
		"aria-flowto",
		"aria-grabbed",
		"aria-haspopup",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-level",
		"aria-live",
		"aria-owns",
		"aria-posinset",
		"aria-relevant",
		"aria-roledescription",
		"aria-selected",
		"aria-setsize"
	]),
	widget: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	]),
	window: new Set([
		"aria-atomic",
		"aria-busy",
		"aria-controls",
		"aria-current",
		"aria-describedby",
		"aria-details",
		"aria-dropeffect",
		"aria-flowto",
		"aria-grabbed",
		"aria-hidden",
		"aria-keyshortcuts",
		"aria-label",
		"aria-labelledby",
		"aria-live",
		"aria-modal",
		"aria-owns",
		"aria-relevant",
		"aria-roledescription"
	])
};
//#endregion
//#region src/plugin/rules/a11y/role-supports-aria-props.ts
const buildMessageDefault = (role, propName) => `Screen reader users get no help from \`${propName}\` because role \`${role}\` ignores it, so remove it or change the role.`;
const buildMessageImplicit = (role, propName, elementType) => `Screen reader users get no help from \`${propName}\` because \`${elementType}\` has role \`${role}\`, which ignores it, so remove \`${propName}\` or change the element.`;
const getImplicitRole = (node, elementType) => {
	const propStringValue = (propName) => {
		const attribute = hasJsxPropIgnoreCase(node.attributes, propName);
		return attribute ? getJsxPropStringValue(attribute) : null;
	};
	let implicit = "";
	switch (elementType) {
		case "a":
		case "area":
		case "link":
			implicit = hasJsxPropIgnoreCase(node.attributes, "href") ? "link" : "";
			break;
		case "article":
			implicit = "article";
			break;
		case "aside":
			implicit = "complementary";
			break;
		case "body":
			implicit = "document";
			break;
		case "button":
			implicit = "button";
			break;
		case "datalist":
		case "select":
			implicit = "listbox";
			break;
		case "details":
			implicit = "group";
			break;
		case "dialog":
			implicit = "dialog";
			break;
		case "form":
			implicit = "form";
			break;
		case "h1":
		case "h2":
		case "h3":
		case "h4":
		case "h5":
		case "h6":
			implicit = "heading";
			break;
		case "hr":
			implicit = "separator";
			break;
		case "img": {
			const altAttribute = hasJsxPropIgnoreCase(node.attributes, "alt");
			if (!altAttribute) implicit = "img";
			else {
				const value = getJsxPropStringValue(altAttribute);
				implicit = value === null ? "img" : value === "" ? "" : "img";
			}
			break;
		}
		case "input": {
			const inputType = propStringValue("type");
			if (inputType === null) implicit = "textbox";
			else if (inputType === "button" || inputType === "image" || inputType === "reset" || inputType === "submit") implicit = "button";
			else if (inputType === "checkbox") implicit = "checkbox";
			else if (inputType === "radio") implicit = "radio";
			else if (inputType === "range") implicit = "slider";
			else implicit = "textbox";
			break;
		}
		case "li":
			implicit = "listitem";
			break;
		case "menu":
			implicit = propStringValue("type") === "toolbar" ? "toolbar" : "";
			break;
		case "menuitem": {
			const menuitemType = propStringValue("type");
			implicit = menuitemType === "checkbox" ? "menuitemcheckbox" : menuitemType === "command" ? "menuitem" : menuitemType === "radio" ? "menuitemradio" : "";
			break;
		}
		case "meter":
		case "progress":
			implicit = "progressbar";
			break;
		case "nav":
			implicit = "navigation";
			break;
		case "ol":
		case "ul":
			implicit = "list";
			break;
		case "option":
			implicit = "option";
			break;
		case "output":
			implicit = "status";
			break;
		case "section":
			implicit = "region";
			break;
		case "tbody":
		case "tfoot":
		case "thead":
			implicit = "rowgroup";
			break;
		case "textarea":
			implicit = "textbox";
			break;
		default: implicit = "";
	}
	return implicit && VALID_ARIA_ROLES.has(implicit) ? implicit : null;
};
const roleSupportsAriaProps = defineRule({
	id: "role-supports-aria-props",
	title: "Unsupported ARIA prop for role",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Only use `aria-*` attributes that the element's role supports.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const elementType = getElementType(node, context.settings);
		const roleAttribute = hasJsxPropIgnoreCase(node.attributes, "role");
		const role = roleAttribute ? getJsxPropStringValue(roleAttribute) : getImplicitRole(node, elementType);
		if (!role) return;
		if (!VALID_ARIA_ROLES.has(role)) return;
		const isImplicit = !roleAttribute;
		const supported = ROLE_SUPPORTS_ARIA_PROPS[role];
		if (!supported) return;
		for (const attribute of node.attributes) {
			if (!isNodeOfType(attribute, "JSXAttribute")) continue;
			const attributeNode = attribute;
			if (!isNodeOfType(attributeNode.name, "JSXIdentifier")) continue;
			const propRawName = getJsxAttributeName(attributeNode.name);
			if (!propRawName) continue;
			const propName = propRawName.toLowerCase();
			if (!propName.startsWith("aria-")) continue;
			if (!ARIA_PROPERTIES.has(propName)) continue;
			if (supported.has(propName)) continue;
			context.report({
				node: attribute,
				message: isImplicit ? buildMessageImplicit(role, propName, elementType) : buildMessageDefault(role, propName)
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/rules-of-hooks.ts
const buildTopLevelMessage = (hookName) => `\`${hookName}\` crashes at the top level of a file.`;
const buildNonComponentMessage = (hookName, functionName) => `\`${hookName}\` crashes inside \`${functionName}\` because it isn't a component or a hook.`;
const buildConditionalMessage = (hookName) => `\`${hookName}\` crashes when you call it conditionally.`;
const buildLoopMessage = (hookName) => `\`${hookName}\` crashes when you call it inside a loop.`;
const buildAsyncMessage = (hookName) => `\`${hookName}\` crashes inside an async function.`;
const buildClassComponentMessage = (hookName) => `\`${hookName}\` crashes in a class component.`;
const buildTryMessage = (hookName) => `\`${hookName}\` crashes inside a try/catch/finally block.`;
const buildEffectEventCallMessage = (bindingName) => `\`${bindingName}\` comes from useEffectEvent, so it only works when called from Effects in the same component.`;
const buildEffectEventAssignmentMessage = (bindingName) => `${buildEffectEventCallMessage(bindingName)} It also breaks if saved in a variable or passed around.`;
const buildEffectEventPassedDownMessage = () => `A function from useEffectEvent breaks when passed around.`;
const ASCII_UPPERCASE_A = 65;
const ASCII_UPPERCASE_Z = 90;
const EFFECT_HOOK_NAMES = new Set([
	"useEffect",
	"useLayoutEffect",
	"useInsertionEffect"
]);
const isPascalCaseIdentifier = (identifier) => {
	const firstCharCode = identifier.name.charCodeAt(0);
	return firstCharCode >= ASCII_UPPERCASE_A && firstCharCode <= ASCII_UPPERCASE_Z;
};
const buildAdditionalEffectHooksRegex = (additionalEffectHooks) => {
	if (!additionalEffectHooks) return null;
	try {
		return new RegExp(additionalEffectHooks);
	} catch {
		return null;
	}
};
const getHookNameFromCallee = (callee) => {
	if (isNodeOfType(callee, "Identifier")) return callee.name;
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed && isNodeOfType(callee.property, "Identifier")) return callee.property.name;
	return null;
};
const readAdditionalEffectHooks = (value) => {
	if (typeof value !== "object" || value === null) return "";
	return value.additionalEffectHooks ?? "";
};
const readAllowedPascalCaseHookNamespaces = (value) => {
	if (typeof value !== "object" || value === null) return [];
	return value.allowedPascalCaseHookNamespaces ?? [];
};
const resolveSettings$3 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const reactDoctorSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor["rulesOfHooks"] : void 0;
	return {
		additionalEffectHooks: reactDoctorSettings?.additionalEffectHooks ?? readAdditionalEffectHooks(settings?.["react-hooks"]),
		allowedPascalCaseHookNamespaces: reactDoctorSettings?.allowedPascalCaseHookNamespaces ?? readAllowedPascalCaseHookNamespaces(settings?.["react-hooks"])
	};
};
const isHookCall = (call, scopes, settings) => {
	if (!isNodeOfType(call, "CallExpression")) return null;
	const callee = call.callee;
	if (isNodeOfType(callee, "Identifier") && isReactHookName(callee.name)) {
		const symbol = scopes.symbolFor(callee);
		if (symbol) {
			if (symbol.kind === "parameter" || symbol.kind === "catch-clause-parameter") return null;
			if (callee.name === "use") {
				if (resolveReactImportName(symbol, scopes) !== "use") return null;
				if (symbol.kind === "function") return null;
			}
		}
		return { hookName: callee.name };
	}
	if (isNodeOfType(callee, "MemberExpression") && !callee.computed && isNodeOfType(callee.property, "Identifier") && isReactHookName(callee.property.name)) {
		const callObject = callee.object;
		const propertyName = callee.property.name;
		if (isNodeOfType(callObject, "Identifier")) {
			if (settings.allowedPascalCaseHookNamespaces.includes(callObject.name)) return null;
			if (!isPascalCaseIdentifier(callObject)) return null;
			return { hookName: propertyName };
		}
		return null;
	}
	return null;
};
const isReactImport = (symbol) => {
	let importDeclaration = symbol.declarationNode?.parent;
	while (importDeclaration && !isNodeOfType(importDeclaration, "ImportDeclaration")) importDeclaration = importDeclaration.parent ?? null;
	if (!importDeclaration || !isNodeOfType(importDeclaration, "ImportDeclaration")) return false;
	const source = importDeclaration.source;
	return Boolean(source && isNodeOfType(source, "Literal") && source.value === "react");
};
const resolveReactImportName = (symbol, scopes, visitedSymbolIds = /* @__PURE__ */ new Set()) => {
	if (visitedSymbolIds.has(symbol.id)) return null;
	visitedSymbolIds.add(symbol.id);
	if (symbol.kind === "import") {
		if (!isReactImport(symbol)) return null;
		const declarationNode = symbol.declarationNode;
		if (isNodeOfType(declarationNode, "ImportSpecifier")) {
			const importedName = declarationNode.imported;
			if (isNodeOfType(importedName, "Identifier")) return importedName.name;
		}
		return symbol.name;
	}
	const initializer = symbol.initializer;
	if (!initializer) return null;
	if (isNodeOfType(initializer, "MemberExpression") && isNodeOfType(initializer.object, "Identifier") && initializer.object.name === "React" && isNodeOfType(initializer.property, "Identifier") && !initializer.computed) return initializer.property.name;
	if (isRequireReactCall(initializer)) return symbol.name;
	if (isNodeOfType(initializer, "MemberExpression") && isRequireReactCall(initializer.object) && isNodeOfType(initializer.property, "Identifier") && !initializer.computed) return initializer.property.name;
	if (isNodeOfType(initializer, "Identifier")) {
		const aliasSymbol = scopes.symbolFor(initializer);
		if (aliasSymbol) {
			const aliasResolvedName = resolveReactImportName(aliasSymbol, scopes, visitedSymbolIds);
			if (!aliasResolvedName) return null;
			const destructureKey = inferDestructureSourceKey(symbol.bindingIdentifier);
			if (destructureKey !== null) return destructureKey;
			return aliasResolvedName;
		}
	}
	return null;
};
const isRequireReactCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	if (!isNodeOfType(node.callee, "Identifier") || node.callee.name !== "require") return false;
	const firstArgument = node.arguments[0];
	if (!firstArgument || !isNodeOfType(firstArgument, "Literal")) return false;
	return firstArgument.value === "react";
};
const inferDestructureSourceKey = (bindingIdentifier) => {
	let current = bindingIdentifier.parent;
	while (current) {
		if (isNodeOfType(current, "Property")) {
			if (!current.computed && isNodeOfType(current.key, "Identifier")) return current.key.name;
			if (isNodeOfType(current.key, "Literal") && typeof current.key.value === "string") return current.key.value;
			return null;
		}
		if (isNodeOfType(current, "VariableDeclarator") || isNodeOfType(current, "ArrowFunctionExpression") || isNodeOfType(current, "FunctionExpression") || isNodeOfType(current, "FunctionDeclaration")) return null;
		current = current.parent ?? null;
	}
	return null;
};
const isReactUseHook = (hookName) => hookName === "use";
const getCallExpressionCalleeName = (callExpression) => {
	const callee = callExpression.callee;
	if (isNodeOfType(callee, "Identifier")) return callee.name;
	if (isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && callee.object.name === "React" && isNodeOfType(callee.property, "Identifier")) return `React.${callee.property.name}`;
	return null;
};
const inferFunctionName = (functionNode) => {
	if ((isNodeOfType(functionNode, "FunctionDeclaration") || isNodeOfType(functionNode, "FunctionExpression")) && functionNode.id) return functionNode.id.name;
	let parent = functionNode.parent;
	while (parent && isNodeOfType(parent, "CallExpression")) {
		const calleeName = getCallExpressionCalleeName(parent);
		if (calleeName !== null && REACT_HOC_NAMES.has(calleeName)) parent = parent.parent ?? null;
		else break;
	}
	while (parent && (isNodeOfType(parent, "AssignmentPattern") || parent.type === "TSAsExpression" || parent.type === "TSSatisfiesExpression" || parent.type === "TSNonNullExpression" || parent.type === "ChainExpression")) parent = parent.parent ?? null;
	if (!parent) return null;
	if (isNodeOfType(parent, "VariableDeclarator") && isNodeOfType(parent.id, "Identifier")) return parent.id.name;
	if (isNodeOfType(parent, "AssignmentExpression") && isNodeOfType(parent.left, "Identifier")) return parent.left.name;
	if (isNodeOfType(parent, "Property") && !parent.computed && isNodeOfType(parent.key, "Identifier")) return parent.key.name;
	return null;
};
const findEnclosingFunctionInfo = (node) => {
	let current = node.parent;
	while (current) {
		if (isNodeOfType(current, "FunctionDeclaration") || isNodeOfType(current, "FunctionExpression") || isNodeOfType(current, "ArrowFunctionExpression")) {
			const resolvedName = inferFunctionName(current);
			const displayName = resolvedName ?? "anonymous";
			return {
				node: current,
				name: displayName,
				hasResolvedName: resolvedName !== null,
				isAsync: Boolean(current.async),
				isComponentOrHook: resolvedName === null ? false : isReactComponentOrHookName(displayName)
			};
		}
		current = current.parent ?? null;
	}
	return null;
};
const isInsideClassComponent = (node) => {
	let current = node.parent;
	while (current) {
		if (isNodeOfType(current, "ClassDeclaration") || isNodeOfType(current, "ClassExpression")) return true;
		if (isNodeOfType(current, "FunctionDeclaration") || isNodeOfType(current, "FunctionExpression") || isNodeOfType(current, "ArrowFunctionExpression")) {
			const functionParent = current.parent;
			if (!(Boolean(functionParent) && (isNodeOfType(functionParent, "MethodDefinition") || isNodeOfType(functionParent, "PropertyDefinition")))) return false;
		}
		current = current.parent ?? null;
	}
	return false;
};
const hasShortCircuitAncestor = (descendant, ancestor) => {
	let current = descendant.parent;
	while (current && current !== ancestor) {
		if (isNodeOfType(current, "ConditionalExpression")) return true;
		if (isNodeOfType(current, "LogicalExpression") && (current.operator === "&&" || current.operator === "||" || current.operator === "??") && isWithinRange(descendant, current.right)) return true;
		current = current.parent ?? null;
	}
	return false;
};
const isWithinRange = (descendant, sibling) => {
	const descendantSpan = descendant;
	const siblingSpan = sibling;
	if (typeof descendantSpan.start !== "number" || typeof siblingSpan.start !== "number" || typeof siblingSpan.end !== "number") return false;
	return descendantSpan.start >= siblingSpan.start && (descendantSpan.end ?? siblingSpan.end) <= siblingSpan.end;
};
const isInsideTry = (descendant, ancestor) => {
	let current = descendant.parent;
	while (current && current !== ancestor) {
		if (isNodeOfType(current, "TryStatement") || isNodeOfType(current, "CatchClause")) return true;
		current = current.parent ?? null;
	}
	return false;
};
const isInsideLoop = (descendant, ancestor) => {
	let current = descendant.parent;
	while (current && current !== ancestor) {
		if (isNodeOfType(current, "ForStatement") || isNodeOfType(current, "ForInStatement") || isNodeOfType(current, "ForOfStatement") || isNodeOfType(current, "WhileStatement") || isNodeOfType(current, "DoWhileStatement")) return true;
		current = current.parent ?? null;
	}
	return false;
};
const isUseEffectEventSymbol = (symbol) => {
	const initializer = symbol.initializer;
	if (!initializer || !isNodeOfType(initializer, "CallExpression")) return false;
	return getHookNameFromCallee(initializer.callee) === "useEffectEvent";
};
const findEnclosingComponentOrHookFunction = (node) => {
	let current = node.parent;
	while (current) {
		if (isFunctionLike$2(current)) {
			const resolvedName = inferFunctionName(current);
			if (resolvedName !== null && isReactComponentOrHookName(resolvedName)) return current;
		}
		current = current.parent ?? null;
	}
	return null;
};
const isSameComponentOrHookScope = (symbol, referenceNode) => findEnclosingComponentOrHookFunction(symbol.declarationNode) === findEnclosingComponentOrHookFunction(referenceNode);
const isAllowedEffectEventHook = (hookName, additionalEffectHooksRegex) => hookName === "useEffectEvent" || EFFECT_HOOK_NAMES.has(hookName) || Boolean(additionalEffectHooksRegex && additionalEffectHooksRegex.test(hookName));
const isCallbackArgumentForAllowedEffectEventHook = (functionNode, additionalEffectHooksRegex) => {
	const parent = functionNode.parent;
	if (!parent || !isNodeOfType(parent, "CallExpression")) return false;
	const hookName = getHookNameFromCallee(parent.callee);
	if (!hookName) return false;
	if (!parent.arguments.some((argument) => argument === functionNode)) return false;
	return isAllowedEffectEventHook(hookName, additionalEffectHooksRegex);
};
const isInsideAllowedEffectEventCallback = (node, additionalEffectHooksRegex) => {
	let current = node.parent;
	while (current) {
		if (isFunctionLike$2(current) && isCallbackArgumentForAllowedEffectEventHook(current, additionalEffectHooksRegex)) return true;
		current = current.parent ?? null;
	}
	return false;
};
const isCallCallee = (identifier) => {
	const parent = identifier.parent;
	return Boolean(parent && isNodeOfType(parent, "CallExpression") && parent.callee === identifier);
};
const isUseEffectEventInitializer = (node) => {
	const parent = node.parent;
	return Boolean(parent && isNodeOfType(parent, "VariableDeclarator") && parent.init === node && isNodeOfType(parent.id, "Identifier"));
};
const rulesOfHooks = defineRule({
	id: "rules-of-hooks",
	title: "Hook called conditionally",
	severity: "error",
	tags: ["test-noise"],
	recommendation: "Call hooks at the top level of a React function component or a custom Hook.",
	category: "Correctness",
	create: (context) => {
		const settings = resolveSettings$3(context.settings);
		const additionalEffectHooksRegex = buildAdditionalEffectHooksRegex(settings.additionalEffectHooks);
		return {
			CallExpression(node) {
				const hookContext = isHookCall(node, context.scopes, settings);
				if (!hookContext) return;
				const { hookName } = hookContext;
				if (hookName === "useEffectEvent" && !isUseEffectEventInitializer(node)) {
					context.report({
						node: node.callee,
						message: buildEffectEventPassedDownMessage()
					});
					return;
				}
				const enclosing = findEnclosingFunctionInfo(node);
				if (!enclosing) {
					context.report({
						node: node.callee,
						message: buildTopLevelMessage(hookName)
					});
					return;
				}
				if (isInsideClassComponent(node)) {
					context.report({
						node: node.callee,
						message: buildClassComponentMessage(hookName)
					});
					return;
				}
				if (enclosing.isAsync) {
					context.report({
						node: node.callee,
						message: buildAsyncMessage(hookName)
					});
					return;
				}
				if (isReactUseHook(hookName)) {
					let outerWalker = enclosing.node;
					let isInsideComponentOrHook = enclosing.isComponentOrHook;
					while (!isInsideComponentOrHook && outerWalker) {
						const parentInfo = findEnclosingFunctionInfo(outerWalker);
						if (!parentInfo) break;
						outerWalker = parentInfo.node;
						if (parentInfo.isComponentOrHook) isInsideComponentOrHook = true;
					}
					if (!isInsideComponentOrHook) {
						context.report({
							node: node.callee,
							message: buildNonComponentMessage(hookName, enclosing.name)
						});
						return;
					}
					if (isInsideTry(node, enclosing.node)) context.report({
						node: node.callee,
						message: buildTryMessage(hookName)
					});
					return;
				}
				if (!enclosing.hasResolvedName) {
					let outerWalker = enclosing.node;
					let outerIsComponentOrHook = false;
					while (outerWalker) {
						const outerInfo = findEnclosingFunctionInfo(outerWalker);
						if (!outerInfo) break;
						if (outerInfo.isComponentOrHook) {
							outerIsComponentOrHook = true;
							break;
						}
						outerWalker = outerInfo.node;
					}
					if (!outerIsComponentOrHook) return;
					context.report({
						node: node.callee,
						message: buildConditionalMessage(hookName)
					});
					return;
				}
				if (!enclosing.isComponentOrHook) {
					context.report({
						node: node.callee,
						message: buildNonComponentMessage(hookName, enclosing.name)
					});
					return;
				}
				if (isInsideLoop(node, enclosing.node)) {
					context.report({
						node: node.callee,
						message: buildLoopMessage(hookName)
					});
					return;
				}
				if (isInsideTry(node, enclosing.node)) {
					context.report({
						node: node.callee,
						message: buildTryMessage(hookName)
					});
					return;
				}
				if (hasShortCircuitAncestor(node, enclosing.node)) {
					context.report({
						node: node.callee,
						message: buildConditionalMessage(hookName)
					});
					return;
				}
				if (!context.cfg.isUnconditionalFromEntry(node)) context.report({
					node: node.callee,
					message: buildConditionalMessage(hookName)
				});
			},
			Identifier(node) {
				const symbol = context.scopes.referenceFor(node)?.resolvedSymbol;
				if (!symbol || !isUseEffectEventSymbol(symbol)) return;
				if (!isSameComponentOrHookScope(symbol, node)) return;
				if (isInsideAllowedEffectEventCallback(node, additionalEffectHooksRegex)) return;
				context.report({
					node,
					message: isCallCallee(node) ? buildEffectEventCallMessage(symbol.name) : buildEffectEventAssignmentMessage(symbol.name)
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/a11y/scope.ts
const MESSAGE$3 = "Screen reader users get no help from `scope` here because it only works on `<th>` cells, so remove it.";
const scope = defineRule({
	id: "scope",
	title: "scope attribute on non-th element",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Only use `scope` on `<th>` cells.",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const scopeAttribute = hasJsxProp(node.attributes, "scope");
		if (!scopeAttribute) return;
		const tag = getElementType(node, context.settings);
		if (!HTML_TAGS.has(tag)) return;
		if (tag !== "th") context.report({
			node: scopeAttribute,
			message: MESSAGE$3
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/self-closing-comp.ts
const MESSAGE$2 = "This tag has no children.";
const resolveSettings$2 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	const ruleSettings = typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.selfClosingComp ?? {} : {};
	return {
		component: ruleSettings.component ?? true,
		html: ruleSettings.html ?? true
	};
};
const isLowercaseIdentifier = (name) => {
	if (!isNodeOfType(name, "JSXIdentifier")) return false;
	const firstCharacter = name.name.charCodeAt(0);
	return firstCharacter >= 97 && firstCharacter <= 122;
};
const selfClosingComp = defineRule({
	id: "self-closing-comp",
	title: "Element not self-closing",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Use the self-closing form `<X />` for elements with no children.",
	category: "Architecture",
	create: (context) => {
		const settings = resolveSettings$2(context.settings);
		return { JSXElement(node) {
			if (!node.closingElement) return;
			if (node.children.length > 1) return;
			if (node.children.length === 1) {
				const onlyChild = node.children[0];
				if (!isNodeOfType(onlyChild, "JSXText")) return;
				const textValue = onlyChild.value;
				if (!(textValue.includes("\n") && /^\s*$/.test(textValue))) return;
			}
			const elementName = node.openingElement.name;
			const isCustomComponent = isNodeOfType(elementName, "JSXMemberExpression") || isNodeOfType(elementName, "JSXNamespacedName") || isNodeOfType(elementName, "JSXIdentifier") && !isLowercaseIdentifier(elementName);
			const isHtmlTag = isNodeOfType(elementName, "JSXIdentifier") && isLowercaseIdentifier(elementName);
			if (isCustomComponent && !settings.component) return;
			if (isHtmlTag && !settings.html) return;
			context.report({
				node: node.openingElement,
				message: MESSAGE$2
			});
		} };
	}
});
//#endregion
//#region src/plugin/rules/server/server-after-nonblocking.ts
const CONSOLE_DEFERRABLE_METHODS = new Set([
	"log",
	"info",
	"warn"
]);
const ANALYTICS_DEFERRABLE_OBJECTS = new Set([
	"analytics",
	"posthog",
	"mixpanel",
	"segment",
	"amplitude",
	"datadog",
	"sentry"
]);
const ANALYTICS_DEFERRABLE_METHODS = new Set([
	"track",
	"identify",
	"page",
	"capture",
	"captureMessage",
	"captureException",
	"log"
]);
const isDeferrableSideEffectCall = (objectName, methodName) => {
	if (objectName === "console") return CONSOLE_DEFERRABLE_METHODS.has(methodName);
	if (ANALYTICS_DEFERRABLE_OBJECTS.has(objectName)) return ANALYTICS_DEFERRABLE_METHODS.has(methodName);
	return false;
};
const serverAfterNonblocking = defineRule({
	id: "server-after-nonblocking",
	title: "Blocking side effect before response",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "`import { after } from 'next/server'`, then wrap it: `after(() => analytics.track(...))`. The response sends right away.",
	create: (context) => {
		let fileHasUseServerDirective = false;
		let serverFunctionDepth = 0;
		const enterIfServerFunction = (node) => {
			if (hasUseServerDirective(node)) serverFunctionDepth++;
		};
		const leaveIfServerFunction = (node) => {
			if (hasUseServerDirective(node)) serverFunctionDepth = Math.max(0, serverFunctionDepth - 1);
		};
		return {
			Program(programNode) {
				fileHasUseServerDirective = hasDirective(programNode, "use server");
			},
			FunctionDeclaration: enterIfServerFunction,
			"FunctionDeclaration:exit": leaveIfServerFunction,
			FunctionExpression: enterIfServerFunction,
			"FunctionExpression:exit": leaveIfServerFunction,
			ArrowFunctionExpression: enterIfServerFunction,
			"ArrowFunctionExpression:exit": leaveIfServerFunction,
			CallExpression(node) {
				if (!fileHasUseServerDirective && serverFunctionDepth === 0) return;
				if (!isNodeOfType(node.callee, "MemberExpression")) return;
				if (!isNodeOfType(node.callee.property, "Identifier")) return;
				const objectName = isNodeOfType(node.callee.object, "Identifier") ? node.callee.object.name : null;
				if (!objectName) return;
				const methodName = node.callee.property.name;
				if (!isDeferrableSideEffectCall(objectName, methodName)) return;
				context.report({
					node,
					message: `${objectName}.${methodName}() runs before the response, so your users wait longer for it.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/server/server-auth-actions.ts
const isAsyncFunctionLikeNode = (node) => {
	if (!node) return false;
	if (!isNodeOfType(node, "FunctionDeclaration") && !isNodeOfType(node, "FunctionExpression") && !isNodeOfType(node, "ArrowFunctionExpression")) return false;
	return Boolean(node.async);
};
const unwrapTypeWrappedCallee = (node) => {
	let currentNode = node;
	while (currentNode) {
		if (isNodeOfType(currentNode, "TSAsExpression") || isNodeOfType(currentNode, "TSNonNullExpression") || isNodeOfType(currentNode, "TSTypeAssertion") || isNodeOfType(currentNode, "TSSatisfiesExpression") || isNodeOfType(currentNode, "TSInstantiationExpression")) {
			currentNode = currentNode.expression;
			continue;
		}
		if (isNodeOfType(currentNode, "ChainExpression")) {
			currentNode = currentNode.expression;
			continue;
		}
		return currentNode;
	}
	return null;
};
const buildDottedReceiverSource = (receiverNode) => {
	const unwrapped = unwrapTypeWrappedCallee(receiverNode);
	if (!unwrapped) return "";
	if (isNodeOfType(unwrapped, "Identifier")) return unwrapped.name;
	if (isNodeOfType(unwrapped, "ThisExpression")) return "this";
	if (isNodeOfType(unwrapped, "MemberExpression")) {
		const objectSource = buildDottedReceiverSource(unwrapped.object);
		const propertyName = isNodeOfType(unwrapped.property, "Identifier") ? unwrapped.property.name : "";
		if (!propertyName) return objectSource;
		return objectSource ? `${objectSource}.${propertyName}` : propertyName;
	}
	return "";
};
const isMemberCallAuthRelated = (receiverNode, methodName, genericMethodNames) => {
	if (!genericMethodNames.has(methodName)) return true;
	const receiverSource = buildDottedReceiverSource(receiverNode);
	return AUTH_OBJECT_PATTERN.test(receiverSource);
};
const getAuthCallName = (callExpression, allowedFunctionNames, genericMethodNames) => {
	const calleeNode = unwrapTypeWrappedCallee(callExpression.callee);
	if (!calleeNode) return null;
	if (isNodeOfType(calleeNode, "Identifier")) return allowedFunctionNames.has(calleeNode.name) ? calleeNode.name : null;
	if (isNodeOfType(calleeNode, "MemberExpression") && isNodeOfType(calleeNode.property, "Identifier")) {
		const methodName = calleeNode.property.name;
		if (!allowedFunctionNames.has(methodName)) return null;
		if (!isMemberCallAuthRelated(calleeNode.object, methodName, genericMethodNames)) return null;
		return methodName;
	}
	return null;
};
const containsAuthCheck = (rootNodes, allowedFunctionNames, genericMethodNames) => {
	let foundAuthCall = false;
	for (const rootNode of rootNodes) walkAst(rootNode, (child) => {
		if (foundAuthCall) return;
		if (isFunctionLike$2(child)) return false;
		if (!isNodeOfType(child, "CallExpression")) return;
		if (getAuthCallName(child, allowedFunctionNames, genericMethodNames)) foundAuthCall = true;
	});
	return foundAuthCall;
};
const getAuthScanRoots = (functionNode) => {
	const bodyNode = functionNode.body;
	if (!bodyNode) return [];
	if (isNodeOfType(bodyNode, "BlockStatement")) return (bodyNode.body ?? []).slice(0, 10);
	return [bodyNode];
};
const inspectServerAction = (candidate, fileHasUseServerDirective, allowedFunctionNames, context) => {
	if (!(fileHasUseServerDirective || hasUseServerDirective(candidate.functionNode))) return;
	if (containsAuthCheck(getAuthScanRoots(candidate.functionNode), allowedFunctionNames, GENERIC_AUTH_METHOD_NAMES)) return;
	context.report({
		node: candidate.reportNode,
		message: `Anyone can call server action "${candidate.displayName}" without logging in, since it has no auth check.`
	});
};
const collectCandidatesFromVariableDeclaration = (variableDeclaration) => {
	const candidates = [];
	for (const declarator of variableDeclaration.declarations ?? []) {
		if (!isAsyncFunctionLikeNode(declarator.init)) continue;
		const bindingNode = isNodeOfType(declarator.id, "Identifier") ? declarator.id : null;
		candidates.push({
			functionNode: declarator.init,
			displayName: bindingNode?.name ?? "anonymous",
			reportNode: bindingNode ?? declarator
		});
	}
	return candidates;
};
const getCandidateFromDefaultDeclaration = (node) => {
	const declaration = node.declaration;
	if (!isAsyncFunctionLikeNode(declaration)) return null;
	const functionId = (isNodeOfType(declaration, "FunctionDeclaration") || isNodeOfType(declaration, "FunctionExpression")) && declaration.id ? declaration.id : null;
	return {
		functionNode: declaration,
		displayName: functionId?.name ?? "default",
		reportNode: functionId ?? node
	};
};
const serverAuthActions = defineRule({
	id: "server-auth-actions",
	title: "Server action without auth check",
	severity: "error",
	recommendation: "Add `const session = await auth()` at the top, and throw or redirect if the user isn't allowed before touching any data.",
	create: (context) => {
		let fileHasUseServerDirective = false;
		const customAuthFunctionNames = getReactDoctorStringArraySetting(context.settings, "serverAuthFunctionNames");
		const allowedFunctionNames = customAuthFunctionNames.length > 0 ? new Set([...AUTH_FUNCTION_NAMES, ...customAuthFunctionNames]) : AUTH_FUNCTION_NAMES;
		const inspect = (candidate) => inspectServerAction(candidate, fileHasUseServerDirective, allowedFunctionNames, context);
		return {
			Program(programNode) {
				fileHasUseServerDirective = hasDirective(programNode, "use server");
			},
			ExportNamedDeclaration(node) {
				const declaration = node.declaration;
				if (!declaration) return;
				if (isAsyncFunctionLikeNode(declaration)) {
					if (!isNodeOfType(declaration, "FunctionDeclaration")) return;
					inspect({
						functionNode: declaration,
						displayName: declaration.id?.name ?? "anonymous",
						reportNode: declaration.id ?? node
					});
					return;
				}
				if (isNodeOfType(declaration, "VariableDeclaration")) for (const candidate of collectCandidatesFromVariableDeclaration(declaration)) inspect(candidate);
			},
			ExportDefaultDeclaration(node) {
				const candidate = getCandidateFromDefaultDeclaration(node);
				if (candidate) inspect(candidate);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/server/server-cache-with-object-literal.ts
const serverCacheWithObjectLiteral = defineRule({
	id: "server-cache-with-object-literal",
	title: "React.cache with object literal",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Pass plain values like strings or numbers, not an object. React.cache() matches the exact value, so a new `{}` each render misses the cache.",
	create: (context) => {
		const cachedFunctionNames = /* @__PURE__ */ new Set();
		return {
			VariableDeclarator(node) {
				if (!isNodeOfType(node.id, "Identifier")) return;
				const init = node.init;
				if (!isNodeOfType(init, "CallExpression")) return;
				const callee = init.callee;
				if (!(isNodeOfType(callee, "Identifier") && callee.name === "cache" || isNodeOfType(callee, "MemberExpression") && isNodeOfType(callee.object, "Identifier") && callee.object.name === "React" && isNodeOfType(callee.property, "Identifier") && callee.property.name === "cache")) return;
				cachedFunctionNames.add(node.id.name);
			},
			CallExpression(node) {
				if (!isNodeOfType(node.callee, "Identifier")) return;
				if (!cachedFunctionNames.has(node.callee.name)) return;
				const firstArg = node.arguments?.[0];
				if (!isNodeOfType(firstArg, "ObjectExpression")) return;
				context.report({
					node,
					message: `Passing a new object to React.cache() each render misses the cache, so it refetches every request.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/server/server-dedup-props.ts
const DERIVING_ARRAY_METHODS = new Set([
	"toSorted",
	"toReversed",
	"filter",
	"map",
	"slice"
]);
const getDerivingMethodName = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return null;
	if (!isNodeOfType(node.callee, "MemberExpression")) return null;
	if (!isNodeOfType(node.callee.property, "Identifier")) return null;
	return node.callee.property.name;
};
const serverDedupProps = defineRule({
	id: "server-dedup-props",
	title: "Duplicate data in server props",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Pass the source array once and build the other version on the client. Sending both ships the data twice to the browser.",
	create: (context) => ({ JSXOpeningElement(node) {
		const identifierAttributes = /* @__PURE__ */ new Map();
		const derivedAttributes = [];
		for (const attr of node.attributes ?? []) {
			if (!isNodeOfType(attr, "JSXAttribute")) continue;
			if (!isNodeOfType(attr.name, "JSXIdentifier")) continue;
			if (!isNodeOfType(attr.value, "JSXExpressionContainer")) continue;
			const expression = attr.value.expression;
			if (!expression) continue;
			if (isNodeOfType(expression, "Identifier")) identifierAttributes.set(expression.name, attr.name.name);
			else if (isNodeOfType(expression, "CallExpression")) {
				const derivingMethod = getDerivingMethodName(expression);
				if (!derivingMethod || !DERIVING_ARRAY_METHODS.has(derivingMethod)) continue;
				const root = getRootIdentifierName(expression, { followCallChains: true });
				if (!root) continue;
				derivedAttributes.push({
					propName: attr.name.name,
					rootName: root,
					node: attr
				});
			}
		}
		for (const derived of derivedAttributes) {
			const sourcePropName = identifierAttributes.get(derived.rootName);
			if (sourcePropName) context.report({
				node: derived.node,
				message: `Passing both "${derived.propName}" & "${sourcePropName}" ships the same data twice to your users (source: ${derived.rootName}).`
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/server/server-fetch-without-revalidate.ts
const isFetchCall = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return false;
	return isNodeOfType(node.callee, "Identifier") && node.callee.name === "fetch";
};
const objectExpressionHasNextRevalidate = (objectExpression) => {
	if (!isNodeOfType(objectExpression, "ObjectExpression")) return false;
	for (const property of objectExpression.properties ?? []) {
		if (!isNodeOfType(property, "Property")) continue;
		if (!isNodeOfType(property.key, "Identifier")) continue;
		if (property.key.name === "cache") return true;
		if (property.key.name !== "next") continue;
		if (!isNodeOfType(property.value, "ObjectExpression")) return true;
		for (const innerProperty of property.value.properties ?? []) {
			if (!isNodeOfType(innerProperty, "Property")) continue;
			if (!isNodeOfType(innerProperty.key, "Identifier")) continue;
			if (innerProperty.key.name === "revalidate" || innerProperty.key.name === "tags") return true;
		}
		return true;
	}
	return false;
};
const APP_ROUTER_FILE_PATTERN = /\/app\/(?:[^/]+\/)*(?:route|page|layout|template|loading|error|default)\.(?:tsx?|jsx?)$/;
const NON_PROJECT_PATH_PATTERN = /\/(?:node_modules|dist|build|\.next)\//;
const serverFetchWithoutRevalidate = defineRule({
	id: "server-fetch-without-revalidate",
	title: "Fetch without revalidate",
	severity: "warn",
	disabledBy: ["nextjs:15"],
	recommendation: "Pass `{ next: { revalidate: <seconds> } }` (or `cache: \"no-store\"`) so old data doesn't stick around.",
	create: (context) => {
		let isServerSideFile = false;
		return {
			Program(node) {
				const filename = normalizeFilename$1(context.filename ?? "");
				if (!APP_ROUTER_FILE_PATTERN.test(filename)) {
					isServerSideFile = false;
					return;
				}
				if (NON_PROJECT_PATH_PATTERN.test(filename)) {
					isServerSideFile = false;
					return;
				}
				isServerSideFile = !(node.body ?? []).some((statement) => isNodeOfType(statement, "ExpressionStatement") && isNodeOfType(statement.expression, "Literal") && statement.expression.value === "use client");
			},
			CallExpression(node) {
				if (!isServerSideFile) return;
				if (!isFetchCall(node)) return;
				const optionsArg = node.arguments?.[1];
				if (optionsArg && objectExpressionHasNextRevalidate(optionsArg)) return;
				const urlArg = node.arguments?.[0];
				const urlText = isNodeOfType(urlArg, "Literal") && typeof urlArg.value === "string" ? `"${urlArg.value}"` : "url";
				context.report({
					node,
					message: `fetch(${urlText}) is cached forever by default, so your users can see stale data.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/server/server-hoist-static-io.ts
const STATIC_IO_FUNCTIONS = new Set([
	"readFileSync",
	"readFile",
	"readdir",
	"readdirSync",
	"stat",
	"statSync",
	"access",
	"accessSync"
]);
const isStaticIoCall = (call) => {
	if (!isNodeOfType(call, "CallExpression")) return false;
	const callee = call.callee;
	if (isNodeOfType(callee, "Identifier") && STATIC_IO_FUNCTIONS.has(callee.name)) return true;
	if (!isNodeOfType(callee, "MemberExpression")) return false;
	const propertyName = isNodeOfType(callee.property, "Identifier") ? callee.property.name : null;
	if (!propertyName || !STATIC_IO_FUNCTIONS.has(propertyName)) return false;
	return true;
};
const isFetchOfImportMetaUrl = (call) => {
	if (!isNodeOfType(call, "CallExpression")) return false;
	if (!isNodeOfType(call.callee, "Identifier") || call.callee.name !== "fetch") return false;
	const arg = call.arguments?.[0];
	if (!arg) return false;
	if (!isNodeOfType(arg, "NewExpression")) return false;
	if (!isNodeOfType(arg.callee, "Identifier") || arg.callee.name !== "URL") return false;
	const secondArg = arg.arguments?.[1];
	if (!secondArg) return false;
	return isNodeOfType(secondArg, "MemberExpression") && isNodeOfType(secondArg.object, "MetaProperty") && isNodeOfType(secondArg.property, "Identifier") && secondArg.property.name === "url";
};
const callReadsHandlerArgs = (call, handlerParamNames) => {
	if (handlerParamNames.size === 0) return false;
	let referencesArg = false;
	walkAst(call, (child) => {
		if (referencesArg) return;
		if (isNodeOfType(child, "Identifier") && handlerParamNames.has(child.name)) referencesArg = true;
	});
	return referencesArg;
};
const PAGES_ROUTER_API_PATH_PATTERN = /\/pages\/api\//;
const inspectHandlerBody = (context, handlerBody, handlerLabel, handlerParamNames) => {
	walkAst(handlerBody, (child) => {
		let staticCall = null;
		if (isStaticIoCall(child)) staticCall = child;
		else if (isFetchOfImportMetaUrl(child)) staticCall = child;
		else if (isNodeOfType(child, "AwaitExpression") && child.argument && (isStaticIoCall(child.argument) || isFetchOfImportMetaUrl(child.argument))) staticCall = child.argument;
		if (!staticCall) return;
		if (callReadsHandlerArgs(staticCall, handlerParamNames)) return;
		if (!isNodeOfType(staticCall, "CallExpression")) return;
		const calleeText = isNodeOfType(staticCall.callee, "MemberExpression") && isNodeOfType(staticCall.callee.property, "Identifier") ? `${isNodeOfType(staticCall.callee.object, "Identifier") ? staticCall.callee.object.name : "?"}.${staticCall.callee.property.name}` : isNodeOfType(staticCall.callee, "Identifier") ? staticCall.callee.name : "io";
		context.report({
			node: staticCall,
			message: `${calleeText}() runs on every request in ${handlerLabel}, re-reading the same file each time.`
		});
	});
};
const collectIdentifierParams = (params) => {
	const names = /* @__PURE__ */ new Set();
	for (const param of params) if (isNodeOfType(param, "Identifier")) names.add(param.name);
	return names;
};
const serverHoistStaticIo = defineRule({
	id: "server-hoist-static-io",
	title: "Static file read on every request",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Move the read to module scope so it runs once at load: `const FONT_DATA = await fetch(new URL('./fonts/Inter.ttf', import.meta.url)).then(r => r.arrayBuffer())`.",
	create: (context) => ({
		ExportNamedDeclaration(node) {
			const declaration = node.declaration;
			if (!isNodeOfType(declaration, "FunctionDeclaration")) return;
			const handlerName = declaration.id?.name;
			if (!handlerName || !ROUTE_HANDLER_HTTP_METHODS.has(handlerName)) return;
			if (!isNodeOfType(declaration.body, "BlockStatement")) return;
			inspectHandlerBody(context, declaration.body, `${handlerName} route handler`, collectIdentifierParams(declaration.params ?? []));
		},
		ExportDefaultDeclaration(node) {
			const filename = normalizeFilename$1(context.filename ?? "");
			if (!PAGES_ROUTER_API_PATH_PATTERN.test(filename)) return;
			const declaration = node.declaration;
			if (!declaration || !isNodeOfType(declaration, "FunctionDeclaration") && !isNodeOfType(declaration, "FunctionExpression") && !isNodeOfType(declaration, "ArrowFunctionExpression")) return;
			if (!declaration.async) return;
			const body = declaration.body;
			if (!isNodeOfType(body, "BlockStatement")) return;
			inspectHandlerBody(context, body, "pages/api handler", collectIdentifierParams(declaration.params ?? []));
		}
	})
});
//#endregion
//#region src/plugin/rules/server/server-no-mutable-module-state.ts
const MUTABLE_CONTAINER_CONSTRUCTORS = new Set([
	"Map",
	"Set",
	"WeakMap",
	"WeakSet"
]);
const isMutableConstInitializer = (init) => {
	if (!init) return null;
	if (isNodeOfType(init, "ArrayExpression")) return "[]";
	if (isNodeOfType(init, "ObjectExpression")) return "{}";
	if (isNodeOfType(init, "NewExpression") && isNodeOfType(init.callee, "Identifier") && MUTABLE_CONTAINER_CONSTRUCTORS.has(init.callee.name)) return `new ${init.callee.name}()`;
	return null;
};
const serverNoMutableModuleState = defineRule({
	id: "server-no-mutable-module-state",
	title: "Mutable module state on the server",
	severity: "error",
	recommendation: "Keep per-request data inside the action, or in headers, cookies, or `React.cache`. Module-scope `let`/`var` is shared by every request.",
	create: (context) => {
		let fileHasUseServerDirective = false;
		return {
			Program(programNode) {
				fileHasUseServerDirective = hasDirective(programNode, "use server");
			},
			VariableDeclaration(node) {
				if (!fileHasUseServerDirective) return;
				if (!isNodeOfType(node.parent, "Program")) return;
				for (const declarator of node.declarations ?? []) {
					const variableName = isNodeOfType(declarator.id, "Identifier") ? declarator.id.name : "<unnamed>";
					if (node.kind === "let" || node.kind === "var") {
						context.report({
							node: declarator,
							message: `Module-scoped ${node.kind} "${variableName}" leaks state between your users, since every request shares it.`
						});
						continue;
					}
					const containerKind = isMutableConstInitializer(declarator.init);
					if (containerKind) context.report({
						node: declarator,
						message: `Module-scoped const "${variableName} = ${containerKind}" leaks state between your users, since every request shares it.`
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/server/server-sequential-independent-await.ts
const collectDeclaredNames = (declaration) => {
	const names = /* @__PURE__ */ new Set();
	if (!isNodeOfType(declaration, "VariableDeclaration")) return names;
	for (const declarator of declaration.declarations ?? []) if (isNodeOfType(declarator.id, "Identifier")) names.add(declarator.id.name);
	else if (isNodeOfType(declarator.id, "ObjectPattern")) {
		for (const property of declarator.id.properties ?? []) if (isNodeOfType(property, "Property") && isNodeOfType(property.value, "Identifier")) names.add(property.value.name);
		else if (isNodeOfType(property, "RestElement") && isNodeOfType(property.argument, "Identifier")) names.add(property.argument.name);
	} else if (isNodeOfType(declarator.id, "ArrayPattern")) {
		for (const element of declarator.id.elements ?? []) if (isNodeOfType(element, "Identifier")) names.add(element.name);
	}
	return names;
};
const declarationStartsWithAwait = (declaration) => {
	if (!isNodeOfType(declaration, "VariableDeclaration")) return false;
	for (const declarator of declaration.declarations ?? []) if (isNodeOfType(declarator.init, "AwaitExpression")) return true;
	return false;
};
const declarationReadsAnyName = (declaration, names) => {
	if (names.size === 0) return false;
	let didRead = false;
	walkAst(declaration, (child) => {
		if (didRead) return;
		if (isNodeOfType(child, "Identifier") && names.has(child.name)) didRead = true;
	});
	return didRead;
};
const serverSequentialIndependentAwait = defineRule({
	id: "server-sequential-independent-await",
	title: "Sequential independent awaits",
	severity: "warn",
	tags: ["test-noise"],
	recommendation: "These two awaits don't depend on each other. Wrap them in `Promise.all([...])` so they run at the same time.",
	create: (context) => {
		const inspectStatements = (statements) => {
			for (let statementIndex = 0; statementIndex < statements.length - 1; statementIndex++) {
				const currentStatement = statements[statementIndex];
				if (!isNodeOfType(currentStatement, "VariableDeclaration")) continue;
				if (!declarationStartsWithAwait(currentStatement)) continue;
				const declaredNames = collectDeclaredNames(currentStatement);
				const nextStatement = statements[statementIndex + 1];
				if (!isNodeOfType(nextStatement, "VariableDeclaration")) continue;
				if (!declarationStartsWithAwait(nextStatement)) continue;
				if (declarationReadsAnyName(nextStatement, declaredNames)) continue;
				context.report({
					node: nextStatement,
					message: "This await doesn't use the previous result, so your users wait twice as long for nothing."
				});
				statementIndex++;
			}
		};
		const visitFunctionBody = (node) => {
			if (!isNodeOfType(node, "FunctionDeclaration") && !isNodeOfType(node, "FunctionExpression") && !isNodeOfType(node, "ArrowFunctionExpression")) return;
			if (!node.async) return;
			if (!isNodeOfType(node.body, "BlockStatement")) return;
			inspectStatements(node.body.body ?? []);
		};
		return {
			FunctionDeclaration: visitFunctionBody,
			FunctionExpression: visitFunctionBody,
			ArrowFunctionExpression: visitFunctionBody
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/state-in-constructor.ts
const ALWAYS_MESSAGE = "This component's state is set up inconsistently.";
const NEVER_MESSAGE = "This component's state is set up inconsistently.";
const resolveSettings$1 = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { mode: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.stateInConstructor ?? {} : {}).mode ?? "always" };
};
const isStateKey = (key) => {
	if (isNodeOfType(key, "Identifier")) return key.name === "state";
	if (isNodeOfType(key, "Literal") && typeof key.value === "string") return key.value === "state";
	return false;
};
const findEnclosingClass = (node) => {
	let ancestor = node.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "ClassDeclaration") || isNodeOfType(ancestor, "ClassExpression")) return ancestor;
		ancestor = ancestor.parent ?? null;
	}
	return null;
};
const isInConstructor = (node) => {
	let ancestor = node.parent;
	while (ancestor) {
		if (isNodeOfType(ancestor, "MethodDefinition") && "kind" in ancestor && ancestor.kind === "constructor") return true;
		if (isNodeOfType(ancestor, "ClassDeclaration") || isNodeOfType(ancestor, "ClassExpression")) return false;
		ancestor = ancestor.parent ?? null;
	}
	return false;
};
const stateInConstructor = defineRule({
	id: "state-in-constructor",
	title: "State initialized in constructor",
	severity: "warn",
	defaultEnabled: false,
	recommendation: "Pick one way to set up state in class components and stick with it.",
	category: "Architecture",
	create: (context) => {
		const { mode } = resolveSettings$1(context.settings);
		return {
			PropertyDefinition(node) {
				if (mode !== "always") return;
				if (node.static) return;
				if (!isStateKey(node.key)) return;
				const enclosingClass = findEnclosingClass(node);
				if (!enclosingClass || !isEs6Component(enclosingClass)) return;
				context.report({
					node: node.key,
					message: ALWAYS_MESSAGE
				});
			},
			AssignmentExpression(node) {
				if (mode !== "never") return;
				const target = node.left;
				if (!isNodeOfType(target, "MemberExpression")) return;
				if (!isNodeOfType(target.object, "ThisExpression")) return;
				if (!isStateKey(target.property)) return;
				const enclosingClass = findEnclosingClass(node);
				if (!enclosingClass || !isEs6Component(enclosingClass)) return;
				if (!isInConstructor(node)) return;
				context.report({
					node: target,
					message: NEVER_MESSAGE
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/react-builtins/style-prop-object.ts
const MESSAGE$1 = "Your styles don't render because you passed the `style` prop a string instead of an object.";
const resolveSettings = (settings) => {
	const reactDoctor = settings?.["react-doctor"];
	return { allow: (typeof reactDoctor === "object" && reactDoctor !== null ? reactDoctor.stylePropObject ?? {} : {}).allow ?? [] };
};
const isStaticallyInvalidStyleExpression = (expression) => {
	const stripped = stripParenExpression(expression);
	if (isNodeOfType(stripped, "ObjectExpression")) return false;
	if (isNodeOfType(stripped, "Literal")) {
		if (stripped.value === null) return false;
		if (typeof stripped.value === "object") return false;
		return true;
	}
	if (isNodeOfType(stripped, "TemplateLiteral")) return true;
	if (isNodeOfType(stripped, "Identifier") && stripped.name === "undefined") return false;
	return false;
};
const resolveIdentifierToInitializer = (expression) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "Identifier")) return null;
	const binding = findVariableInitializer(stripped, stripped.name);
	if (!binding || !binding.initializer) return null;
	return binding.initializer;
};
const classifyTsType = (typeNode) => {
	if (typeNode.type === "TSStringKeyword" || typeNode.type === "TSNumberKeyword" || typeNode.type === "TSBooleanKeyword" || typeNode.type === "TSBigIntKeyword" || typeNode.type === "TSSymbolKeyword") return "primitive";
	if (typeNode.type === "TSObjectKeyword" || typeNode.type === "TSTypeLiteral" || typeNode.type === "TSTypeReference" || typeNode.type === "TSArrayType" || typeNode.type === "TSTupleType" || typeNode.type === "TSFunctionType") return "object";
	if (typeNode.type === "TSNullKeyword" || typeNode.type === "TSUndefinedKeyword") return "object";
	if (typeNode.type === "TSUnionType") {
		let anyPrimitive = false;
		for (const member of typeNode.types) {
			if (member.type === "TSUndefinedKeyword" || member.type === "TSNullKeyword" || member.type === "TSNeverKeyword") continue;
			const classified = classifyTsType(member);
			if (classified === "unknown") return "unknown";
			if (classified === "primitive") anyPrimitive = true;
			if (classified === "object") return "object";
		}
		return anyPrimitive ? "primitive" : "unknown";
	}
	return "unknown";
};
const findBindingTypeAnnotation = (expression) => {
	const stripped = stripParenExpression(expression);
	if (!isNodeOfType(stripped, "Identifier")) return null;
	const binding = findVariableInitializer(stripped, stripped.name);
	if (!binding) return null;
	const typeAnnotationParent = binding.bindingIdentifier.typeAnnotation;
	if (!typeAnnotationParent || typeof typeAnnotationParent !== "object") return null;
	return typeAnnotationParent.typeAnnotation ?? null;
};
const isInvalidStyleExpression = (expression) => {
	if (isStaticallyInvalidStyleExpression(expression)) return true;
	const initializer = resolveIdentifierToInitializer(expression);
	if (initializer && isStaticallyInvalidStyleExpression(initializer)) return true;
	if (!initializer) {
		const typeAnnotation = findBindingTypeAnnotation(expression);
		if (typeAnnotation && classifyTsType(typeAnnotation) === "primitive") return true;
	}
	return false;
};
const getJsxOpeningElementName = (node) => {
	if (isNodeOfType(node.name, "JSXIdentifier")) return node.name.name;
	return null;
};
const stylePropObject = defineRule({
	id: "style-prop-object",
	title: "Style prop is not an object",
	severity: "warn",
	recommendation: "Pass the `style` prop as `{{ color: 'red' }}` (object literal), not a string.",
	category: "Correctness",
	create: (context) => {
		const { allow } = resolveSettings(context.settings);
		const allowSet = new Set(allow);
		return {
			JSXOpeningElement(node) {
				const elementName = getJsxOpeningElementName(node);
				if (elementName && allowSet.has(elementName)) return;
				if (elementName) {
					const firstCharCode = elementName.charCodeAt(0);
					if (!(firstCharCode >= 97 && firstCharCode <= 122)) return;
				}
				for (const attribute of node.attributes) {
					if (!isNodeOfType(attribute, "JSXAttribute")) continue;
					if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
					if (attribute.name.name !== "style") continue;
					const value = attribute.value;
					if (!value) continue;
					if (isNodeOfType(value, "Literal") && typeof value.value === "string") {
						context.report({
							node: attribute,
							message: MESSAGE$1
						});
						return;
					}
					if (isNodeOfType(value, "JSXExpressionContainer")) {
						const innerExpression = value.expression;
						if (innerExpression && innerExpression.type !== "JSXEmptyExpression" && isInvalidStyleExpression(innerExpression)) context.report({
							node: attribute,
							message: MESSAGE$1
						});
					}
				}
			},
			CallExpression(node) {
				if (!isCreateElementCall(node)) return;
				const firstArgument = node.arguments[0];
				if (!firstArgument) return;
				let elementName = null;
				if (isNodeOfType(firstArgument, "Literal") && typeof firstArgument.value === "string") elementName = firstArgument.value;
				else if (isNodeOfType(firstArgument, "Identifier")) elementName = firstArgument.name;
				if (elementName && allowSet.has(elementName)) return;
				const propsArgument = node.arguments[1];
				if (!propsArgument || !isNodeOfType(propsArgument, "ObjectExpression")) return;
				for (const property of propsArgument.properties) {
					if (!isNodeOfType(property, "Property")) continue;
					if (property.computed) continue;
					const key = property.key;
					if (!(isNodeOfType(key, "Identifier") && key.name === "style" || isNodeOfType(key, "Literal") && key.value === "style")) continue;
					if (isInvalidStyleExpression(property.value)) context.report({
						node: property,
						message: MESSAGE$1
					});
				}
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/a11y/tabindex-no-positive.ts
const MESSAGE = "Keyboard users get jumped out of the normal order by a positive `tabIndex`, so use `0` or `-1`.";
const tabindexNoPositive = defineRule({
	id: "tabindex-no-positive",
	title: "Positive tabindex value",
	tags: ["react-jsx-only"],
	severity: "warn",
	recommendation: "Use `tabIndex={0}` (focusable in source order) or `tabIndex={-1}` (focus only in code).",
	category: "Accessibility",
	create: (context) => ({ JSXOpeningElement(node) {
		const tabIndex = hasJsxPropIgnoreCase(node.attributes, "tabIndex");
		if (!tabIndex) return;
		const stringValue = getJsxPropStringValue(tabIndex);
		let numericValue = null;
		if (stringValue !== null) {
			const parsed = Number(stringValue);
			if (Number.isFinite(parsed)) numericValue = parsed;
		} else if (tabIndex.value && isNodeOfType(tabIndex.value, "JSXExpressionContainer")) numericValue = parseJsxValue(tabIndex.value);
		if (numericValue !== null && numericValue > 0) context.report({
			node: tabIndex,
			message: MESSAGE
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/utils/walk-server-fn-chain.ts
const walkServerFnChain = (outerNode) => {
	const result = {
		isServerFnChain: false,
		specifiedMethod: null,
		hasInputValidator: false
	};
	if (!isNodeOfType(outerNode, "CallExpression")) return result;
	if (!isNodeOfType(outerNode.callee, "MemberExpression")) return result;
	let currentNode = outerNode.callee.object;
	while (isNodeOfType(currentNode, "CallExpression")) {
		const calleeName = getCalleeName$1(currentNode);
		if (calleeName && TANSTACK_SERVER_FN_NAMES.has(calleeName)) {
			result.isServerFnChain = true;
			const optionsArgument = currentNode.arguments?.[0];
			if (isNodeOfType(optionsArgument, "ObjectExpression")) {
				for (const property of optionsArgument.properties ?? []) if (isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "method" && isNodeOfType(property.value, "Literal") && typeof property.value.value === "string") result.specifiedMethod = property.value.value;
			}
		}
		if (calleeName === "inputValidator") result.hasInputValidator = true;
		if (isNodeOfType(currentNode.callee, "MemberExpression")) currentNode = currentNode.callee.object;
		else break;
	}
	return result;
};
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-get-mutation.ts
const tanstackStartGetMutation = defineRule({
	id: "tanstack-start-get-mutation",
	title: "Mutation in GET server function",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	category: "Security",
	recommendation: "Use `createServerFn({ method: 'POST' })` for data changes. GET requests can be prefetched and are open to CSRF.",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		if (!isNodeOfType(node.callee.property, "Identifier") || node.callee.property.name !== "handler") return;
		const chainInfo = walkServerFnChain(node);
		if (!chainInfo.isServerFnChain) return;
		if (chainInfo.specifiedMethod && MUTATING_HTTP_METHODS.has(chainInfo.specifiedMethod.toUpperCase())) return;
		const handlerFunction = node.arguments?.[0];
		if (!handlerFunction) return;
		if (!isNodeOfType(handlerFunction, "ArrowFunctionExpression") && !isNodeOfType(handlerFunction, "FunctionExpression")) return;
		const handlerBody = handlerFunction.body;
		if (!handlerBody) return;
		const sideEffect = findSideEffect(handlerBody, {
			locallyScopedSafeBindings: collectLocallyScopedSafeBindings(handlerBody),
			locallyScopedCookieBindings: collectLocallyScopedCookieBindings(handlerBody)
		});
		if (sideEffect) context.report({
			node,
			message: `This GET server function's side effect (${sideEffect}) is vulnerable to CSRF attacks.`
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/utils/get-route-options-object.ts
const getRouteOptionsObject = (node) => {
	if (!isNodeOfType(node, "CallExpression")) return null;
	const callee = node.callee;
	if (isNodeOfType(callee, "CallExpression") && isNodeOfType(callee.callee, "Identifier")) {
		if (!TANSTACK_ROUTE_CREATION_FUNCTIONS.has(callee.callee.name)) return null;
		const optionsArgument = node.arguments?.[0];
		if (isNodeOfType(optionsArgument, "ObjectExpression")) return optionsArgument;
		return null;
	}
	if (isNodeOfType(callee, "Identifier")) {
		if (!TANSTACK_ROUTE_CREATION_FUNCTIONS.has(callee.name)) return null;
		const optionsArgument = node.arguments?.[0];
		if (isNodeOfType(optionsArgument, "ObjectExpression")) return optionsArgument;
		return null;
	}
	return null;
};
//#endregion
//#region src/plugin/rules/tanstack-start/utils/get-property-key-name.ts
const getPropertyKeyName = (property) => {
	if (!isNodeOfType(property, "Property") && !isNodeOfType(property, "MethodDefinition")) return null;
	if (isNodeOfType(property.key, "Identifier")) return property.key.name;
	if (isNodeOfType(property.key, "Literal")) return String(property.key.value);
	return null;
};
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-loader-parallel-fetch.ts
const hasTopLevelAwait = (statement) => {
	if (isNodeOfType(statement, "VariableDeclaration")) return statement.declarations?.some((declarator) => isNodeOfType(declarator.init, "AwaitExpression"));
	if (isNodeOfType(statement, "ExpressionStatement")) return isNodeOfType(statement.expression, "AwaitExpression") || isNodeOfType(statement.expression, "AssignmentExpression") && isNodeOfType(statement.expression.right, "AwaitExpression");
	if (isNodeOfType(statement, "ReturnStatement")) return isNodeOfType(statement.argument, "AwaitExpression");
	if (isNodeOfType(statement, "ForOfStatement") && statement.await) return true;
	return false;
};
const tanstackStartLoaderParallelFetch = defineRule({
	id: "tanstack-start-loader-parallel-fetch",
	title: "Sequential awaits in loader",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	category: "Performance",
	recommendation: "Use `const [a, b] = await Promise.all([fetchA(), fetchB()])` to avoid request waterfalls in route loaders",
	create: (context) => ({ CallExpression(node) {
		const optionsObject = getRouteOptionsObject(node);
		if (!optionsObject) return;
		const properties = optionsObject.properties ?? [];
		for (const property of properties) {
			if (getPropertyKeyName(property) !== "loader") continue;
			if (!isNodeOfType(property, "Property")) continue;
			const loaderValue = property.value;
			if (!loaderValue || !isNodeOfType(loaderValue, "ArrowFunctionExpression") && !isNodeOfType(loaderValue, "FunctionExpression")) continue;
			const functionBody = loaderValue.body;
			if (!functionBody || !isNodeOfType(functionBody, "BlockStatement")) continue;
			let sequentialAwaitCount = 0;
			for (const statement of functionBody.body ?? []) {
				if (hasTopLevelAwait(statement)) sequentialAwaitCount++;
				if (sequentialAwaitCount >= 2) {
					context.report({
						node: property,
						message: "Sequential awaits in this loader create a request waterfall that slows the route."
					});
					break;
				}
			}
		}
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-missing-head-content.ts
const TANSTACK_ROUTER_PACKAGE = "@tanstack/react-router";
const HEAD_CONTENT_COMPONENT_NAME = "HeadContent";
const DOCUMENT_HEAD_ELEMENT_NAME = "head";
const getJsxMemberRootName = (node) => {
	if (isNodeOfType(node.object, "JSXIdentifier")) return node.object.name;
	if (isNodeOfType(node.object, "JSXMemberExpression")) return getJsxMemberRootName(node.object);
	return null;
};
const getJsxMemberPropertyName = (node) => {
	if (isNodeOfType(node.property, "JSXIdentifier")) return node.property.name;
	return null;
};
const getMemberRootName = (node) => {
	if (isNodeOfType(node.object, "Identifier")) return node.object.name;
	if (isNodeOfType(node.object, "MemberExpression")) return getMemberRootName(node.object);
	return null;
};
const getMemberPropertyName = (node) => {
	if (isNodeOfType(node.property, "Identifier")) return node.property.name;
	return null;
};
const isDocumentHeadElement = (node) => isNodeOfType(node, "JSXElement") && isNodeOfType(node.openingElement.name, "JSXIdentifier") && node.openingElement.name.name === DOCUMENT_HEAD_ELEMENT_NAME;
const isInsideDocumentHeadElement = (node) => {
	let currentNode = node.parent;
	while (currentNode) {
		if (isDocumentHeadElement(currentNode)) return true;
		currentNode = currentNode.parent;
	}
	return false;
};
const isCustomJsxElementName = (node) => {
	if (isNodeOfType(node, "JSXIdentifier")) {
		const firstCharacter = node.name.charAt(0);
		return firstCharacter.toUpperCase() === firstCharacter && firstCharacter.toLowerCase() !== firstCharacter;
	}
	if (!isNodeOfType(node, "JSXMemberExpression")) return false;
	const rootName = getJsxMemberRootName(node);
	if (!rootName) return false;
	const firstCharacter = rootName.charAt(0);
	return firstCharacter.toUpperCase() === firstCharacter && firstCharacter.toLowerCase() !== firstCharacter;
};
const tanstackStartMissingHeadContent = defineRule({
	id: "tanstack-start-missing-head-content",
	title: "Root route missing HeadContent",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	recommendation: "Add `<HeadContent />` inside `<head>` in your __root route. Without it, route `head()` meta tags are dropped.",
	create: (context) => {
		let hasHeadContentElement = false;
		let hasDocumentHeadElement = false;
		let hasCustomHeadChildElement = false;
		const headContentComponentNames = new Set([HEAD_CONTENT_COMPONENT_NAME]);
		const tanstackRouterNamespaceNames = /* @__PURE__ */ new Set();
		const collectImportBindings = (node) => {
			if (!isNodeOfType(node, "ImportDeclaration")) return;
			const isTanstackRouterImport = node.source.value === TANSTACK_ROUTER_PACKAGE;
			const specifiers = node.specifiers ?? [];
			for (const specifier of specifiers) {
				if (isTanstackRouterImport && isNodeOfType(specifier, "ImportNamespaceSpecifier")) {
					tanstackRouterNamespaceNames.add(specifier.local.name);
					continue;
				}
				if (!isNodeOfType(specifier, "ImportSpecifier")) continue;
				if (!isNodeOfType(specifier.imported, "Identifier") || specifier.imported.name !== HEAD_CONTENT_COMPONENT_NAME) continue;
				headContentComponentNames.add(specifier.local.name);
			}
		};
		const collectVariableAlias = (node) => {
			if (!isNodeOfType(node, "VariableDeclarator")) return;
			if (!isNodeOfType(node.id, "Identifier")) return;
			const initializer = node.init;
			if (!initializer) return;
			if (isNodeOfType(initializer, "Identifier")) {
				if (headContentComponentNames.has(initializer.name)) headContentComponentNames.add(node.id.name);
				if (tanstackRouterNamespaceNames.has(initializer.name)) tanstackRouterNamespaceNames.add(node.id.name);
				return;
			}
			if (!isNodeOfType(initializer, "MemberExpression")) return;
			const rootName = getMemberRootName(initializer);
			const propertyName = getMemberPropertyName(initializer);
			if (rootName && tanstackRouterNamespaceNames.has(rootName) && propertyName === HEAD_CONTENT_COMPONENT_NAME) headContentComponentNames.add(node.id.name);
		};
		return {
			Program(node) {
				const filename = context.filename ?? "";
				if (!TANSTACK_ROOT_ROUTE_FILE_PATTERN.test(filename)) return;
				const statements = node.body ?? [];
				for (const statement of statements) collectImportBindings(statement);
				for (const statement of statements) {
					if (!isNodeOfType(statement, "VariableDeclaration")) continue;
					for (const declaration of statement.declarations ?? []) collectVariableAlias(declaration);
				}
			},
			ImportDeclaration(node) {
				const filename = context.filename ?? "";
				if (!TANSTACK_ROOT_ROUTE_FILE_PATTERN.test(filename)) return;
				collectImportBindings(node);
			},
			VariableDeclarator(node) {
				const filename = context.filename ?? "";
				if (!TANSTACK_ROOT_ROUTE_FILE_PATTERN.test(filename)) return;
				collectVariableAlias(node);
			},
			JSXOpeningElement(node) {
				const filename = normalizeFilename$1(context.filename ?? "");
				if (!TANSTACK_ROOT_ROUTE_FILE_PATTERN.test(filename)) return;
				if (isNodeOfType(node.name, "JSXIdentifier")) {
					if (node.name.name === DOCUMENT_HEAD_ELEMENT_NAME) hasDocumentHeadElement = true;
					if (headContentComponentNames.has(node.name.name)) hasHeadContentElement = true;
					if (isInsideDocumentHeadElement(node) && isCustomJsxElementName(node.name)) hasCustomHeadChildElement = true;
					return;
				}
				if (!isNodeOfType(node.name, "JSXMemberExpression")) return;
				const rootName = getJsxMemberRootName(node.name);
				const propertyName = getJsxMemberPropertyName(node.name);
				if (rootName && tanstackRouterNamespaceNames.has(rootName) && propertyName === HEAD_CONTENT_COMPONENT_NAME) hasHeadContentElement = true;
				if (isInsideDocumentHeadElement(node) && isCustomJsxElementName(node.name)) hasCustomHeadChildElement = true;
			},
			"Program:exit"(programNode) {
				const filename = normalizeFilename$1(context.filename ?? "");
				if (!TANSTACK_ROOT_ROUTE_FILE_PATTERN.test(filename)) return;
				if (hasDocumentHeadElement && !hasHeadContentElement && !hasCustomHeadChildElement) context.report({
					node: programNode,
					message: "Without <HeadContent /> in the __root route, your route head() meta tags never render."
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-no-anchor-element.ts
const tanstackStartNoAnchorElement = defineRule({
	id: "tanstack-start-no-anchor-element",
	title: "Plain anchor for internal navigation",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	recommendation: "`import { Link } from '@tanstack/react-router'` for type-safe routes, preloading via `preload=\"intent\"`, and client-side navigation",
	create: (context) => ({ JSXOpeningElement(node) {
		const filename = normalizeFilename$1(context.filename ?? "");
		if (!TANSTACK_ROUTE_FILE_PATTERN.test(filename)) return;
		if (!isNodeOfType(node.name, "JSXIdentifier") || node.name.name !== "a") return;
		const hrefAttribute = (node.attributes ?? []).find((attribute) => isNodeOfType(attribute, "JSXAttribute") && isNodeOfType(attribute.name, "JSXIdentifier") && attribute.name.name === "href");
		if (!hrefAttribute || !isNodeOfType(hrefAttribute, "JSXAttribute")) return;
		if (!hrefAttribute.value) return;
		let hrefValue = null;
		if (isNodeOfType(hrefAttribute.value, "Literal")) hrefValue = hrefAttribute.value.value;
		else if (isNodeOfType(hrefAttribute.value, "JSXExpressionContainer") && isNodeOfType(hrefAttribute.value.expression, "Literal")) hrefValue = hrefAttribute.value.expression.value;
		if (typeof hrefValue === "string" && hrefValue.startsWith("/")) context.report({
			node,
			message: "Plain <a> reloads the whole page on internal navigation."
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-no-direct-fetch-in-loader.ts
const tanstackStartNoDirectFetchInLoader = defineRule({
	id: "tanstack-start-no-direct-fetch-in-loader",
	title: "Direct fetch in route loader",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	recommendation: "Use `createServerFn()` from @tanstack/react-start for type-safe RPC, input validation, and proper server/client code splitting",
	create: (context) => ({ CallExpression(node) {
		const optionsObject = getRouteOptionsObject(node);
		if (!optionsObject) return;
		const properties = optionsObject.properties ?? [];
		for (const property of properties) {
			if (getPropertyKeyName(property) !== "loader") continue;
			walkAst(isNodeOfType(property, "Property") ? property.value : property, (child) => {
				if (!isNodeOfType(child, "CallExpression")) return;
				if (isNodeOfType(child.callee, "Identifier") && child.callee.name === "fetch") context.report({
					node: child,
					message: "Direct fetch() in a route loader runs on the client too with no type safety."
				});
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-no-dynamic-server-fn-import.ts
const tanstackStartNoDynamicServerFnImport = defineRule({
	id: "tanstack-start-no-dynamic-server-fn-import",
	title: "Dynamic server function import",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "error",
	recommendation: "Use `import { myFn } from '~/utils/my.functions'`. The bundler only swaps server code for RPC stubs on static imports.",
	create: (context) => ({ ImportExpression(node) {
		const source = node.source;
		if (!source) return;
		let importPath = null;
		if (isNodeOfType(source, "Literal") && typeof source.value === "string") importPath = source.value;
		else if (isNodeOfType(source, "TemplateLiteral") && source.quasis?.length === 1) importPath = source.quasis[0].value?.raw ?? null;
		if (importPath && TANSTACK_SERVER_FN_FILE_PATTERN.test(importPath)) context.report({
			node,
			message: "Dynamically importing a server-functions file leaks server code into the client bundle."
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-no-navigate-in-render.ts
const tanstackStartNoNavigateInRender = defineRule({
	id: "tanstack-start-no-navigate-in-render",
	title: "navigate() called during render",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	recommendation: "Use `throw redirect({ to: '/path' })` in `beforeLoad` or `loader`. navigate() during render causes hydration issues.",
	create: (context) => {
		let deferredCallbackDepth = 0;
		let eventHandlerDepth = 0;
		const isDeferredHookCall = (node) => isHookCall$1(node, EFFECT_HOOK_NAMES$1) || isHookCall$1(node, "useCallback") || isHookCall$1(node, "useMemo");
		const isEventHandlerAttribute = (node) => isNodeOfType(node, "JSXAttribute") && isNodeOfType(node.name, "JSXIdentifier") && typeof node.name.name === "string" && node.name.name.startsWith("on") && UPPERCASE_PATTERN.test(node.name.name.charAt(2));
		return {
			CallExpression(node) {
				const filename = normalizeFilename$1(context.filename ?? "");
				if (!TANSTACK_ROUTE_FILE_PATTERN.test(filename)) return;
				if (isDeferredHookCall(node)) deferredCallbackDepth++;
				if (deferredCallbackDepth > 0 || eventHandlerDepth > 0) return;
				if (isNodeOfType(node.callee, "Identifier") && node.callee.name === "navigate" && (node.arguments?.length ?? 0) > 0) context.report({
					node,
					message: "navigate() during render causes hydration errors."
				});
			},
			"CallExpression:exit"(node) {
				const filename = normalizeFilename$1(context.filename ?? "");
				if (!TANSTACK_ROUTE_FILE_PATTERN.test(filename)) return;
				if (isDeferredHookCall(node)) deferredCallbackDepth = Math.max(0, deferredCallbackDepth - 1);
			},
			JSXAttribute(node) {
				const filename = normalizeFilename$1(context.filename ?? "");
				if (!TANSTACK_ROUTE_FILE_PATTERN.test(filename)) return;
				if (isEventHandlerAttribute(node)) eventHandlerDepth++;
			},
			"JSXAttribute:exit"(node) {
				const filename = normalizeFilename$1(context.filename ?? "");
				if (!TANSTACK_ROUTE_FILE_PATTERN.test(filename)) return;
				if (isEventHandlerAttribute(node)) eventHandlerDepth = Math.max(0, eventHandlerDepth - 1);
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-no-secrets-in-loader.ts
const SAFE_BUILD_ENV_VARS = new Set([
	"NODE_ENV",
	"MODE",
	"DEV",
	"PROD"
]);
const SECRET_KEYWORD_PATTERN = /(?:secret|token|api[_]?key|password|private)/i;
const isLikelySecret = (envVarName) => {
	if (SAFE_BUILD_ENV_VARS.has(envVarName)) return false;
	return SECRET_KEYWORD_PATTERN.test(envVarName);
};
const tanstackStartNoSecretsInLoader = defineRule({
	id: "tanstack-start-no-secrets-in-loader",
	title: "Secret exposed in a loader",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "error",
	category: "Security",
	recommendation: "Loaders are isomorphic (run on both server and client). Wrap secret access in `createServerFn()` so it stays server-only",
	create: (context) => ({ CallExpression(node) {
		const optionsObject = getRouteOptionsObject(node);
		if (!optionsObject) return;
		const properties = optionsObject.properties ?? [];
		for (const property of properties) {
			const keyName = getPropertyKeyName(property);
			if (keyName !== "loader" && keyName !== "beforeLoad") continue;
			walkAst(isNodeOfType(property, "Property") ? property.value : property, (child) => {
				if (!isNodeOfType(child, "MemberExpression")) return;
				const isProcessEnvAccess = isNodeOfType(child.object, "MemberExpression") && isNodeOfType(child.object.object, "Identifier") && child.object.object.name === "process" && isNodeOfType(child.object.property, "Identifier") && child.object.property.name === "env";
				const isImportMetaEnvAccess = isNodeOfType(child.object, "MemberExpression") && isNodeOfType(child.object.object, "MetaProperty") && isNodeOfType(child.object.property, "Identifier") && child.object.property.name === "env";
				if (!isProcessEnvAccess && !isImportMetaEnvAccess) return;
				const envVarName = isNodeOfType(child.property, "Identifier") ? child.property.name : null;
				if (envVarName && isLikelySecret(envVarName)) {
					const envSource = isImportMetaEnvAccess ? "import.meta.env" : "process.env";
					context.report({
						node: child,
						message: `Reading ${envSource}.${envVarName} in ${keyName} leaks this secret to the client, where anyone can read it.`
					});
				}
			});
		}
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-no-use-server-in-handler.ts
const tanstackStartNoUseServerInHandler = defineRule({
	id: "tanstack-start-no-use-server-in-handler",
	title: "Redundant use server in handler",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "error",
	recommendation: "TanStack Start handles server boundaries automatically via the Vite plugin. \"use server\" inside createServerFn causes compile errors.",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		if (!isNodeOfType(node.callee.property, "Identifier") || node.callee.property.name !== "handler") return;
		const handlerFunction = node.arguments?.[0];
		if (!handlerFunction || !isNodeOfType(handlerFunction, "ArrowFunctionExpression") && !isNodeOfType(handlerFunction, "FunctionExpression")) return;
		const body = handlerFunction.body;
		if (!isNodeOfType(body, "BlockStatement")) return;
		if (body.body?.some((statement) => isNodeOfType(statement, "ExpressionStatement") && (statement.directive === "use server" || isNodeOfType(statement.expression, "Literal") && statement.expression.value === "use server"))) context.report({
			node: handlerFunction,
			message: "\"use server\" inside a createServerFn handler causes compile errors."
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-no-use-effect-fetch.ts
const tanstackStartNoUseEffectFetch = defineRule({
	id: "tanstack-start-no-useeffect-fetch",
	title: "fetch inside useEffect in route",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	recommendation: "Fetch data in the route `loader` instead. The router loads it before rendering and avoids waterfalls.",
	create: (context) => ({ CallExpression(node) {
		const filename = normalizeFilename$1(context.filename ?? "");
		if (!TANSTACK_ROUTE_FILE_PATTERN.test(filename)) return;
		if (!isHookCall$1(node, EFFECT_HOOK_NAMES$1)) return;
		const callback = node.arguments?.[0];
		if (!callback) return;
		let hasFetchCall = false;
		walkAst(callback, (child) => {
			if (hasFetchCall) return;
			if (isNodeOfType(child, "CallExpression") && isNodeOfType(child.callee, "Identifier") && child.callee.name === "fetch") hasFetchCall = true;
		});
		if (hasFetchCall) context.report({
			node,
			message: "fetch() inside useEffect makes your users wait through a loading spinner after render."
		});
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-redirect-in-try-catch.ts
const tanstackStartRedirectInTryCatch = defineRule({
	id: "tanstack-start-redirect-in-try-catch",
	title: "redirect() inside try-catch",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	recommendation: "TanStack Router's `redirect()` and `notFound()` throw special errors caught by the router. Move them outside the try block or re-throw in the catch",
	create: (context) => {
		let tryBlockDepth = 0;
		let catchClauseDepth = 0;
		return {
			TryStatement() {
				tryBlockDepth++;
			},
			"TryStatement:exit"() {
				tryBlockDepth--;
			},
			CatchClause() {
				catchClauseDepth++;
			},
			"CatchClause:exit"() {
				catchClauseDepth--;
			},
			ThrowStatement(node) {
				if (tryBlockDepth === 0) return;
				if (catchClauseDepth > 0) return;
				const argument = node.argument;
				if (!isNodeOfType(argument, "CallExpression")) return;
				if (!isNodeOfType(argument.callee, "Identifier")) return;
				if (!TANSTACK_REDIRECT_FUNCTIONS.has(argument.callee.name)) return;
				context.report({
					node,
					message: `throw ${argument.callee.name}() inside a try block gets swallowed, so the redirect silently fails.`
				});
			}
		};
	}
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-route-property-order.ts
const tanstackStartRoutePropertyOrder = defineRule({
	id: "tanstack-start-route-property-order",
	title: "Wrong route property order",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "error",
	recommendation: "Follow the order: params/validateSearch → loaderDeps → context → beforeLoad → loader → head. See https://tanstack.com/router/latest/docs/eslint/create-route-property-order",
	create: (context) => ({ CallExpression(node) {
		const optionsObject = getRouteOptionsObject(node);
		if (!optionsObject) return;
		const properties = optionsObject.properties ?? [];
		const orderedPropertyNames = [];
		for (const property of properties) {
			const propertyName = getPropertyKeyName(property);
			if (propertyName !== null) orderedPropertyNames.push(propertyName);
		}
		const sensitiveProperties = orderedPropertyNames.filter((propertyName) => TANSTACK_ROUTE_PROPERTY_ORDER.includes(propertyName));
		let lastIndex = -1;
		for (const propertyName of sensitiveProperties) {
			const currentIndex = TANSTACK_ROUTE_PROPERTY_ORDER.indexOf(propertyName);
			if (currentIndex < lastIndex) {
				const expectedBefore = TANSTACK_ROUTE_PROPERTY_ORDER[lastIndex];
				context.report({
					node: optionsObject,
					message: `Ordering route property "${propertyName}" after "${expectedBefore}" breaks type inference.`
				});
				return;
			}
			lastIndex = currentIndex;
		}
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-server-fn-method-order.ts
const tanstackStartServerFnMethodOrder = defineRule({
	id: "tanstack-start-server-fn-method-order",
	title: "Wrong server function method order",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "error",
	recommendation: "Chain methods in order: .middleware() → .inputValidator() → .client() → .server() → .handler(). Types depend on this sequence.",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		const methodNames = [];
		let currentNode = node;
		while (isNodeOfType(currentNode, "CallExpression") && isNodeOfType(currentNode.callee, "MemberExpression")) {
			const methodName = isNodeOfType(currentNode.callee.property, "Identifier") ? currentNode.callee.property.name : null;
			if (methodName) methodNames.unshift(methodName);
			currentNode = currentNode.callee.object;
		}
		if (isNodeOfType(currentNode, "CallExpression") && isNodeOfType(currentNode.callee, "Identifier")) {
			if (!TANSTACK_SERVER_FN_NAMES.has(currentNode.callee.name)) return;
		} else return;
		const ownMethodName = isNodeOfType(node.callee.property, "Identifier") ? node.callee.property.name : null;
		if (methodNames[methodNames.length - 1] !== ownMethodName) return;
		const orderSensitiveMethods = methodNames.filter((name) => TANSTACK_MIDDLEWARE_METHOD_ORDER.includes(name));
		let lastIndex = -1;
		for (const methodName of orderSensitiveMethods) {
			const currentIndex = TANSTACK_MIDDLEWARE_METHOD_ORDER.indexOf(methodName);
			if (currentIndex < lastIndex) {
				const expectedBefore = TANSTACK_MIDDLEWARE_METHOD_ORDER[lastIndex];
				context.report({
					node,
					message: `Chaining .${methodName}() after .${expectedBefore}() breaks type inference.`
				});
				return;
			}
			lastIndex = currentIndex;
		}
	} })
});
//#endregion
//#region src/plugin/rules/tanstack-start/tanstack-start-server-fn-validate-input.ts
const tanstackStartServerFnValidateInput = defineRule({
	id: "tanstack-start-server-fn-validate-input",
	title: "Server function without input validation",
	tags: ["test-noise"],
	requires: ["tanstack-start"],
	severity: "warn",
	recommendation: "Add `.inputValidator(schema)` before `.handler()`. This data crosses the network and must be validated at runtime.",
	create: (context) => ({ CallExpression(node) {
		if (!isNodeOfType(node.callee, "MemberExpression")) return;
		if (!isNodeOfType(node.callee.property, "Identifier")) return;
		if (node.callee.property.name !== "handler") return;
		const chainInfo = walkServerFnChain(node);
		if (!chainInfo.isServerFnChain) return;
		const handlerFunction = node.arguments?.[0];
		if (!handlerFunction) return;
		let accessesData = false;
		walkAst(handlerFunction, (child) => {
			if (isNodeOfType(child, "MemberExpression") && isNodeOfType(child.property, "Identifier") && child.property.name === "data") accessesData = true;
			if (isNodeOfType(child, "ObjectPattern") && child.properties?.some((property) => isNodeOfType(property, "Property") && isNodeOfType(property.key, "Identifier") && property.key.name === "data")) accessesData = true;
		});
		if (accessesData && !chainInfo.hasInputValidator) context.report({
			node,
			message: "This server function reads network data with no inputValidator(), so anyone can send unvalidated input."
		});
	} })
});
//#endregion
//#region src/plugin/rules/bundle-size/use-lazy-motion.ts
const useLazyMotion = defineRule({
	id: "use-lazy-motion",
	title: "Full Framer Motion import",
	tags: ["test-noise"],
	severity: "warn",
	recommendation: "Use `import { LazyMotion, m } from \"framer-motion\"` with `domAnimation` features. Saves about 30kb.",
	create: (context) => ({ ImportDeclaration(node) {
		const source = node.source?.value;
		if (source !== "framer-motion" && source !== "motion/react") return;
		if (node.importKind === "type") return;
		if (node.specifiers?.some((specifier) => {
			if (!isNodeOfType(specifier, "ImportSpecifier")) return false;
			if (specifier.importKind === "type") return false;
			return getImportedName$1(specifier) === "motion";
		})) context.report({
			node,
			message: "Importing \"motion\" ships about 30 kb of extra code to your users & slows page load. Use \"m\" with LazyMotion instead."
		});
	} })
});
//#endregion
//#region src/plugin/rules/react-builtins/void-dom-elements-no-children.ts
const VOID_DOM_ELEMENTS = new Set([
	"area",
	"base",
	"br",
	"col",
	"embed",
	"hr",
	"img",
	"input",
	"keygen",
	"link",
	"menuitem",
	"meta",
	"param",
	"source",
	"track",
	"wbr"
]);
const buildMessage = (tag) => `React errors when \`<${tag}>\` has children because it's a void element.`;
const findChildrenLikePropName = (attributes) => {
	for (const attribute of attributes) {
		if (!isNodeOfType(attribute, "JSXAttribute")) continue;
		if (!isNodeOfType(attribute.name, "JSXIdentifier")) continue;
		if (attribute.name.name === "children" || attribute.name.name === "dangerouslySetInnerHTML") return true;
	}
	return false;
};
const voidDomElementsNoChildren = defineRule({
	id: "void-dom-elements-no-children",
	title: "Children on a void element",
	severity: "warn",
	recommendation: "Remove the children, or use a tag that can hold children.",
	create: (context) => ({
		JSXElement(node) {
			const openingElement = node.openingElement;
			if (!isNodeOfType(openingElement.name, "JSXIdentifier")) return;
			const tagName = openingElement.name.name;
			if (!VOID_DOM_ELEMENTS.has(tagName)) return;
			const hasChildrenContent = node.children.length > 0;
			const hasChildrenLikeProp = findChildrenLikePropName(openingElement.attributes);
			if (hasChildrenContent || hasChildrenLikeProp) context.report({
				node: openingElement.name,
				message: buildMessage(tagName)
			});
		},
		CallExpression(node) {
			if (!isCreateElementCall(node)) return;
			const firstArgument = node.arguments[0];
			if (!firstArgument) return;
			if (!isNodeOfType(firstArgument, "Literal") || typeof firstArgument.value !== "string") return;
			const tagName = firstArgument.value;
			if (!VOID_DOM_ELEMENTS.has(tagName)) return;
			const propsArgument = node.arguments[1];
			const childrenArguments = node.arguments.slice(2);
			let hasChildrenLikeProp = false;
			if (propsArgument && isNodeOfType(propsArgument, "ObjectExpression")) for (const property of propsArgument.properties) {
				if (!isNodeOfType(property, "Property")) continue;
				const propertyKey = property.key;
				if (isNodeOfType(propertyKey, "Identifier") && (propertyKey.name === "children" || propertyKey.name === "dangerouslySetInnerHTML") || isNodeOfType(propertyKey, "Literal") && (propertyKey.value === "children" || propertyKey.value === "dangerouslySetInnerHTML")) {
					hasChildrenLikeProp = true;
					break;
				}
			}
			if (childrenArguments.length > 0 || hasChildrenLikeProp) context.report({
				node: firstArgument,
				message: buildMessage(tagName)
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/zod/utils/zod-ast.ts
const ZOD_MODULE = "zod";
const getStaticPropertyName = (member) => {
	const property = member.property;
	if (!member.computed && isNodeOfType(property, "Identifier")) return property.name;
	if (member.computed && isNodeOfType(property, "Literal") && typeof property.value === "string") return property.value;
	return null;
};
const getImportInfoForIdentifier = (identifier) => {
	const specifier = findVariableInitializer(identifier, identifier.name)?.initializer;
	if (!specifier) return null;
	const declaration = specifier.parent;
	if (!declaration || !isNodeOfType(declaration, "ImportDeclaration")) return null;
	if (declaration.source?.value !== ZOD_MODULE) return null;
	if (isNodeOfType(specifier, "ImportNamespaceSpecifier")) return {
		imported: null,
		isDefault: false,
		isNamespace: true
	};
	if (isNodeOfType(specifier, "ImportDefaultSpecifier")) return {
		imported: null,
		isDefault: true,
		isNamespace: false
	};
	if (isNodeOfType(specifier, "ImportSpecifier")) {
		const imported = specifier.imported;
		if (isNodeOfType(imported, "Identifier")) return {
			imported: imported.name,
			isDefault: false,
			isNamespace: false
		};
		if (isNodeOfType(imported, "Literal") && typeof imported.value === "string") return {
			imported: imported.value,
			isDefault: false,
			isNamespace: false
		};
	}
	return null;
};
const isZodNamespaceIdentifier = (node) => {
	const inner = stripParenExpression(node);
	if (!isNodeOfType(inner, "Identifier")) return false;
	const info = getImportInfoForIdentifier(inner);
	return Boolean(info && (info.isNamespace || info.isDefault || info.imported === "z"));
};
const getZodNamedImport = (node) => {
	const inner = stripParenExpression(node);
	if (!isNodeOfType(inner, "Identifier")) return null;
	const info = getImportInfoForIdentifier(inner);
	if (!info || info.isNamespace || info.isDefault) return null;
	return info.imported;
};
const getZodNamespaceMemberName = (node) => {
	const inner = stripParenExpression(node);
	if (!isNodeOfType(inner, "MemberExpression")) return null;
	if (!isZodNamespaceIdentifier(inner.object)) return null;
	return getStaticPropertyName(inner);
};
const isZodFactoryCall = (callExpression, factoryNames) => {
	const factoryName = getZodFactoryCallName(callExpression);
	return factoryName !== null && factoryNames.has(factoryName);
};
const getZodFactoryCallName = (callExpression) => {
	const callee = stripParenExpression(callExpression.callee);
	if (isNodeOfType(callee, "Identifier")) return getZodNamedImport(callee);
	if (!isNodeOfType(callee, "MemberExpression")) return null;
	const memberName = getStaticPropertyName(callee);
	if (memberName === null) return null;
	if (!isZodNamespaceIdentifier(callee.object)) return null;
	return memberName;
};
const getMethodCall = (callExpression) => {
	const callee = stripParenExpression(callExpression.callee);
	if (!isNodeOfType(callee, "MemberExpression")) return null;
	const methodName = getStaticPropertyName(callee);
	if (!methodName) return null;
	return {
		methodName,
		receiver: callee.object
	};
};
const isDirectMethodCallOnZodFactory = (callExpression, factoryNames, methodNames) => {
	const methodCall = getMethodCall(callExpression);
	if (!methodCall || !methodNames.has(methodCall.methodName)) return false;
	const receiver = stripParenExpression(methodCall.receiver);
	return isNodeOfType(receiver, "CallExpression") && isZodFactoryCall(receiver, factoryNames);
};
const isObjectExpressionWithAnyProperty = (node, propertyNames) => {
	if (!node) return false;
	const inner = stripParenExpression(node);
	if (!isNodeOfType(inner, "ObjectExpression")) return false;
	return inner.properties.some((property) => {
		if (!isNodeOfType(property, "Property")) return false;
		const key = property.key;
		if (isNodeOfType(key, "Identifier")) return propertyNames.has(key.name);
		return isNodeOfType(key, "Literal") && typeof key.value === "string" && propertyNames.has(key.value);
	});
};
//#endregion
//#region src/plugin/rules/zod/zod-v4-no-deprecated-error-apis.ts
const DEPRECATED_ZOD_ERROR_MEMBERS = new Set([
	"addIssue",
	"addIssues",
	"errors",
	"flatten",
	"formErrors",
	"format"
]);
const ZOD_ERROR_API_MESSAGE = "Zod 4 dropped this ZodError method, so it breaks when you upgrade.";
const isZodErrorReference = (node) => {
	const inner = stripParenExpression(node);
	if (isNodeOfType(inner, "Identifier")) return getZodNamedImport(inner) === "ZodError";
	if (!isNodeOfType(inner, "MemberExpression")) return false;
	return getZodNamespaceMemberName(inner) === "ZodError";
};
const isDirectZodErrorValue = (node) => {
	const inner = stripParenExpression(node);
	if (isNodeOfType(inner, "NewExpression")) return isZodErrorReference(inner.callee);
	if (!isNodeOfType(inner, "CallExpression")) return false;
	const methodCall = getMethodCall(inner);
	return methodCall?.methodName === "create" && isZodErrorReference(methodCall.receiver);
};
const isDeprecatedZodErrorMemberAccess = (node) => {
	if (!isNodeOfType(node, "MemberExpression")) return false;
	const memberExpression = node;
	const propertyName = getStaticPropertyName(memberExpression);
	return propertyName !== null && DEPRECATED_ZOD_ERROR_MEMBERS.has(propertyName) && isDirectZodErrorValue(memberExpression.object);
};
const isZodErrorCreateCall = (callExpression) => {
	const methodCall = getMethodCall(callExpression);
	return methodCall?.methodName === "create" && isZodErrorReference(methodCall.receiver);
};
const isReceiverOfDeprecatedZodErrorMember = (callExpression) => {
	const parent = callExpression.parent;
	if (!parent || !isNodeOfType(parent, "MemberExpression")) return false;
	if (stripParenExpression(parent.object) !== callExpression) return false;
	return isDeprecatedZodErrorMemberAccess(parent);
};
const zodV4NoDeprecatedErrorApis = defineRule({
	id: "zod-v4-no-deprecated-error-apis",
	title: "Deprecated Zod error API",
	requires: ["zod:4"],
	tags: ["migration-hint"],
	severity: "warn",
	recommendation: "Use the Zod 4 helpers instead: `z.treeifyError()`, `z.flattenError()`, `z.prettifyError()`, or read `error.issues` directly.",
	create: (context) => ({
		CallExpression(node) {
			if (isZodErrorCreateCall(node) && isReceiverOfDeprecatedZodErrorMember(node)) return;
			if (!isZodErrorCreateCall(node) && !isDeprecatedZodErrorMemberAccess(node.callee)) return;
			context.report({
				node,
				message: ZOD_ERROR_API_MESSAGE
			});
		},
		MemberExpression(node) {
			const parent = node.parent;
			if (parent && isNodeOfType(parent, "CallExpression") && stripParenExpression(parent.callee) === node) return;
			if (!isDeprecatedZodErrorMemberAccess(node)) return;
			context.report({
				node,
				message: ZOD_ERROR_API_MESSAGE
			});
		}
	})
});
//#endregion
//#region src/plugin/rules/zod/zod-v4-no-deprecated-error-customization.ts
const ZOD_FACTORIES_WITH_ERROR_PARAMS = new Set([
	"any",
	"array",
	"bigint",
	"boolean",
	"date",
	"enum",
	"literal",
	"map",
	"nativeEnum",
	"never",
	"null",
	"number",
	"object",
	"record",
	"set",
	"string",
	"tuple",
	"undefined",
	"union",
	"unknown",
	"void"
]);
const DROPPED_ERROR_OPTION_PROPERTIES = new Set([
	"errorMap",
	"invalid_type_error",
	"required_error"
]);
const FACTORIES_WITH_LEGACY_FIRST_ARG_MESSAGE = new Set([
	"bigint",
	"boolean",
	"date",
	"number",
	"string"
]);
const ERROR_MAP_PROPERTY = new Set(["errorMap"]);
const PARSE_METHODS = new Set([
	"parse",
	"safeParse",
	"parseAsync",
	"safeParseAsync"
]);
const firstArgumentIsMessageString = (callExpression) => {
	const firstArgument = callExpression.arguments[0];
	const inner = firstArgument ? stripParenExpression(firstArgument) : null;
	return Boolean(inner && isNodeOfType(inner, "Literal") && typeof inner.value === "string");
};
const factoryUsesDeprecatedErrorParameter = (callExpression) => {
	const factoryName = getZodFactoryCallName(callExpression);
	if (factoryName === null || !ZOD_FACTORIES_WITH_ERROR_PARAMS.has(factoryName)) return false;
	return FACTORIES_WITH_LEGACY_FIRST_ARG_MESSAGE.has(factoryName) && firstArgumentIsMessageString(callExpression) || callExpression.arguments.some((argument) => isObjectExpressionWithAnyProperty(argument, DROPPED_ERROR_OPTION_PROPERTIES));
};
const parseCallUsesErrorMap = (callExpression) => {
	const methodCall = getMethodCall(callExpression);
	if (!methodCall || !PARSE_METHODS.has(methodCall.methodName)) return false;
	const receiver = stripParenExpression(methodCall.receiver);
	if (!isNodeOfType(receiver, "CallExpression")) return false;
	if (!isZodFactoryCall(receiver, ZOD_FACTORIES_WITH_ERROR_PARAMS)) return false;
	return isObjectExpressionWithAnyProperty(callExpression.arguments[1], ERROR_MAP_PROPERTY);
};
const zodV4NoDeprecatedErrorCustomization = defineRule({
	id: "zod-v4-no-deprecated-error-customization",
	title: "Deprecated Zod error customization",
	requires: ["zod:4"],
	tags: ["migration-hint"],
	severity: "warn",
	recommendation: "In Zod 4, use the single `{ error }` option instead of message strings, `invalid_type_error`, `required_error`, or `errorMap`.",
	create: (context) => ({ CallExpression(node) {
		if (!factoryUsesDeprecatedErrorParameter(node) && !parseCallUsesErrorMap(node)) return;
		context.report({
			node,
			message: "Zod 4 changed how you customize error messages, so this breaks when you upgrade."
		});
	} })
});
//#endregion
//#region src/plugin/rules/zod/zod-v4-no-deprecated-schema-apis.ts
const OBJECT_FACTORY = new Set(["object"]);
const OBJECT_METHODS = new Set([
	"deepPartial",
	"merge",
	"nonstrict",
	"passthrough",
	"strict",
	"strip"
]);
const NUMBER_FACTORY = new Set(["number"]);
const NUMBER_METHODS = new Set(["safe"]);
const FUNCTION_FACTORY = new Set(["function"]);
const FUNCTION_CHAIN_METHODS = new Set(["args", "returns"]);
const DEPRECATED_TOP_LEVEL_FACTORIES = new Set([
	"nativeEnum",
	"ostring",
	"onumber",
	"oboolean",
	"oarray",
	"promise"
]);
const FACTORIES_WITH_DROPPED_CREATE = new Set([
	"any",
	"array",
	"bigint",
	"boolean",
	"date",
	"enum",
	"function",
	"literal",
	"map",
	"nativeEnum",
	"never",
	"null",
	"number",
	"object",
	"optional",
	"promise",
	"record",
	"set",
	"string",
	"tuple",
	"undefined",
	"union",
	"unknown",
	"void"
]);
const ENUM_PROPERTY_ALIASES = new Set(["Enum", "Values"]);
const ENUM_FACTORY = new Set(["enum"]);
const RECORD_FACTORY = new Set(["record"]);
const LITERAL_FACTORY = new Set(["literal"]);
const reportSchemaMigration = (context, node) => {
	context.report({
		node,
		message: "Zod 4 deprecated or changed this API, so it breaks when you upgrade."
	});
};
const isCallToDeprecatedTopLevelFactory = (callExpression) => isZodFactoryCall(callExpression, DEPRECATED_TOP_LEVEL_FACTORIES);
const isCallToDroppedCreateFactory = (callExpression) => {
	const methodCall = getMethodCall(callExpression);
	if (!methodCall || methodCall.methodName !== "create") return false;
	const receiver = stripParenExpression(methodCall.receiver);
	const namespaceMemberName = getZodNamespaceMemberName(receiver);
	if (namespaceMemberName !== null) return FACTORIES_WITH_DROPPED_CREATE.has(namespaceMemberName);
	if (!isNodeOfType(receiver, "Identifier")) return false;
	const imported = getZodNamedImport(receiver);
	return imported !== null && FACTORIES_WITH_DROPPED_CREATE.has(imported);
};
const isSingleArgumentRecordCall = (callExpression) => callExpression.arguments.length === 1 && isZodFactoryCall(callExpression, RECORD_FACTORY);
const isDeprecatedFunctionChainCall = (callExpression) => isDirectMethodCallOnZodFactory(callExpression, FUNCTION_FACTORY, FUNCTION_CHAIN_METHODS);
const isSymbolLiteralArgument = (node) => {
	if (!node) return false;
	const inner = stripParenExpression(node);
	if (isNodeOfType(inner, "CallExpression")) {
		const callee = stripParenExpression(inner.callee);
		return isNodeOfType(callee, "Identifier") && callee.name === "Symbol";
	}
	if (!isNodeOfType(inner, "MemberExpression")) return false;
	const object = stripParenExpression(inner.object);
	return isNodeOfType(object, "Identifier") && object.name === "Symbol";
};
const isLiteralSymbolCall = (callExpression) => callExpression.arguments.length > 0 && isZodFactoryCall(callExpression, LITERAL_FACTORY) && isSymbolLiteralArgument(callExpression.arguments[0]);
const isDroppedEnumAliasAccess = (memberExpression) => {
	const propertyName = getStaticPropertyName(memberExpression);
	if (propertyName === null || !ENUM_PROPERTY_ALIASES.has(propertyName)) return false;
	const receiver = stripParenExpression(memberExpression.object);
	return isNodeOfType(receiver, "CallExpression") && isZodFactoryCall(receiver, ENUM_FACTORY);
};
const isRefineSecondArgumentFunction = (callExpression) => {
	const methodCall = getMethodCall(callExpression);
	if (!methodCall || methodCall.methodName !== "refine") return false;
	const receiver = stripParenExpression(methodCall.receiver);
	if (!isNodeOfType(receiver, "CallExpression")) return false;
	if (!isZodFactoryCall(receiver, FACTORIES_WITH_DROPPED_CREATE)) return false;
	const secondArgument = callExpression.arguments[1];
	return isNodeOfType(secondArgument, "FunctionExpression") || isNodeOfType(secondArgument, "ArrowFunctionExpression");
};
const isZodNamespaceImportMemberCreate = (memberExpression) => {
	if (getStaticPropertyName(memberExpression) !== "create") return false;
	const receiver = stripParenExpression(memberExpression.object);
	if (!isNodeOfType(receiver, "MemberExpression")) return false;
	const factoryName = getStaticPropertyName(receiver);
	return factoryName !== null && FACTORIES_WITH_DROPPED_CREATE.has(factoryName) && isZodNamespaceIdentifier(receiver.object);
};
const zodV4NoDeprecatedSchemaApis = defineRule({
	id: "zod-v4-no-deprecated-schema-apis",
	title: "Deprecated Zod schema API",
	requires: ["zod:4"],
	tags: ["migration-hint"],
	severity: "warn",
	recommendation: "Switch to the Zod 4 versions: top-level factories like `z.enum()`, object helpers like `z.strictObject()`, the new `z.function({ input, output })` form, and explicit key/value schemas for `z.record()`.",
	create: (context) => ({
		CallExpression(node) {
			if (isCallToDeprecatedTopLevelFactory(node) || isCallToDroppedCreateFactory(node) || isSingleArgumentRecordCall(node) || isLiteralSymbolCall(node) || isDeprecatedFunctionChainCall(node) || isDirectMethodCallOnZodFactory(node, OBJECT_FACTORY, OBJECT_METHODS) || isDirectMethodCallOnZodFactory(node, NUMBER_FACTORY, NUMBER_METHODS) || isRefineSecondArgumentFunction(node)) reportSchemaMigration(context, node);
		},
		MemberExpression(node) {
			const parent = node.parent;
			if (parent && isNodeOfType(parent, "CallExpression") && stripParenExpression(parent.callee) === node) return;
			if (isDroppedEnumAliasAccess(node) || isZodNamespaceImportMemberCreate(node)) reportSchemaMigration(context, node);
		}
	})
});
//#endregion
//#region src/plugin/rules/zod/zod-v4-prefer-top-level-string-formats.ts
const ZOD_STRING_FACTORY = new Set(["string"]);
const STRING_FORMAT_METHODS = new Set([
	"base64",
	"base64url",
	"cidr",
	"cidrv4",
	"cidrv6",
	"cuid",
	"cuid2",
	"date",
	"datetime",
	"duration",
	"email",
	"emoji",
	"ip",
	"ipv4",
	"ipv6",
	"jwt",
	"nanoid",
	"time",
	"ulid",
	"url",
	"uuid"
]);
const zodV4PreferTopLevelStringFormats = defineRule({
	id: "zod-v4-prefer-top-level-string-formats",
	title: "Format method on z.string()",
	requires: ["zod:4"],
	tags: ["migration-hint"],
	severity: "warn",
	recommendation: "Use the Zod 4 top-level format checks like `z.email()`, `z.uuid()`, or `z.ipv4()` instead of `z.string().<format>()`.",
	create: (context) => ({ CallExpression(node) {
		if (!isDirectMethodCallOnZodFactory(node, ZOD_STRING_FACTORY, STRING_FORMAT_METHODS)) return;
		context.report({
			node,
			message: "Zod 4 deprecated format methods on `z.string()`, so they break when you upgrade."
		});
	} })
});
//#endregion
//#region src/plugin/rule-registry.ts
const reactDoctorRules = [
	{
		key: "react-doctor/activity-wraps-effect-heavy-subtree",
		id: "activity-wraps-effect-heavy-subtree",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...activityWrapsEffectHeavySubtree,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/advanced-event-handler-refs",
		id: "advanced-event-handler-refs",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...advancedEventHandlerRefs,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/alt-text",
		id: "alt-text",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...altText,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/anchor-ambiguous-text",
		id: "anchor-ambiguous-text",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...anchorAmbiguousText,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/anchor-has-content",
		id: "anchor-has-content",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...anchorHasContent,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/anchor-is-valid",
		id: "anchor-is-valid",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...anchorIsValid,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/aria-activedescendant-has-tabindex",
		id: "aria-activedescendant-has-tabindex",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...ariaActivedescendantHasTabindex,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/aria-props",
		id: "aria-props",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...ariaProps,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/aria-proptypes",
		id: "aria-proptypes",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...ariaProptypes,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/aria-role",
		id: "aria-role",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...ariaRole,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/aria-unsupported-elements",
		id: "aria-unsupported-elements",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...ariaUnsupportedElements,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/async-await-in-loop",
		id: "async-await-in-loop",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...asyncAwaitInLoop,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/async-defer-await",
		id: "async-defer-await",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...asyncDeferAwait,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/async-parallel",
		id: "async-parallel",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...asyncParallel,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/autocomplete-valid",
		id: "autocomplete-valid",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...autocompleteValid,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/button-has-type",
		id: "button-has-type",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...buttonHasType,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/checked-requires-onchange-or-readonly",
		id: "checked-requires-onchange-or-readonly",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...checkedRequiresOnchangeOrReadonly,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/click-events-have-key-events",
		id: "click-events-have-key-events",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...clickEventsHaveKeyEvents,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/client-localstorage-no-version",
		id: "client-localstorage-no-version",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...clientLocalstorageNoVersion,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/client-passive-event-listeners",
		id: "client-passive-event-listeners",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...clientPassiveEventListeners,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/control-has-associated-label",
		id: "control-has-associated-label",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...controlHasAssociatedLabel,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/design-no-em-dash-in-jsx-text",
		id: "design-no-em-dash-in-jsx-text",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noEmDashInJsxText,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/design-no-redundant-padding-axes",
		id: "design-no-redundant-padding-axes",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noRedundantPaddingAxes,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/design-no-redundant-size-axes",
		id: "design-no-redundant-size-axes",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noRedundantSizeAxes,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/design-no-space-on-flex-children",
		id: "design-no-space-on-flex-children",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noSpaceOnFlexChildren,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/design-no-three-period-ellipsis",
		id: "design-no-three-period-ellipsis",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noThreePeriodEllipsis,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/design-no-vague-button-label",
		id: "design-no-vague-button-label",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noVagueButtonLabel,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/display-name",
		id: "display-name",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...displayName,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/effect-needs-cleanup",
		id: "effect-needs-cleanup",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...effectNeedsCleanup,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/exhaustive-deps",
		id: "exhaustive-deps",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...exhaustiveDeps,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/expo-no-non-inlined-env",
		id: "expo-no-non-inlined-env",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...expoNoNonInlinedEnv,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...expoNoNonInlinedEnv.tags ?? []])]
		}
	},
	{
		key: "react-doctor/forbid-component-props",
		id: "forbid-component-props",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...forbidComponentProps,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/forbid-dom-props",
		id: "forbid-dom-props",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...forbidDomProps,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/forbid-elements",
		id: "forbid-elements",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...forbidElements,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/forward-ref-uses-ref",
		id: "forward-ref-uses-ref",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...forwardRefUsesRef,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/heading-has-content",
		id: "heading-has-content",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...headingHasContent,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/hook-use-state",
		id: "hook-use-state",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...hookUseState,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/hooks-no-nan-in-deps",
		id: "hooks-no-nan-in-deps",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...hooksNoNanInDeps,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/html-has-lang",
		id: "html-has-lang",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...htmlHasLang,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/html-no-invalid-paragraph-child",
		id: "html-no-invalid-paragraph-child",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...htmlNoInvalidParagraphChild,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/html-no-invalid-table-nesting",
		id: "html-no-invalid-table-nesting",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...htmlNoInvalidTableNesting,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/html-no-nested-interactive",
		id: "html-no-nested-interactive",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...htmlNoNestedInteractive,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/iframe-has-title",
		id: "iframe-has-title",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...iframeHasTitle,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/iframe-missing-sandbox",
		id: "iframe-missing-sandbox",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...iframeMissingSandbox,
			framework: "global",
			category: "Security"
		}
	},
	{
		key: "react-doctor/img-redundant-alt",
		id: "img-redundant-alt",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...imgRedundantAlt,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/interactive-supports-focus",
		id: "interactive-supports-focus",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...interactiveSupportsFocus,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/jotai-derived-atom-returns-fresh-object",
		id: "jotai-derived-atom-returns-fresh-object",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jotaiDerivedAtomReturnsFreshObject,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/jotai-select-atom-in-render-body",
		id: "jotai-select-atom-in-render-body",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jotaiSelectAtomInRenderBody,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/jotai-tq-use-raw-query-atom",
		id: "jotai-tq-use-raw-query-atom",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jotaiTqUseRawQueryAtom,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/js-async-reduce-without-awaited-acc",
		id: "js-async-reduce-without-awaited-acc",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsAsyncReduceWithoutAwaitedAcc,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-batch-dom-css",
		id: "js-batch-dom-css",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsBatchDomCss,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-cache-property-access",
		id: "js-cache-property-access",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsCachePropertyAccess,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-cache-storage",
		id: "js-cache-storage",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsCacheStorage,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-combine-iterations",
		id: "js-combine-iterations",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsCombineIterations,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-early-exit",
		id: "js-early-exit",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsEarlyExit,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-flatmap-filter",
		id: "js-flatmap-filter",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsFlatmapFilter,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-hoist-intl",
		id: "js-hoist-intl",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsHoistIntl,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-hoist-regexp",
		id: "js-hoist-regexp",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsHoistRegexp,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-index-maps",
		id: "js-index-maps",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsIndexMaps,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-length-check-first",
		id: "js-length-check-first",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsLengthCheckFirst,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-min-max-loop",
		id: "js-min-max-loop",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsMinMaxLoop,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-set-map-lookups",
		id: "js-set-map-lookups",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsSetMapLookups,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/js-tosorted-immutable",
		id: "js-tosorted-immutable",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...jsTosortedImmutable,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/jsx-boolean-value",
		id: "jsx-boolean-value",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxBooleanValue,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/jsx-curly-brace-presence",
		id: "jsx-curly-brace-presence",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxCurlyBracePresence,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/jsx-filename-extension",
		id: "jsx-filename-extension",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxFilenameExtension,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/jsx-fragments",
		id: "jsx-fragments",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxFragments,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/jsx-handler-names",
		id: "jsx-handler-names",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxHandlerNames,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/jsx-key",
		id: "jsx-key",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxKey,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/jsx-max-depth",
		id: "jsx-max-depth",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxMaxDepth,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/jsx-no-comment-textnodes",
		id: "jsx-no-comment-textnodes",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoCommentTextnodes,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/jsx-no-constructed-context-values",
		id: "jsx-no-constructed-context-values",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoConstructedContextValues,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/jsx-no-duplicate-props",
		id: "jsx-no-duplicate-props",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoDuplicateProps,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/jsx-no-jsx-as-prop",
		id: "jsx-no-jsx-as-prop",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoJsxAsProp,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/jsx-no-new-array-as-prop",
		id: "jsx-no-new-array-as-prop",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoNewArrayAsProp,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/jsx-no-new-function-as-prop",
		id: "jsx-no-new-function-as-prop",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoNewFunctionAsProp,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/jsx-no-new-object-as-prop",
		id: "jsx-no-new-object-as-prop",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoNewObjectAsProp,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/jsx-no-script-url",
		id: "jsx-no-script-url",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoScriptUrl,
			framework: "global",
			category: "Security"
		}
	},
	{
		key: "react-doctor/jsx-no-target-blank",
		id: "jsx-no-target-blank",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoTargetBlank,
			framework: "global",
			category: "Security"
		}
	},
	{
		key: "react-doctor/jsx-no-undef",
		id: "jsx-no-undef",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoUndef,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/jsx-no-useless-fragment",
		id: "jsx-no-useless-fragment",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxNoUselessFragment,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/jsx-pascal-case",
		id: "jsx-pascal-case",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxPascalCase,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/jsx-props-no-spread-multi",
		id: "jsx-props-no-spread-multi",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxPropsNoSpreadMulti,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/jsx-props-no-spreading",
		id: "jsx-props-no-spreading",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...jsxPropsNoSpreading,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/label-has-associated-control",
		id: "label-has-associated-control",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...labelHasAssociatedControl,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/lang",
		id: "lang",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...lang,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/media-has-caption",
		id: "media-has-caption",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...mediaHasCaption,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/mouse-events-have-key-events",
		id: "mouse-events-have-key-events",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...mouseEventsHaveKeyEvents,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/nextjs-async-client-component",
		id: "nextjs-async-client-component",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsAsyncClientComponent,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-error-boundary-missing-use-client",
		id: "nextjs-error-boundary-missing-use-client",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsErrorBoundaryMissingUseClient,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-global-error-missing-html-body",
		id: "nextjs-global-error-missing-html-body",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsGlobalErrorMissingHtmlBody,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-image-missing-sizes",
		id: "nextjs-image-missing-sizes",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsImageMissingSizes,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-inline-script-missing-id",
		id: "nextjs-inline-script-missing-id",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsInlineScriptMissingId,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-missing-metadata",
		id: "nextjs-missing-metadata",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsMissingMetadata,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-a-element",
		id: "nextjs-no-a-element",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoAElement,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-client-fetch-for-server-data",
		id: "nextjs-no-client-fetch-for-server-data",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoClientFetchForServerData,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-client-side-redirect",
		id: "nextjs-no-client-side-redirect",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoClientSideRedirect,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-css-link",
		id: "nextjs-no-css-link",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoCssLink,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-default-export-in-route-handler",
		id: "nextjs-no-default-export-in-route-handler",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoDefaultExportInRouteHandler,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-edge-og-runtime",
		id: "nextjs-no-edge-og-runtime",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoEdgeOgRuntime,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-font-link",
		id: "nextjs-no-font-link",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoFontLink,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-google-analytics-script",
		id: "nextjs-no-google-analytics-script",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoGoogleAnalyticsScript,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-head-import",
		id: "nextjs-no-head-import",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoHeadImport,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-img-element",
		id: "nextjs-no-img-element",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoImgElement,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-native-script",
		id: "nextjs-no-native-script",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoNativeScript,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-polyfill-script",
		id: "nextjs-no-polyfill-script",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoPolyfillScript,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-redirect-in-try-catch",
		id: "nextjs-no-redirect-in-try-catch",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoRedirectInTryCatch,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-script-in-head",
		id: "nextjs-no-script-in-head",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoScriptInHead,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-side-effect-in-get-handler",
		id: "nextjs-no-side-effect-in-get-handler",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoSideEffectInGetHandler,
			framework: "nextjs",
			category: "Security"
		}
	},
	{
		key: "react-doctor/nextjs-no-use-search-params-without-suspense",
		id: "nextjs-no-use-search-params-without-suspense",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoUseSearchParamsWithoutSuspense,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/nextjs-no-vercel-og-import",
		id: "nextjs-no-vercel-og-import",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...nextjsNoVercelOgImport,
			framework: "nextjs",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-access-key",
		id: "no-access-key",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noAccessKey,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-adjust-state-on-prop-change",
		id: "no-adjust-state-on-prop-change",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noAdjustStateOnPropChange,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-aria-hidden-on-focusable",
		id: "no-aria-hidden-on-focusable",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noAriaHiddenOnFocusable,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-array-index-as-key",
		id: "no-array-index-as-key",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noArrayIndexAsKey,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-array-index-key",
		id: "no-array-index-key",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noArrayIndexKey,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-autofocus",
		id: "no-autofocus",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noAutofocus,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-barrel-import",
		id: "no-barrel-import",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noBarrelImport,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-cascading-set-state",
		id: "no-cascading-set-state",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noCascadingSetState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-chain-state-updates",
		id: "no-chain-state-updates",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noChainStateUpdates,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-children-prop",
		id: "no-children-prop",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noChildrenProp,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-clone-element",
		id: "no-clone-element",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noCloneElement,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-create-context-in-render",
		id: "no-create-context-in-render",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noCreateContextInRender,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-create-store-in-render",
		id: "no-create-store-in-render",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noCreateStoreInRender,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-danger",
		id: "no-danger",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noDanger,
			framework: "global",
			category: "Security"
		}
	},
	{
		key: "react-doctor/no-danger-with-children",
		id: "no-danger-with-children",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noDangerWithChildren,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-dark-mode-glow",
		id: "no-dark-mode-glow",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noDarkModeGlow,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-default-props",
		id: "no-default-props",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noDefaultProps,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-derived-state",
		id: "no-derived-state",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noDerivedState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-derived-state-effect",
		id: "no-derived-state-effect",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noDerivedStateEffect,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-derived-useState",
		id: "no-derived-useState",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noDerivedUseState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-did-mount-set-state",
		id: "no-did-mount-set-state",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noDidMountSetState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-did-update-set-state",
		id: "no-did-update-set-state",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noDidUpdateSetState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-direct-mutation-state",
		id: "no-direct-mutation-state",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noDirectMutationState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-direct-state-mutation",
		id: "no-direct-state-mutation",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noDirectStateMutation,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-disabled-zoom",
		id: "no-disabled-zoom",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noDisabledZoom,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-distracting-elements",
		id: "no-distracting-elements",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noDistractingElements,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-document-start-view-transition",
		id: "no-document-start-view-transition",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noDocumentStartViewTransition,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-dynamic-import-path",
		id: "no-dynamic-import-path",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noDynamicImportPath,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-effect-chain",
		id: "no-effect-chain",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noEffectChain,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-effect-event-handler",
		id: "no-effect-event-handler",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noEffectEventHandler,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-effect-event-in-deps",
		id: "no-effect-event-in-deps",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noEffectEventInDeps,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-effect-with-fresh-deps",
		id: "no-effect-with-fresh-deps",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noEffectWithFreshDeps,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-eval",
		id: "no-eval",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noEval,
			framework: "global",
			category: "Security"
		}
	},
	{
		key: "react-doctor/no-event-handler",
		id: "no-event-handler",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noEventHandler,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-event-trigger-state",
		id: "no-event-trigger-state",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noEventTriggerState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-fetch-in-effect",
		id: "no-fetch-in-effect",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noFetchInEffect,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-find-dom-node",
		id: "no-find-dom-node",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noFindDomNode,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-flush-sync",
		id: "no-flush-sync",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noFlushSync,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-full-lodash-import",
		id: "no-full-lodash-import",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noFullLodashImport,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-generic-handler-names",
		id: "no-generic-handler-names",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noGenericHandlerNames,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-giant-component",
		id: "no-giant-component",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noGiantComponent,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-global-css-variable-animation",
		id: "no-global-css-variable-animation",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noGlobalCssVariableAnimation,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-gradient-text",
		id: "no-gradient-text",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noGradientText,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-gray-on-colored-background",
		id: "no-gray-on-colored-background",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noGrayOnColoredBackground,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-initialize-state",
		id: "no-initialize-state",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noInitializeState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-inline-bounce-easing",
		id: "no-inline-bounce-easing",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noInlineBounceEasing,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-inline-exhaustive-style",
		id: "no-inline-exhaustive-style",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noInlineExhaustiveStyle,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-inline-prop-on-memo-component",
		id: "no-inline-prop-on-memo-component",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noInlinePropOnMemoComponent,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-interactive-element-to-noninteractive-role",
		id: "no-interactive-element-to-noninteractive-role",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noInteractiveElementToNoninteractiveRole,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-is-mounted",
		id: "no-is-mounted",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noIsMounted,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-jsx-element-type",
		id: "no-jsx-element-type",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noJsxElementType,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-justified-text",
		id: "no-justified-text",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noJustifiedText,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-large-animated-blur",
		id: "no-large-animated-blur",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noLargeAnimatedBlur,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-layout-property-animation",
		id: "no-layout-property-animation",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noLayoutPropertyAnimation,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-layout-transition-inline",
		id: "no-layout-transition-inline",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noLayoutTransitionInline,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-legacy-class-lifecycles",
		id: "no-legacy-class-lifecycles",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noLegacyClassLifecycles,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-legacy-context-api",
		id: "no-legacy-context-api",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noLegacyContextApi,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-long-transition-duration",
		id: "no-long-transition-duration",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noLongTransitionDuration,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-many-boolean-props",
		id: "no-many-boolean-props",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noManyBooleanProps,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-mirror-prop-effect",
		id: "no-mirror-prop-effect",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noMirrorPropEffect,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-moment",
		id: "no-moment",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noMoment,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-multi-comp",
		id: "no-multi-comp",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noMultiComp,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-mutable-in-deps",
		id: "no-mutable-in-deps",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noMutableInDeps,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-mutating-reducer-state",
		id: "no-mutating-reducer-state",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noMutatingReducerState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-namespace",
		id: "no-namespace",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noNamespace,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-nested-component-definition",
		id: "no-nested-component-definition",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noNestedComponentDefinition,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-noninteractive-element-interactions",
		id: "no-noninteractive-element-interactions",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noNoninteractiveElementInteractions,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-noninteractive-element-to-interactive-role",
		id: "no-noninteractive-element-to-interactive-role",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noNoninteractiveElementToInteractiveRole,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-noninteractive-tabindex",
		id: "no-noninteractive-tabindex",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noNoninteractiveTabindex,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-outline-none",
		id: "no-outline-none",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noOutlineNone,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-pass-data-to-parent",
		id: "no-pass-data-to-parent",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noPassDataToParent,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-pass-live-state-to-parent",
		id: "no-pass-live-state-to-parent",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noPassLiveStateToParent,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-permanent-will-change",
		id: "no-permanent-will-change",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noPermanentWillChange,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-polymorphic-children",
		id: "no-polymorphic-children",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noPolymorphicChildren,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-prevent-default",
		id: "no-prevent-default",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noPreventDefault,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-prop-callback-in-effect",
		id: "no-prop-callback-in-effect",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noPropCallbackInEffect,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-prop-types",
		id: "no-prop-types",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noPropTypes,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-pure-black-background",
		id: "no-pure-black-background",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noPureBlackBackground,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-random-key",
		id: "no-random-key",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noRandomKey,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-react-children",
		id: "no-react-children",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noReactChildren,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-react-dom-deprecated-apis",
		id: "no-react-dom-deprecated-apis",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noReactDomDeprecatedApis,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-react19-deprecated-apis",
		id: "no-react19-deprecated-apis",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noReact19DeprecatedApis,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-redundant-roles",
		id: "no-redundant-roles",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noRedundantRoles,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-redundant-should-component-update",
		id: "no-redundant-should-component-update",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noRedundantShouldComponentUpdate,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-render-in-render",
		id: "no-render-in-render",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noRenderInRender,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-render-prop-children",
		id: "no-render-prop-children",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noRenderPropChildren,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-render-return-value",
		id: "no-render-return-value",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noRenderReturnValue,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-reset-all-state-on-prop-change",
		id: "no-reset-all-state-on-prop-change",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noResetAllStateOnPropChange,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-scale-from-zero",
		id: "no-scale-from-zero",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noScaleFromZero,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-secrets-in-client-code",
		id: "no-secrets-in-client-code",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noSecretsInClientCode,
			framework: "global",
			category: "Security"
		}
	},
	{
		key: "react-doctor/no-self-updating-effect",
		id: "no-self-updating-effect",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noSelfUpdatingEffect,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-set-state",
		id: "no-set-state",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noSetState,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-set-state-in-render",
		id: "no-set-state-in-render",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noSetStateInRender,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-side-tab-border",
		id: "no-side-tab-border",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noSideTabBorder,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-static-element-interactions",
		id: "no-static-element-interactions",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noStaticElementInteractions,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-string-refs",
		id: "no-string-refs",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noStringRefs,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-this-in-sfc",
		id: "no-this-in-sfc",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noThisInSfc,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-tiny-text",
		id: "no-tiny-text",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noTinyText,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/no-transition-all",
		id: "no-transition-all",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noTransitionAll,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-uncontrolled-input",
		id: "no-uncontrolled-input",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noUncontrolledInput,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-undeferred-third-party",
		id: "no-undeferred-third-party",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noUndeferredThirdParty,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-unescaped-entities",
		id: "no-unescaped-entities",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noUnescapedEntities,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-unknown-property",
		id: "no-unknown-property",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noUnknownProperty,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-unsafe",
		id: "no-unsafe",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noUnsafe,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-unstable-nested-components",
		id: "no-unstable-nested-components",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noUnstableNestedComponents,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-usememo-simple-expression",
		id: "no-usememo-simple-expression",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noUsememoSimpleExpression,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/no-wide-letter-spacing",
		id: "no-wide-letter-spacing",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noWideLetterSpacing,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/no-will-update-set-state",
		id: "no-will-update-set-state",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...noWillUpdateSetState,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/no-z-index-9999",
		id: "no-z-index-9999",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...noZIndex9999,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/only-export-components",
		id: "only-export-components",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...onlyExportComponents,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/preact-no-children-length",
		id: "preact-no-children-length",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preactNoChildrenLength,
			framework: "preact",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/preact-no-react-hooks-import",
		id: "preact-no-react-hooks-import",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preactNoReactHooksImport,
			framework: "preact",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/preact-no-render-arguments",
		id: "preact-no-render-arguments",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preactNoRenderArguments,
			framework: "preact",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/preact-prefer-ondblclick",
		id: "preact-prefer-ondblclick",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preactPreferOndblclick,
			framework: "preact",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/preact-prefer-oninput",
		id: "preact-prefer-oninput",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preactPreferOninput,
			framework: "preact",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/prefer-dynamic-import",
		id: "prefer-dynamic-import",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferDynamicImport,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/prefer-es6-class",
		id: "prefer-es6-class",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...preferEs6Class,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/prefer-explicit-variants",
		id: "prefer-explicit-variants",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferExplicitVariants,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/prefer-function-component",
		id: "prefer-function-component",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...preferFunctionComponent,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/prefer-html-dialog",
		id: "prefer-html-dialog",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferHtmlDialog,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/prefer-module-scope-pure-function",
		id: "prefer-module-scope-pure-function",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferModuleScopePureFunction,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/prefer-module-scope-static-value",
		id: "prefer-module-scope-static-value",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferModuleScopeStaticValue,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/prefer-stable-empty-fallback",
		id: "prefer-stable-empty-fallback",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferStableEmptyFallback,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/prefer-tag-over-role",
		id: "prefer-tag-over-role",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...preferTagOverRole,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/prefer-use-effect-event",
		id: "prefer-use-effect-event",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferUseEffectEvent,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/prefer-use-sync-external-store",
		id: "prefer-use-sync-external-store",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferUseSyncExternalStore,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/prefer-useReducer",
		id: "prefer-useReducer",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...preferUseReducer,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/query-destructure-result",
		id: "query-destructure-result",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...queryDestructureResult,
			framework: "tanstack-query",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/query-mutation-missing-invalidation",
		id: "query-mutation-missing-invalidation",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...queryMutationMissingInvalidation,
			framework: "tanstack-query",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/query-no-query-in-effect",
		id: "query-no-query-in-effect",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...queryNoQueryInEffect,
			framework: "tanstack-query",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/query-no-rest-destructuring",
		id: "query-no-rest-destructuring",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...queryNoRestDestructuring,
			framework: "tanstack-query",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/query-no-usequery-for-mutation",
		id: "query-no-usequery-for-mutation",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...queryNoUseQueryForMutation,
			framework: "tanstack-query",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/query-no-void-query-fn",
		id: "query-no-void-query-fn",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...queryNoVoidQueryFn,
			framework: "tanstack-query",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/query-stable-query-client",
		id: "query-stable-query-client",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...queryStableQueryClient,
			framework: "tanstack-query",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/react-compiler-no-manual-memoization",
		id: "react-compiler-no-manual-memoization",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...reactCompilerNoManualMemoization,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/react-in-jsx-scope",
		id: "react-in-jsx-scope",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...reactInJsxScope,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/redux-useselector-inline-derivation",
		id: "redux-useselector-inline-derivation",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...reduxUseselectorInlineDerivation,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/redux-useselector-returns-new-collection",
		id: "redux-useselector-returns-new-collection",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...reduxUseselectorReturnsNewCollection,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rendering-animate-svg-wrapper",
		id: "rendering-animate-svg-wrapper",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...renderingAnimateSvgWrapper,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rendering-conditional-render",
		id: "rendering-conditional-render",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...renderingConditionalRender,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/rendering-hoist-jsx",
		id: "rendering-hoist-jsx",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...renderingHoistJsx,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rendering-hydration-mismatch-time",
		id: "rendering-hydration-mismatch-time",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...renderingHydrationMismatchTime,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/rendering-hydration-no-flicker",
		id: "rendering-hydration-no-flicker",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...renderingHydrationNoFlicker,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rendering-script-defer-async",
		id: "rendering-script-defer-async",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...renderingScriptDeferAsync,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rendering-svg-precision",
		id: "rendering-svg-precision",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...renderingSvgPrecision,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rendering-usetransition-loading",
		id: "rendering-usetransition-loading",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...renderingUsetransitionLoading,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/require-render-return",
		id: "require-render-return",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...requireRenderReturn,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/rerender-defer-reads-hook",
		id: "rerender-defer-reads-hook",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderDeferReadsHook,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rerender-dependencies",
		id: "rerender-dependencies",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderDependencies,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/rerender-derived-state-from-hook",
		id: "rerender-derived-state-from-hook",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderDerivedStateFromHook,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rerender-functional-setstate",
		id: "rerender-functional-setstate",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderFunctionalSetstate,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rerender-lazy-ref-init",
		id: "rerender-lazy-ref-init",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderLazyRefInit,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rerender-lazy-state-init",
		id: "rerender-lazy-state-init",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderLazyStateInit,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rerender-memo-before-early-return",
		id: "rerender-memo-before-early-return",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderMemoBeforeEarlyReturn,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rerender-memo-with-default-value",
		id: "rerender-memo-with-default-value",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderMemoWithDefaultValue,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rerender-state-only-in-handlers",
		id: "rerender-state-only-in-handlers",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderStateOnlyInHandlers,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rerender-transitions-scroll",
		id: "rerender-transitions-scroll",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rerenderTransitionsScroll,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/rn-animate-layout-property",
		id: "rn-animate-layout-property",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnAnimateLayoutProperty,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnAnimateLayoutProperty.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-animation-reaction-as-derived",
		id: "rn-animation-reaction-as-derived",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnAnimationReactionAsDerived,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnAnimationReactionAsDerived.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-bottom-sheet-prefer-native",
		id: "rn-bottom-sheet-prefer-native",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnBottomSheetPreferNative,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnBottomSheetPreferNative.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-detox-missing-await",
		id: "rn-detox-missing-await",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnDetoxMissingAwait,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnDetoxMissingAwait.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-list-callback-per-row",
		id: "rn-list-callback-per-row",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnListCallbackPerRow,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnListCallbackPerRow.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-list-data-mapped",
		id: "rn-list-data-mapped",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnListDataMapped,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnListDataMapped.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-list-missing-estimated-item-size",
		id: "rn-list-missing-estimated-item-size",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnListMissingEstimatedItemSize,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnListMissingEstimatedItemSize.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-list-recyclable-without-types",
		id: "rn-list-recyclable-without-types",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnListRecyclableWithoutTypes,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnListRecyclableWithoutTypes.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-deep-imports",
		id: "rn-no-deep-imports",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoDeepImports,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoDeepImports.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-deprecated-modules",
		id: "rn-no-deprecated-modules",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoDeprecatedModules,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoDeprecatedModules.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-dimensions-get",
		id: "rn-no-dimensions-get",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoDimensionsGet,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoDimensionsGet.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-falsy-and-render",
		id: "rn-no-falsy-and-render",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoFalsyAndRender,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoFalsyAndRender.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-image-children",
		id: "rn-no-image-children",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoImageChildren,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoImageChildren.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-inline-flatlist-renderitem",
		id: "rn-no-inline-flatlist-renderitem",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoInlineFlatlistRenderitem,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoInlineFlatlistRenderitem.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-inline-object-in-list-item",
		id: "rn-no-inline-object-in-list-item",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoInlineObjectInListItem,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoInlineObjectInListItem.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-legacy-expo-packages",
		id: "rn-no-legacy-expo-packages",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoLegacyExpoPackages,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoLegacyExpoPackages.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-legacy-shadow-styles",
		id: "rn-no-legacy-shadow-styles",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoLegacyShadowStyles,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoLegacyShadowStyles.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-non-native-navigator",
		id: "rn-no-non-native-navigator",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoNonNativeNavigator,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoNonNativeNavigator.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-panresponder",
		id: "rn-no-panresponder",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoPanresponder,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoPanresponder.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-raw-text",
		id: "rn-no-raw-text",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoRawText,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoRawText.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-renderitem-key",
		id: "rn-no-renderitem-key",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoRenderitemKey,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoRenderitemKey.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-scroll-state",
		id: "rn-no-scroll-state",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoScrollState,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoScrollState.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-scrollview-mapped-list",
		id: "rn-no-scrollview-mapped-list",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoScrollviewMappedList,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoScrollviewMappedList.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-set-native-props",
		id: "rn-no-set-native-props",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoSetNativeProps,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoSetNativeProps.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-no-single-element-style-array",
		id: "rn-no-single-element-style-array",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnNoSingleElementStyleArray,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnNoSingleElementStyleArray.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-prefer-content-inset-adjustment",
		id: "rn-prefer-content-inset-adjustment",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnPreferContentInsetAdjustment,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnPreferContentInsetAdjustment.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-prefer-expo-image",
		id: "rn-prefer-expo-image",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnPreferExpoImage,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnPreferExpoImage.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-prefer-pressable",
		id: "rn-prefer-pressable",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnPreferPressable,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnPreferPressable.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-prefer-pressable-over-gesture-detector",
		id: "rn-prefer-pressable-over-gesture-detector",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnPreferPressableOverGestureDetector,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnPreferPressableOverGestureDetector.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-prefer-reanimated",
		id: "rn-prefer-reanimated",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnPreferReanimated,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnPreferReanimated.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-pressable-shared-value-mutation",
		id: "rn-pressable-shared-value-mutation",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnPressableSharedValueMutation,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnPressableSharedValueMutation.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-scrollview-dynamic-padding",
		id: "rn-scrollview-dynamic-padding",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnScrollviewDynamicPadding,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnScrollviewDynamicPadding.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-scrollview-flex-in-content-container",
		id: "rn-scrollview-flex-in-content-container",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnScrollviewFlexInContentContainer,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnScrollviewFlexInContentContainer.tags ?? []])]
		}
	},
	{
		key: "react-doctor/rn-style-prefer-boxshadow",
		id: "rn-style-prefer-boxshadow",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...rnStylePreferBoxShadow,
			framework: "react-native",
			category: "Bugs",
			tags: [...new Set(["react-native", ...rnStylePreferBoxShadow.tags ?? []])]
		}
	},
	{
		key: "react-doctor/role-has-required-aria-props",
		id: "role-has-required-aria-props",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...roleHasRequiredAriaProps,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/role-supports-aria-props",
		id: "role-supports-aria-props",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...roleSupportsAriaProps,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/rules-of-hooks",
		id: "rules-of-hooks",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...rulesOfHooks,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/scope",
		id: "scope",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...scope,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/self-closing-comp",
		id: "self-closing-comp",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...selfClosingComp,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/server-after-nonblocking",
		id: "server-after-nonblocking",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...serverAfterNonblocking,
			framework: "global",
			category: "Bugs",
			tags: [...new Set(["server-action", ...serverAfterNonblocking.tags ?? []])]
		}
	},
	{
		key: "react-doctor/server-auth-actions",
		id: "server-auth-actions",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...serverAuthActions,
			framework: "global",
			category: "Bugs",
			tags: [...new Set(["server-action", ...serverAuthActions.tags ?? []])]
		}
	},
	{
		key: "react-doctor/server-cache-with-object-literal",
		id: "server-cache-with-object-literal",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...serverCacheWithObjectLiteral,
			framework: "global",
			category: "Bugs",
			tags: [...new Set(["server-action", ...serverCacheWithObjectLiteral.tags ?? []])]
		}
	},
	{
		key: "react-doctor/server-dedup-props",
		id: "server-dedup-props",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...serverDedupProps,
			framework: "global",
			category: "Bugs",
			tags: [...new Set(["server-action", ...serverDedupProps.tags ?? []])]
		}
	},
	{
		key: "react-doctor/server-fetch-without-revalidate",
		id: "server-fetch-without-revalidate",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...serverFetchWithoutRevalidate,
			framework: "global",
			category: "Bugs",
			tags: [...new Set(["server-action", ...serverFetchWithoutRevalidate.tags ?? []])]
		}
	},
	{
		key: "react-doctor/server-hoist-static-io",
		id: "server-hoist-static-io",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...serverHoistStaticIo,
			framework: "global",
			category: "Bugs",
			tags: [...new Set(["server-action", ...serverHoistStaticIo.tags ?? []])]
		}
	},
	{
		key: "react-doctor/server-no-mutable-module-state",
		id: "server-no-mutable-module-state",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...serverNoMutableModuleState,
			framework: "global",
			category: "Bugs",
			tags: [...new Set(["server-action", ...serverNoMutableModuleState.tags ?? []])]
		}
	},
	{
		key: "react-doctor/server-sequential-independent-await",
		id: "server-sequential-independent-await",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...serverSequentialIndependentAwait,
			framework: "global",
			category: "Bugs",
			tags: [...new Set(["server-action", ...serverSequentialIndependentAwait.tags ?? []])]
		}
	},
	{
		key: "react-doctor/state-in-constructor",
		id: "state-in-constructor",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...stateInConstructor,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/style-prop-object",
		id: "style-prop-object",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...stylePropObject,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tabindex-no-positive",
		id: "tabindex-no-positive",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...tabindexNoPositive,
			framework: "global",
			category: "Accessibility"
		}
	},
	{
		key: "react-doctor/tanstack-start-get-mutation",
		id: "tanstack-start-get-mutation",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartGetMutation,
			framework: "tanstack-start",
			category: "Security"
		}
	},
	{
		key: "react-doctor/tanstack-start-loader-parallel-fetch",
		id: "tanstack-start-loader-parallel-fetch",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartLoaderParallelFetch,
			framework: "tanstack-start",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/tanstack-start-missing-head-content",
		id: "tanstack-start-missing-head-content",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartMissingHeadContent,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-no-anchor-element",
		id: "tanstack-start-no-anchor-element",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartNoAnchorElement,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-no-direct-fetch-in-loader",
		id: "tanstack-start-no-direct-fetch-in-loader",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartNoDirectFetchInLoader,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-no-dynamic-server-fn-import",
		id: "tanstack-start-no-dynamic-server-fn-import",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartNoDynamicServerFnImport,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-no-navigate-in-render",
		id: "tanstack-start-no-navigate-in-render",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartNoNavigateInRender,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-no-secrets-in-loader",
		id: "tanstack-start-no-secrets-in-loader",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartNoSecretsInLoader,
			framework: "tanstack-start",
			category: "Security"
		}
	},
	{
		key: "react-doctor/tanstack-start-no-use-server-in-handler",
		id: "tanstack-start-no-use-server-in-handler",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartNoUseServerInHandler,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-no-useeffect-fetch",
		id: "tanstack-start-no-useeffect-fetch",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartNoUseEffectFetch,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-redirect-in-try-catch",
		id: "tanstack-start-redirect-in-try-catch",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartRedirectInTryCatch,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-route-property-order",
		id: "tanstack-start-route-property-order",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartRoutePropertyOrder,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-server-fn-method-order",
		id: "tanstack-start-server-fn-method-order",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartServerFnMethodOrder,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/tanstack-start-server-fn-validate-input",
		id: "tanstack-start-server-fn-validate-input",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...tanstackStartServerFnValidateInput,
			framework: "tanstack-start",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/use-lazy-motion",
		id: "use-lazy-motion",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...useLazyMotion,
			framework: "global",
			category: "Performance"
		}
	},
	{
		key: "react-doctor/void-dom-elements-no-children",
		id: "void-dom-elements-no-children",
		source: "react-doctor",
		originallyExternal: true,
		rule: {
			...voidDomElementsNoChildren,
			framework: "global",
			category: "Bugs"
		}
	},
	{
		key: "react-doctor/zod-v4-no-deprecated-error-apis",
		id: "zod-v4-no-deprecated-error-apis",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...zodV4NoDeprecatedErrorApis,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/zod-v4-no-deprecated-error-customization",
		id: "zod-v4-no-deprecated-error-customization",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...zodV4NoDeprecatedErrorCustomization,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/zod-v4-no-deprecated-schema-apis",
		id: "zod-v4-no-deprecated-schema-apis",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...zodV4NoDeprecatedSchemaApis,
			framework: "global",
			category: "Maintainability"
		}
	},
	{
		key: "react-doctor/zod-v4-prefer-top-level-string-formats",
		id: "zod-v4-prefer-top-level-string-formats",
		source: "react-doctor",
		originallyExternal: false,
		rule: {
			...zodV4PreferTopLevelStringFormats,
			framework: "global",
			category: "Maintainability"
		}
	}
];
const ruleRegistry = Object.fromEntries(reactDoctorRules.map((rule) => [rule.id, rule.rule]));
//#endregion
//#region src/plugin/utils/is-react-native-file.ts
const WEB_FILE_EXTENSION_PATTERN = /\.web\.[cm]?[jt]sx?$/;
const NATIVE_FILE_EXTENSION_PATTERN = /\.(?:ios|android|native)\.[cm]?[jt]sx?$/;
const isReactNativeFileActive = (context) => {
	const rawFilename = context.filename;
	if (!rawFilename) return true;
	const filename = normalizeFilename$1(rawFilename);
	if (NATIVE_FILE_EXTENSION_PATTERN.test(filename)) return true;
	if (WEB_FILE_EXTENSION_PATTERN.test(filename)) return false;
	const packagePlatform = classifyPackagePlatform(filename);
	if (packagePlatform === "web") return false;
	if (packagePlatform === "expo" || packagePlatform === "react-native") return true;
	const framework = getReactDoctorStringSetting(context.settings, "framework");
	if (framework === "react-native" || framework === "expo") return true;
	if (framework === "nextjs" || framework === "vite" || framework === "cra" || framework === "remix" || framework === "gatsby" || framework === "tanstack-start") return false;
	return true;
};
//#endregion
//#region src/plugin/utils/wrap-react-native-rule.ts
const EMPTY_VISITORS = {};
const wrapReactNativeRule = (rule) => {
	const innerCreate = rule.create.bind(rule);
	return {
		...rule,
		create: (context) => {
			if (!isReactNativeFileActive(context)) return EMPTY_VISITORS;
			return innerCreate(context);
		}
	};
};
//#endregion
//#region src/plugin/semantic/control-flow-graph.ts
let nextBlockId = 0;
const createBlock = (builder) => {
	const block = {
		id: nextBlockId++,
		nodes: [],
		successors: [],
		predecessors: []
	};
	builder.blocks.push(block);
	return block;
};
const addEdge = (from, to, kind) => {
	const edge = {
		from,
		to,
		kind
	};
	from.successors.push(edge);
	to.predecessors.push(edge);
};
const appendNode = (builder, block, node) => {
	block.nodes.push(node);
	if (!builder.nodeBlock.has(node)) builder.nodeBlock.set(node, block);
};
const mapDescendantsToBlock = (builder, node, block) => {
	builder.nodeBlock.set(node, block);
	if (isFunctionLike$2(node)) return;
	const record = node;
	for (const key of Object.keys(record)) {
		if (key === "parent") continue;
		const child = record[key];
		if (Array.isArray(child)) {
			for (const item of child) if (isAstNode(item)) mapDescendantsToBlock(builder, item, block);
		} else if (isAstNode(child)) mapDescendantsToBlock(builder, child, block);
	}
};
const hasInternalControlFlow = (node) => {
	switch (node.type) {
		case "IfStatement":
		case "WhileStatement":
		case "DoWhileStatement":
		case "ForStatement":
		case "ForInStatement":
		case "ForOfStatement":
		case "SwitchStatement":
		case "TryStatement":
		case "ReturnStatement":
		case "ThrowStatement":
		case "BreakStatement":
		case "ContinueStatement":
		case "BlockStatement":
		case "LabeledStatement": return true;
		default: return false;
	}
};
const findLabel = (builder, name) => {
	if (name === null) {
		if (builder.loopStack.length > 0) {
			const top = builder.loopStack[builder.loopStack.length - 1];
			return {
				merge: top.merge,
				header: top.header
			};
		}
		if (builder.switchStack.length > 0) return {
			merge: builder.switchStack[builder.switchStack.length - 1].merge,
			header: null
		};
		return null;
	}
	for (let i = builder.labelStack.length - 1; i >= 0; i--) {
		const entry = builder.labelStack[i];
		if (entry.label === name) return {
			merge: entry.merge,
			header: entry.header
		};
	}
	return null;
};
const buildStatements = (builder, statements, current) => {
	let cursor = current;
	for (const statement of statements) cursor = buildStatement(builder, statement, cursor);
	return cursor;
};
const buildStatement = (builder, statement, current) => {
	builder.nodeBlock.set(statement, current);
	if (!hasInternalControlFlow(statement)) {
		appendNode(builder, current, statement);
		mapDescendantsToBlock(builder, statement, current);
		return current;
	}
	if (isNodeOfType(statement, "BlockStatement")) return buildStatements(builder, statement.body, current);
	if (isNodeOfType(statement, "LabeledStatement")) {
		const merge = createBlock(builder);
		const labelEntry = {
			label: statement.label.name,
			merge,
			header: null
		};
		builder.labelStack.push(labelEntry);
		const body = statement.body;
		const end = buildStatement(builder, body, current);
		builder.labelStack.pop();
		addEdge(end, merge, "uncond");
		return merge;
	}
	if (isNodeOfType(statement, "ReturnStatement")) {
		if (statement.argument) mapDescendantsToBlock(builder, statement.argument, current);
		addEdge(current, builder.exit, "uncond");
		return createBlock(builder);
	}
	if (isNodeOfType(statement, "ThrowStatement")) {
		if (statement.argument) mapDescendantsToBlock(builder, statement.argument, current);
		const top = builder.tryStack[builder.tryStack.length - 1];
		if (top?.catch) addEdge(current, top.catch, "uncond");
		else if (top?.finally) addEdge(current, top.finally, "uncond");
		else addEdge(current, builder.exit, "throw");
		return createBlock(builder);
	}
	if (isNodeOfType(statement, "BreakStatement")) {
		const target = findLabel(builder, statement.label ? statement.label.name : null);
		if (target) addEdge(current, target.merge, "uncond");
		else addEdge(current, builder.exit, "uncond");
		return createBlock(builder);
	}
	if (isNodeOfType(statement, "ContinueStatement")) {
		const target = findLabel(builder, statement.label ? statement.label.name : null);
		if (target?.header) addEdge(current, target.header, "uncond");
		return createBlock(builder);
	}
	if (isNodeOfType(statement, "IfStatement")) {
		mapDescendantsToBlock(builder, statement.test, current);
		const thenBlock = createBlock(builder);
		const merge = createBlock(builder);
		addEdge(current, thenBlock, "cond");
		addEdge(buildStatement(builder, statement.consequent, thenBlock), merge, "uncond");
		if (statement.alternate) {
			const elseBlock = createBlock(builder);
			addEdge(current, elseBlock, "cond");
			addEdge(buildStatement(builder, statement.alternate, elseBlock), merge, "uncond");
		} else addEdge(current, merge, "cond");
		return merge;
	}
	if (isNodeOfType(statement, "WhileStatement") || isNodeOfType(statement, "DoWhileStatement")) {
		const isDoWhile = isNodeOfType(statement, "DoWhileStatement");
		mapDescendantsToBlock(builder, statement.test, current);
		const header = createBlock(builder);
		const body = createBlock(builder);
		const merge = createBlock(builder);
		if (isDoWhile) addEdge(current, body, "uncond");
		else {
			addEdge(current, header, "uncond");
			addEdge(header, body, "cond");
			addEdge(header, merge, "cond");
		}
		builder.loopStack.push({
			header,
			merge,
			label: null
		});
		const bodyEnd = buildStatement(builder, statement.body, body);
		builder.loopStack.pop();
		if (isDoWhile) {
			addEdge(bodyEnd, header, "uncond");
			addEdge(header, body, "cond");
			addEdge(header, merge, "cond");
		} else addEdge(bodyEnd, header, "uncond");
		return merge;
	}
	if (isNodeOfType(statement, "ForStatement")) {
		if (statement.init) mapDescendantsToBlock(builder, statement.init, current);
		if (statement.test) mapDescendantsToBlock(builder, statement.test, current);
		const header = createBlock(builder);
		const body = createBlock(builder);
		const merge = createBlock(builder);
		addEdge(current, header, "uncond");
		addEdge(header, body, "cond");
		addEdge(header, merge, "cond");
		builder.loopStack.push({
			header,
			merge,
			label: null
		});
		const bodyEnd = buildStatement(builder, statement.body, body);
		builder.loopStack.pop();
		if (statement.update) mapDescendantsToBlock(builder, statement.update, header);
		addEdge(bodyEnd, header, "uncond");
		return merge;
	}
	if (isNodeOfType(statement, "ForInStatement") || isNodeOfType(statement, "ForOfStatement")) {
		mapDescendantsToBlock(builder, statement.right, current);
		mapDescendantsToBlock(builder, statement.left, current);
		const header = createBlock(builder);
		const body = createBlock(builder);
		const merge = createBlock(builder);
		addEdge(current, header, "uncond");
		addEdge(header, body, "cond");
		addEdge(header, merge, "cond");
		builder.loopStack.push({
			header,
			merge,
			label: null
		});
		const bodyEnd = buildStatement(builder, statement.body, body);
		builder.loopStack.pop();
		addEdge(bodyEnd, header, "uncond");
		return merge;
	}
	if (isNodeOfType(statement, "SwitchStatement")) {
		mapDescendantsToBlock(builder, statement.discriminant, current);
		const merge = createBlock(builder);
		builder.switchStack.push({
			merge,
			label: null
		});
		let previousCaseEnd = null;
		let hasDefault = false;
		for (const switchCase of statement.cases) {
			const caseBlock = createBlock(builder);
			addEdge(current, caseBlock, "cond");
			if (previousCaseEnd) addEdge(previousCaseEnd, caseBlock, "uncond");
			previousCaseEnd = buildStatements(builder, switchCase.consequent, caseBlock);
			if (switchCase.test === null) hasDefault = true;
		}
		builder.switchStack.pop();
		if (previousCaseEnd) addEdge(previousCaseEnd, merge, "uncond");
		if (!hasDefault) addEdge(current, merge, "cond");
		return merge;
	}
	if (isNodeOfType(statement, "TryStatement")) {
		const tryBlock = createBlock(builder);
		const merge = createBlock(builder);
		let catchBlock = null;
		let finallyBlock = null;
		if (statement.handler) catchBlock = createBlock(builder);
		if (statement.finalizer) finallyBlock = createBlock(builder);
		addEdge(current, tryBlock, "uncond");
		builder.tryStack.push({
			catch: catchBlock,
			finally: finallyBlock
		});
		const tryEnd = buildStatements(builder, statement.block.body, tryBlock);
		builder.tryStack.pop();
		if (catchBlock) addEdge(tryBlock, catchBlock, "cond");
		if (statement.handler && catchBlock) {
			const handlerBody = statement.handler.body;
			const catchEnd = buildStatement(builder, handlerBody, catchBlock);
			if (finallyBlock) addEdge(catchEnd, finallyBlock, "uncond");
			else addEdge(catchEnd, merge, "uncond");
		}
		if (finallyBlock && statement.finalizer) {
			addEdge(tryEnd, finallyBlock, "uncond");
			addEdge(buildStatements(builder, statement.finalizer.body, finallyBlock), merge, "uncond");
		} else addEdge(tryEnd, merge, "uncond");
		return merge;
	}
	appendNode(builder, current, statement);
	mapDescendantsToBlock(builder, statement, current);
	return current;
};
const buildFunctionCfg = (functionNode, body) => {
	const builder = {
		blocks: [],
		entry: null,
		exit: null,
		nodeBlock: /* @__PURE__ */ new Map(),
		loopStack: [],
		switchStack: [],
		tryStack: [],
		labelStack: []
	};
	const entry = createBlock(builder);
	const exit = createBlock(builder);
	builder.entry = entry;
	builder.exit = exit;
	let bodyEnd;
	if (isNodeOfType(body, "BlockStatement")) bodyEnd = buildStatements(builder, body.body, entry);
	else {
		mapDescendantsToBlock(builder, body, entry);
		bodyEnd = entry;
	}
	addEdge(bodyEnd, exit, "uncond");
	const blockOf = (node) => builder.nodeBlock.get(node) ?? null;
	return {
		owner: functionNode,
		entry,
		exit,
		blocks: builder.blocks,
		blockOf
	};
};
const computeUnconditionalSet = (cfg) => {
	const reachableFromEntry = (excluded) => {
		const visited = /* @__PURE__ */ new Set();
		const queue = [];
		if (cfg.entry !== excluded) queue.push(cfg.entry);
		while (queue.length > 0) {
			const block = queue.shift();
			if (visited.has(block)) continue;
			visited.add(block);
			for (const edge of block.successors) {
				if (edge.kind === "throw") continue;
				if (edge.to === excluded) continue;
				queue.push(edge.to);
			}
		}
		return visited;
	};
	const reachableFromEntryFull = reachableFromEntry(null);
	const unconditional = /* @__PURE__ */ new Set();
	unconditional.add(cfg.entry);
	unconditional.add(cfg.exit);
	for (const block of cfg.blocks) {
		if (unconditional.has(block)) continue;
		if (!reachableFromEntryFull.has(block)) {
			unconditional.add(block);
			continue;
		}
		if (!reachableFromEntry(block).has(cfg.exit)) unconditional.add(block);
	}
	return unconditional;
};
const computeDominatesExit = (cfg) => {
	const reachableToExit = /* @__PURE__ */ new Set();
	const queue = [cfg.exit];
	while (queue.length > 0) {
		const block = queue.shift();
		if (reachableToExit.has(block)) continue;
		reachableToExit.add(block);
		for (const edge of block.predecessors) queue.push(edge.from);
	}
	const dominatesExit = /* @__PURE__ */ new Set();
	const visit = (block) => {
		if (block === cfg.exit) return true;
		if (dominatesExit.has(block)) return true;
		if (block.successors.length === 0) return false;
		dominatesExit.add(block);
		let allReach = true;
		for (const edge of block.successors) if (!visit(edge.to)) {
			allReach = false;
			break;
		}
		if (!allReach) dominatesExit.delete(block);
		return allReach;
	};
	for (const block of cfg.blocks) visit(block);
	return dominatesExit;
};
const analyzeControlFlow = (program) => {
	nextBlockId = 0;
	const functionCfgs = /* @__PURE__ */ new Map();
	const buildFor = (functionNode, body) => {
		const cfg = buildFunctionCfg(functionNode, body);
		functionCfgs.set(functionNode, {
			cfg,
			unconditionalSet: computeUnconditionalSet(cfg),
			dominatesExitSet: computeDominatesExit(cfg)
		});
	};
	if (isNodeOfType(program, "Program")) buildFor(program, {
		type: "BlockStatement",
		body: program.body
	});
	const visit = (node) => {
		if (isFunctionLike$2(node)) {
			const body = node.body;
			if (body) buildFor(node, body);
		}
		const record = node;
		for (const key of Object.keys(record)) {
			if (key === "parent") continue;
			const child = record[key];
			if (Array.isArray(child)) {
				for (const item of child) if (isAstNode(item)) visit(item);
			} else if (isAstNode(child)) visit(child);
		}
	};
	visit(program);
	const enclosingFunction = (node) => {
		let current = node;
		while (current) {
			if (isFunctionLike$2(current)) return current;
			if (isNodeOfType(current, "Program")) return current;
			current = current.parent ?? null;
		}
		return null;
	};
	const cfgFor = (functionLike) => {
		return functionCfgs.get(functionLike)?.cfg ?? null;
	};
	const isUnconditionalFromEntry = (node) => {
		const owner = enclosingFunction(node);
		if (!owner) return true;
		const entry = functionCfgs.get(owner);
		if (!entry) return true;
		const block = entry.cfg.blockOf(node);
		if (!block) return true;
		return entry.unconditionalSet.has(block);
	};
	const dominatesExit = (node) => {
		const owner = enclosingFunction(node);
		if (!owner) return true;
		const entry = functionCfgs.get(owner);
		if (!entry) return true;
		const block = entry.cfg.blockOf(node);
		if (!block) return true;
		return entry.dominatesExitSet.has(block);
	};
	return {
		cfgFor,
		enclosingFunction,
		isUnconditionalFromEntry,
		dominatesExit
	};
};
//#endregion
//#region src/plugin/utils/wrap-with-semantic-context.ts
const buildFallbackScopes = () => ({
	rootScope: {
		id: 0,
		kind: "module",
		node: {},
		parent: null,
		children: [],
		symbols: [],
		references: [],
		symbolsByName: /* @__PURE__ */ new Map()
	},
	scopeFor: () => ({ id: 0 }),
	ownScopeFor: () => null,
	symbolFor: () => null,
	referenceFor: () => null,
	isGlobalReference: () => false
});
const FALLBACK_CFG = {
	cfgFor: () => null,
	enclosingFunction: () => null,
	isUnconditionalFromEntry: () => false,
	dominatesExit: () => false
};
const wrapWithSemanticContext = (rule) => ({
	...rule,
	create: (baseContext) => {
		let programRoot = null;
		let cachedScopes = null;
		let cachedCfg = null;
		const getScopes = () => {
			if (cachedScopes) return cachedScopes;
			if (!programRoot) return buildFallbackScopes();
			cachedScopes = analyzeScopes(programRoot);
			return cachedScopes;
		};
		const getCfg = () => {
			if (cachedCfg) return cachedCfg;
			if (!programRoot) return FALLBACK_CFG;
			cachedCfg = analyzeControlFlow(programRoot);
			return cachedCfg;
		};
		const enrichedContext = {
			report: baseContext.report,
			get filename() {
				return baseContext.filename ?? baseContext.getFilename?.();
			},
			settings: baseContext.settings,
			get scopes() {
				return getScopes();
			},
			get cfg() {
				return getCfg();
			}
		};
		const captureRootIfNeeded = (node) => {
			if (programRoot) return;
			programRoot = findProgramRoot(node);
		};
		const visitors = rule.create(enrichedContext);
		const wrappedVisitors = {};
		for (const [nodeType, handler] of Object.entries(visitors)) {
			if (typeof handler !== "function") continue;
			wrappedVisitors[nodeType] = ((node) => {
				captureRootIfNeeded(node);
				handler(node);
			});
		}
		if (!visitors.Program) wrappedVisitors.Program = (node) => {
			captureRootIfNeeded(node);
		};
		return wrappedVisitors;
	}
});
//#endregion
//#region src/plugin/react-doctor-plugin.ts
const applyFrameworkRuleWrappers = (registry) => {
	const wrapped = {};
	for (const [ruleId, rule] of Object.entries(registry)) wrapped[ruleId] = wrapWithSemanticContext(rule.framework === "react-native" ? wrapReactNativeRule(rule) : rule);
	return wrapped;
};
const plugin = {
	meta: { name: "react-doctor" },
	rules: applyFrameworkRuleWrappers(ruleRegistry)
};
//#endregion
//#region src/rules.ts
const toRuleMap = (entries) => Object.fromEntries(entries.map((entry) => [entry.key, entry.severity]));
const toKeyedSeverity = (entries) => entries.map((entry) => ({
	key: entry.key,
	severity: entry.rule.severity
}));
const isRecommendedByDefault = (entry) => entry.rule.defaultEnabled !== false;
const collectReactDoctorRulesByFramework = (frameworkName) => reactDoctorRules.filter((entry) => entry.rule.framework === frameworkName && isRecommendedByDefault(entry));
const collectExternalRulesBySource = (source) => EXTERNAL_RULES.filter((rule) => rule.source === source);
const collectFrameworkSpecificRuleKeys = () => {
	const collected = /* @__PURE__ */ new Set();
	for (const entry of reactDoctorRules) if (entry.rule.framework !== "global") collected.add(entry.key);
	return collected;
};
const REACT_DOCTOR_RULES = reactDoctorRules;
const EXTERNAL_RULES = [
	{
		key: "react-hooks-js/set-state-in-render",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/immutability",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/refs",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/purity",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/hooks",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/set-state-in-effect",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/globals",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/error-boundaries",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/preserve-manual-memoization",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/unsupported-syntax",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/component-hook-factories",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/static-components",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/use-memo",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/void-use-memo",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/incompatible-library",
		source: "react-compiler",
		severity: "error"
	},
	{
		key: "react-hooks-js/todo",
		source: "react-compiler",
		severity: "error"
	}
];
const RULES = [...REACT_DOCTOR_RULES, ...EXTERNAL_RULES];
const RECOMMENDED_RULES = toRuleMap(toKeyedSeverity(collectReactDoctorRulesByFramework("global")));
const NEXTJS_RULES = toRuleMap(toKeyedSeverity(collectReactDoctorRulesByFramework("nextjs")));
const REACT_NATIVE_RULES = toRuleMap(toKeyedSeverity(collectReactDoctorRulesByFramework("react-native")));
const TANSTACK_START_RULES = toRuleMap(toKeyedSeverity(collectReactDoctorRulesByFramework("tanstack-start")));
const TANSTACK_QUERY_RULES = toRuleMap(toKeyedSeverity(collectReactDoctorRulesByFramework("tanstack-query")));
const PREACT_RULES = toRuleMap(toKeyedSeverity(collectReactDoctorRulesByFramework("preact")));
const ALL_REACT_DOCTOR_RULES = toRuleMap(toKeyedSeverity(REACT_DOCTOR_RULES));
const ALL_REACT_DOCTOR_RULE_KEYS = new Set(REACT_DOCTOR_RULES.map((rule) => rule.key));
const FRAMEWORK_SPECIFIC_RULE_KEYS = collectFrameworkSpecificRuleKeys();
const REACT_COMPILER_RULES = toRuleMap(collectExternalRulesBySource("react-compiler"));
//#endregion
//#region src/index.ts
var src_default = plugin;
//#endregion
export { ALL_REACT_DOCTOR_RULES, ALL_REACT_DOCTOR_RULE_KEYS, EXTERNAL_RULES, FRAMEWORK_SPECIFIC_RULE_KEYS, MOTION_LIBRARY_PACKAGES, NEXTJS_RULES, PREACT_RULES, REACT_COMPILER_RULES, REACT_DOCTOR_RULES, REACT_NATIVE_DEPENDENCY_NAMES, REACT_NATIVE_DEPENDENCY_PREFIXES, REACT_NATIVE_RULES, RECOMMENDED_RULES, RULES, TANSTACK_QUERY_RULES, TANSTACK_START_RULES, src_default as default, isReactNativeDependencyName };

//# sourceMappingURL=index.js.map