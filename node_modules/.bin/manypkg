#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/danielhowells/Sites/lint/node_modules/.pnpm/@manypkg+cli@0.25.1/node_modules/@manypkg/cli/node_modules:/Users/danielhowells/Sites/lint/node_modules/.pnpm/@manypkg+cli@0.25.1/node_modules:/Users/danielhowells/Sites/lint/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/Users/danielhowells/Sites/lint/node_modules/.pnpm/@manypkg+cli@0.25.1/node_modules/@manypkg/cli/node_modules:/Users/danielhowells/Sites/lint/node_modules/.pnpm/@manypkg+cli@0.25.1/node_modules:/Users/danielhowells/Sites/lint/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@manypkg/cli/bin.js" "$@"
else
  exec node  "$basedir/../@manypkg/cli/bin.js" "$@"
fi
# cmd-shim-target=/Users/danielhowells/Sites/lint/node_modules/@manypkg/cli/bin.js
